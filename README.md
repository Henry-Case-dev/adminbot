# AdminBot — ваш карманный цифровой абьюзер с элементами искусственного интеллекта

*Решение для тех, кто хочет токсичности в чате, но ленится писать сам. Теперь с памятью слона и терпением снайпера.*

**Версия:** v2.40.0 | **Тестов:** 2354 | **Эпиков:** 56 (T-001…T-441)

---

## 🚀 Возможности

### Слава: возвращение блудного долбоёба (F1)
Когда Слава заходит в чат (покинул → вернулся) — бот пишет **«ДОЛБОЕВ ВЕРНУЛСЯ»**. Dead page при входе по умолчанию не шлётся (`DEAD_PAGE_POST_ON_JOIN=False`, Epic 22) — не надо перебивать классику. Каждый раз одно и то же, и каждый раз смешно. Славе — нет.

### Слава: Dead Page (F2 v2)
- Кто-то делает репост из канала **@d_pages** → бот выбирает случайный пост из своего приватного канала и пересылает его в чат. Технология «слышу звон — несу что попало» доведена до совершенства.
- Если приватный канал упал или бот оттуда выгнали → fallback: случайная картинка + текст из папки `media/dead_page/`. Качество контента не гарантируется, но volume присутствует.
- При входе Славы → «ДОЛБОЕВ ВЕРНУЛСЯ». Dead page при входе по умолчанию выключен (`DEAD_PAGE_POST_ON_JOIN=False`, Epic 22) — Слава и так знает, зачем вернулся.

**Исправлено в v2.11.0 (Epic 14):**
- **Альбомы (media groups)**: если в приватном канале лежит пост из нескольких фото (альбом), бот теперь пересылает **весь альбом целиком**, а не одно первое фото. Раньше из трёх фото с подписью пересылалось только одно — теперь все три.
- **Как работает**: бот отслеживает `media_group_id` новых постов в релейном канале и сохраняет в БД (путь 1 — точный, через `forward_messages`). Для старых постов — эвристический поиск соседей по дате (путь 2 — запасной).
- **Дедупликация триггера**: когда кто-то репостит альбом из @d_pages, Telegram шлёт N событий (по одному на фото). Бот теперь обрабатывает только первое — не спамит кулдаун лишними запросами.

**Обновлено в v2.20.0 (Epic 22):**
- **Анти-повтор PostPicker (D54)**: бот запоминает последний отправленный пост (ключ БД `dead_page_last_sent:{chat_id}`) и в следующий раз выбирает ДРУГОЙ. Раньше при sparse-канале sequential scan упорно находил «почти всегда id 3» — теперь Слава видит разнообразие. Если в канале только один пост — осознанный повтор (лучше повториться, чем удариться в ALL RANGES EXHAUSTED).
- **Триггер только для репостов Славы (D53)**: dead page реагирует на репосты из @d_pages исключительно от Славы. Репост от кого-то другого проходит мимо — Слава был и остаётся главным героем.

**Обновлено в v2.37.0 (Epic 52):**
- **Месть за удалённый репост (D214)**: если Славик удаляет свой репост мёртвой страницы — бот узнаёт об этом гениальным пассивным детектом. Telegram не шлёт боту delete-updates (а `getMessage` вообще выпилили из Bot API 8.3), поэтому бот терпеливо ждёт, пока кто-то ответит или отцитирует удалённое сообщение — в update оно прилетит как `InaccessibleMessage` (`date==0`, без `from_user`). Телепатия по переписке, иначе никак.
- **Что дальше**: бот сверяется с таблицей `dead_page_repost_map` (память «чей репост → какое сообщение бота») и удаляет свою dead page. Нет прав на удаление (403) — шлёт ОДНУ токсичную фразу из пула `DEAD_PAGE_DELETE_PHRASES` про то, что Славик снёс репост: «снёс репост мёртвой страницы? стыдно стало?», «мёртвые страницы так просто не умирают» и ещё три в том же духе. Ровно одно срабатывание на пару (чат, репост) — атомарный claim в БД, двойной реплай не даст двойной порции.
- **Новый роутер** `handlers/dead_page_delete.py` на позиции 4a; маппинг пишется при отправке dead page (аддитивная таблица, миграций БД не потребовалось).

### Слава: чат-реакции (F3/F4)
Трёхуровневая система эскалации абьюза:
- Просто написал → **«пошёл нахуй»** (catch-all, потому что Слава всегда заслуживает)
- Упомянул «куча»/«кучи»/«кучу» → **«ДАЛБАЕБ»** (F4 — прицельный выстрел по грамматике)
- Каждое 5-е сообщение → **GIF** со славным членом (F3 — визуальное подкрепление)

**Эпик 52 (v2.37.0): одно сообщение — одно действие.** Концерт по заявкам отменён: на сообщение Славы срабатывает ровно один сценарий в жёстком порядке — dead page (репост из @d_pages) > гифка (реально отправленная `slavic_chlen.mp4` блокирует всё остальное через data-флаг `slavik_gif_sent`) > рандом-медиа (ЗАМЕЩАЕТ «пошёл нахуй», а не скапливается с ним) > mimic > классический «пошёл нахуй». На вход в чат — только «ДОЛБОЕВ ВЕРНУЛСЯ», без гифок и медиа-сюрпризов. Гифка перестала стрелять по service-сообщениям (вход/выход) — счётчик такие сообщения не засчитывает. Слава экономит токсичность порциями, как витамины.

### Слава: военный оповещатель (F5v2 — Epic 10)
Когда Слава пишет сообщение с военными ключевыми словами (дрон, БПЛА, ракета, опасность, тревога, убежище и т.д.) — бот отвечает случайной фразой из пула: «потрясись», «повизжи», «прячься под шконку быстрее», «закрой ушки и считай до десяти», «поплачь». Психологическая поддержка наоборот.

**Новое в v2 (Epic 10):**
- **Caption support**: теперь бот видит угрозу даже в подписях к медиа. Раньше был слеп как крот — теперь зряч как орёл.
- **Channel reposts**: если **любой пользователь** репостит из каналов «ЧП Пермь» или «Радар БПЛА» — бот тоже орёт. Коллективная паника.
- **Расширенный словарь**: 90+ словоформ. Бот знает о войне больше, чем некоторые военкоры.
- **Настраиваемый пул фраз**: `WAR_REPLIES` в `.env`. Хотите «всё пропало» вместо «потрясись»? Да пожалуйста.
- **Детальное логгирование**: каждый чих пишется в Better Stack. Можно потом пересматривать как сериал.

**Исправлено в v2.9.0 (Epic 12):**
- **Forwarded messages fix**: бот теперь перехватывает военные слова не только в сообщениях, которые Слава пишет сам, но и в его репостах (forwarded messages). Раньше Слава мог репостнуть «БПЛА летит» и бот молчал как партизан. Теперь не молчит.

### Слава: фото-сюрприз (F6 — Epic 12, обновлено в v2.18.0) 🆕
Каждый N-й ответ «пошёл нахуй» (по умолчанию N=10) бот вместо текста отправляет **случайное медиа** из директории `media/slavik/slavik_random/`. Счётчик сбрасывается после отправки — и дальше по кругу.

**Поддерживаемые типы медиа (v2.18.0):**
- 📷 **Photo**: `.jpg`, `.jpeg`, `.png`, `.webp`, `.bmp`
- 🎬 **Video**: `.mp4`, `.mov`, `.webm`
- ✨ **Animation (GIF)**: видеофайлы с `gif` в имени (word-boundary детект — `.gif.`, `_gif`, `gif` в начале)
- 🎵 **Audio**: `.mp3` → `send_audio`
- 🎤 **Voice**: `.ogg` → `send_voice`
- 📄 **Document (fallback)**: всё остальное отправляется как документ. `.pdf`? `.zip`? `.txt`? Да без разницы, Слава примет любой подарок.

**Настройка:**
- `SLAVIC_PHOTO_INTERVAL=10` — каждые сколько ответов слать медиа (в `.env`)
- `SLAVIC_RANDOM_DIR=media/slavik/slavik_random` — директория со случайным медиа всех типов
- `SLAVIC_PHOTO_PATH` — **deprecated**, используется как запасной путь если `SLAVIC_RANDOM_DIR` пуст
- `SLAVIC_PHOTO_INTERVAL=0` — отключает фичу (только текст)
- Счётчик хранится в БД (переживает рестарт)
- **Не зависит от F3** (GIF-счётчик) — разные таблицы, разные интервалы, работают параллельно

### Alan_Z: автоответчик (F6)
Каждые N сообщений Алана бот проявляет **крайний интерес** к темам: тренировки, лонгковид, нейросети, жим дьявола. С v2.38.0 (Epic 53) пул дорос до **52 фраз**: пять тем Алана (NixOS/Линукс, продажа SSD, витамины Life Extension, 5-секундные прогулки с гантелями, уличный тренажёр + реванш за колени) расширены до пяти фраз каждая — «флейк посчитал себя зависимостью сам от себя», «продаю ssd по цене трёх таких же — инфляция, сам виноват» и прочие перлы. А присказка «разминался сегодня?» заменена пулом из 5 издевательско-токсичных вопросов («а ты вообще разминался? или как обычно — с дивана сразу к штанге?») — бот больше не хвастается своим сплитом, а допрашивает Алана о его. Случайная фраза из пула, маскируется под живого собеседника. Алан до сих пор не понял. А на проде реплики по-прежнему выключены гейтом `ALAN_REPLIES_ENABLED=False` — новые 52 перла написаны, отрепетированы и пока никому не показаны (F7v2 greeting при этом живёт безусловно).

### Alan_Z: приветственное видео (F7)
Когда Alan заходит в чат — бот отправляет случайное видео из `media/leha_greeting/` с подписью `@Alan_Z`. Срабатывает через `ChatMemberUpdated` (основной детект) и `new_chat_members` (fallback для параноиков). Кулдаун 10 секунд — чтобы не заддосить чат радостью.

### Common Service: отбой + Danger Detection (F9 — Epic 15) 🆕
**Общий сервис для ВСЕХ пользователей чата.** Четыре sub-service'а на одном роутере (позиция 4c):

**1. Отбой (F9):** Когда кто-то пишет слово «отбой» — бот отправляет случайное медиа из `media/common/otboy/` с нативным цитированием именно этого слова через Telegram quote API.

**2. Danger Detection (Epic 15):** Когда кто-то пишет danger-слово (бпла, ракетная, опасность, сирена, дрон, взрыв, хлопок и др. — 118 словоформ) или danger-фразу-связку («укрыться в убежище», «уйти в бомбоубежище», «беспилотная атака», «ракетная атака», «атака дронов» и др. — 17 фраз, итого 135 паттернов) — бот отправляет случайное медиа из `media/common/danger/`. Одиночные «атака»/«угроза»/«прилет» больше не в словаре — только в связках, как и положено серьёзному джентльменскому набору.

**3. Selfdev (Epic 30, v2.28.0):** Написал «саморазвитие», «личностный рост», «прокачка» — бот отправляет мотивирующее медиа из `media/common/selfdev/` с reply+quote. Репосты игнорируются: саморазвитие — дело личное, чужие амбиции бота не интересуют. «Развиваться» в словаре НЕТ — иначе бот бы орал про личностный рост на каждое «события развиваются».

**4. Work (Epic 30, v2.28.0):** Написал «устал», «заебался», «запарился», «нет сил» — бот сочувствующе шлёт медиа из `media/common/work/`. Сочувствие в данном случае — случайный файл с reply+quote. Репосты тоже мимо: чужая усталость — чужая проблема. С v2.37.0 (Epic 52) сочувствие регулируется рубильником `COMMON_WORK_MEDIA_ENABLED` (на проде `False`) — усталые страдают молча, триггеры при этом остаются зарегистрированными, как спящий полицейский.

**5. Goodmorning (Epic 30, v2.28.0):** Раз в сутки в настроенное время бот будит чат случайным медиа из `media/common/goodmorning/` + случайным caption из пула («❗️❗️❗️ ПАДЪЕМ НИГЕРЫ…» и ещё 5 вариантов в том же тоне). Plain-send без reply: цитировать спящих нечем. Пока `GOODMORNING_TARGET_CHAT_IDS` пуст — будильник молча выключен, никто не узнает о вашем провале утренней мотивации.

**Где ловятся слова:**
- Обычные текстовые сообщения
- Подписи к фото/видео (caption)

**Как работает:**
- Regex с Cyrillic word boundary (`(?<![а-яё])...(?![а-яё])`) — «отбойный» не матчится, только отдельное слово
- Фразы-связки (17 шт.) проверяются **перед** одиночными словами: границы только по краям фразы, «в бункер» не сработает внутри «в бункере», зато «ракетная атака» сработает целиком
- Регистронезависимый: «Отбой», «ОТБОЙ», «БПЛА», «бпла» — все сработают
- Quote через нативный `ReplyParameters` — в интерфейсе Telegram подсвечивается именно найденное слово
- Ответ приходит reply-to на исходное сообщение
- Media directory-based: auto-detect photo (jpg/png/webp), video (mp4/mov/webm) или animation (mp4/mov/webm с "gif" в имени). **Обновлено в v2.16.0:** детект GIF использует word-boundary матчинг (`_gif`, `gif_`, `.gif.`) — надёжно определяет анимацию даже при любом расположении слова "gif" в имени файла.
- **Dual cooldown (v2.16.0, обобщено v2.28.0):** двухуровневая система защиты от спама:
  - `COMMON_COOLDOWN` (глобальный) + пер-сабдирные `DANGER_COOLDOWN` / `SELFDEV_COOLDOWN` / `WORK_COOLDOWN`. Danger/selfdev/work срабатывают только если **оба** кулдауна истекли; otboy проверяет только общий
  - Сабдиры независимы: selfdev не блокирует work и наоборот. Спам «саморазвитием» не отменяет сочувствие усталым
- **Относительные ко времени значения**: новые кулдауны принимают human-readable форматы: `1h`, `30s`, `5m`, `1d`, или просто число секунд. `0` = отключено.
- Поддержка обратной совместимости: старые plain-integer значения из `.env` работают без изменений.

**Настройка:**

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `COMMON_COOLDOWN` | `0` | Общий кулдаун между отправками в одном чате. `0` = без кулдауна |
| `DANGER_COOLDOWN` | `60s` | Доп. кулдаун только для danger. `0` = без доп. ограничения |
| `SELFDEV_COOLDOWN` | `5m` | Доп. кулдаун только для selfdev (Epic 30) |
| `WORK_COOLDOWN` | `5m` | Доп. кулдаун только для work (Epic 30) |
| `COMMON_MEDIA_BASE` | `media/common` | Базовая директория с подпапками otboy/, danger/, selfdev/, work/ |
| `DANGER_WORDS` | *(118 слов + 17 фраз)* | Danger-слова через запятую. Пусто = встроенные дефолты (118 словоформ + 17 фраз-связок = 135 паттернов; фразы не переопределяются) |
| `COMMON_MEDIA_ENABLED` | `True` | Глобальный рубильник ВСЕХ common-медиа (otboy/danger/selfdev/work) в единой точке `CommonRelay.send_common`. `False` = бот разом теряет и сочувствие, и панику (Epic 52) |
| `COMMON_WORK_MEDIA_ENABLED` | `True` | Точечный рубильник work-сабдира (`media/common/work`). `False` = усталость остаётся без ответа. На проде `False` (Epic 52) |

**Масштабируемость:** сервис изолирован в отдельных модулях (`filters/otboy_word.py` + `filters/danger_word.py` + `filters/selfdev_word.py` + `filters/work_word.py` → `services/common_relay.py` → `handlers/common.py`). Не зависит от других фич, не использует БД. Добавление новых sub-service'ов — новый filter + handler на том же common_router, новый subdir в media/common/. Selfdev/work это уже доказали — добавились за один эпик, никого не спросив.

**Обновлено в v2.12.1 (Epic 16):** DangerWordFilter расширен с 22 до **187 словоформ** (полная синхронизация с `WarWordFilter.WAR_WORDS` из `filters/war_word.py`). Добавлены все падежные формы, а также новые категории: обстрел, сбитие, падение, эвакуация, отбой.

**Исправлено в v2.14.0 (Epic 18):**
- **Критический фикс `.env.example`**: переменная `DANGER_WORDS` в шаблоне была жёстко прописана как `бпла,ракетная,опасность` (всего 3 слова вместо 135+). Это приводило к тому, что бот на сервере игнорировал 132+ словоформ из `word_lists.py`. Теперь `.env.example` содержит пустое значение — бот автоматически использует полный словарь из кода.
- **Фикс propagation**: все хендлеры в `common_router` и `war_alert_router` возвращают `UNHANDLED` — события больше не блокируются между роутерами.

### Goodmorning: утренний будильник (Epic 30, v2.28.0) 🆕
Раз в сутки бот отправляет в целевой чат случайный файл из `media/common/goodmorning/` + случайный caption из пула. Пул капций: 3 канона пользователя («❗️❗️❗️ПАДЪЕМ НИГЕРЫ…») + 3 новых в том же стиле. Расширение пула — новая строка в `services/goodmorning_captions.py`, согласования с отделом кадров не требуется.

**Правила приличия рассылки:**
- photo/video/animation отправляются С caption; `goodmorning_05_gif.MP4` → animation по gif-маркеру (регистр расширения боту по барабану)
- audio/voice пропускаются с WARNING — капцию к музыке не прицепить, а молча будить чат нельзя
- пустая папка → тихий skip с WARNING: утро отменяется по техническим причинам
- `GOODMORNING_TARGET_CHAT_IDS` пуст → планировщик не стартует, WARNING в лог, все спят дальше

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `GOODMORNING_TIME` | `07:00` | Время рассылки (HH:MM). Кривой формат → WARNING + fallback 07:00 |
| `GOODMORNING_TZ` | `Asia/Yekaterinburg` | Часовой пояс расписания |
| `GOODMORNING_TARGET_CHAT_IDS` | *(пусто)* | ID чатов через запятую. **Пусто = рассылка выключена** |
| `GOODMORNING_MEDIA_DIR` | `media/common/goodmorning` | Папка с утренним медиа |

### Mimic: передразнивание (F11 — Epic 18) 🆕 — обновлён v2.15.0
**Шуточная механика трансформации текста «под шепелявость».** Чистая алгоритмическая (не LLM) замена букв. Алгоритм полностью заменён на новый многослойный:

| Слой | Правило | Пример |
|------|---------|--------|
| WORD_MAP | Целые слова: что→фе, почему→патему, вообще→вафе, хочу→хатю... | «что случилось?» → «фе случилось?» |
| SUBSTR_MAP | Окончания/подстроки: шься→фся, ться→тца, сл→фл... | «учишься» → «учифся» |
| Согласные | р→л, ш/щ→ф, ж→з, ч→т | рука→люка, шум→фум |
| Гласные (после согл.) | у→ю | тут→тют |
| Регистр | Сохраняется | Рыба→Лыба, ЩУКА→ФЮКА |

**Две независимые интеграции:**

**1. Common сервис (жертва):** Реагирует на сообщения от `MIMIC_VICTIM_USER_ID`. Условия: > N слов (по умолчанию 5) и таймаут (по умолчанию 60 сек).

**2. Slavik сервис (мьютекс):** Встроен в `slavik_catchall_handler` с приоритетом (Epic 52): dead page > service-сообщение > GIF > фото > mimic > «пошёл нахуй». Если сообщение Славика > 5 слов и прошёл таймаут — mimic. Иначе — классический ответ. Дублирование исключено через if/elif/else: одно сообщение — ровно одно действие.

**Настройка:**
| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `MIMIC_VICTIM_USER_IDS` | `138811255` | ID жертв через запятую. Пусто или "0" = отключено |
| `MIMIC_MIN_WORDS` | `5` | Мин. число слов для mimic |
| `MIMIC_COOLDOWN` | `1h` | Кулдаун между mimic (common) |
| `MIMIC_FORWARDS_ENABLED` | `False` | Передразнивать репосты? `False` = только собственные сообщения (репосты игнорируются), `True` = старый беспредел |
| `SLAVIK_MIMIC_MIN_WORDS` | `5` | Мин. слов для mimic Славика. -1 = отключено |
| `SLAVIK_MIMIC_COOLDOWN` | `60s` | Кулдаун между mimic (Славик) |

**Admin-команда:** `/mimic [текст]` — тест трансформации (DM: любой, группа: только админ).

**Эпик 22 (D52):** mimic больше не передразнивает **репосты** — только собственные сообщения жертвы (`MIMIC_FORWARDS_ENABLED=False`). Раньше бот шепелявил чужие цитаты, теперь цитирует только первоисточники.

### Olya: автоответ на видео (F12 — Epic 19) 🆕
**Реакция на видео и фото от @ole4444444ka.** Когда Оля отправляет видео/фото в чат — бот отвечает случайным медиа из директории `media/olya/cringe/` простым сообщением (без цитирования). Потому что Оля заслуживает отдельного внимания.

**Гибкая настройка триггера:**
- **Caption detection:** если под видео есть текст «Спасибо, что пользуетесь - @SaveAsBot'ом» — триггер срабатывает (настраивается). **Epic 32:** сравнение нормализованное — боту теперь плевать на длину тире («-»/«–»/«—»), вид апострофа («'»/«’»/«ʼ»), регистр и лишние пробелы. Точность прежде всего, но без буквоедства.
- **Mention trigger:** упоминание `@SaveAsBot` в подписи (даже без полного текста) тоже срабатывает — управляется `OLYA_CAPTION_MENTION_ENABLED` (дефолт `True`; на проде можно отключить, оставив только точный текст).
- **Repost detection:** если видео — репост **от юзера/бота SaveAsBot** (`OLYA_SAVEASBOT_USER_IDS`, дефолт 523131145) или из канала SaveAsBot (`OLYA_SAVEASBOT_CHANNEL_IDS`) — триггер срабатывает. Раньше бот искал ID бота в канальном ключе — как искать рыбу в холодильнике. Теперь ID лежит там, где должен.
- **Always send:** если `OLYA_ALWAYS_SEND=True` — бот отправляет медиа на любое видео/фото от Оли, независимо от caption и repost условий. **По умолчанию `False` (Epic 22):** бот реагирует только на SaveAsBot-видео (caption-признак ИЛИ репост из SaveAsBot) — точность прежде всего.
- **Media type filter:** `OLYA_MEDIA_TYPE` может быть `"video"` (только видео), `"photo"` (только фото) или `"any"` (любое медиа)

**Настройка:**
| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `OLYA_ENABLED` | `True` | Включает/отключает сервис |
| `OLYA_USER_ID` | `834424825` | ID пользователя (@ole4444444ka) |
| `OLYA_COOLDOWN` | `60s` | Кулдаун между отправками в одном чате |
| `OLYA_MEDIA_BASE` | `media/olya/cringe` | Директория с медиафайлами |
| `OLYA_SAVEASBOT_USER_IDS` | `523131145` | ID юзера/бота SaveAsBot через запятую (репосты от него) |
| `OLYA_SAVEASBOT_CHANNEL_IDS` | `()` — пусто | ID каналов SaveAsBot через запятую (пусто = каналы не матчим) |
| `OLYA_CAPTION_ENABLED` | `True` | Проверять caption на текст SaveAsBot |
| `OLYA_CAPTION_TEXT` | `Спасибо, что пользуетесь - @SaveAsBot'ом` | Текст для поиска в caption (сравнение нормализованное) |
| `OLYA_CAPTION_MENTION_ENABLED` | `True` | Доп. триггер: упоминание `@SaveAsBot` в caption |
| `OLYA_REPOST_ENABLED` | `True` | Проверять репосты от юзера/из каналов SaveAsBot |
| `OLYA_MEDIA_TYPE` | `video` | Тип медиа: `"video"`, `"photo"`, или `"any"` |
| `OLYA_ALWAYS_SEND` | `False` | Всегда отправлять от этого пользователя. `True` = старое поведение (на любое видео Оли) |

**Ключевые отличия от CommonRelay:**
- Медиа отправляется **простым сообщением** без reply/quote — не цитирует исходное сообщение
- Один пользователь вместо всех
- Плоская директория вместо поддиректорий (otboy/, danger/)
- Один кулдаун вместо двухуровневого
- Медиа-триггер вместо текстового (видео/фото, а не слова)

### SmartModule: Summary — бот, который подслушивает (Epic 24) 🧠 🆕
**Первый сервис с настоящим ИИ.** Бот подслушивает **весь** чат, а каждые 6 часов (00:00/06:00/12:00/18:00 по Екатеринбургу) выдаёт токсично-ироничное саммари через LLM: кто с кем спорил, кто какую хуйню сморозил и кто в итоге оказался «самым главным шизом». Или по команде `/summary` — если не спамить, конечно (троттлинг 60 секунд на (чат, юзер); повтор внутри окна — фраза-отборка с реальным оставшимся временем, а не тишина).

**Промпт v2 (Epic 27) — бот-абьюзер пошёл учиться печатать:** теперь он имитирует «ленивую печать» живого двушника — регистр в начале предложений скачет случайным образом, а не сидит на маленькой букве, как отличник младших классов. Маркдаун, списки и эмодзи — под строжайшим запретом: за несанкционированную звёздочку в тексте полагается расстрел типографики. А длинные тире и кавычки-ёлочки с Epic 29 промпт уже не воспитывает (v4): их вычищает сам бот на выходе — расстрельная бригада переехала в бэкенд и не промахивается. Сплошной текст с абзацами — потому что читать его стены без абзацев согласны только мазохисты.

**Трёхуровневая память — как у слона с амнезией по расписанию:**
- **L1 — окно генерации (6 часов):** свежая история, которую бот реально читает. По умолчанию последние 500 сообщений, но срезается до 120 000 символов, чтобы LLM не подавился.
- **L2 — сырая память (30 дней):** полная переписка для RAG. Когда бот делает саммари, он вытаскивает из прошлого «точные цитаты» по ключевым словам окна через FTS5 — да, он цитирует то, что вы сказали 3 недели назад. Готовьтесь.
- **L3 — векторный архив (90 дней):** всё старше 30 дней сжимается LLM'ом в факты по темам и ищется по векторам (sqlite-vec KNN). Если векторы недоступны — факты всё равно живут в тексте и ищутся через FTS5.

**Как работает деградация (бот не умеет падать):**
- sqlite-vec не загрузился на Windows/Linux → вся векторная ветка тихо превращается в FTS5-поиск.
- Эмбеддинги упали/таймаут → то же самое, факты ищутся текстом.
- LLM упал → в чат уходит честное «не смог сделать саммари потому что упал апи», в логи — полный стектрейс.
- База данных подавилась → «база данных подавилась». Маленькими буквами, без эмодзи, без техдеталей. UX-дизайн уровня «прости, я в пролёте».

**Настройка:**

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `LLM_API_KEY` | *(пусто)* | Ключ от OpenAI-совместимого API (apinet.cloud). Без него — LLMAuthError и UX-фраза, бот жив |
| `LLM_BASE_URL` | `https://apinet.cloud/v1` | Базовый URL провайдера. Хочешь другой хаб — вперёд, клиент провайдер-агностик |
| `LLM_MODEL_NAME` | `deepseek-v4-flash` | Модель генерации саммари |
| `EMBEDDING_MODEL_NAME` | `gemini-embedding-001` | Модель эмбеддингов для L3 |
| `LLM_TIMEOUT` | `60.0` | Таймаут одного запроса (сек) |
| `LLM_MAX_RETRIES` | `2` | Повторы на 429/5xx/таймаут с экспоненциальным backoff |
| `EMBEDDING_DIM` | `768` | Размерность векторов (gemini-embedding-001 = 768) |
| `SUMMARY_ENABLED` | `True` | Главный рубильник: `False` = роутеры не регистрируются, бот ведёт себя как раньше |
| `SUMMARY_WINDOW_HOURS` | `6.0` | Окно L1 (часы) |
| `FULL_MEMORY_RETENTION_DAYS` | `30` | Сколько дней хранить сырьё (L2) |
| `ARCHIVE_MEMORY_RETENTION_DAYS` | `90` | Срок жизни архивных фактов (L3) |
| `MAX_SUMMARY_PARTS` | `1` | Во сколько «телеграмных сообщений» бот имеет право уложиться. Влияет на лимит символов в промпте: `(parts × 4000) − 200` |
| `SUMMARY_TIMEZONE` | `Asia/Yekaterinburg` | Часовой пояс расписания |
| `ALLOWED_SUMMARY_IDS` | *(пусто)* | Пусто = `/summary` можно всем. Непусто = только перечисленным ID через запятую. Перекрывается `SUMMARY_ADMIN_ONLY=True` |
| `SUMMARY_ADMIN_ONLY` | `False` | `True` = `/summary` только для `ADMIN_USER_ID` (`ALLOWED_SUMMARY_IDS` игнорируется); `False` = всем/по списку (Epic 31) |
| `SUMMARY_ALIASES` | *(пусто)* | JSON-словарь алиасов: `{"479167456":"главный шиз","138811255":"жим-дьявола"}`. Каскад имён: alias → имя+фамилия → username (без @) → ID |
| `SUMMARY_THROTTLE_SECONDS` | `60.0` | Троттлинг ручного `/summary` на (чат, юзер). Повтор внутри окна — фраза-отборка из пула с реальным оставшимся временем, а не молчание |
| `SUMMARY_CHUNK_DELAY` | `2.0` | Пауза между чанками ответа (сек). Осознанно 2.0 вместо дизайнерского 1.0 — бережём rate limit Telegram |
| `SUMMARY_TARGET_CHAT_IDS` | *(пусто)* | Пусто = саммари во все чаты, где есть сохранённые сообщения; иначе — только перечисленные |
| `SUMMARY_MAX_WINDOW_MESSAGES` | `500` | Кап сообщений окна L1 |
| `SUMMARY_MAX_MESSAGE_CHARS` | `2000` | Кап длины одного сообщения в XML-контексте |
| `SUMMARY_MAX_CONTEXT_CHARS` | `120000` | Жёсткий кап всего XML-контекста |
| `SUMMARY_RAG_L2_LIMIT` | `10` | Сколько цитат из L2 подмешивать в промпт |
| `SUMMARY_RAG_L3_LIMIT` | `10` | Сколько фактов из L3 подмешивать в промпт |
| `SUMMARY_COMPRESS_BATCH` | `100` | Размер пачки сжатия L3 за один LLM-вызов |

**Осознанные решения (зафиксировано после ревью):**
- **Кап размера ответа — только через промпт.** Жёсткого кода «отрезать по `MAX_SUMMARY_PARTS`» нет: LLM получает `{max_symbols}` в системном промпте, а чанкинг ≤4096 — это защита от лимита Telegram, а не enforcement длины. Если LLM разойдётся — сообщение уйдёт чанками, и бот не будет рубить мысль на полуслове.
- **location/contact/dice и прочие «бестекстовые» сообщения не сохраняются.** Осознанно: саммарить геопозицию и кубики — то ещё развлечение. Текст, подписи и медиа (фото/видео/войс/стикер/файл) — сохраняются; у медиа без подписи в контекст попадает описание вида `[фото]`.
- **`SUMMARY_CHUNK_DELAY=2.0`** — отклонение от дизайна (1.0) в пользу лимитов Telegram: 1 сообщение/сек на чат, 20/мин в группе.
- **`/summary` не удаляется из чата** (в отличие от админ-команд): команда — тоже сообщение, память собирает всё, а свидетелей трогать нельзя.

**Требования к проду (обязательно!):**
- **BotFather → `/setprivacy` → Disable.** С включённой privacy mode бот в группах видит только команды и упоминания — память будет пустой, саммари будет про «ничего».
- **`LLM_API_KEY` в продовом `.env`** — в `.env.example` лежит плейсхолдер `your_key_here`, реальный ключ не коммитим (секреты — не для git'а).

### SmartModule: FactCheck — суд присяжных по заявке (Epic 33) 🆕
**Второй сервис с ИИ: бот научился проверять чужие слова.** Отвечаешь реплаем на чьё-то сообщение и начинаешь ответ со слова **«фактчек»** — бот берёт целевой текст, гоняет его через поисковики (каскад Tavily → Exa → DuckDuckGo) и выдаёт токсичный вердикт через LLM **реплаем на целевое сообщение**. Как будто позвал арбитра, который не умеет быть вежливым.

- **Уточнение учитывается:** всё, что написано после слова «фактчек» («фактчек да ты что, это давно опровергли»), уходит в LLM как подсказка. Бот читает между строк твоего реплая.
- **Стикер/фото без текста** в целевом сообщении → бот не зовёт поисковики, а отвечает реплаем на цель порцией абьюза из пула. Проверять нечего — осуждать всегда есть кого.
- **Репост тоже цель:** реплай не нужен — можно фактчекнуть репост напрямую, если триггер в подписи.
- **Регистр не важен**, слово целиком («фактчекинг» не матчится — не надо тут терминов).
- Все вердикты и ошибки уходят **реплаем на целевое сообщение**; только троттлинг-отборка прилетает реплаем на твой вызов — чтобы ты понял, кого именно отшили.
- Кулдаун `FACTCHECK_COOLDOWN_SECONDS` per (чат, юзер), независимый от поиска.

### SmartModule: SmartSearch — «загугли за меня, я устал» (Epic 33) 🆕
**Третий сервис с ИИ: бот вышел в интернет по требованию.** Пишешь **«найди/поищи/загугли + запрос»** — бот собирает факты через тот же SearchAggregator, просит LLM выжать суть и присылает **токсичную выжимку реплаем на твоё сообщение**. Не сарказм ради сарказма — там и факты есть.

- **Пустой запрос** («найди» и всё, сдох на полуслове) → порция абьюза из пула, без обращения к поисковикам. Поисковики — для тех, кто дописал мысль.
- **ВСЕ** ответы (выжимка, ошибки, пустой запрос, троттлинг) прилетают реплаем на твоё сообщение — бот не путает, кому отвечает.
- Кулдаун `SEARCH_COOLDOWN_SECONDS` per (чат, юзер), **независимый от фактчека**: можно одновременно фактчекать одно и гуглить другое, но спамить — нельзя.

### SearchAggregator: каскадный фолбек (Epic 33) 🆕
**Единый поисковый движок для FactCheck и SmartSearch.** Каскад: **Tavily → Exa → DuckDuckGo**. Уровень упал (таймаут/HTTP/пустая выдача) → следующий. Пустой API-ключ → уровень молча выключен ещё при старте (WARNING в логах). DuckDuckGo — без ключа, доступен всегда, последняя надежда человечества. Упали все три — честное **«интернет сдох, ищи сам»** (или соседняя фраза из пула — все одинаково полезные). Новая зависимость `duckduckgo-search` — единственный новичок в `requirements.txt`; httpx уже был, SDK tavily/exa не тянем, голого httpx достаточно.

**Настройка (FactCheck + SmartSearch):**

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `TAVILY_API_KEY` | *(пусто)* | Ключ Tavily (уровень 1 каскада). Пусто = уровень отключён, каскад стартует с Exa. Секрет — только в `.env` |
| `EXA_API_KEY` | *(пусто)* | Ключ Exa (уровень 2 каскада). Пусто = уровень отключён. Секрет — только в `.env` |
| `SEARCH_MAX_SYMBOLS` | `4000` | Сколько символов поисковой выдачи уходит в LLM для SmartSearch. Ниже 100 — дефолт + WARNING |
| `FACTCHECK_MAX_SYMBOLS` | `4000` | То же для FactCheck. Ниже 100 — дефолт + WARNING |
| `SEARCH_COOLDOWN_SECONDS` | `300` | Кулдаун SmartSearch per (чат, юзер). `0` = шпарит без тормозов |
| `FACTCHECK_COOLDOWN_SECONDS` | `300` | Кулдаун FactCheck per (чат, юзер), независимый от поиска. `0` = шпарит без тормозов |

### Dead Page: фикс репостинга Rich Message (Epic 18) 🆕
**Проблема:** бот застрял на старом посте (id=3) и не видел новые Rich Message посты (Telegram Bot API 10.2, июль 2026).

**Исправления:**
- **Forward scan**: при каждом вызове бот сначала сканирует вперёд от последнего известного ID (до 50 ID, увеличено с 20 в v2.15.0), находя свежие посты до старого fallback-а.
- **Rich Message guard**: посты с `rich_message` (новый формат Telegram) распознаются и форвардятся как единое целое, без запуска heuristic album detection — что исключает ложные склейки и падения.
- **Resilient error handling**: непредвиденные ошибки форварда больше не обрывают весь поиск — бот логирует и продолжает со следующим диапазоном.
- **Mock-safe checks**: `isinstance` вместо `getattr` для проверки Rich Message — корректно работает в тестах с MagicMock.

### Исправления конфигурации (Epic 18) 🆕
- **DANGER_WORDS в `.env.example` исправлен с жёстко прописанных 3 слов на пустое значение** (135+ слов из `filters/word_lists.py` используется автоматически)

### Alan_Z: приветствие после сна (F7 v2 — Epic 11) 🆕
**Самая пассивно-агрессивная фича бота.** Если Алан **молчит** в чате дольше `ALAN_SILENCE_GREETING_HOURS` часов, а потом наконец пишет сообщение — бот встречает его приветственным видео. Как будто соскучился. На самом деле — просто фототропизм на движение.

**Как это работает:**
1. Бот запоминает timestamp каждого сообщения Алана в БД (переживает рестарт — память как у слона).
2. Перед каждым новым сообщением вычисляет `elapsed = сейчас − последнее_сообщение`.
3. Если elapsed ≥ N часов → «С ВОЗВРАЩЕНИЕМ!» через случайное видео.
4. Если Алан пишет слишком часто → таймер сбрасывается без приветствия (не заслужил).
5. Общий anti-spam с join-приветствием: если видео только что отправлено при входе — silence не дублирует.

**Зачем это нужно:** потому что наблюдать за реакцией Алана, когда бот «узнаёт» его после долгого отсутствия — бесценно.

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `ALAN_SILENCE_GREETING_HOURS` | `6.0` | Часы молчания, после которых следующее сообщение триггерит видео. `0.5` = 30 мин, `0.0` = фича отключена. Требует перезапуска бота |

### Костик: генератор случайных оскорблений (F8)
На каждое сообщение — оскорбление из пула. Вероятность настраивается: `1.0` = всегда (классика), `0.0` = никогда (режим «Костик в отпуске»). Костик не выбирал эту жизнь, жизнь выбрала его.

### Вася / Админ: лингвистическая симметрия
- Написал «вася» (в любых формах) → **«АДМИН»**
- Написал «админ» → **«ВАСЯ»**

Обратите внимание: это не баг, это зеркало души чата.

### Тестовые команды для админа (Epic 10)
Ручное тестирование без ожидания Славу/Алана:

| Команда | Что делает | Где работает |
|---------|-----------|-------------|
| `/deadpage` | Триггерит dead page | DM — всем, группа — только админу |
| `/alangreet` | Триггерит приветственное видео Алана | DM — всем, группа — только админу |

Сообщение с командой **автоматически удаляется** — админ должен оставаться неуловимым.

---

## ⚡ Быстрый старт

```bash
pip install -r requirements.txt
cp .env.example .env          # впиши API_TOKEN от @BotFather, остальное опционально
mkdir -p media/dead_page      # накидай .jpg и .txt (для fallback'а)
mkdir -p media/leha_greeting  # накидай .mp4 для приветствия Алана
mkdir -p media/common/otboy   # накидай медиа для ответа на «отбой»
mkdir -p media/common/danger  # накидай медиа для danger detection
mkdir -p media/slavik/slavik_random  # накидай фото/видео/GIF/аудио/войсы/доки для Slavik Random Media
python bot.py
```

Через 3 секунды бот готов страдать фигнёй в продакшене.

---

## 🛠 Настройка через `.env`

Все параметры живут в `.env`. После изменения — перезапусти бота. Hot-reload не завезли (и не планировали).

### Общие

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `API_TOKEN` | *(обязательно)* | Токен бота от @BotFather. Без него бот — просто набор файлов на диске |
| `DB_PATH` | `local_database.db` | Путь к SQLite. Переносимый, надёжный, не требует Docker и Kubernetes |
| `MEDIA_BASE` | `media` | Корневая папка с медиафайлами. Меняй только если переехал |

### Мониторинг (Better Stack)

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `SENTRY_DSN` | *(опционально)* | DSN для Sentry. Если пусто — ошибки уходят в /dev/null (и в консоль, если повезёт) |
| `LOGTAIL_SOURCE_TOKEN` | *(опционально)* | Токен для стриминга логов в Better Stack. Если пусто — логи только локально, как в 1995-м |

### Пользователи

| Переменная | По умолчанию | Кто это |
|-----------|-------------|---------|
| `KOSTIK_USER_ID` | `350803143` | Жертва случайного абьюза |
| `SLAVIK_USER_ID` | `479167456` | Главный герой этого цирка |
| `ALAN_USER_ID` | `138811255` | Объект пристального интереса бота |
| `ADMIN_USER_ID` | `5885953495` | Тот, кто нажимает `/deadpage` и `/alangreet` |

### Костик (F8)

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `KOSTIK_REPLY_PROBABILITY` | `1.0` | Вероятность оскорбления: `0.0` = тишина, `1.0` = всегда. Поставь `0` если Костик устал |

### Alan_Z: автоответчик (F6)

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `ALAN_REPLY_INTERVAL` | `10` | Каждые N сообщений — ответ. `0` = бот игнорирует Алана (жестоко) |
| `ALAN_REPLIES_ENABLED` | `True` | Гейт reply-блока (Epic 52): `False` = Алан молчит про SSD и витамины, но счётчик тикает, а F7v2 greeting работает безусловно. На проде `False` |

### Alan_Z: приветственное видео (F7 + F7v2)

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `ALAN_USERNAME` | `@Alan_Z` | Подпись под приветственным видео |
| `ALAN_GREETING_DIR` | `media/leha_greeting` | Папка с видео (`.mp4`, `.avi`, `.mov`, `.webm`) |
| `ALAN_GREETING_COOLDOWN` | `10` | Минимальный интервал (сек) между любыми приветствиями (join + silence) |
| `ALAN_SILENCE_GREETING_HOURS` | `6.0` | Часы молчания Алана, после которых следующее сообщение триггерит приветствие. `0.0` = отключено. Float: `0.5` = 30 мин |

### GIF-счётчик Славы (F3)

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `GIF_INTERVAL` | `5` | Каждые N сообщений Славы — гифка. `0` = без визуала |
| `GIF_PATH` | `media/slavik/slavic_chlen.mp4` | Путь к mp4. Да, файл реально так называется (и да, он переехал в v2.15.0, а код заметил это только сейчас) |

С v2.37.0 (Epic 52) гифка не шлётся на service-сообщения (вход/выход) — счётчик их не засчитывает, а data-флаг «уже ответил» ставится только при РЕАЛЬНОЙ отправке. Гифка ушла → медиа/mimic/«пошёл нахуй» не дублируют её. Одно сообщение — одна гифка максимум.

### Dead Page (F2 v2)

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `DEAD_PAGE_SOURCE_CHANNEL_USERNAME` | `d_pages` | Канал-триггер |
| `DEAD_PAGE_SOURCE_CHANNEL_ID` | `0` | ID канала-триггера (0 = проверять по username) |
| `DEAD_PAGE_RELAY_CHANNEL_ID` | `4228645624` | Приватный канал-источник постов. Бот должен быть там админом |
| `DEAD_PAGE_COOLDOWN` | `10s` | Анти-спам. `0` = шпарит без тормозов |
| `DEAD_PAGE_MAX_FORWARD_RETRIES` | `5` | Попыток найти пост в канале. Как лотерея, только с мемами |
| `DEAD_PAGE_POST_ON_JOIN` | `False` | Постить ли при входе Славы. `False` (Epic 22) = только «ДОЛБОЕВ ВЕРНУЛСЯ», `True` = старый бедлам |
| `DEAD_PAGE_CAPTION_MAX_CHARS` | `1024` | Лимит подписи для fallback |
| `DEAD_PAGE_DIR` | `media/dead_page` | Папка fallback-контента |

### War Alert (F5v2 — Epic 10)

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `WAR_CHANNEL_IDS` | `1654872411` | ID каналов через запятую (ЧП Пермь, Радар БПЛА) |
| `WAR_CHANNEL_USERNAMES` | *(пусто)* | Username каналов через запятую |
| `WAR_REPLIES` | *(пусто)* | Свои фразы через запятую. Пусто = дефолтные |

---

## 🖼 Как работают dead page и fallback

### Основной режим (канал жив)
1. Кто-то репостит из **@d_pages** → бот просыпается
2. Идёт в приватный канал, выбирает случайный пост, форвардит в чат
3. Всё одним сообщением, без разрывов. Магия.

### Fallback (канал мёртв / бота выгнали)
1. Бот берёт случайный `.jpg` и случайный `.txt` из `DEAD_PAGE_DIR`
2. Отправляет фото + подпись (до 1024 символов)
3. Если текста больше — второе сообщение с хвостом

Пополнение: кидай `.jpg` и `.txt` в `media/dead_page/`. Profit.

---

## 🎨 Как изменить текст ответов

Все ответы лежат в файлах хендлеров. Каждый пул — список строк. Добавь строку → расширил пул. Элементарно, Ватсон.

### Костик — `handlers/kostik.py`
```python
KOSTIK_REPLIES = [
    "пошёл нахуй кринжатура ебаная",
    "твой новый insult сюда",        # ← добавь
]
```

### Alan_Z: автоответчик — `handlers/alan.py`
```python
ALAN_REPLIES = [
    "так интересно, продолжай",
    "твоя новая ироничная фраза про SSD-кризис или витамины сюда",  # ← добавь
]
```
Пул (52 фразы с Epic 53: 5 тем × 5 фраз + 5 вопросов-подколов) живёт под гейтом `ALAN_REPLIES_ENABLED`: на проде реплики выключены, но F7v2 greeting от пула не зависит.

### Alan_Z: приветственное видео — `media/leha_greeting/`
Кидай `.mp4` (и `.avi`, `.mov`, `.webm`) в папку. Бот сам выберет случайный.

### Слава — `handlers/slavik.py`
Ответы захардкожены прямо в коде:
```python
await message.reply("ДАЛБАЕБ")       # «куча»
await message.reply("трясло ебаное")  # военные слова
await message.reply("пошёл нахуй")    # всё остальное
```

### Вася / Админ — `handlers/vasya.py`
```python
await message.reply("АДМИН")  # «вася»
await message.reply("ВАСЯ")   # «админ»
```

---

## 🔍 Как узнать Telegram ID

1. `@userinfobot` в личку
2. `@getidsbot` — перешли любое сообщение
3. Настройки Telegram → Appearance → Show Profile ID

---

## 🧪 Тестирование

```bash
# Все 1555 тестов
py -m pytest tests/ -v

# Только SmartModule
py -m pytest tests/test_summary_*.py -v

# Только Alan
py -m pytest tests/test_alan.py -v

# Только Alan silence greeting
py -m pytest tests/test_alan.py -v -k "silence"

# Только БД
py -m pytest tests/test_database.py -v

# С покрытием
py -m pytest tests/ -v --cov=. --cov-report=term-missing
```

---

## 📊 Мониторинг (Better Stack)

| Компонент | Что делает | Библиотека |
|-----------|-----------|-----------|
| **Sentry** | Отлов исключений со стектрейсами | `sentry-sdk` |
| **Logtail** | Стриминг логов в реальном времени | `logtail-python` |

### Как смотреть логи
1. [betterstack.com](https://betterstack.com/) → **Logs & Metrics** → **Live tail**
2. Фильтруй по `level:error`, имени модуля, или тексту
3. Все `logger.info/warning/error` из кода прилетают с тегом `source`

### Как смотреть ошибки
1. Better Stack → **Errors** → Issues со стектрейсами
2. Контекст: чат, сообщение, теги. Как чёрный ящик, только полезный.

### Локально (без Better Stack)
Нет `SENTRY_DSN` и `LOGTAIL_SOURCE_TOKEN` в `.env` → логи только в консоль. Всё работает.

---

## 🐛 Исправлено в v2.12.1 (Epic 16)

### Epic 16: Bug Fixes — исправление двух критических багов

**Bug 1: Репостинг альбомов (DeadPageRelay)**
- **До**: для каждого фото в альбоме вызывался отдельный `forward_message`, что приводило к разрыву группы и дублированию.
- **После**: `_forward_with_heuristic()` переписан на **Collect-then-Group** — собирает все `message_id` одного альбома и делает **один** `forward_messages`. Альбомы пересылаются целиком, без разрывов.

**Bug 2: Propagation forwarded-сообщений к common_router**
- **До**: `war_channel_repost_handler` использовал `F.forward_origin` как propagation stopper, что блокировало **все** forwarded-сообщения от попадания в `common_router`. Danger Detection не срабатывал на репосты.
- **После**: **новый `TargetChannelFilter`** — проверяет только целевые каналы (ЧП Пермь, Радар БПЛА). Forwarded-сообщения из других источников свободно проходят к `common_router`, и Danger Detection работает корректно.

**DangerWordFilter: 22 → 187 слов**
- Словарь `DangerWordFilter` синхронизирован с `WarWordFilter.WAR_WORDS` — расширен с 22 до **187 словоформ**. Добавлены: склонения «ракетная/ракетной/ракетную», «обстрел/обстрелы/обстрелов», «сбит/сбита/сбито/сбиты», «падение/упал/упала», «эвакуация», «отбой» и др.

**Тесты: 372 → 387 (+15)**
- 6 тестов на альбомы (heuristic + DB path)
- 6 тестов на DangerWordFilter (caption, forward, no regression)
- 3 теста на propagation (war_alert + common_router interaction)
- Полный прогон: 387 тестов, 0 регрессий

---

## 🐛 Исправлено в v2.13.0 (Epic 17)

### Epic 17: Danger Word Fix — восстановление реакции на danger-слова

**Root Cause:** `war_alert_router` (позиция 4b) блокировал propagation сообщений от Славы, не давая `common_router` (позиция 4c) обработать danger-слова. `DangerWordFilter` и `WarWordFilter` содержат одинаковый словарь из 135+ слов — когда Слава пишет danger-слово, `war_alert_router` перехватывает сообщение и останавливает цепочку, не доходя до `common_router`.

**Исправления:**

1. **Propagation fix (T3):** Все 4 хэндлера (`war_keyword_handler`, `war_channel_repost_handler`, `otboy_handler`, `danger_handler`) теперь возвращают `UNHANDLED` из `aiogram.dispatcher.event.bases` — канонический паттерн aiogram 3.x для продолжения propagation. Теперь сообщения Славы обрабатываются обоими роутерами: war_alert отвечает текстом, common — медиа.

2. **Merge словарей (T2):** Создан единый модуль `filters/word_lists.py` с объединённым списком `DANGER_WORDS` (DRY). Оба фильтра — `DangerWordFilter` и `WarWordFilter` — импортируют из одного источника. Устранена ручная синхронизация.

3. **CommonRelay: audio/voice + graceful degradation (T4):**
   - Добавлена поддержка `.mp3` → `send_audio` и `.ogg` → `send_voice`
   - `_scan_directory` больше не падает с `FileNotFoundError` при отсутствии папки — возвращает пустой список с `logger.warning`
   - 5 медиа-типов: photo (jpg/png/webp), video (mp4/mov/webm), animation (mp4/mov/webm с "gif" в имени), audio (mp3), voice (ogg)

**Тесты: 387 → 399 (+12)**
- 5 новых edge case тестов: оба слова в одном сообщении, повреждённый файл, неподдерживаемый формат, PermissionError, cooldown при ошибке
- 7 тестов на propagation с `UNHANDLED`
- Полный прогон: 399 тестов, 0 регрессий

---

## 🐛 Исправлено в v2.16.0 (Epic 18)

### Epic 18: Danger Service Fixes — фикс выбора файлов, GIF-детекта и кулдауна

**Bug A — File scanning robustness:**
- **Проблема:** третий файл в danger-папке ни разу не был отправлен за сотню запросов. Плюс ошибки доступа к отдельным файлам (OSError) крашили всё сканирование директории.
- **Исправление:** per-entry try/except OSError в `_scan_directory()` — битые/недоступные файлы пропускаются с WARNING, а не ломают всю отдачу. Добавлен INFO-лог со списком всех найденных файлов для диагностики. `random.choice()` корректно выбирает из всех файлов без хардкод-лимитов.

**Bug B — GIF detection by filename:**
- **Проблема:** `danger_02_gif.mp4` отправлялся как видео вместо анимации. Детект использовал `filepath.stem` (только последнее расширение) — не покрывал edge-кейсы.
- **Исправление:** переписан `_detect_media_type()` на `filepath.name` с word-boundary матчингом: `_gif` в имени, `gif_` в начале, `.gif.` в середине. Исключены ложные срабатывания на `gift`. Все 14 текущих файлов корректно определяются как animation (13 с gif) или video (1 без).

**Bug C — Separate danger cooldown:**
- **Проблема:** не было отдельного кулдауна для danger — общий `COMMON_COOLDOWN` блокировал оба сервиса одинаково. При активном чате danger мог спамить.
- **Исправление:** добавлен `DANGER_COOLDOWN` (по умолчанию 60 сек = 1 минута). Двухуровневый кулдаун: danger проверяет и общий, и свой кулдаун; otboy — только общий. Матрица блокировок полностью покрыта тестами.

**Тесты: 458 → 478 (+23)**
- 8 тестов на GIF-детекцию (все позиции "gif", camelCase, false-positive prevention)
- 3 теста на robustness сканирования (OSError, logging, random coverage)
- 10 тестов на dual cooldown (все комбинации shared/danger и otboy/danger)
- 2 бонусных теста на edge-кейсы
- Полный прогон: 478 тестов, 0 регрессий

**Файлы:** `services/common_relay.py`, `config/settings.py`, `bot.py`, `.env.example`, `tests/test_common.py`

---

## 🐛 Исправлено в v2.18.0 (Epic 20)

### Epic 20: Slavik Random Media Enhancement — полная поддержка медиа-типов и hardened GIF detection

**Bug D — Slavik random media: только фото и видео:**
- **Проблема:** Славик умел отправлять только фото и видео из `slavik_random/`. Аудио, войсы, документы — робот тупо игнорировал. Как Слава, который ест только пельмени и отказывается от супа.
- **Исправление:** `_detect_slavik_media_type()` расширен до 6 типов: photo (.jpg/.png/.webp/.bmp/.jpeg), video (.mp4/.mov/.webm), animation (видео с gif-детектом), audio (.mp3), voice (.ogg), document (fallback для всего остального). `_send_slavik_media()` вызывает соответствующий метод Telegram API: `send_photo`, `send_video`, `send_animation`, `send_audio`, `send_voice`, `send_document`.

**Bug E — GIF detection в slavik_random нестандартный:**
- **Проблема:** GIF-детект в slavik_random отличался от CommonRelay — не ловил `_gif`, `.gif.`, и `gif` в начале имени.
- **Исправление:** тройной word-boundary матчинг, идентичный CommonRelay: (1) `.gif.` (двойная точка), (2) `_gif` (подчёркивание), (3) `startswith("gif")` (в начале). Исключены ложные срабатывания на `gift` и `notagif`.

**Bug F — Per-entry OSError: робот падал от битых файлов:**
- **Проблема:** один битый или недоступный файл в `slavik_random/` — и `os.scandir()` крашил весь `_pick_random_slavik_media()`, лишая Славу медиа-сюрпризов. Как если бы Слава падал от одного плохого падика.
- **Исправление:** per-entry `try/except OSError` с `logger.warning` — битые файлы пропускаются, остальные работают. Робот устойчив к битым файлам, как Слава устойчив к падикам.

**Bug G — Dead code и диагностика:**
- **Убран dead code:** охранный `if media_type is not None` (media_type НИКОГДА не был None — всегда был минимум `document` как fallback).
- **Добавлен `len(files)` в диагностический лог** — теперь видно, сколько всего файлов найдено, а не только список имён.

**Тесты: 509 → 570 (+61)**
- 61 новый тест в `tests/test_slavik_media_types.py`:
  - 33 теста на `_detect_slavik_media_type` (все расширения + GIF-детект + edge-кейсы)
  - 7 тестов на `_send_slavik_media` (все 6 типов + unknown fallback)
  - 7 тестов на `_pick_random_slavik_media` (пустая/битая/смешанная директория, поддиректории)
  - 14 тестов на GIF detection edge-кейсы (starts_with, underscore, double-dot, false positives)
- Полный прогон: **570 тестов**, 0 регрессий

**Файлы:** `handlers/slavik.py`, `tests/test_slavik_media_types.py`

---

## 🐛 Исправлено в v2.20.0 (Epic 22)

### Epic 22: Гонка функций и точность триггеров (D51–D54)

Четыре фикса точности. Если коротко: бот перестал стрелять из всех орудий сразу и начал целиться.

**D51 — Olya: только SaveAsBot-видео:**
- **Проблема:** Оля отправляла ЛЮБОЕ видео — бот отвечал кринжем. Никакой дискриминации.
- **Исправление:** `OLYA_ALWAYS_SEND` дефолт `True → False`. Теперь триггер — caption «Спасибо, что пользуетесь - @SaveAsBot'ом» **ИЛИ** репост из каналов SaveAsBot (523131145). Логика ИЛИ сохранена, потому что репост часто приходит без caption, а caption может встретиться и не в репосте — AND ломал бы оба сценария.
- **Откат:** `OLYA_ALWAYS_SEND=True` в `.env` — и старый беспредел возвращается по щелчку.

**D52 — Mimic: не передразнивать репосты:**
- **Проблема:** бот шепелявил чужие репосты. Некрасиво цитировать без разрешения.
- **Исправление:** единый параметр `MIMIC_FORWARDS_ENABLED=False` для обоих mimic-механизмов (common + Славик). `forward_origin is not None` + параметр выключен → mimic пропускается.

**D53 — Славик: устранена гонка ответов:**
- **Проблема:** при входе Славы бот палил одновременно «ДОЛБОЕВ ВЕРНУЛСЯ» + dead page; на репост из @d_pages — dead page + «пошёл нахуй». Концерт по заявкам радиослушателей.
- **Исправление:** join → только «ДОЛБОЕВ ВЕРНУЛСЯ» (`DEAD_PAGE_POST_ON_JOIN=False`); dead page — только на репосты Славы из @d_pages; catch-all Славика пропускает d_pages-репосты (`UNHANDLED`). Одно событие — один ответ. Приоритет: приветствие > dead page > «пошёл нахуй».

**D54 — PostPicker: анти-повтор:**
- **Проблема:** при sparse-канале sequential scan находил первый существующий пост — «почти всегда id 3». Слава уже знал этот пост наизусть.
- **Исправление:** ключ БД `dead_page_last_sent:{chat_id}` (не путать с `last_known_message_id` — это верхняя граница скана). last_sent исключается из forward-scan, sequential и random веток; re-roll без сжигания попытки; при единственном посте — осознанный повтор вместо ALL RANGES EXHAUSTED. После успешного форварда (включая альбомы) пишется первичный msg_id.

**Тесты: 586 → 621 (+35)**
- Полный прогон: **621 тест**, 0 регрессий.

**Файлы:** `config/settings.py`, `.env.example`, `handlers/common.py`, `handlers/slavik.py`, `handlers/dead_page_trigger.py`, `services/database.py`, `services/dead_page_relay.py`, `tests/test_olya.py`, `tests/test_common.py`, `tests/test_slavik_handlers.py`, `tests/test_dead_page_trigger.py`, `tests/test_dead_page_relay.py`, `tests/test_database.py`, `tests/test_slavik_priority.py` (новый)

---

## 🐛 Изменено в v2.21.0 (Epic 23)

### Epic 23: Точная настройка danger-словаря (D55–D58)

Бот перестал подпрыгивать на каждом «летит» и «прилет» — теперь он целится во фразы. Меньше ложных тревог, больше смысла.

**D55 — Механика фраз:** новая константа `DANGER_PHRASES` (17 фраз-связок) в `filters/word_lists.py` + отдельная ветка матчинга в `DangerWordFilter`: фразы проверяются **до** одиночных слов, границы только по краям фразы, регистр не важен. Возврат `{"matched_word"}` остался совместимым — Telegram quote теперь цитирует целую фразу, и ни один потребитель не сломался.

**D56 — Shelter только фразами:** 26 одиночных форм `укрытие*`/`убежище*`/`бункер*` отправлены в утиль. Одиночное «бункер» больше не считается угрозой — можно спокойно обсуждать бункер как образ жизни. Вместо них — 10 фраз: «укрыться в убежище», «уйти в бомбоубежище», «в бункер», «спрятаться в бункере» и др.

**D57 — «Хлопок»:** в секцию Flash добавлены журналистские синонимы взрыва: `хлопок`, `хлопки`, `хлопнуло`, `хлопнул`. `вспышка*`/`взрыв*` остаются на месте. Да, аплодисменты теперь тоже могут сработать — омоним-риск принят осознанно.

**D58 — Атака только связками:** 28 одиночных форм `атака*`/`угроза*`/`обстрел*` удалены — «атака» в разговоре про игру больше не будит бота. Вместо них — 7 фраз: «беспилотная атака», «ракетная атака», «атака дронов», «атака беспилотников», «ракетный обстрел» и др.

**Flight и Падение/сбитие удалены целиком:** «летит самолёт», «прилетел поезд» и «упал в лужу» больше не считаются новостями с фронта.

**Итог словаря:** 118 словоформ + 17 фраз-связок = **135 паттернов** (было 191; README ранее честно писал «187» — теперь всё сходится).

**Тесты: 621 → 672 (+51)**
- `TestDangerPhrases` — 42 кейса: позитив на все 17 фраз, негативы одиночных слов, границы фраз («в бункер» vs «в бункере»), регистронезависимость
- Контрактный тест на 17 фраз
- Полный прогон: **672 теста**, 0 регрессий

**Файлы:** `filters/word_lists.py`, `filters/danger_word.py`, `tests/test_filters.py`, `tests/test_common.py`, `README.md`, `plans/ARCHITECTURE.md`, `plans/MEMORY.md`

---

## 🧠 Новое в v2.22.0 (Epic 24)

### SmartModule: Summary — подслушивание как сервис

Бот научился запоминать. Всё. Постоянно. А потом пересказывать это чату в токсично-ироничной форме с припиской «самым главным шизом объявляется…».

**Что завезли:**
- **Трёхуровневая память**: окно 6 часов (L1) → сырые сообщения 30 дней для цитат (L2) → векторный архив фактов на 90 дней (L3, sqlite-vec KNN). Деградация изящная: нет sqlite-vec или упали эмбеддинги → всё ищется через FTS5. Бот падает только вместе с сервером.
- **LLM-клиент**: провайдер-агностик на httpx (`deepseek-v4-flash` + `gemini-embedding-001` через apinet.cloud), retry на 429/5xx/таймауты с backoff. `LLM_API_KEY` — только в `.env`, в git не лежит.
- **APScheduler**: 00:00/06:00/12:00/18:00 по Екатеринбургу, MemoryJobStore, `max_instances=1` + `coalesce` — саммари не гоняется параллельно с самим собой.
- **`/summary`** — ручной триггер с троттлингом (60 сек на чат+юзер) и списком допущенных (`ALLOWED_SUMMARY_IDS`). Спам молча глотается.
- **Чанкинг ≤4096 по пробелам** + `TelegramRetryAfter`-обработка: длинное саммари не упирается в лимит Telegram, а спокойно приезжает кусками с паузой.
- **UX-ошибки** без техдеталей: «не смог сделать саммари потому что упал апи» / «база данных подавилась». Маленькими буквами. Как в жизни.
- **Наблюдатель (позиция 0a)**: ловит каждое сообщение и всегда отдаёт `UNHANDLED` — остальные фичи (Слава, Алан, Костик, danger…) работают как ни в чём не бывало. Проверено интеграцией на все 13 роутеров.

**По мелочи (замечания ревьюера закрыты):**
- Цитаты L2 и факты L3 теперь проходят то же XML-экранирование, что и `<chat_history>` (Low-2).
- Троттлинг понимает `/summary@BotName` — суффикс имени бота отрезается перед проверкой (Low-3).

**Тесты: 672 → 835 (+163)**
- 157 тестов Epic 24 (LLM-клиент, память L1/L2/L3, XML, алиасы, чанкинг, троттлинг, планировщик, хендлеры + интеграция на 13 роутеров)
- 6 тестов на Low-фиксы после ревью (экранирование `<memory>`/`<facts>`, троттлинг с @BotName)
- Полный прогон: **835 тестов**, 0 регрессий

**Файлы:** `services/llm_client.py`, `services/summary_prompts.py`, `services/summary_aliases.py`, `services/summary_xml.py`, `services/summary_memory.py`, `services/summary_generator.py`, `services/summary_scheduler.py`, `services/summary_throttling.py`, `handlers/summary.py`, `config/settings.py`, `.env.example`, `requirements.txt`, `services/database.py`, `bot.py`, `tests/test_summary_*.py` (9 файлов), `tests/test_database.py`

---

## 🧠 Новое в v2.24.0 (Epic 26)

### GraphRAG: граф знаний — бот теперь помнит, кто кого назвал долбоёбом

Трёхуровневой памяти стало мало, и бот завёл себе **граф знаний** — таблицы `nodes` и `edges` прямо в SQLite. Серьёзные корпорации для этого берут Neo4j, но у нас корпораций нет.

**Как это работает:**
- **Entity extraction при архивации:** когда старые сообщения сжимаются в факты (L3), бот делает ещё один LLM-вызов через `deepseek-v4-flash` и извлекает **триплеты** — «кто с кем спорил», «кто кого жёстко оскорбил», «кто на чём фанатеет». Результат складывается в граф: участники и темы становятся узлами, связи — рёбрами.
- **Вес имеет значение:** повторное упоминание одной и той же связи не плодит дубликаты, а накачивает вес ребра. Да, у ваших конфликтов теперь есть вес. Буквально.
- **Гибридный поиск в `/summary`:** при генерации саммари бот сопоставляет участников и темы окна с узлами графа (без лишних LLM-вызовов, детерминированно) и подмешивает в промпт самые жирные связи в виде `[Историческая справка: Слава (жестко оскорбил) Костика]`. Саммари стало умнее, архив — цитируемее.
- **Граф не ломает ничего:** упал extraction, пустой граф или сущности не сматчились — секция справок просто исчезает из промпта, бот делает вид, что так и было. Узел не найден — fallback на топ рёбер всего чата.

**Настройка:**

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `GRAPH_RAG_ENABLED` | `True` | Главный рубильник графа. `False` = аварийный выключатель: extraction при архивации не вызывается, поведение ровно как до фичи |
| `GRAPH_EDGE_WEIGHT_INCREMENT` | `1` | На сколько толстеет ребро при повторном упоминании связи |
| `GRAPH_TOP_EDGES_LIMIT` | `5` | Сколько рёбер-справок «[Историческая справка: …]» подмешивать в `/summary` |
| `GRAPH_EXTRACT_MAX_TRIPLETS` | `50` | Максимум триплетов, сохраняемых за один extraction-вызов |

**Исправлен P1-баг (T-206, pre-existing с Epic 24):** медиа без подписи не попадали в FTS-индекс (вставка под `if text:`), но FTS-удаление шло безусловно — при чистке старых сообщений SQLite выдавал «database disk image is malformed». Теперь удаление зеркалит условие вставки, и фото без подписи выпиливаются из памяти без членовредительства.

**Тесты: 860 → 939 (+79)**
- 73 теста Epic 26: парсер триплетов (валидный/кривой JSON, code-fence, обёртка-объект), upsert nodes/edges с накачкой веса, traversal графа, чат-изоляция, пустой граф, pipeline с `graph_facts`
- 6 тестов T-206: FTS-консистентность удаления (медиа без подписи не падает, обычный текст удаляется, сирот в FTS нет)
- Полный прогон: **939 тестов**, 0 регрессий

**Файлы:** `services/database.py` (DDL nodes/edges + методы графа), `services/summary_memory.py` (extraction + lookup), `services/summary_generator.py` (`<historical_graph_facts>`), `services/summary_prompts.py` (EXTRACT_PROMPT), `config/settings.py`, `.env.example`, `tests/test_graphrag_database.py`, `tests/test_graphrag_memory.py` (новые), `tests/test_summary_*.py`, `tests/test_database.py`

---

## 🆕 Новое в v2.28.0 (Epic 30)

### Common Expansion: selfdev/work-реакции, goodmorning-рассылка, фикс нумерации промпта

Чат теперь не просто абьюзят — чат развивают, жалеют и будят. Комплексный подход к токсичности.

**Selfdev + Work (R30-1/R30-2):**
- Триггер-слова: «саморазвитие»-семья (48 словоформ + 17 фраз: «личностный рост», «зона роста»…) и «устал/заебался»-семья (128 словоформ + 31 фраза: «нет сил», «заебала работа»…). Reply + нативный quote найденного слова, случайное медиа из `media/common/{selfdev,work}/`
- Репосты НЕ триггерят (`forward_origin is None`) — чужое саморазвитие и чужая усталость бота не волнуют
- Анти-спам: пер-сабдирные кулдауны `SELFDEV_COOLDOWN`/`WORK_COOLDOWN` (дефолт 5m) поверх общего `COMMON_COOLDOWN`; сабдиры независимы друг от друга
- Пересечений списков слов с danger/«отбой» нет — danger не украдёт чужой матч

**Goodmorning (R30-3):**
- APScheduler (прецедент summary_scheduler): раз в сутки в `GOODMORNING_TIME` по `GOODMORNING_TZ` случайный файл + случайный caption из пула (3 канона + 3 новых)
- Plain-send без reply/quote; audio/voice → skip с WARNING; пустые `GOODMORNING_TARGET_CHAT_IDS` = рассылка выключена (WARNING в лог)

**Фикс нумерации промпта (R30-4):** SYSTEM_PROMPT v4 перенумерован 1–6 последовательно (зазор «1,2,4,5,6,7» устранён), текст пунктов дословно, `COMPRESS_PROMPT`/`EXTRACT_PROMPT` не тронуты. Эталон в backlog и байт-в-байт тест обновлены синхронно.

**Тесты: 1002 → 1327 (+325)**
- 87 юнитов selfdev-фильтра (все 48 форм + 17 фраз, границы, репосты-гейт)
- 183 юнита work-фильтра (все 128 форм + 31 фраза, мат-формы, границы)
- 39 юнитов goodmorning (капции, детект медиа, планировщик, парсинг времени)
- Полный прогон: **1327 тестов**, 0 failed, 0 регрессий

**Файлы:** `filters/selfdev_word.py`, `filters/work_word.py` (новые), `services/goodmorning_captions.py`, `services/goodmorning_relay.py`, `services/goodmorning_scheduler.py` (новые), `filters/word_lists.py`, `services/common_relay.py`, `handlers/common.py`, `services/summary_prompts.py`, `config/settings.py`, `.env.example`, `bot.py`, `tests/test_selfdev_word.py`, `tests/test_work_word.py`, `tests/test_goodmorning.py` (новые), `tests/test_common.py`, `tests/test_summary_prompts.py`, `media/common/{selfdev,work,goodmorning}/`

---

## 🆕 Новое в v2.29.0 (Epic 31)

### /summary для всех + меню команд + таймаут-отборки

Три маленьких шага к большой демократии:

**Доступ для всех (R31-1):** `/summary` теперь вызывается ЛЮБЫМ юзером чата, а не только владельцем. Кому демократия поперёк горла — переключатель `SUMMARY_ADMIN_ONLY`: `False` (дефолт) = доступно всем (или по списку `ALLOWED_SUMMARY_IDS`), `True` = только `ADMIN_USER_ID` (список игнорируется). Отказ — по-прежнему молчаливое игнорирование: ни удаления команды, ни ответа, ни извинений.

**Меню команд без BotFather (R31-2):** бот сам регистрирует `/summary` в меню «/» через `setMyCommands` при каждом старте (идемпотентно, best-effort — сбой API не роняет старт). Описание: «Саммари чата — прочитай, что ты пропустил, ленивец». Меню видно во всех чатах и всем юзерам; админ-команды `/deadpage` и `/alangreet` в меню сознательно не выносим — скрытые тестовые кнопки должны оставаться скрытыми. Нюанс: `setMyCommands` **перезаписывает** весь список команд бота — если в BotFather было своё меню, оно сменится на один `/summary` (риск задокументирован в ARCHITECTURE 40.7).

**Таймаут-отборки (R31-3):** повторный `/summary` внутри окна троттлинга больше не глотается молча — бот отвечает reply-ом одной из 7 случайных фраз с РЕАЛЬНЫМ оставшимся временем: «хули ты дрочишь, подожди 42 секунды» и ещё шесть в том же духе. Пул — `_THROTTLE_PHRASES` в `services/summary_throttling.py`; расширение пула = новая строка в кортеже, согласования с отделом кадров не требуется.

**Тесты: 1327 → 1366 (+39)**
- Полный прогон: **1366 тестов**, 0 failed, 0 регрессий

**Файлы:** `config/settings.py`, `handlers/summary.py`, `services/summary_throttling.py`, `services/bot_commands.py` (новый), `bot.py`, `.env.example`, `tests/test_summary_throttling.py`, `tests/test_summary_handlers.py`, `tests/test_bot_commands.py` (новый)

---

## 🔧 Исправлено в v2.30.0 (Epic 32)

### Гифка Славика воскресла, Оля наконец реагирует, троттлинг — 5 минут

Три поломки, одна версия. Как в хорошем автосервисе: сначала диагностика, потом запчасти.

**Гифка Славика (R32-1):**
- **Проблема:** файл переехал в `media/slavik/slavic_chlen.mp4` ещё в v2.15.0 (2026-08-02), а middleware упорно искал его по старому адресу, игнорируя `settings` как старшеклассник игнорирует домашку. Ошибка глушилась `except: pass` — тишина в эфире ~две недели.
- **Исправление:** middleware читает `settings.GIF_PATH`/`settings.GIF_INTERVAL`; файла нет → WARNING «GIF file not found» и пропуск (бот живёт дальше); ошибки отправки логируются с путём (`FileNotFoundError` отдельно, остальное с traceback); успех → INFO. `GIF_INTERVAL <= 0` → гифка выключена с WARNING вместо ZeroDivisionError. Слава снова получает свой визуальный контент каждые 5 сообщений. Повезло ему.

**Триггеры Оли (R32-2):**
- **Проблема:** caption-матч был чувствителен к тире/апострофам/регистру, а ID SaveAsBot жил в канальном ключе — но SaveAsBot это **бот**, то есть юзер. Репост-ветка была недостижима, как «сделаю завтра».
- **Исправление:** нормализация caption (тире-варианты → «-», апострофы → «'», нижний регистр, схлоп пробелов) + substring-матч; новый триггер «упоминание @SaveAsBot» (`OLYA_CAPTION_MENTION_ENABLED`); репост-матрица: `MessageOriginUser` → `OLYA_SAVEASBOT_USER_IDS` (дефолт 523131145), `MessageOriginChannel` → `OLYA_SAVEASBOT_CHANNEL_IDS` (дефолт теперь пусто). Обычные видео Оли по-прежнему игнорируются — как и положено.

**Троттлинг `/summary` (R32-3):** прод-значение `SUMMARY_THROTTLE_SECONDS=300.0` (5 минут) — правка только прод `.env` (@DevOps), код не трогали. Таймаут-отборки честно покажут «подожди 4 минуты 59 секунд».

**Тесты: 1366 → 1392 (+26)**
- Полный прогон: **1392 теста**, 0 failed, 0 регрессий

**Файлы:** `services/message_counter.py`, `filters/olya_video.py`, `config/settings.py`, `.env.example`, `tests/test_message_counter.py`, `tests/test_olya.py`

---

## 🧠 Новое в v2.31.0 (Epic 33)

### SmartModule Extension: FactCheck + SmartSearch + SearchAggregator

Бот перестал быть просто «попугаем с памятью» и вышел в интернет. Теперь он проверяет факты и ищет инфу — с фирменной вежливостью, разумеется.

**FactCheck (R33-3):**
- Триггер: реплай/репост, начинающийся со слова «фактчек» (регистронезависимо, слово целиком). Целевой текст → SearchAggregator → токсичный вердикт LLM реплаем на **целевое** сообщение
- Уточнение после «фактчек» → `user_hint` в LLM; репост → `forward_source` в контекст
- Стикер/фото без текста → порция абьюза из пула без вызова поисковиков (проверять нечего, а отвечать есть что)
- Кулдаун `FACTCHECK_COOLDOWN_SECONDS` per (чат, юзер); троттлинг-отборка — реплаем на вызов, всё остальное — на цель

**SmartSearch (R33-4):**
- Триггер: «найди/поищи/загугли + запрос». Выжимка фактов через SearchAggregator + LLM, **все** ответы реплаем на сообщение вызова
- Пустой запрос → порция абьюза из пула, поисковики не беспокоим
- Кулдаун `SEARCH_COOLDOWN_SECONDS` per (чат, юзер), независимый от фактчека

**SearchAggregator (R33-2):**
- Каскад **Tavily → Exa → DuckDuckGo**; упал уровень → следующий; пустой ключ → уровень выключен (WARNING при старте)
- Упали все три → `AllSearchEnginesFailedException` → «интернет сдох, ищи сам» (или сосед из пула)
- Новая зависимость `duckduckgo-search>=8.1.0,<9.0.0`; обрезка выдачи до `SEARCH_MAX_SYMBOLS`/`FACTCHECK_MAX_SYMBOLS`

**Конфиг (R33-1):** 6 новых env-ключей (`TAVILY_API_KEY`, `EXA_API_KEY`, `SEARCH_MAX_SYMBOLS`, `FACTCHECK_MAX_SYMBOLS`, `SEARCH_COOLDOWN_SECONDS`, `FACTCHECK_COOLDOWN_SECONDS`) + хелперы `_env_int_min`/`_env_float_min` (кривое значение → дефолт + WARNING). Ключи — секреты, только в `.env`, в `.env.example` — пустые плейсхолдеры.

**Роутеры:** 0c factcheck и 0d smartsearch встали сразу после 0b summary, гейт `SUMMARY_ENABLED`, порядок остальных 13 роутеров не тронут. Не-триггер → `UNHANDLED`, пропагация живёт (danger-слова по-прежнему срабатывают, «найди ракету» отвечает ровно один сервис — проверено интеграционными тестами через `Dispatcher.feed_update`).

**Тесты: 1392 → 1555 (+163)**
- 150 тестов Epic 33: агрегатор (каскад/фолбеки/обрезка/пустые ключи), factcheck-сервис и хендлеры, smartsearch-сервис и хендлеры, промпты байт-в-байт с эталоном, пулы фраз, троттлинг/форматтер
- 13 тестов после ревью: 4 интеграционных на изоляцию роутеров (`feed_update`) + 9 на новые settings-хелперы (заодно вскрыли и закрыли `NameError: logging` в WARNING-ветках конфига)
- Полный прогон: **1555 тестов**, 0 failed, 0 регрессий

**Файлы:** `services/search_aggregator.py`, `services/factcheck_service.py`, `services/search_service.py`, `services/factcheck_prompts.py`, `services/search_prompts.py`, `services/smartmodule_phrases.py`, `services/smartmodule_throttling.py`, `services/smartmodule_utils.py` (новые), `handlers/factcheck.py`, `handlers/search.py` (новые), `config/settings.py`, `.env.example`, `requirements.txt`, `bot.py`, `tests/test_search_aggregator.py`, `tests/test_factcheck_*.py`, `tests/test_smartsearch_*.py`, `tests/test_smartmodule_phrases.py`, `tests/test_epic33_router_isolation.py`, `tests/test_settings_helpers.py` (новые)

---

## 🔧 Исправлено в v2.31.1 (Epic 34)

### SmartSearch больше не играет в молчанку

- **Проблема:** в супергруппе сообщение-триггер «найди …» успевали удалить за время каскада Tavily→Exa→DDG + LLM (десятки секунд). Telegram отвечал 400 «message to be replied not found», ответ улетал в общий except, повторный reply упирался в тот же мёртвый id — вторая 400, и пользователь получал тишину вместо выжимки.
- **Исправление:** `services/smartmodule_utils.py` — единая точка отправки `_send_once`: при 400 «message to be replied not found» и заданном reply — ровно один повтор **без** `reply_to_message_id` (WARNING → INFO). Ответ доставляется в любом случае, просто без привязки к удалённому сообщению. Прочие 400 не ретраятся — ретраить «chat not found» всё равно что стучаться в пустую квартиру.
- **FactCheck** покрыт автоматически (делит те же утилиты); хендлеры не трогали.

**Тесты: 1555 → 1564 (+9)**
- Полный прогон: **1564 теста**, 0 failed, 0 регрессий

**Файлы:** `services/smartmodule_utils.py`, `tests/test_smartmodule_utils.py`, `tests/test_smartsearch_handlers.py`, `tests/test_factcheck_handlers.py`

---

## 🔧 Исправлено в v2.31.2 (Epic 35)

### Алан больше не здоровается трижды (это не эхо)

- **Проблема:** после 10.8 часов молчания Алан вернулся с пачкой сообщений, и бот от радости отправил greeting-видео ТРИ раза подряд (05:03:24–26 UTC). Три параллельных апдейта прошли проверку «а прошло ли 2 часа молчания» одновременно — кулдаун и таймер записывались уже ПОСЛЕ отправки видео (1.3–2.7с). Классический check-then-act race: каждый подумал, что он первый и единственный.
- **Исправление:** per-chat `asyncio.Lock` в `handlers/alan_greeting.py` (`_greeting_locks` + `_get_greeting_lock`), общий для silence-пути (F7v2) и обоих join-путей (F7), + заявка кулдауна/таймера ДО `await` отправки (claim-before-send): второй и третий хендлеры внутри лока уже видят свежий таймер → «threshold not reached» → тишина. При неудаче отправки заявка откатывается — старая семантика сохранена. Пачка сообщений → ровно ОДИН greeting.
- **Легитимный silence-greeting сохранён:** после рестарта бот по-прежнему помнит молчание (таймер в БД) и здоровается ровно один раз.

**Тесты: 1564 → 1573 (+9)**
- 7 конкурентных кейсов F7v2 (`asyncio.gather` трёх параллельных вызовов → 1 greeting; повтор в кулдаун → 0; после истечения → 1; ts записан ДО отправки; рестарт-симуляция; per-chat изоляция; неудача → rollback)
- 2 конкурентных кейса join-путей (2 параллельных join → 1 видео; join + сообщение одновременно → 1)
- Полный прогон: **1573 теста**, 0 failed, 0 регрессий

**Файлы:** `handlers/alan_greeting.py`, `handlers/alan.py`, `tests/test_alan.py`, `tests/test_alan_greeting.py`

---

## ✨ Новое в v2.31.3 (Epic 36)

### Фактчек научился читать альбомы, а промпты — выбирать размер ответа

- **Альбомы (R36-1):** раньше reply «фактчек» на 2-е/3-е фото альбома получал в ответ фирменное «проверять нечего» — Telegram шлёт caption только на ПЕРВОМ фото, а aiogram не собирает альбомы в кучу. Теперь `services/media_group_buffer.py` (TTL 60с, LRU 100) запоминает caption группы из наблюдателя 0a, и фактчек достаёт текст новости, куда бы ты ни ткнул реплаем. Пересланные альбомы по-прежнему вне зоны ответственности — Telegram не сохраняет у них media_group_id, так что им придётся жить со своим репостом без поддержки.
- **Адаптивный размер (R36-2):** жёсткое «строго до 4000 символов» в промптах обоих сервисов заменено блоком «ОБЪЕМ И ДИНАМИЧЕСКИЙ РАЗМЕР ОТВЕТА»: очевидный фейк — короткий плевок на 2-4 предложения, сложный вброс — разбор на пару абзацев с пруфами. Лить воду для объёма теперь запрещено на уровне инструкции — бот отвечает ровно столько, сколько заслуживает тема.

**Тесты: 1573 → 1593 (+20)**
- Буфер альбомов: запись, не-затирание caption, TTL, LRU, изоляция групп (9)
- Observer 0a заполняет буфер и не падает при сбое заполнения (2)
- Фактчек: caption из буфера, приоритет прямого caption, 5.3 при пустоте/TTL/LRU (5)
- Полный Dispatcher-пайплайн: альбом → ровно 1 ответ с текстом новости (2)
- Промпты: дословность блока, подстановка `{max_symbols}` (2)

**Файлы:** `services/media_group_buffer.py` (новый), `handlers/summary.py`, `handlers/factcheck.py`, `services/factcheck_prompts.py`, `services/search_prompts.py`, `tests/test_media_group_buffer.py` (новый), `tests/test_summary_handlers.py`, `tests/test_factcheck_handlers.py`, `tests/test_epic33_router_isolation.py`, `tests/test_factcheck_prompts.py`, `tests/test_smartsearch_prompts.py`, `tests/test_factcheck_service.py`, `tests/test_smartsearch_service.py`, `tests/conftest.py`

---

## ✨ Новое в v2.32.0 (Epic 37)

### SmartModule Expansion: YouTubeSummarizer + WebSummarizer

Бот наконец научился не только искать, но и **смотреть за тебя** — теперь он выжимает ролики и статьи с фирменной ленью и ядом.

**YouTubeSummarizer (R37-1):**
- Триггеры: «транскрипт», «че за видос», «о чем видео», «поясни за видос», «перескажи видос», «че в видосе» (регистронезависимо) + ссылка на `youtube.com/watch?v=`, `shorts/` или `youtu.be/`
- Субтитры тянет через `youtube-transcript-api` (приоритет ru → en → автогенерация), клеит таймкоды `[MM:SS]` и скармливает LLM с токсичным промптом
- Нет субтитров / видео сдохло → порция абьюза из нового пула 5.6 («в этом высере нет субтитров, сиди и слушай ушами» и соседи)

**WebSummarizer (R37-2):**
- Триггеры: «поясни за ссылку», «че по ссылке», «о чем статья», «поясни за статью», «выжимка», «че на сайте», «перескажи статью» + любой http(s)-URL (YouTube-ссылки в веб-парсер не уходят — D128)
- Страницу читает локальный `trafilatura` (markdown), при провале — каскад Tavily `/extract` → Exa `/contents` (порог качества: строго >150 символов, пустые ключи — уровень пропускается)
- Сайт сдох или за пейволлом → пул 5.7 («сайт сдох или закрылся пейволлом, читать нечего»)

**Сценарии вызова:** реплай на сообщение с ссылкой → ответ на исходное сообщение; ссылка+триггер в одном сообщении → ответ на него. Кулдауны раздельные (`YOUTUBE_COOLDOWN_SECONDS` / `WEBPAGE_COOLDOWN_SECONDS`, дефолт 300с) — выжимка ролика не блокирует выжимку статьи.

**Конфиг:** 4 новых env-ключей: `YOUTUBE_MAX_SYMBOLS`, `WEBPAGE_MAX_SYMBOLS` (дефолт 4000), `YOUTUBE_COOLDOWN_SECONDS`, `WEBPAGE_COOLDOWN_SECONDS` (дефолт 300); веб-экстрактор переиспользует `TAVILY_API_KEY`/`EXA_API_KEY` (Epic 33). Роутеры 0e/0f встали после 0d smartsearch, до admin; не-триггер → `UNHANDLED`, пропагация живёт (проверено интеграционными тестами через `Dispatcher.feed_update`).

**Тесты: 1593 → 1757 (+164)**
- URL-экстракция (все формы YouTube, D128-приоритет, чистка пунктуации), transcript-engine (приоритеты языков, формат таймкодов, truncate), экстрактор (каскад trafilatura→Tavily→Exa, порог 150, пустые ключи → skip), промпты байт-в-байт с эталонами Section 46.7, сервисы (пайплайн, XML-экранирование, cleanup), хендлеры (сценарии А/Б/D126, троттлинг, пулы), изоляция роутеров
- Полный прогон: **1757 тестов**, 0 failed, 0 регрессий

**Файлы:** `services/smartmodule_urls.py`, `services/youtube_transcript_engine.py`, `services/web_content_extractor.py`, `services/youtube_prompts.py`, `services/web_prompts.py`, `services/youtube_summarizer_service.py`, `services/web_summarizer_service.py`, `handlers/youtube.py`, `handlers/web.py` (новые), `config/settings.py`, `services/smartmodule_phrases.py`, `bot.py`, `requirements.txt`, `.env.example`, `tests/test_*epic37*` (10 тест-файлов)

---

## ✨ Новое в v2.32.1 (Epic 38)

### WebSummarizer: сторонний reader → trafilatura + Tavily/Exa фолбеки

- **Проблема:** Web-фича мертва на проде — сторонний reader-сервис отдавал 401 (ключ пуст + блок анонимных запросов AS36352), а селектор не вычленял статью («только реклама»).
- **Движок:** интеграция со сторонним reader удалена полностью (движок с ретраями, его исключение и env-ключ выпилены из конфига). Страницу теперь читает локальный `trafilatura` (GET с UA Chrome/122, `follow_redirects`, timeout 10с, `extract` в `asyncio.to_thread`, output markdown, без ссылок/картинок, с таблицами, `favor_precision`), при провале — каскад Tavily `/extract` → Exa `/contents` (timeout 15с). Успех уровня — строго >150 символов (ровно 150 → фолбек); ретраев внутри уровней нет; пустые `TAVILY_API_KEY`/`EXA_API_KEY` → уровень пропускается с WARNING. Все уровни упали → `WebContentExtractionFailedException` → пул 5.7 реплаем на целевое сообщение.
- **Конфиг:** новых env-ключей не добавлено — веб-экстрактор переиспользует `TAVILY_API_KEY`/`EXA_API_KEY` (Epic 33); ключ стороннего reader из `.env` можно вычистить. Триггеры, Reply-To, троттлинг, промпты и пулы — без изменений.

**Тесты:** 1757 → 1763 (+6) (удалены 18 кейсов стороннего reader, добавлены 24 кейса `tests/test_web_content_extractor.py`)

**Файлы:** `services/web_content_extractor.py` (новый), `services/web_summarizer_service.py`, `handlers/web.py`, `bot.py`, `config/settings.py`, `.env.example`, `requirements.txt` (+`trafilatura>=2.2.0,<3.0`), `tests/test_web_content_extractor.py` (новый), `tests/test_settings_helpers.py`, `tests/test_web_summarizer_service.py`, `tests/test_web_handlers.py`; удалены движок стороннего reader и его тест-файл

---

## ✨ Новое в v2.33.0 (Epic 39)

### YouTube-движок: yt-dlp в главной роли, youtube-transcript-api на подхвате

- **Проблема:** YouTube деградирует датацентровый IP прода (TranscriptsDisabled / пустое тело timedtext) — выжимка роликов падала в «нет субтитров», хотя с домашнего IP те же видео работали.
- **Движок:** основной движок теперь `yt-dlp` (качает субтитры ru/en сам, JSON3/VTT/SRT/TTML → нормализация в сегменты, socket_timeout 20с). При любой неудаче (429 на субтитрах, блок IP, сеть) — автоматический фолбек на `youtube-transcript-api`; упали оба → прежний пул 5.6. Приоритет треков прежний: manual ru → manual en → автогенерация ru/en; прочий автогенерат делегируется фолбеку.
- **Конфиг:** 2 новых env-ключа `YOUTUBE_TRANSCRIPT_PROXY_URL` и `YOUTUBE_COOKIES_FILE` (пусто = выключено) — прокси и Netscape-cookies прокидываются в ОБА движка. Значения секретные: в логи пишутся только факты `proxy=set/empty`, `cookies=set/empty`. Триггеры, промпты, пулы, Reply-To и троттлинг — без изменений.

**Тесты:** 1763 → 1789 (+26) (yt-dlp primary/фолбек/оба фейл/ImportError, прокидование proxy/cookies в оба движка, приоритеты треков, делегация приоритета 5, нормализация JSON3/VTT/SRT/TTML/таймкодов, 2 кейса settings; реальная сеть в тестах не дёргается)

**Файлы:** `services/youtube_transcript_engine.py` (единственный меняемый боевой файл), `config/settings.py`, `.env.example`, `requirements.txt` (+`yt-dlp>=2026.7.4`), `tests/test_youtube_transcript_engine.py`, `tests/test_settings_helpers.py`

---

## ✨ Новое в v2.33.1 (Epic 41)

### YouTube-движок: ретраи каскада с токсичным уведомлением, ru-first, статус-логи фолбека

- **Проблема:** живой мониторинг прода показал дефекты каскада — 429 на 'en'-треке валил весь запрос даже при скачанном ru; каскад не ретраил (один транзиентный 429/5xx/сетевой сбой → мгновенный «нет субтитров»); в логах фолбека не было HTTP-статуса/размера тела, в логах хендлера — video_id.
- **Ретраи:** попытка = весь каскад (yt-dlp → transcript-api); 4 ретрая = 5 попыток, backoff 1с/2с/4с/8с (суммарно 15с). Ретраится только транзиентный фейл (429/5xx/403, сеть/прокси, таймауты, «Sign in to confirm you're not a bot», пустой/битый timedtext); перманентный (нет субтитров, видео удалено, 407 от мёртвого прокси) — мгновенный ответ 5.6 без спама. На каждом ретрае — токсичное сообщение в чат из нового пула 5.8 («ютуб опять тупит, пробую выдрать текст еще раз» и 4 соседа), реплаем на целевое сообщение.
- **ru-first:** `ignoreerrors=True` — 429 на 'en' больше не валит скачанный ru; язык без файла пропускается, читается первый читаемый. `extract_info → None` трактуется как транзиентный фейл yt-dlp.
- **Диагностика:** WARNING фолбека теперь содержит `status=` (из HTTPError/регэкспа «HTTP Error NNN») и `body_bytes=`; финальное исключение — детали обоих движков; в 5 лог-строках хендлера добавлен `video_id=%r`. Значения секретные по-прежнему не логируются.

**Тесты:** 1796 → 1854 (+58) (ретраи: 2-попытки-успех, exhausted после 5 попыток с on_retry ровно (1,4)…(4,4) и backoff [1,2,4,8], перманент → 0 ретраев, смешанный транзиент/перманент, on_retry=None, колбэк-исключение не роняет каскад; ru-first: ru скачан+en без файла, все без файла → фолбек без ретраев, пустой ru → en; info=None транзиент; классификация D155 по таблице ~34 кейсов + unwrap __cause__; статус/размер тела в логах (атрибут и регэксп-пути); ignoreerrors в opts; хендлер: on_retry шлёт фразы 5.8 на target; сервис: проброс on_retry; пул 5.8 байт-в-байт; asyncio.sleep в тестах замокан — реальные паузы не случаются)

**Файлы:** `services/youtube_transcript_engine.py`, `services/youtube_summarizer_service.py`, `handlers/youtube.py`, `services/smartmodule_phrases.py`, `tests/test_youtube_transcript_engine.py`, `tests/test_youtube_handlers.py`, `tests/test_youtube_summarizer_service.py`, `tests/test_smartmodule_phrases.py`, `tests/test_epic37_router_isolation.py`

---

## 🩺 Новое в v2.34.0 (Epic 42)

### Checkup: бот лезет в свои логи и орёт на тебя оттуда

Спроси в чате «чекап», «ты в порядке», «живой собака», «пульс бота», «чекни здоровье» или «как сервак» — бот устроит самодиагностику и выдаст токсичный отчёт о своём здоровье реплаем на твой вопрос. «чекапчик» и «живой собакен» пролетают мимо — триггер знает границы слова.

- **Каскад логов:** сначала Betterstack `/api/v2/events` (Bearer-токен, логи за 24ч, фильтр ошибок/ворнингов, пагинация до 5 страниц, потолки 200 событий / 20К символов). Betterstack отвалился — бот честно предупреждает фразой из пула («беттерстак обосрался, лезу ковырять локальные логи...») и читает локальный `journalctl -u admin_bot -n 300`. Сдохли обе ступени — пул полного отказа. Токен — `BETTERSTACK_TOKEN`, а если пусто — существующий `LOGTAIL_SOURCE_TOKEN`.
- **LLM-отчёт:** токсичный DevOps-инженер из промпта разбирает логи, унижая разработчика за кривой код; пусто — с недовольным ебалом признаёт сервак живым. Скрытая приписка при фолбеке напоминает ему поиздеваться над сдохшим облачным мониторингом.
- **Кулдаун** `CHECKUP_COOLDOWN_SECONDS` (300с) per-chat — спам «чекапом» встретит порцию «отъебись от меня». Лимит ответа `CHECKUP_MAX_SYMBOLS=3000`.

**Тесты: 1854 → 1976 (+122, вместе с Epic 43)**: триггеры (регистр/пунктуация/границы), кулдаун per-chat, каскад (MockTransport Betterstack: JSON:API/плоская схема/пагинация/потолки/401/таймаут/битый JSON; journalctl: rc=0/пусто/rc!=0/OSError/таймаут), сервис (промпт, скрытая приписка ровно 1 раз в конец system, cleanup, LLMError), промпты и пулы байт-в-байт с канонами.

**Файлы:** `services/system_logs_fetcher.py`, `services/checkup_service.py`, `services/checkup_prompts.py`, `handlers/checkup.py` (новые), `services/smartmodule_phrases.py`, `config/settings.py`, `.env.example`, `bot.py`, `tests/test_checkup_*` (4 файла)

---

## ℹ️ Новое в v2.34.0 (Epic 43)

### /info + live-редактор /edit_info

- **`/info`** — справка по фичам бота в красивом HTML (`<b>` заголовки, `<code>` команды): фактчек, поиск, YouTube, веб-статьи, Checkup и команды. Команда удаляется из чата сразу; нет прав на удаление — порция возмущения и СТОП. Текст читается из файла `info_text.md` в корне репо (путь — `INFO_TEXT_FILE`), файл создаётся сам с дефолтной справкой при первом старте. Файл поправлен руками мимо редактора → бот молча откатывается на plain-текст, не падая.
- **`/edit_info [текст]`** — только для `ADMIN_USER_ID`: новый текст сначала рендерится превью админу в DM (битая разметка → пул «твой маркдаун говно, телега его не жрет», файл не тронут), и только потом пишется в файл + кэш. Пустой аргумент — показать текущий текст. Команда не удаляется — ответ живёт реплаем.
- **Кулдаун** `/info` — `INFO_COOLDOWN_SECONDS` (300с) per-chat → старый добрый THROTTLE_PHRASES. `/info` добавлена в меню команд вторым пунктом («Справка по фичам бота»).
- `send_chunked_reply` получил опциональный kwarg `parse_mode` — все существующие вызовы работают как раньше.

**Тесты:** 100% покрытие `info_service.py` и `handlers/info.py` (порядок delete→кулдаун→отправка, HTML→plain-фолбек, админ-гейт, DM-превью до сохранения, OSError, ФС-инициализация каноном), канон DEFAULT_INFO_TEXT байт-в-байт, пулы дословно, set_my_commands 1→2, router_count 13→14.

**Файлы:** `services/info_service.py`, `handlers/info.py`, `info_text.md` (новые), `services/smartmodule_utils.py`, `services/bot_commands.py`, `config/settings.py`, `.env.example`, `bot.py`, `tests/test_info_service.py`, `tests/test_info_handlers.py` (новые)

---

## 🔧 Новое в v2.34.1 (Epic 44)

### Новый текст /info + фикс прав удаления + Betterstack Telemetry

- **Новый канон /info:** справка переписана целиком — «Гайд по фичам бота с выходом в сеть internet»: 5 секций (фактчек, поиск, ютуб, веб-страницы, Checkup) с жирными заголовками, `<code>`-триггерами («фактчек», «загугли», «транскрипт», «чекап»…) и кликабельными ссылками. Никаких слеш-команд — только нативные триггеры прямо в диалоге. Канон живёт в `info_text.md` и `DEFAULT_INFO_TEXT` (байт-в-байт, `/edit_info` по-прежнему работает).
- **Фикс /info:** раньше отсутствие прав на удаление команды обрубало логику (пул возмущения и СТОП). Теперь бот шлёт пул «нет прав» И справку — реплаем на висящую в чате команду. Команда удалилась — справка без реплая, как раньше.
- **Betterstack Telemetry (облачная ступень Checkup):** правильный эндпоинт `GET https://telemetry.betterstack.com/api/v2/query/live-tail` (Live Tail Query API v2): Bearer team-токен, follow-redirects, параметры `source_ids`/`batch=100`/`from`/`to`/`query` (опционально), пагинация курсором до 5 страниц, потолки 200 событий / 20К символов. Настройка: `CHECKUP_BETTERSTACK_URL`, `BETTERSTACK_SOURCE_IDS`, `BETTERSTACK_QUERY`. Пустой токен/источники или отказ облака — честный фолбек на `journalctl` (неприкосновенен).

**Тесты: 1976 → 1981 (+5)**: новый канон байт-в-байт (python + html блоки Section 53.3 + суть verbatim R44-1), фикс /info (пул + ПРОДОЛЖЕНИЕ, reply-таргеты, кулдаун), Betterstack live-tail (params страницы 1, плоская схема `data[]`, `pagination.next`, skip при пустых `source_ids`, 401/500/таймаут/битый JSON → фолбек).

**Файлы:** `services/info_service.py`, `handlers/info.py`, `info_text.md`, `services/system_logs_fetcher.py`, `config/settings.py`, `.env.example`, `tests/test_info_service.py`, `tests/test_info_handlers.py`, `tests/test_checkup_logs_fetcher.py`

---

## ✨ Новое в v2.35.0 (Epic 45 + Epic 46)

### Checkup: Betterstack SQL API вместо live-tail + GraphRAG v2

- **Checkup SQL API (Epic 45):** облачная ступень чекапа переведена на SQL/ClickHouse-коннекшн — POST SQL-тела на `https://eu-fsn-3-connect.betterstackdata.com` (Basic auth, `Content-type: plain/text`, формат `JSONEachRow`). Шаблон-канон запроса (`remote(..._logs) UNION ALL s3Cluster(..._s3)` + `ORDER BY dt DESC LIMIT 200`) или полный оверрайд через `CHECKUP_BETTERSTACK_SQL_QUERY`. Настройка: `CHECKUP_BETTERSTACK_SQL_HOST/USER/PASSWORD/TABLE` (креды — только в прод `.env`). Каскад SQL API → `journalctl` остаётся неприкосновенным; обе ступени мертвы — пул полного отказа. Легаси live-tail Epic 44 удалён полностью.
- **GraphRAG v2 (Epic 46):** (1) Fact Extractor — сырая фактура источников (поиск/фактчек/ютуб/веб/саммари) фоново извлекается в граф фактов (`origin`/`expires_at`, TTL 14 дней, chat_history — вечно); токсичные ответы бота в память НЕ сохраняются. (2) Гибридный RAG — перед генерацией бот подмешивает в контекст собственные прошлые знания (векторный KNN по `graph_facts_vec` → FTS5-фолбек) в канон-XML `<context>/<user_gossip>/<bot_knowledge>`; все 5 системных промптов получили RAG-инструкцию. (3) Фиксы: `EMBEDDING_DIM=3072`, ретраи эмбеддингов (403-устойчивость), автопереактивация vec0 (re-probe раз в 10 мин) и ленивый backfill векторов архива; миграция БД (`origin`/`expires_at`, CHECK `entity_type` +`fact`, `PRAGMA user_version=1`, `busy_timeout=5000`) + скрипт `scripts/migrate_graphrag_v2.py` (идемпотентный). Аудит фактчека — `plans/FACTCHECK_AUDIT.md`: выход в сеть гарантирован структурно.

**Тесты: 1981 → 2069 (+88, −0)**: SQL-fetcher (Basic/POST/JSONEachRow/потолки/фолбеки), миграции (origin/expires_at/CHECK/user_version/busy_timeout), Fact Extractor (канон R46-2 байт-в-байт, кривой JSON, embed-фейл → текст), хуки fire-and-forget, RAG-сборка (канон R46-4, префиксы, escape, TTL), промпт-эталоны 55.7.1–55.7.5, фиксы диагностики (ретраи/реактивация/backfill).

**Файлы:** `services/system_logs_fetcher.py`, `services/summary_memory.py`, `services/database.py`, `scripts/migrate_graphrag_v2.py` (новый), 4 сервиса-хуков, `services/summary_generator.py`, 5 промпт-файлов, `config/settings.py`, `.env.example`, `plans/FACTCHECK_AUDIT.md` (новый), хендлеры search/factcheck/youtube/web, `bot.py`.

---

## ✨ Новое в v2.36.0 (Epic 48–51)

### Summary: «LLM или ничего» (Epic 48) — degraded-саммари отменён

Деградированный саммари «выжимка без нейронки» удалён полностью по требованию пользователя. Финальная цепочка: **retry-once** (пауза `SUMMARY_RETRY_ONCE_PAUSE`, 5с) → второй `LLMError` → честное **«не смог сделать саммари потому что упал апи»**. Настройки `SUMMARY_DEGRADED_ENABLED`/`SUMMARY_DEGRADED_COUNT` выпилены из конфига и `.env.example`.

### Checkup: разбор инцидента 400 (Epic 49)

- **Диагностика:** при финальном 4xx (400/404/422 и пр.) в логи уходит ERROR-строка с длиной payload и телом ответа провайдера (≤500 симв., без секретов R17): `LLM HTTP %d | url | request_len | content_chars | num_messages | body_4xx`.
- **Фикс первопричины:** raw-логи проходят scrub управляющих C0-символов (кроме `\n`/`\t` — каждый становится пробелом) и урезаются до `CHECKUP_MAX_INPUT_SYMBOLS=12000` символов (мин 1000). Кириллица не затрагивается.
- **UX-сплит:** пул `CHECKUP_LLM_ERROR_PHRASES` стал чистым LLM-пулом из 4 фраз — «база подавилась логами» архивирована: при падении нейронки фразы про базу больше нет (DoD T-390). Логи `[checkup] LLM failed`/`all log sources failed` — WARNING без traceback.

### DirectChat: бот вливается в разговор (Epic 50) 🆕

Бот отвечает на **прямые обращения** — и только на них (реактивный режим, сам бот никогда не инициирует):

- **Триггеры:** reply на сообщение бота, упоминание `@username` (entities + fallback-текст, регистронезависимо). Команды `/…` не перехватываются.
- **Keyword-триггеры (Epic 52, v2.37.0):** слова «бот»/«ботохуета» и синонимы (ботяра/ботик/ботина) с word-boundary матчингом — «робот», «работа» и «забота» бота не будят, а вот «ботохуета» будит: токен собирается своим куском `бот+охуета`. Ответ — с reply на вызвавшее сообщение. Выключатель `DIRECT_CHAT_BOTWORD_ENABLED` (дефолт `True`). Алан и Костик из keyword-ветки исключены: их «бот»-сообщения уходят своим роутерам (reply/упоминание для них работают как раньше — осознанное обращение не дискриминируется).
- **Token Bucket:** 3 обращения подряд (`CHAT_BURST_LIMIT`), дальше — кулдаун-фраза с реальным оставшимся временем; полное восстановление зарядов через `CHAT_COOLDOWN_SECONDS=300`.
- **Контекст-партишн:** `<RAG_Memory>` (факты DirectChat-диалогов в хронологии), `<Global_Context>` (последние 100 сообщений, ≤4000 симв.), `<Conversation_Thread>` (reply-цепочка глубиной 6, ≤2000 симв.), `<Target_User>` (алиас → имя → юзернейм).
- **Память:** диалоги запоминаются с `origin='bot_direct_reply'` и меткой `target_user`; TTL — `CHAT_DIRECT_REPLY_TTL_DAYS` (пусто = вечное хранение). В чужие пайплайны (summary/фактчек/поиск) direct-флуд НЕ подмешивается.
- **Миграция БД** `user_version` 1→2 (CHECK `graph_facts` + `bot_direct_reply`, колонки `target_user`/`tg_message_id`): идемпотентный скрипт `scripts/migrate_direct_chat_v2.py`, запуск на остановленном боте.
- **Circuit Breaker (Epic 53, v2.38.0):** три транзиентных фейла LLM подряд (5xx/транспорт/таймаут) — и бот на `LLM_CB_COOLDOWN_SECONDS` (300с) перестаёт дёргать LLM, отвечая человечными фразами из пула `CHAT_LLM_DOWN_PHRASES` («мозги временно ушли на профилактику, скоро вернутся» и ещё три). Истёк кулдаун — одна half-open пробная попытка: успех сбрасывает счётчик, фейл — новый кулдаун. 429/4xx/auth не считаются (апстрим жив, это не его вина). Скоуп — только direct_chat.
- **Фоллбэк-провайдер (Epic 53, опциональный):** `LLM_FALLBACK_BASE_URL/MODEL/API_KEY` — пустые по умолчанию (фоллбэк выключен); заданы все три → одна попытка на запасном провайдере после исчерпания primary-ретраев, без ретраев, таймаут 30с. Финальные 5xx логируют тело ответа апстрима (≤500 симв.) — теперь видно, что именно подавилось. **С 2026-08-23 (Epic 54, v2.38.1) фоллбэк включён на проде:** все три переменные заданы в прод `.env` (ключ — от пользователя, значение не публикуем) — если основной канал прилёг, бот пересаживается на запасной стул, а не отвечает «я сломался».

### SmartCache: повторная ссылка — мгновенный ответ (Epic 51) 🆕

- **Exact Match Cache:** хэш `MD5(команда + нормализованный ввод)` → СГЕНЕРИРОВАННЫЙ ответ бота в SQLite (таблица `smart_cache`). Повторная ссылка/запрос за 30 минут (`SMART_CACHE_TTL_SECONDS=1800`) → бот НЕ лезет в Trafilatura/Tavily и НЕ дёргает LLM — моментально отдаёт токсичный ответ из кэша реплаем на текущее сообщение. Лимит 1000 строк (`SMART_CACHE_MAX_ROWS`) с ленивой очисткой старейших. Кэшируются только успешные генерации — пулы кулдауна/ошибок в кэш не попадают.
- **Нормализация URL:** strip `utm_*`/`fbclid`/`gclid`, trailing `/`, регистр хоста, фрагменты. Запрос — lower/strip/схлопывание пробелов.
- **Аварийный рубильник:** `SMART_CACHE_ENABLED=False` по умолчанию — кэш спит, ничего не создаёт. Включение — осознанное решение на проде (деплой T-407).
- **Prompt Caching:** порядок payload «system → алиасы → RAG → динамика» (system всегда на индексе 0) — унифицирован через `build_messages` для DirectChat; существующие генераторы уже соблюдают порядок (guard-тест), тексты канонов не переписывались.

### Новые переменные `.env`

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `CHECKUP_MAX_INPUT_SYMBOLS` | `12000` | Потолок user-входа чекапа после scrub C0 (мин 1000) |
| `CHAT_GLOBAL_CONTEXT_LIMIT` | `100` | Сообщений фона DirectChat `<Global_Context>` |
| `CHAT_BURST_LIMIT` | `3` | Обращений DirectChat подряд до кулдауна |
| `CHAT_COOLDOWN_SECONDS` | `300` | Восстановление зарядов Token Bucket |
| `CHAT_DIRECT_REPLY_TTL_DAYS` | *(пусто)* | TTL памяти direct-диалогов; пусто = вечно |
| `CHAT_GLOBAL_CONTEXT_MAX_CHARS` | `4000` | Потолок `<Global_Context>` |
| `CHAT_THREAD_MAX_DEPTH` | `6` | Глубина reply-цепочки |
| `CHAT_THREAD_MAX_CHARS` | `2000` | Потолок `<Conversation_Thread>` |
| `SMART_CACHE_ENABLED` | `False` | Рубильник Exact Match Cache |
| `SMART_CACHE_TTL_SECONDS` | `1800` | Время жизни кэш-строк (30 мин) |
| `SMART_CACHE_MAX_ROWS` | `1000` | Потолок строк таблицы кэша |

**Тесты: 2069 → 2192 (+123, −3)**: откат degraded (retry×2 → UX R13), 4xx-диаг-лог (тело ≤500, 401 без body-лога), scrub C0/обрезка чекапа, WARNING-уровни, UX-сплит, каноны DirectChat VERBATIM (промпт + пулы), token bucket, контекст-партишн, memorize-метаданные, миграция 1→2 + smoke-скрипт, SmartCache (нормализация/ключи/hit-miss/очистка/no-op-файла), кэш-хиты в хендлерах (LLM не вызывается), guard system@0 для 10 генераторов.

**Файлы:** `services/direct_chat_service.py`, `services/chat_prompts.py`, `services/smart_cache.py`, `services/payload_builder.py`, `handlers/direct_chat.py`, `scripts/migrate_direct_chat_v2.py` (новые), `services/summary_generator.py`, `services/checkup_service.py`, `services/llm_client.py`, `services/smartmodule_phrases.py`, `services/database.py`, `services/summary_memory.py`, `services/smartmodule_utils.py`, `handlers/checkup.py`, `handlers/factcheck.py`, `handlers/search.py`, `handlers/youtube.py`, `handlers/web.py`, `handlers/summary.py`, `config/settings.py`, `.env.example`, `bot.py`.

---

## ✨ Новое в v2.37.0 (Epic 52)

### Alan: реплики снова ироничные, но теперь по рубильнику
Трейдинг-фразы выпилены из `ALAN_REPLIES` как класс. Вместо них бот заинтересованно спрашивает про NixOS и арч («дефолтный префикс на всю жизнь»), нейрокластер, планшет с линуксом, продажу SSD на фоне кризиса цен на память, витамины Life Extension (100500% в составе — бигфарма в шоке), 5-секундные прогулки с гантелями по коридору и реванш за колени с уличным тренажёром. Новый гейт `ALAN_REPLIES_ENABLED` (дефолт `True`, на проде `False`) закрывает ТОЛЬКО reply-блок: счётчик инкрементится всегда, F7v2 greeting работает безусловно.

### Common: два рубильника сочувствия
`COMMON_MEDIA_ENABLED` — глобальный рубильник всех common-медиа (otboy/danger/selfdev/work) в единой точке `CommonRelay.send_common`. `COMMON_WORK_MEDIA_ENABLED` — точечный гейт первой строкой `work_handler` (папка `media/common/work`). Оба дефолт `True`; на проде work-сочувствие отключено — усталые страдают молча, триггеры остаются зарегистрированными.

### Славик: одно сообщение — одно действие
Жёсткий приоритет: dead page > гифка > рандом-медиа > mimic > «пошёл нахуй». Медиа ЗАМЕЩАЕТ фразу, а не копится с ней в одну кучу; на вход в чат — только «ДОЛБОЕВ ВЕРНУЛСЯ»; гифка не шлётся на service-сообщения, а data-флаг `slavik_gif_sent` ставится middleware только при реальной отправке.

### Dead page: детект удаления через InaccessibleMessage
Bot API не шлёт боту delete-updates — поэтому бот пассивно ждёт reply/quote на удалённый репост (в update он прилетит как `InaccessibleMessage`, `date==0`). Сверился с `dead_page_repost_map` — удалил свою dead page; прав нет (403) — одну токсичную фразу из `DEAD_PAGE_DELETE_PHRASES` («снёс репост мёртвой страницы? стыдно стало?» и соседи) с reply. Ровно одно срабатывание на пару (чат, репост), consume вместо UNHANDLED — Славик не получает вдогонку «пошёл нахуй».

### DirectChat: «ботва» и «забота» бота не будят
Новые keyword-триггеры «бот»/«ботохуета» и синонимы (ботяра/ботик/ботина) с word-boundary: «робот»/«работа»/«забота» пролетают мимо (lookbehind/lookahead на страже). Ответ — с reply на вызвавшее сообщение. Флаг `DIRECT_CHAT_BOTWORD_ENABLED` (дефолт `True`); Алан и Костик — вне зоны keyword-ветки, их «бот»-сообщения уходят своим роутерам.

### Новые переменные `.env`

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `ALAN_REPLIES_ENABLED` | `True` | Гейт reply-блока Алана (Epic 52). `False` = реплики молчат, F7v2 greeting жив. На проде `False` |
| `COMMON_MEDIA_ENABLED` | `True` | Глобальный рубильник всех common-медиа (otboy/danger/selfdev/work) |
| `COMMON_WORK_MEDIA_ENABLED` | `True` | Точечный рубильник work-сабдира (`media/common/work`). На проде `False` |
| `DIRECT_CHAT_BOTWORD_ENABLED` | `True` | Keyword-триггеры «бот»-семьи в DirectChat. `False` = только reply/упоминание |

**Тесты: 2205 → 2302 (+97)**, 0 failed, 0 регрессий.

**Файлы:** `handlers/alan.py`, `handlers/slavik.py`, `services/message_counter.py`, `handlers/dead_page_delete.py` (новый), `handlers/dead_page_trigger.py`, `services/dead_page_relay.py`, `handlers/direct_chat.py`, `services/common_relay.py`, `handlers/common.py`, `services/database.py` (таблица `dead_page_repost_map`), `config/settings.py`, `.env.example`, `tests/test_alan.py`, `tests/test_slavik_*.py`, `tests/test_common.py`, `tests/test_dead_page_*.py`, `tests/test_direct_chat*.py`.

---

## ✨ Новое в v2.38.0 (Epic 53)

### Circuit Breaker: бот научился затыкаться на 5 минут, когда его мозг ушёл на обед

Слово «бот» → ReadTimeout → 502 → 502. Расследование показало: апстрим apinet.cloud лежит устойчиво, а ретраи честно долбят мёртвый URL три раза и умирают вместе с ним — retry-storm в чистом виде. Решение — **Circuit Breaker** (`services/llm_circuit_breaker.py`): три транзиентных фейла подряд — и бот на 300 секунд перестаёт дёргать LLM, отвечая на обращения пулом `CHAT_LLM_DOWN_PHRASES` (4 фразы в духе «мозги временно ушли на профилактику, скоро вернутся»). Никаких «внутренняя ошибка базы» — только честное «я сейчас не в ресурсе». Кулдаун истёк — одна half-open пробная попытка: мозг вернулся с обеда → работаем; снова 502 → ещё пять минут тишины. Успех сбрасывает счётчик.

- **Фоллбэк-провайдер (опциональный):** `LLM_FALLBACK_BASE_URL/MODEL/API_KEY` — пустые по умолчанию, то есть выключен; заданы все три → одна попытка на запасном провайдере без ретраев (таймаут 30с). Секрет — только в `.env`.
- **Диагностика 502:** финальные 5xx логируют тело ответа апстрима (≤500 символов) — теперь видно, что именно подавилось, а не только «502» как тонкий намёк.
- Скоуп — только direct_chat: фоновые пайплайны (саммари, memorize) живут своей жизнью и знать не знают ни о каком брейкере.

### Alan: пул переписан, но он пока держит фразы при себе

`ALAN_REPLIES` дорос до **52 фраз**: пять тем (NixOS/Линукс, продажа SSD, витамины Life Extension, 5-секундные прогулки с гантелями, уличный тренажёр + реванш за колени) — по 5 фраз на тему. Присказка «разминался сегодня?…» заменена пулом из 5 издевательско-токсичных вопросов — бот больше не хвастается своим сплитом, а допрашивает Алана о его. Всё отрепетировано, покрыто тестами — и невидимо: `ALAN_REPLIES_ENABLED=False` на проде, Алан по-прежнему страдает молча. Написали, но пока не показываем — как выдержанное вино, только наоборот.

### RESEARCH_HUMAN: ресёрч на человеческом

`plans/RESEARCH_HUMAN.md` — все рекомендации по direct_chat простыми словами: 51 пункт с чекбоксами, никакого LLM-диалекта. Человеческий перевод ресёрча для тех, кто не обязан понимать, что такое «транзиентный фейл». Отметил галочки — выбранное превратится в новый план.

### Новые переменные `.env`

| Переменная | По умолчанию | Что делает |
|-----------|-------------|-----------|
| `LLM_CB_ENABLED` | `True` | Рубильник Circuit Breaker для direct_chat |
| `LLM_CB_FAILURE_THRESHOLD` | `3` | Транзиентных фейлов подряд до открытия цепи |
| `LLM_CB_COOLDOWN_SECONDS` | `300` | Сколько секунд бот «молчит по-человечески» (5 минут) |
| `LLM_FALLBACK_BASE_URL` | *(пусто)* | URL запасного LLM-провайдера. Пусто = фоллбэк выключен |
| `LLM_FALLBACK_MODEL` | *(пусто)* | Модель запасного провайдера |
| `LLM_FALLBACK_API_KEY` | *(пусто)* | Ключ запасного провайдера (секрет — только в `.env`) |

**Тесты: 2302 → 2354 (+52)**, 0 failed, 0 регрессий.

**Файлы:** `services/llm_circuit_breaker.py` (новый), `services/direct_chat_service.py`, `services/llm_client.py`, `services/smartmodule_phrases.py`, `handlers/alan.py`, `config/settings.py`, `.env.example`, `plans/RESEARCH_HUMAN.md` (новый), `tests/test_llm_circuit_breaker.py` (новый), `tests/test_alan.py`, `tests/test_direct_chat.py`, `tests/test_llm_client.py`, `tests/test_smartmodule_phrases.py`, `tests/test_settings_helpers.py`.

---

## ✨ Новое в v2.38.1 (Epic 54, chore)

### Запасной аэродром принят в эксплуатацию

Кода — ноль изменений (тесты всё те же 2354), зато на проде официально **включён фоллбэк-провайдер**: если основной канал прилёг — бот пересаживается на запасной стул, а не заламывает руки с «я сломался». Запасной аэродром — прямой API DeepSeek с той же моделью `deepseek-v4-flash` (совпадает с основной, чтобы нейронка не заметила переезда). Рейс выполняется по расписанию: фоллбэк взлетает только после исчерпания primary-ретраев, одна попытка без ретраев, бюджет 30 секунд — такси ждёт ровно полминуты и уезжает. Embeddings переезжать отказываются: они остаются на apinet.cloud — у эмбеддингов прописка, и они ею дорожат.

| Переменная | Что делает |
|-----------|-----------|
| `LLM_FALLBACK_BASE_URL` | URL запасного аэродрома (прямой API DeepSeek). На проде задан |
| `LLM_FALLBACK_MODEL` | Модель запасного аэродрома (совпадает с основной). На проде задана |
| `LLM_FALLBACK_API_KEY` | Ключ запасного аэродрома. На проде задан пользователем — значение живёт только в прод `.env` и нигде не публикуется |

---

## ✨ Новое в v2.39.0 (Epic 55)

### /info научился хвастаться апдейтами

Справка `/info` дополнена разделом **«6. Что нового (свежие апдейты, о которых ты не спрашивал)»** — бот теперь сам рассказывает, что изменилось: хвастается апдейтами, как будто ты спрашивал. Стиль прежний — иронично-свойский HTML, подзаголовки по версиям и человеческие формулировки, а не changelog для CI:

- **v2.37.0 — «Алан, Славик и немного дисциплины»:** Алану завезли новые темы для трепа (написали, но не показываем — фича выключена), common/work выключено (бот больше не напоминает про работу, и правильно), Славик призван к порядку (одно сообщение — одно действие), месть за удалённый репост и бот-слова в direct_chat.
- **v2.38.0 — «Алан версия 2 и предохранитель»:** Алан v2 — пять тем по пять фраз плюс вопросы-подколы (всё ещё выключен, но уже готов причинять добро); фикс зависаний — circuit breaker: когда нейросеть-провайдер лежит, бот отвечает человечной фразой и молчит пять минут, а не ноет «я сломался».
- **v2.38.1 — «запасной аэродром»:** если основной канал прилёг, бот незаметно пересаживается на прямой API DeepSeek с той же моделью. Ты ничего не заметишь, кроме того, что бот продолжает отвечать.

Никаких слеш-команд, ключей, URL и env-переменных — хвастовство строго в рамках приличия, секреты не публикуются. Существующие 5 разделов справки не тронуты: новый раздел пристроен аккуратно в конец. Канон-цепочка из пяти мест синхронна байт-в-байт: `DEFAULT_INFO_TEXT` = `info_text.md` = ARCHITECTURE 53.3 = backlog «Канон R44-1».

**Тесты: 2354** — не изменились, добавлены только маркеры `test_covers_features` («100500%», «пересаживается», «ботяра»): они следят, чтобы бот не перестал хвастаться.

---

## ✨ Новое в v2.40.0 (Epic 56)

### /info перешёл с кода на цитирование

Код-теги в справке `/info` ушли в небытие: на мобильных Telegram их просто не было видно — `<code>` рисовался невидимкой, и бот цитировал вхолостую. Теперь 27 мест переведены на `<blockquote>`: триггер-слова бота цитируются, как мудрость из паблика, — с бордовой полосой слева, чтобы ни у кого не осталось сомнений в источнике цитаты. Блок-цитаты поддержаны в HTML parse mode с Bot API 7.0 (December 29, 2023); expandable-блоки (Bot API 7.7) сознательно не используются — простой `<blockquote>` читают даже старые клиенты.

### Раздел 6: «Прямое обращение к Богу Машине»

Справка пополнилась разделом **«6. Прямое обращение к Богу Машине»** — от пользователя: три способа достучаться до бота лично — слова-триггеры, реплай на сообщение бота и тег @. Молитвы хором не принимаются: только личное обращение, только хардкор.

Текст справки не тронут ни на байт — заменены только теги (27 пар `<code>`→`<blockquote>`), parse mode остался HTML. Канон-цепочка из пяти мест синхронна: `DEFAULT_INFO_TEXT` = `info_text.md` = ARCHITECTURE 53.3 = backlog «Канон R44-1». Новые счётчики канона: 7 `<b>` / 0 `<code>` / 27 `<blockquote>` / 2 `<a href>`.

**Тесты: 2354** — счётчики `test_html_tags_balanced` переведены с code на blockquote, маркеры `test_covers_features` обновлены («Богу Машине»/«ботохуета» следят, чтобы обращение к Богу Машине не потерялось); 0 регрессий.

---

## 🐛 Известные нюансы

- `DEAD_PAGE_COOLDOWN=0` → спам на каждый репост. Весело, но чревато баном.
- Слава иногда обижается, но всегда возвращается. Это не баг, это фича личности.
- Костик не понимает, за что. И не поймёт.
- Без интернета уровень токсичности падает до нуля. Техническое ограничение вселенной.
- Бот должен быть **админом** приватного канала из `DEAD_PAGE_RELAY_CHANNEL_ID`.
- Приветственное видео (F7) срабатывает при **переходе статуса**. Если Алан уже в чате при старте бота — используй `/alangreet`.
- **F7v2 (silence greeting):** таймер хранится в БД и **переживает рестарт бота**. Если бот перезапустить, Алан не «обнулится» — elapsed считается от реального последнего сообщения.
- При поиске постов в relay-канале используется прогрессивный поиск. Если канал разросся до 2000+ сообщений — бот справится, но не мгновенно.
- **SmartModule:** без `BotFather → /setprivacy → Disable` бот в группе слепой — память пустая, саммари про пустоту. Это не баг, это настройка Telegram.
- **SmartModule:** `location`/`contact`/`dice` и прочие бестекстовые сообщения в память не попадают (осознанно — саммарить кубики нечего). Медиа без подписи попадают как описания (`[фото]` и т.п.).
- **SmartModule:** если sqlite-vec на сервере не завёлся — бот молча переходит на FTS5-поиск архива. В логах будет WARNING, в чате никто ничего не заметит.
- **GraphRAG:** `GRAPH_RAG_ENABLED=False` — аварийный выключатель графа: extraction при архивации не вызывается, `/summary` работает ровно как до Epic 26. Справки из графа пропадают, граф остаётся лежать в БД как приятное воспоминание.
- **Dead page delete (Epic 52):** бот узнаёт об удалении репоста Славика, только когда кто-то ответит или отцитирует удалённое сообщение (delete-updates боту не приходят). Удалил репост тихо, молча, без реплаев — и бот ничего не заметил. Такая вот слежка.
