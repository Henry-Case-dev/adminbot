"""Epic 43 — InfoService (R43-2, Section 52.3): info_text.md + кэш в память.

Epic 85 (84.13, T-638): источник истины — БД/ConfigCache (ключ
content.info_how_it_works, сидится ConfigCache.init при первом старте из
info_text.md); get_text() читает ConfigCache → in-memory кэш → DEFAULT_INFO_TEXT
(legacy-фолбек при PG down, R6). save_text() — единственная точка записи:
in-memory кэш + ConfigCache (хендлер /edit_info и веб-POST /api/info сходятся
в ней) — **PG-only** (F2 10.16): `info_text.md` НЕ перезаписывается в рантайме,
это read-only сид и байт-эталон канона (устраняет дрейф/git-pull-блок).

F2 10.16 (ADR-1016-3): value содержит `canon_version` (код-константа
INFO_CANON_VERSION); ручная правка через save_text версию НЕ повышает — это
drift (`canon_drift`), который лечится явным force-reset (reset_canon) с
бэкапом `prev_html` и аудитом `updated_by`/`updated_at`.
"""
import datetime
import logging
from pathlib import Path

from config.settings import settings
from services import hot_config as hot

logger = logging.getLogger(__name__)

# КАНОН дефолтной справки (Epic 44 R44-1 → … → Epic 71 T-549, Section 53.3 → Epic 83 T-599, Section 83 D306: байт-в-байт c info_text.md) — байт-в-байт тест.
# F7 (10.15, T-1604): текст переписан под канонический реестр F6 (17 префиксных
# команд + bare-исключения «чекап»/«фактчек») и логику Имени (обязательное
# обращение); rich-HTML-структура и фирменный ироничный стиль сохранены.
# Раунд 10.20 / Фаза H (БЛОК 8, ADR-1020-8, T-1929): канон актуализирован под
# фактические фичи фаз B–G — добавлен «Летописец» (БЛОК 1, `compile_lore_story`,
# HTML-история + UPD/«Свежак», флаг ON), обновлён «Фактчек» (доступ к локальным
# тулам + умный выбор источника веб/база чата, развёрнутый вердикт),
# добавлен раздел про безлимиты (`-1` → «Безлимит (∞)», retention `0` →
# «Импорт: Вечно», per-chat override → «Модули → Бюджеты»). Дерзкий ироничный
# tone of voice сохранён (мат не переписывается — политика владельца/ТЗ).
# PREV_DEFAULT_INFO_TEXT — слепок канона ДО F7; PREV_R2020_DEFAULT_INFO_TEXT —
# слепок канона v2 (F7 … до 10.20). Оба нужны идемпотентной миграции
# services/config_cache._migrate_info_how_it_works_v1015, чтобы отличить
# «ещё не обновлённый канон» от ручной правки владельца.
PREV_DEFAULT_INFO_TEXT = """<h1>Гайд по фичам бота. Никаких слеш-команд, всё работает нативно прямо в диалоге.</h1>

<h2>1. Фактчек сообщений и новостей (чтобы чекать репосты Лехи)</h2>
<h5>- Как вызвать: сделай Reply (ответ) на любое сообщение или репост в чате и напиши слово <h4><b><i>фактчек</i></b></h4>.
- С уточнением: если нужно проверить конкретную деталь, допиши вопрос следом.
Например: <h4><b><i>фактчек правда ли склад сгорел?</i></b></h4> или <h4><b><i>фактчек поясни за цифры</i></b></h4>.
Бот поднимет поисковики, проверит достоверность и выдаст вердикт в своем стиле прямо в ответ на исходный пост.</h5>

<h2>2. Поиск инфы (кому лень зайти в гугл во время срача)</h2>
<h5>- Как вызвать: просто начни сообщение со слов <h4><b><i>найди</i></b></h4>, <h4><b><i>поищи</i></b></h4> или <h4><b><i>загугли</i></b></h4> и дальше пиши суть.
- Примеры: <h4><b><i>загугли почему видеокарта греется в простое</i></b></h4> / <h4><b><i>найди последние новости про новый патч</i></b></h4>
Бот соберет факты из сети и пришлет выжимку реплаем на твое сообщение.
Нюансы: На поиск и <h4><b><i>фактчек</i></b></h4> стоят раздельные кулдауны по 5 минут. Если спамить — бот пошлет вас нахуй.</h5>

<h2>3. Пересказ ролика с ютуба (если есть субтитры):</h2>
<h5>Способ 1 (реплай): Ответь на сообщение с ютуб-ссылкой и напиши: <h4><b><i>транскрипт</i></b></h4>, <h4><b><i>че за видос</i></b></h4>, <h4><b><i>о чем видео</i></b></h4>, <h4><b><i>поясни за видос</i></b></h4>.
Способ 2 (одной строкой): Просто кинь ссылку и фразу в одном сообщении (<a href="https://youtu.be/">https://youtu.be/</a>... <b><i>поясни за видос</i></b>).
Бот не распознает само видео, он парсит сабы и пересказывает суть.</h5>

<h2>4. Пересказ веб-страницы</h2>
<h5>Способ 1 (реплай): Ответь на сообщение с ссылкой и напиши: <h4><b><i>поясни за ссылку</i></b></h4>, <h4><b><i>че по ссылке</i></b></h4>, <h4><b><i>о чем статья</i></b></h4>, <h4><b><i>выжимка</i></b></h4>.
Способ 2 (одной строкой): Ссылка + фраза (<a href="https://какой-то-сайт.ru">https://какой-то-сайт.ru</a> <b><i>выжимка</i></b>).
Опять же бот не "видит" веб-страницу, а парсит ее маркдаун версию, пересказывает на свой лад.</h5>

<h2>5. Checkup (Здоровье бота)</h2>
<h5>Хочешь узнать, жив ли бот и сервак? Команда заставить его посмотреть внутрь себя.
Как вызвать: напиши в чат <h4><b><i>чекап</i></b></h4>, <h4><b><i>ты в порядке</i></b></h4>, <h4><b><i>живой собака</i></b></h4> или <h4><b><i>чекни здоровье</i></b></h4>.
Бот залезет в системные логи, найдет свежие ошибки и токсично пояснит, что отвалилось на сервере.</h5>

<h2>6. Прямое обращение к Богу Машине</h2>
<h5>Способ 1 (словами через рот): Бот откликается на <h4><b><i>бот</i></b></h4>, <h4><b><i>ботик</i></b></h4>, <h4><b><i>ботяра</i></b></h4> и <h4><b><i>ботохуета</i></b></h4>. Как вызвать: просто напиши одно из этих слов в чат - бот ответит реплаем на твое сообщение. Робот, работа и ботва не в счет: они его не разбудят.
Способ 2 (реплай): бот отвечает если ответить (Reply) на его сообщение.
Способ 3 (тегнуть): Бот ответит на тег через "@".</h5>

<h2>7. Скачивание видео</h2>
<h5>Способ 1: напиши <h4><b><i>скачай</i></b></h4>, <h4><b><i>загрузи</i></b></h4> или <h4><b><i>стяни</i></b></h4> и кинь ссылку на видео в одном сообщении.
Способ 2 (реплай): Ответь на сообщение с ссылкой на видео и напиши любое из этих слов.
Бот предложит выбрать качество и зальет файл прямо в чат.
Нюансы: Между скачиваниями стоит кулдаун — сервак не бездонный.</h5>

<h2>8. Расшифровка голосовых и кружочков</h2>
<h5>- Как вызвать: никак, тут авто-режим — просто отправь в чат голосовое или кружочек, бот сам пришлет текст реплаем.
Нюансы: Голосовые длиннее 10 минут бот не осилит.</h5>

<h2>9. Саммари чата</h2>
<h5>Способ 1: Команда <h4><b><i>/summary</i></b></h4> отдает выжимку по истории чата за n времени - по дефолту 6 часов (отсчет идет с 00:00 по таймзоне - дефолт пермское время).
Способ 2: Команда /summary + "по Х", например <h4><b><i>/summary по хуям</i></b></h4> - даст выжимку конкретно по упоминаниям хуев в чате за n времени.
Нюансы:
- Кулдаун между каждым вызовом на одного юзера - по дефолту 5 минут.
- Может путать Никит и Глебов</h5>"""

PREV_R2020_DEFAULT_INFO_TEXT = """<h1>Гайд по фичам бота. Никаких слеш-команд, всё работает нативно прямо в диалоге.</h1>

<h2>1. Фактчек сообщений и новостей (чтобы чекать репосты Лехи)</h2>
<h5>Это исключение: команда вызывается одним словом, без обращения.
- Как вызвать: сделай Reply (ответ) на любое сообщение или репост в чате и напиши слово <h4><b><i>фактчек</i></b></h4>.
- С уточнением: если нужно проверить конкретную деталь, допиши вопрос следом.
Например: <h4><b><i>фактчек правда ли склад сгорел?</i></b></h4> или <h4><b><i>фактчек поясни за цифры</i></b></h4>.
Бот поднимет поисковики, проверит достоверность и выдаст вердикт в своем стиле прямо в ответ на исходный пост.</h5>

<h2>2. Поиск инфы (кому лень зайти в гугл во время срача)</h2>
<h5>- Как вызвать: обратись к боту и начни с <h4><b><i>найди</i></b></h4>, <h4><b><i>поищи</i></b></h4> или <h4><b><i>загугли</i></b></h4> и дальше пиши суть.
- Примеры: <h4><b><i>Бот, загугли почему видеокарта греется в простое</i></b></h4> / <h4><b><i>Олег, загугли последние новости про новый патч</i></b></h4>
Бот соберет факты из сети и пришлет выжимку реплаем на твое сообщение.
Нюансы: На поиск и <h4><b><i>фактчек</i></b></h4> стоят раздельные кулдауны по 5 минут. Если спамить — бот пошлет вас нахуй.</h5>

<h2>3. Пересказ ролика с ютуба (если есть субтитры):</h2>
<h5>Способ 1 (реплай): Ответь на сообщение с ютуб-ссылкой и напиши: <h4><b><i>Бот, транскрипт</i></b></h4>, <h4><b><i>Бот, че за видос</i></b></h4>, <h4><b><i>Бот, о чем видео</i></b></h4> или по имени <h4><b><i>Олег, поясни за видос</i></b></h4>.
Способ 2 (одной строкой): Просто кинь ссылку и фразу в одном сообщении (<a href="https://youtu.be/">https://youtu.be/</a>... <h4><b><i>Олег, поясни за видос</i></b></h4>).
Бот не распознает само видео, он парсит сабы и пересказывает суть.</h5>

<h2>4. Пересказ веб-страницы</h2>
<h5>Способ 1 (реплай): Ответь на сообщение с ссылкой и напиши: <h4><b><i>Бот, поясни за ссылку</i></b></h4>, <h4><b><i>Бот, че по ссылке</i></b></h4>, <h4><b><i>Бот, о чем статья</i></b></h4> или по имени <h4><b><i>Олег, выжимка</i></b></h4>.
Способ 2 (одной строкой): Ссылка + фраза (<a href="https://какой-то-сайт.ru">https://какой-то-сайт.ru</a> <h4><b><i>Олег, выжимка</i></b></h4>).
Опять же бот не "видит" веб-страницу, а парсит ее маркдаун версию, пересказывает на свой лад.</h5>

<h2>5. Checkup (Здоровье бота)</h2>
<h5>Хочешь узнать, жив ли бот и сервак? Команда заставить его посмотреть внутрь себя.
Тут два пути. Первый - исключение: напиши в чат <h4><b><i>чекап</i></b></h4> одним словом, без обращения.
Второй - с обращением: <h4><b><i>Бот, ты в порядке</i></b></h4>, <h4><b><i>Бот, живой?</i></b></h4> или <h4><b><i>Бот, чекни здоровье</i></b></h4>.
Бот залезет в системные логи, найдет свежие ошибки и токсично пояснит, что отвалилось на сервере.</h5>

<h2>6. Прямое обращение к Богу Машине</h2>
<h5>Все команды теперь требуют обращения. Если у бота нет имени, пишите <h4><b><i>Бот, скачай</i></b></h4>. Если вы назвали его Олегом, пишите <h4><b><i>Олег, скачай</i></b></h4>.
Способ 1 (словами через рот): пока имя не задано, бот откликается на <h4><b><i>бот</i></b></h4>, <h4><b><i>ботик</i></b></h4> и <h4><b><i>ботяра</i></b></h4>. Как только в настройках Личности появится имя (например, Олег) - дефолтные слова больше не будят бота, он отзывается только на свое имя.
Способ 2 (реплай): бот отвечает если ответить (Reply) на его сообщение.
Способ 3 (тегнуть): Бот ответит на тег через "@".</h5>

<h2>7. Скачивание видео</h2>
<h5>Способ 1: напиши <h4><b><i>Бот, скачай</i></b></h4>, <h4><b><i>Бот, загрузи</i></b></h4> или <h4><b><i>Бот, стяни</i></b></h4> и кинь ссылку на видео в одном сообщении. По имени - <h4><b><i>Олег, загрузи</i></b></h4>.
Способ 2 (реплай): Ответь на сообщение с ссылкой на видео и напиши любую из этих команд.
Бот предложит выбрать качество и зальет файл прямо в чат.
Нюансы: Между скачиваниями стоит кулдаун — сервак не бездонный.</h5>

<h2>8. Расшифровка голосовых и кружочков</h2>
<h5>- Как вызвать: никак, тут авто-режим — просто отправь в чат голосовое или кружочек, бот сам пришлет текст реплаем.
Нюансы: Голосовые длиннее 10 минут бот не осилит.</h5>

<h2>9. Саммари чата</h2>
<h5>Способ 1: Команда <h4><b><i>/summary</i></b></h4> отдает выжимку по истории чата за n времени - по дефолту 6 часов (отсчет идет с 00:00 по таймзоне - дефолт пермское время).
Способ 2: Команда /summary + "по Х", например <h4><b><i>/summary по хуям</i></b></h4> - даст выжимку конкретно по упоминаниям хуев в чате за n времени.
Нюансы:
- Кулдаун между каждым вызовом на одного юзера - по дефолту 5 минут.
- Может путать Никит и Глебов</h5>"""

# Раунд 10.20 / Фаза H (БЛОК 8, ADR-1020-8, T-1929): КАНОН v3 — актуальные
# механики фаз B–G (Летописец / Фактчек с локальными тулами / безлимиты).
DEFAULT_INFO_TEXT = """<h1>Гайд по фичам бота. Никаких слеш-команд, всё работает нативно прямо в диалоге.</h1>

<h2>1. Фактчек сообщений и новостей (чтобы чекать репосты Лехи)</h2>
<h5>Это исключение: команда вызывается одним словом, без обращения.
- Как вызвать: сделай Reply (ответ) на любое сообщение или репост в чате и напиши слово <h4><b><i>фактчек</i></b></h4>.
- С уточнением: если нужно проверить конкретную деталь, допиши вопрос следом.
Например: <h4><b><i>фактчек правда ли склад сгорел?</i></b></h4> или <h4><b><i>фактчек поясни за цифры</i></b></h4>.
Раньше бот просто поднимал поисковики. Теперь он сам решает, где искать правду: мировые новости и науку проверит по сети, а если спорят, кто что сказал и когда - залезет в вашу же историю чата. Вытащит точную дату, автора и пометку "переслано: откуда", ткнёт вас носом в факты и выдаст развёрнутый вердикт, а не два предложения для галочки.</h5>

<h2>2. Поиск инфы (кому лень зайти в гугл во время срача)</h2>
<h5>- Как вызвать: обратись к боту и начни с <h4><b><i>найди</i></b></h4>, <h4><b><i>поищи</i></b></h4> или <h4><b><i>загугли</i></b></h4> и дальше пиши суть.
- Примеры: <h4><b><i>Бот, загугли почему видеокарта греется в простое</i></b></h4> / <h4><b><i>Олег, загугли последние новости про новый патч</i></b></h4>
Бот соберет факты из сети и пришлет выжимку реплаем на твое сообщение.
Нюансы: На поиск и <h4><b><i>фактчек</i></b></h4> стоят раздельные кулдауны по 5 минут. Если спамить — бот пошлет вас нахуй.</h5>

<h2>3. Летописец (бот надиктует историю чата)</h2>
<h5>- Как вызвать: обратись к боту и попроси рассказать. Например: <h4><b><i>Олег, поясни за литрбол</i></b></h4>, <h4><b><i>Бот, расскажи историю про кухню</i></b></h4> или <h4><b><i>Бот, что там у нас с ремонтом</i></b></h4>.
Не путай с быстрой справкой: односложный вопрос бот разрулит и без Летописца. А вот когда просишь целую сагу про мем, конфликт или целую эпоху чата - врубается Летописец.
Что вернётся: саркастичная история с именами, цитатами и хронологией - от завязки до нынешнего статус-кво. Оформление жирным и курсивом, без markdown-мусора.
- Спросишь про то же самое второй раз: бот не станет пережёвывать старое заново, а допишет блок <h4><b><i>UPD (Свежак):</i></b></h4> - что нового случилось с прошлого рассказа.
- Фича включена по умолчанию. Если админ её выключит, про Летописца можно забыть, остальное работает как раньше.</h5>

<h2>4. Пересказ ролика с ютуба (если есть субтитры):</h2>
<h5>Способ 1 (реплай): Ответь на сообщение с ютуб-ссылкой и напиши: <h4><b><i>Бот, транскрипт</i></b></h4>, <h4><b><i>Бот, че за видос</i></b></h4>, <h4><b><i>Бот, о чем видео</i></b></h4> или по имени <h4><b><i>Олег, поясни за видос</i></b></h4>.
Способ 2 (одной строкой): Просто кинь ссылку и фразу в одном сообщении (<a href="https://youtu.be/">https://youtu.be/</a>... <h4><b><i>Олег, поясни за видос</i></b></h4>).
Бот не распознает само видео, он парсит сабы и пересказывает суть.</h5>

<h2>5. Пересказ веб-страницы</h2>
<h5>Способ 1 (реплай): Ответь на сообщение с ссылкой и напиши: <h4><b><i>Бот, поясни за ссылку</i></b></h4>, <h4><b><i>Бот, че по ссылке</i></b></h4>, <h4><b><i>Бот, о чем статья</i></b></h4> или по имени <h4><b><i>Олег, выжимка</i></b></h4>.
Способ 2 (одной строкой): Ссылка + фраза (<a href="https://какой-то-сайт.ru">https://какой-то-сайт.ru</a> <h4><b><i>Олег, выжимка</i></b></h4>).
Опять же бот не "видит" веб-страницу, а парсит ее маркдаун версию, пересказывает на свой лад.</h5>

<h2>6. Checkup (Здоровье бота)</h2>
<h5>Хочешь узнать, жив ли бот и сервак? Команда заставить его посмотреть внутрь себя.
Тут два пути. Первый - исключение: напиши в чат <h4><b><i>чекап</i></b></h4> одним словом, без обращения.
Второй - с обращением: <h4><b><i>Бот, ты в порядке</i></b></h4>, <h4><b><i>Бот, живой?</i></b></h4> или <h4><b><i>Бот, чекни здоровье</i></b></h4>.
Бот залезет в системные логи, найдет свежие ошибки и токсично пояснит, что отвалилось на сервере.</h5>

<h2>7. Прямое обращение к Богу Машине</h2>
<h5>Все команды теперь требуют обращения. Если у бота нет имени, пишите <h4><b><i>Бот, скачай</i></b></h4>. Если вы назвали его Олегом, пишите <h4><b><i>Олег, скачай</i></b></h4>.
Способ 1 (словами через рот): пока имя не задано, бот откликается на <h4><b><i>бот</i></b></h4>, <h4><b><i>ботик</i></b></h4> и <h4><b><i>ботяра</i></b></h4>. Как только в настройках Личности появится имя (например, Олег) - дефолтные слова больше не будят бота, он отзывается только на свое имя.
Способ 2 (реплай): бот отвечает если ответить (Reply) на его сообщение.
Способ 3 (тегнуть): Бот ответит на тег через "@".</h5>

<h2>8. Скачивание видео</h2>
<h5>Способ 1: напиши <h4><b><i>Бот, скачай</i></b></h4>, <h4><b><i>Бот, загрузи</i></b></h4> или <h4><b><i>Бот, стяни</i></b></h4> и кинь ссылку на видео в одном сообщении. По имени - <h4><b><i>Олег, загрузи</i></b></h4>.
Способ 2 (реплай): Ответь на сообщение с ссылкой на видео и напиши любую из этих команд.
Бот предложит выбрать качество и зальет файл прямо в чат.
Нюансы: Между скачиваниями стоит кулдаун — сервак не бездонный.</h5>

<h2>9. Расшифровка голосовых и кружочков</h2>
<h5>- Как вызвать: никак, тут авто-режим — просто отправь в чат голосовое или кружочек, бот сам пришлет текст реплаем.
Нюансы: Голосовые длиннее 10 минут бот не осилит.</h5>

<h2>10. Саммари чата</h2>
<h5>Способ 1: Команда <h4><b><i>/summary</i></b></h4> отдает выжимку по истории чата за n времени - по дефолту 6 часов (отсчет идет с 00:00 по таймзоне - дефолт пермское время).
Способ 2: Команда /summary + "по Х", например <h4><b><i>/summary по хуям</i></b></h4> - даст выжимку конкретно по упоминаниям хуев в чате за n времени.
Нюансы:
- Кулдаун между каждым вызовом на одного юзера - по дефолту 5 минут.
- Время в диалоге бот называет по часовому поясу чата (настройка «Часовой пояс чата»), а не по своему внутреннему будильнику.
- Может путать Никит и Глебов</h5>

<h2>11. Безлимиты (когда цифры — для слабаков)</h2>
<h5>Лимиты настраиваются на трёх уровнях, и главный всегда тот, что ближе к чату: настройка чата → глобальная настройка → заводской дефолт.
- <b>Безлимит (∞):</b> ставишь в лимит <b><i>-1</i></b> - и бот перестаёт считать. Для контекста при этом остаётся потолок безопасности, чтобы модель не сожрала сама себя.
- <b>Импорт: Вечно:</b> срок хранения <b><i>0</i></b> значит "хранить навсегда", а не "удалить немедленно". Ноль тут - это бесконечность, привыкай.
- Где крутить: <b>Модули → Бюджеты</b>. Там же тумблер «Безлимит по чату», который разом ставит <b><i>-1</i></b> по всем ключам.
- В «Сводке» такой режим подписан как <b>Безлимит (∞)</b>, а не как честные 100% из 100%.</h5>"""

INFO_KEY = "content.info_how_it_works"

# ── F2 (guide-delivery-round1016, ADR-1016-3): версионирование канона ───────
# INFO_CANON_VERSION описывает КОД-канон DEFAULT_INFO_TEXT (1 = до F7/10.15,
# 2 = канон F7: 17 префиксных команд + bare-исключения «чекап»/«фактчек»,
# 3 = канон 10.20/Фаза H (БЛОК 8): Летописец + актуальный Фактчек + безлимиты).
# ПРАВИЛО: любая правка DEFAULT_INFO_TEXT обязана (1) бампнуть INFO_CANON_VERSION,
# (2) добавить ПРЕЖНИЙ текст в KNOWN_INFO_SNAPSHOTS, (3) сохранить байт-тест
# DEFAULT_INFO_TEXT ↔ info_text.md. Иначе прод-миграция не узнает прежний слепок.
INFO_CANON_VERSION = 3
# Реестр известных прошлых канонов (нормализованное сравнение) — по ним
# идемпотентная миграция ConfigCache безопасно обновляет прод-PG.
KNOWN_INFO_SNAPSHOTS: tuple[str, ...] = (
    PREV_DEFAULT_INFO_TEXT,          # v1 — до F7 (10.15)
    PREV_R2020_DEFAULT_INFO_TEXT,    # v2 — F7 (10.15) … до 10.20
)


def normalize_canon(text: str) -> str:
    """Нормализация rich-HTML для сравнения канонов: EOL → \\n, rstrip строк,
    strip по краям. Устраняет дрейф пробелов/переводов строк (прод-кейс F7:
    текст был whitespace-вариантом PREV → байтовое сравнение не ловило)."""
    normalized = text.replace("\r\n", "\n").replace("\r", "\n")
    return "\n".join(line.rstrip() for line in normalized.split("\n")).strip()


def canon_drift(html: str | None) -> bool:
    """True, если текущий текст НЕ является код-каноном (ручная правка/дрейф).
    Пустой/нестроковый текст считаем дрейфом (справка не соответствует канону)."""
    if not isinstance(html, str):
        return True
    return normalize_canon(html) != normalize_canon(DEFAULT_INFO_TEXT)

# ── F6 (help-guide-integration-round1014, spec §2.2): гайд в БД ─────────────
# Файл — ТОЛЬКО источник идемпотентного сида (ConfigCache.init), не источник
# истины. Ручные правки из админки живут в PG и сидом не перезатираются.
GUIDE_KEY = "content.intelligence_guide"
# M2-фикс: абсолютный путь от корня проекта (устойчив к CWD systemd/docker).
_PROJECT_ROOT = Path(__file__).resolve().parents[1]
GUIDE_SEED_FILE = str(
    _PROJECT_ROOT / "plans" / "docs" / "intelligence_user_guide.md")
DEFAULT_GUIDE_MARKDOWN = ""   # R6 fail-open: PG down и файла нет → пусто


def _read_text(path: str) -> str:
    """Чтение текстового файла; OSError → '' (fail-open, без исключений)."""
    try:
        with open(path, encoding="utf-8") as fh:
            return fh.read()
    except OSError:
        logger.warning("[info service] read failed | file=%s", path,
                       exc_info=True)
        return ""


class InfoService:
    def __init__(self, file_path: str | None = None) -> None:
        # Путь резолвится в момент ВЫЗОВА (не в дефолте сигнатуры): default-
        # выражения функций фиксируются при определении класса — monkeypatch
        # settings в тестах и единый источник для web-POST (F5) работают.
        self._file_path = file_path if file_path is not None \
            else settings.INFO_TEXT_FILE
        self._cache: str | None = None

    def load(self) -> None:
        """Чтение сида при старте. F2 10.16 (ADR-1016-3 §4): `info_text.md` —
        READ-ONLY сид/байт-эталон; рантайм его НЕ создаёт и НЕ перезаписывает.
        Файла нет/пустой/OSError → кэш = DEFAULT_INFO_TEXT (без записи)."""
        try:
            with open(self._file_path, encoding="utf-8") as fh:
                text = fh.read()
        except FileNotFoundError:
            self._cache = DEFAULT_INFO_TEXT
            logger.info("[info service] seed file missing → in-memory canon "
                        "(файл НЕ создаётся) | file=%s", self._file_path)
        except OSError:
            logger.warning("[info service] read failed → in-memory default | file=%s",
                           self._file_path, exc_info=True)
            self._cache = DEFAULT_INFO_TEXT
        else:
            if text.strip():
                self._cache = text
            else:
                self._cache = DEFAULT_INFO_TEXT
                logger.warning("[info service] empty seed → in-memory canon "
                               "(файл НЕ перезаписан) | file=%s", self._file_path)

    def get_text(self) -> str:
        """84.13.3 (T-638): ConfigCache → in-memory кэш → DEFAULT_INFO_TEXT.
        Источник истины — БД; при PG down/нет ключа — legacy-фолбек (R6)."""
        cached_value = hot.get(INFO_KEY)
        if isinstance(cached_value, dict):
            html = cached_value.get("html")
            if isinstance(html, str) and html.strip():
                return html
        if self._cache is not None:
            return self._cache
        return DEFAULT_INFO_TEXT

    async def save_text(self, text: str, updated_by: int | None = None,
                        cache=None) -> dict:
        """F2 10.16 (ADR-1016-3 §4): ЕДИНСТВЕННАЯ точка записи — PG-only.
        `info_text.md` НЕ пишется (read-only сид; pull не блокируется).
        Ручная правка НЕ повышает `canon_version` (версия описывает код-канон;
        ручной текст = drift → лечится reset_canon). ВЫЗЫВАТЬ ТОЛЬКО ПОСЛЕ
        успешного превью (D163). PG недоступен → ConfigCacheUnavailableError
        (роут/handler → 503/фраза; локально-только НЕ пишем — иначе прод и
        локалка расходятся). In-memory кэш меняется только при успехе."""
        from services.config_cache import ConfigCacheUnavailableError

        target = cache if cache is not None else hot.get_config_cache()
        if target is None or not getattr(target, "pg_available", False):
            raise ConfigCacheUnavailableError("PostgreSQL недоступен (R6)")
        value = {
            "html": text,
            "canon_version": INFO_CANON_VERSION,
            # Ревью-итер.1 (High F2): ручная правка НЕ должна откатываться
            # одноразовой форс-доставкой на следующем старте — маркер
            # доставки текущей версии сохраняем/выставляем.
            "canon_delivered_version": INFO_CANON_VERSION,
            "updated_at": datetime.datetime.now(
                datetime.timezone.utc).isoformat(),
            "updated_by": (updated_by if updated_by is not None
                           else settings.ADMIN_USER_ID),
        }
        await target.set(INFO_KEY, value, "content")
        self._cache = text
        logger.info("[info service] info saved (PG-only) | chars=%d | by=%s",
                    len(text), value["updated_by"])
        return value

    async def reset_canon(self, updated_by: int | None = None,
                          cache=None) -> dict:
        """F2 10.16 (ADR-1016-3 §3): явный force-reset справки к код-канону.
        Бэкап текущего текста — `prev_html`/`prev_updated_at` внутри значения;
        аудит — `updated_by` (id, R16) + `updated_at`. Без PG → исключение
        (роут → 503). Откат: повторный reset/`save_text(prev_html)`."""
        from services.config_cache import ConfigCacheUnavailableError

        target = cache if cache is not None else hot.get_config_cache()
        if target is None or not getattr(target, "pg_available", False):
            raise ConfigCacheUnavailableError("PostgreSQL недоступен (R6)")
        current = target.get(INFO_KEY)
        value = {
            "html": DEFAULT_INFO_TEXT,
            "canon_version": INFO_CANON_VERSION,
            "canon_delivered_version": INFO_CANON_VERSION,
            "updated_at": datetime.datetime.now(
                datetime.timezone.utc).isoformat(),
            "updated_by": (updated_by if updated_by is not None
                           else settings.ADMIN_USER_ID),
        }
        if isinstance(current, dict):
            prev_html = current.get("html")
            if isinstance(prev_html, str):
                value["prev_html"] = prev_html
            prev_updated_at = current.get("updated_at")
            if prev_updated_at:
                value["prev_updated_at"] = prev_updated_at
        await target.set(INFO_KEY, value, "content")
        self._cache = DEFAULT_INFO_TEXT
        logger.info("[info service] canon force-reset | by=%s | prev_backed_up=%s",
                    value["updated_by"], "prev_html" in value)
        return value

    # ── F6 (10.14): гайд по возможностям (Markdown, PG-only) ───────────────

    def get_guide(self) -> dict:
        """Гайд из ConfigCache (PG); PG down/нет ключа → сид-файл (код-канон)
        → пусто. fail-open, 200, без исключений (spec §2.3)."""
        cached = hot.get(GUIDE_KEY)
        if isinstance(cached, dict):
            markdown = cached.get("markdown")
            if isinstance(markdown, str) and markdown.strip():
                return {
                    "markdown": markdown,
                    "updated_at": cached.get("updated_at"),
                    "updated_by": cached.get("updated_by"),
                }
        return {
            "markdown": _read_text(GUIDE_SEED_FILE) or DEFAULT_GUIDE_MARKDOWN,
            "updated_at": None,
            "updated_by": None,
        }

    async def save_guide(self, markdown: str,
                         updated_by: int | None = None) -> dict:
        """F6: ЕДИНСТВЕННАЯ точка записи гайда (POST /api/info/guide).
        Значение → ConfigCache (PG) + in-memory. Без PG — наверх (роут → 503)."""
        from services.config_cache import ConfigCacheUnavailableError

        value = {
            "markdown": markdown,
            "updated_at": datetime.datetime.now(
                datetime.timezone.utc).isoformat(),
            "updated_by": (updated_by if updated_by is not None
                           else settings.ADMIN_USER_ID),
        }
        cache = hot.get_config_cache()
        if cache is None or not cache.pg_available:
            raise ConfigCacheUnavailableError("PostgreSQL недоступен (R6)")
        await cache.set(GUIDE_KEY, value, "content")
        logger.info("[info service] guide saved | chars=%d | by=%s",
                    len(markdown), value["updated_by"])
        return value
