# Guard-прогон F23 `budget-guardrails-round1024` (локальный артефакт, T-2379)

> **Фича:** F23 · **Итерация:** review iter1 · **Родительский коммит:** `379cfdd` (iter0)
> **Окружение:** Windows, `.venv` Python 3.12.0
> **Назначение:** артефакт guard-прогона для критериев приёмки T-2379 (без секретов).
> **Статус:** локальная часть выполнена @Builder; финальный прод-прогон @DevOps — за @DevOps (локально PG нет).

## Команды и результаты

| Проверка | Команда | Результат |
|---|---|---|
| Полный pytest | `.venv\Scripts\python.exe -m pytest -q` | **7911 passed**, 1 warning, 101.47s — 0 failed |
| F23-тест | `pytest tests/test_budget_guardrails_round1024.py -q` | **25 passed** |
| JS-синтаксис | `node --check web/app.js` | **OK** |
| JS-витрина бюджета | `node tests/js/round1024_budget_toggle_test.js` | **BUDGET-TOGGLE-OK** (13 карточек / 26 вкладок) |
| Пробелы | `git diff --check` | **чисто** |

## Инварианты (подтверждены прогоном)

- Δ каталога = 0, Δ DDL = 0 — F23 меняет только тесты/документы.
- `tma-menu-freeze` не снят: состав `MODULES` (13) / `TABS` (26) заморожен точными списками.
- R16/R17/R18 — чисто; секреты не цитируются (R17-редакция).
- F21/F22 не сломаны (полный прогон включает их тесты).

## Прод-часть (за @DevOps)

- Read-only аудит/ремонт на PG (`audit-chat-overrides`, `apply-chat-overrides`) — prod-only (PG слушает `127.0.0.1`).
- Kill-switch/OFF-поведение на целевом чате — по регламенту @DevOps, без секретов.

_Артефакт подготовлен @Builder. Секреты и реальные значения ключей не цитировались._
