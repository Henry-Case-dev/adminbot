# F10 — своды §75 (карта токенов) и §76 (мониторинг интеллекта) (T-3110/T-3111)

> **Тип:** свод-верификация. Фичевые тесты **не дублируются** — F10 прогоняет/сводит
> и ссылается на артефакты F6/F11 (ADR-1025-24 D7). Дефекты, если найдены,
> заводятся отдельной фичей, а не «правятся» внутри F10.

## §75 — интерактивная карта LLM-вызовов (F6)

Источник фичи: `plans/archive/memory-analytics-reorg-round1025/`
(ADR-1025-19, `spec.md`, `evidence.md`, `review.md`).

| Требование §75 | Покрытие | Артефакт/тест |
|---|---|---|
| Периоды: последний вызов / день / неделя / месяц | F6-режимы карты (агрегированный период vs последний вызов) | `tests/js/round1025_f6_analytics_memory_test.js` |
| Выбор L1 / L2 / модели / модуля | фильтры/дерево вызовов | `tests/js/round1025_f6_execution_graph_test.js` |
| Неизвестная стоимость → «Нет данных» (не $0) | adapter-нормализация | F6 adapter (`adr-1025-19`) + JS-тест |
| Неограниченный бюджет → «Без лимита» | `budgetInfo` | F6 + F11-бюджеты |
| Отсутствие фиктивных этапов | adapter не создаёт узлы без данных | `round1025_f6_execution_graph_test.js` |
| Мобильное представление (вертикальная последовательность) | F6 mobile-карта | JS-тест + §71-матрица (overflow=0) |
| Adapter одно-/двухслойных вызовов | `execution_graph.js` adapter | `tests/js/round1025_f6_execution_graph_test.js` |

F10-проверка: JS-тесты F6 включены в общий прогон (`pytest -q` → **8463 passed**),
дублей не создавалось. Превью последнего вызова на «Статусе» (§21) подтверждено
матрицей F11 (§71, `#/` → `.exec-preview`).

## §76 — мониторинг интеллекта (F11)

Источник: `plans/reports/round1025_f11_verification.md` (T-3085/§18) +
`plans/archive/status-showcase-dashboard-round1025/`.

| Требование §76 | Покрытие | Артефакт |
|---|---|---|
| Убеждения (живая лента) | `cognitionBeliefsLoop` + `_ribbonLoop`, `ribbon-ts` | F11 §18-таблица |
| Парадигмы (история + причина отсутствия) | `cognitionParadigmsLoop` + `ribbon-empty__reason` | F11 §18-таблица |
| Эволюция характера | `cognitionTraitsLoop` + `_traitsAdapter`, `personaExtractorBadge` | F11 §18-таблица |
| Живая лента досье («Новые факты») | `factsFeed` (reuse `dossierFeed`), §19 | F11 §11-таблица |
| Обычный/глубокий сон + бейджи обратного отсчёта | `dreamPhaseBadge`/`deepPhaseBadge`/`sleepWidget` | F11 §11-таблица |
| Позиция прокрутки не сбрасывается при новых событиях | CSS `ribbon-scroll` (transform, не скролл-контейнер) | F11 §18 + JS-тест F11 |

F10-проверка: живые пробы §76 (`.status-grid`, `.status-sleep`, `.status-facts`,
`.exec-preview`) в матрице §71 — 0 нарушений; сохранность виджетов подтверждена
F11 (`0 удалённых виджетов`). Дублей не создавалось.

## Итог

§75 и §76 закрыты **ссылками на F6/F11** + зелёным общим прогоном. Собственных
дефектов F10 не выявил. **НЕ принято:** новых дефектов нет; живая приёмка TMA —
PENDING (владелец).
