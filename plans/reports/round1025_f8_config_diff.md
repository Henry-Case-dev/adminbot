# F8 — diff конфигурации «до/после» по 8 срезам §3

> **Фича:** F8 `parameter-registry-widget-map-round1025` (ADR-1025-21 D4). **APP_VERSION:** `2.58.15`. **Метка:** F8 offline-baseline (read-only; живые PG-снимки — T-2998/T-3000).
> **Метод:** классификация по ключам (`pg_key`/`chat_id`), R17-safe (секреты — только `{configured,last4}`). Живые снимки снимает @DevOps существующим контуром (`manage.py`/`var/backups/`/`.env.bak`); в git — только этот отчёт-статусы.
> **Ожидание F8:** 0 незапланированных изменений (read-only enabler; Δ каталога=0, Δ DDL=0).

## Сводка по срезам

| срез | added | removed | changed | unchanged | вердикт |
|---|---|---|---|---|---|
| global | 0 | 0 | 0 | 337 | OK (без изменений) |
| chats | 0 | 0 | 0 | 0 | OK (без изменений) |
| dm | 0 | 0 | 0 | 0 | OK (без изменений) |
| permsoc | 0 | 0 | 0 | 0 | OK (без изменений) |
| prompts | 0 | 0 | 0 | 21 | OK (без изменений) |
| models | 0 | 0 | 0 | 56 | OK (без изменений) |
| connections | 0 | 0 | 0 | 20 | OK (без изменений) |
| roles | 0 | 0 | 0 | 10 | OK (без изменений) |
| **ИТОГО** | **0** | **0** | **0** | **444** | **OK** |

## Детали изменений (по ключам)

Изменений нет: все ключи всех 8 срезов классифицированы как `unchanged` → значения, которые пользователь не менял, остались прежними.

## R17

- Секреты во всех срезах — только `{configured,last4}`; открытых значений нет.
- Инструмент дополнительно маскирует «похожее на секрет» при чтении снимков.

## Отсутствие скрытых миграций

- Δ каталога = 0 (снимок ключей совпадает; см. `param-registry-round1025.meta.md`).
- Δ DDL = 0 (схема/миграции не менялись; см. `round1025_f8_results.md`).
