# F2 — `design-tokens-liquidglass-v2-round1025` (Эпик 1)

> **ТЗ:** `plans/current_task.md` §8, §9, §10, §70, §117 (п.8). **Тип:** визуальная система (web/CSS).
> **Приоритет:** P0. **Зависит от:** F1 (shell). **От F2 зависят:** F4, F11.
> **Статус:** 🟦 Step 1 @PM — заготовка. **Детализация на Шаге 1 при старте фичи** (после приёмки F1; `spec.md`/ADR — Step 2 @Architect).
> **SUPERSEDE:** палитра OD4 (`#161616`/`#14CBB6`, 10.20) и градиент `#FF8A3D` (10.20-UPD3) → палитра §8; AMEND ADR-1020-9. Инвариант CSP/zero-build (`tests/js/vue_mount_test.js`).

## Верхнеуровневые задачи (детализация при старте)
- [ ] Токены палитры §8 (`#090D17`/`#151B2A`/`#1C2537`/`#F4F7FB`/`#AAB6C8`/`#42D6C4`/`#A78BFA`/`#77A8FF`/`#F6C56F`/`#F07178`), контроль контраста, иерархия кнопок (осн./второст./удаление).
- [ ] Liquid Glass §9 — уровни **A** (настоящее преломление `feDisplacementMap`), **B** (blur+блики), **C** (непрозрачный fallback); преломление сильнее у краёв; реальная проверка Chromium + WebKit; без WebGL-рендера на каждой карточке.
- [ ] Избирательное применение: sidebar/header/селектор/hero Статуса/карточка графа/крупные виджеты; **НЕ** применять к textarea/таблицам прав/логам/длинным формам/редакторам промптов; текст всегда резкий.
- [ ] Анимированный фон §10: замедление ≥1.5×, 60–90 с (вторичные слои 90–120 с), цвета бирюза/индиго/фиолет/синий, без постоянно яркой оранжевой области; анимация `transform`/`opacity`; приостановка при скрытии приложения.
- [ ] `prefers-reduced-motion` §10/§70.
- [ ] Проверка Liquid Glass §117 п.8 + скриншоты.

## Зависимости / ступень
- Ступень web: F1 → **F2** → F3 → F4 → …
- Риск: нарушение CSP/zero-build тяжёлой библиотекой (**High**); искажение текста glass (**High**); производительность тяжёлого blur всего viewport (**Medium**).
