# F8 — `parameter-registry-widget-map-round1025` (Эпик 1, enabler)

> **ТЗ:** `plans/current_task.md` §1, §2, §3, §69, §117 (п.1–4). **Тип:** аудит + сохранность данных (enabler).
> **Приоритет:** P0. **Зависит от:** — (**Wave 0**, стартует первым вместе с F1). **Читается:** F1 (инвентарь), далее — все фичи.
> **Статус:** 🟦 Step 1 @PM — заготовка. **Детализация на Шаге 1 при старте фичи** (Wave 0; `spec.md`/ADR — Step 2 @Architect).
> **Инвариант:** ни один существующий параметр не остаётся без нового места (§1/§2/§79/§116); никаких скрытых миграций.

## Верхнеуровневые задачи (детализация при старте)
- [ ] Обязательный аудит репозитория §2: маршруты, компоненты, формы, конфигурационные параметры, API чтения, API сохранения, модели/провайдеры, промпты, источники телеметрии, виджеты Статуса/Сводки, механизмы наследования, генеральные переключатели модулей, локальные функции PERMsoc, права доступа, механизм публикации Саммари.
- [ ] **Полный реестр параметров** §2: для каждого — `internal_key`/`display_name`/`description`/`data_type`/`current_value`/`default_value`/`scope`/`inheritance`/`read_api`/`write_api`/`validation`/`permissions`. **Секреты — без вывода открытого значения.**
- [ ] **Карта «Старый экран → Параметр → Новый экран → API»** §2: ни один параметр не без нового места; неизвестный параметр — сохранить и внести в реестр.
- [ ] **Карта виджетов** §2: «Старый виджет → Источник данных → Новое расположение → Сохранённые действия»; не считать данные сохранёнными, если они есть в API, но исчезли из UI.
- [ ] **Сохранность конфигурации** §3: безопасный способ восстановления ДО рефакторинга (существующий механизм бэкапа); без публичных файлов с ключами; после рефакторинга — сравнение до/после отдельно по глобальной конфигурации, чатам, ЛС, PERMsoc, промптам, моделям, подключениям, ролям/разрешениям; все неизменённые пользователем значения остаются прежними; **никаких скрытых миграций**.
- [ ] Результаты §117 п.1–4: карта экранов, полный реестр параметров, карта виджетов, подтверждение сохранности конфигурации.

## Зависимости / ступень
- **Wave 0:** F8 ∥ F1. `param_catalog` — F1 (nav-метаданные) → **F8 (реестр/недостающие ParamSpec)** → F4/F5/F7.
- Риски: потеря параметра при переносе (**Critical**); скрытая миграция/подмена дефолтов (**High**); утечка значения секрета в отчёт (**Critical, R17**).
