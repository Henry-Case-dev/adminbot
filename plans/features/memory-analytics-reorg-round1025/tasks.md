# F6 — `memory-analytics-reorg-round1025` (Эпик 1)

> **ТЗ:** `plans/current_task.md` §4, §21–§30, §52–§59, §75, §76. **Тип:** UI + adapter (web + api).
> **Приоритет:** P0. **Зависит от:** F1 (IA), F4 (store). **От F6 зависит:** F11 (превью карты).
> **Статус:** 🟦 Step 1 @PM — заготовка. **Детализация на Шаге 1 при старте фичи** (после F1/F4; `spec.md`/ADR — Step 2 @Architect).
> **Ограничение:** §22/§25/§111 — ExecutionGraph уже существует (`web/api/analytics.py` + `llm_usage_events` + `web/app.js::tokenFlowTree/tokenFlowNodes`); **вторую визуализацию не создавать**, только adapter + новые узлы. (В ТЗ @Memory фича названа `memory-access-reorg`.)

## Верхнеуровневые задачи (детализация при старте)
- [ ] Раздел **«Память»** §52: Досье, Факты, Связи, Лор чатов, Настройки; не смешивать просмотр людей с настройками RAG; настройки — Поиск / Граф знаний / Хранение / Ночной синтез / Отношения.
- [ ] Перенос параметров по §53–§59 (точная семантика — из кода): Поиск (`limits.dig_*`, `flags.dig_*`), Граф знаний (`limits.graph_*`, `flags.graph_*`), Хранение (`limits.full_memory_retention_days`, `limits.memory_backup_hour`, `flags.memory_backup_enabled`/`vec_int8_enabled`/`graph_user_quota_enabled`), Ночной синтез (`limits.deep_sleep_top_k`, `flags.deep_sleep_enabled`/`belief_decay_enabled` + реальные параметры из кода), Отношения §57 (`flags.relations_*`/`limits.relations_*`), Лор чатов §58 (ручной/автоматический/автогенерация/история; ручной лор не удалять при очистке авто), Умный кэш §59.
- [ ] **«Сводка» → «Аналитика»** §21: открывается из Статуса; содержит расходы/бюджеты/токены/распределение по моделям и модулям/дерево вызовов/историю/интерактивную карту LLM-вызовов; на Статусе остаётся компактное превью последнего вызова (не одна строка с числом токенов).
- [ ] Adapter ExecutionGraph §22–§25: нормализованная модель этапа (§23 `ExecutionNode`), разделение kind (§24: llm/algorithm/tool/format/publish/other), отдельный слой «backend metrics → normalized graph → UI» (преобразование не внутри SVG-компонента); не изображать агрегаты как точные трассировки; не придумывать `parent_id`.
- [ ] Два режима карты §26 (последний вызов / агрегированный период: день/неделя/месяц); интерактивность §27 (детали узла desktop side-panel / mobile bottom sheet, поиск и фильтры по модулю/модели/этапу/периоду/статусу); «Нет данных»/«Без лимита» §28; mobile-карта §29 (вертикальная последовательность/агрегат, без сжатия до 320 px); совместимость с Эпиком 2 §30.
- [ ] Тесты §75 (карта токенов: периоды, выбор L1/L2/модели/модуля, неизвестная стоимость, безлимит, отсутствие фиктивных этапов, adapter одно-/двухслойных вызовов) и §76 (мониторинг интеллекта — убедиться, не менять).
- [ ] Сохранить существующие виджеты памяти/сна/ленты; позиция прокрутки не сбрасывается.

## Зависимости / ступень
- Ступень web: F4 → F5 → **F6** → F7 → …
- Ступень `web/api`: F6 (analytics adapter) — изолировано от routes (F9).
- Риски: вторая несовместимая система аналитики (**Critical** по §116); фиктивные узлы/токены (**High**); подмена стоимости `$0` (**High**); статичное дерево вызовов (**High**).
