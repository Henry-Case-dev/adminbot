# F9 — `secrets-and-save-states-round1025` (Эпик 1)

> **ТЗ:** `plans/current_task.md` §50, §51, §69, §78. **Тип:** UI (секреты) + визуальный SaveBar.
> **Приоритет:** P0. **Зависит от:** **F0 ✅ ARCHIVED** (`plans/archive/f0-config-bugfixes-round1025/` — persistence/409/state-machine **прочитано, не дублируется**), F4, F5 (формы).
> **Статус:** 🟦 Step 1 @PM — заготовка. **Детализация на Шаге 1 при старте фичи** (после F4/F5; `spec.md`/ADR — Step 2 @Architect).
> **Инвариант R17:** секрет никогда не возвращается в браузер открытым; наружу только `{configured,last4}`.
> **Границы F9 (НЕ дублировать F0):** серверная логика **persistence / 409 / 412 / единая state-machine / безопасный merge / версия** закрыта в **F0** (`plans/archive/f0-config-bugfixes-round1025/` — T-2412…T-2419, `adr-1025-2-save-state-machine.md`; синхронизировано с `plans/ARCHITECTURE.md` §9/§52). **В F9 остаются только:** UI-слой секретов **§50** + **визуальная часть SaveBar §69/§78** (safe area, клавиатура, последнее поле, отображение состояния). F9 читает результат F0 и **не переписывает** его серверные блоки.

## Верхнеуровневые задачи (детализация при старте)
- [ ] Аудит **ВСЕХ** полей секретов §50: если секрет есть — видно (`••••••••a7F2`); последние 4 символа соответствуют реальному секрету; если сервер не отдаёт last4 — «Ключ установлен».
- [ ] Маска **не является** значением `input`; полный секрет не возвращается только ради маскирования; замена ключа — отдельное действие; пустое поле нового ключа **не удаляет** старый; удаление — отдельное подтверждаемое действие; при сохранении несекретных параметров старый секрет остаётся прежним.
- [ ] Состояния формы §51/§69 — **только отображение**: загрузка / без изменений / есть изменения / сохранение / успех / ошибка / конфликт; «Сохранено» — только после подтверждения сервера. **Серверная часть** (state-machine сохранения, 409/412, безопасный merge, контроль версии) — **F0** (`plans/archive/f0-config-bugfixes-round1025/`), здесь **не дублируется**; UI показывает понятное сообщение по ответу сервера.
- [ ] Sticky SaveBar §69: safe area, клавиатура, последнее поле, состояние сохранения.
- [ ] Тест секретов §78 (маска есть/не перезаписывается без изменения/новая маска после замены/маска не уходит как новый секрет).

## Зависимости / ступень
- **F0 ✅ ARCHIVED** (`plans/archive/f0-config-bugfixes-round1025/`) — persistence/409/state-machine: **читается, не переписывается**.
- Ступень web: F5 → F6 → F7 → F11 → **F9** → F10.
- Ступень `web/api`: **F9** (секрет-пути) — по возможности изолировать от F6; серверные save/409-пути уже закрыты F0.
- Риски: секрет уходит в браузер/лог (**Critical, R17/R18**); «Сохранено» до подтверждения сервера (**High**); молчаливая перезапись при конфликте (**High**); потеря старого секрета при сохранении несекретных полей (**High**).
