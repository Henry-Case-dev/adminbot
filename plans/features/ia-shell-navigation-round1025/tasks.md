# F1 — `ia-shell-navigation-round1025` (Эпик 1 «Liquid Glass Control Center»)

> **ТЗ:** `plans/current_task.md` — MASTER SPECIFICATION v6.0 (§0, §1, §4–§7, §68, §70, §71, §79, §116, §117).
> **Тип:** информационная архитектура + app shell (Vue 3 global zero-build, self-host, CSP `script-src 'self'`).
> **Приоритет:** **P0**. **Зависит от:** F8 (`parameter-registry-widget-map`, инвентарь/бэкап, Wave 0) — читает; **от F1 зависят** F3, F4, F6, F11 и опосредованно все.
> **Статус:** 🟦 Step 1 @PM — декомпозиция. `spec.md` / ADR — Step 2 @Architect (PM не создаёт).
> **Инварианты:** сохранить **100%** существующих разделов/параметров/виджетов (§1/§79/§116); **Δ DDL = 0**; **Δ каталога (ParamSpec/GROUPS) = 0** (меняются только nav-метаданные); порядок роутеров `bot.py` не трогать; R16/R17/R18; CSP/zero-build; `plans/current_task.md` **НЕ коммитить** (untracked, содержит секрет — не цитировать).
> **SUPERSEDE:** «заморозка меню/навигации» раундов 10.20/10.21 (`test_frontend_tab_mapping`, `test_webapp_nav_disclosure_ui`, `test_round106_ia_smoke`, `_TAB_BY_GROUP`/`TAB_NAV`/`NAV_ORDER`) **снимается** новой IA §4 — маркер-тесты обновляются атомарно.
> **Ступени общих файлов:** `services/param_catalog.py` — **F1 (nav-метаданные) → F8 (реестр) → F4/F5/F7**; `web/app.js` / `web/index.html` / `web/static/app.css` — **F1 → F2 → F3 → F4 → F5 → F6 → F7 → F11 → F9 → F10** (строго сериализовано).
> **Флаг:** env-only kill-switch `ClassVar` (вне `param_catalog`, **default ON**) `IA_V2_ENABLED`; доставка UI-флага — паттерн **ADR-1024-13** (`GET /api/me.ui_flags`, только bool). OFF → прежний navbar байт-в-байт.

## Учтённые свежие замечания @Scanner / @Reviewer (релевантные F1)
- **`round1024-web-architecture.md` §3:** UI-флаги 10.22/10.23 «объявлялись, но не были реализованы» — по этой фиче флаг обязан иметь реальное OFF-поведение и тест (T-2404).
- **`round1024_scanner_audit.md` §2:** маркерные тесты каталога/меню обновлять **в тех же коммитах** (T-2393) — иначе «подгонка эталонов».
- **`round1023_scanner_audit.md` §2 (пункт 8/12):** пин-тесты `test_param_catalog`/`test_frontend_tab_mapping` — атомарно с кодом.
- **10.20-UPD3 / 10.21 урок:** headless ≠ Telegram WebView; «одна успешная сборка» не является доказательством — реальный Chromium/Playwright + live-приёмка в TMA (T-2407).
- **10.22 F2 урок («ложное закрытие»):** закрывать экран только при живом доказательстве (скриншоты/boundingClientRect), а не по отчёту.

## UPD — Human Gate GO по F1 (применено Step 1 @PM, 20.09.2026; `current_task.md` §7–§10)
- **Аналитика:** технический маршрут `#/oversight` сохранён, пользовательское название «Аналитика», переход из Статуса, **дублирующую страницу не создавать** (T-2395/T-2396).
- **Профиль:** использовать **существующий** блок Telegram-пользователя/администратора; новый экран без необходимости не создавать; не путать с «Личностью бота» (уточнение к §68; в T-2400/T-2401 новых экранов не добавлять).
- **Планшет 768–1199 px:** **без постоянного sidebar** — компактная навигация/drawer; полноценный sidebar — с 1200 px; селектор области легкодоступен (T-2397).
- **Нижнее меню:** 4 пункта — Статус / Модули / ИИ / Ещё; «Память» — в «Ещё»; на вложенных страницах — текущий раздел + понятный путь назад (T-2398).
- **Заморозка меню 10.20–10.21 снята**; **аккордеоны 10.24 — не основной способ навигации** (только второстепенные настройки) — SUPERSEDE/REVISE, T-2391/T-2393 (детально — F5).
- **Палитра `#090D17`/`#151B2A` + замедление анимированного градиента** (градиент **не удалять**) — зона **F2**.
- **§9.1/§9.3:** быстрые генеральные тумблеры модулей (глобально/чат) без открытия страницы параметров; **до старта нового каталога сохранить доступ к старому работающему каталогу и существующим виджетам** (мониторинг интеллекта, убеждения, парадигмы, эволюция, сон, лента досье, дерево LLM-вызовов, граф, логи) — **без заглушек** (T-2402/T-2403).
- **§9.5:** `IA_V2_ENABLED` — **только временный механизм отката F1**, не вторая архитектура (T-2404).
- **§9.6:** в F1 **не менять** алгоритмы Саммари, роутинг моделей, генерацию обложки, публикацию Rich Message (Эпик 2 — после приёмки Эпика 1).

---

## 0. Пре-флайт и точка отката
- [x] **T-2388** — Точка отката до правок: бэкап фронтенда (`web/**` → `var/backups/web-round1025-<ts>/`), `git tag pre-round1025`, `.env.bak.round1025`, фиксация baseline (`da561bc`, pytest **7911/0**, SQLite `user_version=12`).
  **Готово, когда:** тег + бэкап + `.env.bak` существуют, baseline зафиксирован, список файлов задокументирован. **КРИТИЧНО:** `plans/current_task.md` НЕ добавлять в git и НЕ коммитить (§0; R17/R18).
- [x] **T-2389** — Read-only инвентаризация существующих разделов/маршрутов/подстраниц/виджетов Статуса (вход для F8 §2): полный список `TABS` (26), хабов `HUBS`, `NAV_ITEMS` (6), карта «Старый экран → Новый раздел IA».
  **Готово, когда:** таблица инвентаря покрывает 100% текущих маршрутов, «сирот» нет (§1/§2).

## 1. Новая информационная архитектура (§4)
- [x] **T-2390** — Утвердить карту IA §4: Публичные (Статус, Справка); Административные (Модули, ИИ, **Память**, Доступы); Локальное (PERMsoc). hash-маршруты и deeplinks на каждый раздел/подстраницу.
  **Готово, когда:** каждый из 26 `TABS`/хабов имеет новое место; отдельной конкурирующей главной «Обзор» нет.
- [x] **T-2391** — `services/param_catalog.py`: `NAV_ORDER`/`NAV_TITLES`/`TAB_NAV` под новую IA (4 админ-раздела + публичные + локальный PERMsoc); перераспределение групп «Память»/«ИИ» по §47/§52/§57 (chat_lore, relations → «Память»; people_names, smart_cache → «ИИ»). **Δ ParamSpec/GROUPS = 0.**
  **Готово, когда:** модуль импортируется, `_TAB_BY_GROUP` без дублей, `tab_nav()` консистентен, счётчики REGISTRY/GROUPS не изменились.
- [x] **T-2392** — `web/app.js`: `NAV_ITEMS`, `TABS[].menu`, `HUBS`, `NAV_GROUP_ORDER`/`NAV_GROUP_TITLES` под новую IA; hash-роутер (`openTab`/`navigateTo`) без vue-router.
  **Готово, когда:** навигация из sidebar/нижнего меню/хабов работает на всех разделах.
- [x] **T-2393** — **Атомарно** (одним коммитом) обновить маркерные тесты: `tests/test_frontend_tab_mapping.py`, `tests/test_webapp_nav_disclosure_ui.py`, `tests/test_round106_ia_smoke.py`, `tests/test_param_catalog.py`, `tests/js/routing_test.js`, `tests/js/vue_mount_test.js`.
  **Готово, когда:** тесты зелёные, ожидания соответствуют фактической IA; правки не «ослабляют» эталоны (см. замечание Scanner).
- [x] **T-2394** — Статус — **стартовая страница** (`#/`); Статус и Справка доступны **всем**; остальные разделы — по разрешениям (§4).
  **Готово, когда:** при редиректе на `#/` открывается Статус; разделы без прав не показываются.
- [x] **T-2395** — «Память» — **отдельный** nav-раздел (§4/§52), не под «ИИ»; PERMsoc **остаётся локальным** (не переносить в глобальные настройки, §4/§60); «Сводка» → **«Аналитика»** как подмаршрут Статуса (место в IA; наполнение — F6).
  **Готово, когда:** PERMsoc доступен только в локальном контексте и не дублируется в глобальных настройках.
- [x] **T-2396** — Стабильные hash-deeplinks (разделы, подстраницы, будущие модульные workspace F5); неизвестный hash → Статус без падения.
  **Готово, когда:** прямые ссылки открывают корректный экран; мусорный hash не ломает приложение.

## 2. Desktop / Mobile shell (§6/§7)
- [x] **T-2397** — **Desktop shell §6** (≥1200 px): sidebar слева ≈232 px, header сверху ≈64 px, контент справа; формы 1200–1440 px; широкие графики — шире; длинные textarea не растягивать на 2560 px.
  **Готово, когда:** структура воспроизводится на 1280/1440/1920/2560 без поломок.
- [x] **T-2398** — **Mobile shell §7** (<768 px): компактный header; селектор области под заголовком (реализация — F3); одна колонка; нижняя навигация администратора (Статус/Модули/ИИ/Ещё) или пользователя (Статус/Справка); меню «Ещё» (Справка/Память/Доступы/PERMsoc/Профиль); **без** горизонтальной полосы из 7 вкладок; сложные редакторы — отдельными экранами.
  **Готово, когда:** нет горизонтального скролла; разделы без прав скрыты; touch-таргеты ≥44×44.
- [x] **T-2399** — TMA-интеграция §7: `BackButton`, `viewportStableHeight`, `safeAreaInset`, `contentSafeAreaInset` (расширить существующий `web/static/telegram-init.js`).
  **Готово, когда:** Back на подстраницах корректно возвращает на родителя; safe-area не обрезает контент.
- [x] **T-2400** — **Справка §68**: desktop — оглавление слева + содержимое справа; mobile — поиск/оглавление/текст; якорные ссылки; существующий стиль текста сохранить.
  **Готово, когда:** якоря работают на desktop/mobile, стиль v5 не изменён.
- [x] **T-2401** — **Доступы §68**: desktop — матрица прав; mobile — последовательный редактор ролей; права проверяются на сервере (семантику RBAC не менять).
  **Готово, когда:** мобильный редактор не требует горизонтального скролла; решения по правам принимает сервер.

## 3. Сохранность (§1/§79/§116)
- [x] **T-2402** — 100% покрытие: каждый существующий раздел/маршрут/параметр/виджет доступен в новой IA. Авто-тест покрытия «inventory ↔ IA-map».
  **Готово, когда:** тест доказывает отсутствие потерянных экранов; ни один ParamSpec/группа не удалён.
- [x] **T-2403** — Виджеты Статуса остаются смонтированными и доступными (граф, сон/бейджи, мониторинг интеллекта, лента досье, логи, метрики); **логи не выносятся** на отдельную техническую страницу (§20). Полная композиция — F11.
  **Готово, когда:** каждый виджет открывается; лог-вьюер доступен со Статуса.

## 4. Проверка F1
- [x] **T-2404** — Kill-switch `IA_V2_ENABLED`: OFF → прежний navbar байт-в-байт; доставка через `/api/me.ui_flags` (ADR-1024-13); тест «флаг реально работает» (не декларация).
  **Готово, когда:** OFF-режим воспроизводит старое меню, ON — новую IA; тест на оба состояния.
- [x] **T-2405** — Адаптивность §70: нет горизонтального скролла; переключатели/названия не обрезаются; container queries там, где поведение зависит от ширины компонента.
  **Готово, когда:** проверено на 320/390/768/992/1200/1440+.
- [x] **T-2406** — JS-гейты: `node --check web/app.js`, `tests/js/routing_test.js`, `tests/js/vue_mount_test.js` зелёные + новые JS-тесты новой IA (без inline-скриптов, CSP-safe).
  **Готово, когда:** все JS-гейты зелёные, новых inline-скриптов нет.
- [x] **T-2407** — **Реальная Playwright/Chromium-проверка** §71 (не «одна сборка»): 320×700, 360×780, 390×844, 430×932, 768×1024, 1024×768, 1280×800, 1440×900, 1920×1080, 2560×1440 + низкое окно TG Desktop; `documentElement.scrollWidth <= innerWidth + 1`; реальные `getBoundingClientRect()`; скриншоты desktop/mobile (§117 п.7).
  **Готово, когда:** артефакты прогона приложены, нарушений нет.
- [ ] **T-2408** — @Reviewer итерация (Needs-fixes → Approved) + фикс по замечаниям.
  **Готово, когда:** @Reviewer подтвердил.
- [ ] **T-2409** — Отчёт-карта «старые→новые экраны» (§117 п.1) + фиксация/очистка бэкапа после утверждения владельцем (§0).
  **Готово, когда:** карта приложена; бэкап удалён только после утверждения.
  **Статус:** карта создана — `plans/reports/f1-round1025-report.md`; бэкап `var/backups/f1-wip-20260921-015653/` и теги `pre-round1025*` **сохранены** (удаление — только после утверждения владельцем, R18).

---

## Порядок и зависимости
- **Порядок:** F8 (инвентарь/бэкап) → **F1 T-2388…T-2409** → далее F3/F4/F6/F11 (зависят от F1).
- **Ступень `param_catalog`:** F1 (nav) → F8 (реестр) → F4/F5/F7.
- **Ступень web:** F1 → F2 → F3 → F4 → F5 → F6 → F7 → F11 → F9 → F10.

---

## UPD — T-2481: возобновление F1 после ASAP-хотфикса (21.09.2026)

- **Точка отката:** тег `pre-round1025-f1-resume` (на `65e39fb`); файловый бэкап дерева — `var/backups/pre-f1-resume-20260921-032524/`.
- **Восстановление:** F1-WIP возвращён `git stash apply` (`stash@{0}`, 25 tracked-файлов) поверх `65e39fb` + untracked F1 из `var/backups/f1-wip-20260921-015653/`. Авто-слияние без конфликтов; оба интента сохранены — хотфикс (`APP_VERSION 2.58.1`, `TELEGRAM_LOCAL`/`LOCAL_BOT_API_URL`, R17-логи, `?v=__APP_VERSION__`) **и** F1 (IA v2/shell).
- **Каталог/DDL:** Δ каталога = 0 (`REGISTRY 459 / GROUPS 98 / _TAB_BY_GROUP 96 / TAB_RULES 21 / TAB_NAV 21 / CONFIG_TAB_TITLES 21`); Δ DDL = 0 (`user_version=12`).
- **Проверка:** полный pytest **7996 passed / 0 failed** (baseline 7976 + 20 новых F1); JS **21/21**; `node --check web/app.js` OK; `git diff --check` exit 0; Playwright-матрица §71 (`tools/ui_round1025_matrix.py`) — **0 нарушений** на 10 вьюпортах.
- **F0-слой:** `persistItems`/`saveState`/`notify`/`stickyFieldFailed` не переписывались; F0-тесты зелёные.
- **Открыто:** **T-2408** (@Reviewer) — не выполнено; **T-2409** — карта создана, очистка бэкапа ждёт утверждения владельца. Live-гейт хотфикса (T-2463/T-2472/T-2479) — вне F1.

### Ревью-итерация T-2408 (Changes Requested → правки, 21.09.2026)
- **[M] `activeNav`:** `#/memory*` теперь гейтится `iaV2` — при `IA_V2_ENABLED=false` активным подсвечивается «ИИ» (в legacy `NAV_ITEMS` раздела «Память» нет). Ассерт в `tests/js/round1025_ia_routing_test.js` (OFF + `#/memory` → `ai`).
- **[M] safe-area:** `.app-sidebar`/`.app-drawer`/`.bottom-nav`/`.more-sheet` (bottom + padding-bottom) и padding-bottom контента под bottom-nav переведены на `max(env(safe-area-inset-bottom), --tg-safe-area-inset-bottom, --tg-content-safe-area-inset-bottom)` (образец F0 `app.css:567`).
- **[L] `--tg-viewport-stable-height`:** задействован — `min-height` `.app-shell` (fallback `100vh`).
- **[L] a11y:** `:inert` для закрытых `.app-drawer`/`.more-sheet`; `.help-toc-link` ≥44 px на mobile.
- **[L] baseline-фикстура:** происхождение зафиксировано в `catalog_baseline.json` (`_provenance`) и в `spec.md` §8.4 (F1 меняет только nav-метаданные → множества идентичны до-правковому состоянию).
- **Проверка после правок:** pytest **7996/0**; JS **21/21** (+ассерт); Playwright §71 — **0 нарушений**.

### Scanner-аудит F1 (H-1/M-1/M-2, 21.09.2026)
- **[H-1, блокер] `bottomNavItems`:** «Ещё» теперь строится от `navItems` по `group`, а не по `canViewTab('modules'|'llm_providers')` — при любом непубличном разделе (Память/Доступы/PERMsoc/Модули/ИИ) шторка появляется, раздел достижим на <768. Поведенческие тесты на роли «только Память/Доступы/PERMsoc» в `tests/js/round1025_ia_routing_test.js`.
- **[M-1] `_routeLabel`:** подпись берётся из карточки хаба для маршрутов вне `TABS` (`#/ai/persona` → «Личность и стиль», `#/memory/rag` → «Память и RAG») — без сырого hash; тест + breadcrumb.
- **[M-2] Kill-switch OFF:** объём OFF зафиксирован в ADR-1025-1 D5 и spec §6.4 — откатывается только навигация/шелл; layout «Справки», мобильный редактор «Доступов» и label «Аналитика» не откатываются (осознанный nav-scope, «мёртвых» состояний нет).
- **[Low] L-1:** убран дубль `min-width` в `.bottom-nav-link`; **[Low] L-5:** пустые `.sidebar-sep` скрыты (`v-if="sidebarGroups.admin/local.length"`).
- **[Low] L-2/L-3/L-4:** `.more-sheet`-offset / restricted-role-прогон матрицы / pre-change baseline — техдолг (не блокеры).
- **Проверка после правок:** pytest **7996/0**; JS **21/21**; Playwright §71 — **0 нарушений**.

### P0 prod-инцидент после F1 (21.09.2026) — фикс за одну итерацию
- **FIX 1 (render):** `stickyFieldFailed` ошибочно в `computed` → шаблоны вызывали как функцию → `TypeError` в render → пустой generic config-раздел (`#/ai/llm`, `#/ai/names`, `#/smart-cache`, `#/memory/rag`). Перенесён в `methods` (`web/app.js:2149`); тест в `tests/js/round1025_save_state_test.js`.
- **FIX 2 (media/аватары):** локальный Bot API отдаёт контейнерный абсолютный путь `/var/lib/telegram-bot-api/<id>:<token>/…` — нормализуется к host-корню (`services/media_download.py:normalize_api_file_path`/`read_host_file_bytes`), traversal-guard сохранён; `web/api/avatars.py` использует тот же helper. Тесты — `tests/test_media_local_path_round1025.py` (видео/ГС/escape/аватары).
- **FIX 3 (cache-bust):** `APP_VERSION` **2.58.1 → 2.58.2** (`config/settings.py`, `README.md`); `?v=__APP_VERSION__` покрывает tailwind/app.css/app.js/telegram-init.js/font.
- **FIX 4 (тест-инструмент):** `tools/ui_round1025_matrix.py` — непустой `/api/config` из `services/param_catalog.py` (458 items/98 groups), маршруты `#/ai/{llm,prompts,smart-cache,names}` и `#/memory/rag`, FAIL на console/pageerror.
- **Проверка:** pytest **8003/0**; JS **21/21**; Playwright §71 — **0 нарушений**, console/pageerror пусты. (Сетевой `test_betterstack…302` — флак, зелёный в изоляции и на повторе.)

## Риски F1
- **SUPERSEDE маркер-тестов без обновления** → красный CI и «подгонка эталонов» (**High**): T-2391/T-2393 атомарно.
- **Потеря раздела/параметра при переносе IA** (**Critical**): T-2389/T-2402 + авто-тест покрытия.
- **Мобильный скролл/обрезание переключателей** (**High**): T-2398/T-2405/T-2407 (Playwright).
- **Флаг `IA_V2_ENABLED` декларативен** (**Medium**): T-2404 — обязательный тест OFF/ON.
- **Утечка секрета из `current_task.md`** (**Critical, R17/R18**): T-2388 — файл не коммитить, значения не цитировать.
