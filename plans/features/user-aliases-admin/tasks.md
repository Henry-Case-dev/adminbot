# Задачи: user-aliases-admin

## Внедрение в админку (выполнено)

- [x] Настройки алиасов в «Лимитах и Модулях» (limits.summary_aliases, сохранение в PG через админку) — коммит `5d011d2`.
- [x] Перенос keys.youtube_transcript_proxy_retries из «API Ключей» в «Лимиты и Модули» — коммит `5d011d2` (MEMORY.md п.2).

## Аудит и эффект

- [ ] Аудит приоритета alias→nickname→username→user_id: подтвердить тестами каскад `AliasResolver.resolve` (по коду приоритет верный — summary_aliases.py:50-75) и canon_name для графа.
- [ ] Реальный эффект: изменить алиас в админке → /summary/граф используют новое имя (проверка через /debug_config SUMMARY_ALIASES + живой чат).

## Человеческая верификация

- [ ] ⏸ ждёт человека: подтверждение в живом чате.

## Регресс

- [ ] регресс: полный pytest.
