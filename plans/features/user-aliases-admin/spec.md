# Настройки алиасов юзеров в админке (F-6)

## Контекст и цель

Требование 01.09.2026 (MEMORY.md п.1): после переноса настроек в мини-апп «потерялся» слайс с алиасами — бот обращается к участникам по никнейму вместо алиаса. Нужно: настройки алиасов в «Лимитах и Модулях» админки + проверка сохранения и реального эффекта (бот использует новое имя). Внедрение в админку выполнено (коммит `5d011d2`); остаток — аудит каскада и проверка эффекта.

## Архитектурные привязки

- **summary_aliases.py** — каскад alias→nickname→username→user_id, canon_name для графа (приоритет подтверждён кодом, summary_aliases.py:50-75).
- **param_catalog** — `limits.summary_aliases` (группа limits_user_aliases, тип json).
- **84.24.2** — «Лимиты и Модули» (группировка/описания параметров).
- **bot.py:204** — AliasResolver из `hot.get`.
- README / Epic 27 — 36 пар алиасов в `.env SUMMARY_ALIASES` (перенос в PG через админку).

## Границы

Не менять приоритет каскада без отдельного решения (код подтверждает корректность). Не трогать механику /summary и графа, только источник имён.

## Связи с другими фичами

- F-5 (`features/config-read-path-audit/`) — чтение через hot.get.
- F-4 (`features/frontend-admin-bugfixes/`) — фронт «Лимитов и Модулей».

## Критерии готовности

- Алиасы видны в «Лимитах и Модулях» и сохраняются в PG (updated_by фиксируется).
- `keys.youtube_transcript_proxy_retries` перенесён в «Лимиты и Модули» (без дубля в «API Ключах»).
- Каскад alias→nickname→username→user_id подтверждён тестами (AliasResolver.resolve) и canon_name для графа.
- Изменение алиаса в админке → /summary/граф используют новое имя (проверка /debug_config SUMMARY_ALIASES + живой чат).
- Человек подтвердил эффект в живом чате (⏸).
- Полный pytest — 0 failed.
