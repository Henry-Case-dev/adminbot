# Задачи: scam-incident-security-followup

## Хардненинг (T-662)

- [x] T-662 (частично) — хардненинг: chmod 600 для .env* (35 бэкапов), remote без PAT, деплой-скрипты без секретов — DONE (@DevOps, MEMORY.md; коммиты `2f0b262`/`1528455`). *Остаток внутри задачи:*
  - [ ] (подпункт) — завершить секрет-скан дерева plans/ и git-истории (результат @DevOps, MEMORY.md);
  - [ ] (подпункт) — sudo chmod 600 `backups/facts_*.txt` (root-owned, группа developers);
  - [ ] (подпункт) — .env.bak* на сервере (35 шт) — по решению DevOps/человека.

## Гейты и логи (T-663…T-664)

- [ ] T-663 — админ-гейт + аудит-логи (user_id/chat_id) для DM-веток /mimic /deadpage /alangreet (сейчас admin_commands.py:40-127 без проверки в DM).
- [ ] T-664 — логи локального Bot API: logging-драйвер/ротация для сервиса telegram-bot-api в docker-compose.yml.

## Ротации (T-665…T-666)

- [x] T-665 — ротации выполнены: токен бота (BotFather /revoke), revoke GitHub PAT, ротация ключей — уже в проде (заявление владельца 03.09.2026).
- [ ] T-666 — ⏸ ждёт человека: точный username из скам-сообщения → вердикт «утечка» vs «фейк-бот» (критерии — git-история до 03.09.2026).

## Регресс

- [ ] регресс: полный pytest.
