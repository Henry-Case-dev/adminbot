# Скам-инцидент: security follow-up (F-3)

## Контекст и цель

Скам-инцидент 30.08.2026 (сообщение в личку от имени бота, MEMORY.md). Вердикт расследования: внутренние пути исключены (нет скам-строк в коде; /mimic — только трансформация реплая в своём чате); вероятна утечка/внешнее использование токена; репозиторий public. Цель: закрыть выявленные риски (секрет-скан, гейты DM-команд, логи Bot API) и вынести человеческие действия (ротация) на явный трек.

## Архитектурные привязки

- **R17** — секреты: только `.env` на сервере, маскировка, sanitize в log_ring.
- **handlers/admin_commands.py:40-127** — DM-ветки /mimic /deadpage /alangreet без админ-гейта и аудит-логов («anyone can test» в DM) — риск.
- **docker-compose.yml** — сервис telegram-bot-api без logging-конфига.
- **MEMORY.md** — бэклог T-662…T-666 (хардненинг .env, ротации — на человеке).

## Границы

НЕ менять поведение групповых веток admin_commands. Ротации секретов (токен бота, GitHub PAT, ключи) и приватизация репозитория — только человек (@DevOps/владелец), задачи здесь только фиксируют ожидание.

## Связи с другими фичами

- F-2 (`features/admin-debug-webview/`) — /debug_config как инструмент проверки без раскрытия значений.

## Критерии готовности

- Секрет-скан дерева plans/ и git-истории завершён, находки обработаны (акт @DevOps).
- `backups/facts_*.txt` — chmod 600 (root-owned, группа developers).
- DM-ветки /mimic /deadpage /alangreet: админ-гейт + аудит-логи (user_id/chat_id) в DM.
- telegram-bot-api в docker-compose: logging-драйвер/ротация.
- Ротации токена бота и GitHub PAT выполнены (03.09.2026, в проде); приватизация репо — на человеке (T-665).
- Полный pytest — 0 failed.
