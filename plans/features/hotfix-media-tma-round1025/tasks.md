# ASAP-хотфикс — `hotfix-media-tma-round1025` (внеплановый, между F0 и F1)

> **Триггер:** подтверждённые боевые дефекты после деплоя F0 (`3a91c84`, 20.09.2026): (1) видео/транскрибация падает; (2) LLM `ReadTimeout`; (3) «разделы миниаппа не открываются»; (4) диагностируемость логов. Диагностика уже выполнена (@Memory + @DevOps) — **перепроверке не подлежит**, ниже используется как данность.
> **Тип:** инфра (Docker/Bot API) + backend (логирование/фразы/регресс-тесты) + web (cache-bust) + ops (деплой/верификация) + git-hygiene (изоляция WIP F1).
> **Приоритет:** **P0** — блокирует приёмку F1 и повседневное использование (медиа + миниапп). LLM-таймауты и смежная инфра — **P1/P2** (не смешивать с хотфикс-ядром без необходимости).
> **Статус:** 🟦 Step 1 @PM — декомпозиция. `spec.md`/ADR — Step 2 @Architect **при необходимости** (PM не создаёт).
> **Задачи:** **T-2456…T-2481 (26)**. Преемственность нумерации: максимум до хотфикса — `T-2455` (F0, `plans/archive/f0-config-bugfixes-round1025/`), дублей нет.
> **Не связано с F0:** установлено отдельно — дефекты (1) и (2) первопричинно **не** связаны с F0. Регрессий F0 не допускать: **pytest 7946/0**, **JS 19/19**, `database is locked` = **0**.
> **Инварианты:** R16/R17/R18; секреты не выводить и не копировать; `plans/current_task.md` **НЕ коммитить** (untracked, содержит SSH-креды — не цитировать); крупные архивы `bot.zip` / `admin_bot.zip` / `adminbot.zip` (~0.9 ГБ) **не коммитить**; порядок роутеров `bot.py` не трогать; CSP/zero-build (Vue 3 global, self-host).

## Контекст: незавершённая F1 в рабочей копии (изолировать до фикса)
В рабочей копии лежит **прерванная** работа по F1 (`ia-shell-navigation-round1025`), **не закоммиченная**:
- **tracked-изменения:** `web/app.js`, `web/index.html`, `web/api/routes.py`, `config/settings.py`, `services/param_catalog.py`, `web/static/app.css`, `web/static/telegram-init.js` + множество тестов (`tests/js/*`, `tests/test_*`).
- **untracked-файлы F1:** `tests/fixtures/round1025/`, `tests/js/round1025_ia_routing_test.js`, `tests/js/round1025_shell_breakpoints_test.js`, `tests/test_ia_inventory_round1025.py`, `tests/test_ia_shell_round1025.py`, `tools/ui_round1025_matrix.py`, `tools/_ui_round1025_*`.
- **Точка отката F1 существует:** тег `pre-round1025-f1` + бэкап `var/backups/web-round1025-f1-*`.
- **База:** HEAD **`7c38f70`** (= `origin/master`); F0-деплой — `3a91c84`.
- **Крупные untracked-архивы** `bot.zip`, `admin_bot.zip`, `adminbot.zip` (~0.9 ГБ) — **не коммитить, не трогать**.

## Feature flags / раскатка
Поэтапная раскатка internal→10%→50%→100% **не требуется** (пет-проект, один прод). Вместо этого:
- **Откат хотфикса** — тег точки отката (T-2456) + `git revert` коммита хотфикса.
- **Медиа:** фактический «switch» — env `TELEGRAM_LOCAL` сервиса `telegram-bot-api` (OFF-путь = текущее облачное поведение лимита 20 МБ).
- **Cache-bust:** фактический «switch» — bump `APP_VERSION`/суффикса `?v=` (T-2470); откат — возврат версии + повторный bump.

---

## 0. Изоляция F1-WIP и точка отката (выполнять ПЕРВЫМ)

- [x] **T-2456** — **Точка отката хотфикса + фиксация baseline.** Тег `pre-round1025-hotfix`; бэкап изменяемых областей в `var/backups/hotfix-round1025-<ts>/`; `.env.bak.round1025-hotfix` (значения не печатать); зафиксировать baseline: HEAD **`7c38f70`**, pytest **7946/0**, JS **19/19**, SQLite `user_version=12`, APP_VERSION **2.58.0**.
  **Готово, когда:** тег + бэкап + `.env.bak` существуют, baseline записан, доказана возможность возврата (R18). **КРИТИЧНО:** `plans/current_task.md` и zip-архивы в git не добавлять.
- [x] **T-2457** — **Изоляция tracked-WIP F1 в git-stash** (только tracked-изменения, untracked **не** трогаем): `git stash push -u=false -m "f1-wip: ia-shell-navigation-round1025 (продолжить после hotfix)"` по списку файлов из раздела «Контекст».
  **Готово, когда:** `git status` не содержит tracked-изменений F1; в `git stash list` есть запись с понятным сообщением; WIP не потерян.
- [x] **T-2458** — **Перенос untracked-файлов F1** в `var/backups/f1-wip-<ts>/` (сохранить структуру путей): `tests/fixtures/round1025/`, `tests/js/round1025_ia_routing_test.js`, `tests/js/round1025_shell_breakpoints_test.js`, `tests/test_ia_inventory_round1025.py`, `tests/test_ia_shell_round1025.py`, `tools/ui_round1025_matrix.py`, `tools/_ui_round1025_*`. Переместить **только** F1-файлы.
  **Готово, когда:** untracked F1 в рабочей копии отсутствует, копии в `var/backups/f1-wip-<ts>/` целостны; zip-архивы в корне **остались** на месте (не трогать).
- [x] **T-2459** — **Верификация чистой базы:** HEAD `7c38f70`, `git status` без tracked-изменений, untracked — только zip-архивы (+ возможные служебные `plans/current_task.md`/`var/`); baseline-прогон подтверждён.
  **Готово, когда:** база чистая, хотфикс можно начинать; зафиксирован список «что осталось untracked».
- [x] **T-2460** — **(P2, опционально) `.gitignore`** для крупных архивов `bot.zip`, `admin_bot.zip`, `adminbot.zip` отдельной задачей — чтобы случайно не ушли в коммит.
  **Готово, когда:** архивы игнорируются, `.gitignore` закоммичен отдельным коммитом (или задача явно закрыта как «не требуется»). **Zip-файлы НЕ коммитить и НЕ удалять.**

## 1. Медиа/транскрибация — P0 (инфра Bot API)

> **Установленная причина:** локальный контейнер `telegram-bot-api` запущен **без** `--local` (env `TELEGRAM_LOCAL` не задан) → `getFile` ограничен 20 МБ, крупные видео отклоняются, файл не кладётся на диск; `services/media_download.py:131-152` делает 3 попытки локального чтения → фолбэк `bot.download` → `TelegramBadRequest`. Симптомы: `handlers.youtube` `video-file fetch failed`, `media_download` `local file unavailable after 3 attempts`. **С F0 не связано.**

- [x] **T-2461** — **Подтверждение конфигурации `telegram-bot-api`:** зафиксировать фактические значения env/аргументов сервиса (`TELEGRAM_LOCAL`, `TELEGRAM_API_FILES_DIR`, `LOCAL_BOT_API_URL`) и способ их задания (compose/deploy-скрипт/systemd).
  **Готово, когда:** есть file:line конфигурации + текущее фактическое состояние (local ON/OFF).
- [ ] **T-2462** — **Включить локальный режим:** `TELEGRAM_LOCAL` — ЕДИНЫЙ рубильник из общего `.env` (compose `"${TELEGRAM_LOCAL:-}"`; хостовый бот читает ту же переменную через `load_dotenv`/systemd `EnvironmentFile`). Деплой-шаг @DevOps: добавить `TELEGRAM_LOCAL=1` в `.env` → перезапустить `telegram-bot-api` И `admin_bot` (для применения хостовым процессом). Проверить согласованность `TELEGRAM_API_FILES_DIR`/`LOCAL_BOT_API_URL`. *(Правка compose/кода и паритет семантики («непустое=local, unset=cloud») — @Builder; `.env` + рестарты — @DevOps.)*
  **Готово, когда:** контейнер `telegram-bot-api` поднят в локальном режиме, приложение — `local` (лог гейта `mode=local`); конфиг не содержит секретов в открытом виде сверх необходимого (R17).
- [ ] **T-2463** — **Верификация лимита:** крупное видео **> 20 МБ** скачивается, файл реально появляется на диске (`TELEGRAM_API_FILES_DIR`); `getFile` поддерживает файлы до **2000 МБ**.
  **Готово, когда:** приложено доказательство (лог/путь к файлу/размер) скачивания > 20 МБ; лимит `getFile` подтверждён.
- [x] **T-2464** — **Ранний честный гейт размера + согласование лимита транскрибации.** Если `--local` включён — добавить предварительную проверку размера (не доводить до 3 неудачных попыток и `TelegramBadRequest`). Если `--local` **невозможен** — согласовать `VIDEO_TRANSCRIBE_MAX_SIZE_MB` с облачным лимитом 20 МБ и дать понятное сообщение пользователю **до** попытки скачивания.
  **Готово, когда:** выбран ветка (local ON / fallback), поведение при превышении лимита — честное и раннее; зафиксировано в отчёте.
- [x] **T-2465** — **Регресс-тесты медиа:** размерный гейт (порог/сообщение), путь `local → fallback`, отсутствие ложного «успеха» без файла на диске.
  **Готово, когда:** тесты зелёные и **падают на старом коде** (доказывают фикс).

## 2. Честные ошибки и диагностируемость — P0

> **Установленная причина:** `handlers/youtube.py:1042-1044` логирует только **класс** исключения (без текста); `services/llm_client.py:820,827` — **пустой** error. Также нужен понятный текст пользователю при превышении размера.

- [x] **T-2466** — **`handlers/youtube.py`:** логировать **текст** исключения (не только класс), R17-safe: без ключей/токенов/содержимого медиа; при необходимости — `repr`/`str` с маскированием.
  **Готово, когда:** в логе видна причина сбоя (например, `TelegramBadRequest` с текстом), секретов в логе нет.
- [x] **T-2467** — **`services/llm_client.py:820,827`:** логировать текст ошибки (message/type/status), R17-safe; сохранить существующее поведение ретраев.
  **Готово, когда:** «пустой» error заменён на содержательный; провайдерские секреты не утекают.
- [x] **T-2468** — **Понятное сообщение пользователю при превышении размера** (не общая фраза): проверить/дополнить `services/smartmodule_phrases.py`; текст должен различать «файл слишком большой» / «сервис недоступен» / «провайдер не ответил».
  **Готово, когда:** при превышении размера пользователь получает конкретную причину, а не общее «не удалось».
- [x] **T-2469** — **Регресс-тесты диагностики:** лог содержит текст исключения; фраза о размере выбрана корректно; отсутствие секретов в логе (R17).
  **Готово, когда:** тесты зелёные и падают до фикса.

## 3. Cache-bust / TMA «разделы не открываются» — P0

> **Установленная причина:** серверных ошибок нет, статика отдаётся и побайтово совпадает с задеплоенным `3a91c84`; **НО** cache-bust не менялся (`?v=2.58.0` c 10.24), при том что F0 изменила и `index.html`, и `app.js` → Telegram WebView может держать старый `app.js` со новым `index.html` (ReferenceError в config-разделах).

- [x] **T-2470** — **Bump `APP_VERSION`/суффикса `?v=`:** разобрать механизм в `web/app.py` + `web/index.html`; поднять версию так, чтобы клиент получил **согласованные** `index.html` + `app.js` + `app.css` (единый cache-bust).
  **Готово, когда:** суффикс версии изменён для всех трёх ассетов одновременно.
- [x] **T-2471** — **Проверка/тест механизма cache-bust:** тест (или детерминированная проверка), что версия в URL меняется при bump и что `index.html` и `app.js` ссылаются на одну версию.
  **Готово, когда:** тест подтверждает изменение версии и согласованность ссылок.
- [ ] **T-2472** — **Live-проверка консоли клиента:** после деплоя открыть TMA, убедиться в отсутствии `ReferenceError` в config-разделах и что грузится новая версия ассетов.
  **Готово, когда:** приложены доказательства (версия в network/консоль), разделы миниаппа открываются.

## 4. LLM ReadTimeout — P1

> **Установленная причина:** провайдер `nano-gpt.com` и резервная модель одновременно ушли в таймаут; хроническая картина (**167 случаев за сутки** до деплоя). **Корень — провайдер.** С F0 не связано. Логи `services/llm_client.py:820,827` без текста ошибки — закрывается в T-2467.

- [ ] **T-2473** — **Безопасная подстройка таймаутов/ретраев:** оценить и при обоснованности изменить `models.llm_timeout`, `LLM_FALLBACK_TIMEOUT_SECONDS`, `LLM_MAX_RETRIES` (без роста расходов/зависаний; fail-fast вместо долгого ожидания).
  **Готово, когда:** решение обосновано (числа до/после), не ухудшает успешность ответов; при отсутствии уверенности — зафиксировано как «не менять».
- [ ] **T-2474** — **Второй резервный провайдер и мониторинг:** оценить целесообразность второй резервной цепочки; **явно пометить, что корневая причина — провайдер**, и настроить наблюдаемость (доля таймаутов/fallback-срабатываний).
  **Готово, когда:** вывод по второму провайдеру зафиксирован (внедряем/нет + почему); есть метрика/событие для мониторинга таймаутов.

## 5. Смежная инфра и техдолг — P1/P2 (не смешивать с ядром хотфикса)

> Зафиксировано как отдельный контур: WAL-файл SQLite ~159 МБ (нужен checkpoint), swap почти исчерпан, RAM 961 МБ, медленный graceful-stop. **Не расширять хотфикс без необходимости** — оформлять отдельными решениями/фичами.

- [ ] **T-2475** — **(P1) SQLite WAL checkpoint:** WAL-файл ~159 МБ — выполнить/запланировать checkpoint, оценить авто-checkpoint (`wal_autocheckpoint`), подтвердить, что `database is locked` остаётся 0.
  **Готово, когда:** WAL уменьшен/расписание задано, отсутствие `database is locked` подтверждено.
- [ ] **T-2476** — **(P2) Ресурсы и graceful-stop:** swap почти исчерпан, RAM 961 МБ, медленный graceful-stop (`TimeoutStopSec=30`) — зафиксировать как инфра/техдолг с приоритетами и планом (без изменения поведения в рамках хотфикса).
  **Готово, когда:** отчёт/пункт техдолга создан с приоритетом и предложением; хотфикс ответственности за это не несёт.

## 6. Регресс, деплой, верификация

- [x] **T-2477** — **Регресс-тесты для всех код-изменений хотфикса** + полный прогон pytest и JS-гейтов.
  **Готово, когда:** новые тесты зелёные; полный pytest = **7946/0** (без регрессий F0); JS = **19/19**; `node --check web/app.js` чист.
- [ ] **T-2478** — **Деплой:** коммит(ы) в стиле репозитория (русские conventional commits, атомарно код+тесты) → push `origin/master` → на сервере `git pull --ff-only` → `systemctl restart admin_bot` (+ перезапуск только `telegram-bot-api` при необходимости).
  **Готово, когда:** прод обновлён, `/api/health` = 200; в отчёте указаны commit и версия.
- [ ] **T-2479** — **Пост-деплойная верификация:** медиа > 20 МБ скачивается и транскрибируется; cache-bust-версия на клиенте новая, разделы миниаппа открываются; логи содержат текст ошибок (не пустые).
  **Готово, когда:** приложены доказательства (файл/лог/скриншот/версия ассетов).
- [ ] **T-2480** — **Подтверждение отсутствия регрессий F0:** pytest **7946/0**, JS **19/19**, `database is locked` = **0**; сохранность конфигурации/сохранения не нарушена.
  **Готово, когда:** цифры подтверждены (лог/отчёт), расхождений нет.

## 7. Возврат к F1

- [ ] **T-2481** — **Восстановление F1-WIP после хотфикса:** вернуть untracked-файлы из `var/backups/f1-wip-<ts>/` на исходные места и `git stash pop` (tracked-изменения) поверх уже задеплоенного хотфикса; сверить целостность с тегом `pre-round1025-f1`.
  **Готово, когда:** WIP F1 восстановлен, конфликтов нет либо они явно разрешены и зафиксированы; продолжение F1 разблокировано.

---

## Порядок и зависимости (критический путь)
- **Изоляция (T-2456…T-2459)** — **обязательна ДО** любых правок. Zip-архивы не трогать; `.gitignore` (T-2460) — по желанию.
- **Медиа (T-2461…T-2465)** — **P0, инфра**, независимо от остальных. Ветка T-2464 зависит от результата T-2461/T-2462.
- **Честные ошибки (T-2466…T-2469)** — **P0**, независимо; T-2468 связан с T-2464.
- **Cache-bust (T-2470…T-2472)** — **P0**, независимо; T-2472 требует деплоя (T-2478).
- **LLM (T-2473…T-2474)** — **P1**, после P0-ядра; T-2467 (лог текста) полезен для T-2474.
- **Инфра/техдолг (T-2475…T-2476)** — **P1/P2**, параллельно, **без смешивания** с ядром.
- **Регресс+деплой+верификация (T-2477…T-2480)** — **после** всех код-изменений; T-2472 и T-2479 зависят от T-2478.
- **Возврат F1 (T-2481)** — **после** T-2480.

## Критично vs отложено
- **Критично (P0):** изоляция WIP (T-2456…T-2459), медиа/local Bot API (T-2461…T-2465), честные ошибки (T-2466…T-2469), cache-bust (T-2470…T-2472), регресс/деплой/верификация (T-2477…T-2480).
- **Отложено (P1/P2):** LLM timeout-подстройка и второй провайдер (T-2473…T-2474), WAL checkpoint (T-2475), ресурсы/graceful-stop (T-2476), `.gitignore` для zip (T-2460).

## Порядок задач
**T-2456 → T-2457 → T-2458 → T-2459** → (T-2460 опц.) → **T-2461…T-2465** ∥ **T-2466…T-2469** ∥ **T-2470/T-2471** → **T-2473…T-2474** ∥ **T-2475…T-2476** → **T-2477 → T-2478 → T-2472/T-2479 → T-2480** → **T-2481**.

## Риски
- **Потеря незавершённой F1** (**Critical**): T-2457/T-2458 — сохранить и tracked (stash), и untracked (бэкап), до любых правок; тег `pre-round1025-f1` уже есть.
- **Случайный коммит ~0.9 ГБ архивов** (**High**): T-2459 фиксирует untracked-список; T-2460 — опциональный `.gitignore`; zip не удалять.
- **Ложное «исправлено» по cache-bust** (**High**): без реального bump версии и проверки клиента Telegram WebView продолжит держать старый `app.js` → T-2470 не закрывать без T-2471/T-2472.
- **Тихая деградация media-пути** (**High**): T-2464 — ранний честный гейт размера, иначе `TelegramBadRequest` остаётся невидимым для пользователя.
- **Вывод секретов (SSH/токены) в отчёт/лог** (**Critical, R17/R18**): не цитировать `plans/current_task.md`; логирование только R17-safe; `.env` не коммитить.
- **Расширение хотфикса смежной инфрой** (**Medium**): T-2475/T-2476 держать отдельно, не блокируют ядро.
- **Регрессия F0** (**High**): T-2480 — обязательные показатели pytest 7946/0 / JS 19/19 / `database is locked` = 0.

## Открытые вопросы (владельцу / @Architect при Step 2)
1. **Медиа:** возможен ли `--local` у `telegram-bot-api` в текущей инфре (диск/сеть)? Если нет — какой `VIDEO_TRANSCRIBE_MAX_SIZE_MB` считать каноном (20 МБ облачного лимита)? (T-2464)
2. **LLM:** допустимо ли поднимать таймауты/менять число ретраев, или предпочтительнее fail-fast + второй провайдер? Кто владелец провайдерской деградации `nano-gpt.com`? (T-2473/T-2474)
3. **Cache-bust:** достаточно ли bump `APP_VERSION`, или нужен иный механизм инвалидации (query-параметры на ассеты, заголовки)? (T-2470)
4. **Zip-архивы:** добавлять `bot.zip`/`admin_bot.zip`/`adminbot.zip` в `.gitignore` или оставить как есть? (T-2460)
5. **Смежная инфра:** нужно ли поднимать WAL checkpoint/ресурсы (swap/RAM/graceful-stop) в этом же хотфиксе или вынести отдельной инфра-задачей? (T-2475/T-2476)
6. **F1:** продолжать ли F1 сразу после хотфикса (T-2481) или требуется повторная сверка приёмки? (Step 1 @PM / владелец)
