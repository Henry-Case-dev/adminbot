# Хотфикс-3 — `hotfix3-summary-stt-anticliche-round1025` (внеплановый, после F1/hotfix2, перед F2)

> **Триггер:** прод-дефекты после hotfix2 (`fea2daa`, 21.09.2026): (A) **[P0]** саммари уходит plain-текстом без обложки — Stage-1 «Редактор» System 2 handoff не парсится из-за таймаутов `nano-gpt.com` → `summary system2: невалидная выжимка редактора — fallback` → одиночный путь → `cover_prompt=""` → plain; (B) **[P0]** видео/ГС не транскрибируются (лимиты 25/20 МБ, реальный файл 28 МБ) — нет извлечения/сжатия аудио перед STT; (C) **[P0]** анти-клише — ручные фразы молча не сохраняются; (D) **[P1]** хронические LLM-таймауты `nano-gpt.com`.
> **Диагностика @explore + @DevOps — используется КАК ДАННОСТЬ, перепроверке не подлежит.** PM архитектуру не проектирует.
> **Тип:** backend (summary/media/anticliche/LLM) + web (UI анти-клише) + ops (деплой/верификация).
> **Приоритет:** **P0** (A/B/C — прод-деградация), **P1** (D). **Зависит от:** F1 (`ia-shell-navigation-round1025`, `fe0f7bb`) + hotfix2 (`p0-fix-render-media-paths-round1025`, `fea2daa`).
> **Задачи:** **T-2482…T-2506 (25)**. Преемственность нумерации: максимум до хотфикса — **T-2481** (`plans/archive/hotfix-media-tma-round1025/`), дублей нет.
> **Статус:** 🟦 Step 1 @PM — декомпозиция. `spec.md`/ADR — Step 2 @Architect **при необходимости** (PM не создаёт). Реализация — @Builder, деплой — @DevOps.
> **Инварианты:** R16/R17/R18; секреты не выводить/не копировать; **Δ DDL = 0** (если @Architect не санкционирует иное в `spec.md`); **Δ каталога `param_catalog` = 0** (флаги — env-only `ClassVar`); не ломать рабочий rich-путь саммари; бэкапы/теги `pre-round1025*`/`git stash@{0}` **НЕ удалять**; F0/Эпик 2/промпты-каноны не трогать; force-ru conventional commits, код+тесты атомарно.
> **Базовая линия (hotfix2):** pytest **8003/0**, JS **21/21**, matrix **0**, `database is locked` = **0**, APP_VERSION **2.58.2**, HEAD **`fe0f7bb`** (+ hotfix2 `fea2daa`).

## Установленные причины (данность, НЕ перепроверять)

- **A — саммари без обложки.** Причина НЕ в обложке: на Stage-1 «Редактор» System 2 handoff не парсится (`parse_summary_handoff` — невалидная выжимка) из-за таймаутов `nano-gpt.com` → `summary system2: невалидная выжимка редактора — fallback` → одиночный путь → `cover_prompt=""` → plain. Успешные rich-отправки с обложкой в логах **есть** (01:03/05:13/07:03). Файлы: `services/summary_generator.py` (ветки `:393-399`, `:500-504`; `_deliver_rich` `:667-716`; логи `:685-709`), `services/telegram_send.py` (`build_cover_media` `:168-187`, `send_rich_message` `:190-215`).
- **B — STT не выполняется.** Нет извлечения/сжатия аудио перед STT. Файлы: `SmartModule/service.py:94-148` (гейт `size_mb > max_mb`), лимиты `SmartModule/transcriber/{groq,openrouter}_transcriber.py`, `config/settings.py:1373-1374`, вызовы `handlers/youtube.py:607-655,1082-1206`, `handlers/voice_transcription.py:272-280`.
- **C — анти-клише молча дропает.** `services/anticliche_worker.py:226-258` `build_patterns` тихо отбрасывает: длина <2/>120 (`services/negative_constraints.py:217-237`), совпадение с хардкод-клише (`find_forbidden_cliches` `:297-335`, напр. «подводя итог»), дубли, cap. API `web/api/anticliche.py` (`:29-42,130-141`) отдаёт 200 и уменьшенный `count`; UI `web/app.js:4938-4959` игнорирует `count` и всегда пишет «Сохранено».
- **D — LLM-таймауты (корень A).** За 24ч: `ReadTimeout=189`, `LLMTimeoutError=28`, `GraphExtractionError=10`; провайдер `nano-gpt.com`. Корень провайдерский.

---

## 0. Точка отката и baseline (выполнять ПЕРВЫМ)

- [x] **T-2482** — **Точка отката hotfix3 + фиксация baseline.** Тег `pre-round1025-hotfix3`; бэкап изменяемых областей в `var/backups/hotfix3-round1025-<ts>/`; `.env.bak.round1025-hotfix3` (значения не печатать). Зафиксировать baseline: HEAD `fe0f7bb` (+`fea2daa`), pytest **8003/0**, JS **21/21**, matrix **0**, `database is locked` = **0**, APP_VERSION **2.58.2**, SQLite `user_version` — как есть.
  **Готово, когда:** тег + бэкап + `.env.bak` существуют, baseline записан, доказана возможность возврата (R18). Секреты/`plans/current_task.md`/zip-архивы в git не добавлять.

## 1. [P0] A — Саммари: обложка и rich на fallback-пути (выполнять ∥ с разделом 2)

> Инвариант: **рабочий rich-путь с обложкой не ломать** (логи 01:03/05:13/07:03 — эталон «как должно быть»).

- [x] **T-2483** — **Диагностическая фиксация причины фолбэка (сейчас тихо).** В `services/summary_generator.py` (ветки `:393-399`, `:500-504`; логи `:685-709`) добавить структурированное событие с **причиной** перехода на одиночный путь (невалидный `parse_summary_handoff`, таймаут Stage-1, пустая выжимка) — R17-safe, без содержимого сообщений/ключей.
  **Готово, когда:** при любом фолбэке в логе есть событие с причиной (не молча); секретов/текста пользователя в логе нет.
- [x] **T-2484** — **Устойчивость Stage-1:** безопасный парсинг `parse_summary_handoff` (невалидный JSON/выжимка → явная ошибка с причиной, а не молчаливый дроп) + повтор при таймауте провайдера (ограниченное число попыток, без роста расходов/зависаний).
  **Готово, когда:** при таймауте Stage-1 выполняется повтор; при валидном втором ответе handoff используется (одиночный путь не включается); число попыток ограничено и залогировано.
- [x] **T-2485** — **Fallback-путь НЕ теряет обложку.** На fallback-пути **не** выставлять `cover_prompt=""`: сгенерировать `cover_prompt` и отправить rich (через существующие `build_cover_media` `services/telegram_send.py:168-187` + `send_rich_message` `:190-215`); plain — только если обложка/rich реально недоступны (с явной причиной в логе).
  **Готово, когда:** при фолбэке Stage-1 сообщение уходит **rich с обложкой** (либо, в исключительном случае, plain с залогированной причиной) — подтверждено тестом и логом.
- [x] **T-2486** — **Kill-switch (env-only `ClassVar`, default ON):** `SUMMARY_COVER_FALLBACK_ENABLED` (вне `param_catalog` → **Δ каталога = 0**).
  **Готово, когда:** OFF → поведение **байт-в-байт** прежнее (plain-фолбэк), флаг читается только из env; ON — новое поведение.
- [x] **T-2487** — **Регресс-тесты A:** (1) фолбэк Stage-1 → rich с обложкой; (2) успешный rich-путь не изменён; (3) логирование причины; (4) парсинг невалидного handoff без молчаливого дропа.
  **Готово, когда:** тесты зелёные и **падают на старом коде** (доказывают фикс); рабочий rich-путь подтверждён.

## 2. [P0] C — Анти-клише: честный отчёт о сохранении (выполнять ∥ с разделом 1)

- [x] **T-2488** — **Согласование лимита длины (500 vs 120) — решение и приведение к канону.** Зафиксировать единое значение лимита для ручных фраз и для автогенерации (`services/negative_constraints.py:217-237`), синхронизировать с UI/API; при расхождении семантики — развести «лимит длины фразы» и «лимит числа паттернов».
  **Готово, когда:** лимит(ы) заданы единым каноном (значение зафиксировано в отчёте), нет расхождения код↔UI↔API; изменение покрыто тестом.
- [x] **T-2489** — **`build_patterns` возвращает разбивку отброшенного.** В `services/anticliche_worker.py:226-258` вместо молчаливого дропа вернуть структуру: `invalid` | `hardcoded` | `duplicate` | `over_limit` + число фактически сохранённых (`saved`/`count`).
  **Готово, когда:** на выходе есть разбивка по причинам и итоговое число сохранённых; ни одна причина не теряется молча.
- [x] **T-2490** — **Семантика пользовательских фраз: НЕ фильтровать хардкод-правилами.** Фразы, добавленные пользователем вручную, **сохраняются** (пользователь добавляет их осознанно); `find_forbidden_cliches` (`:297-335`) применяется только к **автогенерации**, но не дропает ручной ввод (допустимо вернуть их в отчёте как `hardcoded`-помеченные, сохранив запись).
  **Готово, когда:** ручная фраза «подводя итог» сохраняется и не исчезает; хардкод-фильтр больше не блокирует пользовательский ввод.
- [x] **T-2491** — **API `web/api/anticliche.py` (`:29-42,130-141`): честный контракт.** Возвращать разбивку отброшенного и фактический `count` сохранённых; не маскировать частичное сохранение под полный успех (200 ≠ «всё сохранено»).
  **Готово, когда:** ответ API содержит `saved` + `dropped` по причинам; частичное сохранение отличимо от полного.
- [x] **T-2492** — **UI `web/app.js:4938-4959`: честный warn.** Учитывать `count`; показывать «Сохранено N из M» + причины отброса (invalid/hardcoded/duplicate/over_limit); **не** писать «Сохранено», если что-то отброшено.
  **Готово, когда:** при частичном сохранении пользователь видит честное предупреждение с разбивкой; при полном — прежний успех.
- [x] **T-2493** — **Регресс-тесты C:** сохранение ручных фраз; возврат разбивки; UI-текст «N из M»; тесты **падают до фикса**.
  **Готово, когда:** тесты зелёные и доказывают фикс.

## 3. [P0] B — STT: извлечение/сжатие аудио перед транскрибацией

> Цель: файл **28 МБ** транскрибируется; покрыть **видео** и **ГС/кружки**; логировать `reason=compressed`.

- [x] **T-2494** — **Helper подготовки аудио:** ffmpeg → **ogg/opus mono 16 kHz, ~24 kbps**; если после сжатия всё ещё > лимита — **fallback-чанкинг**; ffmpeg недоступен → чанкинг/честная ошибка (не молчаливый отказ).
  **Готово, когда:** helper принимает файл > `max_mb` и отдаёт сжатый(е) артефакт(ы) под лимит; отсутствие ffmpeg не приводит к тихому провалу.
- [x] **T-2495** — **Гейт размера `SmartModule/service.py:94-148`:** заменить «`size_mb > max_mb` → отказ» на compress-then-STT (при необходимости — чанкинг); логировать `reason=compressed` (+ размеры до/после).
  **Готово, когда:** крупный файл не отбрасывается по размеру, а сжимается/чанкуется; в логе есть `reason=compressed`.
- [x] **T-2496** — **Покрыть путь видео** (`handlers/youtube.py:607-655,1082-1206`; лимит `config/settings.py:1373-1374`): сжатие/чанкинг перед вызовом транскрибера.
  **Готово, когда:** видео 28 МБ доходит до STT через сжатие; поведение лимитов согласовано с `VIDEO_TRANSCRIBE_MAX_SIZE_MB`.
- [x] **T-2497** — **Покрыть путь ГС/кружков** (`handlers/voice_transcription.py:272-280`): сжатие/чанкинг перед STT (лимит ГС 20 МБ).
  **Готово, когда:** ГС/кружок > лимита транскрибируется через сжатие; ошибок размерного гейта нет.
- [x] **T-2498** — **Kill-switch (env-only `ClassVar`, default ON):** `STT_AUDIO_COMPRESS_ENABLED` (Δ каталога = 0).
  **Готово, когда:** OFF → прежнее поведение (честный отказ по размеру), флаг env-only.
- [x] **T-2499** — **Регресс-тесты B:** 28 МБ → успешная транскрибация (сжатие + чанкинг); `reason=compressed` в логе; малые файлы не деградируют; отсутствие ffmpeg → корректный фолбэк. Тесты **падают до фикса**.
  **Готово, когда:** тесты зелёные и доказывают фикс; малый файл обрабатывается как раньше.

## 4. [P1] D — LLM-таймауты `nano-gpt.com` (корень A)

- [x] **T-2500** — **Безопасная подстройка таймаутов/ретраев + логирование причины.** Оценить и при обоснованности изменить таймауты/число ретраев (fail-fast вместо долгого ожидания, без роста расходов/зависаний); **явно пометить корень как провайдерский**; добавить R17-safe логирование причины таймаута (`ReadTimeout`/`LLMTimeoutError`).
  **Готово, когда:** решение обосновано (числа до/после) либо явно зафиксировано «не менять»; причина таймаута логируется; успешность ответов не ухудшена.
- [x] **T-2501** — **Второй резервный провайдер + наблюдаемость.** Оценить целесообразность второй резервной цепочки (внедряем/нет + почему); добавить метрику/событие доли таймаутов и fallback-срабатываний.
  **Готово, когда:** вывод по второму провайдеру зафиксирован; есть метрика/событие для мониторинга таймаутов.

## 5. Регресс, ревью, деплой и верификация

- [x] **T-2502** — **Полный регресс:** pytest **8003/0 (+ новые), JS 21/21, matrix 0**; `database is locked` = **0**; `node --check web/app.js` OK; `git diff --check` exit 0; отсутствие регрессий F0/hotfix2/F1.
  **Готово, когда:** все показатели подтверждены логом/отчётом, расхождений нет.
- [ ] **T-2503** — **Ревью/аудит:** @Reviewer **Approved**; @Scanner **Critical 0 / High 0** (Medium/Low — в техдолг) — отчёт `plans/reports/round1025_hotfix3_scanner_audit.md`.
  **Готово, когда:** вердикты зафиксированы отчётом.
- [ ] **T-2504** — **Деплой:** атомарные русские conventional commits (код+тесты) → push `origin/master` → прод `git pull --ff-only` → `systemctl restart admin_bot` (+ bump `APP_VERSION`/`?v=`, если менялись ассеты) → `/api/health` 200.
  **Готово, когда:** прод обновлён, health 200, в отчёте commit(ы) и версия.
- [ ] **T-2505** — **Live-верификация (post-deploy):** (1) саммари реально уходит **rich с обложкой** (и на fallback-пути); (2) видео **28 МБ** транскрибируется; (3) ручные фразы анти-клише сохраняются с **честным отчётом**; (4) таймауты логируются с причиной; (5) `database is locked` = 0; (6) нет регрессий разделов TMA (консоль без `ReferenceError`).
  **Готово, когда:** приложены доказательства (лог/файл/скрин/версия) по каждому пункту.
- [ ] **T-2506** — **Архивация (Шаг 8 @PM) + интеграция @Architect.** Перенос `plans/features/hotfix3-summary-stt-anticliche-round1025/` → `plans/archive/`; обновить `plans/ARCHITECTURE.md` (§) и `plans/backlog.md`; бэкапы/теги/`stash@{0}` не удалять (R18).
  **Готово, когда:** папка в `plans/archive/`, ARCHITECTURE/backlog обновлены, отчёт оставлен.

---

## Feature flags / раскатка

- **Поэтапная раскатка internal → 10% → 50% → 100% НЕ требуется** (пет-проект, один прод; прецедент §53/§54.1). Вместо этого — **env-only `ClassVar` kill-switches**:
  - **A:** `SUMMARY_COVER_FALLBACK_ENABLED` — default **ON**; OFF → прежнее plain-поведение (байт-в-байт).
  - **B:** `STT_AUDIO_COMPRESS_ENABLED` — default **ON**; OFF → прежний размерный гейт.
  - **C/D:** отдельные флаги не заводить (фикс контракта/честности); откат — `git revert` + cache-bust при правке ассетов.
- **Откат целиком:** тег точки отката (T-2482) + `git revert` коммита хотфикса (+ возврат `APP_VERSION`, если менялся).
- **Δ каталога = 0** (флаги вне `param_catalog`); **Δ DDL = 0**.

## Порядок и зависимости (критический путь)

- **T-2482** — обязательно ДО любых правок.
- **A (T-2483…T-2487) ∥ C (T-2488…T-2493)** — независимы (разные модули: summary/telegram_send vs anticliche/negative_constraints/api/app.js).
- **B (T-2494…T-2499)** — после A∥C.
- **D (T-2500…T-2501)** — после B (T-2500 опирается на логирование из T-2483/T-2484).
- **Регресс/ревью/деплой/верификация (T-2502…T-2505)** — после всех код-изменений; T-2505 — после T-2504.
- **Архивация (T-2506)** — после T-2505.
- Дубли ID нет; **ступень общих файлов:** `web/app.js` — только раздел C (T-2492), с A/B не пересекается.

## Риски

- **Ломка рабочего rich-пути саммари** (**Critical**): логи 01:03/05:13/07:03 — эталон; T-2487 обязателен, T-2486 kill-switch.
- **Тихий фолбэк остаётся незамеченным** (**High**): без T-2483/T-2484 причина не видна; T-2485 не закрывать без теста+лога.
- **Ложное «Сохранено» в анти-клише** (**High**): T-2491/T-2492 — иначе 200 маскирует потерю ручных фраз; T-2490 — ручные фразы не фильтровать.
- **ffmpeg/чанкинг на проде** (**High**): T-2494/T-2495 — проверить доступность ffmpeg и корректный фолбэк; иначе B останется сломанным.
- **Секреты в логах/отчётах** (**Critical, R17/R18**): логи только R17-safe; `.env`/`current_task.md` не коммитить; бэкапы/теги/`stash@{0}` не удалять.
- **Смежная инфра/таймауты отвлекают от P0** (**Medium**): D (P1) — после A/B/C; не расширять хотфикс.

## Открытые вопросы (владельцу / @Architect при Step 2)

1. **Стратегия Stage-1 (ключевой):** приоритет — **генерация обложки на fallback-пути** (гарантированная обложка) или **устойчивость парсинга Stage-1** (retry/повтор, меньше фолбэков)? Делать оба или выбрать одно как основное? (T-2484/T-2485)
2. **Анти-клише — канонический лимит длины:** **500** (UI) или **120** (backend `negative_constraints`)? Или развести два лимита (длина фразы vs число паттернов)? (T-2488)
3. **Анти-клише — фильтрация ручных фраз:** подтверждаем, что пользовательские фразы **НЕ** фильтруются хардкод-правилами (`find_forbidden_cliches`), а ограничиваются только длиной/дублями/cap? (T-2490)
4. **STT — режим:** сжатие в **ogg/opus mono 16 kHz ~24 kbps** достаточно для качества транскрипции, или нужен другой формат/битрейт? Чанкинг — только как fallback после сжатия? (T-2494/T-2495)
5. **STT — инфра:** гарантирован ли `ffmpeg` на прод-хосте/в контейнере; если нет — допустим ли fallback-чанкинг без ffmpeg или требуется установка (ops)? (T-2494)
6. **D — провайдер:** менять таймауты/ретраи или fail-fast + **второй резервный провайдер**? Кто владелец деградации `nano-gpt.com`, нужна ли эскалация? (T-2500/T-2501)
7. **Флаги:** имена/env-only `ClassVar` (`SUMMARY_COVER_FALLBACK_ENABLED`, `STT_AUDIO_COMPRESS_ENABLED`) — согласованы? Нужны ли отдельные UI-флаги (ADR-1024-13) или env достаточно? (T-2486/T-2498)

---

## Правки по ревью (итерация 2, commit `090d2e7`)

- [x] **R-1 [High]** `SmartModule/transcriber/audio_prep.py`: temp-каталоги чанкинга (`stt_seg_*`) регистрируются в `AudioPrepResult.tmp_dirs` и удаляются в `cleanup()` (`shutil.rmtree`); тест «после cleanup нет файлов и каталогов».
- [x] **R-2 [Medium]** `SmartModule/service.py::transcribe_voice`: сбой одной части чанкинга не теряет уже расшифрованные — частичный результат склеивается (лог `reason=partial`); тест «сбой одного чанка».
- [x] **R-3 [Medium]** `services/llm_client.llm_stats` отдаётся аддитивно в `GET /api/status` (поле `llm_stats`, только числа); тесты `/api/status` и снимка.
- [x] **R-4 [Medium]** `tests/test_hotfix3_summary_fallback_round1025.py`: стабит `sg.build_cover_media` — тесты не зависят от версии aiogram (`InputRichMessageMedia`); падения были средовыми (те же тесты падают и на `fe0f7bb`).
- [x] **R-5 [Medium]** prep вынесена ДО `async with self._semaphore`; тест порядка (prep → семафор).
- [x] **R-6 [Low]** `_compress_target_mb`: сжатие только при `size_mb > max(limits)`; тест «22 МБ при гейтах [25,20] не сжимается».
- [x] **R-7 [Low]** UI: per-line кап 120 (`limitClicheDraft` в `app.js`, `@input` в `index.html`) + подсказка; HTML `maxlength` на многострочное поле не применяется (ограничил бы весь черновик).
- [x] **R-8 [Low]** ADR-1025-7: обложка на fallback — детерминированная (без доп. LLM-вызова); `LLM_FALLBACK_TIMEOUT_SECONDS` — таймаут одной попытки, не бюджет цепочки (комментарии `settings.py`/`llm_client.py` синхронизированы).
