# Задачи: config-read-path-audit

## Миграция (выполнена)

- [x] Миграция read-путей всех параметров на hot.get — коммит `6156047`.

## Остаточный аудит

- [ ] Остаточный аудит: grep по `settings\.` в рантайм-пути (хендлеры/сервисы) → каждое прямое чтение: мигрировать ИЛИ осознанно оставить (дефолт/не-горячее) с пометкой.
- [ ] Import-time чтения: декораторы/инициализация (пример — `UserIdFilter(hot.get(...))` в alan.py:120, N3-комментарий) — вынести за cache.init или зафиксировать фолбек-семантику.

## Верификация

- [ ] Верификация: изменение параметра в админке → `/debug_config <ENV_NAME>` показывает новое значение (сквозная процедура MEMORY.md).

## Регресс

- [ ] Тесты-регресс: test_hot_config/test_config_cache/test_hot_migration + полный pytest.
