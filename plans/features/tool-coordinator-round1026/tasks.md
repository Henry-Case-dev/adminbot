# A1 — `tool-coordinator-round1026` (Эпик 3 «Agentic Intelligence», Wave 1, Раунд 10.26)

> **ТЗ:** `plans/current_task.md` **§13 «ЦЕЛЕВАЯ АРХИТЕКТУРА»** (`:4440–4479`): логический процесс «Сообщение Telegram → Определение намерения и адресата → **Синтезатор / Координатор** → Решение о действии → При необходимости — инструменты → Проверка результатов → Формирование итогового действия → Вербализатор, генератор изображения, реакция или молчание»; «Не каждый запрос должен проходить через все этапы.»; «Если достаточно молчания, Вербализатор не запускается.»; «Если достаточно реакции, полноценный текст не генерируется.»; «Если требуется несколько инструментов, их результаты последовательно передаются следующему этапу.»; **«Не создавать дополнительный LLM-вызов там, где достаточно обычной программной логики.»** **§14 «ЕДИНЫЙ КООРДИНАТОР ИНСТРУМЕНТОВ»** (`:4482–4517`): «Существующий Синтезатор расширяется возможностью выбирать и координировать инструменты.»; «решение о действии и итоговый текст — разные задачи.»; Синтезатор — «определяет намерение; разрешает адресата; определяет необходимость памяти; выбирает инструменты; оценивает результаты; выбирает действие.»; Вербализатор — «получает подготовленные факты; получает выбранный стиль; создаёт текст; **не выполняет самостоятельно скрытые серверные операции**.»; «Использовать существующую архитектуру tool calling, если она поддерживает последовательные вызовы.»; «Не реализовывать отдельный обработчик для каждой комбинации инструментов. Нужен общий механизм выполнения цепочек.»
> **Границы (verbatim-ориентиры; НЕ реализуются в A1):** **§15–§17** (последовательные tool calls, контракты инструментов, ограничение цепочек — **A2**); **§38–§40/§42–§48** (`action`/`style`, новый Decision Making — **A7**); **§18–§25** (генерация изображений, единый image request — **A3/A4**); **§32–§35** (`get_user_context`, structured memory lookup — **A6**); **§41/§44** (реакции Telegram, когда молчать/отвечать/реагировать — **A8**); **§49/§51** (события/карта вызовов, REUSE ExecutionGraph — **A9**). Разметка волн зафиксирована Step 2 — ADR-1026-14 **D8** (T-3501 ✅).
> **Тип:** backend/agentic (оркестрация инструментов); канон промптов — **NOT_APPLICABLE** (ADR-1013-3, ADR-1026-14 **D5**: промпты не меняются). **P0** — Wave 1 Эпика 3.
> **Зависит от:** **A0** ✅ (`plans/docs/agentic-audit-round1026.md`, APPROVED, handoff §8.3 — durable-вход по анкорам `#tool-map-llm`, `#tool-map-direct`, `#s12-12`, `#epic3-reuse`, `#epic3-a1-a10`, `#epic3-summary`), **Эпик 1** ✅ (§70) и **Эпик 2** ✅ (S1–S10, deploy VERIFIED 2.58.29) — завершены. **A2** — следом (последовательные вызовы/контракты/лимиты цепочек).
> **Baseline (из Step 0 @Memory, verified A0/S10; в Step 1 НЕ перемеряется):** HEAD **`e3ea367`** == `origin/master`; `APP_VERSION` **2.58.29**; pytest `.venv` **9026/0** (verified A0/S10); JS **47/47**; каталог **REGISTRY 469 / GROUPS 100 / `_TAB_BY_GROUP` 98 / TAB_RULES 21 / categorized 444 / Settings 426**; **Δ DDL=0** (SQLite v12). Рабочее дерево — только машинный блок `plans/workflow_state.md`. Точка отката — annotated-тег **`pre-round1026-a1`** → **`e3ea367`** (T-3500, @DevOps); теги/бэкапы **не удаляются** (R18).
> **ID:** **T-3500…T-3524 (25)** — дублей нет, гэпов нет (максимум фактически занятого ID — **T-3499**, архивация A0; `T-3500` встречается в `plans/workflow_state.md` только как порог «T-IDs ≥ T-3500», как ID задачи **не использовался**). *Сверка T-3502 подтвердила: SC/ADR Step 2 укладываются в существующие T-3500…T-3524; **новые ID не вводятся**, сквозная нумерация без пропусков сохранена.*
> **ADR:** **Step 2 @Architect ✅** (T-3501) — **ADR-1026-14** (`Proposed` → `Accepted` по merge, T-3520), решения **D1–D10**. Закрыты открытые вопросы (a)–(e): scope = вариант (i) (**D1**); контракт решения внутренний, wire-`action` не вводится (**D2**); общий механизм цепочек = reuse `tool_loop`, §15–§17 → A2 (**D3**); изоляция Вербализатора (**D4**); AMEND-карта и канон промптов (**D5**); Δ DDL=0/Δ каталога=0 (**D6**); deploy ДА/bump (**D7**); границы волн + REUSE ExecutionGraph + §104 (**D8**); R17-safe наблюдаемость (**D9**); Risk-Level **R2** (**D10**). Карта **D → задачи** — ниже (раздел «Карта решений…»).
> **Deploy (подтверждено Step 2 — ADR-1026-14 D7):** **ДА**, bump `APP_VERSION` **2.58.29 → 2.58.30** (точную версию подтверждает @DevOps в T-3522). Активация автоматическая (push/ff); координатор активен по умолчанию. **Kill-switch — env-only `DIRECT_COORDINATOR_ENABLED`** (default ON, `ClassVar`, **Δ каталога=0**, резолв per-call); OFF → точный legacy-путь. Откат: hot — kill-switch; cold — annotated-тег **`pre-round1026-a1`** → `e3ea367` + `git revert`; **канон-откат не требуется** (промпты не менялись). Теги/бэкапы не удаляются (R18).
> **Риск (`Risk-Level` — финальный @Architect, ADR-1026-14 **D10**):** **R2** (сверка T-3502 подтверждает; Reviewer может повысить — T-3518/T-3519). Обоснование: A1 меняет живой контур принятия решений direct-чата (выбор инструментов/действия) при запрете 3-го LLM-вызова; blast radius — каждый диалог; дизайн поведенчески-сохраняющий (структура+изоляция+наблюдаемость), без DDL/каталога, обратимый (kill-switch + тег). **KG-риски (4, заданы Step 0):** **Critical** — «3-й LLM-вызов / регресс живого direct-чата»; **High** — «per-combination обработчики»; **High** — «дрейф канона промптов»; **High** — «дублирование инструментов/сервисов». `SpecDecision-a1-coordinator-scope` — **CLOSED** (Step 2; **D1** — вариант (i)).
> **⛔ Границы:** product code — только **Step 3 @Builder** (после Step 1/2 и сверки T-3502); **`plans/current_task.md` (immutable user-input) — НЕ трогать**; машинный блок `OPENCODE_WORKFLOW_STATE_V1` — не редактировать вручную; `spec.md`/ADR — **Step 2 @Architect ✅** (сверкой **не изменяются**); сверка T-3502 правит **только этот `tasks.md`**; `plans/backlog.md`, `plans/metrics.md`, `plans/ARCHITECTURE.md` — на своих шагах (handoff/merge); durable-аудит A0 `plans/docs/agentic-audit-round1026.md` — **только чтение**. **Без коммитов из этого шага (сверка T-3502).** **@Scanner удалён** — единый Reviewer gate; нигде не назначать.

## Трассируемость (REQ → verbatim §ТЗ → блок → задачи → SC/ADR) — колонки SC/ADR **заполнены сверкой T-3502 (✅)**
> REQ-A1-01…-12 выделены из §13/§14 (+ границы §15–§51) и KG-рисков Step 0. Колонки **SC** и **ADR (Dn)** заполнены по `spec.md` §2/§7 и ADR-1026-14 (D1–D10) после Step 2. REQ-состав **не сужен**; новых REQ не добавлено; **orphan-REQ нет**.
| REQ | Источник (verbatim, `current_task.md`) | Блок | Задачи | SC / ADR (Dn) |
|---|---|---|---|---|
| REQ-A1-01 | §13 «Сообщение Telegram → Определение намерения и адресата → Синтезатор / Координатор → Решение о действии → При необходимости — инструменты → Проверка результатов → Формирование итогового действия → Вербализатор, генератор изображения, реакция или молчание. Не каждый запрос должен проходить через все этапы. Если достаточно молчания, Вербализатор не запускается. Если достаточно реакции, полноценный текст не генерируется.» | B | T-3503, T-3505 | SC-A1-01; **D1, D2, D4** |
| REQ-A1-02 | §13 «Если требуется несколько инструментов, их результаты последовательно передаются следующему этапу.» | C | T-3506, T-3507 | SC-A1-02; **D3** |
| REQ-A1-03 | §13 «Не создавать дополнительный LLM-вызов там, где достаточно обычной программной логики.» | B, C, D | T-3503, T-3505, T-3506, T-3509 | SC-A1-03; **D1, D2** |
| REQ-A1-04 | §14 «Существующий Синтезатор расширяется возможностью выбирать и координировать инструменты.» + Синтезатор «определяет намерение; разрешает адресата; определяет необходимость памяти; выбирает инструменты; оценивает результаты; выбирает действие.» | B | T-3503, T-3504, T-3505 | SC-A1-04; **D1** |
| REQ-A1-05 | §14 «решение о действии и итоговый текст — разные задачи.» | B, D | T-3505, T-3509 | SC-A1-05; **D1, D2** |
| REQ-A1-06 | §14 Вербализатор «получает подготовленные факты; получает выбранный стиль; создаёт текст; не выполняет самостоятельно скрытые серверные операции.» | D | T-3509, T-3510 | SC-A1-06; **D4** |
| REQ-A1-07 | §14 «Использовать существующую архитектуру tool calling, если она поддерживает последовательные вызовы.» | C | T-3506, T-3507, T-3508 | SC-A1-07; **D1, D3** |
| REQ-A1-08 | §14 «Не реализовывать отдельный обработчик для каждой комбинации инструментов. Нужен общий механизм выполнения цепочек.» | C | T-3506, T-3507, T-3508 | SC-A1-08; **D3** |
| REQ-A1-09 | REUSE ExecutionGraph (§65 F6 / §79 S8): `execution_graph_source.build_graph` → `web/api/analytics.py` → `web/static/execution_graph.js`; **вторую аналитику не создавать**; события/наблюдаемость координатора | E, G | T-3512, T-3516 | SC-A1-09; **D8, D9** |
| REQ-A1-10 | Границы ближайших волн: §15–§17 «Инструменты должны уметь использовать результаты предыдущих инструментов… Не выполнять зависимые инструменты параллельно…» (A2); §38–§40/§42–§48 (`action`/`style`, новый Decision Making — A7) | G | T-3517 | SC-A1-10; **D3, D8** |
| REQ-A1-11 | Границы смежных волн: §18–§25 (изображения — A3/A4); §32–§35 (`get_user_context` — A6); §41/§44 (реакции Telegram — A8); §49/§51 (события/карта вызовов — A9) | G | T-3517 | SC-A1-11; **D5, D8** |
| REQ-A1-12 | Инварианты проекта: freeze канона **10** инструментов; **нет 3-го LLM-вызова**; общий механизм цепочек; Вербализатор без скрытых серверных операций; REUSE ExecutionGraph; **§104 `generate_image` не трогать**; **§85-UI — отдельная санкция**; канон промптов **ADR-1013-3**; **Δ DDL=0**; R17/R18; обратная совместимость живого direct-чата (OFF/legacy) | 0, B–H | T-3500, T-3501, T-3502, T-3511, T-3513, T-3514, T-3515, T-3516, T-3518, T-3522, T-3524 | SC-A1-12; **D2, D5, D6, D7, D9, D10** |

## Приёмочные инварианты A1 (нарушение = НЕ принято)
> Опираются на §13/§14 + границы §15–§51 и KG-риски Step 0. **SC-номера проставлены сверкой T-3502** (`spec.md` §7, SC-A1-01…-12): 13 инвариантов сохранены **без потерь** и привязаны к SC/REQ/задачам; противоречий не выявлено.
1. **Freeze канона 10 инструментов — не дублировать:** `TOOL_CALLING_TOOLS` = **10** (`services/tool_schemas.py:337–348`, ADR-1020-4→-1023-5→-1024-20; ADR-1026-13 D3) — состав/порядок не меняются; новых инструментов/сервисов не создаётся; координатор — **расширение** существующего контура. **REQ-A1-12; T-3503, T-3504, T-3516.** → **SC-A1-12** (и SC-A1-04/-08).
2. **Нет 3-го LLM-вызова:** решение о действии — **внутри существующих Stage-1/2** (`_synthesize_direct_answer:899–988`) либо обычной программной логикой; 2-вызовность direct-чата (`await_count==2`) сохраняется; где достаточно программной логики — LLM не вызывается. **REQ-A1-03/-05; T-3503, T-3505, T-3514, T-3515.** → **SC-A1-03** (и SC-A1-01).
3. **Общий механизм цепочек (не per-combination):** используется существующий многораундовый `tool_loop.chat_with_tools` (`TOOL_MAX_ROUNDS=4`, ≤2/раунд); нет обработчика на каждую комбинацию инструментов. **REQ-A1-08; T-3506, T-3507, T-3508.** → **SC-A1-02/-08**.
4. **Вербализатор не выполняет скрытых серверных операций:** получает только подготовленные факты + выбранный стиль → создаёт текст; не вызывает инструменты/сервисы; при молчании/реакции Вербализатор не запускается и текст не генерируется. **REQ-A1-06; T-3509, T-3510.** → **SC-A1-06.**
5. **REUSE ExecutionGraph (§65/§79):** события/карта вызовов — на существующем `execution_graph_source`/`analytics.py`/`execution_graph.js`; **вторая система аналитики не создаётся**; без выдуманных токенов. **REQ-A1-09; T-3512, T-3516.** → **SC-A1-09.**
6. **§104 `generate_image` не трогать:** модель/провайдер/API-ключи/промпт обложки/параметры/обработка ошибок/порядок публикации/прикрепление — без изменений; image-контур (§18–§25) — A3/A4. **REQ-A1-12; T-3516.** → **SC-A1-12** (§104).
7. **§85-UI — отдельная санкция:** изменения каталога/UI (Δ каталога ≠ 0) в A1 не выполняются. **REQ-A1-12; T-3516.** → **SC-A1-12** (§85-UI).
8. **Канон промптов ADR-1013-3 — Step 2 зафиксировал NOT_APPLICABLE (ADR-1026-14 D5):** промпты в A1 **не меняются**; `PREV_*`-слепки/`PROMPT_MIGRATIONS`/эталонные байт-тесты **не добавляются**. Любая правка промпта в ходе реализации — только атомарно по ADR-1013-3 (код + эталон + тесты) и с возвратом к Step 2. **REQ-A1-12; T-3511 (NOT_APPLICABLE).** → **SC-A1-12** (и SC-A1-08: дрейфа канона нет).
9. **Δ DDL=0:** новых таблиц/колонок/индексов нет (SQLite v12); любое изменение — только по санкции Step 2. **REQ-A1-12; T-3500, T-3516.** → **SC-A1-12.**
10. **R17/R18:** в логах/событиях — только числа/коды/id/host/HTTP-статус/`error_type`; без ключей/промптов/сырых текстов/сырых ответов LLM; теги/бэкапы не удаляются. **REQ-A1-12; T-3500, T-3514, T-3518.** → **SC-A1-09/-12.**
11. **Обратная совместимость живого direct-чата:** при OFF/legacy-поведении — артефакты/поведение байт-в-байт (или явный AMEND Step 2); живой диалог не регрессирует. **REQ-A1-12; T-3515.** → **SC-A1-12.**
12. **Границы волн не реализуются в A1:** §15–§17 (A2), §38–§40/§42–§48 (A7), §18–§25 (A3/A4), §32–§35 (A6), §41/§44 (A8), §49/§51 (A9) — фиксируются как вход, но не реализуются. **REQ-A1-10/-11; T-3517.** → **SC-A1-10/-11.**
13. **Трассируемость без orphan'ов:** каждый REQ-A1-01…-12 привязан к блоку/задаче/SC; задач без обоснования нет. **REQ-A1-01…-12; T-3502, T-3524.** → **SC-A1-01…-12** (полное покрытие).

## Карта решений ADR-1026-14 (D1–D10) → задачи/блоки (сверка T-3502 ✅)
> Заполнена сверкой T-3502 по ADR-1026-14 (D1–D10) и `spec.md` §4/§7. Каждое решение привязано к задачам и SC; решений без задач и задач без обоснования нет.
| Решение | Суть (кратко) | Блок | Задачи | SC |
|---|---|---|---|---|
| **D1** | Scope Координатора = **вариант (i)**: модельный выбор инструментов сохранён (`tool_choice='auto'`), добавлен **программный слой решения о действии** внутри существующих Stage-1/2; 0 новых LLM-вызовов; вариант (ii) отклонён | B | T-3503, T-3504, T-3505; тесты T-3513, T-3515 | SC-A1-01, SC-A1-03, SC-A1-04, SC-A1-07 |
| **D2** | Контракт решения — **внутренний** объект (`intent`/`addressee`/`memory_need`/`tool_calls`/`evaluation`/`action`/`style`); **wire-`action` не вводится**; JSON Stage-1/Stage-2 не меняются | B, G | T-3505 (явный запрет `action`-поля), T-3510; тесты T-3513, T-3515; граница T-3517 | SC-A1-01, SC-A1-03, SC-A1-05, SC-A1-12 |
| **D3** | Общий механизм цепочек = **reuse существующего `tool_loop`** (`TOOL_MAX_ROUNDS=4`, ≤2/раунд, fail-open); per-combination обработчики запрещены; §15–§17 → **A2** | C | T-3506, T-3507, T-3508; T-3513; граница T-3517 | SC-A1-02, SC-A1-03, SC-A1-07, SC-A1-08, SC-A1-10 |
| **D4** | Изоляция Вербализатора: только подготовленные факты + стиль → текст; **без скрытых серверных операций**; при молчании не запускается, при реакции текст не генерируется | D | T-3509, T-3510; T-3513, T-3515 | SC-A1-01, SC-A1-05, SC-A1-06 |
| **D5** | **AMEND-карта** (см. ниже) и канон промптов: **ADR-1013-3 → NOT_APPLICABLE** | E, G | **T-3511 (NOT_APPLICABLE)**; T-3516 | SC-A1-11, SC-A1-12 |
| **D6** | Санкции: **Δ DDL=0**; **Δ каталога=0** (`param_catalog.py` вне diff); §85-UI — отдельная санкция; kill-switch только **env-only** (`ClassVar`) | G, H | T-3516; T-3522 | SC-A1-12 |
| **D7** | Deploy = **ДА**, bump `APP_VERSION` **2.58.29 → 2.58.30**; hot-OFF `DIRECT_COORDINATOR_ENABLED=false`; cold rollback — тег **`pre-round1026-a1`** + `git revert`; канон-откат не требуется | 0, H | T-3500 (тег/бэкап), T-3522 (deploy); регресс T-3515 | SC-A1-12 |
| **D8** | Границы волн (A2/A7/A3/A4/A6/A8/A9) **не реализуются**; REUSE ExecutionGraph; §104 `generate_image` не трогать | G | T-3512, T-3516, T-3517 | SC-A1-09, SC-A1-10, SC-A1-11 |
| **D9** | Наблюдаемость Координатора **R17-safe** (числа/коды/id/`error_type`); reuse существующих логов; вторую аналитику не создавать | E, G | T-3512, T-3514, T-3516 | SC-A1-09, SC-A1-12 |
| **D10** | `Risk-Level` **R2** (повышение до R3 — по условиям D10/`spec.md` §10) | 0–H | шапка; T-3518, T-3519 (Reviewer может повысить); T-3524 | SC-A1-12 |

**AMEND-карта (D5) — зафиксирована сверкой T-3502:**
| ADR | Статус в A1 | Суть |
|---|---|---|
| ADR-1026-13 | **REUSE** | Durable-аудит как вход; freeze канона 10 (D3) |
| **ADR-1022-4** | **NOT_APPLICABLE** | Саммари не затрагивается |
| **ADR-1022-5** | **effective AMEND-заметка** (без структурного изменения) | Координатор — внутри существующего пайплайна; 2 вызова System 2 сохранены |
| **ADR-1023-3** | **effective AMEND-заметка** (без структурного изменения) | Внутреннее решение о действии; wire-`action` и полное разведение `action`/`style` — **A7** |
| **ADR-1013-3** | **NOT_APPLICABLE** | Промпты не меняются; `PREV_*`/`PROMPT_MIGRATIONS`/эталон не добавляются → **T-3511 закрывается подтверждением NOT_APPLICABLE** |
| ADR-1025-24 | **REUSE** | Дисциплина верификационного гейта (deploy-вердикт) |

## Связь с A2 и смежными волнами (границы Эпика 3 — не реализуется в A1)
> A1 — **Wave 1**: координатор внутри Синтезатора + общий механизм цепочек + изоляция Вербализатора. Ниже — что A1 **НЕ** реализует (зафиксировано Step 2 — ADR-1026-14 **D8**). **§104 `generate_image` — НЕ ТРОГАТЬ**; **§85 UI — вне scope** (Δ каталога ≠ 0 требует отдельной санкции).
| §ТЗ | Тема | Ориентир |
|---|---|---|
| §15–§17 | Последовательные tool calls, контракты инструментов, ограничение цепочек | **A2** (A1 — только общий механизм на существующем `tool_loop`, с явной границей) |
| §18–§25 | Генерация изображений, единый image request, image на основе памяти | A3/A4 |
| §26–§31 | Дневной лимит изображений, Mini App, TZ/сброс | A5 |
| §32–§35 | `get_user_context`, structured memory lookup, досье+RAG/фактчек | A6 |
| §36–§37 | Комбинация URL+фактчек, безопасность tool output | A7 |
| §38–§40/§42–§48 | Новый Decision Making, разведение `action`/`style`, контракт решения | **A7** (A1 не вводит `action`) |
| §41/§44 | Реакции Telegram, когда молчать/отвечать/реагировать | A8 |
| §49/§51 | Настройки Decision Making, диагностика, карта вызовов (REUSE ExecutionGraph) | A9 |

## Блок 0 — baseline / точка отката (T-3500)
- [x] **T-3500 [@DevOps] ✅ baseline** — **Цель:** зафиксировать baseline и точку отката A1. **Выход:** annotated-тег **`pre-round1026-a1`** → **`e3ea367`** (+ бэкапы `var/backups/a1-round1026-*/`, `.env.bak.round1026-a1` — по стандарту), фиксация baseline-чисел. **Критерий:** HEAD == `origin/master` == `e3ea367`; `APP_VERSION` **2.58.29**; pytest `.venv` ≥ **9026/0** (при перезамере), JS **47/47**, каталог **469/426/444/100/98/21**, **Δ DDL=0** (SQLite v12) — подтверждены; тег/бэкапы **не удаляются** (R18). **Зависимости:** Step 1 @PM ✅; выполнено **параллельно** Step 2 @Architect. *Deploy для A1 — решение Step 2 (**D7**: ДА/bump **2.58.30**); тег — страховка отката.*

## Блок A — spec + ADR (Step 2 @Architect) + сверка @PM (T-3501…T-3502)
- [x] **T-3501 [@Architect] ✅ spec + ADR-1026-14** — **Цель:** `spec.md` + **ADR** для A1 (ожидаемо **ADR-1026-14**). **Выход:** `spec.md` (REQ-A1-01…-12, SC, трассировка §13/§14 + границы) и ADR (D1…Dn): **scope координатора** (что именно расширяется в `_synthesize_direct_answer`/`parse_direct_synthesis` vs текущее decision-inside-model; где программная логика вместо LLM); **статус AMEND ADR-1022-4/ADR-1023-3** (2-вызовность; `action`/`style` — не в A1); **канон промптов ADR-1013-3** (меняем ли промпты и как — `PREV_*`/`PROMPT_MIGRATIONS`/эталон, атомарно); **общий механизм цепочек** на существующем `tool_loop` (граница A2); **изоляция Вербализатора**; **REUSE ExecutionGraph**; **deploy/Δ DDL/Δ каталога** (санкции). Закрытие `SpecDecision-a1-coordinator-scope` и открытых вопросов (a)–(e) ✅. **Критерий:** каждый REQ-A1-01…-12 имеет SC; ни одно решение не противоречит §13/§14; spec/ADR согласуются с `tasks.md` после сверки T-3502; архитектурная трактовка не сужает требования. **Зависимости:** Step 1 @PM ✅ (параллельно T-3500).
- [x] **T-3502 [@PM] ✅ эта сверка (PLANNING_CONSISTENT)** — **Цель:** сверка `tasks.md` ↔ `spec.md`/ADR после Step 2. **Выход:** заполненные колонки **SC/ADR** в трассировке (REQ-A1-01…-12 → SC → ADR-1026-14 Dn), **карта решений ADR-1026-14 → задачи**, приёмочные инварианты, привязанные к SC (без потерь), подтверждение deploy/отката. **Критерий:** каждый REQ-A1-01…-12 привязан к SC/решению ADR; расхождений, требующих правки @Architect, нет; `PLANNING_CONSISTENT` выдаётся только при согласии scope/исключений, риска, приёмки, зависимостей, deploy/rollback. **Зависимости:** T-3501.

## Блок B — контракт Координатора в Синтезаторе (намерение/адресат/память/выбор инструментов/оценка/действие; без 3-го вызова) (T-3503…T-3505) — @Builder
> **Границы:** расширяется **существующий** Синтезатор (`_synthesize_direct_answer:899–988`); **новый агент не создаётся**; решение о действии — внутри существующих Stage-1/2 либо программной логикой; `tool_choice='auto'` не форсируется.
- [ ] **T-3503 [@Builder]** — **Цель:** определение **намерения и адресата** + **необходимости памяти** (первая часть роли Синтезатора, §14): расширить существующий Синтезатор (Stage-1 `_synthesize_direct_answer:932–935` → `parse_direct_synthesis:936`) так, чтобы намерение/адресат/память определялись **внутри существующего** контура; где достаточно программной логики — без LLM. **Выход:** правки + тесты. **Критерий:** REQ-A1-01/-04/-03; намерение/адресат/память доступны координатору; **3-й LLM-вызов не добавлен**; существующий JSON-контракт не сломан; **wire-`action` не вводится** (D2). **Зависимости:** T-3501 ✅, T-3502 ✅.
- [ ] **T-3504 [@Builder]** — **Цель:** **выбор инструментов** + **оценка результатов** (§14) в рамках существующего tool-контура: координатор выбирает инструменты и оценивает результаты, **reuse** `tool_loop`/`ToolRouter` (не создавать второй роутер/дубликаты инструментов); учёт `active_tools`-гейтов. **Выход:** правки + тесты. **Критерий:** REQ-A1-04/-07/-08; выбор инструментов и оценка результатов работают через существующий контур; **freeze 10 инструментов** не нарушен; per-combination обработчиков нет; **wire-`action` не вводится** (D2). **Зависимости:** T-3503.
- [ ] **T-3505 [@Builder]** — **Цель:** **выбор действия** как отдельная задача от итогового текста (§14): решение о действии формируется координатором, а **не** итоговый текст; точка интеграции — существующий гейт Stage-2 (`:814–818`); **не** вводить `action`-поле (§38–§40 — A7) — минимально развести «решение о действии» и «текст». **Выход:** правки + тесты. **Критерий:** REQ-A1-01/-05/-03; решение о действии ≠ текст; 2-вызовность сохранена; **wire-`action`/`style` не введены** (граница A7; контракт решения — **внутренний**, D2). **Зависимости:** T-3504.

## Блок C — общий механизм цепочек на существующем `tool_loop` (зависимые вызовы A→B; лимиты; fail-open) — с явной границей A2 (T-3506…T-3508) — @Builder
> **Границы A2:** §15–§17 (последовательные вызовы, контракты инструментов, ограничение цепочек) — **не реализуются здесь**; A1 обеспечивает/подтверждает **общий механизм** на существующем многораундовом `tool_loop` (`TOOL_MAX_ROUNDS=4`, ≤2/раунд), без тяжёлой инфраструктуры.
- [ ] **T-3506 [@Builder]** — **Цель:** подтвердить и (при необходимости) усилить **общий механизм цепочек** на существующем `tool_loop.chat_with_tools:89` (многораундовый цикл, role `tool`, второй шаг после tool response — уже поддержан): результаты нескольких инструментов **последовательно** передаются следующему этапу; зависимые вызовы A→B не параллелятся. **Выход:** правки/заключение + тесты. **Критерий:** REQ-A1-02/-07/-08; нет обработчика на каждую комбинацию инструментов; существующий цикл переиспользован. **Зависимости:** T-3501 ✅.
- [ ] **T-3507 [@Builder]** — **Цель:** лимиты/деградация цепочек **в рамках A1**: reuse `TOOL_MAX_ROUNDS=4` / `_TOOL_CALLS_PER_ROUND_MAX=2`; fail-open — частичный результат/заглушка, без бесконечных циклов; **не добавлять тяжёлую инфраструктуру**. Полное ограничение цепочек §17 — **A2**. **Выход:** правки + тесты. **Критерий:** REQ-A1-02/-07; лимиты соблюдены; при исчерпании — graceful degradation (не общий отказ LLM); границы A2 не нарушены. **Зависимости:** T-3506.
- [ ] **T-3508 [@Builder]** — **Цель:** fail-open/частичный результат и **явная граница A2**: зафиксировать, что §15–§17 (параллельные независимые вызовы, структурированные контракты §16, полное ограничение цепочек §17) **не реализуются** в A1; результаты инструментов передаются последовательно. **Выход:** заключение/тесты. **Критерий:** REQ-A1-07/-08/-10; границы A2 задокументированы; общий механизм не превращён в per-combination. **Зависимости:** T-3507.

## Блок D — изоляция Вербализатора (факты+стиль→текст; без серверных операций) (T-3509…T-3510) — @Builder
- [ ] **T-3509 [@Builder]** — **Цель:** изоляция Вербализатора (§14): Stage-2 (`_synthesize_direct_answer:972` → `verbalize_validated`) получает **только подготовленные факты + выбранный стиль** (`response_mode`) и создаёт текст; **не выполняет скрытых серверных операций** (не вызывает инструменты/сервисы, не ходит в сеть/БД сверх передачи фактов). **Выход:** правки + тесты. **Критерий:** REQ-A1-06/-05/-03; Вербализатор не выполняет серверных операций; разведение «решение о действии» ↔ «текст» соблюдено; 2-вызовность сохранена. **Зависимости:** T-3505.
- [ ] **T-3510 [@Builder]** — **Цель:** поведение Вербализатора при не-текстовых исходах (§13): если достаточно **молчания** — Вербализатор **не запускается**; если достаточно **реакции** — полноценный текст **не генерируется**; зафиксировать условия (границы реакций/молчания §41–§47 — **A8**, здесь — только изоляция/условия запуска существующего Stage-2). **Выход:** правки/заключение + тесты. **Критерий:** REQ-A1-01/-06; Вербализатор не запускается при молчании; текст не генерируется при реакции; границы A8 не нарушены. **Зависимости:** T-3509.

## Блок E — промпты/канон (если нужно — AMEND по ADR-1013-3) + события/наблюдаемость (граница A9) (T-3511…T-3512) — @Builder
- [ ] **T-3511 [@Builder]** — **Цель:** промпты — **Step 2 зафиксировал: НЕ меняются** (ADR-1013-3 → **NOT_APPLICABLE**, ADR-1026-14 **D5**). Задача закрывается **подтверждением NOT_APPLICABLE**: `PREV_*`-слепки, `PROMPT_MIGRATIONS`/`ROLLBACK_MIGRATIONS`, эталонные байт-тесты **не добавляются**. **Выход:** заключение NOT_APPLICABLE. **Критерий:** REQ-A1-12; канон ADR-1013-3 соблюдён (NOT_APPLICABLE); дрейфа канона нет. *Если в ходе реализации понадобится правка промпта — только атомарно по ADR-1013-3 и с возвратом к Step 2 (по умолчанию — нет).* **Зависимости:** T-3501 ✅.
- [ ] **T-3512 [@Builder]** — **Цель:** события/наблюдаемость координатора: логировать решения координатора (намерение/выбор инструментов/оценка/действие) **R17-safe** (числа/коды/id/`error_type`, без промптов/сырых текстов); **reuse** существующих логов/событий; **граница A9** — карта вызовов/ExecutionGraph и вторая аналитика **не создаются** здесь. **Выход:** события + тесты. **Критерий:** REQ-A1-09/-12; события R17-safe; вторая система аналитики не создана; граница A9 соблюдена. **Зависимости:** T-3505.

## Блок F — тесты / R17 / регресс живого direct-чата (OFF байт-в-байт; 2-вызовность) (T-3513…T-3515) — @Builder
- [ ] **T-3513 [@Builder]** — **Цель:** тесты координатора: намерение/адресат/память; выбор инструментов + оценка результатов; выбор действия ≠ текст; **общий механизм цепочек** (зависимые A→B последовательны; независимые не ломаются); **отсутствие per-combination обработчиков**. **Выход:** тесты. **Критерий:** REQ-A1-01/-02/-04/-05/-08; координатор покрыт; регрессов нет. **Зависимости:** T-3503, T-3504, T-3505, T-3506, T-3507, T-3509.
- [ ] **T-3514 [@Builder]** — **Цель:** R17-тесты + тесты событий: в логах/событиях координатора — только числа/коды/id/`error_type`; без ключей/промптов/сырых текстов/сырых ответов LLM. **Выход:** тесты. **Критерий:** REQ-A1-12; R17 соблюдён; чувствительные данные не выводятся. **Зависимости:** T-3512.
- [ ] **T-3515 [@Builder]** — **Цель:** регресс **живого direct-чата**: при OFF/legacy-поведении — артефакты/поведение байт-в-байт (или явный AMEND Step 2); **2-вызовность** (`await_count==2`, 0 новых LLM-вызовов); существующий двухвызовный direct (`SYSTEM2_DIRECT_ENABLED`) не регрессирует; `tool_loop`-финал/`lore_compiled`-условия сохранены. **Выход:** тесты. **Критерий:** REQ-A1-12/-03; живой диалог не регрессирует; 2-вызовность подтверждена; OFF/legacy не сломан; **kill-switch `DIRECT_COORDINATOR_ENABLED=false` (env-only, default ON) → точный legacy-путь**; OFF/legacy байт-в-байт. **Зависимости:** T-3505, T-3509, T-3513.

## Блок G — интеграция/границы (§104, §85-UI, ExecutionGraph REUSE, A2/A7) (T-3516…T-3517) — @Architect/@Builder
- [ ] **T-3516 [@Architect/@Builder]** — **Цель:** подтвердить границы (REQ-A1-09/-12): **§104 `generate_image`** — не тронут (модель/провайдер/ключи/промпт/параметры/обработка/порядок/прикрепление); **§85-UI** — отдельная санкция (Δ каталога ≠ 0), вне A1; **REUSE ExecutionGraph** (§65/§79) — вторая аналитика не создана; **Δ DDL=0** (SQLite v12); freeze канона **10** инструментов; `param_catalog.py`/каталог — вне diff (санкции нет). **Выход:** evidence/diff-аудит. **Критерий:** REQ-A1-09/-12; границы не нарушены; Δ DDL=0/Δ каталога=0 подтверждены. **Зависимости:** T-3511, T-3512, T-3515.
- [ ] **T-3517 [@Architect/@Builder]** — **Цель:** границы волн: подтвердить, что §15–§17 (**A2**), §38–§40/§42–§48 (**A7**), §18–§25 (A3/A4), §32–§35 (A6), §41/§44 (A8), §49/§51 (A9) **не реализованы** в A1; зафиксировать handoff-вход для A2 (последовательные вызовы/контракты/лимиты цепочек) и заметку по A7 (`action`/`style`). **Выход:** заключение/границы. **Критерий:** REQ-A1-10/-11; ни одна граница не нарушена; вход A2/A7 зафиксирован. **Зависимости:** T-3516.

## Блок H — финализация (единый Reviewer gate БЕЗ Scanner, merge, архивация, deploy, handoff → A2) (T-3518…T-3524)
- [ ] **T-3518 [@Reviewer]** — **Цель:** **линза 1 единого Reviewer gate** (requirements/correctness): §13/§14 (намерение/адресат/память/выбор инструментов/оценка/действие; решение о действии ≠ текст; изоляция Вербализатора; общий механизм цепочек; последовательные результаты); REQ-A1-01…-12; SC Step 2; **нет 3-го LLM-вызова**; 2-вызовность; нет per-combination; отсутствие ложных закрытий. **Выход:** `review.md` с вердиктом. **Критерий:** все release-блокеры закрыты или отклонены письменно; Critical/High = 0 → «к приёмке ДА» либо блокеры возвращены. **Зависимости:** T-3515, T-3516, T-3517.
- [ ] **T-3519 [@Reviewer]** — **Цель:** **линза 2 единого Reviewer gate** — focused change audit (все содержательные обязанности удалённого @Scanner): freeze канона **10** инструментов; **Δ DDL=0** / **Δ каталога=0** (санкции нет); R17/R18; **§104 `generate_image`** вне diff; **§85-UI** вне diff; REUSE ExecutionGraph (второй аналитики нет); обратная совместимость живого direct-чата; отсутствие утечек и скрытых побочных эффектов; пересчитанные `Reviewed-Commit`/`Working-Tree-Hash`/`Spec-Hash`. **Выход:** единый авторитетный результат в **`review.md`** (отдельный Scanner-отчёт **не создаётся** — Scanner удалён). **Критерий:** Critical/High = 0 → «к деплою ДА» либо блокеры возвращены; binding соответствует фактическому состоянию. **Зависимости:** T-3518.
- [ ] **T-3520 [@Architect]** — **Цель:** merge: раздел `plans/ARCHITECTURE.md` (нумерация по факту) + финализация **ADR-1026-14** (Accepted) + карта решений; реконсиляция, если Step 2 менял архитектурные решения. **Выход:** раздел + ADR. **Критерий:** архитектура сверена; координатор/изоляция Вербализатора/границы задокументированы. **Зависимости:** T-3518, T-3519 (при findings — rework @Builder до merge).
- [ ] **T-3521 [@PM]** — **Цель:** архивация фичи в `plans/archive/` с сохранением ID/ссылок. **Выход:** папка `plans/archive/tool-coordinator-round1026/`. **Критерий:** только после подтверждённого единого `Approved` + binding + deploy VERIFIED (или явно NOT_APPLICABLE) + reconcile @Architect; пути/ссылки разрешены, истории не теряются. **Зависимости:** T-3520.
- [ ] **T-3522 [@DevOps]** — **Цель:** deploy (**Step 2 — ADR-1026-14 D7: ДА/bump** — A1 меняет рантайм direct-чата): bump `APP_VERSION` **2.58.29 → 2.58.30** + `README.md` + cache-bust; push без force; прод ff; `/api/health` 200; `database is locked`=0; применимые §114-проверки. Активация авто (координатор ON по умолчанию); **kill-switch — env-only `DIRECT_COORDINATOR_ENABLED` (Δ каталога=0)**. **Выход:** `deployment.md` (**VERIFIED** или явно **NOT_APPLICABLE** — если так решит Step 2; здесь — VERIFIED). **Критерий:** вердикт с evidence; откат — annotated-тег **`pre-round1026-a1`** → **`e3ea367`** + `git revert`; канон-откат не требуется (промпты не менялись); теги/бэкапы не удаляются (R18). **Зависимости:** T-3521.
- [ ] **T-3523 [@PM/@Orchestrator]** — **Цель:** handoff → **A2** (Wave 2 Эпика 3, §15–§17): зафиксировать вход A2 (общий механизм цепочек, последовательные вызовы, контракты §16, ограничение цепочек §17), обновить backlog-указатель/гейты. **Выход:** handoff-запись. **Критерий:** границы A1/A2 разведены; следующий actionable — A2. **Зависимости:** T-3522.
- [ ] **T-3524 [@PM]** — **Цель:** (резерв) финальная проверка трассируемости A1: полный REQ-A1-01…-12 → §13/§14 → блок → задача → SC → ADR → evidence замкнут; метрики фичи переданы @Orchestrator. **Выход:** подтверждение покрытия. **Критерий:** orphan-REQ нет; каждый REQ имеет задачу/SC; состав задач не сужен. **Зависимости:** T-3523.

## Открытые вопросы (a)–(e) → **закрыты Step 2 @Architect ✅ (T-3501; ADR-1026-14 D1–D10; сверка T-3502)**
- **(a) Точный scope координатора:** что именно меняется в Синтезаторе (`_synthesize_direct_answer:899–988`/`parse_direct_synthesis`) относительно текущего **decision-inside-model** (`tool_loop.chat_with_tools:89`, `tool_choice='auto'`); какие шаги — программная логика, какие — LLM. → **ЗАКРЫТО: вариант (i)** (**D1**) — модельный выбор инструментов сохранён + программный слой решения о действии внутри Stage-1/2; 0 новых LLM-вызовов. T-3501; задачи T-3503…T-3505.
- **(b) AMEND ADR-1022-4/ADR-1023-3 и канон ADR-1013-3:** нужны ли AMEND по 2-вызовности и `action`/`style`; меняются ли промпты (и как — `PREV_*`/`PROMPT_MIGRATIONS`/эталон, атомарно) или NOT_APPLICABLE. → **ЗАКРЫТО** (**D5**): ADR-1022-4 → NOT_APPLICABLE; ADR-1022-5/-1023-3 → effective-заметки; **ADR-1013-3 → NOT_APPLICABLE** (промпты не меняются). T-3501; T-3511.
- **(c) Δ DDL / Δ каталога:** требуется ли Δ DDL (по умолчанию **0**); §85-UI — **отдельная санкция** (Δ каталога ≠ 0 в A1 не допускается без неё). → **ЗАКРЫТО** (**D6**): Δ DDL=0; Δ каталога=0; kill-switch только env-only. T-3501; T-3516.
- **(d) Deploy/bump:** деплоить ли A1 и с каким bump `APP_VERSION`; процедура активации/отката. → **ЗАКРЫТО** (**D7**): deploy ДА, bump 2.58.29 → **2.58.30**; hot-OFF `DIRECT_COORDINATOR_ENABLED=false`; cold — тег `pre-round1026-a1` + `git revert`. T-3501; T-3522.
- **(e) Границы A2 (последовательные вызовы) и A7 (`action`):** что именно уходит в A2 (§15–§17) и A7 (§38–§40/§42–§48) и что A1 обязан оставить нетронутым. → **ЗАКРЫТО** (**D3, D8, D2**): §15–§17 → A2; `action`/`style` → A7 (wire-`action` не вводится); REUSE ExecutionGraph. T-3501; T-3517, T-3523.

## Риски
- **3-й LLM-вызов / регресс живого direct-чата** (Critical, KG Step 0) — координатор обязан решать внутри существующих Stage-1/2 либо программной логикой; инвариант 2; тесты T-3515; линза 1 (T-3518).
- **Per-combination обработчики** (High) — запрет отдельного обработчика на комбинацию (§14); общий механизм на `tool_loop`; инвариант 3; T-3506/T-3507/T-3513.
- **Дрейф канона промптов** (High) — **снят на уровне Step 2: ADR-1013-3 → NOT_APPLICABLE** (промпты не меняются, D5); инвариант 8; T-3511. *Любая непредвиденная правка промпта — только атомарно по ADR-1013-3 и с возвратом к Step 2 (тогда риск возвращается).*
- **Дублирование инструментов/сервисов** (High) — freeze канона 10; REUSE существующих контуров; инвариант 1; T-3504/T-3516.
- **Размытие границ A2/A7** (Medium) — §15–§17 и `action`/`style` не реализуются в A1; инвариант 12; T-3517.
- **Δ каталога ≠ 0 / §85-UI без санкции** (Medium) — санкции нет: Δ каталога=0; любое отклонение — блокер (T-3516).

## Миграционное примечание
> **@Scanner удалён** (24.09.2026) — его обязанности перешли в **единый Reviewer gate**; отдельного Scanner-approval не существует. В A1 ни одна задача не назначается @Scanner; верификация — только **T-3518/T-3519 [@Reviewer]** (обе линзы). Исторические Scanner-отчёты в `plans/reports/` сохраняются и не переименовываются. Управляющие документы (`plans/current_task.md`, машинный блок `OPENCODE_WORKFLOW_STATE_V1`) **не изменяются**; `spec.md`/ADR-1026-14 — **Step 2 @Architect ✅** (сверкой **не изменяются**); сверка T-3502 правит **только этот `tasks.md`**; `plans/metrics.md` — @Orchestrator; `plans/backlog.md`/`plans/ARCHITECTURE.md` — на своих шагах (handoff/merge). **R17/R18** соблюдены.

## Статус шагов (24.09.2026)
- **Step 1 @PM ✅** — создан `tasks.md`: блоки **0/A–H**, **T-3500…T-3524 (25 задач)**, трассируемость REQ-A1-01…-12 (§13/§14 + границы), приёмочные инварианты (13), связь с A2/смежными волнами, открытые вопросы (a)–(e), миграционное примечание (Scanner → единый Reviewer gate).
- **Step 0 @DevOps ✅ (T-3500)** — baseline: HEAD **`e3ea367`** == `origin/master`; `APP_VERSION` **2.58.29**; pytest `.venv` **9026/0**; JS **47/47**; каталог **469/426/444/100/98/21**; **Δ DDL=0** (SQLite v12). Точка отката — annotated-тег **`pre-round1026-a1`** → **`e3ea367`**; бэкапы `var/backups/a1-round1026-20260924-124338/` + `.env.bak.round1026-a1`. Теги/бэкапы **не удаляются** (R18).
- **Step 2 @Architect ✅ (T-3501)** — `spec.md` (REQ-A1-01…-12 → SC-A1-01…-12) + **ADR-1026-14** (D1–D10); открытые вопросы (a)–(e) закрыты; `Risk-Level` **R2**; deploy **ДА**, bump **2.58.30**.
- **Сверка @PM ✅ (T-3502, этот файл)** — колонки **SC/ADR** заполнены; карта **D1–D10 → задачи**; 13 приёмочных инвариантов привязаны к **SC-A1-01…-12** (без потерь); зафиксированы **AMEND-карта (D5)** и **T-3511 = NOT_APPLICABLE**; подтверждены deploy/откат (**D7**), **Δ DDL=0**/**Δ каталога=0** (**D6**) и env-only kill-switch `DIRECT_COORDINATOR_ENABLED`; проверено, что **T-3503…T-3505 не вводят wire-`action`** (**D2**). **Расхождений, требующих правки @Architect, нет → `PLANNING_CONSISTENT` (factual handoff).**
- **Далее:** **Step 3 @Builder** (T-3503…T-3520: блоки B–G, реализация + тесты) → единый Reviewer gate (T-3518/T-3519) → merge (T-3520) → архивация (T-3521) → deploy (T-3522) → handoff → A2 (T-3523/T-3524). Риск — **R2**.
