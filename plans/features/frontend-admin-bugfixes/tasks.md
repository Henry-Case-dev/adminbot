# Задачи: frontend-admin-bugfixes

## Критические фиксы (01.09)

- [x] SDK: подключить официальный telegram-web-app.js (пустая админка/initData) — коммит `09d8c1b`.
- [x] Баг 1 — вкладка «Статус» не грузится до переключения (автозагрузка вкладок) — коммит `591e305`.
- [x] Баг 2 — пустые логи + прямая линия аптайма (единый лог-ring синглтон + аптайм-фолбэк) — коммит `591e305`.
- [x] Баг 3 — тумблеры не меняют состояние (флаги через hot.get + read-пути) — коммиты `591e305`+`6156047`.
- [x] Баг 5 — «API Ключи не настроен» при наличии (jsonb codecs + нормализация + маска v3) — коммиты `1217567`, `bc07397`, `49789a9`.
- [x] Баг 6 — белый select уровня логов (тёмный стиль) — коммит `591e305`.
- [ ] Баг 4 — сверка синхронизации разделов с реальными данными (остаточная проверка по всем вкладкам, включая roles/control).

## /menu и WebApp-кнопка

- [x] /menu во всех чатах (Command-фильтр с @mention) — коммит `c9fc0c6`.
- [x] WebApp-кнопка «🛠 Меню бота» + themeParams — коммиты `6156047`/`c9fc0c6`.
- [x] Фолбэк-подсказка в группах (web_app-кнопки запрещены) — коммит `5d011d2`.

## Живая верификация

- [ ] ⏸ ждёт человека: живая проверка всех вкладок из Telegram (десктоп/Android/Nekogram) — финальный вердикт.

## Регресс

- [ ] регресс: полный pytest.
