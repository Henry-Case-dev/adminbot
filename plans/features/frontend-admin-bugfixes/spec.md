# Багфиксы фронта админки (F-4)

## Контекст и цель

Баг-репорты человека 01.09.2026 (MEMORY.md): 6 багов после входа в админку + «пустая админка у @PERMsoc_bot» (после смены URL в BotFather) + /menu в группах и WebApp-кнопка. Большинство фиксов уже в master (коммиты 01–03.09); задача — закрыть статусы, доделать остаточную проверку (Баг 4) и провести живую человеческую верификацию.

## Архитектурные привязки

- **Фронт**: `web/index.html` + `web/app.js` (Vue3/Tailwind CDN zero-build, 6 вкладок, DOMPurify).
- **API**: /api/status, /api/status/logs; log_ring (deque 1000 + sanitize) — единый синглтон.
- **84.4** — ConfigCache; **84.11** — status_service (psutil, uptime-бакеты); флаги через `hot.get` + read-пути.
- **SDK**: официальный telegram-web-app.js (пустая админка/initData).
- **84.22** — TMA-авторизация (initData HMAC), /menu + WebApp-кнопка.

## Границы

Не редизайн: только баги и доведение UX до стабильного состояния. Групповая стилизация/поведение кнопок — только в рамках уже принятых решений.

## Связи с другими фичами

- F-2 (`features/admin-debug-webview/`) — статика `?v=`, /menu-проверка.
- F-5 (`features/config-read-path-audit/`) — баг тумблеров (read-пути через hot.get).
- F-6 (`features/user-aliases-admin/`) — настройки алиасов в «Лимитах и Модулях».

## Критерии готовности

- Вкладка «Статус» грузится сразу; логи не пустые; аптайм-график рисуется.
- Тумблеры реально меняют состояние (флаг → hot.get → /debug_config).
- «API Ключи» показывает настроенные ключи (маска v3) без ложного «не настроен».
- Разделы синхронизированы с реальными данными (Баг 4 — все вкладки, включая roles/control).
- /menu работает в группах; WebApp-кнопка открывает дашборд; в группах — фолбэк-подсказка.
- Живой вердикт человека (десктоп/Android/Nekogram) зафиксирован.
- Полный pytest — 0 failed.
