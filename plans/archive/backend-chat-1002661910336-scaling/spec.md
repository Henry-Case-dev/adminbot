# spec.md — backend-chat-1002661910336-scaling (ТЗ 8 раунда 10.4, фича G)

Задачи: T-1045…T-1056 (tasks.md). Стадия старта — PLANNED → ARCHITECTED (наст. файл).
База: HEAD `1410a68`. Порядок: **последняя** (деплой-бэкфил не пересекается с переносами каталога B/F/H; F-5-аудит после). **F-1 (T-648/T-651/T-652/T-654) — ДО** (каст-семантика учтена).

## 1. Контекст и проблема

Чат -1002661910336 — «домашняя» супергруппа (текущий чат бота, в TMA — активный). У него нет BYOK-ключа (F-15: глобальный 25 req/сутки → sandbox R16 при исчерпании). ТЗ 8: расширить для ЭТОГО чата лимиты контекста/саммари/памяти — через per-chat overrides (chat_params), НЕ трогая дефолты других чатов. Матрица: у чата есть активные настройки в TMA (per_chat) — значит наиболее безопасный способ — chat_params overrides.

## 2. Ключевые решения

| ID | Решение | Обоснование |
|----|---------|-------------|
| **G-1** | **Резолв — существующий `get_chat_param(chat_id, key, default)`** (chat_params.py:210-217: overrides (каст) → `hot.get(key, default)` → default). **Новый хелпер НЕ создаётся** (T-1045-вариант (б) уже реализован). | Готовый per-chat слой (F-7/F-9/F-10); сигнатуры/публичные API без изменений; кэш TTL 120с + NOTIFY — как есть. |
| **G-2** | **Усиление `_resolve_from_root` для AC-G4** (мусор → fallback): после `normalize_value` добавить проверку `_cast_type_ok(spec.type, value)` (как в `get_chat_param_defaulted` :366-370): не сошлось → `hot.get(key, default)`. Аддитивно; поведение ВАЛИДНЫХ значений не меняется (существующие тесты F-9/F-10 зелёные: они не передают мусор; +1 юнит-тест «мусор → fallback»). | AC-G4 (безопасность каста; T-654-значение: float-NaN отсеивается `_cast_type_ok` float-веткой: NaN — isinstance(float) True! **Доп. правило**: для float-типа отсеивать NaN/inf (`math.isfinite`) — точечно в G-ветке (аддитивно к существующему `_cast_type_ok` — не менять его поведение глобально: поднять в `_resolve_from_root` доп. проверку `math.isfinite` для float-каста). |
| **G-3** | **Точки чтения** — перевод на `get_chat_param(chat_id, key, <текущий fallback>)` только в перечисленных местах (таблица §3); fallback = НЫНЕШНЯЯ точка чтения (в основном `hot.get(key, settings.FIELD)`), для :1011 — `settings.<FIELD>` (существует прямое чтение settings). **байт-в-байт для чатов без override** (fallback = старый код). | AC-G2/G3; точечные диффы (ID названных точек). |
| **G-4** | **Per-chat ретенция (крон)**: точки ретенции (full/archive, compress_batch) — per-chat значения применяются **только если функция уже итерирует/имеет chat_id в скоупе** (`_compress_and_purge`/`/summary`-путь — по чатам; крон-цикл по активным чатам — chat_id есть в контексте); **глобальный purge/cleanup-проход без чата** (merge/VACUUM-путь) — остаётся глобальными defaults (документированная граница; риск «per-chat ретенция -1002661910336 не применяется в глобальном purge-проходе» — зафиксирован в §7 + KPI-наблюдение; полное решение — готовится след. раундом/аудитом F-5). | T-1047-решение («резолв в цикле катронов» — где цикл есть; где нет — честная граница, без изменения архитектуры крона — риск-минимум). |
| **G-5** | **Набор значений override** — идемпотентный скрипт `scripts/backfill_104_overrides.py` (или расширение существующего сид-паттерна; решение: отдельный скрипт — по прецеденту backfill_feature_gates): через `ensure_scope_profile(-1002661910336, dm=False)` + `set_chat_params(chat_id, {"overrides": {...}}, changed_by=None, expected_updated_at=None)` — запись ТОЛЬКО отсутствующих ключей (get_all_chat_params → check; никаких перезаписей); таблица значений — §5; повторный прогон no-op. | Канон write-path (единая точка + NOTIFY + 409; никаких прямых SQL UPDATE — канон D111-семантика). |
| **G-6** | **ЛС-профиль владельца**: решено — **НЕ создавать** override для ЛС (ТЗ «и/или профиль ЛС» — у чата один групп-чат; ЛС-настройки — пер-чат механика F-14, ТЗ про них не говорит). Явное решение в отчёте (AC-G7). | Меньше скоупа; DM-плоскость не трогаем (T-663-перепроверка). |
| **G-7** | **KPI-риск (F-15-наблюдение)**: у -1002661910336 нет BYOK, глобальный ключ 25 req/сутки; расширение окон увеличит токены/запрос (context ×1.5→×2, summary ×2, L2-RAG ×2) → оценка +N% токенов на запрос в отчёте фичи; наблюдаемость — существующая F-15 деталей (`no key — sandbox` details расывает used/limit) + worker_budget; **связка с фичей B**: `flags.chat_context_budgets_enabled`=false для этого чата (B-4) — бюджеты выключаются → контекст-партишинг не режется — это БАЛАНС: расход растёт, но без «случайных обрезок»; РИСК: «больший контекст+выключенные бюджеты» может дать больше токенов на вызов, но и более полноценные ответы — зафиксировать как осознанный выбор владельца; восстановление бюджетов — 1 клик (override). | AC-G5. |

## 3. Таблица точек чтения (канон перевода)

| # | Файл:строки | Ключ | Fallback (текущий код) | Комментарий |
|---|-------------|------|------------------------|-------------|
| 1 | `services/direct_chat_service.py:996-1014` | `limits.chat_context_budget_tokens` | `hot.get(key, settings.CHAT_CONTEXT_BUDGET_TOKENS)` | база бюджета; гейт флага — фича B (B-2/B-5) |
| 2 | `services/direct_chat_service.py:1498-1522` | `limits.chat_map_participants_hours`, `limits.chat_map_participants_cap` | существующие hot.get | карта участников |
| 3 | `services/direct_chat_service.py:1622` | `limits.chat_global_context_limit`, `limits.chat_global_context_max_chars`, `limits.chat_global_context_max_tokens`, `limits.chat_level2_max_chars` | существующие | контекст-партишинг |
| 4 | `services/direct_chat_service.py:1684-1880` | `limits.chat_thread_max_tokens`, `limits.chat_thread_max_depth` (если в перечислении — разрешён к кандидатам ТЗ: thread_max_tokens) | существующие | тред |
| 5 | `services/direct_chat_service.py:1726` | `limits.chat_level2_max_chars` | существующие | L2-кап |
| 6 | `services/summary_generator.py:133-169` | `limits.summary_max_context_tokens`, `limits.summary_max_context_chars`, `limits.summary_rag_l2_limit` | существующие hot.get | L2-RAG |
| 7 | `services/summary_xml.py:63-68` | `limits.summary_max_window_messages`, `limits.summary_max_context_chars` | существующие | окна |
| 8 | `services/summary_memory.py:1283-1290` | `limits.summary_max_window_messages` | существующие (уже hot.get) | L1-окно (S1) |
| 9 | `services/summary_memory.py:1966-2004` | `limits.graph_rag_facts_limit`, `limits.graph_rag_context_max_chars` | существующие | graphrag RAG |
| 10 | `services/summary_memory.py:2406-2483` | `limits.full_memory_retention_days`, `limits.archive_memory_retention_days`, `limits.summary_compress_batch` | существующие | ретенция/compress (крон; см. G-4) |
| 11 | `services/summary_memory.py:2572` | `limits.graph_edge_weight_increment` | существующие | edge weight memorize |
| 12 | `services/summary_memory.py:2614` | `limits.archive_memory_retention_days` | существующие | archive retention |

ВАЖНО: точки 10/12 — только при chat_id в скоупе (G-4). Точные номера строк — перепроверить на старте фичи (сдвиги после B/F несущественны, но координаты в задач-листе должны совпасть; правки — по функциональной принадлежности, не по номеру строки).

## 4. Целевые значения override (-1002661910336) — таблица (AC-G6)

| Ключ | Текущий дефолт (кодовый/каталог) | Целевое значение | Обоснование |
|------|----------------------------------|------------------|-------------|
| `limits.chat_context_budget_tokens` | существующий | ×2 (или точное из каталога; точное число — реализатором в скрипте, параметризовано) | «база ×2» — ТЗ «расширить»; запас под контекст-партишинг |
| `limits.chat_global_context_max_tokens` | существующий | ×1.5 | контекст шире |
| `limits.chat_global_context_max_chars` | существующий | ×1.5 | |
| `limits.chat_global_context_limit` | существующий | ×1.5 | |
| `limits.chat_thread_max_tokens` | существующий | ×2 | |
| `limits.chat_level2_max_chars` | существующий | ×2 | L2-конспект |
| `limits.chat_map_participants_cap` | существующий | ×2 (≤ 300) | больше участников на карте |
| `limits.summary_max_context_tokens` | существующий | ×2 | саммари глубже |
| `limits.summary_max_context_chars` | существующий | ×2 | |
| `limits.summary_max_window_messages` | существующий | ×2 | L1-окно шире |
| `limits.summary_rag_l2_limit` | существующий | ×2 | L2-RAG |
| `limits.summary_compress_batch` | существующий | ×2 | пачки сжатия |
| `limits.full_memory_retention_days` | существующий (напр. 180?) | ×2 (или 365) | ретенция истории |
| `limits.archive_memory_retention_days` | существующий | ×2 | архив |
| `limits.graph_rag_facts_limit` | существующий | ×2 | факты в RAG |
| `limits.graph_rag_context_max_chars` | существующий | ×2 | |
| `limits.graph_edge_weight_increment` | существующий | без изменений (или ×1 — НЕ трогать; edge weight — семантика графа, ТЗ не заявлено: оставить) | точка 11 — перевод резолва, значение НЕ меняем |

Принцип: каждый ключ — **множитель к текущему кодовому дефолту** (параметризован в скрипте; константные множители в таблице — решение @Architect: ×1.5…×2). НЕ меняем: edge_weight (семантика), maps hours (не применимо — подтвердить).

## 5. Критерии приёмки

| AC | Критерий | Проверка |
|----|----------|----------|
| AC-G1 | Для чатов БЕЗ override — поведение совпадает с текущим (byte-for-byte hot.get/fallback): юнит-тест сравнения; override — берётся; кэш TTL 120с/NOTIFY — как есть | T-1045 |
| AC-G2 | direct_chat: без override — как сейчас; с override (увеличенное) — срабатывает; стороны других ключей не смещены (перечисленные только) | T-1046 |
| AC-G3 | summary_generator/xml/memory: L1-окно пери-чат; L2-RAG пери-чат; ретенция — по G-4 (в циклах с чатом) | T-1047 |
| AC-G4 | Мусорный override → fallback; NaN/inf float → fallback (T-654-совместимость) | T-1048 |
| AC-G5 | Отчёт: влияние на глобальный бюджет (25 req); метрика наблюдения (details/used-limit) — в отчёте | T-1049 |
| AC-G6 | Скрипт идемпотентен; значения только для -1002661910336; таблица «ключ → значение → обоснование» — в отчёте | T-1050 |
| AC-G7 | Явное решение ЛС (НЕ нужен — G-6) | T-1051 |
| AC-G8 | Деплой-инструкция + откат (DELETE overrides по списку; суммарно чистый уход — json; откат одной командой) — в README/отчёте | T-1052 |
| AC | Полный pytest 0 failed (baseline 4831 + новые); git diff --check; REGISTRY/SQLite/промпты/роутеры — без дифов; счётчики каталога 383/71/359 | T-1053…T-1056 |

## 6. Негативные тесты (что НЕ сломать)

- **REGISTRY 383 / 71 / Settings 359** — ноль изменений каталога (только per-chat overrides — не каталог!; MED-017).
- **SQLite v8** — ноль миграций/DDL; **PG** — ноль новых DDL/таблиц (канон: overrides в chat_params JSONB).
- **Роутеры bot.py** — без дифов; **каноны промптов** — без дифов.
- **Другие чаты** — байт-в-байт (юнит-тест сравнения до/после); **глобальные дефолты Settings** — не менялись (юнит).
- **F-10-бюджеты воркеров** — worker_budget/приоритеты — не задеваются (пути G — не воркер-бюджет; только наблюдение).
- **F-15-details** — подробности WARNING не ломаются (путь остаётся; детализация не диффится).
- **B-гейт бюджетов** (`flags.chat_context_budgets_enabled`=false для чата) — не конфликтует: G-точка 1 (база токенов) и B-гейт — независимы (гейт управляет партишингом, база — размером доли).
- **409/кэш/NOTIFY** — не ломаются.

## 7. Риски и блокеры

| Риск | Митигация |
|------|-----------|
| **Риск G-1 (главный, бюджетный)**: ×2-контекст + выключенные бюджеты (B) → больше токенов на запрос; если 25 req/сутки исчерпаны — sandbox R16 (бот молчит с фразой). Усиленный риск: «выключенные бюджеты» могут дать неограниченный контекст — максимальный budget — cap-функции остаются (партишинг-доли); перепроверка: при флаге false — как работает партишинг? (очень важно: `_apply_context_budget` при флаге off — НЕ вызывается? Если off — нечастичный ctx → unbounded? **Разобрать в B-спека** — при off бюджеты не применяются: контекст = полный сбор (все блоки) → большой вызов — и это ИМЕННО то, что необходимо проверить в Б-AC-B2-тесте; в G — KPI-наблюдение и metrics). | Наблюдаемость (details used/limit), отчёт; восстановление бюджетов — 1 клик; риск фиксирован. |
| **Риск G-2**: крон-ретенция — if оставлена глобальной (G-4-граница) — «ретенция -1002661910336 не расширяется в крон-проходе» (несоответствие ТЗ на 1 пузырь). | Явная граница §2 G-4 + отчёт; след. раунд-F-5-аудит. |
| **Риск G-3**: точки чтения по номерам строк — смещаются (после B/F) — правки по функциональной принадлежности. | Примечание §3 («перепроверить номера») — Builder сверяет на старте. |
| **Риск G-4**: `_resolve_from_root`-усиление (G-2) — задевает все текущие per-chat чтения (F-7/F-9/F-10) — но только в случае мусора (валидные — без изменений). | +юнит-тест «мусор → fallback»; полный pytest — контроль. |
| **Блокер**: нет (бэкенд-фича, изолирована; порядок — последней). | — |

## 8. Зависимости

- До: F-1 (атомарный POST; касты), A/B/F/H (пер-чат флоу; B-флаг; F-вкладка; H-каскад) — после (финал). F-5 — ПОСЛЕ G (read-path audit включает новые get_chat_param-точки).
- F-15 — диагностика (details) — не конфликтует.
- Деплой: скрипт override — @DevOps; RUNBOOK-шаг (T-1052).
