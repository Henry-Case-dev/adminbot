# Фича F7 — `guide-rewrite-persona-round1015` (Обновление «Гайда по фичам» под префиксы/имена)

> **Статус: ✅ COMPLETED** (Step 4 @Builder, 14.09.2026). Реализованы T-1604…T-1608; гейты T-1603/T-1609 — за @Architect/@PM. pytest **5703 passed / 0 failed**; каталог Δ=0 (435/406/411/90/88/19). Не закоммичено.
> **Раунд:** 10.15. **Нумерация:** T-1603…T-1609 (продолжает F6 T-1602).
> **Тип:** docs+backend+UI. **Приоритет:** **P2** (**последняя** — пишется по факту F6/F5).
> **Зависимости:** **F6** (реестр/имена) — обязательно; **F5** (упоминания бейджей) — желательно.
> **Конфликт файлов:** `services/info_service.py`, `info_text.md`.
> **ТЗ:** `plans/current_task.md`, раздел **7** + UPD §2.
> **Эпик:** `Epic: Багфиксы Графа памяти, Воркера Сна и Ностальгии round1015`.
> **Baseline:** HEAD `798e044`; pytest **5589 passed / 0 failed**; каталог **435/90/406/411/88/19**.

## 0. Цель

Полностью переписать «Гайд по фичам бота» (раздел «Справка») под актуальные команды и логику Личности: правило обязательного обращения («Бот, …» или «Олег, …»), ироничный фирменный стиль, визуальное выделение команд. Синхронизировать БД-сид и файл-источник.

**Текущее состояние (Step 0):** канон `DEFAULT_INFO_TEXT` `services/info_service.py:21-70` (HTML: h1 + 9×h2, h4-акценты); седится в PG-ключ `content.info_how_it_works` (`services/config_cache.py:38,139,209-215`); байт-тест `tests/test_info_service.py:22-26`; структурные счётчики `:28-45`. Второй гайд — `content.intelligence_guide` (`info_service.py:77-82`, markdown, `plans/docs/intelligence_user_guide.md`).

## 1. Требования (ТЗ §7 + UPD §2)

- [x] Переписать текст **«Гайд по фичам бота»** с учётом канонического реестра F6 (17 префиксных + `чекап`/`фактчек` bare).
- [x] Явно указать правило: *«Все команды требуют обращения. Нет имени — `Бот, скачай`; назвали Олегом — `Олег, скачай`»*.
- [x] Отдельно оговорить исключения: `чекап` и `фактчек` — одним словом, без обращения.
- [x] Сохранить фирменный ироничный стиль; команды выделить HTML-акцентами (info) / blockquote+inline-code (intelligence_guide).

## 2. Целевые модули (`file:line` на HEAD `798e044`)

- `services/info_service.py:20-70` — `DEFAULT_INFO_TEXT`.
- `info_text.md` (корень) — файл-источник идемпотентного сида.
- `services/config_cache.py:139-140,209-215` — `_seed_info_key` (сид только при отсутствии ключа!) + новая миграция.
- `services/info_service.py:77-82` / `plans/docs/intelligence_user_guide.md` — второй гайд.
- Тесты: `tests/test_info_service.py:22-45`, `tests/test_help_guide_round1014.py`, `tests/test_config_cache.py`; новый `tests/test_guide_round1015.py`.

## 3. Инварианты (constraints)

- **Байт-канон:** `DEFAULT_INFO_TEXT == info_text.md`; структурные счётчики обновить согласованно, теги сбалансированы.
- **PG-сид идемпотентен и «только при отсутствии ключа»** — доставка нового текста через DML-миграцию по слепку `PREV_DEFAULT_INFO_TEXT` (spec §4); ручные правки не затирать.
- Гайд соответствует реальному поведению после F6; снятые алиасы (`живой собака`, `пульс бота`, `как сервак`, `перескажи видос`) как актуальные НЕ упоминать.
- Формат канона — rich-HTML; Markdown-quote/код — для `intelligence_guide`.
- DOMPurify self-host, без новых `v-html`/CDN; R17 (без секретов/endpoint-деталей).
- Каталог-инварианты **435/90/406/411/88/19** (Δ=0). `media/`/`.env`/порядок роутеров `bot.py` не трогать.
- **Ревью-гейты:** полный `pytest` 0 регрессий, байт-тесты, `node --check web/app.js`, русские commits.

## 4. Зависимости

- **Вверх:** **F6** (реестр/имена — финальная семантика), **F5** (бейджи, если упоминаются).
- **Вниз:** нет (последняя фича раунда).
- **Конфликт файлов:** `services/info_service.py` — не делится; вливать после F6.

## 5. Definition of Done

- [x] «Гайд по фичам» переписан: правило обращения явно и с примерами; `чекап`/`фактчек` — исключения.
- [x] Команды выделены акцентами; ироничный стиль сохранён.
- [x] `DEFAULT_INFO_TEXT == info_text.md` байт-в-байт; структурные тесты обновлены и зелёные.
- [x] Новый текст доставляется в существующую БД (миграция по слепку), ручные правки не затираются.
- [x] Полный `pytest` — **0 failed**; `node --check` clean; каталог Δ=0.

## 6. Чек-лист задач

- [ ] **T-1603 (@Architect/@Copywriter, гейт):** финальный текст гайда по каноническому реестру F6 (17+2), правило обращения, исключения `чекап`/`фактчек`, формат, стратегия доставки в PG. *(итерация 2 выполнена)*
- [x] **T-1604 (@Builder):** переписать `DEFAULT_INFO_TEXT` (`services/info_service.py:20-70`) под реестр/имена, сохранив rich-HTML-структуру и ироничный стиль.
- [x] **T-1605 (@Builder):** синхронизировать `info_text.md` байт-в-байт; обновить структурные счётчики `tests/test_info_service.py:28-45`.
- [x] **T-1606 (@Builder):** реализовать доставку канона (миграция `_migrate_info_how_it_works_v1015` по слепку `PREV_DEFAULT_INFO_TEXT`) — без затирания ручных правок, идемпотентно.
- [x] **T-1607 (@Builder):** визуальное выделение команд (blockquote/inline-code для `content.intelligence_guide`; HTML-акценты для info-канона); точечная правка `intelligence_user_guide.md` §9.
- [x] **T-1608 (@Builder):** тесты: байт-тест, счётчики, наличие правила/примеров, `чекап`/`фактчек`-исключения, отсутствие `живой собака`, корректность миграции; `node --check`; полный `pytest` **0 failed**; русский commit.
- [ ] **T-1609 (@PM/@Reviewer, гейт):** сверка DoD, live-чеклист владельцу (текст в «Справке», читаемость, отсутствие устаревших упоминаний).

## 7. Открытые вопросы (@Architect → владелец)

- **F7-Q1…Q5 — RESOLVED** (spec §8). Новых Human Gate нет.

## 8. Feature flag / progressive delivery

- **Feature flag не требуется** (docs/справка). Rollback = `git revert` (+ откат сида).
- **Progressive delivery неприменим;** идёт последней после стабилизации F6.
