# Хотфикс-6 (пакет Волны 1.5) — `hotfix6-webview-shell-heartbeat-round1025` (внеплановый, после Волны 1, перед F4)

> **Триггер:** живая приёмка владельца после деплоя **Волны 1** (пакет `4cde1bc`, `APP_VERSION` **2.58.6**): **A** Liquid Glass фактически без преломления/искажения + панели меню (sidebar/drawer) и шапка (header) без стекла; **B** нижняя панель на мобильном всё ещё уползает за нижний край экрана; **C** сердцебиение (§15) — виджет расширяется за пределы экрана на мобильном + фактически это SVG с циклической подсветкой, а ТЗ §15 требует реальный интерактивный Canvas 2D + `requestAnimationFrame`; **D** верхняя панель/шапка — ⛶ ниже нативных кнопок Telegram, плотная группа селектор/бейдж/аватар/роль, наложение на контент, фуллскрин не работает.
> **Диагностика @Memory (Шаг 0, KG `HOTFIX6-webview-shell-heartbeat-round1025`) и конфликт-анализ — используются КАК ДАННОСТЬ, перепроверке не подлежат.** PM архитектуру не проектирует.
> **Тип:** web (CSS/glass/шапка/shell/Canvas-компонент) + ops (деплой/верификация). Backend — только если требуется **расширение существующего** `GET /api/status` без DDL и без новых ключей каталога.
> **Приоритет:** **P0** (A/B/C/D — прод-деградация по приёмке владельца). **Зависит от:** Волны 1 (F2 `design-tokens-liquidglass-v2-round1025` + F3 `global-scope-selector-round1025` + hotfix5), F1 (`ia-shell-navigation-round1025`).
> **Задачи:** **T-2581…T-2618 (38)**. Преемственность нумерации: максимум до пакета — **T-2580** (`plans/archive/global-scope-selector-round1025/`, F3; `plans/archive/hotfix5-summary-cover-window-round1025/`), **дублей нет** (проверено по `plans/features/` и `plans/archive/`).
> **Step 2 @Architect:** `spec.md` + **ADR-1025-12** (кросс-движковая лестница преломления; контракт §15; компоновка шапки) — создаёт @Architect (T-2582). PM `spec.md`/ADR **не создаёт**.
> **Статус:** ✅ **COMPLETED + MERGED + ARCHIVED (Шаг 8 @PM, 22.09.2026)** — реализация A/B/C/D (@Builder), ревью @Reviewer **Approved** (итер.2, 12/12), аудит @Scanner **Critical 0 / High 0** (`plans/reports/round1025_hotfix6_scanner_audit.md`), Merge @Architect — `plans/ARCHITECTURE.md` **§58**. Архив — `plans/archive/hotfix6-webview-shell-heartbeat-round1025/`. **⏳ deploy — Шаг 9 @DevOps (T-2616); код ещё НЕ закоммичен (рабочее дерево, `APP_VERSION` 2.58.7). ⏳ live-гейт владельца (T-2617) — открыт.**
>
> **Инварианты (жёсткие, не нарушать):**
> - **Δ DDL = 0**, **Δ каталога `param_catalog` = 0** (никаких новых ключей/таблиц; при необходимости — расширение **существующего** `GET /api/status`).
> - **CSP/zero-build** (ADR-1016-2 / ADR-1024-13): только self-host, **без CDN**, **без inline-скриптов**, без новых state-библиотек; подключение ассетов — через существующий конвейер + cache-bust `?v=`/`APP_VERSION`.
> - **Запрет WebGL**; **Canvas 2D разрешён**. **Уровень A glass не может опираться на `backdrop-filter: url()`** (§9; подтверждено @Memory как корень отсутствия эффекта).
> - **UI-флаги — только env-only** через `GET /api/me.ui_flags` (ADR-1024-13); inline/сторонние флаги запрещены.
> - **Нативные кнопки Telegram (⋮/Закрыть) CSS не двигать** (ARCHITECTURE **§604/§642**, §56). ⛶ позиционируется только относительно безопасной области/сетки шапки.
> - **R17/R18**: в логах/отчётах — без секретов и без дампа контента (только маркеры/длины/`host`); бэкапы/теги `pre-round1025*`/`stash@{0}` **НЕ удалять**; `plans/current_task.md` **не коммитить**.
> - Правка канона/эталонов (если затрагивается) — код + эталон + тест **одним** коммитом (project.md); force-ru conventional commits; код+тесты атомарно.
> - **Ступень общих файлов (строго сериализовано, без одновременного редактирования):** `web/static/app.css` и `web/index.html` — блоки **A → B → C → D**; `web/static/telegram-init.js` — только **B**; `web/app.js` — **A (лестница/UA-gate), C (компонент), D (фуллскрин)**. Внутри блока допускается ∥ по разным файлам, но по общему файлу — только один @Builder.
>
> **Базовая линия (Шаг 0 @Memory, 22.09.2026):** HEAD **`441e8f7`** (== `origin/master`), `APP_VERSION` **2.58.6**, pytest **8146 / 5 / 1** (5 — env aiogram, вне диффа), SQLite `user_version` **12**, Δ каталога = 0. Волна 1 — COMPLETED + MERGED + ARCHIVED + DEPLOYED; **live-гейты владельца (F2/F3/hotfix5) остаются открытыми**.
> **Отчёты-источники (читать, не перепроверять):** `plans/reports/round1025_package_scanner_audit.md`, `plans/reports/global_map.md`, `plans/reports/audit_backlog.md`.

## ⚠️ SUPERSEDE / AMEND (объявить на Step 2 @Architect, T-2582)

- **SUPERSEDE (перенос области):** «Живое сердцебиение §15» **исключается из F11** `status-showcase-dashboard-round1025` и реализуется в этом пакете (C2). В F11 остаются §12/§13/§14/§16/§17/§18/§19/§20/§21. Верхнеуровневая строка F11 в `plans/backlog.md` скорректирована; при старте F11 §15 из её скоупа убирается.
- **AMEND ADR-1025-9 (F2, Liquid Glass §9):** уровень A и стекло панелей — механизм преломления **без `backdrop-filter: url()`**, честная кросс-движковая лестница A/B/C, снятие/ослабление UA-gate (Blink-only) и пересмотр `min-240`, edge-weighted карта смещения.
- **AMEND ADR-1025-8 / hotfix4 (B):** компенсация `contentSafeAreaInset.bottom` (+ `--tg-viewport-bottom-offset`) поверх недодела позиционирования `.bottom-nav`/`.more-sheet`.
- **AMEND ADR-1025-10 (F3) и F1 (shell/шапка):** перенос селектора чата/бейджа/аватара/роли в отдельную компактную строку и перекомпоновка шапки (D1–D4).
- **НОВЫЙ ADR:** **ADR-1025-12** (Step 2 @Architect) — определяет лестницу A/B/C, контракт компонента §15, компоновку шапки; ссылается на этот `tasks.md`.

---

## 0. Точка отката и baseline (выполнять ПЕРВЫМ)

- [x] **T-2581** [@DevOps] — **Точка отката hotfix6 + фиксация baseline.** Тег `pre-round1025-hotfix6`; бэкап изменяемых областей в `var/backups/hotfix6-round1025-<ts>/`; `.env.bak.round1025-hotfix6` (значения не печатать). Зафиксировать baseline: HEAD **`441e8f7`**, `APP_VERSION` **2.58.6**, pytest **8146 / 5 / 1** (5 — env), SQLite `user_version` **12**, Δ каталога = 0. Проверить, что live-гейты Волны 1 (F2/F3/hotfix5) зафиксированы в отчётах.
  **Готово, когда:** тег + бэкап + `.env.bak` существуют, baseline записан, доказана возможность возврата (R18). В git не добавлять секреты/`plans/current_task.md`/zip-архивы.

## 1. [P0] Step 2 @Architect — spec + ADR-1025-12 (гейт перед реализацией A/B/C/D)

- [x] **T-2582** [@Architect] — **Step 2: `spec.md` + ADR-1025-12.** Зафиксировать: (1) кросс-движковую **честную лестницу A/B/C** преломления **без `backdrop-filter: url()`** (feature-detect + перф-бюджет, edge-weighted карта смещения, поведение при отсутствии поддержки); (2) контракт **§15** (источники, состояния, polling 10–30 с, сглаживание/гистерезис, UNKNOWN, тултип, reduced-motion, mobile-область; Canvas 2D + rAF; телеметрия отдельно от рендеринга); (3) компоновку шапки **D1–D5** (сетка/безопасная область, отдельная строка, резерв высоты, «не двигать нативные кнопки»); (4) SUPERSEDE/AMEND-карту выше.
  **Готово, когда:** `plans/features/hotfix6-webview-shell-heartbeat-round1025/spec.md` + `adr-1025-12-*.md` созданы, SUPERSEDE/AMEND объявлены, лестница A/B/C и контракт §15 однозначны. **PM этот артефакт не создаёт.**

## 2. [P0] A — Liquid Glass: реальное преломление + стекло панелей (§9) (выполнять после T-2582)

> Инвариант §9: **запрещено опираться на `backdrop-filter: url()`**; эффект обязан быть честным и кросс-движковым (или давать осознанный fallback, а не «пустое стекло»). Файлы: `web/static/app.css:44-64` (токены), `web/static/app.css:941-981` (уровень B default / A opt-in / downgrade), `web/index.html:27-46` (SVG `#lg-displace`, `feDisplacementMap scale=14`), `web/app.js:8102-8116` (UA-gate Blink-only), `web/app.js:8122-8143` (min-240), `web/index.html:1420/2762/2930` (3 узла `[data-glass="a"]`).

### A1 — механизм преломления без `backdrop-filter: url()`

- [x] **T-2583** [@Builder] — **Диагностика/наблюдаемость A (R17-safe).** Установить факт и причину отсутствия эффекта в **реальном Telegram WebView**: применить ли уровень A, поддержан ли `backdrop-filter`-путь, что даёт feature-detect, как срабатывает downgrade. Добавить **маркер** выбранного tier в диагностику/лог (например `glass_tier=a|b|c`, `glass_reason=...`) — без дампа контента, без секретов.
  **Готово, когда:** по диагностике однозначно видно, какой tier реально применён и почему; воспроизведено «нет эффекта» на реальном устройстве/в WebView; текста пользователя в логах нет.
- [x] **T-2584** [@Builder] — **Новый механизм преломления без `backdrop-filter: url()`.** Реализовать искажение/преломление **edge-weighted** картой смещения (`feDisplacementMap` поверх **edge-weighted** `feTurbulence`, допускается `feComponentTransfer`/`feComposite`/градиентная маска вместо равномерного шума; edge-weighted — обязательное условие, чтобы не было «размазанного» шума) и/или SVG-фильтром на самом слое, применимым **без** `backdrop-filter: url()`. Механизм обязан давать видимый, но не декоративно-шумный эффект.
  **Готово, когда:** на опорных узлах видно преломление/искажение «как стекло» (скриншот/видео), опоры на `backdrop-filter: url()` нет (grep/инвентарь), производительность в бюджете (см. T-2586).
- [x] **T-2585** [@Builder] — **Честная кросс-движковая лестница A/B/C.** Ввести явный выбор tier по **feature-detect + перф-бюджету** (не по UA): A — полное преломление; B — упрощённое стекло (`backdrop-filter` blur/saturate, без `url()`); C — непрозрачный осознанный fallback без потери контраста/раскладки. Снять/ослабить UA-gate Blink-only (`web/app.js:8102-8116`), пересмотреть/обосновать порог `min-240` (`web/app.js:8122-8143`). Расширить opt-in уровня A с 3 узлов на узлы панелей (A2).
  **Готово, когда:** tier выбирается детерминированно и объяснимо; нет UA-гейта, отсекающего эффект; downgrade корректно срабатывает; на неподдерживающем движке — осознанный fallback, а не пустое стекло.
- [x] **T-2586** [@Builder] — **Перф- и motion-бюджет.** Обеспечить, что эффект не «съедает» FPS мобильного WebView: ограничение площади/слоёв, отключение на дорогих слоях; при `prefers-reduced-motion: reduce` — статичный кадр без анимации искажения.
  **Готово, когда:** измеримо нет просадки ниже бюджета на контрольных размерах; reduced-motion соблюдён.
- [x] **T-2587** [@Builder] — **Регресс-тесты A1.** (1) отсутствие опоры на `backdrop-filter: url()`; (2) корректный выбор tier (feature-detect/downgrade); (3) edge-weighted карта (маркер/инвентарь SVG); (4) reduced-motion off; (5) отчёт диагностики tier.
  **Готово, когда:** тесты зелёные и **падают на старом коде** (доказывают фикс); inventory-тест синхронизирован.

### A2 — стекло на панелях (sidebar/drawer/header/bottom-nav/more-sheet) без потери контраста

> Источник конфликта: `.app-sidebar` `app.css:1457`, `.app-drawer` `:1477`, `.bottom-nav` `:1499`, `.more-sheet` `:1546` — непрозрачный `--surface-1`; `header.header-sticky` `app.css:745-752` — `--glass-bg-strong` без `backdrop-filter`.

- [x] **T-2588** [@Builder] — **Стекло на sidebar + drawer.** Прозрачность + blur/saturate по tier, граница/контраст, сохранение читаемости пунктов меню и состояний hover/active/focus. Учесть планшетный drawer (768–1199).
  **Готово, когда:** sidebar/drawer выглядят стеклянными по §9, контраст AA сохранён, раскладка/тач-цели не поехали.
- [x] **T-2589** [@Builder] — **Стекло на header.** Добавить `backdrop-filter` (по tier) к `header.header-sticky` (`app.css:745-752`), сохранив читаемость на прокрутке и поверх фона 60–90/90–120.
  **Готово, когда:** шапка стеклянная, без «серой плашки», контраст текста/иконок AA сохранён.
- [x] **T-2590** [@Builder] — **Стекло на bottom-nav + more-sheet.** Применить тот же tier к `.bottom-nav` и `.more-sheet` (согласованно с блоком B — позиционированием).
  **Готово, когда:** нижняя панель и шторка «Ещё» стеклянные, контраст и тач-цели ≥44×44 сохранены.
- [x] **T-2591** [@Builder] — **Контраст/доступность + осознанный fallback + deny-list.** Проверка AA (текст, иконки, активные состояния) на всех стеклянных панелях; tier C — непрозрачный fallback **без потери контраста и раскладки**; deny-list (textarea/таблицы/логи/формы/редакторы) остаётся нетронутым.
  **Готово, когда:** контраст подтверждён на панелях; deny-list сохранён; fallback не «ломает» композицию.
- [x] **T-2592** [@Builder] — **Регресс-тесты A2 + inventory-маркеры.** Обновить inventory-тест маркеров glass (панели включены; уровень A opt-in расширен), добавить проверки контраста/fallback.
  **Готово, когда:** тесты зелёные и падают на старом коде; маркеры F2 не ослаблены.

## 3. [P0] B — Нижняя панель на мобильном: не уползает за нижний край (выполнять после A2)

> Источник конфликта: `web/static/telegram-init.js:30-74` (`--tg-viewport-bottom-offset`), `.bottom-nav` `app.css:1491-1502`, `.more-sheet` `:1538-1550`, `viewport-fit=cover` `index.html:5-7`, рендер `index.html:3310-3321`. Гипотеза @Memory: **не учтён `contentSafeAreaInset.bottom`**.

- [x] **T-2593** [@Builder] — **Компенсация `contentSafeAreaInset.bottom`.** В `web/static/telegram-init.js` вычислять/прокидывать нижний offset с учётом **`contentSafeAreaInset.bottom`** (а не только `safeAreaInset`/`viewportStableHeight`) и обновлять на `safeAreaChanged`/`contentSafeAreaChanged`/`viewportChanged`; применить к `.bottom-nav` так, чтобы панель была **целиком** в видимой области на реальном WebView (Android/iOS/Desktop-клиенты с нулевой safe-area).
  **Готово, когда:** панель не выходит за нижний край на реальном Telegram WebView и на контрольных размерах; CSP не нарушен (без inline).
- [x] **T-2594** [@Builder] — **Симметрия `.more-sheet`.** Применить ту же компенсацию к шторке «Ещё», чтобы она не уезжала за экран и была согласована с `.bottom-nav`.
  **Готово, когда:** шторка целиком в видимой области; поведение согласовано с панелью.
- [x] **T-2595** [@Builder] — **Матрица + stable-инвариант.** В `tools/ui_round1025_matrix.py` усилить вертикальный контроль: `rect.bottom <= stableHeight` (при симулированном системном нижнем баре) и `rect.bottom <= innerHeight + 1` для `.bottom-nav`/`.more-sheet`; FAIL при выходе. Учесть `contentSafeAreaInset.bottom`.
  **Готово, когда:** матрица ловит «панель за экраном» как FAIL и проходит на фиксе (инвариант сформулирован явно).
- [x] **T-2596** [@Builder] — **Регресс-тесты B.** Вертикальные границы панели/шторки, отсутствие горизонтального overflow, сохранение тач-цели; проверки падают на старом коде.
  **Готово, когда:** тесты зелёные; регресс hotfix4/ADR-1025-8 D2 не возвращается.

## 4. [P0] C — Сердцебиение §15: overflow + реальный интерактивный компонент (выполнять после B)

> Источник: `web/index.html:2751-2780` (`.status-block`, `<svg class="ekg">`), `app.css:803-829` (`.ekg-trace` анимация), computed `web/app.js:1706-1749`, источник `GET /api/status` → `services/status_service.py:380-410` (psutil), polling `app.js:6364-6368` (30 с). **Текущее = SVG с циклической подсветкой → требуется реальный Canvas 2D + rAF (§15).**

### C1 — overflow на мобильном

- [x] **T-2597** [@Builder] — **Устранение overflow мобильного heartbeats.** Виджет (§15/`.status-block`/`.ekg`) расширяется далеко за пределы экрана на мобильном: ограничить контейнер по ширине/переполнению, на мобильном — **отдельная горизонтальная область** (а не выход за вьюпорт); без горизонтальной прокрутки страницы.
  **Готово, когда:** на мобильных размерах (320/360/390/414 и т.п.) виджет целиком в контейнере, `scrollWidth <= clientWidth`, нет выхода за экран.
- [x] **T-2598** [@Builder] — **Регресс-тесты C1.** Playwright-проверка отсутствия overflow виджета на мобильных размерах (и отсутствия влияния на общий горизонтальный overflow страницы).
  **Готово, когда:** тесты зелёные и падают на старом коде.

### C2 — полноценный Canvas 2D + rAF компонент §15

> Требования ТЗ §15: реальные источники (доступность бота/CPU/RAM/диск/крит.процессы/актуальность телеметрии); состояния **HEALTHY/WARNING/CRITICAL/UNKNOWN**; **Canvas 2D + `requestAnimationFrame`**; телеметрия **отдельно** от рендеринга, polling 10–30 с; сглаживание/гистерезис; **UNKNOWN при отсутствии данных**; **без выдуманного BPM**; тултип hover/tap (CPU/RAM/диск/статус/время обновления/причина); reduced-motion; на мобильном — отдельная горизонтальная область.

- [x] **T-2599** [@Builder] — **Каркас Canvas 2D + rAF.** Заменить SVG-заглушку на `<canvas>` с рендером через `requestAnimationFrame`; рендеринг **не** запускается при отсутствии данных; не «рисовать» метрики без источника. Плейсхолдер/подпись при UNKNOWN.
  **Готово, когда:** компонент рисуется на Canvas 2D (не SVG), цикл rAF управляем, при `reduced-motion` — статичный кадр, без WebGL.
- [x] **T-2600** @Builder — **Телеметрия ОТДЕЛЬНО от рендеринга.** Отдельный слой получения данных: polling **10–30 с** к существующему `GET /api/status` (`services/status_service.py:380-410`), хранение последнего снимка + `last_updated`/`stale`; рендер читает готовый снимок (без сети в кадре rAF); **расширение `/api/status` допустимо только аддитивно и без DDL/каталога**.
  **Готово, когда:** источник данных отделён от отрисовки; polling в диапазоне 10–30 с; при недоступности API — UNKNOWN, не «0».
- [x] **T-2601** [@Builder] — **Состояния + сглаживание/гистерезис.** HEALTHY/WARNING/CRITICAL/UNKNOWN — с различимыми цветом/интенсивностью/частотой/амплитудой/характером импульса; сглаживание и **гистерезис** (без «дребезга» на границе порогов); **без выдуманного BPM**.
  **Готово, когда:** переходы состояний стабильны, гистерезис доказан тестом, BPM/производные метрики не выдумываются.
- [x] **T-2602** [@Builder] — **Тултип hover/tap.** Показать CPU/RAM/диск/статус/время обновления/**причину** (почему WARNING/CRITICAL/UNKNOWN); доступен hover (desktop) и tap (mobile); не перекрывает системные элементы.
  **Готово, когда:** тултип открывается hover/tap, содержит обязательные поля, доступен на mobile.
- [x] **T-2603** [@Builder] — **Реальные источники §15.** Обеспечить учёт: доступность бота, CPU, RAM, диск, **крит.процессы**, **актуальность телеметрии**. Если нужных полей нет в `/api/status` — **аддитивно** расширить эндпоинт (без DDL, без новых ключей каталога).
  **Готово, когда:** каждое состояние выводится из реальных данных; при отсутствии метрики — UNKNOWN/причина, а не фикция.
- [x] **T-2604** [@Builder] — **reduced-motion + мобильная область.** `prefers-reduced-motion` — без пульсации/анимации (статика с корректным состоянием); на мобильном — отдельная горизонтальная область (согласовано с C1).
  **Готово, когда:** reduced-motion соблюдён; мобильная композиция без overflow.
- [x] **T-2605** [@Builder] — **Регресс-тесты C2.** JS/`node`-тест компонента (состояния, гистерезис, UNKNOWN-при-отсутствии-данных, reduced-motion, отделение телеметрии) + Playwright-проверки на размерах.
  **Готово, когда:** тесты зелёные и падают на старом SVG-коде.

## 5. [P0] D — Верхняя панель/шапка: нативные UI-грани, отдельная строка, резерв высоты, рабочий фуллскрин (выполнять после C)

> Источник: `web/index.html:89-214` (`flex-wrap`), кнопка ⛶ `:195-197` внутри `.ml-auto`-группы `:182-198`, селектор `:128-172`, `<details class="scope-tech">` `:176-181`, аватар/роль `:187-191`; CSS `app.css:745-752`, `:779-783`, `:785-797`; контент `main.scroll-area` `app.css:763-765`; fullscreen-логика `web/app.js:4631-4655`, `:4657-4706`, `:4691-4700`, `:4702-4710`; ADR-1024-24; ARCHITECTURE **§604/§642**, §56. **Нативные кнопки в DOM отсутствуют — двигать их CSS нельзя.**

- [x] **T-2606** [@Builder] — **D1 — ⛶ в правый верхний угол на уровне нативных кнопок.** Перенести кнопку полноэкранного режима (`.ml-auto`-группа, `index.html:182-198`) в правый верхний угол шапки, визуально на один уровень с нативными «⋮»/«Закрыть», **одинаковые размеры/отступы** тач-цели (≥44×44), с учётом безопасной области. **Нативные кнопки CSS не двигать.** Если WebView не даёт одну строку — ближайшее безопасное положение **без наложений**.
  **Готово, когда:** ⛶ визуально выровнен с нативными кнопками (размеры/отступы), не накладывается на них, учтена safe-area.
- [x] **T-2607** [@Builder] — **D5 — фуллскрин должен работать.** Починить toggle-логику (`web/app.js:4631-4655`, `:4657-4706`, `:4691-4700`, `:4702-4710`) / синхронизацию (ADR-1024-24): кнопка реально входит/выходит из полноэкранного режима в Telegram WebView, состояние кнопки синхронизировано.
  **Готово, когда:** фуллскрин включается/выключается на реальном устройстве; иконка/состояние соответствуют режиму.
- [x] **T-2608** [@Builder] — **D2 — отдельная компактная строка.** Перенести селектор чата (`index.html:128-172`), бейдж `chat_id`, аватар и роль админа (`:187-191`) в **отдельную компактную строку ниже** — подальше от нативных кнопок. При нехватке ширины: сокращать название чата (ellipsis), второстепенное — в «подробности» (`.scope-tech`/details). Полный `chat_id` — только в технических подробностях (согласовано с F3/§5).
  **Готово, когда:** группа не примыкает к нативным кнопкам; усечение работает; ничего не теряется без доступа (второстепенное — в «подробностях»).
- [x] **T-2609** [@Builder] — **D3 — header не накладывается на контент.** Исправить высоту/отступы/позиционирование sticky-шапки: дать `main.scroll-area` (`app.css:763-765`) корректный резерв высоты/`scroll-margin`, чтобы верх карточки «Аналитика» не подлезал под панель. Без «магических» отступов, зависящих от ширины.
  **Готово, когда:** контент начинается ниже шапки на всех размерах; нет перекрытия при скролле.
- [x] **T-2610** [@Builder] — **D4 — компактная шапка на мобильном + постоянно доступный селектор области.** Сохранить компактную шапку и **постоянно доступный** селектор области на мобильном; без горизонтальной прокрутки и **без перекрытия системных кнопок Telegram**.
  **Готово, когда:** на мобильном селектор области доступен всегда, нет горизонтального overflow и перекрытия нативных кнопок.
- [x] **T-2611** [@Builder] — **Регресс-тесты D (композиция).** Playwright-проверки: ⛶ выровнен и не пересекается с областью нативных кнопок (по безопасным зонам), единая аккуратная композиция шапки, резерв высоты контента, фуллскрин-toggle (где эмулируемо).
  **Готово, когда:** тесты зелёные и падают на старом коде; единая композиция зафиксирована.

## 6. Общее: маркер-тесты, версия, регресс, ревью, деплой, live, архив

- [x] **T-2612** [@Builder] — **Атомарное обновление маркер-тестов (одним коммитом).** Синхронизировать эталонные маркер-тесты, затронутые A/B/C/D: **F2** (glass A/B/C, inventory-маркеры), **F3** (шапка/селектор/`.scope-tech`), **hotfix4** (позиционирование `.bottom-nav`/навигация) — код + эталон + тесты в **одном** коммите. Без ослабления проверок.
  **Готово, когда:** тесты/эталоны отражают новое поведение; `node --check web/app.js` OK; расхождений между кодом и эталоном нет.
- [x] **T-2613** [@Builder] — **Bump `APP_VERSION` 2.58.6 → 2.58.7** (cache-bust, т.к. меняются `web/app.js`/`web/index.html`/`web/static/app.css`/`web/static/telegram-init.js`); обновить `?v=`-пины в тестах; README-версия не разъезжается (pin-тест).
  **Готово, когда:** версия единым источником, `?v=` подставляется, pin-тесты зелёные.
- [x] **T-2614** [@Builder] — **Полный регресс.** pytest (**8146/5/1 + новые**), JS (все + новые), matrix **0**, Playwright **§71 (10 вьюпортов)**, `node --check web/app.js` OK, `git diff --check` exit 0; отсутствие регрессий F0/F1/F2/F3/hotfix/hotfix2/3/4/5. **Δ DDL=0, Δ каталога=0.**
  **Готово, когда:** все показатели подтверждены логом/отчётом, расхождений нет.
- [x] **T-2615** [@Reviewer/@Scanner] — **Ревью/аудит.** @Reviewer **Approved**; @Scanner **Critical 0 / High 0** (прочее — в техдолг); отчёт `plans/reports/round1025_hotfix6_scanner_audit.md`. Проверить: отсутствие `backdrop-filter: url()`; CSP/zero-build; «не двигать нативные кнопки»; §15-контракт (без выдуманного BPM, UNKNOWN, reduced-motion); R17-safe логи.
  **Готово, когда:** вердикты зафиксированы отчётом.
- [ ] **T-2616** [@DevOps] — **Деплой.** Атомарные русские conventional commits (код+тесты) → push `origin/master` → прод `git pull --ff-only` → `systemctl restart admin_bot` → `/api/health` 200; `database is locked` = 0.
  **Готово, когда:** прод обновлён (`APP_VERSION` 2.58.7), health 200, в отчёте commit(ы) и версия.
- [ ] **T-2617** [@DevOps] — ⏳ **LIVE-ГЕЙТ ВЛАДЕЛЬЦА (post-deploy).** **(1) Playwright** — мобильные + десктоп размеры (§71, 10 вьюпортов); **(2) ОТДЕЛЬНО реальный Telegram WebView** (владелец): (a) glass даёт реальное преломление, панели/шапка стеклянные; (b) нижняя панель целиком в пределах экрана; (c) сердцебиение — Canvas 2D, в пределах экрана, тултип, состояния; (d) **⛶ выровнен с нативными кнопками и работает**, шапка — **единая аккуратная композиция**, контент не перекрыт, селектор области доступен; (e) регрессии F0–F3/hotfix* = 0; консоль TMA без `ReferenceError`.
  **Готово, когда:** приложены доказательства (лог/скрин/видео/версия) по каждому пункту, включая реальный WebView.
- [x] **T-2618** [@PM] — **Архивация (Шаг 8) + интеграция @Architect.** Перенос `plans/features/hotfix6-webview-shell-heartbeat-round1025/` → `plans/archive/`; обновить `plans/ARCHITECTURE.md` (§) и `plans/backlog.md`; §15 — исключён из F11. Бэкапы/теги/`stash@{0}` не удалять (R18).
  **Готово, когда:** папка в `plans/archive/`, ARCHITECTURE/backlog обновлены, отчёт оставлен.

---

## Feature flags / раскатка (progressive delivery)

- **Поэтапная раскатка internal → 10% → 50% → 100% НЕ применяется** (пет-проект, один прод; прецедент §53/§54.1/§55/§56). «Поставка» = деплой + cache-bust через **`APP_VERSION`** (T-2613). Поэтому стадии 10/50/100% = **N/A** с обоснованием; контроль — live-гейт владельца (T-2617).
- **UI-флаги — только env-only** через `GET /api/me.ui_flags` (ADR-1024-13), default ON, OFF → прежнее поведение:
  - **`UI_GLASS_TIER_OVERRIDE`** = `auto|a|b|c` (default `auto`) — принудительный выбор tier стекла (диагностика/откат A).
  - **`UI_HEARTBEAT_CANVAS_ENABLED`** = `true|false` (default `true`) — OFF → прежний SVG-виджет (мягкий откат C2).
  - **`UI_HEADER_COMPACT_V2`** = `true|false` (default `true`) — OFF → прежняя компоновка шапки (откат D).
- **Откат:** тег точки отката (T-2581) + `git revert` коммита(ов) пакета; флаги env-only — без редеплоя кода.
- **Δ DDL = 0**, **Δ каталога = 0**.

## Порядок и зависимости (критический путь)

- **T-2581** — обязательно ДО любых правок. **T-2582 (Step 2 @Architect)** — **гейт** перед реализацией A/B/C/D.
- **Ступень общих файлов (строго сериализовано):** `web/static/app.css` / `web/index.html` — **A → B → C → D**; `web/static/telegram-init.js` — только **B**; `web/app.js` — A (лестница/UA-gate) → C (компонент) → D (фуллскрин).
- **A1 → A2** (механизм → панели). **B** — после A2 (общая ступень `app.css`/`index.html`). **C** — после B. **D** — после C (общая ступень `app.css`/`index.html`/`app.js`).
- **T-2612 → T-2613 → T-2614 → T-2615 → T-2616 → T-2617 → T-2618** — строго последовательно.
- Дубли ID нет; `services/param_catalog.py` и `sidebarGroups` не трогаются; backend меняется только аддитивно (`/api/status`) и без DDL.

## Риски

- **Уровень A «невидим» и после фикса** (**Critical**): без T-2583/T-2584/T-2585 эффект останется декоративным или пустым; обязательна проверка в **реальном** WebView, не только Playwright.
- **Размазанный шум вместо стекла** (**High**): T-2584 — edge-weighted обязателен, иначе «шумное» искажение.
- **Контраст/доступность на панелях** (**High**): T-2591 — иначе стекло ухудшит читаемость (AA).
- **Панель всё ещё за экраном** (**High**): T-2593/T-2595 — компенсация `contentSafeAreaInset.bottom` + реальный WebView; иначе регресс hotfix4 повторится.
- **Overflow/перекрытие шапки нативными кнопками** (**High**): T-2606/T-2610 — не двигать нативные кнопки, использовать только безопасные зоны; ближайшее безопасное положение без наложений.
- **Выдуманные метрики/BPM** (**Critical** по §15/§116): T-2599/T-2600/T-2603 — только реальные данные, UNKNOWN при отсутствии.
- **Перф мобильного WebView** (**Medium**): T-2586/C — бюджет и reduced-motion.
- **Секреты в логах/отчётах** (**Critical, R17/R18**): только маркеры/длины/`host`; `.env`/`current_task.md` не коммитить; бэкапы/теги/`stash@{0}` не удалять.

## Открытые вопросы (владельцу / @Architect на Step 2)

1. **A:** какой tier считать «достаточным» для приёмки, если движок WebView не даёт полноценного преломления — допускается ли честный fallback B/C, или требуется довести A на этом же устройстве (влияет на T-2584/T-2585)?
2. **B:** подтвердить целевое поведение, если WebView не отдаёт `contentSafeAreaInset` (Android/Desktop-клиенты) — дополнительный CSS-резерв или фиксированная высота?
3. **D1:** допустимо ли, если WebView физически не даёт одну строку, размещать ⛶ в ближайшем безопасном положении (не строго на уровне нативных кнопок)?
4. **C2:** polling 10 с или 30 с как дефолт (компромисс нагрузка/актуальность)? Библиотека графиков — уже self-host `chart` (reuse) или чистый Canvas 2D (предпочтительно по §15)?
