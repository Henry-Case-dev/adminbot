# Фича F3 — `audit-recent-epics-round1016` (Полный аудит 10.13/10.14/10.15 + смоук-тесты)

> **Статус: ✅ COMPLETED** (14.09.2026; @Reviewer APPROVED, итерация 2). Реализация — T-1643…T-1649; гейт T-1650 закрыт.
> **Spec:** [`spec.md`](spec.md).
> **Раунд:** 10.16. **Нумерация:** T-1642…T-1650.
> **Тип:** test/audit. **Приоритет:** **P1**.
> **Зависимости:** **F1** (download/probe), **F2** (guide) — аудит их результатов; остальные подсистемы независимы.
> **Конфликт файлов:** `tests/*` (новые смоук-файлы); минимизировать правки продового кода (только исправления, доказанные аудитом, — отдельными T).
> **Эпик:** `Epic: Багфиксы + полный аудит round1016`.
> **ТЗ:** `plans/current_task.md`, **UPD2** (строка 152: «Перепроверь всё что сделано последними эпиками, полный аудит, тесты…»).
> **Baseline:** HEAD `18a9aa1`; pytest **5774 passed / 0 failed**; каталог **435/90/406/411/88/19** (Δ=0); SQLite **v9**; APP_VERSION 2.57.0.

## 0. Цель

Провести **полный аудит** последних эпиков (10.13 Cognition, 10.14 Persona, 10.15 Graph/Sleep/Nostalgia/Tool Calling): ревизия логики и корнер-кейсов; **смоук-тесты, имитирующие реальную работу** (а не только unit с MagicMock); закрыть релевантный Low-техдолг.

Смоук-покрытие (по рекомендации @Memory): download/probe, tool-loop, guide, graph, sleep, nostalgia, persona.

## 1. Доказательства / источники (`file:line` на HEAD `18a9aa1`)

- Отчёты: `plans/reports/round10.15_scanner_audit.md`, `round10.14_scanner_audit.md`; архивы `plans/archive/*-round1013|1014|1015/`.
- Техдолг Low (подлежит закрытию, если релевантно):
  - `S10.13-6b / S10.13-9 / S10.13-11 / S10.13-13` — источник §5 scanner 10.13;
  - `R10.14-4` (`dynamic_traits` без фильтра `chat_id` — осознанно/модель «общий характер»), `L5` (hot-path 2 доп. PG-запроса на ответ);
  - `R10.15-4` (yield в `direct_chat` по hot-флагу vs startup-регистрация download-роутера 4e), `R10.15-10` (link-first по первому вхождению имени), `R10.15-11` (триггер anywhere + любой http-URL консьюмит речь).
- Подсистемы: `services/tool_router.py`/`tool_loop.py`/`tool_schemas.py`, `services/direct_chat_service.py`, `services/dream_worker.py`, `services/nostalgia_worker.py`, `services/database.py` (`graph_snapshot`), `services/info_service.py`/`config_cache.py`.
- Существующие тесты, которые **не поймали** дефекты (пример: `tests/test_tool_calling_round1015.py` на direct `.mp4` + MagicMock) — расширить/дополнить.

## 2. Требования (UPD2 §3)

- [x] Смоук-тесты «имитация реальной работы» для: download/probe, tool-loop, guide, graph, sleep, nostalgia, persona.
- [x] Ревизия логики и корнер-кейсов эпиков 10.13–10.15 (утечки, границы, идемпотентность, fail-open/fail-safe).
- [x] Закрыть **релевантный** Low-техдолг (S10.13-6b/9/11/13, R10.14-4, R10.15-4/-10/-11) — либо обоснованный WONTFIX с записью в отчёт.
- [x] Отчёт аудита с `file:line` доказательствами и перечнем найденных/закрытых проблем.

## 3. Constraints (инварианты раунда)

- Изменения продового кода — **только** по доказанным аудитом дефектам (отдельными T), не рефакторинг «на всякий случай».
- **SQL/DDL не требуется**; каталог-инварианты **435/90/406/411/88/19** — **Δ=0**.
- **R16/R17** во всех новых тестах и логах; **порядок роутеров `bot.py` не менять**; `media/`/`.env` **не трогать**.
- Тесты не должны требовать сети/реальных секретов (моки yt-dlp/cobalt/LLM/PG).
- **Ревью-гейты:** полный `pytest` 0 регрессий, `node --check web/app.js`, `node tests/js/routing_test.js` → `JS-UNIT-OK`, R17-скан; русские commits.

## 4. Зависимости / порядок

- **Вверх:** **F1**, **F2** (аудит их результатов). **Вниз:** F5 (финализация).
- **Порядок:** после F1/F2; может идти параллельно с F4 (`miniapp`, разные файлы).

## 5. Definition of Done

- [x] Смоук-набор по 7 подсистемам добавлен и зелёный.
- [x] Отчёт аудита (`plans/reports/round10.16_audit.md`) с доказательствами и вердиктами по техдолгу.
- [x] Релевантный Low-техдолг закрыт (или WONTFIX с обоснованием).
- [x] Полный `pytest` **0 failed**; каталог **Δ=0**; JS-гейты чистые.

## 6. Чек-лист задач

- [ ] **T-1642 (@Architect/@PM, гейт):** объём аудита + матрица «подсистема → смоук-сценарий → существующие тесты → пробел»; критерии приёмки смоуков.
- [x] **T-1643 (@Builder):** смоук download/probe — имитация реальной работы через моки yt-dlp/cobalt (успех/бот-чек/истёкшие cookies/unsupported), согласование с F1/F1-тестами.
- [x] **T-1644 (@Builder):** смоук tool-loop — полный цикл `direct_chat → tool_call → dispatch → tool_response` (вкл. фиктивный success скачивания F8), лимиты 4/2, fail-safe.
- [x] **T-1645 (@Builder):** смоук guide — доставка канона в PG (старый текст → миграция → новый), оба блока Справки, `/edit_info`/POST.
- [x] **T-1646 (@Builder):** смоук graph — `graph_snapshot` (Degree Centrality, раскрытие смежных, отсечение сирот, cap после раскрытия, контракт `nodes/edges/truncated/limits`).
- [x] **T-1647 (@Builder):** смоук sleep — fallback порогов (0 убеждений → min_cluster_size=2/min_importance_sum=8), self-healing, `[Sleep]`-пре-гейт-лог.
- [x] **T-1648 (@Builder):** смоук nostalgia + persona — окно ±10, инжект лора/мемов, PREV-байт-канон; persona resolve per-chat→global, traits-cap, `/api/persona/health`.
- [x] **T-1649 (@Builder):** закрыть релевантный Low-техдолг (S10.13-6b/9/11/13, R10.14-4, R10.15-4/-10/-11) либо WONTFIX-обоснование в отчёте; ревизия логики/корнер-кейсов.
- [ ] **T-1650 (@Builder/@Reviewer, гейт):** отчёт `plans/reports/round10.16_audit.md`; полный `pytest` 0 failed; каталог Δ=0; JS/R17-гейты.

## 7. Открытые вопросы (@Architect → владелец)

- Глубина смоуков: in-process (`aiosqlite`/мок-PG) vs отдельный smoke-скрипт с реальным прод-подобным окружением?
- R10.14-4 (`dynamic_traits` без `chat_id`) — закрывать фильтром или фиксировать как осознанное поведение (риск смены модели «общий характер»)?
- Приоритет техдолга при ограниченном объёме: какие из 7 пунктов обязательны в этом раунде?

## 8. Feature flag / progressive delivery

- Не применимо (тесты/аудит). Прод-изменения (если будут по итогам аудита) — минимальные, rollback = `git revert`.
