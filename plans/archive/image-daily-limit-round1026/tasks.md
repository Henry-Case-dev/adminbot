# A5 — `image-daily-limit-round1026` (Эпик 3 «Agentic Intelligence», Wave 3, Раунд 10.26)

> **Статус (T-3599..T-3603 @Builder, 25.09.2026):** Шаги 1–2 ✅ (декомпозиция @PM; `spec.md` + **ADR-1026-17** @Architect; сверка **`PLANNING_CONSISTENT`** T-3589). **Реализация ЗАВЕРШЕНА в рабочем дереве (блоки B–G, T-3590…T-3603 ✅):** PG-журнал `image_reservation` + атомарный условный UPSERT (`reserve/commit/release`), idem-ключ `{chat_id}:{message_id}:{source}`, scope `limits.image_daily_limit` (470/427/445/101/99/21), per-chat TZ, учёт из журнала, env-киль-свитч `IMAGE_DAILY_LIMIT_ENABLED`, UI-врезка `mod_images` (433). Доказательства — `evidence.md` + `threat-failure-analysis.md` (12 угроз). **Проверки:** pytest **9170/0**, JS **47/47**, каталог **470/427/445/101/99/21**, `git diff --check`=0, SQLite v12, APP_VERSION **2.58.30** (без bump). **Risk-Level: R3** (D12). Deploy — **`DEFERRED_TO_EPIC`** (EPIC_ONLY, T-3606). **Шаг 4 @Reviewer (T-3604) ✅ — единый gate Approved C0/H0 (R3), 25.09.2026 (`review-T-3604.md`).** product code менялся (T-3590…T-3603); деплой НЕ выполнялся (EPIC_ONLY).
> **Готово:** T-3587 [x] · T-3588 ✅ · T-3589 ✅ · **T-3590…T-3603 [x] ✅** (блоки B–G) · **T-3604 ✅ (Approved)** · **T-3605 ✅ (архивация)** · **T-3606 ✅ = `DEFERRED_TO_EPIC`** · **T-3607 ✅ (handoff → A6)**. **Открыто:** нет — исполнение T-3606 (деплой/bump 2.58.31) и агрегатный gate F1–F7 происходят **на границе Эпика 3**.
> **Эпик:** Эпика 3 «Agentic Intelligence», **Wave 3 — A5** (следующая после A3 ✅; порядок `A0 ✅ → A1 ✅ → A2 ✅ → A3 ✅ → A5 → A6 → A4 → A7 → A8 → A9 → A10`). **Тип:** backend/лимит + ΔDDL-врезка + учёт + UI-врезка §27/§50 (per backlog `:307` — `backend+DDL+UI`; UI-врезка/Δ каталога — **санкционированы Шагом 2 (D9)**; T-3603 — **активная**, не N/A). **P0**. **Epic-ID:** Эпик 3 «Agentic Intelligence» (раунд 10.26); **release policy — EPIC_ONLY**: эта фича — `included in pending epic release` (per-фича деплоя/тега/bампа нет).
> **ТЗ-источник (verbatim, `plans/current_task.md`, только чтение — R17/R18):** **§26 «ДНЕВНОЙ ЛИМИТ ИЗОБРАЖЕНИЙ»** (`:4905–4922`), **§27 «РАСПОЛОЖЕНИЕ В MINI APP»** (`:4923–4959`), **§28 «СЕМАНТИКА ЛИМИТА»** (`:4962–4992`), **§29 «ЗАЩИТА ОТ ПАРАЛЛЕЛЬНЫХ ЗАПРОСОВ»** (`:4995–5015`), **§30 «ГЛОБАЛЬНЫЕ И ЛОКАЛЬНЫЕ ЛИМИТЫ»** (`:5018–5044`), **§31 «ВРЕМЕННАЯ ЗОНА И СБРОС»** (`:5047–5063`); сопутствующие ориентиры: **§37 «Безопасность TOOL OUTPUT»** (`:5252+`) — контент (URL/файлы/LLM-вывод) не должен иметь возможности отключать лимиты/очереди (инвариант безопасности резерва), **§50 «ИНТЕГРАЦИЯ С MINI APP»** (`:5683–5718`) — «Модули → Генерация изображений: … дневной лимит …» + «Не дублировать один параметр в нескольких независимых формах».
> **Зависимости:** **A0** ✅ (durable-аудит `plans/docs/agentic-audit-round1026.md`: §12 п.5 лимиты, EV-19 «Бюджет `image_calls` (global+per-chat, env-only)» `worker_budget.py:35,284–291`, `#epic3-reuse` «Бюджет изображений — `worker_budget image_calls`; не вводить второй счётчик; **расширять существующий (A5)**»; `#epic3-handoff`), **A1** ✅ (Merge §83, ADR-1026-14), **A2** ✅ (Merge §84, ADR-1026-15 — envelope §16 `ToolLoopResult.tool_results`/`ToolContext.result_for`, §17-лимиты, канон **11**), **A3** ✅ (Merge §85, ADR-1026-16 — единый ImageRequest/`run_image_request` в `services/image_generation.py`, поля-заглушки памяти), **Эпики 1–2** ✅ (§70/§71–§81 — scope/наследование каталога, UI-контур Эпика 1).
> **Baseline (Step 0 @Memory, 25.09.2026; в Step 1 НЕ перемеряется):** HEAD **`e8646af`** == `origin/master` (в рабочем дереве — **незакоммиченный pending epic-release код A2/A3**: НЕ трогать, не коммитить); `APP_VERSION` **2.58.30**; pytest `.venv` **9156/0**; JS **47/47**; каталог **469/426/444/100/98/21**; канон инструментов **11**; **Δ DDL=0** (SQLite v12; PG `worker_budget` — PK `(day, scope, metric)`); прод активен 2.58.30 (MainPID 652183). **Deploy — `DEFERRED` (EPIC_ONLY)**: bump 2.58.31 — только в агрегатном релизе Эпика 3; **release-узел не создаётся**. **Пер-фича baseline-тег НЕ создаётся** (не создать задачу на тег): baseline-анкер = коммит **`e8646af`**; точка отката hot-откат = env-only kill-switch OFF; cold = `git revert`/анкер `e8646af`; теги/бэкапы не удаляются (R18).
> **Карта кода (Step 0 @Memory, факт; EVIDENCE A0):** счётчик `image_calls` — **Postgres** `worker_budget` (`pg_db.py:191–199`; в SQLite отсутствует; runtime-`PgDatabase` DI, `worker_budget.py:78–89`); env-лимиты `WORKER_DAILY_IMAGE_CALLS_GLOBAL/PER_CHAT` **200/60** (`settings.py:941–944`; ветка `_metric_limit` `worker_budget.py:275–305`) с sentinel-семантикой `0=запрет / <0=безлимит / >0=cap`; списание `consume` `worker_budget.py:211–242` — **check-after-increment** (без атомарного резерва и idempotency-key, race при 1 остатке; fail-open при PG down); обвязка `image_generation._consume_budget:491–510` global→per-chat до генерации, вызывается из `generate_and_send:840–852` (A3-нумерация) и пре-гейта `maybe_handle_keyword`; **rollback резерва при сбое генерации/публикации отсутствует**; **учёт запросов/успехов/ошибок (§28) отсутствует**; день — глобальный `WORKER_BUDGET_TZ` (`worker_budget.py:58`, `settings.py:914`), per-chat TZ **прецедент `limits.chat_timezone`** (`direct_chat_service.py:1266–1273`, `tool_router.py:1480–1525`); UI «Использовано сегодня: N/M» частично в `/api/workers/budget`; маршрута «Модули → Генерация изображений → Лимиты» в Модулях нет.
> **ID:** **T-3587…T-3607 (21)** — **дублей нет** (макс. фактически занятый ID — **T-3586** (резерв N/A, A3); гэпов нет; сквозная нумерация продолжена от A3 `T-3552…T-3583`).
> **ADR:** ✅ Шаг 2 @Architect — **ADR-1026-17** (`adr-1026-17-image-daily-limit.md`; **D1–D12**; статус Proposed → Accepted по merge). Открытые вопросы (a)–(e) — **ЗАКРЫТЫ** решениями D1–D12 (spec.md §12; см. раздел ниже). REUSE: ADR-1026-16/-15/-14/-13, ADR-1018-7 D1, ADR-1019-8 D2, ADR-1020-3, ADR-1023-5 (+ effective note — меняется только механизм списания), ADR-1025-11 D3, ADR-1025-24; **ADR-1013-3 = NOT_APPLICABLE** (промптов в A5 нет); **канон инструментов = 11**.
> **Δ DDL ≠ 0 — САНКЦИОНИРОВАНО Шагом 2 (ADR-1026-17 D1, verbatim §«Санкция» ADR):** PostgreSQL — +1 таблица **`image_reservation`** (PK `reservation_key`; статус-буфер `reserved/committed/released/denied`) + **2 индекса** (`(day, status)` / `(chat_id, day)`) в `DDL_STATEMENTS` `services/pg_db.py`; **идемпотентный DDL** (`IF NOT EXISTS`, повторный `init()` — no-op), применяется **автоматически при старте прод** `PgDatabase.init()` (`bot.py`/`web/app.py` → `ConfigCache.init()` → `pg.init()`); отдельная миграционная задача НЕ требуется. **SQLite Δ DDL = 0** (остаётся v12); **схема `worker_budget` не меняется** (PK `(day, scope, metric)`; `used` — единственный счётчик, без второго). Откат кода — без отката DDL (таблица инертна; `DROP` не нужен). **Δ каталога (D9) = +1 ParamSpec `limits.image_daily_limit` (int, группа `limits_images`) +1 GroupSpec `limits_images` (category `limits`, tab `mod_images`)** — ожидаемо **470/427/445/101/99/21**; Builder обновляет тесты каталога; финал — F8 `--check` (T-3600/T-3603).
> **Risk: `Risk-Level: R3` — ФИНАЛ (Шаг 2 @Architect, ADR-1026-17 D12; совпал с предварительной оценкой @PM).** Обоснование: разделяемый бюджет живого direct-чата/tool-пути + DDL + каталог + UI + money-like квота при конкуренции; KG High: `Risk-a5-double-spend`, scope-semantics gap, TZ/reset gap. Усиленные доказательства (обязательны): threat/failure-анализ, race/atomic-order-тесты + concurrency-проба (**SC-A5-04**), rollback-доказательства (OFF/legacy, hot/cold, анкер `e8646af`, отсутствие DDL-отката), adversarial-приёмка + diff-аудит. Reviewer может повысить при усилении blast radius.
> **⛔ Границы:** product code — после Step 1/2 и сверки @PM; **`plans/current_task.md` — НЕ трогать (immutable)**; машинный блок `workflow_state.md` — не редактировать вручную; `spec.md`/ADR — Шаг 2; `plans/metrics.md`, `plans/ARCHITECTURE.md` — чужие шаги; durable-аудит A0 и архивы A0–A3 — только чтение. **Без коммитов из Step 1.** **@Scanner удалён (24.09.2026) — нигде не назначать**; приёмка — единый Reviewer gate. **@DevOps на per-фича деплой не вызывается (EPIC_ONLY).**

## Трассируемость (REQ → verbatim §ТЗ → блок → задачи → SC/ADR (Шаг 2))
> REQ-A5-01…-10 выделены из §26–§31 (+ §27/§50 UI, §37 — ориентир безопасности). Состав **не сужен**, новых REQ не добавлено; все REQ привязаны к блокам/задачам; **орфан-REQ нет**. Колонки SC/ADR **заполнены сверкой @PM T-3589 ✅ (25.09.2026)**: REQ → SC-A5-01…-14 (spec.md §2/§8) → решения D1–D12 (ADR-1026-17); орфан-SC нет; verbatim-цитаты выверены по `current_task.md` — расхождений нет.
| REQ | Источник (verbatim, `current_task.md`) | Блок | Задачи | SC/ADR (Шаг 2) |
|---|---|---|---|---|
| REQ-A5-01 | §29 «Использовать атомарное резервирование квоты или другой подходящий механизм.» (`:5007–5008`) | B, C | T-3590, T-3591, T-3592 | **SC-A5-01**; ADR-1026-17 **D2** (условный UPSERT — отказ не расходует квоту; одна транзакция shared+per-chat) |
| REQ-A5-02 | §29 «Предусмотреть idempotency key, чтобы повторная обработка одного сообщения не списывала квоту повторно.» (`:5013–5015`) | B, C | T-3590, T-3591, T-3594 | **SC-A5-02**; ADR-1026-17 **D3** (idem key `{chat_id}:{message_id}:{source}`, UNIQUE/PK журнала; replay не списывает) |
| REQ-A5-03 | §29 «При ошибке генерации освобождать резерв, если результат не был создан.» (`:5010–5011`) | C | T-3593 | **SC-A5-03** (+SC-A5-07: учёт ошибки); ADR-1026-17 **D7** (release при сбое генерации; commit при успехе, вкл. сбой доставки) |
| REQ-A5-04 | §29 «Проверку лимита выполнять на сервере.» («Не считать клиентский счётчик источником истины.») + «Если осталось одно изображение, два одновременных запроса не должны оба проходить проверку без контроля.» (`:4998–5005`) | B, C, F | T-3590, T-3592, T-3599 | **SC-A5-04**; ADR-1026-17 **D2** (серверная атомарность: при остатке 1 из двух параллельных — ровно один резерв) |
| REQ-A5-05 | §26 «Глобальное значение.» «Локальное значение конкретного чата.» «Использовать существующую систему scope и наследования конфигурации.» «Не создавать отдельный независимый механизм локальных настроек.» (`:4912–4920`); §30 «Глобальный дневной лимит задаёт значение по умолчанию.» + «Локальное значение может переопределять его для конкретного чата.» + «Если дополнительно требуется общий предел на все чаты вместе, это должен быть отдельный явно названный бюджет.» + «Не смешивать: "Глобальный лимит по умолчанию" и "Общая квота всех чатов".» (`:5023–5041`) | D, G | T-3595, T-3596 (частично), T-3602 | **SC-A5-05**; ADR-1026-17 **D4** (каталожный `limits.image_daily_limit` ≠ shared `WORKER_DAILY_IMAGE_CALLS_GLOBAL`) |
| REQ-A5-06 | §31 «Определить календарный день по выбранной временной зоне.» «Для текущего проекта допустимо использовать существующую часовую зону чата.» + «Не сбрасывать квоту каждые 24 часа от момента первого запроса, если интерфейс обещает календарный день.» + «Показывать точное время следующего сброса.» + «При изменении лимита не обнулять уже использованную квоту.» (`:5050–5063`) | D, F | T-3596, T-3599 | **SC-A5-06**; ADR-1026-17 **D5** (per-chat TZ `limits.chat_timezone` reuse `resolve_timezone`; shared — `WORKER_BUDGET_TZ`; календарный день) |
| REQ-A5-07 | §28 «Нужен отдельный учёт: запросов; успешных генераций; ошибок; фактических расходов, если доступны.» (`:4987–4992`) | E | T-3598 | **SC-A5-07**; ADR-1026-17 **D6** (учёт из журнала `image_reservation` — не второй счётчик; расходы N/A) |
| REQ-A5-08 | §28 «Один успешно созданный результат — одна использованная генерация.» + «Ошибки до запуска генерации не должны расходовать пользовательский лимит.» + «При успешном результате, который не удалось доставить в Telegram, расход генерации уже мог произойти.» + «Не выполнять повторную платную генерацию автоматически ради исправления ошибки отправки.» (`:4968–4985`) | C, E, F | T-3592, T-3593, T-3597, T-3599 | **SC-A5-08**; ADR-1026-17 **D6/D7** (`n=1` §104; сбой доставки = commit + `delivery_failed`) |
| REQ-A5-09 | §27 «В разделе "Лимиты": "Дневной лимит изображений".» + «Рядом: "Использовано сегодня: N / M".» + Показывать «источник лимита; время следующего сброса; текущую временную зону.» + «Не дублировать настройку в другом разделе с независимым состоянием.» (`:4941–4959`); §50 «Модули → Генерация изображений: … дневной лимит …» + «Не дублировать один параметр в нескольких независимых формах.» (`:5693–5717`) | G | T-3603 | **SC-A5-09** (+**SC-A5-12**: Δ каталога); ADR-1026-17 **D9** — UI-врезка §27/§50 **санкционирована**, T-3603 — активная (не N/A) |
| REQ-A5-10 | Границы/invariants: §104 «GENERATE_IMAGE — НЕ ТРОГАТЬ» (`:3180–3203`); §85-UI (`:2646+`) — отдельная санкция; reuse `worker_budget` без второго счётчика (A0 `#epic3-reuse`); границы A4/A6/A7; fail-open/fail-closed семантика (cash-аннотации hotfix5); R17/R18; deploy EPIC_ONLY | 0, G, H | T-3587, T-3602, T-3606 | **SC-A5-10…SC-A5-14**; ADR-1026-17 **D10** (границы/REUSE), **D11** (deploy/откат EPIC_ONLY), **D12** (R3) |

## Карта решений ADR-1026-17 → задачи (проверена @PM T-3589; ↔ AMEND/REUSE-карта ADR)

> REUSE (не дублировать): ADR-1026-16 (A3: `ImageRequest`/`run_image_request`, маркер `image_request_handled`), ADR-1026-15 (A2: envelope/§17-лимиты/канон 11), ADR-1026-14 (A1: 2-вызовность, env-only kill-switch-прецедент), ADR-1026-13 (A0: durable-аудит, `#epic3-reuse`), ADR-1018-7 D1 (scope-наследование), ADR-1019-8 D2 (sentinel), ADR-1023-5 (REUSE + effective note: меняется только механизм списания), ADR-1025-11 D3 (hotfix5: env-only ветка `image_calls`, fail-open-аннотации), ADR-1020-3 (прецедент `limits.chat_timezone`), ADR-1025-24 (дисциплина гейта/deploy-вердикта). **ADR-1013-3 = NOT_APPLICABLE** (промптов в A5 нет).

| D | Суть решения (кратко) | Задачи (блоки) | SC |
|---|---|---|---|
| **D1** | Хранилище — **PostgreSQL** (там же, где `worker_budget`): резерв поверх существующей таблицы + журнал `image_reservation`; **SQLite Δ DDL=0** (v12); `worker_budget` схему не меняет | T-3590 (B), T-3591 (B), T-3598 (E) | SC-A5-11 |
| **D2** | Атомарный резерв — **условный UPSERT** `... WHERE used < limit`; отказ не расходует квоту; shared+per-chat в одной транзакции; порядок `reserve → generate → commit/release` | T-3590 (B), T-3591 (B), T-3592 (C) | SC-A5-01/-04 |
| **D3** | idem key = **`{chat_id}:{message_id}:{source}`** (UNIQUE/PK журнала); replay не списывает; retention `IMAGE_RESERVATION_RETENTION_DAYS` (30) | T-3590 (B), T-3591 (B), T-3594 (C) | SC-A5-02 |
| **D4** | Дефолт = каталожный `limits.image_daily_limit` (per-chat override → global → env 60); **≠** shared-квота `WORKER_DAILY_IMAGE_CALLS_GLOBAL` (200, env-only) | T-3595 (D), T-3602 (G) | SC-A5-05 |
| **D5** | Per-chat день по `limits.chat_timezone` (reuse `resolve_timezone`); shared — `WORKER_BUDGET_TZ`; календарный день; точный сброс; смена лимита не обнуляет `used` | T-3596 (D) | SC-A5-06 |
| **D6** | Учёт §28: 1 результат = 1 генерация (`n=1`, §104); запросы/успехи/ошибки — из журнала (не второй счётчик); «расходы» N/A | T-3597 (E), T-3598 (E) | SC-A5-07/-08 |
| **D7** | Сбой генерации → **release** (возврат); успех (вкл. сбой доставки) → **commit** + `delivery_failed`; bounded-retry резервирует один раз; без авто-повтора платной генерации | T-3593 (C), T-3597 (E) | SC-A5-03/-08 |
| **D8** | fail-open (паритет baseline) + честный WARNING с дедупом; транзакция исключает частичный расход | T-3591 (B), T-3601 (F) | SC-A5-13 |
| **D9** | UI §27/§50 — **врезка в существующий контур `mod_images`** (НЕ отдельная панель); **Δ каталога = +1 ParamSpec `limits.image_daily_limit` +1 GroupSpec `limits_images`** (expected 470/427/445/101/99/21) — **T-3603 АКТИВНАЯ** | T-3603 (G); тесты каталога — T-3600 (F) | SC-A5-09/-12 |
| **D10** | Границы A4/A6/A7/§104; REUSE ExecutionGraph/A2-envelope/A3-маркер; канон 11; §37-инвариант (TZ/лимиты/id — только из админ-конфига, не из контента) | T-3602 (G) | SC-A5-10/-13 |
| **D11** | Deploy = **DEFERRED (EPIC_ONLY)**; hot-OFF `IMAGE_DAILY_LIMIT_ENABLED`; cold — `git revert` к `e8646af`; **DDL-откат не требуется** (таблица инертна; `DROP` не нужен) | T-3606 (H); подтверждение — T-3589 | SC-A5-14 |
| **D12** | **Risk-Level R3** + усиленные доказательства: threat/failure-анализ, race/concurrency-пробы, rollback-флоу, adversarial-приёмка, diff-аудит | T-3599 (F), T-3604 (H) | усиление ко всем SC |

## Приёмочные инварианты A5 (нарушение = НЕ принято) → привязка к SC-A5-01…-14 (сверка T-3589; соответствует spec.md §5)
> Опираются на §26–§31 + §27/§50/§37-ориентир + KG-риски Step 0. **14 инвариантов**, каждый имеет SC; losses нет. Соответствие 1:1 спецификации (spec.md §5) — проверено T-3589.
1. **Без двойного списания / без утечки квоты:** атомарный резерв — **условный UPSERT `... WHERE used < limit`** (D2); отказ по лимиту НЕ инкрементирует `used`; idem key — **`{chat_id}:{message_id}:{source}`** (D3, PK журнала); повторная обработка одного сообщения списывает квоту ровно один раз. **SC-A5-01/-02; REQ-A5-01/-02; D2/D3; T-3590, T-3591, T-3592, T-3594.**
2. **Rollback/outcome строго по D7:** сбой генерации (результат не создан) → **release** (возврат квоты, §29); успех генерации, **включая сбой доставки** («расход уже мог произойти») → **commit** + флаг `delivery_failed`; повторная платная генерация ради доставки НЕ запускается; bounded-retry (`generate_image_verbose`) резервирует один раз на запрос. **SC-A5-03; REQ-A5-03/-08; D7; T-3593, T-3597.**
3. **Серверная атомарность без check-after-increment-гонки:** порядок **`reserve → generate → commit/release`** (D2); shared+per-chat в одной транзакции; отказ per-chat откатывает shared-инкремент; при остатке 1 два одновременных запроса → ровно один резерв; все проверки на сервере (клиентский счётчик ≠ источник истины). **SC-A5-04; REQ-A5-04; D2; T-3590, T-3592, T-3599 (эмуляция гонки).**
4. **Scope честный (D4):** «глобальный лимит по умолчанию» = каталожный `limits.image_daily_limit` (chat → global → env-дефолт 60) **≠** «общая квота всех чатов» = shared-бюджет `WORKER_DAILY_IMAGE_CALLS_GLOBAL` (200, env-only, отдельная UI-строка); per-chat override через существующую систему scope/наследования каталога (`resolve_setting_cached`); без нового независимого механизма локальных настроек; sentinel `0/<0/>0` сохранён. **SC-A5-05; REQ-A5-05; D4; T-3595, T-3602.**
5. **TZ/сброс — reuse прецедента `limits.chat_timezone` (D5), не навязывать глобальную TZ всем чатам:** календарный день по TZ чата (per-chat → global → fallback `limits.summary_timezone`); у shared-бюджета — семантика `WORKER_BUDGET_TZ`; сброс на календарной границе дня, никакого «сброса каждые 24 ч от первого запроса»; отображается точное время следующего сброса; изменение лимита не обнуляет уже использованную квоту. **SC-A5-06; REQ-A5-06; D5; T-3596.**
6. **Расширение существующего `worker_budget` без дублирования счётчика:** журнал `image_reservation` — ledger (одна строка на резерв), а не второй агрегатный счётчик; второй счётчик/второй контур локальных настроек запрещены (A0 `#epic3-reuse`); учёт запросов/успехов/ошибок выводится из журнала. **SC-A5-07/-10; REQ-A5-10; D1/D6; T-3591, T-3598.**
7. **Существующие прямые/tool-пути (`run_image_request`/пре-гейт, оба входа) без регрессов:** 2-вызовность System 2 сохранена; A3-контракт `ToolContext.image_request_handled` сохранён (ставшая списанием генерация не дублируется при `skipped/already_handled` — cross-path double-spend исключён A3 + D3-ключ различает входы). **SC-A5-02/-10; REQ-A5-08/-10; T-3592, T-3594, T-3600.**
8. **fail-open — паритет baseline (D8):** при недоступности PG/таблицы — `ok=True, reason='failopen'` + честный WARNING с дедупом; транзакция исключает частичный расход; поведение не хуже baseline; тишина потери квоты/двойное списание на пути отказа исключены; master-рубильник бюджетов OFF и киль-свитч OFF → legacy-путь (baseline-эквивалент). **SC-A5-13/-10; REQ-A5-10; D8; T-3591, T-3601.**
9. **§104 `generate_image` вне diff** (модель/провайдер/ключи/промпты/обработка ошибок/порядок публикации/механизм прикрепления не меняются); обвязка резерва — вокруг неизменного генератора; **ADR-1013-3 = NOT_APPLICABLE** (промптов нет). **SC-A5-10; REQ-A5-10; D10; T-3592, T-3602.**
10. **UI §27/§50 — санкционировано (D9), T-3603 — АКТИВНАЯ (не N/A):** врезка «Модули → Генерация изображений → Лимиты» в существующий контур Эпика 1 (`mod_images`): лимит + «Использовано сегодня: N/M» + источник + время следующего сброса + TZ; **Δ каталога = +1 ParamSpec `limits.image_daily_limit` +1 GroupSpec `limits_images`** — ожидаемо **470/427/445/101/99/21** (Builder обновляет тесты каталога; финал per F8 `--check`); отдельная админ-панель (§50) и дублирование настройки в независимых формах — запрещены. **SC-A5-09/-12; REQ-A5-09; D9; T-3600, T-3603.**
11. **Δ DDL ≠ 0 — санкция ADR-1026-17 (D1, verbatim §«Санкция»):** PG-таблица `image_reservation` (PK `reservation_key`, статус-буфер `reserved/committed/released/denied`) + 2 индекса в `DDL_STATEMENTS`; **идемпотентный DDL** (`IF NOT EXISTS`), применяется автоматически при старте (`PgDatabase.init()`); **SQLite не меняется** (Δ DDL=0, `user_version` = 12); `worker_budget` — без изменения схемы; R17: таблица не хранит контент. Без санкции — Δ DDL=0 обязателен. **SC-A5-11; REQ-A5-10; D1; T-3598, T-3600.**
12. **R17/R18 соблюдены:** в логах и в журнале без R17-нарушений (только числа/коды/id/`source`/`status`/`reason`; сырые тексты/промпты/URL/ключи не логируются), теги/бэкапы/`stash` целы; наблюдаемость — reuse существующих логов + ExecutionGraph (вторая система аналитики не создаётся). **SC-A5-10/-14; D12; T-3599, T-3601, T-3602.**
13. **Deploy = `DEFERRED` (EPIC_ONLY, D11):** пер-фича деплоя/тега/бампа нет; вклад в pending epic-release Эпика 3 (bump 2.58.31 — на границе эпика, T-3606); **DDL применяется автоматически при старте прод (`PgDatabase.init()`) — в составе агрегатного деплоя Эпика 3**; hot-откат = env-киль-свитч `IMAGE_DAILY_LIMIT_ENABLED=OFF`; cold-откат = `git revert` к анкеру `e8646af`; **DDL-откат не требуется (таблица инертна, `DROP` не нужен)**. **SC-A5-14; D11; T-3606.**
14. **Границы волн A4/A6/A7 соблюдены (D10):** §22–§25 (memory-контекст изображений — A4), §32–§35 (`get_user_context` — A6), §36–§37 (URL+фактчек как функциональность — A7; §37 — только инвариант «контент не отключает лимиты») — не реализуются; поля-заглушки памяти A3 остаются заглушками; REUSE ExecutionGraph/A2-envelope/A3-маркер. **SC-A5-10/-13; REQ-A5-10; D10; T-3602.**

## Связь с другими волнами (НЕ реализуется в A5)
| §ТЗ | Тема | Ориентир |
|---|---|---|
| §22–§25 | Изображения на основе памяти (досье/RAG, резолв имён, сборка промпта) | **A4** (`image-context-memory`; поля `context_required`/`context_sources`/`resolved_subjects` остаются заглушками) |
| §32–§35 | `get_user_context(purpose)`, structured memory lookup | **A6** |
| §36–§37 | URL+фактчек, безопасность tool output | **A7** (§37 — только как инвариант-ориентир для резерва: контент не отключает лимиты) |
| §38–§48 | Decision Making/реакции | A7/A8 |
| §49/§51 | ExecutionGraph/события (`IMAGE_GENERATION_*` — A9) | **A9** (REUSE ExecutionGraph; новые события A5 не создаёт) |
| §52–§54 | Сценарии/критерии/результаты | **A10** (gate) |

## Блок 0 — baseline / точка отката (T-3587)
> Baseline снят Step 0 @Memory (не перемеряется в Step 1). Пер-фича annotated-тег **НЕ создаётся** (`EPIC_ONLY`): фиксируем явным примечанием, **задачу на тег не заводим**; baseline-анкер = коммит **`e8646af`**.
- [x] **T-3587 [@Orchestrator/@Memory — подтверждение baseline]** — **Цель:** зафиксировать в дереве решений, что A5 стартует с anchor `e8646af` + незакоммиченный pending epic-release код A2/A3 (НЕ трогать); baseline-числа (2.58.30; pytest 9156/0; JS 47/47; канон 11; Δ DDL=0; прод 652183) заимствуются из Step 0 без перемера. **Выход:** подтверждение в KG/`workflow_state` (машинный блок — только через `workflow_checkpoint`). **Критерий:** baseline-анкер и точка отката согласованы; пер-фича тега НЕТ (EPIC_ONLY); незакоммиченный epic-release кандидат A2/A3 не трогается задачей A5. **Зависимости:** Шаг 0 @Memory ✅. **Статус: [x] ✅ (25.09.2026, подтверждено входом @Orchestrator — сверка T-3589):** baseline-анкер = коммит **`e8646af`**, **БЕЗ пер-фича тега** (EPIC_ONLY, R18); незакоммиченный pending epic-release код A2/A3 не затрагивается A5.

## Блок A — spec + ADR (Шаг 2 @Architect) + сверка @PM
> ADR-1026-17 (D1–D12) создан (Шаг 2 ✅); REQ-A5-01…-10 → **SC-A5-01…-14** (spec.md §2/§8). Открытые вопросы (a)–(e) — **закрыты** (см. раздел ниже).
- [x] **T-3588 [@Architect]** — **Цель:** создать `spec.md` (REQ-A5-01…-10 → SC, трассировка §26–§31/§27/§50; границы §104/§85-UI/A4/A6/A7) + **ADR-1026-17** с verbatim Δ DDL-санкцией: (i) хранилище резерва/учёта и ключевые схемы (SQLite/PG-ветки/унификация), (ii) точный Δ DDL (какие таблицы/индексы, мигра-policy), (iii) scope-семантика, (iv) TZ/сброс, (v) идемпотентность, (vi) fail-open/fail-closed политика. **Выход:** `spec.md` + `adr-1026-17-image-daily-limit.md` (Status Proposed; Accepted — по merge). **Критерий:** каждый REQ-a5 имеет SC; решения не сузают требования §26–§31; Δ DDL зафиксирован verbatim; `SpecDecision-a5-daily-limit` → CLOSED. **Зависимости:** T-3587. **Статус: ✅ (Шаг 2 @Architect, 25.09.2026):** `spec.md` (SC-A5-01…14, §1–§13) + `adr-1026-17-image-daily-limit.md` (D1–D12) созданы; санкция Δ DDL — verbatim (таблица `image_reservation` + 2 индекса; SQLite v12); Δ каталога — §6.1 (D9); Status ADR: Proposed → Accepted по merge.
- [x] **T-3589 [@PM — сверка]** — **Цель:** сверка `tasks.md` ↔ `spec.md` ↔ ADR-1026-17: заполнить SC/ADR-колонки трассируемости/инвариантов, построить карту «решения → задачи/блоки», привести deploy/baseline к ADR. **Выход:** **PLANNING_CONSISTENT** (или список точных расхождений → реконсиляция @Architect через @Orchestrator; Build не стартует). **Критерий:** scope/исключения/риск/приёмка/зависимости/deploy/rollback согласованы; orphan-REQ/SC нет. **Зависимости:** T-3588. **Статус: ✅ (эта сверка, 25.09.2026):** SC/ADR-колонки заполнены (REQ-A5-01…-10 → SC-A5-01…-14 → D1–D12); карта D1–D12 → задачи построена; 14 инвариантов приведены к SC-A5-01…-14 без потерь; закреплены санкции: **Δ DDL ≠ 0 (ADR-1026-17: таблица/индексы/идемпотентный DDL; SQLite v12 не меняется)**, **Δ каталога = +1 параметр +1 группа (Builder обновляет тесты каталога; expected 470/427/445/101/99/21)**, fail-open паритет (D8), Risk **R3** (D12); T-3603 — активная (санкция D9, не N/A); тесты атомарности при гонке связаны со **SC-A5-04**; deploy = DEFERRED (EPIC_ONLY); baseline-анкер `e8646af` подтверждён (без тега); verbatim-цитаты выверены по `current_task.md`. **Итог: `PLANNING_CONSISTENT` — расхождений, требующих эскалации @Architect, нет; Шаг 3 @Builder (T-3590) разрешён.**

## Блок B — контракт резерва/идемпотентности (ADR-1026-17: D1–D5)
> SC: **SC-A5-01/-02/-04/-05/-06**. ADR: **D1** (PG + журнал), **D2** (условный UPSERT), **D3** (idem key), **D4** (scope), **D5** (TZ). Контракт зафиксирован Шагом 2 (spec.md §4/§6.2, ✅) — прототипов не требуется; T-3590 кодифицирует сигнатуры по spec §6.2.
- [x] **T-3590 [@Architect/@Builder]** — ✅ **DONE (25.09.2026, Builder):** контракт spec §6.2 реализован — `reserve_image`/`commit_image`/`release_image`/`image_limits`/`image_day`/`image_next_reset` (`services/worker_budget.py`); условный UPSERT D2, idem D3, scope D4, TZ D5. **Цель:** кодифицировать контракт (решения приняты Шагом 2): (a) атомарный резерв квоты вместо check-after-increment `consume` — **условный UPSERT `... WHERE used < limit`** (D2; отказ не расходует; shared+per-chat — одна транзакция); (b) идемпотентность повторной обработки — idem key **`{chat_id}:{message_id}:{source}`** (D3; UNIQUE/PK журнала; fallback-цепочка по ADR D3); (c) интерфейс scope-резола (D4): каталог `limits.image_daily_limit` (chat → global → env 60) ≠ shared `WORKER_DAILY_IMAGE_CALLS_GLOBAL` (200, env-only); (d) TZ-резолв дня (D5): per-chat `limits.chat_timezone` (reuse `resolve_timezone`), shared — `WORKER_BUDGET_TZ`. **Выход:** сигнатуры spec §6.2 (`reserve_image`/`commit_image`/`release_image`/`image_limits`/`image_day`/`image_next_reset`) в рабочем дереве. **Критерий:** REQ-A5-01/-02/-04/-05/-06 → SC-A5-01/-02/-04/-05/-06; D2–D5; без второго счётчика. **Зависимости:** T-3588 ✅, T-3589 ✅.
- [x] **T-3591 [@Builder]** — ✅ **DONE (Builder):** атомарный reserve/release в существующем `worker_budget` (PG; SQLite v12 Δ DDL=0); fail-open D8 + R17-лог. **Цель:** реализовать атомарный reserve/release в расширенном **существующем** `worker_budget` (без дублирования счётчика; хранилище — **PostgreSQL** per D1: резерв/журнал — PG, **SQLite Δ DDL=0 (v12)**, `worker_budget` без изменения схемы), c сохранением fail-open семантики baseline (D8, cash-аннотации hotfix5) и честным R17-логом. **Выход:** код + юнит-тесты. **Критерий:** REQ-A5-01/-02/-10 → SC-A5-01/-02/-10; D1/D2/D8; атомарность под конкурентным воркером. **Зависимости:** T-3590.

## Блок C — интеграция: atomic reserve → генерация → rollback при сбое (§29)
> SC: **SC-A5-01/-02/-03/-08**. ADR: **D2** (reserve → generate → commit/release), **D3** (idem key), **D7** (rollback/outcome). Единая точка: `generate_and_send` (обходить tool- и direct-входы A3 по единому контракту; A3-маркер — reuse).
- [x] **T-3592 [@Builder]** — ✅ **DONE (Builder):** `_reserve_or_consume` вместо `consume` на входе (`services/image_generation.py:1050-1115`); порядок D2; отказ до платного вызова. **Цель:** интегрировать **atomic reserve на входе** генерации вместо текущего `consume` (`image_generation._consume_budget:491–510` и вызов-сайты `generate_and_send`/`maybe_handle_keyword`): резерв до запуска генерации (порядок D2 `reserve → generate → commit/release`); провал резерва (лимит) → честный отказ до платного вызова. **Выход:** правки + тесты. **Критерий:** REQ-A5-01/-04/-08 → SC-A5-01/-04/-08; D2; прямые/tool-пути без регрессов (2-вызовность, A3-маркер). **Зависимости:** T-3591.
- [x] **T-3593 [@Builder]** — ✅ **DONE (Builder):** `_commit_or_release` (`services/image_generation.py:1118-1137`); сбой генерации→release, успех (вкл. сбой доставки)→commit+`delivery_failed`; без авто-повтора. **Цель:** **rollback резерва при сбое генерации** (результат не создан → **release**, квота возвращена — §29) и честный исход при успехе (D7): успех генерации, **включая сбой доставки в Telegram** («расход уже мог произойти») → **commit** + флаг `delivery_failed=true`; повторная платная генерация ради доставки НЕ запускается. **Выход:** правки + тесты. **Критерий:** REQ-A5-03/-08 → SC-A5-03/-08; D7. **Зависимости:** T-3592.
- [x] **T-3594 [@Builder]** — ✅ **DONE (Builder):** idem-ключ `build_image_idem_key` (`services/image_generation.py:212-230`); replay committed/reserved→`already`, released/denied→прежний отказ; без двойного списания. **Цель:** идемпотентность повторной обработки одного сообщения — ключ **`{chat_id}:{message_id}:{source}`** (D3): одинаковый запрос/ретрай/двойной вызов (прямое+tool, уже защищённые A3 от двойной генерации) не списывает квоту второй раз (`reason='already'`); интеграция с маркером `ToolContext.image_request_handled` (двойного списания при `skipped/already_handled` нет; cross-path double-spend исключён A3-маркером, ADR-1026-16 D3). **Выход:** правки + тесты. **Критерий:** REQ-A5-02 → SC-A5-02; D3. **Зависимости:** T-3592.

## Блок D — Scope / лимиты / TZ (§26/§30/§31)
> SC: **SC-A5-05/-06**. ADR: **D4** (scope), **D5** (TZ/сброс) — решения Шага 2 приняты, «ждать» нечего.
- [x] **T-3595 [@Builder]** — ✅ **DONE (Builder):** `_metric_limit` per-chat `image_calls` резолвит каталог `limits.image_daily_limit` (chat→global→env 60); shared — env-only 200 (`services/worker_budget.py:358-396`, `791-810`). **Цель:** честная scope-семантика (D4): каталожный ключ **`limits.image_daily_limit`** — «глобальный лимит по умолчанию» с per-chat override через существующее наследование (`resolve_setting_cached`: chat → global → env-дефолт 60; sentinel сохранён; per-chat override для `image_calls` сейчас НЕ резолвится — gap §26/§30 fix); «общая квота всех чатов» = **существующий** shared-бюджет `WORKER_DAILY_IMAGE_CALLS_GLOBAL` (200, env-only, явно названный, отдельная UI-строка без каталожного ключа). **Выход:** правки + тесты. **Критерий:** REQ-A5-05 → SC-A5-05; D4; без смешивания «дефолт» и «общая квота». **Зависимости:** T-3591.
- [x] **T-3596 [@Builder]** — ✅ **DONE (Builder):** `_chat_tz_name`/`image_day`/`image_timezone`/`image_next_reset` (`services/worker_budget.py:506-567`); календарный день, точный сброс; смена лимита не обнуляет `used`. **Цель:** календарный день/TZ/сброс (D5): день per-chat по **`limits.chat_timezone`** (per-chat → global → fallback `limits.summary_timezone`, reuse `resolve_timezone` — прецедент `_chat_time_line`/`_chat_timezone`); shared-бюджет — `WORKER_BUDGET_TZ` (legacy `consume` воркеров — `today()` без регресса); воспроизводимая граница без «24ч от первого запроса»; вычисление точного времени следующего сброса (`next_reset_at`, аддитивно — для UI §27); изменение лимита не обнуляет использованную квоту. **Выход:** правки + тесты. **Критерий:** REQ-A5-06 → SC-A5-06; D5. **Зависимости:** T-3591.

## Блок E — учёт: запросы/успехи/ошибки (§28/§30 частичный учёт)
> SC: **SC-A5-07/-08**. ADR: **D6** (учёт из журнала `image_reservation` — не второй счётчик), **D7** (rollback/outcome).
- [x] **T-3597 [@Builder]** — ✅ **DONE (Builder):** `n=1` (§104); ошибки до генерации не расходуют лимит; сбой доставки→commit+`delivery_failed` (`services/image_generation.py:1182-1218`). **Цель:** семантика единицы учёта §28 (D6/D7): один успешно созданный результат = одна генерация (**`n=1`** §104: генератор создаёт ровно один результат — фиксировано); ошибки до запуска генерации не расходуют пользовательский лимит; сбой доставки при успешной генерации → commit + `delivery_failed=true` (см. T-3593). **Выход:** правки + тесты. **Критерий:** REQ-A5-08 → SC-A5-08; D6/D7. **Зависимости:** T-3593.
- [x] **T-3598 [@Builder]** — ✅ **DONE (Builder):** PG-таблица `image_reservation` +2 индекса (`services/pg_db.py:359-379`, идемпотентный DDL); агрегаты `image_usage_summary` (`worker_budget.py:813-874`) — не второй счётчик; R17. **Цель:** отдельный учёт запросов/успешных генераций/ошибок (D6): выводится из журнала `image_reservation` (`committed`/`released`/`denied`, флаг `delivery_failed`) — **не второй счётчик** `image_calls`; «фактические расходы» — N/A (провайдер не отдаёт стоимость, документировано); данные для UI N/M/источника. **Выход:** PG-таблица `image_reservation` (+2 индекса — идемпотентный DDL по ADR §«Санкция»; применяется автоматически `PgDatabase.init()`; отдельная миграционная задача НЕ требуется; **SQLite Δ DDL=0**) + агрегаты `image_usage_summary` (spec §6.2) + тесты. **Критерий:** REQ-A5-07 → SC-A5-07; D1/D6; R17 (в таблице нет контента — только id/`source`/`status`/`error_code`/`day`). **Зависимости:** T-3595, T-3596.

## Блок F — тесты / R17 / регресс §104
> SC: все (SC-A5-01…-14). ADR: **D12 (R3 — усиленные доказательства обязательны)**; adversarial-приёмка обязательна. Ядро атомарности — **SC-A5-04 (атомарность при гонке: два одновременных резерва при остатке 1 → ровно один; отказ не расходует)**.
- [x] **T-3599 [@Builder — adversarial-тесты]** — ✅ **DONE (Builder):** `tests/test_image_daily_limit_round1026.py` (13 passed: A1 happy, A2 race SC-A5-04, A3 deny/release, A4 idem, A5 delivery, A6 TZ, A7 OFF/fail-open); `threat-failure-analysis.md` (12 угроз). **Цель:** тесты атомарности и двойного списания-гонки (**SC-A5-04**: эмуляция двух одновременных запросов при остатке 1 → ровно один резерв; отказ не расходует квоту), rollback при сбое генерации/публикации (D7), идемпотентности (повторная обработка/ретрай — D3), TZ-грейс (TZ-boundary/сброс — D5), OFF/legacy (kill-switch OFF → поведение идентично baseline — D11). **Выход:** тесты. **Критерий:** REQ-A5-01…-08 → SC-A5-01…-08; инварианты 1–5, 8; D12 (race/atomic-order). **Зависимости:** T-3592…T-3598.
- [x] **T-3600 [@Builder — регресс]** — ✅ **DONE (Builder):** pytest **9170/0** (baseline 9156 +14); JS **47/47**; каталог **470/427/445/101/99/21**; SQLite v12; §104 AST-гейт (ядро `generate` SAME); `git diff --check`=0. **Цель:** полный pytest (база **9156/0** → цель +N/0), JS **47/47**; §104 AST-гейт не нарушен; канон **11**; **Δ каталога = санкция D9: +1 ParamSpec +1 GroupSpec — ожидаемо 470/427/445/101/99/21** (Builder обновляет тесты каталога; финал per F8 `--check`); **SQLite `user_version` = 12 (Δ DDL=0 против SQLite)**; идемпотентность DDL (`PgDatabase.init()` ×2 — no-op). **Выход:** отчёт прогона. **Критерий:** 0 регрессий; SC-A5-10/-11/-12. **Зависимости:** T-3599.
- [x] **T-3601 [@Builder — R17/R18 + observability]** — ✅ **DONE (Builder):** журнал хранит только id/`source`/`status`/`error_code`/`day`/`delivery_failed` (`pg_db.py:359-379`); логи fail-open — только chat/source/reason; теги/бэкапы не тронуты. **Цель:** проверка лог-гигиены (R17): в логах и в журнале `image_reservation` — только числа/коды/id/`source`/`status`/`reason` (без сырых текстов/промптов/URL/ключей); fail-open-аннотации в логах честные (WARNING с дедупом — D8); теги/бэкапы/`stash` целы (R18). **Выход:** чек-лист/отчёт. **Критерий:** инварианты 8, 12 → SC-A5-13/-10/-14. **Зависимости:** T-3599.

## Блок G — интеграция / границы
> SC: **SC-A5-10/-13** (границы), **SC-A5-09/-12** (UI/каталог). ADR: **D10** (границы/REUSE), **D9** (UI/Δ каталога).
- [x] **T-3602 [@Builder]** — ✅ **DONE (Builder):** diff-аудит — A4/A6/A7 файлы отсутствуют; канон 11; `ADR-1013-3 = NOT_APPLICABLE`; поля-заглушки A3 не тронуты; Δ DDL по §7 (PG-таблица санкционирована), Δ каталога D9. **Цель:** границы (D10): §104/A4 (§22–§25)/A6 (§32–§35)/A7 (§36–§37) вне diff; REUSE ExecutionGraph/A2-envelope/A3-маркер (без новой аналитики); reuse существующей системы scope/наследования каталога без самостоятельного механизма; §37-инвариант «контент не отключает лимиты» — не ослабить; **канон инструментов = 11** (не расширяется); **ADR-1013-3 = NOT_APPLICABLE** (промптов в diff нет); поля-заглушки A3 — не трогаются. **Выход:** diff-аудит (Δ DDL по §7; Δ каталога по D9; второй счётчик; ExecutionGraph; канон). **Критерий:** инварианты 6, 9, 12, 14 → SC-A5-10/-13. **Зависимости:** T-3598.
- [x] **T-3603 [@Builder — UI §27/§50 — АКТИВНАЯ (санкция D9, не N/A)]** — ✅ **DONE (Builder):** врезка «Лимиты» в `mod_images` (`web/index.html` `data-image-limits`; `web/app.js` `imageUsage`/`imageSourceLabel`/`imageResetLabel`, `WORKSPACE_TABS.mod_images += 'limits'`); аддитивные поля `/api/workers/budget` из `image_usage_summary`; web-тест `test_image_usage_additive_fields` (10 passed). **Цель:** врезка UI «Модули → Генерация изображений → Лимиты» в **существующий** UI-контур Эпика 1 (вкладка `mod_images`): «Дневной лимит изображений» + «Использовано сегодня: N/M» + источник лимита (`chat`/`global`/`default`) + точное время следующего сброса + текущая TZ; отдельная (read-only) строка «Общий бюджет всех чатов»; reuse `/api/workers/budget` (аддитивные поля `day`/`timezone`/`next_reset_at`/`source`); **Δ каталога = +1 ParamSpec `limits.image_daily_limit` (int, группа `limits_images`) +1 GroupSpec `limits_images` (category `limits`, tab `mod_images`)** — ожидаемо 470/427/445/101/99/21 (обновление тестов каталога — вместе с T-3600; финал per F8 `--check`); **без** дублирования настройки в независимых формах и **без** отдельной админ-панели (§50). **Выход:** UI-код (+1 параметр/+1 группа в каталоге). **Критерий:** REQ-A5-09 → SC-A5-09/-12; инвариант 10; D9. **Зависимости:** T-3588 ✅, T-3590, T-3598.

## Блок H — финализация
- [x] **T-3604 [@Reviewer — единый gate]** — **Цель:** приёмка A5 единым Reviewer gate (Scanner **НЕ назначается** — удалён): C0/H0, обе линзы, binding Reviewed-Commit/Spec-Hash; **R3-усиленные доказательства (D12):** threat/failure-анализ, race/atomic-order-тесты + concurrency-проба (**SC-A5-04 — атомарность при гонке: два параллельных резерва при остатке 1 → ровно один; отказ не расходует**), rollback-доказательства (OFF/legacy, hot/cold, анкер `e8646af`, отсутствие DDL-отката), adversarial-приёмка, diff-аудит (Δ DDL §7; Δ каталога D9 = 470/427/445/101/99/21; §104/§85-панель; ExecutionGraph; канон 11; второй счётчик). **Выход:** вердикт. **Критерий:** Approved или документированные блокеры. **Зависимости:** T-3599…T-3603. — ✅ **APPROVED (25.09.2026):** единый Reviewer gate **Approved C0/H0 (R3)**, обе линзы; binding Reviewed-Commit `e8646af` / Working-Tree-Hash `b57f1f0e…` / Spec-Hash `428c44b4…`; findings **F1–F7 — non-blocking watch-items** агрегатного gate Эпика 3; вердикт — `review-T-3604.md`.
- [x] **T-3605 [@PM — merge/archivation]** — **Цель:** merge в `plans/ARCHITECTURE.md` (ожидаемо **§86**) @Architect (ADR-1026-17 → Accepted); архивация feature-папки `plans/archive/image-daily-limit-round1026/` @PM — **в архив входят полные R3-доказательства (D12): threat/failure-анализ, race/concurrency-пробы (SC-A5-04), rollback-флоу — без потерь**; durable-аудит не трогается (только чтение). **Критерий:** html-архив полон; проверка ссылок; handoff-навигация backlog/metrics обновлена на СВОИХ шагах. **Зависимости:** T-3604. — ✅ **Architect-часть DONE (25.09.2026):** merge выполнен — `plans/ARCHITECTURE.md` **§86** добавлен, ADR-1026-17 Status → **Accepted (merge to §86, 25.09.2026)**; **PM-часть ✅ DONE (25.09.2026):** папка перенесена `plans/features/image-daily-limit-round1026/` → `plans/archive/image-daily-limit-round1026/` (6 файлов; SHA-256 до/после переноса идентичны 6/6; далее финализирован **только `tasks.md`** — этот статус; остальные 5 файлов байт-в-байт); inbound-ссылки обновлены (`ARCHITECTURE.md` §86.11 + §86 статус-строка + §85.9, `backlog.md`); durable-аудит `plans/docs/agentic-audit-round1026.md`, `plans/MEMORY.md` и `tests/` — ссылок на фича-папку A5 нет (проверено grep).
- [x] **T-3606 = `DEFERRED_TO_EPIC`** — **Цель:** пометить A5 как **accepted into pending epic release** (deployment `DEFERRED_TO_EPIC`; НЕ «done/VERIFIED»), вклад в агрегатный релиз Эпика 3 (bump 2.58.31 в агрегатном деплое; **DDL применяется автоматически при старте прод — `PgDatabase.init()`, идемпотентно; отдельные миграционные шаги не требуются**). НЕ per-фича деплой; **@DevOps не вызывается**. **Откат (D11):** hot = `IMAGE_DAILY_LIMIT_ENABLED=OFF`; cold = `git revert` к анкеру **`e8646af`**; **DDL-откат не требуется** (таблица `image_reservation` инертна; `DROP TABLE` не нужен). **Зависимости:** T-3604. **Статус: ✅ ЗАКРЫТ КАК `DEFERRED_TO_EPIC` (25.09.2026) — НЕ «done/VERIFIED»:** A5 принята в pending epic-release Эпика 3; bump 2.58.30 → 2.58.31 — только на границе эпика; @DevOps не вызывался, деплой/тег/bамп не выполнялись.
- [x] **T-3607 [@PM — handoff → A6/A4]** — **Цель:** размытие: вердикт порядка (по волновому порядку следующая — **A6**, затем **A4** (`image-context-memory` — §22–§25, идёт **позже**, была перепутана с A5 при архивации A3)); перенос вход-фактов (лимиты/reserve-контракт, SC/D-ссылки) в постановку следующей фичи. **Критерий:** backlog/KG-таблицы согласованы с фактическим порядком. **Зависимости:** T-3606. **Статус: ✅ DONE (25.09.2026):** очередь по таблице бэклога — A5 ✅ → **▶️ следующая A6 `memory-lookup-api`** (§32–§35) → затем A4 `image-context-memory`; обновлены `backlog.md` (статусная строка эпика, таблица A5/A6, строка порядка, «A5 handoff» с вход-фактами A6) и `ARCHITECTURE.md` §85.9 (устаревший «A5 `image-context-memory`» → «A5 ✅ → A6 → A4»).

## Открытые вопросы (a)–(e) — ЗАКРЫТЫ Шагом 2 (ADR-1026-17, spec.md §12; PM НЕ решал ✅; сверено T-3589)
- **(a) Хранилище резерва/учёта и Δ DDL → D1/D2:** хранилище — **PostgreSQL** (`worker_budget` + журнал `image_reservation`); резерв — условный атомарный UPSERT поверх существующей таблицы; **Δ DDL-PG зафиксирован verbatim** (таблич `image_reservation` + 2 индекса; идемпотентный DDL; авто-применение `PgDatabase.init()`); **SQLite Δ DDL=0** (остаётся v12); `worker_budget` — без изменения схемы; вторая таблица-счётчик и SQLite-ветка отклонены.
- **(b) Дефолт лимита и scope-семантика → D4:** каталожный ключ `limits.image_daily_limit` (per-chat override → global → env 60; sentinel сохранён); «общая квота всех чатов» = существующий `WORKER_DAILY_IMAGE_CALLS_GLOBAL` (200, **env-only**, явно названный); смешивание запрещено; независимый механизм локальных настроек не создаётся.
- **(c) TZ/сброс → D5:** per-chat день по `limits.chat_timezone` (reuse `resolve_timezone`, прецедент `_chat_time_line`/`_chat_timezone`), shared — `WORKER_BUDGET_TZ`; календарный день (не «24ч от первого запроса»); точное время сброса в UI; изменение лимита не обнуляет `used`.
- **(d) idem key → D3:** **`{chat_id}:{message_id}:{source}`** (UNIQUE/PK журнала; source ∈ {direct, tool}); fallback-цепочка по ADR D3; replay-семантика; retention `IMAGE_RESERVATION_RETENTION_DAYS` (env-only, default 30); `correlation_id`-ключ отклонён (не переживает повторную доставку update).
- **(e) Границы A4/A6/A7 → D10:** A4 (§22–§25 память/досье; поля-заглушки A3 остаются), A6 (§32–§35), A7 (§36–§48) не реализуются; события `IMAGE_GENERATION_*`/ExecutionGraph — A9 (REUSE); §52-сценарии — A10; §37 — только инвариант; **ADR-1013-3 = NOT_APPLICABLE; канон = 11**.
- **(f) Deploy/risk → D8/D11/D12:** deploy = **DEFERRED (EPIC_ONLY)** (T-3606); hot-OFF `IMAGE_DAILY_LIMIT_ENABLED`; cold — `git revert` к `e8646af`; DDL-откат не требуется; fail-open (паритет baseline); **Risk-Level R3**.

## Миграционное примечание
- @Scanner удалён (24.09.2026) — обязанности в **едином Reviewer gate**; нигде не назначать (блоки F/G/H — только T-3604).
- @DevOps на per-фича деплой **НЕ вызывается** (release policy **EPIC_ONLY**); release-узел не создаётся.
- Бекап per-фича тега **НЕ создаётся** — baseline-анкер `e8646af` (EPIC_ONLY); hot-откат = env-only kill-switch OFF (`IMAGE_DAILY_LIMIT_ENABLED`); cold-откат = `git revert` к `e8646af`; теги/бэкапы не удаляются (R18).
- **Deploy/DDL (D11):** пер-фича деплоя нет; **в агрегатном деплое Эпика 3 (T-3606) DDL применяется автоматически при старте прод (`PgDatabase.init()` — прогон идемпотентных `DDL_STATEMENTS`); отдельные миграционные шаги/бэкапы DDL не требуются; DDL-откат не нужен (таблица инертна, `DROP TABLE IF EXISTS image_reservation` — только опционально/документировано)**.
- **Δ каталога (D9):** +1 ParamSpec `limits.image_daily_limit` +1 GroupSpec `limits_images` (tab `mod_images`); expected **470/427/445/101/99/21** — тесты каталога обновляет Builder (T-3600/T-3603; финал per F8 `--check`); SQLite остаётся v12; канон инструментов = 11; ADR-1013-3 = NOT_APPLICABLE.
- `plans/current_task.md` — immutable (НЕ изменять/не аннотировать); машинный блок — только через `workflow_checkpoint`.
