# Фича F4 — `nostalgia-prompt-revamp-round1015` (Ревамп Ностальгии: окно, инжект, стиль)

> **Статус: ✅ COMPLETED** (Step 4 @Builder, 14.09.2026). Реализовано, pytest 5654 passed / 0 failed.
> **Раунд:** 10.15. **Нумерация:** T-1575…T-1583 (продолжает F3 T-1574).
> **Тип:** backend (+prompt engineering). **Приоритет:** **P1**.
> **Зависимости:** нет. **Конфликт файлов:** `services/nostalgia_worker.py`,
> `services/nostalgia_prompts.py`, `services/database.py` (мемы).
> **ТЗ:** `plans/current_task.md`, раздел **3**.
> **Эпик:** `Epic: Багфиксы Графа памяти, Воркера Сна и Ностальгии round1015`.
> **Baseline:** HEAD `798e044`; pytest **5589 passed / 0 failed**; каталог **435/90/406/411/88/19**.

## 0. Цель

Ностальгия срабатывает слишком редко и «невпопад». Расширить окно «год назад» с `±2` до `±10` дней,
инжектировать в user-блок актуальный **Лор чата** + **локальные мемы**, переписать системный промпт
под живой ироничный стиль («давний участник беседы, которого пробило на воспоминания»).

**Текущее состояние (Step 0):** окно `memory.nostalgia_year_back_days_window=2`
(`config/settings.py:1173-1174`, `services/param_catalog.py:1535`, `services/nostalgia_worker.py:546-552`);
`NOSTALGIA_PROMPT` `services/nostalgia_prompts.py:21-25`; `build_nostalgia_user` `:103-116`
(только `year_lines` + `golden_lines`); вызов `nostalgia_worker.py:602-609`. Лор/мемы:
`services/lore_cache.py` (`LoreProfile`), `services/chat_lore_store.py`, `services/database.py:3554`
`list_chat_memes` (status='chat_meme'); инжект мемов в контекст уже есть у direct-chat
(`direct_chat_service.py:1592-1595`).

## 1. Требования (дословно из ТЗ §3)

- [x] **Расширение окна поиска:** дефолт «окно год назад» изменить с `±2 дня` на `±10 дней`.
- [x] **Инжект контекста в Промпт:** системный промпт слоя B (NostalgiaWorker) обязан принимать не
  только старый факт, но и текущий **Лор чата** + **Список локальных мемов**.
- [x] **Стиль:** жёсткое условие — *«Вбрось этот старый факт так, как будто ты давний участник
  беседы, которого внезапно пробило на воспоминания. Используй иронию и сленг из предоставленного
  Лора. Не пиши как робот-архивариус.»*

## 2. Целевые модули (`file:line` на HEAD `798e044`)

- `config/settings.py:1173-1174` — дефолт окна; `services/param_catalog.py:1535-1539` — описание (Δ0).
- `services/nostalgia_prompts.py:21-25` — `NOSTALGIA_PROMPT`; `:103-116` — `build_nostalgia_user`.
- `services/nostalgia_worker.py:541-553` — `_year_back_lines`; `:595-616` — `_llm_once`.
- `services/nostalgia_worker.py` — сбор лора (`chat_lore_cache`/`lore_cache`) + мемов
  (`db.list_chat_memes`).
- Тесты: `tests/test_nostalgia_prompts.py`, `tests/test_nostalgia_worker.py`; новый
  `tests/test_nostalgia_revamp_round1015.py`.

## 3. Инварианты (constraints — не нарушать)

- **ADR-1013-3 (prompt-canon policy):** `NOSTALGIA_PROMPT` — канон-константа модуля (НЕ PG-сид,
  НЕ REGISTRY-ключ, `nostalgia_prompts.py:5-7`). Правка подчиняется политике: **PREV_<NAME>**-слепок +
  байт-тесты; `PROMPT_MIGRATIONS` **не трогать** (ностальгия в миграции не входит) — подтвердить
  у @Architect (F4-Q1).
- Сохранить контракт: плейсхолдер `{max_words}` (ровно один), детект `UNCHANGED` (регистр/пробелы),
  запрет маркдауна/кавычек-ёлочек/длинных тире в ОТВЕТЕ; `test_nostalgia_prompts.py:29-46` —
  обновить/дополнить, не сломать).
- `clean_llm_text` cap и `is_unchanged_response`-семантика — не менять.
- Инжект меняет user-блок → обязательны тесты на секции (наличие/отсутствие/порядок/кап).
- **Лучше переиспользовать** ключ `memory.nostalgia_year_back_days_window` (меняем дефолт, не добавляем);
  каталог-инварианты **435/90/406/411/88/19** — Δ=0 (только текст описания при необходимости).
- Лор/мемы не должны ломать fail-open: нет лора/мемов → user-блок как раньше (секции опускаются).
- R17: в промпт/лог не тащить секреты; `media/`/`.env` не трогать; роутеры `bot.py` не трогать.
- **Ревью-гейты:** полный `pytest` 0 регрессий, байт-тесты канона, R17-скан, русские commits.

## 4. Зависимости

- **Вверх:** нет.
- **Вниз:** нет (F7-гайд независимо описывает ностальгию, но пишется после).
- **Конфликт файлов:** нет прямых; `services/database.py` — только чтение `list_chat_memes`.

## 5. Definition of Done

- [x] Дефолт окна «год назад» = **10** дней (настройка/каталог синхронны; переопределяется per-chat).
- [x] User-блок ностальгии содержит секции «Лор чата» и «Локальные мемы», когда они есть.
- [x] Новый стиль промпта: «давний участник», ирония/сленг из лора, анти-«робот-архивариус»;
  `{max_words}`/`UNCHANGED` сохранены.
- [x] Существующие тесты канона обновлены; новые тесты инжекта зелёные; полный `pytest` — **0 failed**.

## 6. Чек-лист задач

- [ ] **T-1575 (@Architect, гейт):** подтвердить политику ADR-1013-3 для `NOSTALGIA_PROMPT`
  (PREV-слепок + байт-тесты, `PROMPT_MIGRATIONS` не трогать); спека секций user-блока
  (заголовки, порядок, капы лора/мемов), формат инжекта и лимиты токенов; решить дефолт 10 (Q1).
- [x] **T-1576 (@Builder):** окно `±2` → `±10`: `config/settings.py:1173-1174` +
  `services/param_catalog.py:1535-1539` (описание «(10)»); проверить фоллбэк в
  `nostalgia_worker.py:546-548` (не хардкодить 2).
- [x] **T-1577 (@Builder):** расширить `build_nostalgia_user` (`nostalgia_prompts.py:103-116`) —
  аддитивные секции «Лор чата» / «Локальные мемы», пустые опускаются; обратная совместимость вызова.
- [x] **T-1578 (@Builder):** в `nostalgia_worker._llm_once` (`:595-616`) собрать лор
  (`chat_lore_cache`/`lore_cache`) и мемы (`db.list_chat_memes(chat_id, limit=…)`) и передать в
  user-блок; fail-open при недоступности.
- [x] **T-1579 (@Builder):** переписать `NOSTALGIA_PROMPT` (`:21-25`) под стиль из ТЗ: «давний
  участник, которого пробило на воспоминания», ирония/сленг из лора, анти-«робот-архивариус», 1-2
  фразы «кстати...», сохранить `{max_words}` и `UNCHANGED`.
- [x] **T-1580 (@Builder):** PREV-слепок канона + обновить байт/маркер-тесты
  (`tests/test_nostalgia_prompts.py:29-46`); `PROMPT_MIGRATIONS` не трогать.
- [x] **T-1581 (@Builder):** тесты инжекта: лор есть / мемов нет / оба пусты (блок как раньше) /
  порядок секций / капы; тест расширенного окна (границы ±10).
- [x] **T-1582 (@Builder):** прогон гейтов: полный `pytest` **0 failed**, каталог-Δ=0, R17-скан,
  `git diff --check`; commit отложен по указанию раунда (не коммитить).
- [ ] **T-1583 (@PM/@Reviewer, гейт):** сверка DoD, ревью длины промпта/стоимости вызова, live-чеклист
  владельцу (частота и уместность ностальгии).

## 7. Открытые вопросы (@Architect → владелец)

- **F4-Q1:** нужен ли PREV-слепок для `NOSTALGIA_PROMPT` при отсутствии его в `PROMPT_MIGRATIONS`
  (достаточно ли байт-тестов)? Подтвердить политику ADR-1013-3.
- **F4-Q2:** кап лора и мемов (сколько символов/штук) и приоритет при переполнении токенов?
- **F4-Q3:** брать ли лор только глобальный или per-chat профиль активного чата?
- **F4-Q4:** мемы — все чата или только релевантные последней теме (как golden-факты)?
- **F4-Q5:** дефолт окна 10 — только дефолт (переопределяемо) или принудительно для всех чатов?

## 8. Feature flag / progressive delivery

- **Feature flag:** не требуется (промпт-канон + дефолт настройки), rollback = `git revert`.
- **Progressive delivery:** неприменимо; при необходимости — `memory.nostalgia_year_back_days_window`
  остаётся рантайм-настройкой для быстрой откатки окна.
