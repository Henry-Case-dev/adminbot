# Спецификация: TMA — информационная архитектура + прогрессивное раскрытие (F-11)

**Эпик:** раунд 10 (07.09.2026), части 2.2 (прогрессивное раскрытие) + 3.1 (уровень навигации) «Multi-chat scaling» (research §4 п. 2/3/9). **Задачи:** T-903…T-913 (база HEAD `fac1b9f`).
**Статус:** спецификация @Architect (T-903) — закрывает Q1–Q6 раздела A `tasks.md`. Зависимости: F-7 (X-Chat-Id, `/api/access/chats`, роль-фильтрация, `params-meta`), F-10 (gates-API блоки), F-12 (вход Oversight на «Главную»).

---

## 1. Скоуп и инварианты

1. Меняются **только** `web/app.js` + `web/index.html` (+ опционально `services/param_catalog.py` — разметка `progressive_level`; БЕЗ изменений API-контрактов, кроме чтения новых полей из уже существующих ответов).
2. Vue 3 Options API, zero-build, `TABS`-массив (:18-47) — **не расширять типами**; добавляется лёгкий тег `menu` + `MENU_ORDER`.
3. Порядок объявлений в `canViewTab`/`hasPerm`/`setTab` — сохранить; рендер `v-else-if` по `activeTab` — сохранить (вкладки — первичный слой, меню — группировочный).
4. Память/API/воркеры — вне скоупа; старые тесты `tests/test_frontend_tab_mapping.py` обновляются **аддитивно** (не ломать ассерты, фиксы F-8 сохранить).

---

## 2. Q1 — Меню-структура (5 секций)

**Решение: тег `menu` у каждой TABS-записи + `MENU_ORDER`** (минимальный дифф; НЕ новый MENU-массив ссылок — дублирование источников данных, два места правды).

| Меню (id, label_ru) | Вкладки (TABS id) | Тип | Кто видит (canViewTab-правило) |
|---|---|---|---|
| `home` — «Главная» | `status` (раскрывается сразу), `info` («Как это работает» — вторым чипом внутри), **`oversight`** (F-12, только global) | status/info | всегда (status) / info всегда / oversight — глобальный админ |
| `chat_profile` — «Чат-Профиль» | `chat_lore` (лор + relations + админы лора) | chat_lore | секция chat_lore / wildcard / грант (существующее canViewTab) |
| `modules` — «Модули и Фичи» | `reactions_triggers` (конфиг), **`modules_feats`** (НОВАЯ вкладка — карточки: «Тяжёлые фичи» F-10, «Функции PERMsoc» F-9, «Бюджет фона» F-10) | config + modules | секции reactions/flags_media; модульная вкладка — по min-роли (moderator+) или локальный админ |
| `ai` — «Настройки AI» | `llm_providers`, `prompts`, `limits`, `memory_rag` | config | существующие права секций моделей/промптов/лимитов/памяти |
| `access` — «Доступы и Роли» | `access` (+ новые карточки F-7: локальные админы, мой доступ, роль-пикер) | access | секция access / wildcard |

- **`oversight`** — вкладка F-12 (`type:'oversight'`, рендер отдельным блоком index.html) получает здесь точку интеграции: при F-12 внедряется как TABS-запись; до него — карточка на `status` для глобальных (временная, за F-12).
- `modules_feats` — тип `modules` (гибрид: конфиг-слои + карточки-сервисы; рендер dedicated-блок index.html), данные — gates API (F-10) + budget API; кнопки toggle — по `who_can_toggle` (F-10 §7).
- Приоритет рендера меню-секций: `MENU_ORDER = ['home','chat_profile','modules','ai','access']`; вкладка не отображается в меню, если её не пропускает `canViewTab` (кроме never-visibility home/status/info).
- `activeMenu` — отдельное поле (данные), `activeTab` — как есть; `setMenu(m)` → первый доступный tab секции.

## 3. Q2 — Селектор чатов

- **Источник**: `GET /api/access/chats` (F-7 §6) — первичный; фолбэк `GET /api/chat_lore/chats` (существующий; если part-1 ещё не выкатан — UI работает по старому).
- **Поля селектора**: `chat_id`, `title` (title берётся из ответа доступов; иначе из `chat_lore`-списка; иначе «Чат -100…»), активный — localStorage `adminbot.active_chat_id`.
- **Правила:**
  1. Пользователь с глобальным доступом к >1 чату (global admin) → селектор виден, значения: «Весь бот» (глобальный режим, NULL) + каждый чат; дефолт при загрузке — «Весь бот» (старое поведение).
  2. global admin с 1 чатом/0 чатов → селектор **скрыт**, режим «Весь бот».
  3. Локальный админ/mod-грант (список непустой) → селектор виден, дефолт — первый доступный; «Весь бот» недоступен (у них нет глобального режима).
  4. user (нет чатов) → селектор скрыт, режим глобальный (только status/info — старое поведение).
  5. Сохранённый `active_chat_id` не в списке → сброс к правилу 1/3, `localStorage.removeItem`.
- **Применение**: при выборе чата `api()` автоматически добавляет `X-Chat-Id` (F-7); при «Весь бот» — заголовок отсутствует. Конфиг-вкладки/«Чат-Профиль» перерисовываются (`activeChatChanged`-событие).
- Индикатор активного скоупа в шапке: чип-бейдж «Весь бот» / название чата + мелкий chat_id (прецедент T-873).

## 4. Q3 — Прогрессивное раскрытие

### 4.1 Разметка `progressive_level`

- `ParamSpec.progressive_level: str = ""` (пусто = basic).
- **Правило по умолчанию** (функция `resolve_progressive_level(spec)` в `param_catalog.py`): `advanced`, если:
  - `spec.group` ∈ {`limits_memory`, `limits_graph`, `flags_memory`, `memory_infinite`} (память/RAG-группы), ИЛИ
  - ключ (`ship пустое — это техgroup` читаем по pg_key) содержит одно из: `search_top_k`, `vector`, `graph`, `rag`, `retr`, `timeout`, `context`, `summary_length`, `budget`, `dedup`, `ttl`, `window`, `density`, `chunk`.
  Для остальных — `basic`.
- Для конкретных ключей, где правило неоднозначно (например `prompts.*`), разметка **явно** в REGISTRY (`progressive_level='basic'`).
- Потребители: `GET /api/config` (F-7 `params-meta` — включить `progressive_level` в состав) + фронт-рендер.

### 4.2 Рендер (config-вкладки: llm_providers, prompts, limits, memory_rag, reactions_triggers)

- generic-рендер групп (index.html :320-440 по 84.24) получает `item.progressive_level`; группы делятся: «Базовые» рендерятся как сейчас; «Расширенные» — аккордеон `<details class="advanced">` (первый `<summary>` — «Расширенные настройки»), по умолчанию `closed` (без `open`).
- Расширенные **не рендерятся вообще**, если параметр отфильтрован по min-ролям (F-7: `hidden_from_local`/ключи) — удаление до раскрытия, НЕ схлопывание.
- Состояние раскрытия per tab в localStorage: `adminbot.expand:<tab_id>` = `'1'` (другие значения — closed); меняется по клику на `<summary>` (без реактивных событий — нативное поведение).

### 4.3 Видимость для ролей

- **User (read-only)**: ТОЛЬКО «Главная/Статус» и «Как это работает» (`canViewTab` → false для остальных; меню-секции кроме home — скрыты).
- **Local admin/moderator**: видят свои секции (по F-7 `can_view_param`+секциям); расширенный блок доступен (его содержимое всё равно фильтруется по min-ролям).
- **Global admin**: всё.

## 5. Q4 — «Главная» (home)

- Состав: карточка «Статус» (существующий рендер status) + «Как это работает» (info) — два пункта-суб-навигации внутри меню home (чипы «Статус» / «Справка», сохраняющие `setTab('status')`/`setTab('info')`).
- Для глобального админа: дополнительно карточка-вход «Oversight» (F-12) — видимость по `hasPerm`/wildcard; для остальных — не рендерится.

## 6. Q5 — Оформление

- **Селектор**: `select.chat-select` (минимальная ширина 220px, максимум 320px, `max-width:100%`; в шапке flex-item); превью-название: `title`; подсветка активного: бейдж-чип рядом (стилизация тёмной темы, существующие цвета/палитры).
- **Аккордеон**: нативный `<details>/<summary>` (без новых зависимостей; RUNTIME: не тянуть фреймворки), маркер `▶` ротация CSS, `padding` компактный, тёмная тема — существующие токены (`--bg-*`/`bg-[#...]`-классы Tailwind-смесь — использовать текущие классы из index.html, без новых).
- Анимации — `transition: 0.15s` только на `max-height` не нужно: `<details>` нативное.

## 7. Q6 — Инвентарь для @Builder

- TABS-маппинг — §2; `canViewTab` (:369) — добавить ветку для `oversight`/`modules_feats`; `hasPerm` — не менять.
- `tabs`-объект: `tabs` ✕ `menu`-тег добавляется к каждому TABS-объекту.
- REGISTRY-ключи для progressive-разметки: полный список — по правилу §4.1; явно `basic` для `prompts.*`.
- Тесты: `test_frontend_tab_mapping.py` — маркеры: `menu:'home'`|`menu:'modules'`…5 значений, `MENU_ORDER`, `active_chat_id`, `X-Chat-Id` в `api()`, `<details class="advanced">`, `adminbot.expand`, read-only-gate `canViewTab` (user → только status|info).
- Регресс-аудит F-8-фиксов (маркеры `pre code`, flex-шапка, «авто»-чип) — сохранить.

## 8. Тест-инварианты

1. Старый контракт GET /api/config — без изменений снаружи (плюс новый ключ `progressive_level` у item).
2. Меню рендерится: 5 чипов с правильными id; статус/инфо — всегда; access-секция только при глобальных правах/секции access; user видит 1 секцию.
3. Селектор: поведение NULL (глобальное) сохранено; X-Chat-Id добавлен только при активном чате.
4. Аккордеон: загрузка по умолчанию «Расширенные» закрыт; `localStorage`-персист.
5. Hidden-параметры не рендерятся и в «Расширенных».
6. Полный pytest → 0 failed; `git diff --check`; ручная сверка десктоп/380px.

## 9. Риски для @Builder

1. `modules_feats` — новая вкладка: не попасть в конфиг-TABS-контракт (`TAB_RULES` в param_catalog не трогается; тип не `config`); `group_tab()`-аудит не должен ломаться.
2. `X-Chat-Id` добавление к ALL api() — ресурсы `/api/avatar/...`, `/api/health`, `chat_lore` — серверные руты с другими департаментами: заголовок игнорируется сервером, но для chat_lore-рутов его ИСПОЛЬЗОВАНИЕ прав имеет смысл — проверить, что `can_access_chat` не ломается.
3. `progressive_level` на ключ без spec (мусорные ключи) — дефолт basic (не падать).
