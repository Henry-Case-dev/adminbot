# Задачи: tma-ia-progressive-disclosure

Раунд 10 (07.09.2026), **часть 2.2 + 3.1 (уровень навигации) эпика** «Multi-chat scaling» (research `plans/docs/multi-chat-scaling-research.md`, вариант A). Нумерация продолжает T-902 → **T-903…T-913**; база HEAD fac1b9f. Скоуп — **информационная архитектура TMA + прогрессивное раскрытие**: (3.1-нав) селектор чатов в шапке (список по `chat_admins`/правам) + новая структура меню «Главная/Статус, Чат-Профиль, Модули и Фичи, Настройки AI, Доступы и Роли»; (2.2) каждая настройка-страница делится на «Базовые» (Тон, промпты, модули) и «Расширенные» (RAG, K-параметры, vector density, системные таймауты, лимиты контекста) в Collapse/accordion; read-only роль «User» видит только «Статус»/«Как это работает». Меню-строительство на базе существующего TABS-аррэя (`web/app.js` :18-47), НЕ ломать память/API. Spec.md при планировании НЕ создаётся; A1 (Q1–Q6) закрывает @Architect правкой в разделе A. Связанные части: `multi-chat-rbac-byok` (T-843…T-869) — X-Chat-Id/доступы, `global-oversight-dashboard` (T-915…T-925) — новая вкладка здесь получает точку интеграции, `tma-ui-fixes` (T-870…T-877) — UI-фиксы независимы, `permsoc-module-isolation` (T-878…T-889) — «Функции PERMsoc» карточка в новой структуре.

## Контекст (Step 0; факты проверены @PM при планировании по HEAD fac1b9f)

TABS-массив `web/app.js` :18-47: `{id, icon, label, type…}`; типы: config|access|status|info; известные вкладки: llm_providers, prompts, limits, memory_rag, reactions_triggers, access, chat_lore, status, info. `activeTab` дефолт 'status' (:77), `setTab` (:310), `canViewTab` (:369), `hasPerm`, `api()` (~:264); `web/index.html` — `v-else-if` рендер по activeTab; секреты-маскинг, кастом-вкладок прецедент memory_rag :33-37. Годные переиспользуемые элементы: селектор-чат в «Лор чатов» (T-779: select чатов + превью), `can_access_chat` (web/api/chat_lore.py) + GET /chat_lore/chats. RBAC — часть-1: роль user — read-only. Аудит: tests/test_frontend_tab_mapping.py. RUNTIME: менять только web/app.js + web/index.html + (при необходимости) param_catalog (группы progressive) — дополнительный API не нужен кроме части-1 (X-Chat-Id) и (при необходимости сводных API роутов части-6).

## A. Дизайн (PM-база; Q1–Q6 закрывает @Architect в T-903)

- [ ] **T-903 (A1):** @Architect — протокол дизайна: (Q1) **меню-структура**: маппинг существующего TABS-аррэя в 5 разделов (PM-база: Главная [status+info-объединённый «Статус» — только для read-only], Чат-Профиль [chat_lore + relations + gates-ключи + локальные параметры], Модули и Фичи [reactions_triggers + permsoc + gates], Настройки AI [llm_providers + prompts + limits + memory_rag + конфиг], Доступы и Роли [access + админы + param_permissions-роли]) — как хранится: TABS-объект каждому `{menu: <секция из 5>}`-тег vs новый MENU-массив ссылок (PM-база: лёгкий тег `menu` у существующих TABS + порядок меню-секций — минимальный дифф); (Q2) **селектор чатов**: источник данных (part-1 `GET /api/access/chats`; фолбэк: `GET /chat_lore/chats`), активный чат принцип: localStorage + фолбэк первый доступный; при одно-чатовой панели (глоб. admin без chat-надобности — все чаты) — скрыть/«Все чаты» (PM-база: скрыть селектор для глобального при 1 чате); (Q3) **раскрытие**: разделяющие-классы параметров — REGISTRY `progressive_level: basic|advanced|null` (PM-база: новые поля в param_catalog, определённые по списку: Базовые — tone/prompts/модули/флаги реакций/персоны; Расширенные — memory-параметры (RAG: top_k, vector density, min relevance), K-параметры, system timeouts, context length (summary_length, context budget), rest — null → «Базовые» по умолчанию? Q: не ломать generic-рендер (config-tabs рендер по группам)); какое значение по умолчанию раскрытия (расширенные закрыты при рендере, expand-состояние per tab в localStorage); кто видит расширенные (по part-1 min-роли: скрытые удаляются ПОЛНОСТЬЮ, не схлопнуты); (Q4) **«Главная»**: для role user — только статус+инфо; состав «Главная» для глобального админа (статус + Oversight-дашборд, фича global-oversight-dashboard); правило: info-вкладка «Как это работает» — пункт «Главная/Статус» (рендер двух субсекций: status + info)?; (Q5) **оформление**: вид селектора (компактный select с превью-названием чата — прецедент chat_lore-селектора: В tma-ui-fixes T-873 имя чата), вид аккордеона (нативный `<details>` по стилю — без новых зависимостей — по RUNTIME: не тянуть фреймворк; expand-кнопки), анимационные вещи — минимальные, тема учитывать (dark); (Q6) **инвентарь**: полный список TABS→меню-маппинга, всех config-вкладок/секций, REGISTRY-ключей для progressive-разметки, тесты-аудит (test_frontend_tab_mapping.py — что менять), части-1 сильные связи (X-Chat-Id — закончена в part-1; здесь — ТОЛЬКО передача активного чата в конфиг-запросы). Правки задач — пометками `(изменено @Architect T-903)`.

## B. Config-слой: progressive-разметка

- [x] **T-904 (B1):** `services/param_catalog.py` — прогрессивные группы: для каждой категории/record — поле `progressive_level` ('basic'|'advanced'|'null') (по списку Q3: минимальные маркеры: думать о: memory/memory_rag/limits.*: advanced; prompt-раздел-Базовые; limits: разделить: тональность/cost/timeout: basic, context length/budget — advanced; «Расширенные» по умолчанию: RAG-раздел/память-раздел — advanced (Q3); поля: заскладывать группами-заголовками `group: 'Настройки AI' / 'Мощь RAG'`); без изменения порядка/имен записей; пользователи разметки: ничего не ломают (в существующих конфиг-рендерах прогрессивность игнорируется пока T-907). (services/param_catalog.py)

## C. Навигация и селектор

- [x] **T-905 (C1):** меню-структура: `web/app.js`: TABS-объекты += `menu`-тег (5 секций — Q1) + `MENU_ORDER`; сохранённый активный раздел; `canViewTab` учитывает menu; запрещённые для role user — скрыть; `hasPerm`-логика на месте; index.html: nav-структура меню (чипы-панель — см. Q5) + сохранение предыдущих вкладок; прецедент: маппинг существующих вкладок — их имена/иконки сохранить (kлюч-бейдж). (web/app.js, web/index.html)
- [x] **T-906 (C2):** селектор чатов в шапке: элемент (select + превью-имя), источник — part-1 `GET /api/access/chats` (фолбэк `/chat_lore/chats` — Q2); `setActiveChat`→ app-событие (перерисовать конфиг-вкладки/разделы); localStorage; пропуск: глобальный admin — «Все чаты»? no: q2: при отсутствии выбора — глобальные настройки (старое поведение); при выбор-чате — отправка X-Chat-Id (от part-1); блок индикации: название чата vs «Глобальные» (PM-база: «Весь бот/чат") с фолбэком «без имени» (T-873-прецедент). (web/app.js, web/index.html)
- [x] **T-907 (C3):** роль-просмотр (user): Главная (Статус + «Как это работает» — инфо-контент без правок); блокировка настроек-вкладок (canViewTab: user → только status|info); read-only-бейдж-тост «вы в режиме просмотра»; API-депсь: конфиг-запросы без X-Chat-Id — глобальные ключи не видны (part-1). (web/app.js, web/index.html)

## D. Прогрессивное раскрытие

- [x] **T-908 (D1):** компонент-секции: в config-вкладках (prompts, limits, memory_rag, reactions_triggers — общие группы): раздел «Базовые» (progressive_level basic|null) + скрытый блок «Расширенные настройки» (acсordion `<details>`) — всё в стиле (прецедент тёмной темы, но: NOT redraw — компакт-навигация по группам); только для параметров вкладки с `progressive_level='advanced'`; расширенные по умолчанию схлопнуты, состояние localStorage `adminbot.expand:<tab>`; generic-рендер (:338-440) переделан ЧАСТИЧНО: передать progressive_level (из REGISTRY/param_catalog API — часть-6: config-сводки; Q3: источник разметки: API GET /api/config-разметка? Registry-апи даёт списки — решение A1); скрытые по part-1 min-роли — НЕ РЕНДЕРИТЬ (фильтр до раскрытия). (web/index.html, web/app.js)

## E. Тесты/аудит

- [x] **T-909 (E1):** фронт-аудит (test_frontend_tab_mapping.py): menu-теги присутствуют (5 значений), пункт селектора и localStorage-ключ, `<details>`-ветки и данные-ключи, read-only-гейт (user → только 2 вкладки: маркеры canViewTab), обновить существующие ассерты (создание новых пунктов меню); проверка отсутствия регрессии ранее сделанных фиксов (tma-ui-fixes — маркеры «pre code», flex-шапка, «авто»-чип). (tests/, web/)
- [x] **T-910 (E2):** полный pytest → 0 failed; `git diff --check`; ручная сверка TMA на десктоп/мобайл (рендер меню/селектора, аккордеон, скролл) — отчёт в H. (tests/)

## F. Деплой/README/отчёт

- [ ] **T-911 (F1):** README-доки: раздел «TMA: навигация и прогрессивное раскрытие» (5 разделов меню, селектор чатов, «Базовые/Расширенные», read-only роль user, ключевые изменения; заметка: структура вкладок соответствует RBAC части-1; деплой-шаги — общий деплой раунда в H2 части-1 (T-864); при необходимости — отдельный мини-деплой: решение @DevOps). Полный pytest → 0 failed; grep-проверка секретов; `git diff --check` чист. (README.md) *(@DevOps финальный шаг — вне архива)*
- [ ] **T-912 (F2):** @DevOps: деплой (общий/отдельный — решение в T-911): pytest0 → коммит (код+тесты+plans/backlog.md+plans/features/tma-ia-progressive-disclosure/) → пуш → сервер pull → мягкий рестарт; live-верификация TMA: 5 разделов меню, селектор чатов (данные part-1), аккордеон «Расширенные» (раскрыть/свернуть, персист), read-only пользователь (403-гейты/скрытие), старые вкладки работают (все 8+1: см. маппинг), секреты по-прежнему маскируются. Результат — в F3. (прод) *(@DevOps финальный шаг — вне архива)*
- [ ] **T-913 (F3):** отчёт юзеру + закрытие: аппрув @Reviewer (полный прогон/регресс-обзор); архивация фичи: перенос в plans/archive/tma-ia-progressive-disclosure/; правка plans/backlog.md (статус: выполнено+архив); ARCHITECTURE.md — раздел (решает @Architect, практика раундов 7–9); memory-sync docs-коммит. Отчёт: где что лежит (меню/селектор/аккордеон), пример-скрин/путь, что НЕ тронуто (API/память/воркеры). (plans/, README.md) *(@DevOps/@Memory финальный шаг — вне архива)*
