# Архив — S5 `summary-l2-writer-formatter-round1026` (Эпик 2 «Summary Hybrid Pipeline», раунд 10.26)

- **Статус:** ✅ **DONE / VERIFIED (автономный контур)** — @Reviewer **Approved** (T-3337, итер.2; C0/H0, блокирующих Medium 0; `review.md`), @Scanner **C0/H0/блокирующих Medium 0/L4/I2** (T-3338, итер.2; `plans/reports/round1026_s5_scanner_audit.md`). Итерация 2 — после rework **T-3339**: закрыты **B-R1026S5-1** (фикстура F8 — восстановлена из blob `e3ea608`, 3+/2−, 0 mojibake) и **S-R1026S5-1** (ON-guard перенесён после S1/S2 — L1 получает отфильтрованный/восстановленный вход + `focus`). Архивация — Шаг 8 @PM (T-3341, 23.09.2026). **Автономная приёмка S5 пройдена; live-приёмка Эпика 1 — отдельно.**
- **Deploy:** ⏳ **после T-3342 @DevOps** — deploy = **ДА** (ADR-1026-7 D7; NOT_APPLICABLE отклонён), bump `APP_VERSION` **2.58.23 → 2.58.24** + `README.md` + cache-bust; `/api/health` 200, `database is locked`=0. Прод пока на 2.58.23.
- **ON-путь — GATED:** флаг **`SUMMARY_HYBRID_L2_ENABLED` default OFF** (env-only) + per-chat `_chat_limit`; OFF-путь **байт-в-байт** (`_generate_two_call`), ON — ровно 2 LLM-вызова (`await_count==2` оба пути). **Включение ON — только после live-приёмки Эпика 1** (гейт S6/S10 + ADR-1025-24 D4). В S5 ON проверен на моках.
- **Live:** ⏳ **PENDING OWNER VERIFICATION** — реальный Telegram WebView/прод (ON не активен).
- **Числа (факт, итер.2):** pytest `.venv` **8807/0** (baseline S4 8737, **+70**), JS **43/43** (30 passed); каталог **469/426/444** (GROUPS 100 / `_TAB_BY_GROUP` 98 / TAB_RULES 21) — **F8 переиздан** (ADR-1026-2: REGISTRY 468→469, categorized 443→444, delta 57→58); **Δ DDL=0**; `APP_VERSION` **2.58.24**; 0 правок `web/**`/`generate_image`/обложки/XML.
- **Follow-up → S6** (`summary-publish-integration`): **L-R1026S5-3** (+1 `_chat_limit` до OFF-ветки), **L-R1026S5-4** (§106-классы `SUMMARY_GENERATION_FAILED`/`COVER_GENERATION_FAILED`/`RICH_MESSAGE_SEND_FAILED`/`TEXT_FALLBACK_FAILED`), **L-R1026S5-5** (sanitize после escape), **L-R1026S5-6** (`'[^']*'` склеивает апострофы), **S-R1026S5-7** (ON пропускает `memorize_facts` — spec-вопрос S6). Все — Low/Info, не блокеры.
- **Откат:** `git fetch --tags origin; git reset --hard pre-round1026-s5` (annotated → **`e3ea608`**); hard — `git revert`; soft — `flags.summary_hybrid_l2_enabled=false`; канон-откат — `rollback_prompt_canons` (`PREV_SUMMARY_L2_WRITER_R1026`). Бэкапы/теги/`stash@{0}` не удалять (R18).
- **Оставшиеся шаги:** **T-3342** (deploy @DevOps) → **T-3343** (метрики `10.26-S5` @Memory) → **T-3344** (handoff @Orchestrator → `S5 → [S6 ∥ S9] → S7 → S8 → S10`; **S6 — BLOCKED (GATED D4), S9 — NEXT**).
- **Артефакты:** `spec.md` (SC-01…SC-14), `adr-1026-7-l2-writer-article-formatter.md` (D1–D7, Accepted), `tasks.md` (T-3308…T-3344, блоки 0/A–H), `evidence.md`, `review.md`. Содержимое `tasks.md` сохранено как в рабочей версии; решения T-3337/T-3338 зафиксированы фактически.
