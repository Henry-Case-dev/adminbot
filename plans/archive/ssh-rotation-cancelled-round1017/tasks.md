# Фича F4 — `ssh-rotation-cancelled-round1017` (ОТМЕНА ротации SSH: только документы, без кода)

> **Статус: ✅ COMPLETED** (14.09.2026; docs-only, @Reviewer **APPROVED**, итерация 2). T-1693/T-1694 выполнены; гейты T-1692 (spec) / T-1695 (@Reviewer+@PM) закрыты; архивировано (Step 8 @PM). **Ротация — CANCELLED** (закрыта отменой, не выполнением; не открытый гейт).
> **Spec:** [`spec.md`](spec.md) — ✅ создан @Architect (Step 2, docs-only). **ADR:** не требуется.
> **Раунд:** 10.17. **Нумерация:** T-1692…T-1695 (продолжает T-1691).
> **Тип:** docs (планирование/README). **Приоритет:** **P0 (doc)** — снять неверную формулировку/дедлайн ротации.
> **Зависимости:** связана с 10.16 F5 `security-rotation-finalize-round1016` (**ОТМЕНЯЕТСЯ**). **Конфликт файлов:** `README*`, `plans/archive/security-rotation-finalize-round1016/*`, `plans/backlog.md` (запись об отмене). **Кода — ноль.**
> **Эпик:** `Epic: UPD3 багфиксы round1017` (@Memory, Step 0).
> **ТЗ:** `plans/current_task.md`, **UPD3** (строка 159): «Секреты в этом файле это норма, данная дока не попадает в репозиторий, ротация не нужна, **отмени эту задачу**.»
> **Baseline:** HEAD `772f192`; pytest **5936 passed / 0 failed**; каталог **435/406/411/90/88/19** (Δ=0).

## 0. Цель

**Отменить** задачу ротации SSH-секрета (10.16 F5 `security-rotation-finalize-round1016`). Решение владельца: секреты в `plans/current_task.md` — норма; файл **не попадает в репозиторий** (`.gitignore:70`, untracked; в истории утечки нет — скан 10.16 чист). Ротация **не нужна**.

**Требуется:** снять/пометить задачу как **ОТМЕНЁННУЮ**; **README не должен overclaim'ить** (не сообщать о выполнении/необходимости ротации как о факте); никакого кода.

## 1. Доказательства (`file:line` на HEAD `772f192`)

- `plans/current_task.md:159` (UPD3) — прямое указание владельца «ротация не нужна, отмени эту задачу».
- `plans/backlog.md:43-49` (блок 10.16) — формулировки про ротацию/SSH-ключ и «парольный вход сохранён намеренно».
- `plans/archive/security-rotation-finalize-round1016/` — `spec.md`, `tasks.md`, `ssh-rotation-checklist.md` (артефакты отменяемой фичи).
- `.gitignore:70` — `current_task.md` вне git (untracked; `plans/project.md` §«Секреты (R17)»).
- `plans/reports/round10.16_scanner_audit.md` — скан на секреты чист (в истории утечки нет).

## 2. Требования (UPD3 §4, строка 159)

- [x] Задача ротации SSH помечена **ОТМЕНЁННОЙ** (в backlog-записи 10.16 и примечании к архиву F5) с отсылкой к UPD3.
- [x] README/дока не утверждают, что ротация выполнена/необходима; формулировки приведены к «по решению владельца ротация не требуется».
- [x] Подтверждено: `plans/current_task.md` не в git; `.env`/секреты не тронуты; новых секретов в репо нет.
- [x] **Кода — ноль** (никаких правок `.py`/`.js`/конфигов ради этой фичи).
- [x] **Ревью-итер.1 (M1):** надзорный overclaim в `plans/ARCHITECTURE.md` (§25 строки 326/328, §37 строки 616/628) снят — ротация/`T-1661`/`T-1664` помечены `⚠️ CANCELLED (UPD3 §4, round1017)` и убраны из списка **открытых** @DevOps-гейтов (закрыты отменой, не выполнением).

## 3. Constraints (инварианты раунда)

- **R17:** секреты не коммитить; в README/доках — только плейсхолдеры/`{configured,last4}`; реальные значения не приводить.
- `media/`/`.env` **не трогать**; порядок роутеров `bot.py` не менять; каталог **Δ=0**; SQL/DDL не требуется.
- Не удалять артефакты 10.16 (историю сохранять); отмена — **пометкой/примечанием**, не переписыванием истории.
- **Ревью-гейты:** `git diff --check`; отсутствие диффов кода; русские conventional commits (docs).

## 4. Зависимости / порядок

- **Вверх:** 10.16 F5 (отменяется). **Вниз:** релизный docs-коммит 10.17.
- **Порядок:** независима; можно выполнять последней (не влияет на код).

## 5. Definition of Done

- [x] В backlog/архиве явно указано: ротация **ОТМЕНЕНА** (решение владельца, UPD3), без overclaim.
- [x] README не содержит утверждений о выполненной ротации/необходимости; тон/факты согласованы.
- [x] Подтверждено отсутствие `current_task.md` в git и отсутствие утечек.
- [x] `git diff` содержит **только** docs-файлы (0 изменений кода).

## 6. Чек-лист задач

- [ ] **T-1692 (@Architect, гейт):** минимальный spec.md — что именно отменяется, формулировка пометки, границы «docs-only».
- [x] **T-1693 (@PM/@Builder):** внести запись об **ОТМЕНЕ** ротации в `plans/backlog.md` (10.16-блок) со ссылкой на UPD3; пометить отменённой `plans/archive/security-rotation-finalize-round1016/` (примечание в `tasks.md`, без удаления).
- [x] **T-1694 (@Builder):** вычистить overclaim в README (формулировки о ротации/SSH), сохранив факты и ироничный тон; подтвердить `.gitignore`/отсутствие секретов.
- [ ] **T-1695 (@Reviewer + @PM, гейт):** подтвердить «кода ноль», отсутствие overclaim и утечек; закрыть фичу.

## 7. Открытые вопросы (@Architect → владелец)

- Достаточно ли пометки в backlog, или нужно отдельное примечание-README о политике (секреты в untracked-доке — норма)?
- Нужно ли явно указать в README, что парольный SSH-вход сохранён намеренно (status quo 10.16), или убрать тему ротации вовсе?
- Требуется ли фиксировать отмену в KG (@Memory Step 10) как отдельное решение?

## 8. Feature flag / progressive delivery

- **Не применимо:** docs-only фича, кода нет; rollback — `git revert` docs-коммита.
