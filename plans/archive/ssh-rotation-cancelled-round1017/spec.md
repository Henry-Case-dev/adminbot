# Спека F4 — `ssh-rotation-cancelled-round1017` (ОТМЕНА ротации SSH: только документы, без кода)

> **Статус:** ✅ COMPLETED (14.09.2026; docs-only, @Reviewer **APPROVED**, итерация 2). Ротация SSH — CANCELLED (закрыта отменой, не выполнением).
> **Раунд:** 10.17. **Тип:** docs-only. **Приоритет:** P0 (doc).
> **T-ID:** T-1692…T-1695. **ADR:** не требуется (docs-only; решений уровня архитектуры нет).
> **Baseline:** HEAD `772f192`; pytest 5936/0; каталог 435/406/411/90/88/19 (Δ=0).
> **ТЗ:** `plans/current_task.md` UPD3 (стр. 159): «Секреты в этом файле это норма, данная дока не попадает в репозиторий, ротация не нужна, отмени эту задачу».

## 0. Цель

**Отменить** задачу ротации SSH-секрета (10.16 F5 `security-rotation-finalize-round1016`). Решение владельца: секреты в `plans/current_task.md` — норма; файл **не в репозитории** (`.gitignore:70`, untracked; в истории утечки нет — скан 10.16 чист). Ротация **не нужна**. **Кода — ноль.**

## 1. Scope

**In scope (только docs):**
- Явная пометка **CANCELLED/ОТМЕНЕНА** в `plans/backlog.md` (блок 10.16) и в `plans/archive/security-rotation-finalize-round1016/tasks.md` — без удаления артефактов.
- Устранение overclaim в `README.md` (строки 74 и 387): убрать «ротация выполняется/нужна», «считать вход отозванным можно только после отчёта».
- Подтверждение: `plans/current_task.md` не в git; `.env`/секреты не тронуты; новых секретов нет.

**Out of scope:**
- Любые правки `.py`/`.js`/конфигов/DNS/sshd (ротация как процедура — не выполняется).
- Удаление/переписывание артефактов 10.16 (историю сохраняем).
- Реальные значения секретов в документах (R17: только плейсхолдеры/`{configured,last4}`).

## 2. Точки изменения (`file:line` на `772f192`)

| Файл | Строки | Что | Статус |
|---|---|---|---|
| `plans/backlog.md` | 5, 95-97 | пометка об **ОТМЕНЕ** (UPD3 §4), ссылка на фичу 10.17 | **частично внесена @PM** — верифицировать/завершить |
| `plans/backlog.md` | 50-78 | блок 10.16: снять формулировки «открытые гейты ротации» | уточнить |
| `plans/archive/security-rotation-finalize-round1016/tasks.md` | header | примечание: фича **CANCELLED** (UPD3 §4), гейты T-1661/T-1664 **не являются открытыми действиями** | внести |
| `README.md` | 74 | переписать SSH-абзац: ротация **не требуется** по решению владельца; секреты — untracked-файл | внести |
| `README.md` | 387 | переписать: убрать «процедура для @DevOps в ssh-rotation-checklist.md» как открытое действие | внести |
| `plans/current_task.md` | — | **не в git**, не трогать | evidence |

## 3. Нормативные формулировки

- **Допустимо:** «По решению владельца (UPD3, раунд 10.17) ротация SSH-пароля/ключа **не требуется**: секреты в `plans/current_task.md` — untracked-файл вне репозитория (`.gitignore:70`); в git-истории утечки нет (скан 10.16 чист).»
- **Запрещено (overclaim):** «ротация выполнена», «ротация ожидается», «считать вход отозванным только после отчёта @DevOps», «гейты T-1661/T-1664 ещё открыты» как текущее действие.
- Артефакты 10.16 помечаются, но **не удаляются**: архив остаётся историческим свидетельством.

## 4. Алгоритм (docs-only)

```text
1. backlog.md (блок 10.16): подтвердить/завершить пометку «⚠️ ОТМЕНА (UPD3 §4)» + ссылку на F4 10.17.
2. archive/security-rotation-finalize-round1016/tasks.md: добавить header-примечание
   «Статус: CANCELLED (UPD3 §4, round1017). Гейты T-1661/T-1664 закрыты ОТМЕНОЙ, а не выполнением.»
3. README.md:74: заменить абзац про ротацию на нормативную формулировку (§3).
4. README.md:387: убрать пункт про ротацию как процедуру для @DevOps; оставить факт «в git утечки нет».
5. Верификация: `git ls-files --error-unmatch plans/current_task.md` → пусто (не tracked);
   `git log --all -- plans/current_task.md` → пусто; `git diff --stat` → только docs-файлы.
```

## 5. Тест-план / верификация

| # | Проверка | Ожидание |
|---|---|---|
| 1 | `git ls-files` и `git log --all -- plans/current_task.md` | файл не tracked, в истории нет |
| 2 | `git diff --stat` фичи | только `*.md` (backlog/archive/README), 0 кода |
| 3 | grep README/backlog на overclaim | нет «ротация выполнена/нужна» как факта; есть «не требуется» |
| 4 | R17-скан docs | нет паролей/ключей, только плейсхолдеры/`configured ••••1234` |
| 5 | `git diff --check` | чисто |

## 6. Риски

| Риск | Мера |
|---|---|
| «Отмена» прочитана как «удалить историю 10.16» | явное правило: только пометка, без удаления |
| README снова заявит о выполненной ротации | нормативные формулировки §3 + grep-гейт |
| Спутать с R17-долгом (пароль в рабочей копии untracked) | зафиксировать: рабочий файл владельца — норма, не долг репо |

## 7. Критерии приёмки (DoD)

- [ ] В backlog/архиве явно: ротация **ОТМЕНЕНА** (решение владельца, UPD3), без overclaim.
- [ ] README не утверждает выполненную/необходимую ротацию; тон/факты согласованы.
- [ ] Подтверждено: `current_task.md` не в git; утечек нет; `.env`/секреты не тронуты.
- [ ] `git diff` содержит **только** docs-файлы (0 изменений кода).
- [ ] `git diff --check`; русские conventional commits (docs).

## 8. Feature flag / progressive delivery

Не применимо: docs-only, кода нет; rollback — `git revert` docs-коммита.

## 9. Handoff

`@Orchestrator` — спецификация F4 готова. Реализация — T-1693/T-1694 (@PM/@Builder), гейт T-1695 (@Reviewer/@PM).
