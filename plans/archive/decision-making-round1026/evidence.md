# A7 `decision-making-round1026` — Evidence (Builder, T-3651…T-3662)

- **Epic-ID:** Эпик 3 «Agentic Intelligence», Wave 5 — A7
- **Risk:** **R3** (spec §13 / ADR-1026-20 D12)
- **Baseline anchor:** `e8646af` (HEAD == `origin/master`) + UNCOMMITTED epic-release
  tree A2/A3/A5/A6/A4 — untouched/reverted/committed; A7 builds on top.
- **Feature dir:** `plans/features/decision-making-round1026/`
- **Spec:** `spec.md` (35 REQ → 38 SC) · **ADR:** `adr-1026-20-…md` (D1–D12, binding)
- **Release policy:** EPIC_ONLY — deploy **DEFERRED_TO_EPIC**; no commit/tag/bump by Builder.
- **APP_VERSION:** 2.58.30 (unchanged, no bump).

## Changed files (A7 contribution only)

Source:
- `services/direct_chat_service.py` — A7 core:
  - constants `REACTION_MOAI:477`, `REASON_*` + `REASON_CODES:501`, message classes;
  - `DecisionContext:544`, `DecisionToggles:558`, `CoordinatorDecision` extended
    (`target_message_id`/`reaction`/`reason_code`/`needs_tools` + `__post_init__`
    validation):591;
  - `decision_making_enabled():635` (env-only kill-switch);
  - `_coordinator_reason:720`, `build_coordinator_decision:736` (Phase T reason/
    needs_tools/target), `_decision_message_class:772`, `_decision_pre_action:801`
    (Phase P §3.3), `_log_coordinator_decision:872` (A7 fields only when ON),
    `_log_decision_short_circuit:909`;
  - `_decision_addressed:1042`, `_decision_context:1065` (§47: image/article/
    private/expects_tool_result — T-3663 F-2), `_decision_toggles:1112`
    (§48) — all fail-open;
  - `handle()` **Phase P** insertion:1258–1286 (silent/react short-circuit; reply
    falls through; OFF → reason `disabled`, T-3663 F-5), Phase T integration:1424–1435.
- `config/settings.py` — `DIRECT_DECISION_MAKING_ENABLED` ClassVar:560 (default ON);
  3 per-chat fields `CHAT_DECISION_IGNORE_TRIVIAL/REACTIONS/IMAGE_REACTIONS:1407–1412`
  (default True).
- `services/param_catalog.py` — `GroupSpec("flags_decision_making", …):359`;
  3 `_FLAGS`:856–865; group added to `TAB_MOD_DIRECT`:2168–2170.
- `web/app.js` — `mod_direct` sources +`flags_decision_making`:97–98.

F8 reissue (ADR-1026-2; Δ каталога +3/+1):
- `plans/docs/param-registry-round1025.tsv`, `…meta.md`, `plans/docs/screen-map-round1025.md`
  — regenerated by `tools/gen_param_registry_round1025.py`; `--check` OK.
- `tests/fixtures/round1025/catalog_baseline.json` — counts/keys/group_tab.
- `tests/fixtures/round1025/f8_baseline.json` — counts/delta/sha256(param_catalog)/note.

Tests/artifacts:
- `tests/test_decision_making_round1026.py` (**NEW**, **89** scenarios; 68 в
  базовой сборке A7 + 21 в rework T-3663: F-1/F-2/F-3/F-4/F-5).
- Count re-pins across 44 existing test files (ADR-1026-2): 470→473, 101→102,
  99→100, 427→430, 445→448, flag-cat 69→72; `test_frontend_tab_mapping` mod_direct
  composition +flags_decision_making.
- `plans/features/decision-making-round1026/threat-failure-analysis.md` (**NEW**, R3).

## T-3651…T-3662 — status

- [x] **T-3651 контракт/wire-`action`** — `CoordinatorDecision` аддитивно расширен
  (`action`/`style` разведены; `style="silent"` нормализуется в `""`; `reaction`
  только при `react`; `reason_code` из закрытого enum; `needs_tools`/`tool_calls`/
  `target_message_id`). Служебный JSON не в чат; Stage-1/Stage-2 JSON не изменены.
- [x] **T-3652 вычисление без 3-го LLM-вызова** — Phase P программная (0 LLM);
  Phase T расширяет `_coordinator_choose_action` причиной; на tool-ходе
  `llm.generate.await_count == 2` (`TestHandleOrder::test_await_count_two_on_tool_path`);
  на silent/react — 0 текстовых вызовов (`test_await_count_zero_on_silent_path`).
- [x] **T-3653 политика §42–§45** — детерминированный приоритет (`_decision_pre_action`);
  реальные задачи (explicit/question) всегда reply; «Почему?» → reply; эмоции → react;
  подтверждения/не-адресовано → silent; без `random`.
- [x] **T-3654 порядок §46** — silent → no Verbalizer/no message; react → `react_moai`
  (reuse) на trigger, no Verbalizer; reply → существующий путь; tool → tool_loop →
  итог (недоступность → `tool_unavailable`, не молчание). Safety-net пустого reply
  не тронут.
- [x] **T-3655 контекст §47** — `_decision_context` из `reply_to_message` +
  маркеров (без нового хранилища); `reply_to_is_image` (photo),
  `reply_to_is_article` (web_page), `is_private`, `expects_tool_result`
  (статья/не-фото медиа в ответе на бота) — все читаются политикой (T-3663 F-2);
  «АХАХА» на своё изображение → react; «Почему?»/«шесть пальцев?» → reply.
- [x] **T-3656 настройки §48 + каталог** — 3 параметра + 1 группа в `mod_direct`;
  global/local через `get_chat_param`; F8 переиздан; Δ = ровно +3/+1.
- [x] **T-3657 `reason_code` + R17** — закрытый enum 15 кодов; логи только
  enum/id/числа; текст сообщений не логируется.
- [x] **T-3658 тесты** — 89 сценариев (контракт/политика/порядок/контекст/настройки/
  reason/R17/adversarial; вкл. rework T-3663 F-1…F-5).
- [x] **T-3659 kill-switch + OFF-паритет + регресс** — `DIRECT_DECISION_MAKING_ENABLED`
  env-only, default ON; OFF → политика не строится, A1-baseline, `reason=disabled`;
  полный pytest 9392/0; JS 47/47; Δ DDL=0; `git diff --check`=0; APP_VERSION 2.58.30.
- [x] **T-3660 diff-аудит границ** — см. ниже.
- [x] **T-3661 R3-артефакт** — `threat-failure-analysis.md` (15 threats,
  threat→mechanism→code→test; code-указатели выправлены на фактические
  `handle()`-ветки в rework T-3663 F-1/F-7).
- [x] **T-3662 §53/adversarial** — молчание не съедает задачу; недоступный инструмент →
  ответ/`tool_unavailable`; инструменты не потеряли работоспособность (полный регресс).

## Verification commands + actual results

- `pytest -q` (full `.venv`) → **9392 passed / 0 failed** (153.5s, rework T-3663).
  Baseline (A7 Step 0) = **9303/0**; **+89** = `tests/test_decision_making_round1026.py`.
  Ревизия T-3663: было 9371/0 (68 A7) → стало 9392/0 (89 A7), **+21**. 0 регрессий.
- `tests/test_decision_making_round1026.py` → **89 passed**.
- `tests/test_direct_chat.py` → **159 passed** (поведение direct-чата сохранено).
- `tests/test_direct_chat.py tests/test_param_catalog.py tests/test_round1025_f8_registry.py`
  → **222 passed** (A7-ревизия T-3663).
- A1/A2/A3/A4/A5/A6 regression set (`test_tool_coordinator_round1026`,
  `test_tool_chains_round1026`, `test_unified_image_request_round1026`,
  `test_memory_lookup_round1026`, `test_image_context_memory_round1026`) → green.
- F8: `python tools/gen_param_registry_round1025.py --check` → **CHECK OK: реестр 473**.
- Catalog: REGISTRY **473** · Settings **430** · categorized **448** · GROUPS **102** ·
  `_TAB_BY_GROUP` **100** · `TAB_RULES` **21** = санкционированная Δ (+3/+1).
- JS: `node tests/js/*.js` → **47/47, 0 failures**.
- SQLite: full suite incl. `test_database.py` (`user_version` = **12**) ✓; Δ DDL = 0.
- `git diff --check` → exit **0** (only LF→CRLF informational warnings).
- `APP_VERSION` → **2.58.30** (unchanged); канон `TOOL_CALLING_TOOLS` → **12**.

## Boundary audit (T-3660, verified)

- **Нет 3-го LLM-вызова:** Phase P — чистые функции (0 LLM); Phase T использует
  существующий `_coordinator_choose_action`; тест
  `TestHandleOrder::test_await_count_two_on_tool_path` (`llm.generate.await_count
  == 2`, Stage-1 + Stage-2 на tool-ходе) + `test_await_count_zero_on_silent_path`
  (0 LLM на silent) + A1 `TestHandleTwoCallPreserved::test_await_count_is_two`
  зелёные (F-1c T-3663).
- **Stage-1/Stage-2 JSON не изменены:** `services/system2_handoff.py` вне diff;
  `tests/test_decision_making_round1026.py::test_stage_json_contracts_unchanged`.
- **A1 — AMEND, не дублирование:** второй координатор/датакласс не введён;
  `CoordinatorDecision` расширен аддитивно, A1-поля сохранены.
- **A2 (цепочки/envelope), A3/A4/A5/A6 (image/context/budget/lookup) не менялись:**
  A7-правки только в `direct_chat_service.py`/`settings.py`/`param_catalog.py`/
  `app.js`; `tool_loop.py`/`tool_router.py`/`image_generation.py`/
  `image_context_memory.py`/`worker_budget.py`/`database.py`/`pg_db.py` A7 не трогал.
- **§41-механика (A8) вне diff:** `set_message_reaction` в `direct_chat_service.py`
  отсутствует (тест `test_no_set_message_reaction_direct_call`); реакция — reuse
  `smartmodule_utils.react_moai`.
- **§49/§51 (A9) вне diff:** событийная система/ExecutionGraph не создавалась.
- **§37:** контекст/маркеры трактуются как данные; логи R17-safe.
- **Канон 12 / Δ DDL=0 / Δ каталога санкционирован:** см. проверки выше.
- **§41/A8:** политика §44 (когда реакция) = A7; механика = A8.

## 15 acceptance invariants — status

1. Действие ≠ стиль → **OK** (`__post_init__`; `test_actions_split_from_style`).
2. `style` никогда не `silent` → **OK** (нормализация; тест).
3. Нет пустого текста Вербализатора → **OK** (silent/react до LLM; safety-net не тронут).
4. Служебный JSON не в чат → **OK** (внутренний объект; Stage-JSON без action).
5. Нет «короткое → silent» → **OK** («Почему?» → reply/question).
6. Реальные задачи не теряются; tool unavailable → ошибка → **OK** (adversarial).
7. Нет случайности → **OK** (детерминизм 25×; `random` отсутствует в функции).
8. Бот не полностью пассивен → **OK** (silent только для тривиальных; задачи → reply).
9. silent/react не запускают Вербализатор → **OK** (spy: 0 LLM/0 send).
10. Учёт контекста §47 → **OK** (`_decision_context`; image+«АХАХА» → react;
    article/не-фото медиа → `expects_tool_result`; ЛС → addressed; все поля
    читаются политикой — T-3663 F-2).
11. Минимум настроек (3) → **OK** (ровно 3 параметра).
12. Reuse global/local → **OK** (`get_chat_param` override→global→default; тесты).
13. Без 3-го LLM-вызова → **OK** (см. boundary).
14. R17-диагностика → **OK** (закрытый enum; лог без текста).
15. Границы + EPIC_ONLY → **OK** (A1 AMEND; A2/A3/A4/A5/A6/A8/A9 вне diff; канон 12;
    deploy DEFERRED_TO_EPIC; @DevOps не вызывается).

## R17 / R18

- **R17:** `[decision] action | chat | action | reason | target | reaction` и
  расширенная `[coordinator] decision` — только enum/id/числа/имена инструментов;
  текст сообщений/имена/ключи/промпты не логируются (`test_logs_r17_safe`).
- **R18:** коммитов/тегов/веток не создавалось; `plans/current_task.md`, машинный
  блок, spec/ADR, backlog/metrics/ARCHITECTURE/MEMORY/durable-аудит не изменялись;
  бэкапы/`stash` целы.

## Epic inputs contributed by this feature (release-candidate)

- Code: `services/direct_chat_service.py` (Phase P/T + контекст/настройки/логи),
  `config/settings.py` (env kill-switch + 3 per-chat поля), `services/param_catalog.py`
  (+3/+1), `web/app.js` (секция UI), F8 artifacts.
- Tests: `tests/test_decision_making_round1026.py` (new, 89) + count re-pins.
- R3 artifact: `threat-failure-analysis.md`.
- Deploy: **DEFERRED_TO_EPIC**; hot rollback `DIRECT_DECISION_MAKING_ENABLED=false`
  (или 3 тумблера OFF); cold `git revert` → `e8646af`; DDL-откат не нужен.

## Review rework T-3663 (Reviewer gate `Needs Fixes` → fixes)

> Дизайн (spec/ADR-1026-20) не менялся — только исправление фактических
> дефектов/доказательной базы. Baseline anchor `e8646af`; A7 — uncommitted.

### F-1 (High) — §46-tool / §43-unavailable end-to-end + `await_count==2`
- **Fix code:** end-to-end `handle()`-тесты: tool-путь — реальный
  `chat_with_tools` + router-спай; degraded — mock `ToolLoopResult`.
  Фактические ветки — `handle():1258–1286` (Phase P), `:1411–1528`
  (degraded/reply/error).
- **Fix docs:** `threat-failure-analysis.md` — code-указатели переведены на
  **runtime-ветки** `handle()`; `_coordinator_reason`/`build_coordinator_decision`
  больше не выдаются за механизм ветвления (только обогащают reason).
- **Tests (new):**
  - `TestHandleOrder::test_tool_path_runs_tools_then_final_action` — реальный
    `chat_with_tools` + router-спай: `router.dispatch` исполнен, `action=tool`,
    итоговое действие (reply) отправлено (`llm.generate` Stage-1+Stage-2 = 2).
  - `TestHandleOrder::test_tool_degraded_sends_reply_not_silence` — degraded/
    failed tool → текстовый ответ, **не** молчание; `reason=tool_unavailable`.
  - `TestHandleOrder::test_await_count_two_on_tool_path` — `llm.generate.await_count == 2`.
  - `TestHandleOrder::test_await_count_zero_on_silent_path` — `await_count == 0`.
- **Результат:** 4/4 passed; `await_count` 2 (tool) / 0 (silent).

### F-2 (High) — §47-поля реально вычисляются и читаются
- **Fix code:**
  - `_decision_context:1065` — вычисляет `reply_to_is_article` (web_page),
    `is_private` (chat.type), `expects_tool_result` (ответ на сообщение бота:
    статья или не-фото медиа — pre-LLM-эвристика §3.5(6));
    `reply_to_is_image` — только `photo` (image-ветка 5/6 отдельна).
  - `_decision_pre_action:801` — ветка 4 читает `expects_tool_result`/
    `reply_to_is_article` (→ reply/`tool_result`); ветка 8 — `is_private`
    (`not addressed and not is_private`).
  - `_decision_addressed:1042` — `is_private` → всегда addressed (ЛС).
- **Tests (new):** `TestDecisionContextFields::{test_article_sets_expects_tool_result,
  test_document_result_sets_expects_tool_result, test_photo_is_image_not_tool_result,
  test_private_marks_addressed, test_group_reply_to_other_not_addressed}`;
  `TestPolicy::{test_expects_tool_result_beats_ack_silence,
  test_article_reply_context_replies, test_private_overrides_not_addressed}`;
  `TestHandleOrder::test_private_chat_reply_to_other_still_replies`.
- **`bot_replied_recently`:** оставлен алиасом `reply_to_bot` (reply-ветка §45) —
  помечено как осознанное ограничение (см. `threat-failure-analysis.md`).

### F-3 (Medium) — короткая вопросительная пунктуация → reply
- **Fix code:** `_decision_message_class:772` — `_QUESTION_PUNCT_RE:529`
  (`«?»/«???»/«?!»` → `question`) до emoji-only; `_ELLIPSIS_ONLY_RE:532`
  (`«...»` → `other`, fail-safe reply, не реакция).
- **Tests:** `TestMessageClass::test_classification["?","???","?!","..."]`;
  `TestPolicy::test_bare_question_punctuation_replies` (`?`, `???`, `?!` →
  `ACTION_REPLY`/`question`).

### F-5 (Medium) — OFF kill-switch → reason `disabled`
- **Fix code:** `handle():1258–1263` — `pre_reason = REASON_DISABLED` при OFF
  (политика не строится, A1-формат лога сохранён байт-в-байт).
- **Test:** `TestHandleOrder::test_off_killswitch_reason_disabled`
  (`action=reply`, `reason_code=disabled`).

### Low (F-4, F-6, F-7, F-8, F-9)
- **F-4 (D-1):** bypass не найден; добавлены probe-тесты
  `TestPolicy::test_image_emoji_off_replies`,
  `test_image_reaction_ignores_generic_reactions_off`.
- **F-6:** proof усилен выделенными `await_count`-тестами (см. F-1), формулировка
  evidence про «A1 `await_count==2`» заменена на фактические A7-тесты.
- **F-7:** `threat-failure-analysis.md` построчно сверен; 15 строк; указатели —
  фактические runtime-ветки; claims воспроизводимы.
- **F-8:** счётчик артефакта исправлен 10 → **15**; evidence синхронизирован.
- **F-9:** `test_forbidden_paths_out_of_diff` (A1) намеренно расширен под
  санкционированные A3/A5-diff (`image_generation.py`, `param_catalog.py`,
  `web/app.js`); повторное «затягивание» сломало бы зелёные A3/A5-тесты, поэтому
  **не выполнялось**. A7-дельта каталога независимо пиннится
  `TestBounds::test_counts_sanctioned` + F8 `--check` (473). Зарегистрировано для
  агрегатного эпик-гейта (Reviewer D-2).

### Числа rework T-3663
- Baseline/current: HEAD **`e8646af`** (== reviewed anchor; коммитов нет).
  Изменён `services/direct_chat_service.py` (`M`); `tests/test_decision_making_round1026.py`
  — untracked, sha256 `ee15c6577efb88f55b6779938c01ed55ac5f181314bec7450e55d191ac383c43`;
  feature-папка — untracked. `git status --porcelain` фиксирует presence.
- A7 suite: **89 passed** (68 → 89, +21).
- Direct-chat + catalog + F8: **222 passed**; A7 + A1/A5 sweep: **211 passed**.
- Full `pytest -q`: **9392 passed / 0 failed** (было 9371/0; +21, 0 регрессий).
- F8 `--check` → **CHECK OK: реестр 473**.
- Catalog canon: REGISTRY **473** · fields **430** · categorized **448** ·
  GROUPS **102** · `_TAB_BY_GROUP` **100** · `TAB_RULES` **21**; канон **12**.
- JS: **47/47 exit 0**; `git diff --check` → exit **0**; `APP_VERSION` **2.58.30**;
  Δ DDL = 0 (`test_no_ddl`).

## Checks not run / notes

- Нет сетевых/LLM/provider-вызовов (unit/adversarial only).
- Owner-gate A3 `PENDING OWNER VERIFICATION` — внешний, A7 не закрывает.
- `bot_replied_recently` выводится из `reply_to_bot` (reply-ветка §45); отдельного
  нового хранилища нет.
