# Spec F7 — `status-layout-reorder-round1014` (Перестановка блоков «Статус»)

> **Статус: ✅ COMPLETED** (Step 2 @Architect, **итерация 2**, 13.09.2026). **Порядок подтверждён владельцем (UPD), изменений нет.**
> **Раунд:** 10.14. **T-ID:** T-1535…T-1540. **ТЗ:** `plans/current_task.md` §7.
> **Зависимости:** нет. **Конфликт файлов:** `web/index.html` делят F3/F4/F6 — F7 вливать последней (после F4), чтобы переносить финальную разметку.

---

## §0. Решения Architect (кратко)

| Вопрос | Решение |
|---|---|
| **F7-Q1** позиция «Доступности ключей» | «Доступность ключей» уходит **ниже** «Мониторинга Интеллекта»: ... → Сервер → **Мониторинг Интеллекта** → **Доступность ключей** → История. |
| **F7-Q2** «Мониторинг Интеллекта» | **Строго сразу под «Сервером»** (до «Доступности ключей»). |
| **F7-Q3** заголовок/иконка Сердцебиения | **Не меняем** (`monitoring` + «Сердцебиение») — перенос только позиции. |
| DOM-зависимости | Проверено: блоки адресуются `ref=`/`id`/`computed` (EKG `heartbeat`, `cognitionGraph` ref), порядковых завязок в `web/app.js` нет. |

---

## §1. Целевой порядок блоков («Статус», `activeTab==='status'`)

**Было:** Сводка(hub-карточка `:2776`) → Бот `:2792` → Сервер `:2817` → Доступность ключей `:2848` → Сердцебиение `:2877-2899` → Мониторинг Интеллекта `:2901-2953` → История доступности ключей `:2955+`.

**Станет:**
1. Сводка (hub-card, `#/oversight`) — **без изменений**
2. **Сердцебиение** (SVG-EKG `:2877-2899`) ← перенос наверх
3. Бот (`:2792`)
4. Сервер (`:2817`)
5. **Мониторинг Интеллекта** (`:2901-2953`, включая третью ленту F4) ← перенос под Сервер
6. Доступность ключей (`:2848-2875`)
7. История доступности ключей (`:2955+`)

Требования ТЗ §7 выполнены: «Сердцебиение» сразу под «Сводкой»; «Мониторинг Интеллекта» под блоком «Сервер».

---

## §2. Изменения (`file:line` на HEAD `2edc65b`)

| Файл | Изменение |
|---|---|
| `web/index.html` | переместить блок Сердцебиения (`:2877-2899`) в позицию 2; переместить блок Мониторинга Интеллекта (`:2901-2953`) после блока Сервер (`:2817-2843`); порядок остальных не менять |
| `web/app.js` | правок **нет** (проверено: `heartbeat` computed `:1160+`, `cognitionGraph` `ref`, `loadCognition`/`loadKeyHistory` — порядко-независимы) |
| каталог | **+0** (чистая перестановка разметки) |
| тесты | новый маркер-тест порядка в `tests/test_webapp_round1014_ui.py`; при необходимости обновить маркеры `tests/test_webapp_round1013_ui.py`/`test_webapp_round1013_f5_ui.py` |

## §3. Конфиг-ключи и дефолты

Нет. F7 не меняет каталог, флаги, `/api/status`.

## §4. Feature flag / progressive delivery

Не требуется (render-only). Rollback = `git revert`.

**UPD-примечание (итерация 2):** панель метрик Личности (F4) добавляется в дашборд «Сводка» (`#/oversight`) и на порядок
блоков страницы «Статус» не влияет; лента «Эволюция характера» (F4) входит в блок «Мониторинг Интеллекта», который F7
переносит целиком.

## §5. Тест-план

1. Маркер-тест порядка: индексы вхождения заголовков в `web/index.html` внутри `activeTab==='status'`:
   `Сводка < Сердцебиение < Бот < Сервер < Мониторинг Интеллекта < Доступность ключей < История`.
2. Ни один блок не потерян/не продублирован (count заголовков == 1).
3. Не сломан F4: третья лента внутри «Мониторинга Интеллекта» присутствует.
4. `node --check web/app.js`, полный pytest 0 failed, `git diff --check`.
5. Live-чеклист владельцу (визуальный порядок на Android).

## §6. Риски

| Риск | Митигация |
|---|---|
| F7 переносит блок до/после F4 — конфликт diff | F7 вливать **после F4**; маркер-тест фиксирует финальный порядок |
| Кто-то полагался на DOM-индекс | grep по `children[`/`querySelectorAll`; проверка показала адресацию по `ref`/`id` |
| Скролл/`grid-column: 1/-1` | все блоки — `col-span-full`; перестановка порядок grid не ломает |

## §7. Критерии приёмки (DoD)

- [ ] «Сердцебиение» стоит сразу под «Сводкой».
- [ ] «Мониторинг Интеллекта» стоит сразу под «Сервером» (перед «Доступностью ключей»).
- [ ] Остальные блоки не потеряны/не продублированы; порядок зафиксирован маркер-тестом.
- [ ] Вёрстка/скролл/responsive сохранены; `prefers-reduced-motion` работает.
- [ ] Полный `pytest` 0 failed; `node --check` clean; `git diff --check` чист.
