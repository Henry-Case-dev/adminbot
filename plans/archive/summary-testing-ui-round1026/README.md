# Архив — S9 `summary-testing-ui-round1026` (Эпик 2 «Summary Hybrid Pipeline», Раунд 10.26; §113 + §112)

- **Статус:** ✅ **DONE / VERIFIED (автономный контур)** — @Reviewer **Approved** (T-3373, **итерация 2**; C0/H0, блокирующих Medium 0; `review.md`) + @Scanner **C0/H0/блокирующих Medium 0/L5/I2** (T-3374, итерация 2; `plans/reports/round1026_s9_scanner_audit.md`). Итерация 2 — после rework **T-3375**, которым закрыты оба блокера: **B-R1026S9-2** (High — краш вкладки «Тестирование» на error-путях) и **B-R1026S9-1** (Medium — §112 «Процент отсева» не рендерился). Архивация — Шаг 8 @PM (**T-3377**, 24.09.2026).
- **Суть:** первый **dry-run** потребитель S1–S5 — вкладка `#/modules/summary/testing` (§113): предпросмотр фильтр→контекст→L1→пакет→L2→Rich/plain, метрики §112, диагностика. **0 публикаций / 0 записей памяти / 0 `generate_image` без отдельного подтверждения**; ON-путь включается только per-run (ровно 2 LLM-вызова), глобальный `SUMMARY_HYBRID_L2_ENABLED` не читается и не меняется. Модули: `services/summary_test_run.py` (new), `web/api/summary_test.py` (new, async job + polling, in-memory store TTL 15 мин/≤20, **Δ DDL=0**), additive `SummaryGenerator.build_test_rows`, UI `web/index.html`/`web/app.js`. `routes.py` вне diff.
- **Deploy:** ⏳ **после T-3378 (@DevOps)** — решение ADR-1026-8 **D8 = ДА**, bump `APP_VERSION` **2.58.24 → 2.58.25** + `README.md` + cache-bust; перед деплоем — минимальные §114-тесты; `/api/health` 200, `database is locked`=0.
- **Live-приёмка:** ⏳ **PENDING OWNER VERIFICATION** (S1–S5 / Эпик 1 / S9; владелец не проверял).
- **Флаг:** `SUMMARY_TEST_UI_ENABLED` **default ON** (env-only ClassVar; hot-OFF — `SUMMARY_TEST_UI_ENABLED=false` → 404 + скрытие вкладки). Не гейт публикации.
- **Числа (факт, итерация 2):** pytest `.venv` **8854/0** (+47 к baseline 8807); JS **44/44**; каталог **469/426/444** (без Δ; F8 не переиздаётся — N/A); **Δ DDL=0**; `APP_VERSION` **2.58.25**; `summary_generator.py` — только additive (55+/0−).
- **Follow-up (OPEN, non-blocking, owned):** **L-R1026S9-2** (5 пред-существующих падений rich-media на `py -3`, не регресс S9), **L-R1026S9-6** (dry-run пишет `_filter_metrics[chat_id]`), **L-R1026S9-7** (`window_hours` клампится вместо 422), **L-R1026S9-8** (`exc_info=True` — к S7), **S-R1026S9-5** (doc-drift `503` в docstring/ADR — к S7), **I-R1026S9-1/-2** (`has_more` — эвристика; Info) → **S7/S8**.
- **Откат:** `git reset --hard pre-round1026-s9` (annotated-тег → `cc6105c`); либо `git revert`. Ветки/теги/`stash@{0}` не удалять (R18).
- **Артефакты фичи:** `spec.md` (SC-01…SC-15), `adr-1026-8-summary-test-run-dry-run-ui.md` (D1–D8, Accepted), `tasks.md` (T-3345…T-3379, блоки 0/A–H), `evidence.md`, `review.md`. Границы: S6 — **GATED** (не вызывается), `run_id`→**S7**, ExecutionGraph→**S8**, §85 — вне S9.
