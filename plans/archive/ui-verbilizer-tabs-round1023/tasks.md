# Задачи: ui-verbilizer-tabs-round1023

> **Раунд 10.23** · Приоритет **P1** · Шаг 1 @PM (+ Step 2 @Architect: `spec.md` + **ADR-1023-8**) · Тип: каталог + UI (`web/**`) + API
> **ТЗ:** `plans/current_task.md`, «Рефакторинг UI и Аналитика (Мини-апп) → 2. Управление Промптами (System 2 & Cliches)». Untracked, секреты не цитировать/не коммитить (R17/R18).
> **Baseline:** HEAD `731a845`; pytest **6962/0**; каталог **439/409/414/92/90/20**; SQLite **v12**; прод `a8a6437`.
> **Зависит от:** F3 (режимы), F4 (кэш клише), F5 (ступень `web/**`).

## Цель
Разделить карточки модулей (Фактчек, Поиск, Прямой чат, Саммари, YouTube, Веб) на **«Синтезатор (Логика)»** и **«Вербализатор (Характер)»**. В секции Вербализатора — UI-селектор режимов (Tabs: `Casual` | `Serious` | `Deep Research`). Добавить блок мониторинга `dynamic_cliche_list` с датой обновления, кнопкой форсированного обновления и ручным редактированием.

## Контекст / что уже есть
- Каталог: `services/param_catalog.py` — `_PROMPTS` (`:369-398`, ключи `prompts.*`), `GROUPS` (`:139`), `TAB_RULES` (`:1909`), `_TAB_BY_GROUP` (`:2024`); **stage-1/2 промптов в каталоге нет**, image-блока нет (добавляет F5).
- UI: `web/app.js` config-табы (prompts/modules/llm_providers), `web/index.html` (generic-рендер, `<details>`/basic/advanced).
- API: `web/api/routes.py` (конфиг/POST), `services/access.py`, `param_permissions`.
- Динамический кэш клише: F4 (API GET/POST force/PUT manual).
- **Инвариант:** структура меню (tabs / `NAV_ITEMS` / карточки модулей) — **не менять** (tma-menu-freeze).

## Задачи
- [x] **T-2167** [@Architect] ADR-1023-8: ключи каталога stage-1/stage-2 по модулям, группы/`TAB_RULES`-мэппинг, UI-селектор режимов, блок `dynamic_cliche_list`, новые API-эндпоинты (если нужны), RBAC/param_permissions, сохранение freeze-меню, откат. Создать `spec.md` + `ADR-1023-8.md`.
- [x] **T-2168** [@Builder] Каталог: новые ключи промптов Синтезатор/Вербализатор по модулям + группы (`prompts_*`) и `TAB_RULES`; Δ REGISTRY/GROUPS/TAB_RULES зафиксировать (осознанное изменение эталона `test_param_catalog`).
- [x] **T-2169** [@Builder] UI: разделение карточек модулей на «Синтезатор (Логика)» / «Вербализатор (Характер)» (`web/app.js`, `web/index.html`, generic-рендер; basic≥1 на вкладку).
- [x] **T-2170** [@Builder] UI-селектор режимов Вербализатора (Tabs: `Casual` | `Serious` | `Deep Research`), привязка к ключам F3.
- [x] **T-2171** [@Builder] Блок мониторинга `dynamic_cliche_list`: дата обновления, кнопка форсированного обновления, ручное редактирование (wiring к API F4).
- [x] **T-2172** [@Builder] API: при необходимости новые эндпоинты для stage-1/2 ключей и блока клише (RBAC/param_permissions).
- [x] **T-2173** [@Builder] Тесты: tab-mapping, basic≥1 на вкладку, обновлённые маркер-тесты (`test_frontend_tab_mapping.py`, `test_webapp_*`), блок клише, freeze-меню (25 вкладок/NAV_ITEMS/карточки модулей).
- [x] **T-2174** [@Builder] Регресс: `node --check web/app.js`, JS-гейты, полный pytest 0 failed.
- [ ] **T-2175** [@DevOps] Деплой + живая приёмка: карточки разделены, табы режимов и блок клише работают в TMA; отчёт (R17/R18).

## Критерии приёмки
- Карточки модулей разделены на Синтезатор/Вербализатор; селектор режимов работает.
- Блок `dynamic_cliche_list` показывает дату, форс-обновление и ручное редактирование.
- Структура меню не изменена (freeze), basic≥1 на вкладку.
- Полный pytest — 0 failed.

## Риски
- **R1 (High):** изменение каталога ломает `test_param_catalog` (эталон 439/409/414/92/90/20) → осознанный Δ + обновление эталона (T-2168/T-2173).
- **R2 (High):** структурные изменения UI нарушают freeze-меню → freeze-тесты (T-2169/T-2173).
- **R3 (Medium):** пустые вкладки при разделении карточек (basic=0) → generic-рендер basic≥1 (T-2169/T-2173).
- **R4 (Medium):** гонки/рассинхрон с API F4 → единый источник истины, идемпотентные вызовы (T-2171).
- **R5 (R17/R18):** секреты промптов/ключей не утекают в UI/логи.

## Зависимости / ступени вливания
- **Зависит от F3** (режимы), **F4** (кэш клише), **F5** (ступень `web/**` + группы изображений).
- `web/index.html`/`web/app.js` — **ступень F5 → F7 → F8 → F9** (F8 до F9).
- `services/param_catalog.py` — ступень F2 → F5 → **F8**.
- Требует `spec.md` + `ADR-1023-8` (Step 2 @Architect).

## Feature flag / раскатка
- Отдельного серверного флага нет (UI-раздел всегда доступен; управление — существующим RBAC/param_permissions). Откат — `git revert`. Δ каталога — осознанный (новые ключи), фиксируется в spec.
