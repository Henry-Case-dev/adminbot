# Задачи: help-ui-system2-round1022

> **Раунд 10.22 (UPD3)** · Приоритет **P1** · Шаг 1 @PM · Тип: контент/UI (код-канон) + frontend `web/**`
> **UPD3-дельта (Д-11):** полная переписка Справки по регламенту (`<blockquote>`, `<h1>`/`<h2>`, без точек/запятых, транскрипт vs выжимка, убрать п.11).
> **ТЗ:** `plans/current_task.md`, «ЧАСТЬ 2. Актуализация раздела «Справка» (Frontend)» (строки 220–238). Файл untracked, SSH-креды — не цитировать, не коммитить (R17/R18).
> **Baseline:** HEAD `acd9311`; pytest **6779/0**; каталог **439/409/414/92/90/20**; SQLite **v12**; прод `a923310`.

## Цель
Полностью переписать Справку: правила вёрстки, разница транскрипт vs выжимка, удалить п.11 «Безлимиты», добавить блок «Как бот думает (System 2)». Бампнуть канон-версию и синхронизировать `info_text.md`.

## ТЗ (кратко)
Правила: (1) никаких точек/запятых/союзов «или» между примерами команд; (2) каждая команда — в `<blockquote>`; (3) только `<h1>` (главные) и `<h2>` (подразделы). Ключевое: транскрипт (`Бот, транскрипт`) vs выжимка (`Бот, поясни за видос` / `Бот, о чем видео`); в Гайде по возможностям — блок «Как бот думает (System 2)»; п.11 убрать.

## Решение (Шаг 0)
**Что уже есть:**
- Код-канон `DEFAULT_INFO_TEXT` — `services/info_service.py:145-214`; `INFO_CANON_VERSION = 3` (`:225`); `KNOWN_INFO_SNAPSHOTS` (`PREV_DEFAULT_INFO_TEXT` — v1, `PREV_R2020_DEFAULT_INFO_TEXT` — v2).
- Пункт 11 «Безлимиты» — `services/info_service.py:209-214`.
- Сид `info_text.md` (**корень репозитория**), байт-в-байт с `DEFAULT_INFO_TEXT` (D224: DEFAULT_INFO_TEXT = info_text.md = ARCH 53.3 = R44-1); PG-ключ `content.info_how_it_works`, сид `services/config_cache.py:214+`.
- Рендер: `web/index.html:2585-2608` (`v-html="sanitizedInfoHtml"`, `sanitizeHtml`), API `/info` — `web/api/routes.py:1064-1101` (`canon_version`/`canon_drift`).
- БЛОК 8 (10.20, ADR-1020-8) уже делал v3 → теперь нужен **бамп v3 → v4** + PREV + байт-тест.
- **React/Vue в проекте нет** — есть код-канон + Vue-рендер; «открыть React/Vue компонент» из ТЗ трактуется как правка `web/index.html`.

**Что новое:**
- v4-текст по правилам вёрстки (blockquote/h1/h2), транскрипт vs выжимка.
- Блок «Как бот думает (System 2)» в Гайде по возможностям.
- Удаление п.11 «Безлимиты».
- Бамп `INFO_CANON_VERSION` 3→4 + новый PREV-слепок + миграция PG `content.info_how_it_works` + байт-тест.
- Вёрстка/санитайзер: разрешить `<blockquote>`.

**Конфликт / инварианты:**
- Правило D224 (байт-синхрон `DEFAULT_INFO_TEXT` ↔ `info_text.md` ↔ ARCH 53.3) — любая правка → бамп версии + PREV + байт-тест.
- `sanitizeHtml` может вырезать `<blockquote>` — проверить allowlist (R2).

## Задачи
- [x] **T-2070** [@Architect] ADR/дизайн-заметка: правила вёрстки (blockquote/h1/h2), состав текста (транскрипт vs выжимка), v4-миграция, allowlist санитайзера, откат.
- [x] **T-2071** [@Builder] Переписать `DEFAULT_INFO_TEXT` (`services/info_service.py`): команды только в `<blockquote>`, без точек/запятых/«или» между примерами; только `<h1>`/`<h2>`; транскрипт vs выжимка.
- [x] **T-2072** [@Builder] Убрать п.11 «Безлимиты»; grep тестов/сидов на завязку перед правкой (завязок на канон не найдено).
- [x] **T-2073** [@Builder] Добавить блок «Как бот думает (System 2)» в «Гайд по возможностям» (`plans/docs/intelligence_user_guide.md`, §11; Словарик → §12).
- [x] **T-2074** [@Builder] Бамп `INFO_CANON_VERSION` 3→4 + `PREV_R1022_DEFAULT_INFO_TEXT` (v3) в `KNOWN_INFO_SNAPSHOTS` + миграция PG; `info_text.md` синхронизирован байт-в-байт.
- [x] **T-2075** [@Builder] CSS `.info-html` (h1/h2/blockquote) в `web/static/app.css`; `sanitizeHtml` (DOMPurify default) пропускает blockquote/h1/h2 — рендер-тесты + JS-гейт `tests/js/round1022_help_test.js`.
- [x] **T-2076** [@Builder] Байт-тест канона (DEFAULT_INFO_TEXT ↔ `info_text.md`) + тест миграции v3→v4 (`tests/test_help_ui_round1022.py`, 21 тест).

## Статус (F7, @Builder, 10.22 UPD3)
- Код-канон v4, `info_text.md`, PG-миграция и рендер готовы; полный `pytest` — **6926 passed / 0 failed**.
- **Живая приёмка владельцем (визуальный просмотр Справки в проде/мини-аппе) — ОТКРЫТА, ожидает владельца.**

## Риски
- **R1 (High):** рассинхрон канона (код ↔ `info_text.md` ↔ ARCH 53.3) → байт-тест D224 (T-2076).
- **R2 (High):** `sanitizeHtml` вырежет `<blockquote>`/`<h2>` → правка allowlist + рендер-тест (T-2075).
- **R3 (Medium):** удаление п.11 сломает существующие тесты/сиды → grep перед правкой (T-2072).
- **R4 (Medium):** упоминание System 2 до готовности F3–F5 → формулировать без деталей реализации; сверка после Step 2/реализации.
- **R5 (Low):** `parse_mode`/HTML-теги — сохранить канон; не менять структуру меню.
- **R6 (R17/R18):** не цитировать SSH-креды; без секретов в отчётах.

## Зависимости / ступени вливания
- **F7 после Step 2** (ADR F3/F4/F5 — чтобы честно описать пайплайн) и **после F6** (что именно чистит предохранитель).
- Общие `web/index.html`/`web/app.js` — **ступень F2 → F7**: правки `urgent-summary-aliases-ui-round1022` вливаются первыми.
- Эксклюзив: `services/info_service.py`, `info_text.md` (корень), рендер-блок `web/index.html`.
- Флага нет. **Δ каталога = 0** (версия канона — не ключ `param_catalog`); откат — `git revert` + обратная канон-миграция.
