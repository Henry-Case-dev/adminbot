# Задачи: permsoc-module-isolation

Раунд 10 (07.09.2026), **часть 2.1 эпика** «Multi-chat scaling» (research `plans/docs/multi-chat-scaling-research.md`, вариант A). Нумерация продолжает T-877 → **T-878…T-889**; база HEAD fac1b9f. Скоуп — **изоляция «Функции PERMsoc» в единый модуль-плагин**: все хардкод-триггеры (Славик 479167456, Костя 350803143, Леха/Алан 138811255, Оля 834424825, передразнивания) выносятся в плагин «Функции PERMsoc» с master-тумблером «Включить функции PERMsoc»; по умолчанию OFF для НОВЫХ чатов; существующие чаты — миграция/правило в A1 (PM-база: сохранить текущее поведение живых чатов: флаги-дефолты как было). Spec.md при планировании НЕ создаётся; A1 (Q1–Q6) закрывает @Architect правкой в разделе A. Связанные части: `multi-chat-rbac-byok` (T-843…T-869) — per-chat доступы/скоп, `feature-gates-worker-budget` (T-890…T-902) — Opt-In, `tma-ia-progressive-disclosure` (T-903…T-913) — навигация, `global-oversight-dashboard` (T-914…T-924).

## Контекст (Step 0; факты проверены @PM при планировании по HEAD fac1b9f)

**Роутеры (bot.py порядок НЕ менять):** `slava_presence` → `alan_greeting` → `kostik` → `alan` → `dead_page` → `war_alert` → `common` → `olya` → `slavik` → `vasya`; существующие инклуды добавляются только ДОБАВКАМИ (прецедент round 7, bot.py:483-зоны). Триггеры-фильтры: UserIdFilter-модуль (hot.get при импорте — F-5/alan.py:120-прецедент); `handlers/slavic.py` 479167456, `handlers/kostik.py` 350803143, `handlers/alan.py`+`alan_greeting.py` 138811255, `handlers/olya.py`+`filters/olya_video.py`+`services/olya_relay.py` 834424825 (флаг `flags.olya_enabled`), `handlers/common.py mimic_handler`+`services/mimic_relay.py`+`mimic_transform.py` (`flags.mimic_enabled`). Тумблеры по умолчанию: olya/mimic/kucha — off; slavik/kostik/alan — включены (см. MEMORY.md «Тумблеры персоналий»). Telegram ID персон живут во вкладке «Реакции и Триггеры» TMA (значения в bot_settings категории reactions: reactions.slavik_user_id и т.п. — баг-контекст: админ ID должен быть в access — см. tma-ui-fixes T-876/перенос). SQLite-память/инструменты истории — вне скоупа (RUNTIME WARNING раундов; Epic 86 frozen).

## A. Дизайн (PM-база; Q1–Q6 закрывает @Architect в T-878)

- [ ] **T-878 (A1):** @Architect — протокол дизайна: (Q1) **master-гейт-фильтр**: как навесить «module permsoc enabled» на существующие UserIdFilter-роутеры без смены их порядка (PM-база: общий `PermsocGateFilter` (aiogram Filter, callable-фильтр) с параметрами (module_id, target_user_id) — оборачивает каждый триггер; фильтр вычисляет `permsoc_enabled(chat_id)` из per-chat слоя (chat_params — часть 1) И `module_enabled(chat_id, module)`; UserIdFilter остаётся внутренним; для import-time hot.get-паттернов — отложенный доступ (F-5-прецедент: N3-комментарий/ленивое); (Q2) **master-тумблер**: имя/группа ключа (PM-база: `flags.permsoc_enabled` глобальный дефолт + per-chat override `chat_params['flags.permsoc_enabled']`; под-тумблеры `flags.olya_enabled`/`flags.mimic_enabled` сохранить как per-chat-ключи с фолбэком глобального) — что значит master=OFF (все 5 триггеров игнор, bot-молчание) vs под-тумблером (точечное отключение модуля); (Q3) **включение по умолчанию**: для НОВЫХ чатов — OFF (часть 2.3 Opt-In, согласовать с feature-gates); для существующих активных чатов — правило (PM-база: ярый чат -1002661910336 + чаты с исторической активностью/кастомными включёнными флажками → master ON; остальные OFF; ровная детерминированная логика + `scripts/backfill_permsoc_gates.py`-сид при деплое); (Q4) **структура плагина**: новый `services/permsoc.py` (реестр: 5 модулей: id, name_ru, user_id (из конфига, см. поле «Telegram ID»), handler-роутеры, enable-функция, зависимости (relay-сервисы)); как НЕ ломать direct_chat/общие маршруты и vasya/dead_page/war_alert (вне плагина); (Q5) **TMA-секция** «Функции PERMsoc» (часть-2.2/3.1-зависимость: временно карточка во вкладке «Реакции и Триггеры»): master + под-тумблеры + кто имеет право включать (PM-база: только глобальный admin; локальный админ — читать); (Q6) **инвентарь**: полный список точек (фильтры UserIdFilter по всем 5-ти, olya_video-фильтр, mimic-хуки, relay-сервисы, поля Telegram ID в TMA, REGISTRY-записи реакций), какие тесты затронуты (test_direct_chat/test-фильтров псевд-моках), что остаётся как есть (kucha, vasya, dead_page, war_alert). Изменения задач — пометками `(изменено @Architect T-878)`.

## B. Плагин-модуль

- [x] **T-879 (B1):** `services/permsoc.py` (НОВЫЙ): реестр `PERMSOC_MODULES` (5 записей: id/name_ru/target_user_id (read: hot.get per-ключу reactions.slavik_user_id и т.п. — отложенный доступ), routers-список); функции: `permsoc_enabled(chat_id) -> bool` (per-chat → глобальный флаг `flags.permsoc_enabled` (дефолт true для существующих путей — Q3), fail-open: безопасно-игнор), `module_enabled(chat_id, module_id) -> bool` (как Q2: olya_flags через per-chat-фолбэк), `setup_permsoc(...)` — DI-регистрация (прецедент setup_presence/setup_chat_lifecycle); `PermsocGateFilter(module_id)` — aiogram Filter (check: chat_id есть + enabled; вернуть `True/False`, не UNHANDLED-паттерн). (services/permsoc.py — НОВЫЙ)
- [x] **T-880 (B2):** гейтование «больших» триггеров — Славик/Костя: `handlers/slavik.py` (479167456) и `handlers/kostik.py` (350803143): замена UserIdFilter-фильтра на обёртку `PermsocGateFilter('slavik')` (сохраняя user-match-условие — фильтр делаем эквивалентным: match по id в обёртке); поведение при включённом модуле — БЕЗ изменений (регресс: старые тесты/псевдов-кейсы); при выключенном — тихий игнор (no-op как для чужих сообщений). (handlers/slavik.py, handlers/kostik.py)
- [x] **T-881 (B3):** Леха/Алан: `handlers/alan.py` + `handlers/alan_greeting.py` (138811255): два роутера под гейт `PermsocGateFilter('alan')` (greeting-роутер — приветствие-сообщение: для alan-модуля тоже гейт, но при персональном сообщении — Q4 решение); поведение при master OFF: alan-ветка полный игнор. (handlers/alan.py, handlers/alan_greeting.py)
- [x] **T-882 (B4):** Оля: `handlers/olya.py` + `handlers/filters/olya_video.py` + `services/olya_relay.py` (834424825): гейт `PermsocGateFilter('olya')` + обёртка существующего `flags.olya_enabled` (теперь per-chat: `hot_chat(chat_id, 'flags.olya_enabled', global-deval)`, часть-1-хелпер; порядок: master OFF → игнор ВООБЩЕ, под-флаг OFF → индивидуальный). (handlers/olya.py, filters/olya_video.py, services/olya_relay.py)
- [x] **T-883 (B5):** передразнивания: `handlers/common.py mimic_handler` + `services/mimic_relay.py` + `services/mimic_transform.py`: гейт `PermsocGateFilter('mimic')` при распространении (mimic под фидом чата, не по участнику — фильтр по сообщению: chat-гейт), `flags.mimic_enabled` → per-chat-фолбэк как Q2. (handlers/common.py, services/mimic_relay.py, services/mimic_transform.py)

## C. Lifecycle и миграция

- [x] **T-884 (C1):** дефолт новых чатов OFF: `handlers/chat_lifecycle.py` — при `ensure_profile`/входа (my_chat_member, прецедент T-777): новый chat_id → chat_params `flags.permsoc_enabled=false` (историю-запись field='chat_params' — согласовать с part-1) + при входе — авто-добавление TG-админов в chat_admins (research 2.2: `getChatAdministrators`, только если бот admin? Q3) — ответственность: PART-1 (T-849) и gates-фича, здесь — только пассивная регистрация master-флага (по Q1/Q3). (handlers/chat_lifecycle.py; при ходе part-1 ранее — согласование с ним)
- [x] **T-885 (C2):** миграция существующих чатов: `scripts/backfill_permsoc_gates.py` (НОВЫЙ; запуск @DevOps при деплое, прецедент seed_chat_lore.py): для каждого активного PG-профиля: master ON если чат в списке «живых» (по правилу Q3: есть кастомная активность/настраивались флажки — самую простую логику: ON только для «включённые вручную» глобальные флаги настроены или чат = ярый) — строго по Q3; OFF — иначе; output только stdout; идемпотентный (повтор — no-op, существующий chat_params не затирает). (scripts/backfill_permsoc_gates.py — НОВЫЙ)

## D. TMA

- [x] **T-886 (D1):** секция «Функции PERMsoc» в «Реакции и Триггеры» (временное размещение — до TMA-IA фичи): master-тумблер (включается/меняет chat_params [активный чат, Q5: права global admin]) + 5 под-тумблеров (Славик/Костя/Алан/Оля/Передразнивания) — read-only для не-глобальных (local админ: видит статус, менять не может, если по Q5); показ: только если активный чат в большом списке; значения-флаги: для под-тумблеров — те же ключи (olya_enabled/mimic_enabled — per chat; slavik/kostik/alan — их «включатели»: модуль-часть, новые keys? по Q1/Q2) + мастер-статус бейдж. (web/app.js, web/index.html)

## E. Тесты

- [x] **T-887 (E1):** гейт-логика (моки без aiogram): `permsoc_enabled`/`module_enabled` matrix: master OFF (все 5 игнор), master ON + модуль OFF (игнор модуля), оба ON (прежнее поведение — верно у UserIdFilter-старое), per-chat различие (чат A ON, чат B OFF при глобал master ON — через chat_params-мок); `PermsocGateFilter` едино-инстанс-безопасность; новые чаты OFF (lifecycle-мок); бэкфил-сид: повтор-запуск no-op, существующее не затирается. (tests/test_permsoc.py — НОВЫЙ)
- [x] **T-888 (E2):** регресс поведения включённых путей: тесты существующих триггеров (slavik/kostik/alan/olya/mimic — по мокам aiogram с новыми фильтрами: срабатывают при включённом модуле/персону-мач; не матч чужого юзера — не срабатывают); тесты-инвентарь (маркер-строки: сервисы relay/transform не вызваны при OFF); оп-нование: порядки роутеров в bot.py НЕ менялись (grep-аудita); полный pytest → 0 failed; `git diff --check`; RUNTIME WARNING соблюдён (SQLite-схема/история не тронуты). (tests/, ручные проверки)

## F. Деплой/README/отчёт

- [ ] **T-889 (F1):** README-раздел «Функции PERMsoc (плагин)» (что входит, `flags.permsoc_enabled`, как включить чату, backfill-скрипт, новый класс ролей контроля master — глобальный admin; RUNTIME-заметка: порядок роутеров bot.py сохранён); оформление: деплой раунда — общий H2 части-1 (T-864) или собственный мини-деплой (решение @DevOps: в отчёте); финальный memory-sync. Полный pytest → 0 failed. (README.md) *(@DevOps финальный шаг — вне архива)*
