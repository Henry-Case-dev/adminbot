# Задачи: tg-video-tool-calling-fixes

Bugfix-раунд 04.09.2026 (нумерация T-667…T-685 продолжает T-666). После эпика 04.09 (ae9b4f8..e3e7fd5, 3447 passed) обнаружены три блока дефектов. ПРОБЛЕМА 1: триггеры «транскрипт/че за видос/о чем видео/поясни за видос/перескажи видос/че в видосе» (handlers/youtube.py:47-50, substring-матч; парсер 85-107) срабатывают ТОЛЬКО при YouTube-URL в тексте/caption — без URL `_parse` → `(None, None)` → UNHANDLED. Обычные TG-видео (`message.video`, документы mime video/*, репосты через forward_origin) никем не транскрибируются: единственный медиа-хендлер — `F.voice|F.video_note` (voice_transcription.py:337). Bot API локальный (127.0.0.1:8081, наружу 22/443) — публичных URL файлов нет; `video_cascade_client` понимает только `video_url` (YouTube canonical); ffmpeg в проекте НЕТ (mp4-видео шлётся в Groq upload как есть, в OpenRouter — `input_audio` c форматом m4a). ПРОБЛЕМА 1b: tools/video_downloader.py:255-273 — приоритет (1) `is_direct_media_url` → httpx-стрим (хотфикс), (2) yt-dlp для YouTube, (3) cobalt; в direct-ветке handlers/video_download.py:199-244 нет `cooldown_touch` (баг vs :264), реплай «скачай» на видео-сообщение не разбирается (проверяется только `message.video/document` вызова, 186-196, а не `reply_target`; `_handle_native_media` 486-518 есть и работает). ПРОБЛЕМА 2: direct_chat — `CHAT_SYSTEM_PROMPT` (services/chat_prompts.py:3-14) не упоминает инструменты; `prompts.direct_chat_system_prompt` (param_catalog.py:240-241) сидится из кода с `ON CONFLICT DO NOTHING` — смена канона НЕ обновит кастомизированное прод-значение; description'ы tool_schemas.py:30-31 не покрывают счётные вопросы; tool_loop.py:37-49 тихо деградирует на 1-м раунде при LLMError.

Проверено на локальном master (синхронизирован с origin/master, e3e7fd5): незакоммичен только `plans/docs/memory-project-overview.md` (коммит — на финальном шаге E, вкл. .env-исключение: секреты не коммитить). Роутер youtube 0e регистрируется в bot.py:374 ДО web 0f / checkup 0g / direct_chat 0h (:387) и реакций (slavik :454, vasya :457); video_download — поздно, bot.py:448 (после common, до slavik) — при расширении триггеров не сломать приоритеты. Транскрипт-хелпер локального режима: `voice_transcription._fetch_media_to_tmp` (:180-229, гейт `hot flags.download_enabled`, копия с диска `TELEGRAM_API_FILES_DIR/<bot_id>:<token>/` по относительному file_path, retry×3 + fallback `bot.download`) — кандидат на вынос в общий `services/media_download.py`. Канон промпта держится байт-в-байт в tests/test_direct_chat_prompts.py:31-42 — при смене текста эталон обновлять (в тестах хранить и старый канон для сравнения при миграции прод-БД).

**Статус:** archived (выполнено 2026-09-04, 3526 passed, 0 failed). Аппрув @Reviewer; архитектура — `ARCHITECTURE.md` §14. Задачи T-683/T-684/T-685 остаются открытыми — исполняет @DevOps финальным шагом (README, коммит всего, пуш, деплой).

## A. Планирование/дизайн

- [x] T-667 — spec создан и одобрен: @Architect: spec.md (plans/archive/tg-video-tool-calling-fixes/spec.md) — дизайн всех трёх частей ДО реализации: ветка нативного видео в youtube.py (пересечения с 0f/0g/0h и реакциями), безопасный direct-stream, миграция промпта прод-БД (старый канон как константа, кастом не затирать), FTS count для `query_chat_memory`.

## B. Часть 1 — TG-видео по командам «транскрипт/че за видос/о чем видео/поясни за видос/перескажи видос/че в видосе»

- [x] T-668 — расширить парсер/триггер youtube.py: если в сообщении или `message.reply_to_message` есть медиа, квалифицируемое как «видео-сообщение» (`message.video` | `message.document` с `mime_type video/*`, включая репосты — `forward_origin`/пересланное вложение), и текст вызова содержит триггер — перехватывать вместо `(None, None)`→UNHANDLED; для `document` без mime — решить по имени/размеру (описать в spec); YouTube-URL-ветку (:85-107) не менять; видео без триггера — как и раньше мимо (не ломать 0f/0g/0h и реакции Славика/Оли).
- [x] T-669 — скачивание TG-видео во временный файл: вынести общий хелпер `fetch_media_to_tmp` в services/media_download.py из `voice_transcription._fetch_media_to_tmp` (handlers/voice_transcription.py:180-229 — локальный Bot API: гейт `hot.get("flags.download_enabled", ...)`, копия с диска `TELEGRAM_API_FILES_DIR/<bot_id>:<token>/` при относительном file_path, retry×3, fallback `bot.download`), переиспользовать в voice_transcription БЕЗ дублирования (реэкспорт/импорт) и в новой ветке youtube; tmp-уборка в finally.
- [x] T-670 — лимиты видео: новые горячие ключи `limits.video_transcribe_max_size_mb` (дефолт ~50) и `limits.video_transcribe_max_duration_seconds` (дефолт ~600) в param_catalog.py (категория limits) + settings-фолбек; ПЕРЕД фиксацией дефолтов проверить фактические лимиты Groq (whisper upload) и OpenRouter (`input_audio`); длительность брать из `message.video.duration` (TG отдаёт; для document — из доступных метаданных/имени), размер — после скачивания до транскрибации; человечные фразы отказа (новые пулы в services/smartmodule_phrases.py, стиль существующих, без пересечений — см. test_direct_chat_prompts.py:79-99).
- [x] T-671 — транскрибация и выдача: mp4-файл через существующий VoiceTranscriber-каскад (Groq whisper → OpenRouter, как video_note/m4a — текущие форматы работают); «транскрипт» → текст транскрипта; остальные триггеры → LLM-выжимка (переиспользовать YOUTUBE-промпт-механику или новый `VIDEO_FILE_SUMMARY_PROMPT`); инъекция в память по аналогии voice `_inject_memory`/`wrap_media_fact` (handlers/voice_transcription.py:104, 242-260); кулдаун — переиспользовать youtube-кулдаун (уже touch'ится в :127).
- [x] T-672 — graceful degradation: STT упал/вернул пусто → вежливая фраза из пулов (без трейсбеков, без `logger.exception`-паники), поток НЕ ронять; поведение при битом/обрезанном файле — тоже фраза.
- [x] T-673 — тесты Части 1: reply «транскрипт» на видео-сообщение без URL; репост-видео (forward_origin); документ с `mime video/*`; триггер без URL и без медиа → НЕ триггер (UNHANDLED); лимиты (размер/длительность); деградация (STT-исключение → фраза); приоритет с web/checkup/direct_chat/реакциями (изоляция как tests/test_epic37_router_isolation.py, порядок bot.py:374 vs :387/:448/:454/:457).

## C. Часть 1b — маршрутизация скачивания

- [x] T-674 — защита `is_direct_media_url` (tools/video_downloader.py:255-273): прямой httpx-стрим ТОЛЬКО для «честных» прямых ссылок; известные платформы (youtube/youtu.be/tiktok/instagram/vk.com/twitter.com/x.com/rutube/vimeo/twitch/dzen и пр. — полный список в spec) → yt-dlp/cobalt, даже если URL оканчивается `.mp4/.webm/.mov/.mkv/.avi/.gif` (редиректные/обманные); тесты на ложные срабатывания (суффикс-маскировка, query-параметры после расширения).
- [x] T-675 — реплай «скачай» на видео-сообщение/документ: в handlers/video_download.py разбирать `reply_target` (`message.reply_to_message.video/document`, включая документы mime video/*) → `_handle_native_media` (486-518; сейчас ветка 186-196 смотрит только на медиа самого вызова); кулдаун-семантика как в обычной ветке; тест.
- [x] T-676 — `cooldown_touch` в direct-ветке (handlers/video_download.py:199-244): сейчас только refresh+remaining-проверка, touch НЕТ (сравнить :264 — touch после успешного probe; D279: fail не жжёт кулдаун) — добавить touch по D279-семантике (после успешного старта скачивания, не в except-ветках).
- [x] T-677 — решение/подтверждение: прямые `.mp4`-ссылки стримом — ОСТАЮТСЯ (это фича прод-хотфикса, 266-268); решение фиксируется в отчёте юзеру, README-фиксацию НЕ здесь (перенесена в T-683, @DevOps); отдельного кода не требует.

## D. Часть 2 — Tool Calling direct_chat

- [x] T-678 — ДО фикса проверить прод-логи (SSH 198.46.175.136, выполнит Builder/DevOps на шаге реализации): маркеры `[tools] final round`, `[tools] provider rejected tools`, `LLM generate_chat OK … tool_calls=` — зафиксировать реальную причину (тихая деградация 1-го раунда при LLMError, tool_loop.py:37-49, vs `tool_calls=0` из-за промпта/description) → результат в spec.md/отчёт PM перед D2-D4.
- [x] T-679 — обновить `CHAT_SYSTEM_PROMPT` (services/chat_prompts.py:3-14): добавить явный блок про инструменты — у бота есть `execute_web_search` (свежие внешние данные) и `query_chat_memory` (история чата/упоминания/статистика: «сколько раз», «когда») — вызвать, когда ответ требует таких данных, и опираться на результат; сохранить токсичный стиль, «ленивую печать» и лимит 1-2 предложений (текст короткий, дополняющий, не ломать ограничение). МИГРАЦИЯ: старый текст сохранить в коде как константу прежнего канона; авто-обновление значения `prompts.direct_chat_system_prompt` в прод-БД ТОЛЬКО если текущее значение == старому канону (кастом юзера не затирать); сид param_catalog.py:240-241 и `ON CONFLICT DO NOTHING` при этом не трогать.
- [x] T-680 — расширить descriptions в services/tool_schemas.py: `query_chat_memory` — «сколько раз/как часто/когда упоминалось слово или тема, статистика чата» (+ подсказка «про прошлое чата — сначала query_chat_memory»), `execute_web_search` — «актуальные новости, проверка фактов в интернете, то, чего нет в контексте»; имена/параметры схем (канон 3.3) НЕ менять; обновить/добавить тесты (tests/test_tool_schemas.py).
- [x] T-681 — `tool_router.query_chat_memory`: в результат добавить счётчик совпадений и диапазон дат, чтобы модель могла ответить «N раз» точно: FTS count по истории чата (services/database.py `search_messages_fts`:868 и структура smart_messages — если count-метода нет, добавить аккуратно, напр. `search_messages_fts_count`; смотреть services/summary_memory.py `search_long_term`:1025/`vector_search`:1039) + сниппеты как сейчас.
- [x] T-682 — тесты Части 2: промпт-канон (новый текст + старый канон сохранён как константа; обновить эталон tests/test_direct_chat_prompts.py:31-42); миграция значения в БД (старый канон→новый апдейт, кастом юзера не трогается); description'ы инструментов; tool_router с count (фейковая память как в tests/test_summary_generator.py:49-53); интеграция direct_chat `chat_with_tools` (tests/test_direct_chat.py:1845+).

## E. Документация и деплой

Задачи T-683–T-685 не входят в архив и остаются `[ ]` — исполняет @DevOps финальным шагом (README, коммит всего, пуш, деплой).

- [ ] T-683 — @DevOps (финальный шаг): README-правки — видео-команды для TG-видео (не только YouTube), поведение «скачай» для прямых ссылок и нативных медиа, инструменты и промпт direct_chat (раздел tool calling).
- [ ] T-684 — полный pytest → 0 failed; коммит ВСЕГО незакоммиченного на русском (plans/backlog.md, plans/archive/tg-video-tool-calling-fixes/, plans/docs/memory-project-overview.md, код/тесты частей 1/1b/2; .env НЕ коммитить); пуш в origin/master.
- [ ] T-685 — деплой на 198.46.175.136 (pull, рестарт admin_bot, status), live-верификация (репост TG-видео с «че за видос», «скачай»-реплай на видео, счётный вопрос direct_chat «сколько раз…»), проверка маркеров `[tools]`/`tool_calls=` в прод-логах.

## Регресс

- [x] регресс: полный pytest (3526 passed).
