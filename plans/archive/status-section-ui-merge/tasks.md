# Фича F5 — `status-section-ui-merge` (Статус: объединить Сердцебиение+Бот+Сервер в один блок, убрать «режим»/«версия», уменьшить поле поиска по графу)

> **Статус: ✅ IMPLEMENTED — Батч D (@Builder, 16.09.2026)**; T-1819…T-1824 закрыты; гейты T-1818 (@Architect) / T-1825 (@Reviewer/@PM) — за ревью.
> **Spec:** создаёт @Architect — `spec.md` (+ UI-маркеры; ADR, вероятно, не требуется — оформить как UI-решение).
> **Раунд:** 10.19 (UPD2, `plans/current_task.md:130-175`). **Нумерация:** T-1818…T-1825.
> **Тип:** frontend (`web/index.html`, `web/static/app.css`, `web/app.js`). **Приоритет:** **P2** (UX, не блокер).
> **Зависимости:** — (данные API не меняются). **Конфликт файлов:** `web/index.html`/`web/app.js`/`app.css` (F3 — «Бюджеты»).
> **ТЗ:** UPD2 **п.4** (строка 152) + **п.5** (строка 153).
> **Baseline:** HEAD `fd6acc7`; pytest **6139 passed / 0 failed**; каталог **436/406/411/90/88/19**; APP_VERSION 2.57.0.

## 0. Цель

Привести раздел «Статус» миниаппа к аккуратному виду: **объединить** блоки «Сердцебиение», «Бот» и «Сервер» в **один визуальный блок**; **убрать строки «Режим» и «версия»**; **уменьшить** поле поиска по графу связей (на мобильном — значительно по ширине и высоте; на десктопе — не на всю ширину).

**Требуется (UPD2 п.4/п.5):**
- п.4: поле ввода поиска графов связей в мобильном виде **значительно уменьшить** (ширина и высота); в десктопном виде — **не на всю ширину экрана**.
- п.5: объединить блоки «Сердцебиение», «Бот», «Сервер» в **один визуальный блок**; убрать строки «режим» и «версия».

## 1. Доказательства / карта кода (HEAD `fd6acc7`)

- `web/index.html:2116-2132` — карточка «Сердцебиение» (`grid-column: 1 / -1;`, EKG-SVG).
- `web/index.html:2134-2158` — карточка «Бот» (аптайм, состояние, кнопки рестарт/стоп/старт).
- `web/index.html:2141` — строка «Режим: `<b>{{ statusData.bot.mode }}</b>` · версия `<b>v{{ statusData.bot.version }}</b>`» (к удалению).
- `web/index.html:2160-2186` — карточка «Сервер» (CPU/RAM/Диск/Load/PID).
- `web/index.html:2255-2269` — блок `.graph-search` (поле поиска + кнопки «Найти»/«Сбросить вид» + статус).
- `web/static/app.css:719-726` — стили `.graph-search`/`.graph-search__input`.
- `web/static/app.css:309-318` — базовый стиль `.field`.
- `web/app.js:921-926,5414-5454` — логика поиска по графу (`graphSearchQuery`/`searchCognitionGraph`).
- **Проверить использование** `statusData.bot.mode`/`bot.version` в JS (после удаления строк — не должно падать; API-поле можно оставить аддитивно).

## 2. Требования

- [x] «Сердцебиение» + «Бот» + «Сервер» — **один визуальный блок** (единая карточка/контейнер), внутренняя структура читаемая, на мобиле — столбик.
- [x] Строка со служебными полями (`Режим`/`версия`) **удалена**.
- [x] Поле поиска по графу на мобиле — **значительно меньше** по высоте/шрифту.
- [x] Поле поиска на десктопе — **не на всю ширину** (`max-width: 460px`).
- [x] Никаких регрессов логики поиска/подсветки/сброса вида; JS-гейты чистые.
- [x] Адаптив сохраняется; prefers-reduced-motion/EKG не ломать.

## 3. Constraints (инварианты раунда)

- **Эмодзи/глифы** и существующие классы бейджей не менять сверх требуемого.
- **API не ломать** (R16): поля `bot.mode`/`bot.version` можно оставить; удаление — только на UI.
- **Каталог Δ=0**; CSS/HTML — только по задаче.
- **Порядок роутеров `bot.py` не менять**; `media/`/`.env` **не трогать**; **R17**.
- **Ревью-гейты:** `node --check web/app.js` clean; `node tests/js/routing_test.js` → `JS-UNIT-OK`; `node tests/js/vue_mount_test.js` → `VUE-MOUNT-OK`; `git diff --check`; полный `pytest` 0 регрессий.

## 4. Зависимости / порядок

- **Вверх:** T-1818 (@Architect, UI-маркеры/спека) — формальность.
- **Вниз:** нет.
- **Порядок:** может идти параллельно F1/F2/F6/F7/F8; с F3 (HTML/CSS) — вливать ступенями (F3 → F5).

## 5. Definition of Done

- [x] Три блока визуально объединены в один; на мобиле/десктопе вёрстка не разъезжается.
- [x] Строки «Режим» и «версия» отсутствуют (проверка статическим маркером/диффом).
- [x] Поле поиска графа уменьшено на мобиле и ограничено по ширине на десктопе.
- [x] JS-гейты (`node --check`, `JS-UNIT-OK`, `VUE-MOUNT-OK`) чистые; полный `pytest` **0 failed**.
- [x] API-контракт «Статуса» не изменён (или аддитивно).

## 6. Чек-лист задач

- [ ] **T-1818 (@Architect, гейт):** `spec.md` — макет объединённого блока (порядок подблоков, адаптив), целевые размеры `.graph-search__input` (мобила/десктоп), список удаляемых строк, что делать с полями `bot.mode`/`bot.version` (R16).
- [x] **T-1819 (@Builder):** `web/index.html` — объединить «Сердцебиение»+«Бот»+«Сервер» в один визуальный блок (единая карточка/вложенные секции).
- [x] **T-1820 (@Builder):** удалить строку «Режим … · версия …» (`index.html:2141`); проверить JS-использование полей (без падений; при необходимости оставить API-поля).
- [x] **T-1821 (@Builder):** `web/static/app.css` — уменьшить/ограничить `.graph-search`/`.graph-search__input` (мобила: ширина+высота; десктоп: не на всю ширину); не сломать `.field`.
- [x] **T-1822 (@Builder):** `web/app.js` — верификация, что поиск/подсветка/сброс не зависят от удалённой строки; правки только при необходимости.
- [x] **T-1823 (@Builder):** тесты/гейты — `node --check`, `routing_test.js`, `vue_mount_test.js`; статический маркер отсутствия «Режим/версия»; полный `pytest` 0 failed.
- [x] **T-1824 (@Builder):** адаптив-проверка (мобила/десктоп) — фиксация в отчёте (скриншоты/маркеры).
- [ ] **T-1825 (@Reviewer + @PM, гейт):** сверка DoD; визуальная проверка объединённого блока и размеров поля поиска; отсутствие регрессов логики графа.

## 7. Риски / ADR-конфликты

| # | Риск / конфликт | Мера |
|---|---|---|
| R1 | Удаление строк может сломать JS, читающий `mode`/`version` | T-1820 проверка; API-поля оставить (R16) |
| R2 | Объединение блоков ломает сетку/адаптив | T-1818 макет + T-1824 проверка |
| R3 | Пересечение `web/index.html`/`app.js` с F3 | Вливать ступенями (F3 → F5) |
| R4 | Уменьшение поля поиска ухудшает UX на десктопе | Ограничить ширину, а не «спрятать»; проверить на десктопе |
| R5 | ADR-1013-2 (canvas) не затрагивается | Только CSS-вёрстка, граф-логику не менять |

**ADR:** не требуется (UI-решение); фиксируется в `spec.md`.

## 8. Feature flag / progressive delivery

- **Feature flag:** не требуется (UI-вёрстка). Rollback — `git revert`.
- **Progressive delivery:** неприменимо; проверка на десктопе + мобильном.

## 9. Handoff / деплой

`@Orchestrator` — план F5 готов. Spec — T-1818 (@Architect). Реализация — T-1819…T-1824 (@Builder). **Деплой (SSH + рестарт + live-проверка миниаппа) — @DevOps. Секреты в репозитории НЕ хранятся.**
