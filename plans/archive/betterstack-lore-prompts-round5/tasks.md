# Раунд 5 (фаза 1): betterstack-токен, лор-инжект, промпты

Планирование раунда 5 (04.09.2026, продолжает нумерацию T-727 → T-728…T-746; база HEAD 68fb03e — раунд 4 `betterstack-own-handler-video-memory-cmds`, pytest 3794 passed, деплой active; дерево на момент планирования: `M plans/docs/memory-project-overview.md` — Step 0 @Memory). ЧЕТЫРЕ пункта фазы 1; пункт 5 (импорт истории — фаза 2) в этот раунд НЕ входит, стартует отдельным циклом после деплоя.

**ПУНКТ 1 — BetterStack 401 (root cause найден)**: код УЖЕ корректен — токен читается строго из `.env` через `os.getenv` ПОСЛЕ `load_dotenv` при импорте `config.settings` (bot.py:115-116), не из кэша/БД/хардкода; systemd-юнит без `Environment=`, `/proc/<pid>/environ` пуст. Root cause 401: юзер вставил в `LOGTAIL_SOURCE_TOKEN` на проде **public key из SENTRY_DSN** (значение токена байт-в-байт == pubkey внутри `SENTRY_DSN` — начинается с `R…`; это НЕ Source Token из BetterStack → Logs → Sources). Что делаем в КОДЕ (только эвристики/диагностика, чтение токена НЕ менять — «код по токенам больше не менять»): (а) стартовая эвристика — если `os.getenv('SENTRY_DSN')` содержит `f'https://{token}@'` → WARNING «токен похож на Sentry DSN public key, а не Source Token из Logs → Sources»; (б) маркер `[betterstack] attached` дополнить именем переменной-источника (`from=BETTERSTACK_SOURCE_TOKEN|LOGTAIL_SOURCE_TOKEN`); (в) при HTTP 401 текст WARNING с подсказкой (rate-limit ≤1/60с сохранить); (г) отчёт юзеру с точной инструкцией (curl-тест 200 перед рестартом) — секция F.

**ПУНКТ 2 — лор-инжект (чат 2661910336)**: джаббер-конференция создана «с нулевых» (Пермь; переезды ВК→ТГ; список людей); бот ошибочно считает конфу екатеринбургской. Механика (решения @PM/@Architect по итогам диагностики): protected_facts сейчас `user_name TEXT NOT NULL` + строгий пер-юзерный фильтр → **миграция v6**: `user_name` nullable + `get_protected_facts` `WHERE chat_id=? AND (user_name = ? OR user_name IS NULL)` — «чат-уровневые» protected-факты (user_name NULL) видны ВСЕМ юзерам чата в блоке `<protected_facts>` ВСЕГДА (не урезаются бюджетами; рендер блока уже есть — direct_chat_service.py:625-638). Хранение лора: отдельный модуль `services/chat_lore.py` — константа `CHAT_LORE_2661910336` (текст юзера) + `async inject(db)` (идемпотентно: protected_facts chat-level + graph_facts origin='user_memory' (CHECK уже разрешает — миграция v5), expires_at NULL, weight 1.0, target_user NULL + ручная FTS-синхронизация `graph_facts_fts`; vec-строка доберётся backfill-фоном). Текст лора: в распоряжении планирования только выжимка из Step 0 («создана с нулевых, Пермь, переезды ВК/ТГ, список людей») — **дословный текст юзера (сообщение пункта 2 задания раунда 5) подставляет @Builder в T-733; если текст не доступен в контексте реализации — статус ⏸ до получения от юзера, НЕ выдумывать содержание**. Инжект в прод выполняет @DevOps (T-743) с поиском и удалением/понижением противоречащих фактов про «Екатеринбург» в этом чате (с бэкапом, T-744).

**ПУНКТ 3 — sqlite→PG: НЕ выполняем.** Память остаётся на sqlite; Epic 86 в `plans/backlog.md` помечается «отменён/заморожен (04.09.2026, решение владельца)» (секция E, выполнено @PM при планировании). Код НЕ трогаем.

**ПУНКТ 4 — промпты (правки канонов, 9 констант / 9 PG-ключей `prompts.*`)**: (а) во ВСЕ промпты из списка добавить/унифицировать запрет кавычек-ёлочек («») и длинных тире (—); (б) заменить строгие правила строчных («всегда с маленькой», «чередуй заглавные и строчные») на «иногда начинай с маленькой буквы, имитируя торопливое письмо»; (в) авто-миграция PG-значений: для каждого ключа, если текущее PG-значение == предыдущему канону (слепок ДО правки — `PREV_`-константы, генерируются ДО правки из HEAD), обновить новым; кастом юзера не трогать. Реализация: в каждом `*_prompts.py` рядом с каноном `PREV_`-константа; единый модуль `services/prompt_migrations.py` — `PROMPT_MIGRATIONS: dict[key, list[(prev, new)]]` + `async migrate_prompt_canons(cache)` (current == prev → `cache.set`; current == new → no-op; иначе skip + WARNING «кастом»); вызов в bot.py сразу после `cache.init()` — заменяет `migrate_direct_chat_prompt_if_legacy` (удаляется): direct_chat входит в общий словарь ДВУМЯ ступенями (LEGACY→new5, PREV(канон раунда 4)→new5) — обе проверки из-за возможных отставших прод-значений. `EXTRACT_PROMPT` НЕ трогаем (экстрактор, не user-facing; в списке юзера отсутствует).

**Эталоны унифицированных формулировок (байт-в-байт по файлам, согласовать в T-735..T-739):**
- TYPO (запрет типографики) — существующая строка пункта 3 в checkup/factcheck/search/youtube×2/web: `Типографика: только короткие дефисы (-) и обычные двойные кавычки (""). КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНЫ длинные тире (—) и кавычки-елочки («»).` — для промптов, где её НЕТ (chat, summary SYSTEM, COMPRESS), вставить ту же строку/эквивалентную клаузу без искажений;
- CASING (эталон стиля) — `Имитируй торопливое письмо: иногда начинай предложения с маленькой буквы` (+ локальное продолжение по файлу: «Без форматирования (никакого маркдауна)» для chat; «Текст должен быть читаемым, но выглядеть небрежно.» для summary SYSTEM и т.д.);
- правило «пунктуация/типографика» chat п.2 заменяется на TYPO-строку.

**Канон-файлы (эталоны «НЕ трогать», байт-в-байт дисциплина):** `plans/docs/canon/backlog.md` (R11 v4 summary SYSTEM_PROMPT — блок ~строки 5-32, R42-6 CHECKUP — строка-таблица ~36) и `plans/docs/canon/architecture.md` (блоки CHECKUP ~19-42, SEARCH ~44-69, YOUTUBE ~71-91, WEBPAGE ~93-113, FACTCHECK Section 72 ~115-146). Дословных канон-блоков для direct_chat (R50-4), YOUTUBE_VIDEO и COMPRESS в canon/ НЕТ — добавить в T-739 по аналогии (источник — константы кода; правка эталона = правка константы + эталон + тесты ОДНИМ атомарным коммитом). Дисциплины раунда: полный pytest → 0 failed перед коммитом (3794 на базе); R17 — реальные токены не логировать (только token_len/имя переменной); git diff --check чист.

**Статус:** archived (выполнено 2026-09-05, 3844 passed, 0 failed). Аппрув @Reviewer; архитектура — `ARCHITECTURE.md` §17; спека — `plans/archive/betterstack-lore-prompts-round5/spec.md` (создана @Architect 05.09 после планирования — секция A ниже «spec НЕ создаётся» устарела). Вне архива (исполняет @DevOps финальным шагом в T-743..T-746): лор-инжект в прод (T-743), чистка «екатеринбург»-фактов (T-744), деплой и live-проверки (T-745), отчёт юзеру с curl-инструкцией (T-746).

## A. Spec (встроен в tasks.md; spec.md НЕ создаётся)

Дизайн-решения раунда зафиксированы в шапке выше и в текстах задач B–F; отдельных файлов spec.md в этом раунде нет (по решению юзера). Архитектурные рамки: BetterStack-хендлер (`services/betterstack_handler.py`) и его подключение (bot.py:110-149) не переделываются — только диагностические эвристики (секция B); память остаётся sqlite (Epic 86 заморожен, секция E); миграция v6 — строго по D201-паттерну пересоздания таблиц с сохранением id (прецедент `_migrate_user_memory_v5`, services/database.py:463-508); промпт-миграция — по паттерну `migrate_direct_chat_prompt_if_legacy` (services/chat_prompts.py:47-66, лог-маркер `[prompt_migration]`, кастом не трогаем), расширенному на ВСЕ ключи.

## B. BetterStack: эвристики и диагностика 401 (пункт 1)

- [x] T-727 (B1): стартовая эвристика «токен похож на Sentry DSN public key»: в `services/betterstack_handler.py` чистая функция `looks_like_sentry_public_key(token: str, sentry_dsn: str) -> bool` (условие: dsn непуст и содержит `f"https://{token}@"`; пустые входы → False); в bot.py рядом с чтением токена (после :116, до конструирования хендлера, рядом с существующей стартовой диагностикой :140-149): `if betterstack_token and looks_like_sentry_public_key(betterstack_token, sentry_dsn)` → `logger.warning("[betterstack] токен похож на Sentry DSN public key (…), а не Source Token из Logs → Sources — см. отчёт юзера раунда 5")`; `sentry_dsn` уже читается в bot.py:24 (`os.getenv("SENTRY_DSN")`). Никаких изменений источника/чтения токена (код по токенам больше не меняем). (services/betterstack_handler.py, bot.py)
- [x] T-728 (B2): маркер `[betterstack] attached` дополнить именем переменной-источника: bot.py:145-149 — определить, какая переменная реально дала токен (`BETTERSTACK_SOURCE_TOKEN` приоритетнее, иначе `LOGTAIL_SOURCE_TOKEN`), формат `[betterstack] attached | token_len=%d | handler=own-v1 | from=%s` (from — ИМЯ переменной, не значение; R17). Ветка `else` (нет токена) — оставить `[betterstack] skipped (no BETTERSTACK_SOURCE_TOKEN)` без изменений. (bot.py)
- [x] T-729 (B3): при HTTP 401 текст WARNING с подсказкой: в `services/betterstack_handler.py` `_post`/`_mark_failed`/`_rate_warn` (:175-219) — причина `status=401` логируется с подсказкой: `[betterstack] send failed | reason=status=401 | failed=N | подсказка: проверьте BETTERSTACK_SOURCE_TOKEN/.env — это должен быть Source Token (BetterStack → Logs → Sources), а не Sentry DSN public key`; rate-limit ≤1/60с (`_last_warn_ts`) СОХРАНИТЬ; токен в текст НЕ попадает. Остальные причины (429/5xx/транспорт) — без изменения поведения. (services/betterstack_handler.py)
- [x] T-730 (B4): тесты Части B: `looks_like_sentry_public_key` — dsn с `https://{token}@` → True; пустой dsn/токен, несовпадение → False; маркер attached содержит `from=BETTERSTACK_SOURCE_TOKEN`/`from=LOGTAIL_SOURCE_TOKEN` при обеих конфигурациях env (monkeypatch os.getenv); 401 → лог содержит слово-подсказку и НЕ содержит значения токена (R17); rate-gate ≤1/60с не сломан (серия 401 → одно WARNING в окне); регресс существующих 15 тестов test_betterstack_handler. (tests/test_betterstack_handler.py, tests/)

## C. Лор-инжект: миграция v6 + chat_lore + тесты (пункт 2)

- [x] T-731 (C1): миграция v6 в `services/database.py`: `_SCHEMA_VERSION_CHAT_PROTECTED_FACTS = 6` (рядом с :13-17) + `_migrate_chat_protected_facts_v6()` по точному паттерну `_migrate_user_memory_v5` (:463-508): guard по `sqlite_master` sql (`"user_name TEXT NOT NULL"` в CREATE protected_facts, таблица создана в :383-386) → `ALTER TABLE protected_facts RENAME TO protected_facts_old` → CREATE новой `protected_facts` с `user_name TEXT` (nullable, БЕЗ NOT NULL) + `UNIQUE (chat_id, user_name, fact)` + `CREATE UNIQUE INDEX IF NOT EXISTS idx_protected_facts_chat_level ON protected_facts(chat_id, fact) WHERE user_name IS NULL` (чат-уровневые уникальны по (chat_id, fact); NULL-UNIQUE в SQLite не срабатывает) → `INSERT…SELECT` (сохранение id) → DROP old → `PRAGMA user_version = 6`; вызов в цепочке миграций (:219-220 после `_migrate_user_memory_v5`); повторный запуск — no-op (guard + PRAGMA). Тесты миграции в T-734. (services/database.py)
- [x] T-732 (C2): семантика «чат-уровневых» protected-фактов: `get_protected_facts(chat_id, user_name, include_chat_level=True)` (services/database.py:1343-1351) — SQL `WHERE chat_id = ? AND (user_name = ? OR user_name IS NULL)` с сортировкой чат-уровневые ПЕРВЫМИ (`ORDER BY (user_name IS NULL) DESC, created_at ASC, id ASC` — лор не тонет и не режется при обрезке блока); новый параметр `include_chat_level=False` → только `user_name = ?` (старое поведение). Вызывающие места: `direct_chat_service.py:629` `_build_protected_facts` (контекст-блок `<protected_facts>`, :625-638) — True (чат-лор виден всем юзерам чата ВСЕГДА); `direct_chat_service.py:771` persona-карточка `build_persona_card` (:767-787) — True (дельта spec 4.6.2: чат-лор включается в карточку первой строкой; шапка/N-формат 66.9 не меняются). Рендер блока не меняется (escape_xml_text уже есть). (services/database.py, services/direct_chat_service.py)
- [x] T-733 (C3): новый модуль `services/chat_lore.py`: константа `CHAT_LORE_2661910336` — ДОСЛОВНЫЙ текст юзера (spec Приложение A; байт-проверка скриптом — MATCH); `async ensure_chat_lore(db) -> dict` — идемпотентный инжект в тестовую/прод-БД: (1) SELECT-проверка protected_facts `chat_id=2661910336 AND user_name IS NULL AND fact=CHAT_LORE…` → нет: `INSERT OR IGNORE` (created_at=now; rowcount для корректного inserted/skipped); (2) SELECT-проверка graph_facts `chat_id, fact, origin='user_memory'` → нет: `insert_graph_fact(chat_id, CHAT_LORE…, origin='user_memory', expires_at=None, target_user=None, weight=1.0)` (FTS-строка внутри); возврат `{"inserted": n, "skipped": n}`; при ошибке БД — fail-open с логом, без ронения. Вызов — в `bot.py on_startup()` после `db.initialize()` (дельта spec 4.6.3), await + fail-open WARNING; DevOps/T-743 первичный инжект. (services/chat_lore.py — НОВЫЙ, bot.py)
- [x] T-734 (C4): тесты Части C: миграция v6 — на БД с v5-схемой (с пер-юзерными protected-строками) после запуска: данные сохранены (id), `user_name` nullable принят, partial-индекс существует (дубль chat-level INSERT падает/OR IGNORE работает), повторный запуск — no-op, `PRAGMA user_version=6`; get_protected_facts: chat-level виден при ЛЮБОМ user_name + user-факты только свои (include_chat_level матрица True/False); chat-level всегда первый; persona-карточка СОДЕРЖИТ чат-лор первой строкой (дельта 4.6.2; при отсутствии чат-лора — старое поведение); ensure: первый вызов вставляет 2 строки (protected+graph+FTS), повторный — no-op (same dict, skipped=2); get_protected_facts(2661910336, '<любой>') возвращает лор; CHAT_LORE_2661910336 — непустая, содержит ключевые слова выжимки («Пермск»/«джаббер»/«ВК»/«телеграм»); регресс test_database/test_graphrag_database/test_epic65 (protected_facts-тесты) + user_version-цепочка 5→6 во всех миграционных тестах. (tests/test_chat_lore.py — НОВЫЙ, tests/test_database.py, tests/test_direct_chat.py)

## D. Промпты: правки канонов + PREV-слепки + prompt_migrations (пункт 4)

- [x] T-735 (D1): `services/chat_prompts.py`: ДО правки сгенерировать слепок текущего канона из HEAD как `PREV_CHAT_SYSTEM_PROMPT` (= текущий `CHAT_SYSTEM_PROMPT`, канон раунда 4 с инструментами; комментарий «слепок HEAD 68fb03e до раунда 5»); `LEGACY_CHAT_SYSTEM_PROMPT` не трогать; затем правки `CHAT_SYSTEM_PROMPT` (ТОЛЬКО два пункта, остальное байт-в-байт): п.1 `Имитируй ленивую печать: только строчные буквы (включая начало предложений), без форматирования (никакого маркдауна).` → `Имитируй торопливое письмо: иногда начинай предложения с маленькой буквы. Без форматирования (никакого маркдауна).`; п.2 `Пунктуация базовая, без сложных тире.` → `Типографика: только короткие дефисы (-) и обычные двойные кавычки (""). КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНЫ длинные тире (—) и кавычки-елочки («»).`; функция `migrate_direct_chat_prompt_if_legacy` (:47-66) ПЕРЕНОСИТСЯ/заменяется общим `migrate_prompt_canons` (T-740) и из модуля удаляется (вызов из bot.py тоже). (services/chat_prompts.py)
- [x] T-736 (D2): `services/summary_prompts.py`: слепки HEAD → `PREV_SUMMARY_SYSTEM_PROMPT` и `PREV_COMPRESS_PROMPT`. SYSTEM: п.1 → `Имитируй торопливое письмо: иногда начинай предложения с маленькой буквы. Текст должен быть читаемым, но выглядеть небрежно.` (убрать «чередуй заглавные и строчные… случайным образом» и «Не пиши всё только с маленькой буквы»); добавить в ПРАВИЛА (номер — следующий свободный, без перенумерации остальных) пункт-типографику: `Типографика: только короткие дефисы (-) и обычные двойные кавычки (""). КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНЫ длинные тире (—) и кавычки-елочки («»).`; остальные пункты/блоки (`{max_symbols}`, финальная приписка, `<bot_knowledge>`) — байт-в-байт. COMPRESS: фрагмент `не используй нумерацию, маркдаун и смайлы. пиши с маленькой буквы.` → `не используй нумерацию, маркдаун, смайлы, кавычки-елочки («») и длинные тире (—). иногда начинай предложения с маленькой буквы, имитируя торопливое письмо.`; остальное дословно. EXTRACT_PROMPT НЕ трогать. (services/summary_prompts.py)
- [x] T-737 (D3): `services/checkup_prompts.py` и `services/factcheck_prompts.py`: слепки HEAD → `PREV_CHECKUP_SYSTEM_PROMPT`, `PREV_FACTCHECK_SYSTEM_PROMPT`. В обеих константах: п.1 `Имитируй ленивую печать: чередуй заглавные и строчные буквы в начале предложений. Пиши небрежно.` → `Имитируй торопливое письмо: иногда начинай предложения с маленькой буквы. Пиши небрежно.`; п.3 типографика уже содержит единый TYPO-текст — оставить байт-в-байт (сверка). (services/checkup_prompts.py, services/factcheck_prompts.py)
- [x] T-738 (D4): `services/search_prompts.py`, `services/youtube_prompts.py` (ОБЕ константы: `YOUTUBE_SYSTEM_PROMPT` и `YOUTUBE_VIDEO_SYSTEM_PROMPT`), `services/web_prompts.py`: слепки HEAD → `PREV_SEARCH_SYSTEM_PROMPT`, `PREV_YOUTUBE_SYSTEM_PROMPT`, `PREV_YOUTUBE_VIDEO_SYSTEM_PROMPT`, `PREV_WEBPAGE_SYSTEM_PROMPT`; в каждой константе та же замена п.1 (см. T-737) и байт-сверка п.3 TYPO. (services/search_prompts.py, services/youtube_prompts.py, services/web_prompts.py)
- [x] T-739 (D5): канон-доки (эталоны, байт-в-байт дисциплина): обновить ДОСЛОВНО блоки под новые тексты — `plans/docs/canon/backlog.md`: R11 v4 summary SYSTEM_PROMPT (блок ~строки 5-32, правило п.1 :15 + новый п.7) и R42-6 CHECKUP (строка-таблица :36); `plans/docs/canon/architecture.md`: CHECKUP (~19-38), SEARCH (~46-69), YOUTUBE (~73-91), WEBPAGE (~95-113), FACTCHECK (Section 72, ~115-146); ДОБАВИТЬ недостающие канон-блоки в canon/ по аналогии с существующими (формат «…ДОСЛОВНО (байт-в-байт) из <источник>»): direct_chat (R50-4, текущий канон раунда 5 + PREV_ + LEGACY_), YOUTUBE_VIDEO, COMPRESS — источник: константы кода после T-735/T-736/T-738; актуализировать перечень «Каноны НЕ трогать» в `plans/project.md` (:37-45). (plans/docs/canon/backlog.md, plans/docs/canon/architecture.md, plans/project.md)
- [x] T-740 (D6): `services/prompt_migrations.py` (НОВЫЙ): `PROMPT_MIGRATIONS: dict[str, list[tuple[str, str]]]` — ключи и пары (prev, new): `prompts.direct_chat_system_prompt` → [(LEGACY_CHAT_SYSTEM_PROMPT, CHAT_SYSTEM_PROMPT), (PREV_CHAT_SYSTEM_PROMPT, CHAT_SYSTEM_PROMPT)] (две ступени: прод мог отстать на раунд-2 или раунд-4 каноне), `prompts.summary_system_prompt` → [(PREV_SUMMARY_SYSTEM_PROMPT, SYSTEM_PROMPT)], `prompts.compress_system_prompt` → [(PREV_COMPRESS_PROMPT, COMPRESS_PROMPT)], `prompts.checkup_system_prompt`, `prompts.factcheck_system_prompt`, `prompts.search_system_prompt`, `prompts.youtube_system_prompt`, `prompts.youtube_video_system_prompt`, `prompts.webpage_system_prompt` — по одному PREV→new; `async migrate_prompt_canons(cache) -> dict[str, str]` (итерация по словарю в фиксированном порядке; `cache.pg_available` False → INFO `[prompt_migration] skip: PG недоступен`; current None → skip «сид сделает своё»; current == new → skip «уже новый канон»; current == prev → `cache.set(key, new, "prompts")` + INFO `[prompt_migration] канон обновлён | key=…`; иначе WARNING `[prompt_migration] кастом юзера — НЕ трогаем | key=… | chars=N`; возврат отчёта обновлённых ключей). bot.py: заменить :598-599 (импорт+вызов `migrate_direct_chat_prompt_if_legacy`) на `migrate_prompt_canons(cache)` сразу после `cache.init()` — рядом с `migrate_direct_reply_ttl_default` (:601-604). Существующие тесты миграции direct_chat переведены на новый модуль (tests/test_prompt_migrations.py — НОВЫЙ). (services/prompt_migrations.py — НОВЫЙ, bot.py, services/chat_prompts.py)
- [x] T-741 (D7): тесты Части D: (1) байт-равенство: каждый обновлённый канон-константы == соответствующему канон-блоку plans/docs/canon/* (test_summary_prompts.py / test_checkup_prompts.py / test_factcheck_prompts.py / test_smartsearch_prompts.py / test_youtube_prompts.py / test_web_prompts.py — эталоны обновлены; test_direct_chat_prompts.py — внутренние байт-эталоны new/PREV/LEGACY); (2) PREV_-константы: != new-канону и содержат старые эталонные фразы («только строчные буквы», «чередуй заглавные и строчные», «пиши с маленькой буквы») — гарантия, что слепки реально «ДО правки»; (3) миграция (мок-cache): current == prev → set(new) вызван; current == LEGACY (direct_chat) → set(new); current == new → no-op; кастом → skip + WARNING; None → skip; PG down → skip; отчёт; (4) в промптах после правок нет фраз-запретов в неправильную сторону: TYPO-строка присутствует во всех 9 константах (compress — lowercase-фрагмент), CASING-фраза присутствует, «не пиши всё только с маленькой буквы»/«только строчные буквы»/«чередуй заглавные и строчные»/«пиши с маленькой буквы» отсутствуют; EXTRACT_PROMPT не изменился; (5) полный pytest зелёный. (tests/test_prompt_migrations.py — НОВЫЙ, тест-эталоны обновлены)

## E. Backlog-пометка Epic 86 (пункт 3 — sqlite→PG НЕ выполнять)

- [x] T-742 (E1): в `plans/backlog.md` секция «Epic 86: GraphRAG → PostgreSQL» (:9-11) — статус помечен: **ОТМЕНЁН/ЗАМОРОЖЕН (04.09.2026, решение владельца — раунд 5, пункт 3): миграцию памяти sqlite→PG НЕ выполнять, память остаётся на SQLite; DESIGN @Architect не запускать; при необходимости эпик может быть переоткрыт**; плюс запись-шапка в backlog.md о планировании раунда 5 (T-727..T-746, ссылка на plans/features/betterstack-lore-prompts-round5/tasks.md). *(выполнено @PM при планировании 04.09.2026 — правки уже внесены в plans/backlog.md; код/память не трогаются)*

## F. Деплой и отчёт юзеру (DevOps; лор-инжект в прод + live-проверки)

- [ ] T-743 (F1): инжект лора на проде (198.46.175.136, /var/www/admin_bot): после деплоя кода (миграция v6 уже применится при старте) выполнить `inject` из services/chat_lore.py (python-инлайн на сервере, DB_PATH прода — local_database.db): ожидается inserted=1 (protected_facts chat-level + graph_facts user_memory + FTS-строка) либо skipped при повторном запуске (идемпотентность); верификация SQL: SELECT protected_facts (user_name IS NULL), graph_facts (origin='user_memory', weight=1.0, expires_at NULL), graph_facts_fts rowid; результат — в отчёт F4. (прод, services/chat_lore.py) *(@DevOps финальный шаг — вне архива; инжект лора на проде после деплоя кода, [ ] остаётся до отчёта T-746)*
- [ ] T-744 (F2): чистка противоречащих фактов про «Екатеринбург» в чате 2661910336: БЭКАП БД перед любыми изменениями (копия local_database.db с датой, вне репозитория); поиск фактов, противоречащих лору (конфа пермская/не екатеринбургская): SELECT по graph_facts chat_id=2661910336 (все origin) и protected_facts, факт LIKE '%екатеринбург%' (регистронезависимо); КАЖДЫЙ найденный факт просмотреть вручную — удалить (через существующие пути, с записью в graph_fact_compressions reason='lore_cleanup_round5', удаление FTS-строки) или понизить (weight → 0.1, запись в compressions) по смыслу; массовых слепых DELETE не делать; отчёт о найденном/изменённом — в F4. (прод, БД sqlite) *(@DevOps финальный шаг — вне архива; чистка «екатеринбург»-фактов на проде с бэкапом, [ ] остаётся до отчёта T-746)*
- [ ] T-745 (F3): деплой раунда + live-проверки: полный pytest → 0 failed; grep-проверка на секреты перед коммитом (правило project.md, реальные токены не коммитить); коммит всего (код B-D, tests, plans/backlog.md, plans/features/betterstack-lore-prompts-round5/, memory-project-overview.md — финальный docs-коммит); пуш; на проде git pull --ff-only + мягкий рестарт; journald-проверки: `[database] migration v6: protected_facts rebuild…` (миграция применилась), `[betterstack] attached … from=LOGTAIL_SOURCE_TOKEN` (+ при битом прод-токене — WARNING-подсказка 401 ≤1/60с, ждём, пока юзер не заменит токен), `[prompt_migration]` INFO для обновлённых PG-ключей (ожидаем ВСЕ 9, если прод-значения были канонами; кастом — WARNING «НЕ трогаем»); верификация в БД после F1-F2; живой тест: direct-обращение в чате, ответ модели знает лор конференции (не «екатеринбургская»); TMA-сохранение промпта с «»/—/переносами — 200. Результаты — в отчёт F4. (всё вышеперечисленное) *(@DevOps финальный шаг — вне архива; деплой раунда и live-проверки, [ ] остаётся до отчёта T-746)*
- [ ] T-746 (F4): отчёт юзеру (в отчёт раунда): (1) **BetterStack 401 — root cause и инструкция**: в `LOGTAIL_SOURCE_TOKEN` прод-.env вставлен public key из SENTRY_DSN (это НЕ Source Token); шаги: BetterStack → Logs → Sources → создать/взять source «AdminBot» → скопировать Source Token → на сервере заменить значение в /var/www/admin_bot/.env (переменную `BETTERSTACK_SOURCE_TOKEN`/`LOGTAIL_SOURCE_TOKEN`); ПЕРЕД рестартом curl-тест: `curl -s -o /dev/null -w "%{http_code}" -X POST "https://in.logs.betterstack.com/<SOURCE_TOKEN>" -H "Content-Type: application/json" -d '[{"dt":"2026-09-04T12:00:00Z","level":"info","message":"betterstack round5 test"}]'` → ожидание 200/202; затем `systemctl restart admin_bot`; контроль: journald `[betterstack] attached … | from=…` и событие в панели; (2) итог лор-инжекта (T-743/T-744) и подтверждение, что конфа теперь идентифицируется как пермская; (3) подтверждение пункта 3: память остаётся на sqlite, Epic 86 заморожен; (4) что изменилось в промптах (стиль «торопливое письмо» + запрет «»/—) и что PG-каноны обновлены автоматически, кастом не тронут. (отчёт) *(@DevOps финальный шаг — вне архива; отчёт юзеру с curl-инструкцией по Source Token, [ ] закрывается по факту исполнения T-743..T-745)*
