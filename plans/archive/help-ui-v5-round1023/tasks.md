# Задачи: help-ui-v5-round1023

> **Раунд 10.23** · Приоритет **P2** · Шаг 1 @PM (+ Step 2 @Architect: `spec.md` + **ADR-1023-9**) · Тип: контент/код-канон + UI-доставка
> **ТЗ:** `plans/current_task.md`, «Рефакторинг UI и Аналитика (Мини-апп) → 3. Актуализация Справки (Жесткий маппинг)». Untracked, секреты не цитировать/не коммитить (R17/R18).
> **Baseline:** HEAD `731a845`; pytest **6962/0**; каталог **439/409/414/92/90/20**; SQLite **v12**; прод `a8a6437`; `INFO_CANON_VERSION=4`.
> **Зависит от:** F3 (умный Вербализатор), F4 (анти-клише), F5/F6 (изображения), F7 (аналитика).

## Цель
Справка **v4 → v5**. В блок «Гайд по возможностям» добавить: умный Вербализатор (адаптация под сложность через 3 режима), принцип System 2, работу динамического анти-клише фильтра. В блок «Гайд по фичам бота» добавить команды генерации изображений. Стиль вёрстки сохранить: теги `blockquote`, `h1/h2`, без точек и запятых между примерами команд.

## Контекст / что уже есть
- Справка: `services/info_service.py` — `DEFAULT_INFO_TEXT` (`:228`), `INFO_CANON_VERSION = 4` (`:323`), `KNOWN_INFO_SNAPSHOTS` (`:326`), слепок `PREV_R1022_DEFAULT_INFO_TEXT` (`:151`); `info_text.md` (корень/настройка `settings.INFO_TEXT_FILE`).
- Правило канона (`:320-322`): любая правка → (1) бамп версии, (2) прежний текст в `KNOWN_INFO_SNAPSHOTS`, (3) байт-тест `DEFAULT_INFO_TEXT ↔ info_text.md`.
- PG-миграция v4 применена идемпотентно через `services/config_cache._migrate_info_how_it_works_v1015` (10.22 F7).
- Rich-доставка инфо: `handlers/info.py:103-104` (`send_rich_message`).

## Задачи
- [x] **T-2176** [@Architect] ADR-1023-9: контент-план v5 (описание режимов/System 2/анти-клише, команды изображений), правила вёрстки (`<blockquote>`/`<h1>`/`<h2>`, без точек/запятых между примерами), обновление снапшотов, идемпотентная PG-миграция v4→v5, откат. Создать `spec.md` + `ADR-1023-9.md`.
- [x] **T-2177** [@Builder] Переписать `DEFAULT_INFO_TEXT` в `services/info_service.py`: секция «11. Генерация изображений» (команды в `<blockquote>`); блок «Как бот думает» дополнен в «Гайде по возможностям» (умный Вербализатор/3 манеры, System 2, анти-клише — spec §0.1); бамп `INFO_CANON_VERSION` 4→5.
- [x] **T-2178** [@Builder] Добавить `PREV_R1023_DEFAULT_INFO_TEXT` (= прежний v4) в `KNOWN_INFO_SNAPSHOTS`; версионирование гайда (`GUIDE_CANON_VERSION=2`, `PREV_R1023_INTELLIGENCE_GUIDE`, `KNOWN_GUIDE_SNAPSHOTS`).
- [x] **T-2179** [@Builder] Обновить `info_text.md` байт-в-байт под новый `DEFAULT_INFO_TEXT`.
- [x] **T-2180** [@Builder] Идемпотентная PG-миграция `content.info_how_it_works` v4→v5 (слепок v4 → v5; повтор → no-op) + миграция гайда v1→v2 (`_migrate_intelligence_guide_r1023`).
- [x] **T-2181** [@Builder] Тесты: байт-тест `DEFAULT_INFO_TEXT ↔ info_text.md`, цепочка снапшотов, идемпотентность миграций info/guide, счётчики стиля (`<h1>×1`, `<h2>×11`, `<blockquote>×24`), наличие новых блоков; полный pytest 0 failed.
- [ ] **T-2182** [@DevOps] Деплой + живая приёмка: справка v5 отображается, миграция применилась идемпотентно; отчёт (R17/R18).

## Критерии приёмки
- Справка v5 содержит умный Вербализатор, System 2, динамический анти-клише и команды изображений.
- Стиль вёрстки сохранён (blockquote, h1/h2, без точек/запятых между примерами).
- `INFO_CANON_VERSION=5`; прежний v4 добавлен в `KNOWN_INFO_SNAPSHOTS`; `info_text.md` байт-в-байт.
- PG-миграция идемпотентна; полный pytest — 0 failed.

## Риски
- **R1 (High):** рассинхрон `DEFAULT_INFO_TEXT` ↔ `info_text.md` ↔ снапшоты → байт-тест + правило канона (T-2177/T-2178/T-2179/T-2181).
- **R2 (Medium):** нарушение регламента вёрстки → тесты-счётчики тегов (T-2176/T-2181).
- **R3 (Medium):** неидемпотентная или пропущенная миграция на проде → тест идемпотентности + live-приёмка (T-2180/T-2182).
- **R4 (R17/R18):** секреты/сырьё в тексте справки/отчётах.

## Зависимости / ступени вливания
- **Идёт последней**: после F3 (режимы), F4 (анти-клише), F5/F6 (изображения), F7 (аналитика) — иначе справка не отразит фактуру.
- Эксклюзив: `services/info_service.py`, `info_text.md`.
- `web/index.html`/`web/app.js` — ступень **F5 → F7 → F8 → F9** (если правки доставки справки во фронте).
- Требует `spec.md` + `ADR-1023-9` (Step 2 @Architect).

## Feature flag / раскатка
- Отдельного флага нет (контент-канон, как в 10.22 F7). Откат — `git revert` + обратная PG-миграция (снапшот v4 в `KNOWN_INFO_SNAPSHOTS` позволяет распознать и откатить). **Δ каталога = 0.**
