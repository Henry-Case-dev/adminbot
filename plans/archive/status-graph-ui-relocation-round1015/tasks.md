# Фича F5 — `status-graph-ui-relocation-round1015` (Релокация статистики графа + бейджи Сна)

> **Статус: ✅ COMPLETED** (Step 4 @Builder, round1015). Реализованы T-1585…T-1591
> (гейты `node --check` / `JS-UNIT-OK` / `pytest 5656 passed, 0 failed` / каталог без Δ).
> Коммит не выполнен по указанию раунда (НЕ коммить). Ожидают гейты T-1584/T-1592.
> **Раунд:** 10.15. **Нумерация:** T-1584…T-1592 (продолжает F4 T-1583).
> **Тип:** frontend (+аддитивное поле API). **Приоритет:** **P1**.
> **Зависимости:** **F3** (§2 — статус сна/глубокого сна, аддитивное поле для активной фазы) —
> частично; **F1/F2** — граф. **Конфликт файлов:** `web/index.html`/`web/app.js` делит с F2 — вливать
> ступенями (F2 → F5).
> **ТЗ:** `plans/current_task.md`, разделы **4** и **5**.
> **Эпик:** `Epic: Багфиксы Графа памяти, Воркера Сна и Ностальгии round1015`.
> **Baseline:** HEAD `798e044`; pytest **5589 passed / 0 failed**; каталог **435/90/406/411/88/19**.

## 0. Цель

**(§4)** Перенести виджет «Статистика графа памяти» из «Модулей» в «Сводку», в «Модулях» оставить
только управление — **без дублирования** (консолидировать с уже существующим блоком метрик «Сводки»).
**(§5)** Переработать бейджи Сна/Глубокого сна (динамика текста + свечение, время «через/до»),
сделать мобильный столбик и удалить свободный текст «пробуждение ~11:00».

**Текущее состояние (Step 0):** карточка статистики в «Модулях» `web/index.html:1452-1469`
(узлы/рёбра/типы/факты/убеждения/парадигмы/мемы), API `GET /api/memory/stats`
(`web/api/memory_agi.py:525-544`), загрузка `web/app.js:5127-5135`. В «Сводке» уже есть блок метрик
`web/index.html:1804-1809` (факты/убеждения/защищённые/парадигмы). Бейджи: `web/app.js:1204-1224`
(`dreamPhaseBadge`/`deepPhaseBadge`), использование `web/index.html:1775-1776` (Сводка) и
`:2920-2924` (Мониторинг Интеллекта, включая «пробуждение ~»); `fmtClock` `app.js:5085`.

## 1. Требования (дословно из ТЗ §4/§5)

- [ ] **§4:** перенести виджет «Статистика графа памяти» (Узлы, Рёбра, Факты, Убеждения и т.д.) из
  «Модулей» в «Сводку»; в Модулях — исключительно элементы управления и настроек.
- [ ] **§5 Сон:** вне активной фазы — бейдж не светится, текст `[☀️] Сон через [время]`; в активной —
  светится (акцент), текст `[🌙] Сон до [время]`.
- [ ] **§5 Глубокий сон:** вне активной фазы — не светится, `[🌅] Глубокий сон через [время]`;
  в активной — светится, `[🌌] Глубокий сон до [время]`.
- [ ] **§5 Адаптив:** жёстко выровнять в столбик: 1-я строка — заголовок «Мониторинг Интеллекта»,
  2-я — бейдж Сна, 3-я — бейдж Глубокого сна; текст «пробуждение ~11:00» удалить полностью.

## 2. Целевые модули (`file:line` на HEAD `798e044`)

- `web/index.html:1452-1469` — карточка статистики в «Модулях» (удалить/перенести).
- `web/index.html:1804-1809` — существующий блок метрик «Сводки» (консолидировать, без дубля).
- `web/index.html:1775-1776` — бейджи в «Сводке» (синхронизировать с новой логикой).
- `web/index.html:2911-2927` — шапка/бейджи «Мониторинга Интеллекта» + «пробуждение ~» (`:2923-2924`).
- `web/app.js:1204-1224` — `dreamPhaseBadge`/`deepPhaseBadge`; `:5085` — `fmtClock`.
- `web/api/memory_agi.py:399-495` — `cognition/status` (аддитивное поле для активной фазы).
- Тесты: `tests/test_webapp_round1013_f5_ui.py`, `tests/test_webapp_round1014_ui.py`; новый
  `tests/test_webapp_round1015_ui.py`.

## 3. Инварианты (constraints — не нарушать)

- **§4 — консолидация, не дублирование:** узлы/рёбра/типы/мемы добавляются в существующий блок
  «Сводки» (`:1804-1809`), а не отдельной второй карточкой; в «Модулях» карточка `:1452-1469`
  удаляется целиком.
- **§5 — аддитивность API:** `next_wake_at`/`next_run_at` сейчас осмысленны только для НЕактивной фазы;
  для «до [время]» нужно аддитивное поле (`active_until`/`ends_at`) ИЛИ вычисление на фронте из
  известной длительности окна — **не ломать существующие поля** `cognition/status` (S10.13-5).
- Бейджи — динамические по реальному `cognition/status`; свечение (`.glow`) только в активной фазе;
  fail-open при `cognition=null` (нейтральный текст, без падения).
- R16/R17; новых `v-html`/CDN нет; `reducedMotion`/polling-механику не ломать.
- Каталог-инварианты **435/90/406/411/88/19** — Δ=0.
- `media/`/`.env` не трогать; порядок роутеров `bot.py` не трогать.
- **Ревью-гейты:** полный `pytest` 0 регрессий, `node --check web/app.js`,
  `node tests/js/routing_test.js` → `JS-UNIT-OK`, маркер-тесты UI, русские conventional commits.

## 4. Зависимости

- **Вверх:** частично **F3** (§2 — воркер сна/статус, аддитивное поле) — согласовать с @Architect;
  **F1/F2** — граф/статистика.
- **Вниз:** нет.
- **Конфликт файлов:** `web/index.html`/`web/app.js` делят F2/F5 — вливать после F2.

## 5. Definition of Done

- [ ] В «Модулях» нет карточки «Статистика графа памяти»; там только управление/настройки.
- [ ] В «Сводке» видны консолидированные метрики (узлы, рёбра, типы, факты, убеждения, парадигмы,
  мемы, защищённые) — **без дублирования** блоков.
- [ ] Бейджи: вне активной фазы `[☀️]`/`[🌅] … через [время]` без свечения; в активной —
  `[🌙]`/`[🌌] … до [время]` со свечением.
- [ ] Мобильный столбик: заголовок → бейдж Сна → бейдж Глубокого сна; текст «пробуждение ~11:00» удалён.
- [ ] `node --check` clean, `routing_test` OK, полный `pytest` — **0 failed**.

## 6. Чек-лист задач

- [ ] **T-1584 (@Architect, гейт):** спека консолидации §4 (финальный набор бейджей «Сводки», где
  живёт кнопка `⟳`) и §5 (аддитивное поле API для активной фазы vs вычисление на фронте; источник
  «до [время]», формат `[☀️]/[🌙]/[🌅]/[🌌]`, CSS-класс свечения, breakpoint столбика).
- [x] **T-1585 (@Builder):** удалить карточку `web/index.html:1452-1469` из «Модулей»; добавить
  узлы/рёбра/типы/мемы в существующий блок «Сводки» `:1804-1809` (единый flex-wrap, без дубля).
- [x] **T-1586 (@Builder):** переписать `dreamPhaseBadge`/`deepPhaseBadge` (`app.js:1204-1224`):
  актив → `[🌙] Сон до [время]` / `[🌌] Глубокий сон до [время]` + `.glow`; неактив →
  `[☀️] Сон через [время]` / `[🌅] Глубокий сон через [время]` без свечения; сохранить
  `limit_exhausted`/`enabled=false` как отдельные нейтральные состояния.
- [x] **T-1587 (@Builder):** «до/через [время]»: реализовать по спеке — аддитивное поле
  `cognition/status` (если решено @Architect) ИЛИ вычисление на фронте; обработка `cognition=null`.
- [x] **T-1588 (@Builder):** мобильный столбик шапки «Мониторинга Интеллекта» (`index.html:2911-2927`),
  удалить `:2923-2924` («пробуждение ~»); проверить адаптив (flex-col на узком экране).
- [x] **T-1589 (@Builder):** синхронизировать бейджи «Сводки» (`index.html:1775-1776`) с новой
  логикой/убрать дублирующее время, если оно ушло в текст бейджа.
- [x] **T-1590 (@Builder):** маркер-тесты UI (`tests/test_webapp_round1015_ui.py`): отсутствие
  карточки в «Модулях», наличие метрик в «Сводке», отсутствие «пробуждение ~», тексты бейджей;
  API-тест аддитивного поля (если вводится).
- [x] **T-1591 (@Builder):** прогон гейтов: `node --check web/app.js` clean, `routing_test` OK, полный
  `pytest` **0 failed**, `git diff --check`; русский commit. *(Гейты пройдены; коммит отложен —
  раунд требует НЕ коммитить.)*
- [ ] **T-1592 (@PM/@Reviewer, гейт):** сверка DoD, live-чеклист владельцу (Android: столбик, свечение,
  отсутствие дублей статистики).

## 7. Открытые вопросы (@Architect → владелец)

- **F5-Q1:** «до [время]» — аддитивное поле API (`active_until`/`ends_at`) или вычисление на фронте
  (риск рассинхрона/таймзоны)?
- **F5-Q2:** `limit_exhausted`/`enabled=false` — какие иконки/тексты (сейчас `🌙 Лимит исчерпан` /
  `🌙 Сон выключен`)? Сохранить или переименовать под стиль `[☀️]`?
- **F5-Q3:** свечение — существующий класс `.glow` или новый акцентный (CSS-переменная темы)?
- **F5-Q4:** breakpoint мобильного столбика (ширина) и судьба кнопки `⟳` на мобильном?
- **F5-Q5:** финальный порядок метрик в «Сводке» (метрики БД + граф в одном блоке)?

## 8. Feature flag / progressive delivery

- **Feature flag:** не требуется (render-only; аддитивное поле API безопасно). Rollback = `git revert`.
- **Progressive delivery:** неприменимо.
