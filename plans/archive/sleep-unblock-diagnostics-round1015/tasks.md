# Фича F3 — `sleep-unblock-diagnostics-round1015` (Разблокировка Сна + диагностика)

> **Статус: ✅ COMPLETED** (Step 4 @Builder, 14.09.2026). Реализация T-1567…T-1573 завершена; pytest **5632 passed / 0 failed**.
> **Раунд:** 10.15. **Нумерация:** T-1566…T-1574 (продолжает F2 T-1565).
> **Тип:** backend. **Приоритет:** **P0** (блокер синтеза убеждений).
> **Зависимости:** нет. **Конфликт файлов:** `services/dream_worker.py`; при аддитивном поле
> статуса — `web/api/memory_agi.py` (делит с F5).
> **ТЗ:** `plans/current_task.md`, раздел **2**.
> **Эпик:** `Epic: Багфиксы Графа памяти, Воркера Сна и Ностальгии round1015`.
> **Baseline:** HEAD `798e044`; pytest **5589 passed / 0 failed**; каталог **435/90/406/411/88/19**.

## 0. Цель

Разблокировать синтез убеждений: если за последние 3 дня не синтезировано ни одного убеждения —
динамически снижать пороги до `min_cluster_size = 2` / `min_importance_sum = 8`. Плюс обязательный
лог пре-гейта с причинами отказа, видимый в панели «Логи» (раздел «Статус»).

**Текущее состояние (Step 0):** пороги читаются из каталога `memory.dream_repeat_threshold` /
`memory.dream_importance_sum_threshold` (`config/settings.py:1101-1103`,
`services/param_catalog.py:1414/1419`, дефолты `3`/`12`); гейт — `services/dream_worker.py:485-514`
(`repeat_min`/`sum_min`, `qualified`, `top`); текущий лог `[dream] no qualifying clusters facts=%d`
(`:510-512`) — недостаточен для диагностики (нет chunks/clusters/max importance/threshold).
Счётчик «3 дня без убеждений» берётся по `memory_dream_log`/beliefs (зафиксировать @Architect).

## 1. Требования (дословно из ТЗ §2)

- [x] **Снижение порогов (Fallback):** если за последние 3 дня не было синтезировано ни одного
  убеждения — временно снижать требования до `min_cluster_size = 2` (вместо 3) и
  `min_importance_sum = 8` (вместо 12).
- [x] **Логирование пре-гейта:** в панель «Логи» (раздел Статус) добавить обязательный вывод причин
  отказа. Формат: `[Sleep] Chunks: 15, Clusters formed: 2, Max importance: 9 -> Skipped (threshold 12)`.

## 2. Целевые модули (`file:line` на HEAD `798e044`)

- `services/dream_worker.py:485-514` — кластеризация и гейт (`repeat_min`/`sum_min`/`qualified`/`top`).
- `services/dream_worker.py` — источник «последнего убеждения» (belief / `memory_dream_log`).
- `config/settings.py:1101-1103` — дефолты порогов; `services/param_catalog.py:1414/1419` — описания
  (если добавляются ключи fallback — санкционированный Δ каталога).
- `services/log_ring.py:168-172` — ринг-логгер (логгер `[Sleep]` должен попадать в панель «Логи»).
- `web/api/memory_agi.py` — при необходимости аддитивное поле статуса (согласовать с F5).
- Тесты: `tests/test_dream_worker.py`; новый `tests/test_sleep_fallback_round1015.py`.

## 3. Инварианты (constraints — не нарушать)

- **Лучше переиспользовать существующие ключи** `memory.dream_repeat_threshold` /
  `memory.dream_importance_sum_threshold` — базовые пороги не менять; fallback-значения (2/8) — спека
  @Architect (константа или санкционированный Δ каталога + пин-тесты).
- Каталог-инварианты **435/90/406/411/88/19** — при новых ключах только санкционированная Δ и
  обновление пин-тестов (`test_param_catalog`).
- Формат лога — ровно `[Sleep] Chunks: N, Clusters formed: M, Max importance: K -> Skipped (threshold T)`
  (и аналогично для прохождения), чтобы grep/визуальная отладка работали; без секретов (R17).
- Fallback **не должен** синтезировать мусор из единичных фактов без повторов: условие активации —
  строго «0 убеждений за 3 дня»; при появлении убеждения — возврат к базовым порогам.
- **⛔ SQLite-схему не менять**; записи — только существующими методами (`log_dream_event` и т.п.).
- Порядок роутеров `bot.py` не трогать; `media/`/`.env` не трогать.
- **Ревью-гейты:** полный `pytest` 0 регрессий, пин-тесты каталога, R17-скан, русские commits.

## 4. Зависимости

- **Вверх:** нет.
- **Вниз:** **F5** (§5 UI статуса) — использует аддитивные поля API сна/глубокого сна; воркер-логи
  отображаются в панели «Логи».
- **Конфликт файлов:** `web/api/memory_agi.py` — согласовать с F5.

## 5. Definition of Done

- [ ] Fallback срабатывает ровно при «0 убеждений за 3 дня» и понижает пороги до 2/8; иначе базовые 3/12.
- [ ] В панели «Логи» виден `[Sleep]`-пре-гейт с chunks/clusters/max importance/threshold (skipped и passed).
- [ ] Диагностика позволяет за один взгляд понять, почему LLM не вызывался (расход 0 токенов).
- [ ] Полный `pytest` — **0 failed**; каталог-инварианты не нарушены.

## 6. Чек-лист задач

- [ ] **T-1566 (@Architect, гейт):** спека fallback: как определять «0 убеждений за 3 дня»
  (источник/таймстемп), точные fallback-значения и условие возврата, окно per-chat/global, источник
  `Max importance` и `Chunks`; решить константа vs каталог-ключ; контракт `[Sleep]`-лога.
- [x] **T-1567 (@Builder):** запрос «последнее синтезированное убеждение» + флаг `fallback_active`
  (3 дня) в `dream_worker`.
- [x] **T-1568 (@Builder):** динамическое применение порогов в гейте (`dream_worker.py:491-506`):
  `repeat_min` 3→2 и `sum_min` 12→8 при активном fallback.
- [x] **T-1569 (@Builder):** пре-гейт-лог ровно формата
  `[Sleep] Chunks: %d, Clusters formed: %d, Max importance: %d -> Skipped (threshold %d)` при отказе;
  аналогичная строка `-> Passed (...)` при попадании в топ (grep-safe, без секретов).
- [x] **T-1570 (@Builder):** убедиться, что лог `[Sleep] …` доходит до панели «Логи»
  (`log_ring`, уровень INFO/WARNING, корректный логгер); при необходимости — добавить метрику
  fallback в `memory_dream_log` meta.
- [x] **T-1571 (@Builder):** unit-тесты fallback: активация при «0 убеждений 3 дня», НЕ-активация при
  свежем убеждении, возврат к базовым порогам, кластер с 2 фактами/Σ8 проходит в fallback.
- [x] **T-1572 (@Builder):** тесты формата лога (точная строка) и что `[dream]`-поведение не
  сломано при свежих убеждениях.
- [x] **T-1573 (@Builder):** прогон гейтов: полный `pytest` **0 failed**, каталог-инварианты,
  R17-скан, `git diff --check`; русский commit.
- [ ] **T-1574 (@PM/@Reviewer, гейт):** сверка DoD, проверка отсутствия ложных синтезов и допустимости
  снижения порогов (риск качества убеждений).

## 7. Открытые вопросы (@Architect → владелец)

- **F3-Q1:** «0 убеждений за 3 дня» — по `memory_dream_log` (`kind=distilled`) или по таблице beliefs?
- **F3-Q2:** fallback-пороги 2/8 — константа кода, хардкод или каталог-ключи (Δ)?
- **F3-Q3:** fallback глобальный или per-chat? Если у одного чата убеждения есть, а у другого нет —
  снижать только для «молчащего»?
- **F3-Q4:** нужен ли явный kill-switch fallback (`memory.dream_fallback_enabled`)?
- **F3-Q5:** `threshold` в логе — какой именно порог печатать при двух условиях (min size и sum)?

## 8. Feature flag / progressive delivery

- **Feature flag:** рекомендуется `memory.dream_fallback_enabled` (default ON) для быстрого
  отключения fallback без редеплоя, если пойдёт мусор. Решение — @Architect.
- **Progressive delivery:** при флаге — internal → 100%; off → базовые пороги 3/12 (прежнее поведение).
