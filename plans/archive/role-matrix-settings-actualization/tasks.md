# Фича F6 — `role-matrix-settings-actualization` (Матрица ролей: актуализация структуры настроек под реальные разделы мини-аппа)

> **Статус: ✅ IMPLEMENTED** (Шаг 4 @Builder, 15.09.2026; T-1751…T-1756 закрыты, ждёт ревью T-1757). **Итерация 4 (15.09.2026, фиксы ревью B4-3/B4-4/B4-info(a)):** ADR-номер исправлен (ADR-1018-6); parity-тест JS↔backend nav; зафиксированы 4 фактические nav-группы (есть «Прочее»). Гейты: pytest **6137 passed / 0 failed**, JS-гейты OK, каталог **436/406/411/90/88/19** (Δ=0).
> **Spec:** `spec.md` — ✅ (@Architect). **ADR:** `adr-1018-6-role-matrix-actualization.md` — ✅.
> **Раунд:** 10.18. **Нумерация:** T-1750…T-1757 (продолжает T-1749).
> **Тип:** backend/access + frontend (мини-апп). **Приоритет:** **P2** (админ-UX; функц. блокера нет).
> **Зависимости:** F1/F2/F3/F5 — **все, кто меняет каталог** (единый свод Δ). **Конфликт файлов:** `web/api/access.py`, `services/param_catalog.py`, `config/settings.py`, `web/index.html`/`web/app.js`, пин-тесты каталога.
> **Эпик:** `Epic: BetterStack-Sleep-Graph round1018` (@Memory, Step 0).
> **ТЗ:** `plans/current_task.md` **§5** (строки 89–90) — «Актуализировать в разделе Матрица ролей структуру настроек согласно тому как они реально распределены в разделах миниапп».
> **Baseline:** HEAD `118a03c`; pytest **6007 passed / 0 failed**; каталог **435/406/411/90/88/19** (ожидается санкционированный Δ); SQLite **v9**; APP_VERSION 2.57.0.

## 0. Цель

Привести раздел «Матрица ролей» в админке в соответствие с **фактическим** распределением настроек по разделам мини-аппа (заголовки/группы/табы), чтобы матрица не расходилась с реальной структурой UI и RBAC.

**Требуется (ТЗ §5):**
- Структура настроек в «Матрице ролей» = реальное распределение по разделам мини-аппа.
- Соответствие RBAC/табов фактическому UI.
- Обновление каталог-инварианта и пин-тестов (санкционированный Δ).

## 1. Контекст и доказательства (@Memory Step 0)

- **ТЗ §5 сформулировано кратко и неоднозначно** («согласно тому как они реально распределены») → нужен **human-gate** (см. §7 R1): какой раздел/набор считать эталоном «реального распределения».
- **Каталог-инвариант завязан жёстко:** `TAB_RULES` **19** / `GROUPS` **90** / `mapped` **88** — покрыты **пин-тестами**.
- Точки: `web/api/access.py:319-360` — `param_permissions_list`, `group_tab`, `CONFIG_TAB_TITLES`.
- Перестройка матрицы = **санкционированный Δ каталога** (значения Δ фиксируются ADR + пин-тестами).
- Новый env-хост из F1 также даёт **+1** к REGISTRY (435→436) — сводить Δ согласованно, чтобы не «разъехались» ожидания пин-тестов.

## 2. Требования

- [x] Зафиксирована целевая структура «Матрицы ролей» = фактическая структура разделов мини-аппа (эталон `web/app.js`: 3 nav с настройками — Модули 11 / ИИ 7 / PERMsoc 1).
- [x] `param_permissions_list`/`group_tab`/`CONFIG_TAB_TITLES` соответствуют целевой структуре (аддитивные `nav`/`nav_title`/`nav_order`; подпись `permsoc` исправлена).
- [x] RBAC-права табов согласованы (view/edit), без «мёртвых»/недостижимых прав (каждый tab_id имеет ≥1 группу).
- [x] Пин-тесты каталога обновлены на санкционированные Δ (Δ=0: 436/406/411/90/88/19).
- [x] UI мини-аппа отображает матрицу согласно новой структуре (nav → секция → группа); без регресса существующих разделов.

## 3. Constraints (инварианты раунда)

- **R16** (id — ключ), **R17** (без секретов).
- **Каталог-Δ — только санкционированный** (ADR фиксирует точные числа: TAB_RULES/GROUPS/mapped/REGISTRY); остальное не трогать.
- **SQL/DDL:** не требуется (каталог — код/конфиг).
- **Порядок роутеров `bot.py` не менять**; `media/`/`.env` **не трогать**.
- **Ревью-гейты:** полный `pytest` 0 регрессий (в т.ч. пин-тесты каталога/RBAC); `node --check web/app.js`; JS-гейты; `git diff --check`; русские conventional commits.

## 4. Зависимости / порядок

- **Вверх:** T-1750 (@Architect) — целевая структура + ADR со значениями Δ. **Human-gate:** подтверждение эталона «реального распределения» (@PM → владелец).
- **Вниз:** —.
- **Порядок:** **последняя** (P2); сводит все Δ каталога (F1 +1, возможно F2/F5) в единый обновлённый инвариант.

## 5. Definition of Done

- [x] «Матрица ролей» соответствует фактическим разделам мини-аппа (эталон — `web/app.js` `NAV_ITEMS`/`TABS[].menu`; 3 nav: Модули/ИИ/PERMsoc).
- [x] RBAC/табы синхронизированы; недостижимых прав нет.
- [x] Пин-тесты каталога обновлены на санкционированные Δ (Δ=0); полный `pytest` **0 failed** (6137 passed).
- [x] UI мини-аппа рендерит обновлённую матрицу (nav-группы); регрессов нет (JS-гейты OK).

## 6. Чек-лист задач

- [x] **T-1750 (@Architect, гейт):** `spec.md` + ADR — целевая структура «Матрицы ролей» = реальные разделы мини-аппа; точная карта `CONFIG_TAB_TITLES`/`GROUP`/`TAB_RULES`; санкционированные Δ (Δ=0) и RBAC-политика.
- [x] **T-1751 (@Builder):** аудит фактического распределения настроек по разделам мини-аппа (`web/api/access.py:319-360`, `param_catalog.py`, `CONFIG_TAB_TITLES`) — зафиксирован дрейф `CONFIG_TAB_TITLES[TAB_PERMSOC]` = «Функции PERMsoc» vs факт «PERMsoc».
- [x] **T-1752 (@Builder):** структура матрицы (каталог/группы/табы) приведена к целевой карте: `TAB_NAV`/`NAV_TITLES`/`NAV_ORDER` + `tab_nav()`; подпись исправлена.
- [x] **T-1753 (@Builder):** RBAC/права и `TAB_RULES`/`group_tab` синхронизированы; недостижимых прав нет (тест `test_every_nav_has_sections`).
- [x] **T-1754 (@Builder):** пин-тесты каталога обновлены на санкционированные Δ (Δ=0: 436/406/411/90/88/19); добавлены тесты целостности матрицы (`TAB_NAV`/паритет подписей) и parity-тест JS-зеркала `NAV_GROUP_ORDER`/`NAV_GROUP_TITLES` ↔ backend `NAV_ORDER`/`NAV_TITLES` (B4-4).
- [x] **T-1755 (@Builder):** UI мини-аппа — `matrixSections` группирует по nav → секция → группа; `web/index.html` рендерит nav-заголовки; адаптив сохранён.
- [x] **T-1756 (@Builder):** гейты — полный `pytest` 6137 passed / 0 failed, JS-гейты (`node --check`/JS-UNIT/VUE-MOUNT) OK, свод Δ каталога (436/406/411/90/88/19), R17-скан чист, `git diff --check` OK.
- [ ] **T-1757 (@Reviewer + @PM, гейт):** сверка DoD с эталоном, целостность RBAC/табов, отсутствие регресса каталога.

## 7. Риски / ADR-конфликты

| # | Риск / конфликт | Мера |
|---|---|---|
| R1 | **ТЗ §5 неоднозначно** («реально распределены» — какой эталон?) → риск угадывания | **Human-gate:** @PM подтверждает эталон структуры у владельца до реализации T-1752 |
| R2 | **Перестройка ломает каталог-инвариант** (TAB_RULES 19 / GROUPS 90 / mapped 88, пин-тесты) | Санкционированный Δ в ADR T-1750 + обновление пин-тестов T-1754 |
| R3 | Конфликт Δ с F1 (+1 REGISTRY) и возможными Δ F2/F5 | Единый свод Δ в T-1756; координация |
| R4 | RBAC-политика (view/edit) может стать недостижимой | T-1753 + тесты T-1754 + ревью T-1757 |
| R5 | Регресс UI мини-аппа (разделы/адаптив) | T-1755 + JS-гейты + ревью |

**Требуется новый ADR:** да — ADR-1018-6 «Role matrix actualization + sanctioned catalog Δ».

## 8. Feature flag / progressive delivery

- **Feature flag:** не требуется (структура админки). Rollback = `git revert`.
- **Progressive delivery:** неприменим; проверка в админке (десктоп + мобильный).

## 9. Handoff / деплой

`@Orchestrator` — план F6 готов. Спека/ADR — T-1750 (@Architect). Реализация — T-1751…T-1756 (@Builder). **Деплой (SSH pull + `.env` при необходимости + `systemctl restart admin_bot` + проверка админки) — отдельный шаг @DevOps. Пароль сервера в репозитории НЕ хранится.**
