# Архив — S4 `summary-fact-package-round1026` (Эпик 2 «Summary Hybrid Pipeline», раунд 10.26)

- **Статус:** ✅ **DONE / VERIFIED (автономный контур)** — @Reviewer **Approved** (T-3300; оба обязательных ленза — требования/корректность и focused change-audit; C0/H0, блокирующих Medium 0; `review.md`), @Scanner **C0/H0/M1-процесс(закрыт `review.md`)/L2/I2** (T-3301; `plans/reports/round1026_s4_scanner_audit.md`). Закрыты T-3283…T-3303 (baseline/spec+ADR/сверка/Builder/ревью/аудит/merge); архивация — Шаг 8 @PM (T-3304, 23.09.2026). **Автономная приёмка не заменяет live.**
- **Деплой:** ⏳ **после T-3305 @DevOps** — deploy = **ДА** (ADR-1026-6 D6; NOT_APPLICABLE отклонён), bump `APP_VERSION` **2.58.22 → 2.58.23** + `README.md` + cache-bust; `/api/health` 200, `database is locked`=0, hot-OFF не требуется (живой путь не тронут). Прод пока на 2.58.22.
- **Live:** ⏳ **PENDING OWNER VERIFICATION** — реальный Telegram WebView; врезка `L1Result → пакет → L2` — **GATED (S5/S6, ADR-1025-24 D4)** и намеренно вне diff (модуль вызывается только тестами).
- **Ключевые числа:** pytest `.venv` **8737/0** (baseline 8685, **+52**), JS **43/43**; каталог **468/426/443** без Δ (GROUPS 100 / `_TAB_BY_GROUP` 98 / TAB_RULES 21; F8 не переиздавался — N/A); **Δ DDL=0** (SQLite v12); **0 LLM-вызовов** (ровно 2 вызова живого пайплайна не затронуты); `services/summary_fact_package.py` + 52 теста.
- **Follow-up → S5:** L-R1026S4-1 (char-обрезка фрагмента вне `truncated`/WARN), L-R1026S4-2 (O(n²) `_enforce_budget`), L-R1026S4-3 (§9/ADR D5 «проброс `skipped_ids`/`skipped_tg_ids`» — счётно, не id-списками), I-R1026S4-1 (`unassigned` не усекается бюджетом), I-R1026S4-2 (`invalid`/`error` без `invalid_reason`). Все — Low/Info, не блокеры.
- **Откат:** `git fetch --tags origin; git reset --hard pre-round1026-s4` (annotated → `59f5921`); hard — `git revert`; бэкап `var/backups/s4-round1026-20260923-202326/`, `.env.bak.round1026-s4`. Теги/бэкапы/`stash@{0}` не удалять (R18).
- **Открытые шаги:** **T-3305** (deploy @DevOps) → **T-3306** (метрики 10.26-S4 @Memory) → **T-3307** (handoff @Orchestrator → **S5 `summary-l2-writer-formatter`**). Врезка L1/пакета/L2 и слот/UI L2 — **S5**.
- **Состав:** `spec.md` (SC-01…SC-10), `adr-1026-6-fact-package-contract-deterministic-s5.md` (D1–D6, Accepted), `tasks.md` (T-3283…T-3307, блоки 0/A–H), `evidence.md`, `review.md`. Чекбоксы `tasks.md` сохранены как в рабочей папке; закрытие T-3300/T-3301 подтверждено артефактами.
