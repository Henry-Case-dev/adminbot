# Задачи: global-oversight-dashboard

Раунд 10 (07.09.2026), **часть 3.1 (панель глобального админа) эпика** «Multi-chat scaling» (research `plans/docs/multi-chat-scaling-research.md`, вариант A). Нумерация продолжает T-913 → **T-914…T-924**; база HEAD fac1b9f. Скоуп — **Global Oversight Dashboard** (только Global Admin): сводка всех чатов (активность/Opt-In/тяжёлые фичи/состояние ключей own/global), kill-switch «выключить фичу удалённо» per chat и «запретить глобальный ключ для чата», таблица/карточка + модалка-детали; API + TMA. Spec.md при планировании НЕ создаётся; A1 (Q1–Q5) закрывает @Architect правкой в разделе A. Связанные части: `multi-chat-rbac-byok` (T-843…T-869) — роли/доступы/статусы ключей, `feature-gates-worker-budget` (T-890…T-902) — гейт-состояния, `permsoc-module-isolation` (T-878…T-889) — мастер-флаг PERMsoc, `tma-ia-progressive-disclosure` (T-903…T-913) — точка входа «Главная/Статус».

## Контекст (Step 0; факты проверены @PM при планировании по HEAD fac1b9f)

RBAC: `web/api/deps.py` — `get_tma_user` (:80), `requires_permission(required)` (:182), `can_edit_param` (:198-206); chat-доступ: `web/api/chat_lore.py` `can_access_chat` (глобальный admin/moderator-секция/chat_admins; 403). Состояния: `chat_profiles` (is_active, gates/opt-in — фича 2.3), воркеры-гейты (feature-gates 2.3: dream/nostalgia/lore_auto), `chat_keys` (T-843-часть-1: per-chat свой ключ), `keys.llm_api_key` глобальный (hot.get — только глобальная резолюция): «запрет глобального ключа per chat» — отсутствует (часть-1 вводит только бюджеты; здесь — тумблер-запрет). Уроки-прецеденты: optimistic 409/транзакция+NOTIFY+история (ChatLoreStore, T-772), кэш NOTIFY/TTL (T-774), кастомная вкладка memory_rag (app.js :33-37), фронт-аудит test_frontend_tab_mapping.py. RUNTIME: SQLite-схема/история — вне скоупа; PG — аддитивно.

## A. Дизайн (PM-база; Q1–Q5 закрывает @Architect в T-914)

- [ ] **T-914 (A1):** @Architect — протокол дизайна: (Q1) **состав сводки**: поля таблицы/карточки чата (PM-база: chat_id/имя (T-873-прецедент имени), is_active, gates_opt_in, 3 тяжёлых фичи (биты), permsoc_enabled, ключ: own/global/запрет, кол-во локальных админов (chat_admins JOIN), последняя активность (SQLite smart_messages max-через database GET — дешёвая агрегация), budget-day для чата (worker_budget)); источник данных — единый сервис-сводки `services/oversight.py` реализация GET (зависимости: PG-профили + SQLite-агрегаты) vs SQL-поднятие через PgDatabase + отдельные DatabaseService-методы — по сок-производительности (десятки чатов, кэш 30-60 с + refresh-кнопка); (Q2) **kill-switch**: сущность-хранение «выключить фичу удалённо» (PM-база: `chat_params['gates.<feature>']=false` — приоритет выше локального; история-аудит — field='gates' в chat_lore_history в той же транзакции, changed_by=telegram_id; undo-восстановление); «запретить глобальный ключ»: PM-база `chat_params['keys.allow_global']=false` (модель-вариант: колонка `chat_profiles.allow_global_key BOOLEAN NOT NULL DEFAULT TRUE` — решение: chat_params-простота против SQL-языка — Q2); семантика: запрет глоб. ключа + нет своего → бот не тратит токены (прямой исчезает: «нет ключа»-по-бюджету-путь — согласовать с part-1 D2/T-849); (Q3) **права**: только глобальный admin (роль admin/wildcard; moderator/user → 403); read/write kill-switch: здесь vs через part-1 `PUT /api/chat/{id}/gates` (разделение: часть-1 — обычное переключение через RBAC-доступ; здесь — глобальное, с приоритетом override-записи; единая запись — Q3: общая модалка или отдельный эндпоинт) — не плодить конфликтующие-пути: согласовать порядок приоритетов гейтов (вернуть к part-1 gates: local > global-admin?); (Q4) **ничка** (fill): таблица-загрузка: polling каждые 30-60 с vs refresh-кнопка (PM-база: refresh-кнопка + ручной интервал 60 с разрешителен — дешёво), фильтр-поиск по названию/id, сортировка (is_active, opt-in, статус ключа); модалка-детали: всё, что в части-1/four-5 (админы-список, гейт-значения, параметры чата, история-последние 5 событий); (Q5) **инвентарь**: точки (часть-1 сервис-список чатов, SET-методы chat_params/ChatLoreStore-путь, рынок TMA: Главная-карточка (tma-ia), методы app.js, тесты-аудит, RUNTIME-проверки: PG down → fail-open-карточка-ошибка без падений). Правки задач — пометками `(изменено @Architect T-914)`.

## B. Сервис + API

- [x] **T-915 (B1):** `services/oversight.py` (НОВЫЙ): `build_summary(limit) -> list[ChatSummary]`: profile/chat-имя (название чата из runtime-источника: getChatAdministrators/локальный кэш чатов — Q1), gates/opt-in, permsoc_enabled (из part-3 фичи), ключ-статус (own/global+allow), админы-COUNT, активность (последний раз из SQLite-агрегаты — дешёвый запрос, кэш 60 с — прецедент ChatLoreCache/access-кэш), budget-day (part-4 worker_budget GET per chat); `set_killswitch(chat_id, feature, state, changed_by)` (запись chat_params-гейт + история field='gates' + NOTIFY per-часть-1; подтверждение-реверт undo), `set_global_key_policy(chat_id, allow)` (как Q2) + `get_for_global_admin()`; fail-open/кэш. (services/oversight.py — НОВЫЙ)
- [x] **T-916 (B2):** API (web/api/oversight.py — НОВЫЙ, include после api_router; паттерн web/api/chat_lore.py: Depends(get_tma_user), глобальный admin-гейт 403): `GET /api/oversight/summary` (список + refresh-идентификатор, права Q3), `GET /api/oversight/chat/{chat_id}` (детали чата — ролы/админы/гейты/параметры), `POST /api/oversight/chat/{chat_id}/killswitch` {feature, enabled} (409 optimistic по Q2/409-знаком; 422-валидация feature из списка: dream/nostalgia/lore_auto/permsoc; undo — тот же эндпоинт с enabled пред-значением/подтверждение), `POST /api/oversight/chat/{chat_id}/global_key` {allow: bool} (запрет/разрешение), `GET /api/oversight/budget` (итог-день: global + per-chat — из part-4). 401 без initData, 403 не-global, 404 неизвестный чат, 422/409, R17 (секреты/ключи не отдавать). (web/api/oversight.py, web/app.py)

## C. TMA

- [x] **T-917 (C1):** карточка-вход «Oversight» на «Главная/Статус» (global admin; гейт-видимость hasPerm) + вкладка/меню-пункт (по tma-ia: меню-тег «Главная» — пере-использовать; если не реализовано — временная карточка в «Статус»): таблица чатов (columns: чат (имя+id), активность, Opt-In, тяжёлые (3 бейджа), PERMsoc, ключ: own/global/«запрещён», бюджет-день, админ-COUNT) + «Refresh» + поиск-фильтр (имя/id); сортировка-кликами минимально (или по a1-порядку). (web/index.html, web/app.js)
- [x] **T-918 (C2):** модалка деталей чата (клик по строке): details: гейт-значения (toggle-стрелки-состояние + «выключить удалённо» kill-switch кнопки (confirm-модалка «фича выключится для всех участников — продолжить?»), ключ-политика: кнопка «Запретить глобальный ключ»/«Разрешить» (confirm: «если у чата нет своего ключа — бот перестанет отвечать/тратить»), список админов (из part-1 API access), последние 5 событий-истории (chat_lore_history-фильтр); undo-кнопки после kill-switch (из последних-действий); тосты/ошибки как соседи (401-редирект, 403 toast). (web/index.html, web/app.js)

## D. Тесты

- [x] **T-919 (D1):** сервис/API: build_summary (мок-данные: PG-профили + SQLite-агрегаты: состав корректен, кэш-инвалидация после записи), kill-switch: запись chat_params-гейт + история + NOTIFY; повторное обращение → 409?/200 с телом актуального; undo; ключ-политика: allow=false + no own → llm_client резолв-путь (мок: fallback к глобальному НЕ происходит — «нет ключа»-ответ, директива part-1 D2/T-849 — интеграционный кейс); API-матрица: 401 без initData, 403 moderator/user/local, 404, 422 невалидный feature, 200 global; бюджет-сводка (mock). (tests/test_oversight.py — НОВЫЙ; тесты-расширения part-1-reshah)
- [x] **T-920 (D2):** TMA-аудит (test_frontend_tab_mapping.py-стиль): маркеры-строки (входы: summary-методы, killswitch/global_key методы, поиск-поле, модалка), обновление инвентарей; полный pytest → 0 failed; `git diff --check`; grep-секретов (R17: ключ-значения не приходят); ручная сверка (данные-поток, подтверждённые confirm-модалки). (tests/, web/)
- [x] **T-921 (D3):** финальный регресс раунда: полный pytest → 0 failed (все части), `git diff --check`, проверка блокировки (per-часть-1/X-Chat-Id) без изменений. (tests/)

## E. Деплой/README/отчёт

- [ ] **T-922 (E1):** README-раздел «Global Oversight Dashboard (раунд 10)»: что показывает, kill-switch-семантика (приоритеты гейт-записей: глобальный-admin-override > локальный), «запрет глобального ключа» (последствия: чат с ключом-своим продолжает, без ключа — молчание/бюджет-путь), API-эндпоинты кратко, как пользоваться (вход на Главная). Полный pytest → 0 failed; grep-секреты; `git diff --check`. (README.md) *(@DevOps финальный шаг — вне архива)*
- [ ] **T-923 (E2):** @DevOps: экспорт-шаг раунда: удостовериться (гейты/бюджеты — в журнале), бэкап, рестарт; live-верификация: POST killswitch (лог-строка «oversight: killswitch …», undo), global-key-запрет (чат с own-ключом — работает; без — «нет ключа»-путь), сводка (страница спокойно грузится, корректы-данные), error-traceback COUNT = 0; отчёт-в-результаты. (прод) *(@DevOps финальный шаг — вне архива)*
- [ ] **T-924 (E3):** отчёт юзеру + закрытие всей части-3.1: аппрув @Reviewer; архивация `plans/features/global-oversight-dashboard/` → `plans/archive/`; правка plans/backlog.md (раунд: часть-закончена+архив — как в прецедентах раундов 5–9); ARCHITECTURE.md — раздел (решает @Architect, практика раундов 7–9); финальный memory-sync docs-коммит. Отчёт: дашборд-описание, kill-switch-функции, что НЕ тронуто (SQLite-память/интерактив-настройки и др. — RUNTIME WARNING — см. выше). (plans/, README.md) *(@DevOps/@Memory финальный шаг — вне архива)*
