# Фича F5 — `security-rotation-finalize-round1016` (Ротация секрета + финализация коммита/деплоя)

> **Статус: ✅ COMPLETED** (14.09.2026; @Reviewer APPROVED, итерация 2). Реализованы T-1660/T-1662/T-1663 (git-гигиена, скан секретов, README/R17, чек-лист ротации); открыты @DevOps-гейты T-1661/T-1664 (SSH-ротация/деплой) и T-1665 (@PM — архив). Прод-код не менялся (Δ=0).
>
> **⚠️ CANCELLED (UPD3 §4, раунд 10.17 `ssh-rotation-cancelled-round1017`):** по решению владельца ротация SSH **не требуется** — секреты в untracked `plans/current_task.md` являются нормой, файл не попадает в репозиторий (утечки в истории нет — скан 10.16 чист). Гейты **T-1661/T-1664 закрыты ОТМЕНОЙ, а не выполнением** и **не являются открытыми действиями**. Артефакты фичи сохранены как историческое свидетельство (Step 8 archive), без удаления/переписывания.
> **Spec:** [`spec.md`](spec.md).
> **Раунд:** 10.16. **Нумерация:** T-1660…T-1665.
> **Тип:** security + docs. **Приоритет:** **P0 (безопасность)**.
> **Зависимости:** **F1–F4** (финализация выполняется после реализации; ротация может идти параллельно).
> **Конфликт файлов:** `README*`, `.gitignore`, `plans/current_task.md` (**не коммитить**), git-гигиена.
> **Эпик:** `Epic: Багфиксы + полный аудит round1016`.
> **ТЗ:** `plans/current_task.md` **UPD2** (строка 150: «Проверь что current_task.md не попал в репозиторий»); техдолг **R17 / Info 3** из 10.15.
> **Baseline:** HEAD `18a9aa1`; pytest **5774 passed / 0 failed**; каталог **435/90/406/411/88/19** (Δ=0); SQLite **v9**; APP_VERSION 2.57.0.

## 0. Цель

1. **Проверка git-гигиены:** `plans/current_task.md` **не отслеживается** git (`.gitignore:70`), никогда не коммитился — подтверждено `git ls-files --error-unmatch` (ошибка «did not match»). Повторно подтвердить и защитить от случайного коммита.
2. **Ротация секрета:** рабочая копия `plans/current_task.md` содержит SSH-доступ/пароль (`:62-65`) → **рекомендована ротация/отзыв** пароля (repo-wide R17-долг; пароля в истории нет, но рабочая копия его содержит). — ⚠️ **ОТМЕНЕНО (UPD3 §4, раунд 10.17):** секреты в untracked-доке — норма, ротация **не требуется**.
3. **Финализация:** README/документация безопасности; подготовка к коммиту/деплою (шаги 9–10).

## 1. Доказательства (`file:line` на HEAD `18a9aa1`)

- `plans/current_task.md:61-76` — деплой-инструкция с `ssh nik@…` (`:63`) и паролем (`:65`); **файл не в git** — `git ls-files --error-unmatch plans/current_task.md` → «did not match».
- `.gitignore:70` — `plans/current_task.md` явно исключён.
- Техдолг 10.15 (backlog, блок «ИТОГ 10.15» / Info 3): repo-wide R17-долг — рекомендована **смена/отзыв пароля и очистка истории** (к 10.15 не относился).
- `plans/MEMORY.md` (M, untracked-изменение) — не коммитить вместе с секретами; сверить статус.

## 2. Требования (UPD2 §0 / R17)

- [x] Подтверждено: `plans/current_task.md` не в git (рабочее дерево + история).
- [x] Секрет из рабочей копии **не попадает** в репозиторий; рабочая копия защищена (`.gitignore`, предупреждение).
- [ ] Пароль SSH **ротирован/отозван** на сервере (вне репо, @DevOps) + проверка нового доступа. — ⚠️ **CANCELLED (UPD3 §4)**: не требуется.
- [x] README/документация: правила секретов (R17), порядок деплоя без плейнтекст-пароля.
- [x] Подготовлены шаги финализации коммита/деплоя (Step 9–10).

## 3. Constraints (инварианты раунда)

- **R17:** никаких плейнтекст-секретов в репо, логах, коммитах; секреты — `{configured,last4}`.
- **Не коммитить** `plans/current_task.md`; не выполнять `git add -f` на игнорируемые файлы.
- Ротация/перемены на сервере — **@DevOps, вне репо** (`media/`/`.env` не трогать).
- Каталог **Δ=0**; **SQL/DDL не требуется**; порядок роутеров `bot.py` не менять.
- **Ревью-гейты:** R17-скан (`grep` по паттернам секретов в staged), `git diff --check`; русские commits.

## 4. Зависимости / порядок

- **Вверх:** **F1–F4** (финализация после их завершения). **Вниз:** нет.
- **Порядок:** **последняя** фича раунда (перед/вместе с деплоем Step 9–10). Ротацию можно начать параллельно.

## 5. Definition of Done

- [x] Подтверждена git-гигиена `current_task.md` (не отслеживается, истории чист).
- [ ] Пароль ротирован/отозван, новый доступ проверен (@DevOps), секрет вне репо. — ⚠️ **CANCELLED (UPD3 §4)**: ротация не требуется (секрет и так вне репо).
- [x] README/доки обновлены; R17-скан чист.
- [x] Шаги 9–10 (commit/push/deploy) подготовлены; EPIC-метрики готовы к Step 10 @Memory.

## 6. Чек-лист задач

- [x] **T-1660 (@PM/@Architect, гейт):** политика ротации секрета и финализации (что ротируем, где храним, как проверяем); согласование с R17.
- [ ] **T-1661 (@DevOps):** ⚠️ **CANCELLED (UPD3 §4)** — не открытое действие: ротация/отзыв SSH-пароля сервера `198.46.175.136`, проверка нового доступа; **вне репо**; отчёт без секретов.
- [x] **T-1662 (@Builder):** верификация git-гигиены: `git ls-files`/`git log` по `current_task.md`; скан истории на утечку секрета; hardening `.gitignore`/pre-commit (при необходимости) от случайного коммита.
- [x] **T-1663 (@Builder):** README/документация безопасности (R17, деплой без плейнтекст-пароля, хранение секретов); чистка рабочей копии `current_task.md` от пароля (по согласованию с владельцем — Builder файл не трогает).
- [ ] **T-1664 (@DevOps/@PM):** ⚠️ **CANCELLED (UPD3 §4)** — не открытое действие (деплой 10.16 фактически выполнен commit `eb3fd4a`; ротация отменена): подготовка Step 9–10: commit/push (русский conventional commit), прод fast-forward, restart, live-верификация (`/api/health`=200, `/api/memory/*`=401), отчёт простыми словами.
- [ ] **T-1665 (@PM, гейт):** итоговая сверка DoD эпика; архивирование фич (Step 8) и передача метрик (Step 10 @Memory).

## 7. Открытые вопросы (@Architect → владелец)

- Согласование ротации: допустим ли перевод на SSH-ключи вместо пароля?
- Нужен ли `git filter-repo`/очистка истории? (пароль в истории **не найден** — возможно, неприменимо).
- Кто хранит рабочий `current_task.md` (владелец) и допустимо ли вычистить из него пароль в рабочей копии?

## 8. Feature flag / progressive delivery

- Не применимо (security/ops). Rollback ротации — возврат прежнего ключа/пароля до отзыва (окно по политике).
