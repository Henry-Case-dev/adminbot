# Раунд 9 / Фаза 2 (эпик 06.09.2026): AGI Memory Implementation (2a A-Life Отношения + dig_into_lore → 2b «Сон» beliefs → 2c Ностальгия)

Планирование раунда 9 (06.09.2026, продолжает нумерацию T-814 → **T-815…T-838**; база HEAD 84c4887 — раунд 8 задеплоен; дерево на момент планирования: `M plans/docs/memory-project-overview.md`). Эпик = реализация **Фазы 2** research `plans/docs/agi-memory-research.md` (§3–6, §7, §10) по апруву владельца «AGI Memory уточнена» (п.1 запроса 06.09): 2a «Отношения (A-Life) + dig_into_lore» → 2b «Сон» (beliefs) → 2c «Ностальгия» (слой A, затем B). spec.md при планировании НЕ создаётся (по ТЗ владельца; прецедент раунда 8); раздел A настоящего файла зеркалит дизайн-решения (PM-база) и протокол открытых вопросов Q1–Q14, которые @Architect закрывает в T-815 ПРАВКОЙ в разделе A (строки-решения; если правки меняют требования задач — пометкой `(изменено @Architect T-815)` в тексте задачи).

**Решения @PM по структуре эпика (владелец):**
- **ОДНА реализация + ОДИН деплой**: все три подфазы 2a/2b/2c исполняются в рамках одного эпика одним @Builder (порядок B→C→D→E→F по дереву задач ниже), затем один деплой раунда. Это быстрее отдельного деплоя между подфазами (общий контекст, один прогон регресса, один прод-окно); риск-митигация — каждая подфаза отключаема собственными тумблерами (флаги по умолчанию консервативные: НЕ меняют поведение прода без явного включения; единственное неизбежное изменение поведения — промпт-канон R9 и миграция v8 в 2b). Группировка задач по подфазам сохраняется (B–C = 2a, D = 2b, E = 2c) — для контроля прогресса и ревью.
- **Ограничения владельца (жёсткие):** (1) локальная LLM (Ollama qwen3.5:9b) НЕ используется в боевых воркерах — «сон»/ностальгия/отношения работают только на облачных моделях через существующий LLMClient (apinet/deepseek + fallback, путь обычных ответов бота); (2) настройки A-Life/отношений живут ВНУТРИ раздела «Лор чатов» (PG `chat_profiles`, per-chat) — новый TMA-раздел «Отношения» НЕ заводится; блок «Участники и отношения» — во вкладке «Лор чатов», блоки «Синтез (сон)» и «Ностальгия» — во вкладке «Память и RAG».

**RUNTIME WARNING (критично; обязателен @Architect, @Reviewer, @DevOps):** SQLite-схема на проде и локально — user_version **v7** (обе БД содержат историю: smart_messages ~1.9–2.2M). Правила: (1) **Единственная миграция эпика — v8** (rebuild `graph_facts`: колонки `importance`/`source_ids`/`kind`/`belief_meta` + CHECK origin += `'derived_belief'`, PRAGMA user_version=8) — только в фазе 2b (задача D1); исполняется в коде 2a-задач НЕ ссылается на новые колонки (B–C работают на v7). (2) Новые таблицы (2a `users_meta`, 2b `memory_dream_log`, 2c `nostalgia_log`) — только идемпотентные `CREATE TABLE IF NOT EXISTS` в init SQLite **без bump user_version** (образцы `bot_reply_parents`/`chat_summary_levels` в `_SCHEMA_SQL`, database.py:239-258). (3) PG-дельта — только аддитивная: `ALTER TABLE chat_profiles ADD COLUMN IF NOT EXISTS relations JSONB` в `DDL_STATEMENTS` (`services/pg_db.py:33-121`, идемпотентный DDL при старте) — без иных PG-структур. (4) Промпт-канон R9 меняет `CHAT_SYSTEM_PROMPT` → обязательны PREV-слепок (`PREV_R9_CHAT_SYSTEM_PROMPT` = текущий канон R8 без правок), ступень в `PROMPT_MIGRATIONS` (существующие LEGACY/PREV/PREV_R8-ступени не трогать), байт-тесты (`tests/test_direct_chat_prompts.py`, `tests/test_prompt_migrations.py`), авто-миграция PG при деплое (H2). (5) Запрещено: правки `tools/history_import/`, `manage.py`, существующих миграций v2–v7, `local_database_*.db*`-файлы (не удалять/не блокировать), смена порядка роутеров bot.py. (6) Воркеры «сна»/«ностальгии»: каркас `memory_maintenance.py` (AsyncIOScheduler + IntervalTrigger + `max_instances=1` + `coalesce`), состояние — в SQLite, PG-advisory-lock НЕ нужен (он только у `lore_worker`); старт в bot.py on_startup рядом с lore-блоком (:459-486). (7) Флаги/лимиты — hot-ключи (`hot.get(key, settings.X)`), дефолты консервативные (боевые подфичи — off, кроме `dig_into_lore`-контракта в каноне, который живёт всегда, но тратит раунды только по вызову модели).

**Статус:** реализовано, ожидает деплоя (T-837) — заархивировано 06.09.2026 (@PM Archive Phase). Полный pytest **4562 passed / 0 failed**; аппрув @Reviewer; архитектура — `ARCHITECTURE.md` §21; закрыто по ревью: T-815..T-835 [x]. Вне архива (исполняет @DevOps финальным шагом в T-836..T-838): README-раздел «AGI Memory (Фаза 2, раунд 9)» (T-836), деплой раунда: коммит (код+тесты+plans/backlog.md+архив plans/archive/agi-memory-implementation/) → пуш → сервер `git pull --ff-only`, бэкап SQLite-БД (снапшот перед rebuild), при старте миграция v8 `graph_facts` (PRAGMA user_version=8) + идемпотентные CREATE IF NOT EXISTS (`users_meta`/`memory_dream_log`/`nostalgia_log`) + PG-DDL (`chat_profiles.relations` JSONB) + авто-миграция канона `prompts.direct_chat_system_prompt` → R9 (migrate_prompt_canons, PREV_R9-ступень), рестарт, live-верификация (T-837), отчёт юзеру и закрытие (T-838).

## Контекст (Step 0; факты проверены @PM при планировании по HEAD 84c4887)

**Tool calling direct_chat (2a/dig).** `services/tool_schemas.py`: `TOOL_CALLING_TOOLS` (:53) = 2 элемента (`TOOL_EXECUTE_WEB_SEARCH` :8, `TOOL_QUERY_CHAT_MEMORY` :27); добавление 3-го элемента — по образцу. `services/tool_router.py`: registry (:102-105) + `_query_chat_memory` (:141-202) — образец ветки (FTS по L1 `search_long_term` → dict-нормализация (T-678!) → count_mentions best-effort → vector_search → гибридный RAG-фолбэк → `_truncate` к `_MEMORY_MAX_SYMBOLS`, «ничего не найдено» при пусто; НИКОГДА не бросает); helpers `_require_query` (:206), `_resolve_name` (:212, каскад aliases). Цикл `chat_with_tools` (`services/tool_loop.py`): TOOL_MAX_ROUNDS=4, ≤2 tool_calls/раунд, деградация при 2xx-отказе. Схема `dig_into_lore` — параметры по research §4 (query, year, person, mode messages|facts|both).

**Канон/миграции промптов.** `services/chat_prompts.py`: `CHAT_SYSTEM_PROMPT` (:85-109) — канон R8 (КАК ЧИТАТЬ КОНТЕКСТ :86, СИСТЕМНАЯ РОЛЬ :88, ПРИОРИТЕТЫ :91-95, ПРАВИЛА ОФОРМЛЕНИЯ :97-100, **ИНСТРУМЕНТЫ :102-106** — два тула с контрактами, ГЛАВНОЕ ОГРАНИЧЕНИЕ 1-2 предложения :108-109); `PREV_R8_CHAT_SYSTEM_PROMPT` (:66-83) и `LEGACY_/PREV_`-слепки — байт-эталоны, НЕ трогать. `services/prompt_migrations.py`: `PROMPT_MIGRATIONS["prompts.direct_chat_system_prompt"]` (:59-62) — 3 ступени (LEGACY→CHAT, PREV→CHAT, PREV_R8→CHAT); `migrate_prompt_canons(cache)` (:80+) — авто-миграция PG при старте; runtime: `hot.get("prompts.direct_chat_system_prompt", CHAT_SYSTEM_PROMPT)` (direct_chat_service.py:319-321).

**Сборка контекста direct_chat (инжекты 2a/2c).** `direct_chat_service.py`: `_build_user_content` (:589-666) — блоки map→(branch)→rag→global→thread→target→(protected+lore)→mood→current→anchors→sandwich; `<chat_lore>` сразу после protected (:639-646); `_apply_context_budget` (:795-896) — uncuttable kinds `target/protected/lore/current/sandwich` (:812), per-block доли, порядок урезания; `_render_current_question` (:668). RAG direct-пути: `memory.get_rag_facts` (summary_memory.py:1810-1832, 3-кортежи rel-порядка) → `_build_rag_block` (direct_chat_service.py:1355+); легаси `get_rag_context` (:1773-1806, sort_by_timestamp=False дефолт). Запись фактов: `db.insert_graph_fact` (database.py:1382); memorize-путь — `summary_memory.py` (`memorize_facts`, FACT_EXTRACT_PROMPT :83).

**SQLite-схема (2a/2b).** `database.py`: `_SCHEMA_SQL` — graph_facts CREATE (:192-202, 7 origin-значений — финальный вид дают миграции), `_GRAPH_FACT_ORIGINS_SQL` (:59-63 — единое место списка origins; сюда += `'derived_belief'`), аддитивные образцы `bot_reply_parents` (:239-245), `chat_summary_levels` (:251-258), `user_prefs` (:428 — pattern per-chat-per-user таблицы); цепочка миграций в `initialize` (:275-284); `_migrate_history_import_v7` (:614-709) — образец rebuild (guard по sqlite_master, RENAME→CREATE с расширенным CHECK→INSERT…SELECT с сохранением id→DROP old→индексы→backfill→PRAGMA user_version; FTS5 `graph_facts_fts` НЕ пересоздаётся — rowid стабильны); константы версий (:42-49; v8-константа по образцу). Агрегаты: `get_active_participants` (:1664-1676) — образец SQL-агрегата по smart_messages (idx_smart_messages_chat_ts покрывает). Счётчики сообщений: `user_presence`/`message_counters` — не трогаем.

**PG (2a relations + TMA).** `services/pg_db.py`: `DDL_STATEMENTS` (:33-121), `chat_profiles` (:76-87) — сюда ADD COLUMN IF NOT EXISTS relations JSONB; SEED-категории (:163). `services/chat_lore_store.py`: `_PROFILE_COLS` (:34-37), `_to_profile` (:192+), транзакции+NOTIFY+по-полевая история, `ChatLoreConflict` (:149, optimistic по updated_at), методы get_profile/update_settings/ensure_profile (409-паттерн). `services/lore_cache.py`: `LoreProfile` frozen (:35-50) — при новой колонке расширяется + to_dict. `services/lore_runtime.py`: `set_lore_components`/геттеры store/cache/notify/worker — **db НЕ проброшен** (web/api/chat_lore.py читает только PG-компоненты; дельта для read-only SQLite-скора в TMA-эндпоинте — по Q2). RBAC API: `web/api/chat_lore.py` — can_access_chat (:89-95), `_conflict`→409 (:121-128), роуты /chats /{chat_id} PUT /settings /generate (:331+, 409 auto_disabled) — образцы для relations/сон/ностальгия-эндпоинтов; DI — `Depends(get_tma_user)` + `get_cache`, store из lore_runtime (PG down → 503/404 fail-open). Фронт: `web/app.js` TABS (:33-48: memory_rag type=config :33, chat_lore type=chat_lore :48), data-объект chat_lore (:112-137), методы (:1113+), 409-поток chatLore409; `web/index.html`: generic-конфиг-рендер (:338-440), кастом `chat_lore` (:494-743), карточки профиля/настроек/админов чата (:558-738).

**REGISTRY/config (флаги и лимиты).** `services/param_catalog.py`: GROUPS (:98-242; новые группы категории memory автоматически попадают во вкладку «Память и RAG» — TAB_RULES :1072-1076 `(CATEGORY_MEMORY, None)`), списки-записи (_FLAGS/_LIMITS с полями typ/группа/desc + дефолт в `config/settings.py`, паттерн `flags.lore_inject_enabled`/`limits.lore_inject_max_chars`); PG-сид дефолтов ключей — pg_db SEED_CATEGORIES (:163+). Категория memory уже есть (`memory.infinite_retention`, group `memory_infinite` :239-241).

**Воркеры/старт (2b/2c).** bot.py on_startup: MemoryMaintenance (:452-455, за summary-гейтом), lore-блок (:459-486: LoreWorker init + start + set_lore_components) — сюда добавлять DreamWorker/NostalgiaWorker (после lore-блока, со своим LLMClient при необходимости — образец :468-473); `services/lore_worker.py`: каркас AsyncIOScheduler (:167-181), tick (:198+), PG-lock (:280-316) — НЕ копировать lock; `services/memory_maintenance.py` — образец SQLite-воркера (джобы, hot-ключи). LLM: `services/llm_client.py` (LLMClient — облачный путь). Тесты: полный pytest **4321 passed / 0 failed** (HEAD 84c4887); релевантные файлы: test_tool_schemas (6), test_tool_router (23), test_direct_chat_prompts (21 байт-эталон), test_prompt_migrations, test_database (87), test_direct_chat, test_direct_chat_lore_inject, test_graphrag_memory, test_summary_memory, test_payload_builder; autouse reset_lore_runtime в conftest; pytest.ini timeout.

---

## A. Spec (зеркало дизайна; spec.md отдельным документом НЕ создаётся — ТЗ владельца)

**PM-база дизайна (по умолчанию для B–H; открытые вопросы Q1–Q14 закрывает @Architect в T-815 и фиксирует ПРАВКОЙ в этом разделе — строки-решения; если правки меняют требования задач — пометкой `(изменено @Architect T-815)` в тексте задачи):**

- **Архитектура данных отношений (2a, Q1/Q2):** разделение на «считаемое» и «ручное». (а) **Считаемое** (активность, скоры, авто-стадия) — SQLite `users_meta` (аддитивная, PK (chat_id,user_id)), пересчёт SQL-агрегатами по smart_messages + decay, лениво (RAM-кэш per chat при инжекте/API) + инкрементальный touch при новых сообщениях юзера; read-only для TMA. (б) **Ручное** (manual-стадия админа, заметка) — PG `chat_profiles.relations JSONB` (map user_id → {manual_stage|None, note, updated_by, updated_at}) через chat_lore-стиль store-методов с историей и 409-оптимизмом по `updated_at` профиля (PM-рекомендация: так TMA-редактирование идёт через существующие PG-роуты «Лор чатов» с 409-паттерном, без нового прецедента SQLite-правок из web-api; окончательно — Q1). Декларируемая админом стадия приоритетнее авто (перекрывает инжект и анти-откат).
- **Стадии и decay (2a, Q4/Q5):** стадии `stranger|acquaintance|regular|veteran`; авто-правила (время в чате + частотность/msg_count, пороги — limits.relations_*); анти-откат: плавный decay скоров при отсутствии, стадия понижается не чаще раза/месяц и не ниже regular-потолка за раз (veteran→regular, не veteran→stranger); тон-санкции — существующими кулдаунами, НЕ понижением стадии (research §5). Пересчёт — в services/user_relations.py; инжект `<user_relations>` (формат по Q3) в _build_user_content; гейт: глобальный флаг `flags.relations_tone_enabled` (default false) + per-chat `relations_enabled` в PG-профиле.
- **dig_into_lore (2a, Q6/Q7):** 3-й тул в TOOL_CALLING_TOOLS (research §4-схема: query/year/person/mode); ветка роутера `_dig_into_lore` (FTS5 smart_messages с фильтром года/периода «N лет назад» по timestamp + имена-расширение из граф-обхода (graph_facts/nodes — источник имён, датированные сниппеты даёт только FTS5 по smart_messages) → 5–8 сниппетов «[Имя дата]: текст» с датами; лимит результата ~3500 симв; НЕ бросает); контракт в каноне R9 (C2) + пре-гейт маркеров ностальгии в коде direct_chat (опция, флаг) — модель должна вызвать dig ДО ответа; LLM-справки-«что это было» в v1 нет (только сниппеты).
- **Сон (2b):** миграция v8 в D1 (детали выше в RUNTIME WARNING); importance — правило БЕЗ LLM на записи фактов (по origin/длине/маркерам; дефолт для существующих — по Q8) + колонка DEFAULT; DreamWorker — SQLite-воркер по образцу memory_maintenance (флаг `flags.dream_enabled` default **false**; тик `limits.dream_interval_hours`=24; кластеризация без LLM по subject/объекту/повторяемости; пороги: сумма importance кластера ≥ `limits.dream_importance_sum_threshold`, повторяемость ≥ `limits.dream_repeat_threshold` (PM-база 3); лимит `limits.dream_max_clusters_per_run`/дистилляций/токенов в сутки = денежный бюджет; дистилляция — 1 облачный LLM-вызов на кластер через LLMClient; запись: origin='derived_belief', kind='belief'/'stereotype', weight (PM-база 0.6), importance, source_ids JSON (обязательны, анти-галлюцинации), belief_meta (уверенность/цитаты/conflict), supersede-логика по subject через существующую колонку `supersedes`; аудит `memory_dream_log`; гейт: protected/вечно-хранимое НЕ дистиллируется; эмбеддинг beliefs — существующий боевой embed-путь LLMClient (int8 опция) для участия в KNN-ностальгии/«золотых».
- **Ностальгия (2c):** слой A (бесплатный): в direct-пути отбор «золотых» (importance ≥ порога И давность ≥ `limits.nostalgia_golden_min_days` (PM-база 60) И тема пересекается с запросом; max 1–2) → строка-подсказка «(можно вспомнить: <факт> (дата))» в промпт, 0 добавочных LLM-вызовов, флаг `flags.nostalgia_layer_a_enabled` default false. Слой B: NostalgiaWorker (тик `limits.nostalgia_tick_minutes`=60; флаг `flags.nostalgia_enabled` default false; чаты — активные PG-профили, read-only); условия (тишина ≥ `limits.nostalgia_min_silence_minutes`, quiet hours `limits.nostalgia_quiet_start/end`, cooldown, max в сутки `limits.nostalgia_max_per_day`=2–3, стоп после 2 неотвеченных подряд — детект по smart_messages после последнего nostalgic-сообщения (Q14)); кандидаты: «N лет назад в этот день» (SQL-диапазон по timestamp + тема последнего диалога) и «золотые» по последней теме; 1 облачный LLM-вызов на срабатывание → 1–2 фразы «кстати…»; отправка в чат (relay по Q13); лог `nostalgia_log` (CREATE IF NOT EXISTS: chat_id, ts, fact_id, status sent|skipped, meta) + «агрессивность» `limits.nostalgia_aggressiveness` 0..1.
- **API/TMA (F):** эндпоинты по образцам chat_lore (401/403/404/409/503-паттерны, can_access_chat для relations); SQLite-чтение из web-api — db через расширение lore_runtime (Q2); «Запустить синтез сейчас» и просмотры (beliefs/лог) — кастомные карточки: «Участники и отношения» в chat_lore-вкладке (admin/chat-admin), «Сон»/«Ностальгия»-мини-блоки в memory_rag под generic-списком (внутри config-секции index.html :338-440, v-if activeTab==='memory_rag'); настройки-ключи — REGISTRY: флаги/лимиты в существующие группы `flags_memory`/`limits_memory` (категории flags/limits) ИЛИ memory-категория с новыми группами `memory_dream`/`memory_nostalgia` (renders auto в «Память и RAG», категория CATEGORY_MEMORY полная на вкладке) — точное размещение Q11.
- **Канон-дисциплина:** все промпт-правки — с PREV-слепками и ступенями миграций; LEGACY/PREV/PREV_R8-слепки не трогать; тон/стиль канона не менять; запрет «»/— сохраняется. Промпты дистилляции/ностальгии — константы модулей (services/dream_prompts.py, services/nostalgia_prompts.py) по образцу lore_prompts (НЕ PG-сиды — Q10).
- **Дисциплина исполнения:** порядок B→C→D→E→F (2a: B1→B2→B3→C1→(B4 после C1/C2 — данные раньше рендера; C2 — канон R9 единый: и dig-контракт, и relations-строка) → D (v8 в D1 до E) → E → F), тесты по каждому пункту в задачах + сводные G1–G4; полный pytest → 0 failed; `git diff --check` чист; секреты не логировать; каждая боевая подфича отключаема флагом (дефолты консервативные, прода-поведение без включения НЕ меняется, кроме канона R9 и миграции v8).
- **Новые конфиг-ключи (кандидаты; точный набор/дефолты фиксирует @Architect в A1):** flags: `relations_tone_enabled`(f), `dig_enabled`(t), `dig_pre_gate_enabled`(f?), `dream_enabled`(f), `nostalgia_layer_a_enabled`(f), `nostalgia_enabled`(f); limits: `relations_*` (пороги стадий/decay/периоды), `dig_max_snippets`(8)/`dig_max_symbols`(3500)/`dig_graph_hop_depth`(2), `dream_interval_hours`(24)/`dream_repeat_threshold`(3)/`dream_importance_sum_threshold`/`dream_max_clusters_per_run`/`dream_max_beliefs_per_day`/`dream_tokens_budget`, `nostalgia_golden_min_days`(60)/`nostalgia_min_silence_minutes`(30)/`nostalgia_tick_minutes`(60)/`nostalgia_max_per_day`(2–3)/`nostalgia_cooldown_hours`(12)/`nostalgia_quiet_start|end`/`nostalgia_aggressiveness`(0.3?). REGISTRY + settings-дефолты + категории.

- [x] T-815 (A1): @Architect — единый дизайн 2a+2b+2c в разделе A настоящего файла: для КАЖДОЙ подфазы — что меняется (файл/функция/формат), порядок реализации и риски; закрыть Q1–Q14 строками-решениями (замена PM-базы или подтверждение); проверить полноту списков точек кода (точки записи фактов и importance-хук; точки инжекта/бюджета `_build_user_content`/`_apply_context_budget`; места вызовов `get_rag_facts`/`get_rag_context`; потребители байт-эталонов канона; REGISTRY/группы; точки старта воркеров в bot.py; can_access_chat/409 для новых роутов; autouse-fixtures lore_runtime) и зафиксировать итоговые имена конфиг-ключей, колонок v8, констант-слепков и порядок Builder'а (по умолчанию: B1→B2→B3→C1→C2→B4→D1→D2→D3→D4→E1→E2→F1→F2→F3→F4). Решения «relations: PG vs SQLite manual» (Q1), «db в lore_runtime» (Q2), «позиция/бюджет <user_relations>» (Q3), «важность существующих фактов/дефолт» (Q8), «sleep-window/чаты-кандидаты» (Q9), «промпты в PG или константы» (Q10), «размещение настроек REGISTRY» (Q11), «флаги-дефолты для прода» (Q12), «релей ностальгии» (Q13), «детект неотвеченных» (Q14) — строго здесь. (plans/features/agi-memory-implementation/tasks.md) ✅ (spec.md создан T-815; фикс-раунд по ревью — дельты D-14…D-20 в spec.md §0)

## B. 2a — Отношения (A-Life): users_meta + расчёт + PG relations + инжект `<user_relations>`

- [x] T-816 (B1): таблица `users_meta` (SQLite, аддитивный `CREATE TABLE IF NOT EXISTS` в `_SCHEMA_SQL` БЕЗ bump user_version; PK (chat_id,user_id); колонки по research §5: user_id, chat_id, first_seen, last_seen, msg_count, active_days, activity_score REAL, relationship_stage TEXT DEFAULT 'stranger', trust REAL, intimacy REAL, last_stage_change INTEGER, manual_stage TEXT NULL — состав/наличие trust/intimacy по решению A1) + методы `DatabaseService` (get_by_user/get_chat_relations/upsert_meta/update_activity — SQL-агрегаты по smart_messages по образцу `get_active_participants` (:1664-1676) + инкрементальный touch при новых сообщениях (точка — по A1: общий путь записи сообщений юзера; fail-open, не блокирует горячий путь); decay-функция; всё через единственное соединение WAL, короткие транзакции). Минимум-тесты в задаче (upsert/агрегаты/пересчёт по фикстуре); полные — G1. (services/database.py, тесты test_database/test_user_relations по конвенции)
- [x] T-817 (B2): `services/user_relations.py` — расчёт/хелперы: активность (скользящий decay-скор, полураспад по A1/Q5), авто-стадия по порогам (stranger→acquaintance→regular→veteran: месяцы+частотность, limits.relations_*), анти-откат (плавный decay; понижение ≤1 раза/месяц, потолок veteran→regular — Q5), ручная стадия (manual_stage из PG B3) приоритетнее авто; ленивый пересчёт с RAM-кэшем per chat (TTL-период по A1) для инжекта и API-чтения; «любимые темы» юзера из графа (опционально, Q3). (services/user_relations.py, тесты по конвенции)
- [x] T-818 (B3): PG: `chat_profiles` += колонка `relations JSONB NOT NULL DEFAULT '{}'` (аддитивный `ALTER TABLE ... ADD COLUMN IF NOT EXISTS` в DDL_STATEMENTS pg_db.py) + расширение ChatLoreStore-методов (get_relations/put_relation/delete_relation — по решению Q1: строка-история `chat_lore_history` field='relations', optimistic 409 по `updated_at` профиля (ChatLoreConflict), NOTIFY) + `_PROFILE_COLS`/`_to_profile`/`LoreProfile`(+relations dict, frozen)/to_dict + per-chat `relations_enabled`-тумблер (в JSONB relations.enabled или колонка — Q3). Согласование: при отсутствии PG-профиля relations-часть fail-open (как chat_lore). Минимум-тесты в задаче. (services/chat_lore_store.py, services/lore_cache.py, services/pg_db.py, tests по конвенции)
- [x] T-819 (B4): инжект `<user_relations>` в direct_chat (`_build_user_content`, рядом с protected/lore — позиция по A1/Q3; формат: 1 строка на активного юзера окна: имя+стадия+2–3 параметра (скора), cap символов по A1; бюджет: uncuttable или малый фикс-кап — A1; пустой блок не рендерится): гейты — глобальный `flags.relations_tone_enabled` (default false) И per-chat `relations_enabled` из PG-профиля (нет профиля/флага → без инжекта, 0 влияния на поведение); канон-правило тональности («нюфаг → сдержаннее, ветеран → теплее») — в каноне R9 (C2). Тесты: формат/пусто/флаги/fail-open/PG-нет — в G1. (services/direct_chat_service.py, tests по конвенции)

## C. 2a — dig_into_lore: тул + канон R9

- [x] T-820 (C1): tool `dig_into_lore` (третий элемент `TOOL_CALLING_TOOLS` :53 — тул-сет ≤6; схема по research §4/Q6: query (required), year int|null, person str|null, mode 'messages'|'facts'|'both' default 'both') + ветка `_dig_into_lore` в registry (tool_router.py :102-105): (1) FTS5 по smart_messages (`search_long_term`, как `_query_chat_memory` :141-202) с пост-фильтром периода (year/«N лет назад» — диапазон по timestamp, индексы есть) и расширением ключей именами из граф-обхода (depth ≤ `limits.dig_graph_hop_depth`=2 по graph_facts/nodes — даёт имена/связи для FTS; датированные сниппеты — только FTS5); (2) mode='facts' — `_search_graph_facts`/`vector_search` по графу (subject/person-фильтр); (3) рендер: 5–8 сниппетов «[Имя YYYY-MM-DD]: текст» (лимиты `limits.dig_max_snippets`/`limits.dig_max_symbols` ≈3500), пусто → «ничего не найдено»; НЕ бросает (любая ошибка → внятная строка); helpers переиспользовать (`_require_query`, `_resolve_name`, `_format_timestamp`, `_truncate`); режим ФЛАГА `flags.dig_enabled` (default по A1/Q12: консервативно true — тратит раунды только по вызову). Минимум-тесты в задаче (регистрация/валидация mode/пусто); полные — G1. (services/tool_schemas.py, services/tool_router.py, tests/test_tool_schemas.py, tests/test_tool_router.py)
- [x] T-821 (C2): канон R9 `CHAT_SYSTEM_PROMPT` (chat_prompts.py :85-109): (1) ИНСТРУМЕНТЫ += третий пункт `dig_into_lore` (контракт: «помнишь/а помните/как мы тогда/в 2024/что было с <имя>/год назад → вызови dig_into_lore ДО ответа»; description-фразы-примеры); (2) абзац/правило `<user_relations>` и тональности по стадиям (связь B4): «нюфаг → холоднее/ироничнее, меньше отсылок к лору; ветеран → теплее, можно личные отсылки/ностальгию»; (3) слепок: текущий канон R8 до правки → константа `PREV_R9_CHAT_SYSTEM_PROMPT` (существующие слепки не трогать); (4) `services/prompt_migrations.py`: ступень `(PREV_R9_CHAT_SYSTEM_PROMPT, CHAT_SYSTEM_PROMPT)` в список direct_chat (:59-62 — 4-я ступень); (5) байт-тесты: `tests/test_direct_chat_prompts.py` — новый reference + тесты контракта dig и relations-правила; `tests/test_prompt_migrations.py` — новая ступень (PREV_R9 → updated; канон → no-op; кастом → не трогаем); текст канона пишет @Architect. (6) Пре-гейт маркеров ностальгии в direct_chat (код, НЕ канон): маркеры («помнишь», «как мы тогда», «год назад», «в 2024», «кто был тот») И RAG-контекст беден → принудительный dig-retrieval-раунд до генерации (флаг `flags.dig_pre_gate_enabled` default по A1/Q12; лимиты раундов TOOL_MAX_ROUNDS сохраняются; реализация — по A1). (services/chat_prompts.py, services/prompt_migrations.py, services/direct_chat_service.py, services/tool_loop.py при необходимости, tests/test_direct_chat_prompts.py, tests/test_prompt_migrations.py)

## D. 2b — «Сон» (beliefs): миграция v8 + importance + DreamWorker + дистилляция

- [x] T-822 (D1): миграция v8 `_migrate_agi_memory_v8` (по образцу `_migrate_history_import_v7` :614-709; вызывается в initialize :275-284 после v7): (1) `_GRAPH_FACT_ORIGINS_SQL` (:59-63, единое место) += `'derived_belief'`; (2) rebuild `graph_facts`: guard по sqlite_master (нет 'importance' в CREATE-тексте) → RENAME→CREATE (колонки v7 + `importance INTEGER` (default по Q8), `source_ids TEXT`, `kind TEXT NOT NULL DEFAULT 'fact'` (CHECK по A1), `belief_meta TEXT`) → INSERT…SELECT с сохранением id (FTS5 `graph_facts_fts` rowid валидны — НЕ пересоздавать) → DROP old → индексы (в т.ч. по (chat_id, kind) и частичный по kind='belief'/importance для сна/ностальгии — A1) → PRAGMA user_version=8 (новая константа `_SCHEMA_VERSION_AGI_MEMORY = 8`); повторный запуск — no-op (guard); (3) `memory_dream_log` — аддитивный `CREATE TABLE IF NOT EXISTS` (id, chat_id, run_at, cluster_count, source_ids TEXT, belief_id INTEGER, tokens INTEGER, status TEXT — аудит «снов», БЕЗ bump); (4) `db.insert_graph_fact` (:1382) += параметры importance/source_ids/kind/belief_meta (совместимость с существующими вызовами — дефолты). Тесты миграции: на копии фикстурной БД (данные сохранены, id/FTS валидны, PRAGMA=8, повторный запуск no-op, origins/CHECK); регресс v2–v7. (services/database.py, tests/test_database.py)
- [x] T-823 (D2): importance-эвристика на ЗАПИСИ (правило БЕЗ LLM — research §3: тип origin (user_memory выше, history_import ниже), длина/наличие имён/чисел/дат — формула по A1/Q8) в общей точке записи фактов (memorize-путь и др. origins; НЕ пачечный LLM в v1 — Q8); дефолт для старых/существующих фактов (миграция v8: колонка DEFAULT по Q8; belief-факты — выше, по A1). Тесты: правило/границы/дефолты; существующие записи не ломаются. (services/summary_memory.py, services/database.py, tests/test_graphrag_memory.py)
- [x] T-824 (D3): `services/dream_worker.py` (каркас `memory_maintenance.py`/`lore_worker.py` без PG-lock): start/stop AsyncIOScheduler + IntervalTrigger(hours=`limits.dream_interval_hours`=24, max_instances=1, coalesce=True), джоб регистрируется только при `flags.dream_enabled` (default false); тик: (1) чаты-кандидаты — SQLite-агрегат «есть новые факты с created_at > last_dream чата» (last_dream — из memory_dream_log, max run_at; для чатов без лога — за последние `limits.dream_window_hours` по A1); (2) отбор кандидатов: новые факты (origin любые, кроме гейта protected-семантики по A1), кластеризация БЕЗ LLM (группировка по subject/объекту триплета/общим FTS-токенам + эмбеддинг-кандидаты по vec0 — Q9); (3) пороги кластера: сумма importance ≥ `limits.dream_importance_sum_threshold` И/ИЛИ повторяемость ≥ `limits.dream_repeat_threshold` (default 3); (4) бюджеты: `limits.dream_max_clusters_per_run`, `limits.dream_max_beliefs_per_day`, токенов — денежный лимит (дневной), по достижении — stop run; (5) окно сна не в часы пик (по Q9; по умолчанию 04:00–06:00 local) — реализация через вычисление следующего тика или внутритиковый гейт — A1; логирование тика. (services/dream_worker.py, bot.py on_startup — init после lore-блока :459-486; тесты в G2)
- [x] T-825 (D4): дистилляция: `services/dream_prompts.py` (канон-константа модуля, НЕ PG-сид — Q10; промпт «кластер N фактов с датами и source-ссылками → 0–2 стереотипа/убеждения/правила, каждое с цитатой-доказательством; при недостаточности — ничего»); вызов — ОДИН облачный LLM-вызов на кластер через существующий LLMClient (hot-ключи models/keys как в обычных ответах; локальная LLM запрещена — ограничение владельца); запись: origin='derived_belief', kind='belief' (или 'stereotype'), weight (PM-база 0.6 — A1), importance (A1), source_ids (JSON-массив id фактов-источников — ОБЯЗАТЕЛЕН; пусто → belief не пишется), belief_meta (уверенность/цитаты/conflict), expires_at NULL/вечно по A1; эмбеддинг — боевой embed-путь (участие в RAG/KNN); supersede при противоречии: новая дистилляция по тому же subject против существующего belief → старый помечается `supersedes` (колонка есть); незапирание protected; запись в `memory_dream_log` (run_at/source_ids/belief_id/tokens); рендер beliefs в RAG — по A1 (префикс «[убеждение]»/как есть; не ломать легаси-рендеры). (services/dream_worker.py, services/dream_prompts.py, services/summary_memory.py, services/database.py; тесты в G2)

## E. 2c — Ностальгия (слой A → слой B)

- [x] T-826 (E1): слой A (бесплатный, при ответе): в direct-пути после RAG-отбора («золотые» факты: importance ≥ порог (колонка v8 — D1 обязателен до E1) И давность ≥ `limits.nostalgia_golden_min_days` (PM-база 60) И тема пересекается с запросом (дешёвый KNN/текстовый матч по уже найденным кандидатам); максимум 1–2 на ответ) при совпадении добавить в промпт строку-маркер «кстати, можно вспомнить: <факт> (дата)» (канон-структура по A1; решение о вплетении — за LLM; 0 добавочных LLM-вызовов); флаг `flags.nostalgia_layer_a_enabled` (default false); учёт бюджета (малый фикс-кап); fail-open. (services/direct_chat_service.py, services/summary_memory.py; тесты в G3)
- [x] T-827 (E2): слой B: `services/nostalgia_worker.py` + `services/nostalgia_prompts.py` (канон-константа модуля — Q10; стиль «1–2 фразы "кстати…"» по образцу бота): каркас SQLite-воркера (AsyncIOScheduler + IntervalTrigger(minutes=`limits.nostalgia_tick_minutes`=60, max_instances=1, coalesce=True); джоб при `flags.nostalgia_enabled` default false; старт в bot.py on_startup); чаты — активные PG-профили (list_active_chats из ChatLoreStore, read-only; при PG down — no-op) ИЛИ SQLite-агрегат — Q13; шаги тика per chat: (1) условия: тишина — последнее сообщение юзера ≥ `limits.nostalgia_min_silence_minutes` (30) назад; quiet hours `limits.nostalgia_quiet_start/end`; cooldown ≥ `limits.nostalgia_cooldown_hours` с прошлого срабатывания; лимит `limits.nostalgia_max_per_day` (2–3) по nostalgia_log; стоп-счётчик: ≥2 неотвеченных подряд (детект по Q14); (2) кандидат: «N лет назад в этот день» (SQL-диапазон по message_timestamp + ключевые слова последней темы диалога → FTS) и/или «золотые» факты последней темы (KNN/FTS); (3) один облачный LLM-вызов (LLMClient; локальная LLM запрещена) → текст 1–2 фразы; (4) отправка сообщения в чат (relay по Q13: бот-инстанс/send_message с учётом стиля; ошибка → nostalgia_log status='error'); (5) аудит: `nostalgia_log` (CREATE IF NOT EXISTS: chat_id, ts, fact_id, status sent|skipped|error, meta) + «агрессивность» `limits.nostalgia_aggressiveness` (0..1 — влияет на пороги/частоту/выбор по A1). (services/nostalgia_worker.py, services/nostalgia_prompts.py, services/database.py, bot.py; тесты в G3)

## F. API/TMA (2a/2b/2c — эндпоинты и блоки вкладок)

- [x] T-828 (F1): relations API: `GET /api/chat_lore/{chat_id}/relations` (список юзеров чата: SQLite-скоры/стадии/активность (users_meta — db из runtime по Q2; fail-open при недоступности/пусто) + PG manual-стадии/заметки (relations JSONB); сортировка по активности; рендер-имя — каскад aliases) и `PUT /api/chat_lore/{chat_id}/relations/{user_id}` (body: stage 'auto'|стадия, note, updated_at — optimistic 409 по профилю (ChatLoreConflict → {detail:{code:conflict,current_updated_at}}), история, NOTIFY) + `DELETE` (сброс на авто) — паттерны web/api/chat_lore.py (401/403/404/409/503, can_access_chat :89-95; только глобальный admin/chat-admin); регистрация роутера в web/app.py (рядом chat_lore_router). (web/api/chat_lore.py — relations-роуты в этом же модуле (D-решение §3.6.1), web/app.py; RelationsService+db+воркеры в lore_runtime из bot.py on_startup; тесты — test_webapp_agi_ui.py + relations-кейсы в G4)
- [x] T-829 (F2): API «сон»/«ностальгия»: `GET /api/memory/dream/beliefs` (фильтр chat_id: последние beliefs kind='belief' с source_ids/датами), `POST /api/memory/dream/run` (ручной запуск синтеза «сейчас» — права admin; анти-рейс: если воркер уже бежит → 409/202 по A1; паттерн /chat_lore/{id}/generate), `GET /api/memory/dream/log` (memory_dream_log); `GET /api/memory/nostalgia/log` (последние срабатывания). DI через runtime (воркер-рефы: get_dream_worker()/get_nostalgia_worker()); права — global admin; fail-open 503. (web/api/memory_agi.py — A1, web/app.py; мягкое удаление DELETE /beliefs/{id} (D-7), protect POST /beliefs/{id}/protect; POST nostalgia/test НЕ реализуем — D-8; тесты в G4)
- [x] T-830 (F3): TMA блок «Участники и отношения» в «Лор чатов» (index.html: новая карточка в шаблоне chat_lore :494-743 — ПОСЛЕ «Настройки автогенерации», ДО «В память бота уходит»; видна при доступе к чату (can_access_chat — профиль уже отрендерен); таблица: юзер (каскад-имя), авто-стадия/ручная стадия (select: авто или stranger…veteran), скор/активность (msg_count/activity_score), last_seen, заметка, кнопки «сохранить»/«сброс на авто» (стирает stage+note); app.js: data-поля (chatRelations/relationsEnabled/relationDraft), методы loadRelations/saveRelation/resetRelation/saveRelationsToggle с 409-флоу как chatLore409 (:124), тосты); обновление списка при смене чата (loadProfile → loadRelations). (web/app.js, web/index.html; тесты — test_webapp_agi_ui.py + G4)
- [x] T-831 (F4): TMA блоки «Синтез (сон)» и «Ностальгия» во вкладке «Память и RAG» (memory_rag): (1) ключи-настройки — REGISTRY-записи (категория memory группы memory_dream/memory_nostalgia — Q11; generic-рендер config (:338-440) подхватывает автоматически — сделано в 2b/2c) + дефолты config/settings.py + PG-сид; (2) кастомные мини-блоки ПОД generic-списком в той же config-секции index.html (`v-if="activeTab === 'memory_rag' && isGlobalAdmin"`): «Синтез (сон)» — кнопка «Запустить синтез сейчас» (POST /api/memory/dream/run, спиннер/409 already_running), список последних beliefs (GET, карточки: дата/текст/importance/источники) с кнопками «удалить» (мягкое удаление D-7) и «сделать protected» (POST protect), «лог снов» (GET dream/log, компактно в details); «Ностальгия» — «последние срабатывания» (GET nostalgia/log, статусы sent/skipped/error + meta-reason); app.js-методы runDreamNow/loadDreamBeliefs/dreamDelete/dreamProtect/loadNostalgiaLog (тосты, 401/403-редирект как соседи; право — isGlobalAdmin; GET-логи консервативно — глобальный admin, spec §3.6.2). (services/param_catalog.py/config/settings.py — 2b/2c, web/app.js, web/index.html; тесты — test_webapp_agi_ui.py)

## I. Параллельность smart module (раунд N, T-839…T-842): per-chat пул семафоров

Обновление после раунда 9 (владелец «распараллеливание запросов smart module»; AGI-эпик база 4521 passed). Цель: заменить строгую сериализацию (глобальный lock summary_generator + per-chat asyncio.Lock direct_chat) на per-chat пул из N семафоров — разные чаты больше НЕ блокируют друг друга, в одном чате до N параллельных LLM-генераций. Ключ `limits.smartmodule_concurrency_per_chat` (default 3; 1 = строгая очередь как раньше), таймаут ожидания слота для smart-модулей — `limits.smartmodule_concurrency_wait_seconds` (default 60; direct_chat продолжает использовать `limits.chat_lock_wait_seconds`). STT (SmartModule/service.py VoiceTranscriber, глобальный asyncio.Semaphore D295) НЕ трогаем — это защита провайдера Groq, а не чат-очередь (документировано в докстрингах); youtube-медиа-ветки транскрипции (STT) тоже вне пула.

- [x] T-839: `services/smartmodule_concurrency.py` — `ChatConcurrencyPool`: `max_entries=256`; `_slots: dict[chat_id, _Slot(sem: asyncio.Semaphore, n, pending)]` под `_guard`; `async try_acquire(chat_id, timeout=None) -> _Permit | None` (None = таймаут; timeout=None — ждать как раньше); N читается при создании/смене слота из `hot.get('limits.smartmodule_concurrency_per_chat', settings.SMARTMODULE_CONCURRENCY_PER_CHAT)`; ленивая чистка при len > max_entries (слот со свободным sem (`sem._value == n`) и без pending — по образцу direct_chat_service:389-419 с pending-счётчиком анти-гонки); синглтон `get_smartmodule_concurrency_pool()` + `reset_smartmodule_concurrency_pool()` для тестов; helper `smartmodule_wait_seconds()`. Плюс ключ `limits.smartmodule_concurrency_per_chat` (default 3) и `limits.smartmodule_concurrency_wait_seconds` (default 60) в settings/param_catalog (группа limits_chat — вкладка «Лимиты» рендерится автоматически)/.env.example. (services/smartmodule_concurrency.py, config/settings.py, services/param_catalog.py, .env.example)
- [x] T-840: direct_chat на пуле: `DirectChatService` — удалить `_chat_locks/_chat_locks_guard/_chat_lock_pending/_get_chat_lock/_drop_chat_lock_pending`; DI `concurrency_pool=None` → `get_smartmodule_concurrency_pool()` (bot.py НЕ правим); в handle() — `permit = await pool.try_acquire(chat_id, timeout=hot limits.chat_lock_wait_seconds)`; None → busy-фраза CHAT_LOCK_BUSY_PHRASES + лог `direct: lock wait timeout` (текст сохранён); `permit.release()` в finally. (services/direct_chat_service.py)
- [x] T-841: обёртки пула в smart module: handlers/search.py (вокруг `_service.research`), factcheck.py (`check_claim`), web.py (`summarize`), checkup.py (`checkup`), youtube.py — LLM-ветки (`_process_youtube_summary` summarize_cascade; `_process_url_media`/`_process_video_media` summary-режимы; STT/транскрипт-ветки НЕ трогаем); таймаут — `smartmodule_wait_seconds()`, при None → фраза из нового пула `SMARTMODULE_BUSY_PHRASES` (services/smartmodule_phrases.py) + WARNING-лог; summary_generator.py: глобальный `_lock` → пул per-chat (ручной /summary: busy-ветка `_UX_BUSY`, крон молчит — текст лога `summary: lock busy — queued` сохранён; чат A больше не блокирует чат B, крон тоже через пул по chat_id из задания). (handlers/*, services/summary_generator.py, services/smartmodule_phrases.py)
- [x] T-842: тесты: `tests/test_smartmodule_concurrency.py` (N=3: 3 permit сразу, 4-й ждёт; try_acquire timeout → None; release; N=1 = строгий лок; смена N → новые слоты; чистка переполнения; синглтон/reset), `tests/test_direct_chat_concurrency.py` (N=2: два handle одного чата параллельны; N=1: сериализация + busy при исчерпании; таймаут → CHAT_LOCK_BUSY_PHRASES), `tests/test_summary_concurrency.py` (чаты A и B параллельны — регрессия глобального лока; busy-фраза/лог сохранены); адаптация `tests/test_direct_chat.py` TestChatLock и `tests/test_summary_generator.py` busy-кейсов под пул; полный pytest → 0 failed; `git diff --check`. (tests/)

## G. Тесты — сводная матрица покрытия (по подфазам; минимум 1 тест на пункт)

- [x] T-832 (G1): тесты 2a (после B/C): users_meta/стадии/decay — upsert, SQL-агрегаты (first/last_seen, msg_count, active_days), decay-функция, пороги стадий, анти-откат («стадии не откатываются резко»: отсутствие N дней → decay скора, но стадия ≥ потолка), manual-стадия приоритетна, PG relations put/409/delete; инжект `<user_relations>` — формат строк/имена-каскад/cap, флаг relations_tone_enabled off → блока нет, per-chat relations_enabled off/PG-профиля нет → нет (fail-open), бюджет-урезание не трогает relations при uncuttable-семантике; dig_into_lore — вызов (query/year/person/mode-валидация), формат сниппетов «[Имя дата]: текст», фильтр года/«N лет назад» по timestamp, mode messages/facts/both, лимит 3500 симв/5–8 сниппетов, «ничего не найдено», НЕ бросает (ошибка FTS → строка); контракт промпта — байт-тесты канона R9 + PREV_R9-слепок == канон R8 (свежий срез из git), миграция-ступень; «маркер ностальгии/помнишь → dig вызван ДО ответа» (мок-цикл tool_loop, пре-гейт). (tests/test_user_relations.py, tests/test_tool_router.py, tests/test_tool_schemas.py, tests/test_direct_chat.py, tests/test_direct_chat_prompts.py, tests/test_prompt_migrations.py, tests/test_database.py)
- [x] T-833 (G2): тесты 2b (после D): миграция v8 — rebuild сохраняет факты/id/FTS5-валидность (поиск работает после миграции), CHECK origin включает derived_belief, новые колонки, PRAGMA=8, повторный запуск no-op, регресс v2–v7, время rebuild на сэмпле ~10k фактов приемлемо; importance-правило на записи; DreamWorker (LLM замокан): выбор чатов/окна по last_dream, кластеризация и пороги (кластер без повторяемости не идёт в дистилляцию), дистилляция 0–2 beliefs, «belief без source_ids не пишется», supersede при противоречии (новая запись + supersedes у старой), бюджеты (max clusters/beliefs/день, токены — stop), флаг dream_enabled off → джоб не тикает, memory_dream_log-аудит, гейт protected-семантики. (tests/test_database.py, tests/test_dream_worker.py по конвенции)
- [x] T-834 (G3): тесты 2c (после E): слой A — маркер-строка появляется только для «золотых» (давность ≥ порога + importance + тема-матч), при флаге off — нет, максимум 1–2, контекст не ломается; слой B (LLM замокан) — условие тишины/quiet hours (не срабатывает ночью/днём по A1-часам), cooldown, max_per_day по nostalgia_log, «стоп после 2 неотвеченных» (модель «отправлено → тишина → 2-й skip»), aggressiveness влияет на пороги/частоту, кандидат «N лет назад» по диапазону timestamp, статусы лога sent/skipped/error, отправка через мок-релей. (tests/test_nostalgia_worker.py по конвенции, tests/test_direct_chat.py)
- [x] T-835 (G4): сводный тест-координатор + API/UI-аудит (после B–F): (1) матрица «подфаза 2a/2b/2c → пункт → тест» (все пункты покрыты ≥1 тестом; список тест-имён/файлов — включая: dig-контракт и пре-гейт; инжект relations и флаги; v8-миграция; belief-запись/supersede/бюджеты; ностальгия A/B и анти-спам); (2) API-аудит: relations/dream/nostalgia эндпоинты — 401 без initData, 403 для moderator без права, 404 неизвестный chat/user, 409 optimistic (relations PUT), 503/пусто при PG/SQLite недоступности (fail-open), RBAC chat-admin vs global admin; (3) UI-аудит (по конвенции фронт-тестов проекта, при наличии; иначе ручная проверка + список в отчёте H3): блок «Участники и отношения» рендерится только с правами; blocks «Сон»/«Ностальгия» на memory_rag под generic; (4) полный pytest → 0 failed; `git diff --check` чист; фиксация пропусков/правок — правками в задачах B–F. (tests/, планы-файлы)

## H. Деплой, README, отчёт, архив

- [ ] T-836 (H1): README-доки: раздел «AGI Memory (Фаза 2, раунд 9)»: что изменилось — таблица users_meta + PG relations в chat_profiles (блок «Участники и отношения» в «Лор чатов»), tool dig_into_lore + контракт R9 (канон-миграция), миграция v8 graph_facts (importance/source_ids/kind/belief_meta, origin derived_belief, memory_dream_log), DreamWorker/«Сон», ностальгия слой A/B (nostalgia_log, анти-спам), новые конфиг-ключи (список из A1 с дефолтами) и как включать (флаги off по умолчанию), что НЕ делалось (локальная LLM не боевая; отдельного TMA-раздела «Отношения» нет; инструменты истории не тронуты). .env.example — без изменений (новых env нет). Полный pytest → 0 failed; grep-проверка секретов; `git diff --check` чист. (README.md) *(@DevOps финальный шаг — вне архива)*
- [ ] T-837 (H2): @DevOps: деплой раунда: pytest 0 failed локально → коммит (код+тесты+plans/backlog.md+plans/features/agi-memory-implementation/) → пуш; сервер `git pull --ff-only`; БЭКАП SQLite-БД (контрольный снапшот, прецедент раундов — v8 rebuild идёт на проде при старте); мягкий рестарт `admin_bot` → journald: миграция v8 (PRAGMA user_version=8, rebuild graph_facts с log «[database] migration v8…», FTS валиден), CREATE IF NOT EXISTS (users_meta/memory_dream_log/nostalgia_log), PG-DDL (`chat_profiles.relations` JSONB ADD COLUMN IF NOT EXISTS — DDL ok), канон R9 (`[prompt_migration]` ключ prompts.direct_chat_system_prompt → R9; кастом не трогаем), старт воркеров (Dream/Nostalgia зарегистрированы, тикают по флагам off → no-op), LoreWorker/MemoryMaintenance живы; live-верификация: health HTTP 200, PRAGMA user_version=8, `/api/chat_lore/{chat_id}/relations` без auth → 401, GET/PUT relations с initData (админ) → 200/409-сценарий, error|traceback = 0; флаги по умолчанию (relations_tone_enabled/dream_enabled/nostalgia_*/layer_a — off; dig_enabled по A1). Результат — в H3. (прод) *(@DevOps финальный шаг — вне архива)*
- [ ] T-838 (H3): отчёт юзеру + закрытие: аппрув @Reviewer (полный прогон/регресс-обзор, стоп-условия §6 research — анти-спам и бюджеты часть приёмки); архивация: перенос `plans/features/agi-memory-implementation/` → `plans/archive/agi-memory-implementation/` (после выполнения раунда @PM); правка `plans/backlog.md` (статус раунда 9: выполнено+архив, прецеденты раундов 7–8); `ARCHITECTURE.md` — раздел по эпику (решает @Architect, практика раундов 7–8); финальный memory-sync docs-коммит (правило T-725). Отчёт юзеру: что сделано по 2a/2b/2c (сводка), новые флаги/лимиты и как включать в TMA (вкладки «Лор чатов»/«Память и RAG»), денежные бюджеты (лимиты дистилляций/срабатываний в сутки), что НЕ тронуто (SQLite v7-структуры прошлых раундов, история-БД, tools/history_import — RUNTIME WARNING). (plans/, README.md, ARCHITECTURE.md) *(@DevOps/@Memory финальный шаг — вне архива)*
