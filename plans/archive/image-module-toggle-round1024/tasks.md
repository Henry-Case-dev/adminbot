# Задачи: image-module-toggle-round1024

> **Раунд 10.24 (UPD2 + UPD3)** · Приоритет **P1** · Шаг 1 @PM (revision UPD3) · Тип: каталог + frontend (`web/**`)
> **ТЗ:** `plans/current_task.md`, UPD2, п.3 (строки 174–178) + **UPD3, п.1 (строки 241–242)**. Файл untracked; секреты не цитировать (R17/R18).
> **Baseline:** HEAD `00eab85`; каталог REGISTRY **457** / GROUPS **96** / TAB_RULES; прод `4314ea4`.
> **✅ Human Gate №1 — ЗАКРЫТ (UPD3 №1):** согласовано — **отдельный пункт в меню «Модули»**; запрет `tma-menu-freeze` **для этого пункта снят**.

## Цель
В разделе «Модули» дать **отдельную карточку/подраздел «Генерация изображений»** с главным тумблером включения/отключения, **включённым по умолчанию (глобально)**. Сейчас тумблер спрятан внутри карточки «Прямой чат».

## Что уже есть (координаты)
- Параметр уже существует: `services/param_catalog.py:955-959` (`IMAGE_GENERATION_MODULE_ENABLED`, group `flags_module_images`, default True).
- Группа объявлена: `services/param_catalog.py:329` (`GroupSpec("flags_module_images", …)`).
- **Группа рендерится внутри «Прямой чат»:** `services/param_catalog.py:2056-2059` (`TAB_MOD_DIRECT` включает `flags_module_images`) — источник проблемы.
- Витрина «Модули» `MODULES`: `web/app.js:353-393` — **записи для изображений нет**.
- Провайдер-блок (адрес/модель/ключ/GET-режим) уже есть: `web/app.js:539-551` (`image_generation` в `PROVIDER_BLOCKS`).
- Мэппинг табов/сайдбара: `web/app.js` `_TAB_BY_GROUP`, `TAB_NAV`; пин-тесты `tests/test_param_catalog*`, `tests/test_frontend_tab_mapping*`.

## Задачи
- [x] **T-2221** [@Architect] ADR: форма размещения **зафиксирована владельцем (UPD3 №1): отдельный модуль-таб `mod_images`/`TAB_MOD_IMAGES`** (запрет `tma-menu-freeze` снят для этого пункта); оценка влияния на GROUPS/TAB_RULES и пин-тесты; фиксация Δ каталога.
- [x] **T-2222** [@Builder] Каталог: вынести `flags_module_images` в отдельную вкладку/группу рендера (согласно T-2221); убедиться, что тумблер не дублируется в «Прямой чат».
- [x] **T-2223** [@Builder] Frontend: добавить модуль-карточку «Генерация изображений» в `MODULES` (`web/app.js:353-393`) с `toggleKey: flags.image_generation_module_enabled` и ссылкой на окно параметров; корректный `tab`/`_TAB_BY_GROUP`.
- [x] **T-2224** [@Builder] Значение по умолчанию — **включено глобально**; проверить, что это не переопределяется per-chat неявно; отображение состояния тумблера при отсутствии сохранённого значения.
- [x] **T-2225** [@Builder] Обновить пин-тесты каталога/мэппинга (REGISTRY/GROUPS/TAB_RULES/`_TAB_BY_GROUP`) в том же коммите; JS-гейт `node --check web/app.js`.
- [ ] **T-2226** [@Reviewer] Ревью: карточка на месте, тумблер функционален и влияет на фичу, freeze-меню соблюдён согласно решению, Δ каталога санкционирован.

## Критерии приёмки
- В «Модулях» есть карточка «Генерация изображений» с рабочим главным тумблером.
- По умолчанию фича включена глобально.
- Выключение тумблера реально отключает генерацию изображений.
- Пин-тесты обновлены; pytest и JS-гейт зелёные; Δ каталога задокументирован.

## Риски
- **R1 (Low):** `tma-menu-freeze` снят для этого пункта (UPD3 №1) → сохранить изоляцию изменения меню только под карточку изображений.
- **R2 (Medium):** Δ каталога ломает пин-тесты и счётчики → обновить в том же коммите.
- **R3 (Medium):** дублирование тумблера (останется и в «Прямой чат») → явный чек в тестах.
- **R4 (Low):** per-chat override перекроет глобальный дефолт → проверить порядок разрешения.

## Зависимости / ступени
- **Human Gate №1 — ЗАКРЫТ (UPD3 №1):** отдельный пункт меню; запрет меню снят для этого пункта.
- Ступень `services/param_catalog.py`: **F5 → F6** (обе трогают каталог).
- Ступень `web/app.js`/`index.html`: F3 → **F5** → F6 → F11 → F4 → F10.
- Feature flag: сам тумблер `IMAGE_GENERATION_MODULE_ENABLED` (default ON) + env-only `IMAGE_GENERATION_ENABLED` (наследие 10.23).
- Откат: вернуть группу в `TAB_MOD_DIRECT` / `git revert`; Δ DDL = 0.
