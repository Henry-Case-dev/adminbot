# F6 — `memory-analytics-reorg-round1025` (Эпик 1, Волна 3)

> **ТЗ:** `plans/current_task.md` §4, §21–§30, §52–§59, §75, §76, §111/§116. **Тип:** UI + adapter (web + `web/api`).
> **Приоритет:** P0. **Зависит от:** F1 (IA/nav, ✅), F4 (store §37–§42, ✅). **От F6 зависит:** F11 (превью карты §21).
> **Статус:** ✅ **COMPLETED + MERGED + ARCHIVED (Шаг 8 @PM, 23.09.2026; Merge — `plans/ARCHITECTURE.md` §65; архив — `plans/archive/memory-analytics-reorg-round1025/`).** Step 5 @Reviewer — **Approved** (итер.2, `review.md`); Step 6 @Scanner — **Critical 0 / High 0 / Medium 0 / Low 2 / Info 4 → «к деплою ДА»** (`plans/reports/round1025_f6_scanner_audit.md`). ⏳ deploy/коммиты — Шаг 9 @DevOps (T-2916); ⏳ live (WebView + PG) — **T-2917 PENDING OWNER VERIFICATION** (не блокирует старт F7).
> **Baseline:** HEAD `f103992`, `APP_VERSION` 2.58.13, pytest **8319/0**. Точка отката — T-2870 (тег существует).
> **Факт Step 4 (@Builder):** `APP_VERSION` **2.58.14**; adapter `web/static/execution_graph.js`; тесты JS 37/37, pytest 8330 passed / 5 env-fail (`test_outgoing_guard_round1022`, `test_summary_cover_round1023` — падают и на чистом baseline, не связано с F6); Playwright-матрица `failures: 0`; Δ DDL=0, Δ каталога=0 (`services/**` не менялись).
> **⚠️ Инварианты (Critical):** §22/§25/§111 — ExecutionGraph **уже существует** (`web/api/analytics.py` + `llm_usage_events` + `web/app.js::tokenFlowTree/tokenFlowNodes/tokenSeriesBars/loadTokenAnalytics`); **вторую систему аналитики/визуализации НЕ создавать** (§116). **Δ DDL = 0**, **Δ каталога = 0**, CSP/zero-build (ADR-1016-2/ADR-1024-13), R17/R18. Фиктивные узлы/токены запрещены (§24), `$0` вместо неизвестной стоимости запрещён (§28), `parent_id` не выдумывать (§25), логи (§20) остаются в Статусе.

## Карта @Memory (Step 0 — принято как данность, перепроверке не подлежит)
- «Аналитика» = вкладка `oversight` (`#/oversight`, label «Аналитика» `web/app.js:226`), экран `web/index.html:1927-2230` («Бюджет фона», «Аналитика токенов» — дерево+график+by_module+totals, виджет «Интеллект и Память», таблица чатов); вход из Статуса — карточка `web/index.html:3245-3254`. **Компактного превью последнего вызова на Статусе нет** (цель §21).
- «Память» = nav-раздел `#/memory` (F1 §54): `TABS` `memory_rag`/`relations`/`chat_lore`; `HUBS_V2['#/memory']` (`web/app.js:425-439`) → «Память и RAG» `#/memory/rag`, «Лор чата» `#/memory/lore`, «Участники и отношения» `#/memory/relations`; `memory_rag` — generic config (нет своей ветки в `web/index.html`), `relations` (`:2657-2860`) и `chat_lore` (`:2871-3238`) — свои рендеры. Параметры памяти — группы `limits_memory`/`limits_graph`/`limits_rag`/`flags_memory`/`memory_infinite`/`reactions_memory` (`services/param_catalog.py:245-252,324,368,382`).
- **Разрыв adapter (§22–§25):** модель узла = плоский SQL-список без `parentIds`/kind-enum/`status`/`durationMs`; adapter-слоя нет; агрегаты и трассировка смешаны. ExecutionGraph-стек (`/analytics/usage/latest` → плоский `steps[]` по `correlation_id`; `/usage/summary?period=day|week|month` → totals/by_module/series; `/analytics/prices`; read-only PG, fail-open; `services/usage_events.py`; `TOKEN_ANALYTICS_ENABLED`) — **переиспользовать, не дублировать**.
- **Источники памяти:** досье (`services/dossier_rebuild_jobs.py`, `summary_memory.py`, `oversight.py::dossier_feed`), RAG/граф (`memory_rebuild/maintenance/health`, `graph_*`), лор (`chat_lore_store`, `lore_worker`, `lore_compiler_service`), убеждения/парадигмы/эволюция (`/api/memory/dream/beliefs?kind=belief|paradigm` + persona-трейты), ночной синтез (`dream_worker`), отношения (`user_relations.py`).
- **§43 applied-state = N/A** (read-through; ADR-1025-10). **§85 / backend-пайплайн Эпика 2 — НЕ трогать.**

## Трассируемость требований → задачи (привязка к дословным фрагментам ТЗ)

| REQ | Источник (дословно) | Задачи |
|---|---|---|
| R-F6-1 | §21 «Сводка переименовывается в Аналитику… открывается из Статуса» | T-2873–T-2877 |
| R-F6-2 | §21 «На Статусе остаётся компактное превью последнего вызова… не заменять одной строкой с числом токенов» | T-2876, T-2877 |
| R-F6-3 | §22 «расширяемый компонент… не привязан к двум этапам L1 и L2» | T-2878–T-2883 |
| R-F6-4 | §23 `interface ExecutionNode { id, runId, parentIds, kind, stageKey… }` + «не изменять серверную схему без необходимости; нормализовать через отдельный adapter» | T-2878, T-2879 |
| R-F6-5 | §24 kind = llm/algorithm/tool/format/publish/other; «не показывать выдуманные LLM-токены для алгоритмического фильтра»; «не создавать фиктивные узлы» | T-2880, T-2881 |
| R-F6-6 | §25 «Backend metrics → Normalized execution graph → UI»; «не размещать преобразование внутри SVG-компонента»; «если `parent_id` нет — не придумывать связи» | T-2879, T-2882 |
| R-F6-7 | §26 «РЕЖИМ 1 последний вызов / РЕЖИМ 2 период day|week|month… не смешивать трассировку с агрегатами» | T-2884–T-2887 |
| R-F6-8 | §27 «при выборе узла открывать подробности (desktop side-panel / mobile bottom sheet)» + поиск/фильтры | T-2888–T-2890 |
| R-F6-9 | §28 «Нет данных» вместо `$0`; «Без лимита»; не смешивать LLM-стоимость и ресурсы сервера | T-2891–T-2892 |
| R-F6-10 | §29 mobile-карта: вертикальная последовательность / агрегат, не сжимать до 320 px | T-2893–T-2894 |
| R-F6-11 | §52 структура раздела «Память»; не смешивать просмотр людей с настройками RAG | T-2895–T-2896 |
| R-F6-12 | §53–§56, §59 перенос параметров Поиск/Граф знаний/Хранение/Ночной синтез/Умный кэш (Δ каталога = 0) | T-2897–T-2900, T-2903 |
| R-F6-13 | §57 отношения (алгоритм стадий не менять) | T-2901 |
| R-F6-14 | §58 лор чатов (4 вкладки, сохранить данные, не удалять ручной лор при очистке авто) | T-2902 |
| R-F6-15 | §75 тест карты токенов (вкл. adapter одно-/двухслойных вызовов) | T-2905 |
| R-F6-16 | §76 тест мониторинга интеллекта; прокрутка не сбрасывается | T-2906 |
| R-F6-17 | §30/§111 совместимость с Эпиком 2 (новые kind через тот же контракт; GraphViewer/NodeCard/DetailPanel без рефакторинга) | T-2907, T-2908 |
| R-F6-18 | §116 финальная приёмка: нет второй системы аналитики; не исчезли виджеты памяти/сна; дерево вызовов живо; граф не картинка; логи доступны | T-2907, T-2909 |
| R-F6-19 | §117(1,2,3,10) обязательные результаты: карта экранов, реестр параметров, карта виджетов, проверка карты токенов | T-2895, T-2911, T-2920 |

---

## Блок 0 — Baseline и Step 2
- [x] **T-2870** · @DevOps · Точка отката + baseline-фиксация. **Готово, когда:** создан тег `pre-round1025-f6` на HEAD `f103992`; зафиксированы `APP_VERSION` 2.58.13, pytest 8319/0, SQLite `user_version=12`, счётчики каталога (REGISTRY 459 / GROUPS 98 / `_TAB_BY_GROUP` 96 / TAB_RULES 21 / Settings 418). Файлы: — (git-тег; отчёт в `plans/workflow_state.md`).
- [x] **T-2871** · @Architect · Step 2: `spec.md` + **ADR-1025-19** (adapter, `ExecutionNode`, §52–§58-структура, границы F11/F9). **Готово, когда:** `plans/features/memory-analytics-reorg-round1025/spec.md` + `adr-1025-19-*.md` созданы; закрыты 5 открытых вопросов (см. ниже); задан канонический контракт `ExecutionNode` и точка размещения adapter. Файлы: `spec.md`, `adr-1025-19-*.md` (создаёт @Architect; @PM не создаёт).
- [x] **T-2872** · @PM · Сверка `tasks.md` со `spec.md`/ADR. **Готово, когда:** при расхождении — правка через @Architect (не @Builder); при согласии — отметка в `workflow_state.md`. Файлы: `plans/features/memory-analytics-reorg-round1025/tasks.md`.

## Блок A — «Аналитика» §21 (страница + превью на Статусе)
- [x] **T-2873** · @explore+@PM · Аудит существующего `#/oversight` против списка §21 (расходы/бюджеты/токены/распределение по моделям/модулям/дерево/история/карта). **Готово, когда:** зафиксировано, что уже есть, чего нет; **подтверждено, что переименование «Сводка»→«Аналитика» уже сделано в F1** и дублирующей страницы НЕ создаётся. Файлы: `web/index.html:1927-2230`, `web/app.js:226`.
- [x] **T-2874** · @Builder · Дополнение страницы «Аналитика» до полного списка §21 **без новой страницы** (только недостающие блоки на существующем `oversight`). **Готово, когда:** все 8 пунктов §21 присутствуют; существующие «Бюджет фона»/«Интеллект и Память»/таблица чатов сохранены. Файлы: `web/index.html:1927-2230`, `web/app.js` (oversight-ветка).
- [x] **T-2875** · @Builder · Вход из Статуса. **Готово, когда:** карточка `web/index.html:3245-3254` ведёт в «Аналитику»; логи §20 остаются в Статусе; скролл не сбрасывается. Файлы: `web/index.html:3245-3254`.
- [x] **T-2876** · @Architect+@Builder · **Компактное превью последнего вызова** на Статусе (граница с F11 — только источник данных и место, без полного набора фильтров §27). **Готово, когда:** превью показывает реальные поля последнего вызова (≠ одна строка с числом токенов), данные — из существующего `/analytics/usage/latest`, деградация в «Нет данных» (§28). Файлы: `web/index.html` (Статус-сетка §12–§14), `web/app.js`.
- [x] **T-2877** · @Builder · Тесты превью. **Готово, когда:** покрыты пустой ответ/ошибка/неизвестная стоимость; прокрутка Статуса не сбрасывается. Файлы: `tests/js/*`, `tests/test_*analytics*`.

## Блок B — Adapter §22–§25 (backend → normalized → UI)
- [x] **T-2878** · @Architect+@Builder · Каноническая модель `ExecutionNode` **под реально существующие данные** (адаптация §23, а не копия TS-примера). **Готово, когда:** для каждого поля зафиксирован источник: `id`/`runId` ← `correlation_id`; `parentIds` ← только подтверждённая связь (L1→L2 по времени/этапу), иначе `[]`; `status` ← реальное событие, иначе `"unknown"`; `durationMs` ← только если есть в данных, иначе `null`. Файлы: `web/static/*`/`web/app.js` (чистый модуль), `web/api/analytics.py` (решение — ADR-1025-19).
- [x] **T-2879** · @Builder · Отдельный слой adapter `Backend metrics → Normalized graph → UI` (§25). **Готово, когда:** преобразование API-ответа живёт **вне** SVG/компонента дерева; UI получает только нормализованные узлы. Файлы: `web/static/*.js` / `web/app.js`, `web/api/analytics.py`.
- [x] **T-2880** · @Builder · Маппинг `kind`: `llm`/`algorithm`/`tool`/`format`/`publish`/`other`. **Готово, когда:** kind выводится из реальных признаков (`step`/`tool_name`/`source`), неизвестное → `other`; **`parentIds` без подтверждения не заполняются** (§25). Файлы: adapter-модуль.
- [x] **T-2881** · @Builder · Честный рендер `algorithm`-узлов. **Готово, когда:** для algorithm показываются только реальные метрики (обработано/сохранено/длительность), **LLM-токены не выдумываются**; фиктивных узлов нет (§24). Файлы: adapter, `web/app.js::tokenFlowNodes`.
- [x] **T-2882** · @Builder · Тесты adapter. **Готово, когда:** покрыты однослойный и двухслойный вызов, агрегатный ответ, отсутствие `parent_id`, неизвестные поля → `null`/`other`. Файлы: `tests/test_*analytics*`, `tests/js/*`.
- [x] **T-2883** · @Builder · Расширяемость §22/§30. **Готово, когда:** добавление нового stage/kind не требует правки рендера; контракт готов принять узлы Эпика 2 без рефакторинга GraphViewer/NodeCard/DetailPanel. Файлы: adapter-модуль, `web/app.js`.

## Блок C — Два режима карты §26 (на существующем стеке)
- [x] **T-2884** · @Builder · Режим «Последний вызов». **Готово, когда:** конкретная цепочка на существующем `tokenFlowTree` (2179-2268) через adapter; без второй визуализации. Файлы: `web/app.js`, `web/index.html:1969-2082`.
- [x] **T-2885** · @Builder · Режим «Период» (день/неделя/месяц). **Готово, когда:** распределение по этапам/модулям/моделям на существующем `/usage/summary` + `tokenSeriesBars` (2271); переключение периодов. Файлы: `web/app.js::loadTokenAnalytics:3631`, `tokenSeriesBars`.
- [x] **T-2886** · @Builder · Разделение трассировки и агрегатов. **Готово, когда:** режимы визуально и в данных не смешиваются (§26/§25); uiFlag `TOKEN_FLOW_NODEFLOW_ENABLED` учтён. Файлы: `web/app.js`, `web/index.html`.
- [x] **T-2887** · @Builder · Тесты режимов. **Готово, когда:** покрыты 4 периода (последний вызов/день/неделя/месяц) и переключение. Файлы: `tests/js/*`.

## Блок D — Интерактивность §27
- [x] **T-2888** · @Builder · Детали узла: desktop — side-panel, mobile — bottom-sheet. **Готово, когда:** показываются только реально доступные поля (этап/модель/провайдер/токены/стоимость/длительность/статус); отсутствующие → «Нет данных». Файлы: `web/index.html`, `web/app.js`.
- [x] **T-2889** · @Builder · Поиск и фильтры (модуль/модель/этап/период/статус). **Готово, когда:** фильтры работают на нормализованной модели; пустой результат — понятный empty-state. Файлы: `web/app.js`, `web/index.html`.
- [x] **T-2890** · @Builder · Превью Статуса без полного набора фильтров (§27). **Готово, когда:** фильтры доступны только на странице «Аналитика»; превью компактно. Файлы: `web/index.html:3245-3254`, `web/app.js`.

## Блок E — Честные состояния §28
- [x] **T-2891** · @Builder · «Нет данных» вместо `$0` при неизвестной стоимости; «Без лимита» при бесконечном бюджете **без заполненного progress bar**. **Готово, когда:** ни одного выдуманного `$0`/полного бара; проверено и в карте, и в превью. Файлы: `web/app.js`, `web/index.html`.
- [x] **T-2892** · @Builder · Не смешивать стоимость LLM и расход ресурсов сервера. **Готово, когда:** разные блоки/подписи; тест на отсутствие подмены. Файлы: `web/app.js`, `web/index.html`.

## Блок F — Mobile-карта §29
- [x] **T-2893** · @Builder · Mobile: для конкретного вызова — вертикальная последовательность этапов; для периода — агрегированная диаграмма с раскрытием. **Готово, когда:** горизонтальный граф **не сжимается до 320 px**; значения читаемы без pinch-zoom. Файлы: `web/static/app.css`, `web/app.js`, `web/index.html`.
- [x] **T-2894** · @Builder · Полноэкранное исследование карты (при необходимости). **Готово, когда:** mobile-карта не ломает shell/fullscreen §63/§64 (ARCHITECTURE) и нижнюю навигацию. Файлы: `web/static/app.css`, `web/app.js`.

## Блок G — Раздел «Память» §52–§59
- [x] **T-2895** · @Builder · Структура раздела «Память» §52 (**AMEND-1 H1**, см. `spec.md` D6 + `adr-1025-19a`): **3 карточки верхнего уровня** — «Настройки памяти» / «Лор чатов» / «Люди и связи»; сохранить существующие рендеры `relations` (`web/index.html:2657-2860`) и `chat_lore` (`:2871-3238`) и generic `memory_rag`. Карточки-заглушки «Досье»/«Факты» **не создаются** (R16/`Δ каталога = 0`/§0/§57/§116); их реальные поверхности — модалка «Досье» из «Люди и связи» (`openDossier`) и «Живая лента досье»/«Новые факты» + «Граф знаний» (карта в `spec.md` D6). **Готово, когда:** просмотр людей **не смешан** с настройками RAG (§52); карта «старый экран → новый» приложена (§117(1)). Файлы: `web/app.js:425-446`, `web/index.html`.
- [x] **T-2896** · @Builder · Настройки → 5 групп: Поиск / Граф знаний / Хранение / Ночной синтез / Отношения. **Готово, когда:** маршруты «Память → Настройки → …» работают; параметры только сгруппированы иначе, **Δ каталога = 0**. Файлы: `web/app.js`, `web/index.html`.
- [x] **T-2897** · @Builder · §53 Поиск: `limits.dig_*`, `flags.dig_*` (7 параметров) + русские названия. **Готово, когда:** семантика сверена по коду; значение сохраняется через существующий write-path F0. Файлы: `services/param_catalog.py:245-252,324`, `web/app.js`.
- [x] **T-2898** · @Builder · §54 Граф знаний: `limits.graph_*`, `flags.graph_*` (7). **Готово, когда:** то же; без Δ каталога. Файлы: `services/param_catalog.py`, `web/app.js`.
- [x] **T-2899** · @Builder · §55 Хранение: `full_memory_retention_days`, `memory_backup_hour`, `flags.memory_backup_enabled`/`vec_int8_enabled`/`graph_user_quota_enabled`. **Готово, когда:** то же. Файлы: `services/param_catalog.py:368,382`, `web/app.js`.
- [x] **T-2900** · @Builder · §56 Ночной синтез: `deep_sleep_top_k`, `flags.deep_sleep_enabled`/`belief_decay_enabled` **+ остальные реальные параметры найти в коде и перенести** (§56). **Готово, когда:** найденные параметры перенесены, ничего не потеряно; алгоритм не меняется. Файлы: `services/param_catalog.py`, `web/app.js`.
- [x] **T-2901** · @Builder · §57 Отношения: `flags.relations_*`/`limits.relations_*`, `irony_filter_enabled` **в подраздел «Память → Люди и связи → Настройки отношений»**; алгоритм стадий не менять. **Готово, когда:** сохранены существующие данные/действия; Δ каталога = 0. Файлы: `web/index.html:2657-2860`, `web/app.js`.
- [x] **T-2902** · @Builder · §58 Лор чатов: desktop список+рабочая область; вкладки Ручной лор / Автоматический лор / Автогенерация / История; **ручной лор не удаляется при очистке авто**. **Готово, когда:** все существующие данные/действия сохранены; проверено тестом. Файлы: `web/index.html:2871-3238`, `web/app.js`.
- [x] **T-2903** · @Builder · §59 Умный кэш в «ИИ → Умный кэш»: `limits.smart_cache_*`, `flags.smart_cache_enabled`. **Готово, когда:** серверные единицы измерения не изменены. Файлы: `services/param_catalog.py`, `web/app.js`.
- [x] **T-2904** · @Builder · Сохранность виджетов и скролла. **Готово, когда:** виджеты памяти/сна/ленты/мониторинга интеллекта/графа/логов на месте (§76/§116), прокрутка не сбрасывается, логи (§20) остаются в Статусе. Файлы: `web/index.html`, `web/app.js`.

## Блок H — Тесты §75/§76 и инварианты §116
- [x] **T-2905** · @Builder · §75 тест карты токенов: последний вызов / день / неделя / месяц; выбор L1/L2/модели/модуля; неизвестная стоимость; неограниченный бюджет; отсутствие фиктивных этапов; мобильное представление; **отдельно adapter одно-/двухслойных вызовов**. **Готово, когда:** все пункты покрыты падающими-при-регрессе тестами. Файлы: `tests/js/*`, `tests/test_*analytics*`.
- [x] **T-2906** · @Builder · §76 тест мониторинга интеллекта: убеждения/парадигмы/эволюция/живая лента досье/обычный сон/глубокий сон/бейджи; **не менять**, только убедиться. **Готово, когда:** тесты зелёные; прокрутка при новых событиях не сбрасывается. Файлы: `tests/js/*`.
- [x] **T-2907** · @Builder+@Scanner · Инварианты §116: нет второй системы аналитики; **Δ DDL = 0**; **Δ каталога = 0** (459/98/96/21/418); CSP/zero-build; `backdrop-filter: url(` = 0; grep-маркеры «одна система визуализации». **Готово, когда:** маркер-тесты зелёные, диффы `services/**`/миграции/`handlers/**` пусты. Файлы: `tests/`, `tools/ui_round1025_matrix.py`.
- [x] **T-2908** · @Builder · §30/§111 совместимость с Эпиком 2. **Готово, когда:** контракт принимает новые kind через adapter без рефакторинга; фиктивных узлов Эпика 2 сейчас нет. Файлы: adapter-модуль.

## Блок I — Регресс, ревью, деплой, архив, продолжение
- [x] **T-2909** · @Builder · Полный регресс: pytest, JS, Playwright-матрица, `node --check`, `git diff --check`. **Готово, когда:** 0 падений (кроме известных env), матрица `failures: 0`. Файлы: `tools/ui_round1025_matrix.py`, `tests/`.
- [x] **T-2910** · @DevOps · Bump `APP_VERSION` 2.58.13 → **2.58.14** + синхрон `README.md`/cache-bust `?v=__APP_VERSION__`. **Готово, когда:** версии синхронны. Файлы: `config/settings.py:1702`, `README.md`.
- [x] **T-2911** · @Reviewer · Ревью (в т.ч. §117(2,3): реестр параметров, карта виджетов). **Готово, когда:** Approved либо Changes requested с конкретикой. Файлы: `plans/reports/`.
- [x] **T-2912** · @Scanner · Аудит безопасности/инвариантов (R17, CSP, вторая система аналитики). **Готово, когда:** отчёт с Critical/High/Medium. Файлы: `plans/reports/round1025_f6_scanner_audit.md`.
- [x] **T-2913** · @Builder · Правки по ревью/аудиту (**H2** поиск+статус; **M-F6S-1** сброс фильтров; **L-F6S-3/4/5**; H1 — AMEND-1 @Architect, код не менялся). **Готово, когда:** все Critical/High закрыты. Файлы: `web/app.js`, `web/index.html`, `web/static/app.css`, `web/static/execution_graph.js`, `tests/js/*`, `tests/test_webapp_f6_round1025.py`, `evidence.md`.
- [x] **T-2914** · @Architect · Merge `plans/ARCHITECTURE.md` **§65**. **Готово, когда:** раздел фиксирует фактически принятый объём + AMEND/SUPERSEDE-карту. Файлы: `plans/ARCHITECTURE.md`.
- [x] **T-2915** · @PM · Архивация фичи. **Готово, когда:** папка перенесена в `plans/archive/memory-analytics-reorg-round1025/` (spec + ADR-1025-19 + tasks + review/evidence), ID стабильны, ссылки не битые; `backlog.md` обновлён. Файлы: `plans/archive/…`, `plans/backlog.md`.
- [ ] **T-2916** · @DevOps · Deploy. **Готово, когда:** прод обновлён, `/api/health` 200, `database is locked`=0, `deployment.md` VERIFIED. Файлы: `deployment.md`.
- [ ] **T-2917** · @Owner(live) + @DevOps · Live-гейт: реальный Telegram WebView — карта токенов, превью на Статусе, раздел «Память», mobile. **Готово, когда:** подтверждено владельцем. **Статус: PENDING OWNER VERIFICATION** (живой WebView + реальный PG-пайплайн `/analytics/*`; Playwright — на стабах). **PENDING OWNER VERIFICATION ≠ блокер старта следующего F.** Файлы: `deployment.md`.
- [ ] **T-2918** · @Memory · Step 10: `metrics.md` + `MEMORY.md` синк. **Готово, когда:** записаны реальные start/end, раунды ревью, evidence-ссылки. Файлы: `plans/metrics.md`, `plans/MEMORY.md`.
- [ ] **T-2919** · @PM · Продолжение: следующий actionable F. **Готово, когда:** назван следующий F (по плану — **F7** `permsoc-local-space`, затем F11/F9/F10), `backlog.md` актуализирован. Файлы: `plans/backlog.md`, `plans/workflow_state.md`.
- [ ] **T-2920** · @PM · Финальная сверка с `plans/current_task.md` (полное перечитывание §4/§21–§30/§52–§59/§75/§76/§111/§116). **Готово, когда:** все требования F6 сопоставлены задачам; остаток ушёл в F7+/Эпик 2/3; `current_task.md` не изменён. Файлы: `plans/current_task.md` (read-only), `plans/workflow_state.md`.

---

## Явно зафиксированные границы (НЕ нарушать / НЕ дублировать)
- **viewport/fullscreen/shell из ARCHITECTURE §63 (HOTFIX9, flex-геометрия/`--app-usable-height`/bottom-nav в flex) и §64 (HOTFIX10, `GlassSurface` default OFF/единая высота/`.status-block`/OGL-фон) — не ломать.** F6 не меняет геометрию AppShell и не переделывает фон.
- **SaveBar F9 — не дублировать:** визуальная часть SaveBar (§69/§78) принадлежит F9; F6 использует существующий write-path F0 `persistItems`/`saveConfigItem`.
- **«Сводка» → «Аналитика» уже переименована в F1 (§54):** уточнять/дополнять существующий маршрут `#/oversight`, **дублирующую страницу не создавать**.
- **§43 «Ожидает применения» = N/A** (read-through, ADR-1025-10) — F6 не вводит новую семантику.
- **Логи §20** остаются внизу Статуса; страница «Аналитика» их не забирает.
- **§85 / backend-пайплайн Эпика 2 (S1–S10) не трогаем** — F6 только UI-каркас + adapter под будущие узлы.
- **Параметры памяти:** группировка/названия/раскладка — presentation-слой; `services/param_catalog.py`, миграции, `services/**` — **Δ каталога = 0**.

## Открытые вопросы для @Architect (→ `spec.md` + ADR-1025-19)
1. **Где размещать adapter:** отдельный JS-модуль (`web/static/*.js`, zero-build) внутри `web/app.js` vs расширение `web/api/analytics.py` (серверная нормализация перед отдачей).
2. **Форма канонической модели `ExecutionNode` под существующие данные:** что реально есть из `parentIds`/`kind`/`status`/`durationMs` и откуда брать (вывод из `correlation_id`/порядка этапов/`step`/`tool_name`; `null`/`[]`/`"unknown"` — где данных нет). Никаких выдуманных связей.
3. **Algorithm-узлы:** какие реальные метрики доступны (обработано/сохранено/длительность) и как показывать их без выдуманных LLM-токенов.
4. **Компактное превью на Статусе (граница с F11):** что именно показывать, из какого источника, чтобы не превратиться в «одну строку с числом токенов» и не дублировать F11.
5. **Структура раздела «Память» §52–§58:** какие группы каталога в какую из 5 подгрупп, как сохранить `relations`/`chat_lore`/`memory_rag` рендеры и существующие данные **без Δ каталога**.
6. **Мягкий откат/флаг:** нужен ли env-only UI-флаг для слоя adapter/карты (прецедент `TOKEN_FLOW_NODEFLOW_ENABLED`/ADR-1024-13) или достаточно `git revert` + kill-switch `TOKEN_ANALYTICS_ENABLED`.

## Зависимости / ступень
- Ступень web: F4 → F5 → **F6** → F7 → F11 → F9 → F10 (строго сериализовано, общие файлы `web/app.js`/`web/index.html`/`web/static/app.css`).
- Ступень `web/api`: F6 (analytics adapter) изолированно от routes (F9); существующие эндпоинты `/analytics/*` переиспользуются.
- Риски: вторая несовместимая система аналитики (**Critical** §116); фиктивные узлы/токены (**High** §24); подмена стоимости `$0` (**High** §28); статичное дерево вызовов (**High** §116); потеря параметра памяти при переносе (**Critical** §116).
