# Раунд 3: video-multimodal-pipeline-and-incidents

Планирование раунда 3 (04.09.2026, продолжает нумерацию T-685 → T-686…T-704; база e000297, дерево чистое кроме plans/docs/memory-project-overview.md). ПРОБЛЕМА 1 — BetterStack: на сервере 198.46.175.136 всё исправно (токен 26 симв в .env на месте и не ротировался, сеть до in.logs.betterstack.com 200, logtail-python 0.4.0 установлен, в journald ошибок logtail нет), но после 04.09 события в панель не поступают. Код: bot.py:104-116 basicConfig + LogtailHandler(source_token=os.getenv), уровень INFO; uvicorn log_config=None (хотфикс эпика 85: дефолтный dictConfig uvicorn дедлочил logtail-флашер). logtail 0.4.0: буфер Queue до 1000 событий, флашер-поток 1/с, raise_exceptions=False и drop_extra_events=True (тихие потери), при SIGKILL очередь теряется. Рестарт деплоя 04.09 был жёстким: stop-sigterm таймаут 30с → SIGKILL (02:11:14). Вероятно: (а) тихие потери буфера/сбой флашера, (б) сторона BetterStack (source/тариф/вкладка), (в) юзер смотрит не туда.

ПРОБЛЕМЫ 2+3 — видео. Хронология 04.09: (1) 02:45 «че за видос» на TG-видео: STT Groq 200, транскрипт 29 симв (речи почти нет), дальше выжимка по транскрипту + ПАМЯТИ RAG (facts=10, chars=991) — ответ «про память», а не про видео; визуального канала у медиа-ветки НЕТ (только VoiceTranscriber STT; multimodal video_url — только YouTube-канонические URL в summarize_cascade). (2) 03:21 личка «че за видос»: Groq timeout (10.0s) → OpenRouter STT HTTP 400 (retry 1/3) → OpenRouter timeout (15s) → TranscriptionUnavailable → фраза-отказ. (3) Баг: инъекция видео-транскриптов в GraphRAG падает — CHECK constraint graph_facts.origin не содержит 'video_transcript' (services/database.py:146 CREATE + :304 D201-миграция; в summary_memory.py:72-74 _FACT_ORIGINS значение уже есть) — сотни попыток молча; плюс «smart_message row not found» при L2-инъекции. (4) «Транскрипт» медиа-ветки — простой текст «Имя 🗣:\nтекст» БЕЗ HTML, в отличие от кружков (voice_transcription.py:202-215: «<b>Имя</b> 🗣: <i>текст</i>» parse_mode=HTML, _resolve_transcript_author, у форвардов «(переслал X)»). (5) STT: SmartModule/service.py:45-92 VoiceTranscriber, Groq→OpenRouter, wait_for ~10/15с (мало для длинных видео), OpenRouter input_audio m4a base64 → HTTP 400 для больших файлов?, Groq upload лимит ~25МБ.

Целевая логика (уточнение юзера): выжимка («че за видос/о чем видео/поясни за видос/перескажи видос/че в видосе») для ВСЕХ типов видео: YouTube-ссылки (текущий summarize_cascade L1 minimax/minimax-m3:free → L2 google/gemma-4-31b-it:free → субтитры — ок), прямые ссылки (новое: multimodal video_url; fallback: скачать файл → опубликовать → мультимодалка → STT), нативные TG-видео (новое: скачать → опубликовать по временному публичному signed URL → multimodal L1/L2 → fallback STT + выжимка). Режимы: reply на видео с триггером; «триггер <ссылка>»; caption-триггер (частично работают для медиа, для ссылок-не-YouTube — нет). «Транскрипт» — отдельный режим для всех типов, оформление КАК У КРУЖКОВ; выжимка ТОЛЬКО по реальному контенту (транскрипт ≥ N симв), при «немом» видео — честная фраза, НЕ выжимка по RAG-памяти (фикс поведения 02:45). Для мультимодалки нативных файлов — новая подсистема публикации: FastAPI-эндпоинт (web/app.py, uvicorn 127.0.0.1:8000) /media/<signed>/<file> с HMAC-подписью + TTL (10-15 мин), файлы из временного каталога, MIME video/mp4, очистка после обработки; наружу — Caddy-маршрут https://admin-bot.duckdns.org/media/* → 127.0.0.1:8000 (конфиг Caddy не в репо — DevOps). Fallback при недоступности публикации — старый STT-путь. Не-YouTube платформы (tiktok/instagram/vk и пр. — yt-dlp/cobalt) по «че за видос <ссылка>»: скачать → опубликовать → multimodal → STT.

ПРОБЛЕМА 4 — «сцуко» (расследование завершено): промпт в БД == канон байт-в-байт (миграция 02:11 сработала), «сцуко» в промпте НЕТ. Слово впервые сгенерировала САМА модель deepseek-v4-flash (fallback при аварии apinet ReadTimeout/502: 304 ретрая, 197 фолбэков 04.09) в 03:43:55 («Бот опиши свой внешний вид»). Дальше самоподкрепление: (а) style_anchors (_build_style_anchors, direct_chat_service.py:534-556): последние 3 ответа бота + «держи тон» — модель копирует дословно (14/16 ответов 03:43-04:42 начинали со «сцуко,»); (б) RAG: memorize bot_direct_reply (query+answer через LLM-экстрактор) → 5 graph_facts со «сцуко» (origin bot_direct_reply, TTL пусто = вечно: settings.py:493 CHAT_DIRECT_REPLY_TTL_DAYS=None, param_catalog.py:598, summary_memory.py:1166/1221-1230) подмешиваются через get_rag_context include_direct_reply; (в) Conversation_Thread включает ответы бота. Юзеры подхватили. query_chat_memory ищет по smart_messages (юзерские) — бот «не помнит», что сам начал. Вне скоупа (отметить, не делать): 23 «global context truncated 6033→869»; единичный трейсбек dead_page chat not found. Фиксы C1-C4 ниже.

**Статус:** archived (выполнено 2026-09-04, 3618 passed, 0 failed). Аппрув @Reviewer; архитектура — `ARCHITECTURE.md` §15. T-686 (дизайн) и T-699 (отчёт юзеру) закрыты при архивации — отчёт юзеру будет в финальном отчёте оркестратора. T-698, T-701…T-704 остаются открытыми — исполняет @DevOps финальным шагом.

## A. Дизайн

- [x] T-686: spec.md создан и одобрен @Architect *(закрыта при архивации 04.09.2026 — spec.md в архиве рядом)* (plans/features/video-multimodal-pipeline-and-incidents/spec.md) — дизайн ВСЕГО ДО реализации: (1) подсистема публикации временных файлов (HMAC-signed URL + TTL, эндпоинт /media/, каталог/MIME/очистка, настройки, интеграция Caddy), (2) универсальный видео-маршрутизатор (reply/caption/ссылка × youtube/прямая/нативная/платформа) с сохранением приоритетов бот.py:374 vs 0f/0g/0h/реакции (:387/:448/:454/:457) и youtube-хендлера, (3) мультимодальный каскад по опубликованному файлу + fallback STT, (4) «транскрипт» как у кружков, (5) честная выжимка (мин-длина транскрипта, фразы для «немого»), (6) STT-таймауты/400-разбор, (7) CHECK-миграция graph_facts по D201-паттерну (пересоздание с сохранением id, PRAGMA user_version), (8) style_anchors-антизалипание и TTL bot_direct_reply, (9) BetterStack-диагностика. (plans/features/video-multimodal-pipeline-and-incidents/)

## B. Видео-пайплайн (проблемы 2+3)

- [x] T-687 (B1): подсистема публикации временных медиа: новый модуль signed-URL (HMAC-SHA256 подпись + TTL 10-15 мин, настройки: секрет/срок/каталог — settings.py + hot-параметры через param_catalog.py), FastAPI-эндпоинт /media/<signed>/<file> в web/app.py (uvicorn 127.0.0.1:8000): проверка подписи+срока, отдача файла с MIME video/mp4, безопасные пути (без traversal), очистка после обработки и по TTL; Caddy-заметка для DevOps (внешний маршрут admin-bot.duckdns.org/media/* → 127.0.0.1:8000 — конфиг не в репо); документировать fallback (публикация недоступна → старый STT-путь). (web/app.py, новый модуль в services/, settings, param_catalog)

- [x] T-688 (B2): универсальный маршрутизатор видео-запроса (handlers/youtube.py: расширить _YOUTUBE_TRIGGERS-ветку :81-140/_parse/обработчик :380+): входы — reply на медиа-сообщение с триггером, caption-триггер, «триггер <ссылка>»; типы источника: YouTube-канонический URL (текущий summarize_cascade, youtube_summarizer_service), прямая медиа-ссылка (multimodal video_url, fallback: скачать → публикация → каскад), нативное TG-видео (video/document mime video/*/репост forward_origin: скачать через общий хелпер services/media_download.py → публикация → каскад), не-YouTube платформы (tiktok/instagram/vk и пр. — скачать через tools/video_downloader.py: yt-dlp/cobalt → публикация → каскад); без медиа и без ссылки — НЕ триггер (UNHANDLED); сохранить лимиты размера/длительности и кулдаун-семантику; не ломать 0f/0g/0h и реакции. (handlers/youtube.py, services/media_download.py, tools/video_downloader.py)

- [x] T-689 (B3): мультимодальный каскад для опубликованных файлов: L1 minimax/minimax-m3:free → L2 google/gemma-4-31b-it:free через video_url на signed URL файла (переиспользовать services/video_cascade_client.py / summarize_cascade-механику), graceful degradation: провал/пусто → fallback STT (VoiceTranscriber) + LLM-выжимка по транскрипту; результат и инъекция в память по образцу voice _inject_memory/wrap_media_fact (handlers/voice_transcription.py:128-260); решение о MIME/длительности/размере гейтов — в spec. (services/video_cascade_client.py, youtube_summarizer_service, SmartModule/service.py)

- [x] T-690 (B4): «транскрипт»-режим для ВСЕХ типов видео с оформлением КАК У КРУЖКОВ: единый формат «<b>Имя</b> 🗣: <i>текст</i>» parse_mode=HTML (эталон voice_transcription.py:202-215; _resolve_transcript_author; форвард — «(переслал X)»); источник текста: нативные/прямые/платформенные — STT; YouTube «транскрипт <url>» — субтитры (youtube_transcript_engine), при недоступности — скачать (yt-dlp) → STT; «транскрипт» выводит ТОЛЬКО текст (без выжимки/памяти). (handlers/youtube.py, handlers/voice_transcription.py, services/youtube_transcript_engine.py)

- [x] T-691 (B5): честная выжимка только по реальному контенту: выжимку строить по транскрипту >= N символов (порог — константа/hot-параметр, см. spec), НЕ подмешивая RAG-контекст/память как источник «о чём видео»; при транскрипте < N («немое» видео, тест-кейс 02:45: 29 симв) — честная фраза из пулов (services/smartmodule_phrases.py, стиль существующих, новые без пересечений): «в ролике нет разборчивой речи» и т.п., БЕЗ выжимки по памяти; пулы без пересечений — тест test_direct_chat_prompts.py:79-99-стиль. (handlers/youtube.py, services/smartmodule_phrases.py)

- [x] T-692 (B6): STT-надёжность: увеличить wait_for-таймауты VoiceTranscriber для видео (SmartModule/service.py:45-92; текущие ~10/15с малы для длинных видео — значения в spec: стратегии Groq/OpenRouter, единая точка контроля), разобрать HTTP 400 OpenRouter (input_audio m4a base64: размер/формат/лимиты против Groq upload ~25МБ) и решить ретрай-политику (сейчас строго 1 попытка без ретраев) для транзиентных 400/429/5xx; гейты размера/длительности с человечными фразами (лимиты video_transcribe_max_size_mb/duration_seconds из T-670); проверка фактических лимитов провайдеров — результат в spec. (SmartModule/service.py, tests)

- [x] T-693 (B7): фикс CHECK constraint: добавить 'video_transcript' (и проверить 'voice_transcript') в origin-список graph_facts в sqlite-схеме: CREATE TABLE (services/database.py:146-148) И D201-миграцию (services/database.py:293-311 _migrate_direct_chat_v2: пересоздание таблицы с сохранением id, если 'video_transcript' отсутствует в sqlite_master.sql, PRAGMA user_version bump); если GraphRAG-зеркало/PostgreSQL-путь (pg_db.py) уже содержит схему — синхронно; тест: INSERT origin='video_transcript' успешен и факт попадает в get_rag_context. (services/database.py, services/summary_memory.py:72-74, тесты)

- [x] T-694 (B8): «smart_message row not found» при L2-инъекции (после выжимки файлового видео): разобрать, где ищется smart_message (по tg_message_id?) и почему строки нет (репост/документ/отсутствие записи) → починка: проверка существования + no-op/upsert или корректный fallback, без молчаливых падений; тест на путь L2 с несуществующим tg_message_id. (services/summary_memory.py, handlers/youtube.py)

- [x] T-695 (B9): тесты Части B: маршрутизация по входам (reply-медиа/caption/«триггер <ссылка>») и типам (youtube/прямая/нативная/платформа; репост forward_origin); подпись+TTL+очистка /media/ (просрочка, битая подпись, traversal); каскад опубликованного файла (mock LLM) + fallback STT + деградация; «транскрипт»-формат (эталон HTML как у кружков, автор, форвард); честная фраза на «немом» видео (в ответе НЕТ фактов из RAG-памяти); таймауты/400-ретраи; CHECK-миграция (старая БД → новая схема, id сохраняются); изоляция от 0f/0g/0h/реакций (стиль tests/test_epic37_router_isolation.py). (tests/)

## C. «Сцуко»-петля (проблема 4)

- [x] T-696 (C1): style_anchors анти-залипание (services/direct_chat_service.py:534-556 _build_style_anchors): (а) не включать в якоря «паразитные» ответы — если >=2 из последних N (N=chat_style_anchors_count) начинаются с одного и того же слова/фразы («сцуко,» и пр.) — исключить такие ответы из выбора/объединить, чтобы модель не копировала дословно; (б) смягчить инструкцию секции: «подражай тону, НЕ копируй дословно, не начинай каждый ответ с одного и того же слова»; код + тесты (сэмпл-ответы с залипшим префиксом → якорь чистый). (services/direct_chat_service.py, services/database.py:1199-выборка, tests)

- [x] T-697 (C2): TTL для bot_direct_reply-фактов: кодовый дефолт ~30 дней (settings.py:493 CHAT_DIRECT_REPLY_TTL_DAYS сейчас None → expires_at NULL = вечно; default 30, .env может переопределить; param_catalog.py:598 limits.chat_direct_reply_ttl_days — дефолт 30), проверить применение в summary_memory.py:1166/1221-1230 (memorize) и archive-логике database.py:1461; учесть уже существующие факты без expires_at (докейт — вопрос к spec: TTL-проставление при миграции или точечная чистка C3); тест: новое bot_direct_reply-памятование получает expires_at по дефолту, .env-оверрайд работает. (settings.py, param_catalog.py, summary_memory.py, database.py, tests)

- [ ] T-698 (C3): @DevOps прод-чистка (198.46.175.136): удалить graph_facts с «сцуко» (origin bot_direct_reply и прочие) и bot_replies-ответы бота со «сцуко» (бот-ответы в памяти/истории стиля); сделать бэкап ДО удаления; verify (SQL-выборки до/после), результат — в отчёт PM. (прод-БД) *(остаётся открытой — исполняет @DevOps финальным шагом)*

- [x] T-699 (C4): отчёт юзеру @PM: причина «сцуко» — слово впервые сгенерировала сама модель deepseek-v4-flash (fallback при аварии apinet ReadTimeout/502, 03:43:55, «опиши свой внешний вид»), далее самоподкрепление style_anchors + RAG bot_direct_reply; промпты действительно менялись (новый канон + ИНСТРУМЕНТЫ + авто-миграция 02:11), но причина НЕ в этом; что исправлено (C1-C3); в логах вне скоупа: 23 «global context truncated 6033→869» и единичный трейсбек dead_page chat not found — отметить. (отчёт) *(fix-round 04.09 (m4): в отчёт юзеру включить и смену поведения «транскрипт <yt-url>»: было — каскад-выжимка (как «че за видос»), стало — субтитры текстом (при недоступных субтитрах — скачивание+STT), формат как у кружков; smart_cache больше не пишется.)* *(закрыта при архивации 04.09.2026 — отчёт юзеру будет в финальном отчёте оркестратора)*

## D. BetterStack (проблема 1)

- [x] T-700 (D1): startup-диагностика и устойчивость logtail (bot.py:104-116): на старте — маркер-лог «[logtail] attached | token_len=N | logtail=<version>» (и без токена — явное «[logtail] skipped»), единая инициализация хендлера без дублей при рестартах; при SIGTERM — корректный flush/закрытие очереди logtail перед выходом (не терять буфер); задокументировать в spec/коде поведение 0.4.0 (Queue 1000, raise_exceptions=False, drop_extra_events=True — тихие потери) и решение (лог на потерю событий / наблюдаемость очереди, без рекурсии — фильтр log_ring.py:88-91); уровень INFO и связка с uvicorn log_config=None не меняются без нужды. (bot.py, services/log_ring.py)

- [ ] T-701 (D2): @DevOps live-тест BetterStack (198.46.175.136): (а) отправка тестового события в in.logs.betterstack.com (source-токен) из-под учётки сервиса и проверка появления во вкладке BetterStack (source/тариф/не та ли вкладка); (б) TimeoutStopSec unit'а admin_bot: мягкий restart (SIGTERM), убедиться в graceful-остановке ≤30с БЕЗ SIGKILL (пересечение с T-655) и что флашер успевает отдать буфер; результат + логи в отчёт PM. (сервер, прод-окружение) *(остаётся открытой — исполняет @DevOps финальным шагом)*

## E. Документация и деплой

- [ ] T-702: @DevOps README-правки: видео-режимы для ВСЕХ типов (YouTube/прямые ссылки/нативные TG-видео/платформы tiktok-instagram-vk и пр.) — выжимка vs «транскрипт», оформление транскрипта как у кружков, публикация временных файлов /media/, лимиты; честное поведение на «немом» видео. (README.md) *(остаётся открытой — исполняет @DevOps финальным шагом)*

- [ ] T-703: полный pytest → 0 failed; коммит ВСЕГО незакоммиченного на русском (plans/backlog.md, plans/features/video-multimodal-pipeline-and-incidents/, код/тесты частей B/C/D, plans/docs/memory-project-overview.md; .env НЕ коммитить); пуш в origin/master. *(остаётся открытой — исполняет @DevOps финальным шагом; включает: коммит всего незакоммиченного, пуш, деплой, Caddy-маршрут /media/*, BetterStack live-тест, TimeoutStopSec, чистку «сцуко» в БД прода)*

- [ ] T-704: деплой на 198.46.175.136: pull, рестарт admin_bot (мягкий SIGTERM, без SIGKILL), Caddy-маршрут /media/* → 127.0.0.1:8000 (внешний https), live-верификация: «че за видос»/«транскрипт» на нативном TG-видео и на прямой ссылке, проверка startup-маркера «[logtail] attached» в journald и появления событий в BetterStack; результат — в отчёт PM. *(остаётся открытой — исполняет @DevOps финальным шагом; включает: коммит всего, пуш, деплой, Caddy-маршрут /media/*, BetterStack live-тест, TimeoutStopSec, чистку «сцуко» в БД прода)*
