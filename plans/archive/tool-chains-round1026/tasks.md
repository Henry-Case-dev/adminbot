# A2 — `tool-chains-round1026` (Эпик 3 «Agentic Intelligence», Wave 2, Раунд 10.26)

> **Статус (T-3548 @PM, 24.09.2026):** **COMPLETED + MERGED (§84) + ARCHIVED + deployment DEFERRED_TO_EPIC** — release policy **EPIC_ONLY** (пер-фича деплоя/бампа нет; `APP_VERSION` **2.58.30** без bump; bump 2.58.31 — в агрегатном релизе Эпика 3). Единый Reviewer gate **Approved C0/H0 (R3)** — binding Reviewed-Commit `e8646af` / Working-Tree-Hash `f1f1d544…` / Spec-Hash `e87e530f…`; **ADR-1026-15 (D1–D12) Accepted**; merge — `plans/ARCHITECTURE.md` **§84**; архив — `plans/archive/tool-chains-round1026/`. **Открыто:** **T-3549 `DEFERRED_TO_EPIC`** (агрегатный релиз Эпика 3, **НЕ «done/VERIFIED»**), **T-3550 handoff → A3** `unified-image-request`, non-blocking `L-R1026A2-1/-2/-3`; live-прогон §15–§17 — часть `DEFERRED_TO_EPIC`. **▶️ Следующая — A3 `unified-image-request`** (§18–§21/§25).
>
> **Историческая запись (Step 1/2 + сверка T-3527, 24.09.2026):** **PLANNING_CONSISTENT** — `tasks.md` сверен с `spec.md` и **ADR-1026-15** (D1–D12). Открытые вопросы (a)–(f) **закрыты**; колонки **SC/ADR** трассируемости заполнены; карта «**D1–D12 → задачи**» построена; **13 инвариантов** привязаны к **SC-A2-01…-13**; **Risk R3 подтверждён**; **deploy = EPIC_ONLY (deferred)**. **▶️ Следующий — Step 3 @Builder** (реализация блоков B–G: T-3528…T-3544). **▶️ Активная фича — A2 `tool-chains`** (Wave 2 Эпика 3, §15–§17).
> **ТЗ (verbatim, `plans/current_task.md`; файл только читается — R17/R18):**
> - **§15 «ПОСЛЕДОВАТЕЛЬНЫЕ TOOL CALLS»** (`:4520–4566`): «Инструменты должны уметь использовать результаты предыдущих инструментов.»; пример цепочки «Найти ссылку в доступном контексте → Вызвать инструмент извлечения статьи → Получить Markdown и метаданные → Передать текст в фактчек → Получить результаты проверки → Подготовить ответ»; «Не требовать, чтобы пользователь повторно присылал ссылку, если она однозначно доступна в контексте.»; «Не создавать специальный костыль, который работает только для этой фразы.»; «Общий координатор должен поддерживать зависимые вызовы инструментов. Если инструмент B требует результат инструмента A, сначала выполнить A. Затем передать его результат в B.»; «Не выполнять зависимые инструменты параллельно.»; «Независимые вызовы могут выполняться параллельно, если существующая архитектура это поддерживает.»; «Использовать существующую архитектуру tool calling, если она поддерживает последовательные вызовы.»; «Не реализовывать отдельный обработчик для каждой комбинации инструментов. Нужен общий механизм выполнения цепочек.»
> - **§16 «КОНТРАКТЫ ИНСТРУМЕНТОВ»** (`:4569–4607`): «Для каждого инструмента определить: название; назначение; аргументы; обязательные поля; тип результата; возможные ошибки; ограничения; стоимость, если применимо.»; «Результаты должны быть структурированными. Не передавать результат одного инструмента следующему исключительно как произвольный неструктурированный текст.»; пример JSON (`status/url/title/markdown/source_id`) — «Это пример, а не обязательная схема. Использовать существующие контракты, если они подходят.»; «Ошибки инструментов должны возвращаться в понятном структурированном формате. Не превращать каждую ошибку в общий отказ LLM.»
> - **§17 «ОГРАНИЧЕНИЕ ЦЕПОЧЕК»** (`:4610–4631`): «Не допускать бесконечных циклов инструментов. Предусмотреть: максимальное количество вызовов; общий тайм-аут; ограничение повторных попыток; защиту от повторного одинакового вызова; ограничение расходов; обработку частичного результата.»; «Начальные значения подобрать после анализа существующих инструментов. Не добавлять тяжёлую инфраструктуру ради гипотетических сложных сценариев. Для одного чата достаточно понятного и надёжного координатора.»
> **Границы (verbatim-ориентиры; НЕ реализуются в A2):** **§18–§25** (генерация изображений, единый image request — **A3/A4**); **§32–§35** (`get_user_context`, structured memory lookup — **A6**); **§38–§40/§42–§48** (`action`/`style`, новый Decision Making — **A7**); **§41/§44** (реакции Telegram, когда молчать/отвечать/реагировать — **A8**); **§49/§51** (события/карта вызовов, REUSE ExecutionGraph — **A9**). `generate_image` (§104) — **не трогать**; **§85-UI** — отдельная санкция (Δ каталога ≠ 0).
> **Тип:** backend/agentic (оркестрация инструментов) + **contracts** (структурированные контракты результатов/ошибок §16). **P0** — Wave 2 Эпика 3.
> **Зависит от:** **A0** ✅ (`plans/docs/agentic-audit-round1026.md`, APPROVED, durable-вход по анкорам `#tool-map-llm`, `#s12-3-5`, `#epic3-reuse`, `#epic3-a1-a10`, `#epic3-summary`), **A1** ✅ (`tool-coordinator-round1026`, Merge `plans/ARCHITECTURE.md` **§83**, **ADR-1026-14** Accepted, deploy **2.58.30** VERIFIED; D3: §15–§17 → A2; общий механизм цепочек = reuse `tool_loop`), **Эпик 1** ✅ (§70) и **Эпик 2** ✅ (S1–S10, deploy VERIFIED 2.58.29→2.58.30). Расширение канона инструментов под A2 **санкционировано** ADR-1026-13 **D3** (freeze 10 → расширение **только** A1/A2).
> **Baseline (Step 0 @Memory для A2, verified A1/A0/S10; в Step 1 НЕ перемеряется):** HEAD **`e8646af`** == `origin/master`, дерево чистое (в рабочем дереве — только машинный блок `plans/workflow_state.md`, за @Orchestrator); `APP_VERSION` **2.58.30** (`config/settings.py:1835`); pytest `.venv` **9082/0** (verified A1); JS **47/47** (verified A1); каталог **REGISTRY 469 / Settings 426 / categorized 444 / GROUPS 100 / `_TAB_BY_GROUP` 98 / TAB_RULES 21**; канон инструментов **10** (`services/tool_schemas.py:337–348`), `factcheck_tools` **3** (`:398–399`); **Δ DDL=0** (SQLite v12); прод активен (MainPID 652183). Точка отката — annotated-тег **`pre-round1026-a2`** → **`e8646af`** (T-3525, @DevOps); теги/бэкапы **не удаляются** (R18).
> **Карта кода (Step 0 @Memory, факт; baseline `e8646af`):** цепочки **уже есть** — `services/tool_loop.py` `chat_with_tools:89` (многораундовый цикл: assistant `tool_calls` `:167–171` → роль `tool` `:184–185` → повтор LLM `:172–190`; `TOOL_MAX_ROUNDS=4` `:38`; `_TOOL_CALLS_PER_ROUND_MAX=2` `:39`; усечение `:158–162`; plain-fallback `:125–135`; деградация `:191–196`; `ToolLoopResult` `:48–73` с `tool_trace`/`tool_context`); **зависимые A→B уже поддержаны** (внутри раунда вызовы последовательны, результат возвращается модели ролью `tool` → следующий раунд может вызвать B); **параллельности нет** (`asyncio.gather` отсутствует). Диспетчер `services/tool_router.py:dispatch:455–480` **всегда возвращает plain-строку** (часть инструментов — JSON-строку; ошибки — «ОШИБКА {name}: {Type}»; `_generate_image` JSON-статус `:1437–1468`). **GAP §16:** структурного контракта результата нет — результаты склеиваются в текст `tool_context` и идут в Синтезатор (`services/direct_chat_service.py:1132–1143`); `tool_trace` — метаданные. **GAP §15:** сценарий «ссылка→извлечение→фактчек» каноном 10 **не закрыт** — `WebContentExtractor.extract` (`services/web_content_extractor.py:59`) и `FactCheckService.check_claim` (`services/factcheck_service.py:77`) — **сервисы**, не LLM-тулы. **GAP §17:** максимум вызовов/частичная деградация есть; **общего тайм-аута цепочки, дедупа одинаковых вызовов, лимита расходов — нет** (per-tool тайм-ауты в роутере есть: search 25 с, download 180 с, summarize/lore 300 с, history 10 с — `tool_router.py:79–130`).
> **ID:** **T-3525…T-3551 (27)** — дублей нет, гэпов нет (максимум фактически занятого ID — **T-3524**, резерв A1; `T-3525` встречается в `plans/workflow_state.md` только как порог «T-IDs ≥ T-3525» в `next_action`, как ID задачи **не использовался**). Сквозная нумерация продолжена от A1 (`T-3500…T-3524`).
> **ADR:** ✅ **ADR-1026-15** (`adr-1026-15-tool-chain-contract-and-limits.md`, **D1–D12**, Status Proposed → Accepted по merge T-3547). Открытые вопросы (a)–(f) **закрыты** (см. раздел «Открытые вопросы» ниже). Карта «**Dn → задачи**» заполнена сверкой **T-3527** (см. раздел «Карта решений ADR-1026-15 → задачи»). **ADR-1013-3 = NOT_APPLICABLE** (промпты не меняются; миграции/эталоны не добавляются).
> **Deploy — ⏸ `EPIC_ONLY` (A2 отдельно НЕ деплоится):** release policy — **EPIC_ONLY**; bump `APP_VERSION` **2.58.30 → 2.58.31** входит в **агрегированный релиз Эпика 3** (Wave 2), **не** в пер-фича деплой. Задача **T-3549 = `DEFERRED_TO_EPIC`** (исполняется на границе эпика под агрегатным Reviewer gate; манифест/порядок/откат — там же); точную версию подтверждает @DevOps при эпик-релизе. Активация — автоматическая (лимиты/`fetch_article` — ON по умолчанию). Новые kill-switch'и **env-only** **`TOOL_CHAIN_LIMITS_ENABLED`** (default ON) и **`ARTICLE_TOOL_ENABLED`** (default ON) — `ClassVar`, **Δ каталога=0** (ADR-1026-15 D3/D5; прецедент ADR-1022-4/-5, ADR-1026-14 D6). Откат: hot — env-only kill-switch'и (`=false` → модельно-видимый legacy байт-в-байт); cold — annotated-тег **`pre-round1026-a2`** → `e8646af` + `git revert`; канон-откат промптов не требуется (промпты не менялись). Теги/бэкапы не удаляются (R18).
> **Risk (`Risk-Level` — финал @Architect, ADR-1026-15 **D10**):** ✅ **R3 (подтверждён; НЕ понижен до R2)** — A2 меняет рантайм-семантику **разделяемого** `tool_loop` живого direct-чата (blast radius — каждый диалог с инструментами), вводит изменение контракта `dispatch` (envelope), контроль потока цепочки (лимиты/дедуп/тайм-аут/расходы) и **+1 инструмент канона** (10→11). **KG-риски (4, Step 0):** **Critical** — `Risk-a2-infinite-call-loop`; **Critical** — `Risk-a2-live-chat-regression`; **High** — `Risk-a2-phrase-hack-per-combination`; **High** — `Risk-a2-unstructured-handoff` (неструктурированная передача + «ошибка инструмента → общий отказ LLM»). **Усиленные требования R3 (обязательны, D10):** (1) threat/failure-анализ с тестом на каждую угрозу; (2) rollback-доказательства (OFF → байт-в-байт legacy); (3) расширенная adversarial-приёмка + diff-аудит. Reviewer может повысить при скрытом изменении поведения / 3-м вызове / дублях / утечке. **`SpecDecision-a2-chain-contract` — ✅ CLOSED** (T-3526 → ADR-1026-15 D1).
> **⛔ Границы:** product code — только **Step 3 @Builder** (после Step 1/2 и сверки T-3527); **`plans/current_task.md` (immutable user-input) — НЕ трогать**; машинный блок `OPENCODE_WORKFLOW_STATE_V1` — не редактировать вручную; `spec.md`/ADR — **Step 2 @Architect ✅** (на сверке не правились); ✅ **сверка T-3527 выполнена — правила только этот `tasks.md`**; `plans/backlog.md`, `plans/metrics.md`, `plans/ARCHITECTURE.md` — на своих шагах (handoff/merge); durable-аудит A0 `plans/docs/agentic-audit-round1026.md` — **только чтение**. **Без коммитов из этого шага.** **@Scanner удалён** — единый Reviewer gate; нигде не назначать.

## Трассируемость (REQ → verbatim §ТЗ → блок → задачи → **SC/ADR**) — ✅ заполнено сверкой **T-3527**
> REQ-A2-01…-10 выделены из §15/§16/§17 (+ инварианты §14/§17 и KG-рисков Step 0). REQ-состав **не сужен**; новых REQ не добавлено; **orphan-REQ нет** (каждый REQ привязан к блоку/задаче/SC); **orphan-SC нет** (каждый SC-A2-01…-13 восходит к REQ и покрыт инвариантами ниже). Колонка **SC/ADR** — из `spec.md` §2/§8 + **ADR-1026-15** (D1–D12).
| REQ | Источник (verbatim, `current_task.md`) | Блок | Задачи | SC/ADR (заполнено T-3527) |
|---|---|---|---|---|
| REQ-A2-01 | §15 «Инструменты должны уметь использовать результаты предыдущих инструментов.» + «Если инструмент B требует результат инструмента A, сначала выполнить A. Затем передать его результат в B.» (`:4523–4524`, `:4558–4561`) | B, C | T-3528, T-3529, T-3530, T-3531 | **SC-A2-01**; **ADR D1, D2, D6** |
| REQ-A2-02 | §15 «Не выполнять зависимые инструменты параллельно.» (`:4563`) | C, D | T-3531, T-3534, T-3535 | **SC-A2-02**; **ADR D4** |
| REQ-A2-03 | §15 «Не создавать специальный костыль, который работает только для этой фразы.» (`:4552–4553`) + §14 «Не реализовывать отдельный обработчик для каждой комбинации инструментов. Нужен общий механизм выполнения цепочек.» (`:4514–4517`) | C | T-3531, T-3532, T-3533 | **SC-A2-03**; **ADR D4, D6** |
| REQ-A2-04 | §16 «Для каждого инструмента определить: название; назначение; аргументы; обязательные поля; тип результата; возможные ошибки; ограничения; стоимость, если применимо.» (`:4572–4581`) | B | T-3528, T-3529 | **SC-A2-04**; **ADR D1, D2** |
| REQ-A2-05 | §16 «Результаты должны быть структурированными. Не передавать результат одного инструмента следующему исключительно как произвольный неструктурированный текст.» + «Это пример, а не обязательная схема. Использовать существующие контракты, если они подходят.» (`:4583–4602`) | B | T-3528, T-3530 | **SC-A2-05**; **ADR D1** |
| REQ-A2-06 | §16 «Ошибки инструментов должны возвращаться в понятном структурированном формате. Не превращать каждую ошибку в общий отказ LLM.» (`:4604–4607`) | B | T-3529, T-3530 | **SC-A2-06**; **ADR D2** |
| REQ-A2-07 | §17 «Не допускать бесконечных циклов инструментов. Предусмотреть: максимальное количество вызовов; общий тайм-аут; ограничение повторных попыток; защиту от повторного одинакового вызова; ограничение расходов; обработку частичного результата.» (`:4613–4622`) | D | T-3534, T-3535, T-3536 | **SC-A2-07**; **ADR D3** |
| REQ-A2-08 | §15 «Независимые вызовы могут выполняться параллельно, если существующая архитектура это поддерживает.» (`:4565–4566`) | C, D | T-3533, T-3534 | **SC-A2-08**; **ADR D4** |
| REQ-A2-09 | §15 «Найти ссылку в доступном контексте… Не требовать, чтобы пользователь повторно присылал ссылку, если она однозначно доступна в контексте.» (`:4534–4550`) | C | T-3532 | **SC-A2-09**; **ADR D6** |
| REQ-A2-10 | §14/§15 «Использовать существующую архитектуру tool calling, если она поддерживает последовательные вызовы.» (`:4510–4512`) + §17 «Не добавлять тяжёлую инфраструктуру ради гипотетических сложных сценариев. Для одного чата достаточно понятного и надёжного координатора.» (`:4627–4631`) + инварианты (freeze 10 / санкция расширения ADR-1026-13 D3; нет 3-го LLM-вызова; §104/§85-UI; Δ DDL; R17/R18) | 0, C, D, E, G | T-3525, T-3531, T-3533, T-3534, T-3536, T-3537, T-3538, T-3543, T-3544 | **SC-A2-10, SC-A2-11, SC-A2-12, SC-A2-13**; **ADR D5, D7, D9, D11, D12** |

## Карта решений ADR-1026-15 (D1–D12) → задачи/блоки (сверка T-3527)
> Источник — `adr-1026-15-tool-chain-contract-and-limits.md` (D1–D12) + `spec.md` (§1/§2/§3/§5/§7/§9/§12). Каждое решение имеет ≥1 задачу; каждая задача блоков B–G восходит к REQ/SC/D.
| D | Решение (кратко) | SC | Задачи / блоки | Статус T-3527 |
|---|---|---|---|---|
| **D1** | Контракт §16 — **гибрид**: модельно-видимый канал (`role:"tool"`/`tool_context`) **сохранён** + **out-of-band envelope** на каждый вызов (`status/data/error/truncated/metered/duplicate/…`); reuse/extension `ToolLoopResult`/`tool_trace`; **второй контур не вводится** | SC-A2-05 | T-3528, T-3530 (блок B) | ✅ санкционировано |
| **D2** | Структурированные ошибки (`status:"error"`+`error_code`/`error_type`); ошибка инструмента **≠** общий отказ LLM; fail-safe/деградация сохранены | SC-A2-06 | T-3529, T-3530 (блок B) | ✅ |
| **D3** | §17-лимиты **поверх** `TOOL_MAX_ROUNDS=4`/≤2: **cap 6** (`TOOL_MAX_TOTAL_CALLS`), общий тайм-аут **360 c** (`TOOL_CHAIN_TIMEOUT_SECONDS`, мягкий; in-flight не режется), **дедуп ≤2** (`_TOOL_MAX_SAME_CALL`, имя+канон.аргументы), лимит платных **4** (`TOOL_CHAIN_MAX_METERED_CALLS`) + существующий `worker_budget.image_calls`; частичный результат `chain_timeout`/`chain_call_limit`/`chain_cost_limit`; env-only **`TOOL_CHAIN_LIMITS_ENABLED`** | SC-A2-07 | T-3534, T-3535, T-3536 (блок D); значения — `spec.md` §7 | ✅ (см. «Расхождение R-1») |
| **D4** | Параллельность — **санкционирована последовательность** как норма; `asyncio.gather` **не вводить**; зависимые — всегда последовательно | SC-A2-08 | T-3531, T-3533 (блок C) | ✅ |
| **D5** | Канон **+1 `fetch_article`** (URL→Markdown+метаданные; reuse `web_content_extractor.extract`), **атомарно** (схема+роутер+`active_tools`+тесты), канон **10→11**, «новое — в хвост» (ADR-1020-4); env-only **`ARTICLE_TOOL_ENABLED`**; **фактчек-инструмент — A7** | SC-A2-13 (входит в SC-A2-10/-11) | T-3537, T-3538 (блок E); T-3528 | ✅ расширение **санкционировано** |
| **D6** | Общий резолв ссылки из контекста (`ctx.resolved_url`), **не per-phrase**; неоднозначность → структурный `no_url` | SC-A2-09 (входит в SC-A2-03) | T-3532 (блок C), T-3538 | ✅ |
| **D7** | Санкции: **Δ DDL=0**, **Δ каталога=0**, §85-UI — **отдельная санкция**; kill-switch'и env-only `ClassVar` | SC-A2-11 | T-3525, T-3543, T-3544 (блоки 0/G) | ✅ |
| **D8** | Deploy/bump **2.58.30 → 2.58.31** — **EPIC_ONLY**: A2 **не деплоится отдельно**; входит в агрегированный релиз **Эпика 3** (манифест на границе эпика) | SC-A2-12 (входит) | T-3549 (**`DEFERRED_TO_EPIC`**), T-3548 | ⏸ deferred |
| **D9** | Границы: A3/A4 (§18–§25), A6 (§32–§35), A7 (§36–§37/§38–§48), A8 (§41/§44), A9 (§49/§51) — **не реализуются**; **§104 не трогать**; **REUSE ExecutionGraph**; handoff → A7 (фактчек reuse A2-envelope) | SC-A2-10, SC-A2-11 | T-3543, T-3544 (блок G); T-3545 | ✅ границы закреплены |
| **D10** | **Risk R3** (не понижен); усиленные требования: threat/failure-анализ, rollback-доказательства (**OFF → байт-в-байт**), расширенная adversarial-приёмка | SC-A2-06/-07/-12; инварианты 5/6/13 | T-3540, T-3541, T-3542, T-3545, T-3546 | ✅ подтверждён |
| **D11** | Наблюдаемость **R17-safe** (числа/коды/id/`error_type`/имена/длины); reuse существующих логов + ExecutionGraph; **вторая аналитика не создаётся** | SC-A2-10, SC-A2-12 | T-3542, T-3543 | ✅ |
| **D12** | AMEND/REUSE-карта: ADR-1020-7 §1 (effective AMEND `chain_*`), ADR-1026-13 D3 (санкция **использована**), ADR-1026-14 D3 (граница **исполнена**), ADR-1020-4/1022-5/1023-3/1025-24 REUSE; **ADR-1013-3 = NOT_APPLICABLE** (промпты не меняются) | SC-A2-11 | T-3537, T-3543, T-3547 | ✅ зафиксировано |

> **⚠️ Расхождение R-1 (зафиксировано PM; решение — за @Architect, НЕ решается PM).** В активационном сообщении T-3527 указан общий тайм-аут цепочки **300 c**, тогда как **ADR-1026-15 D3** и `spec.md` §4(b)/§7 **согласованно** задают **`TOOL_CHAIN_TIMEOUT_SECONDS = 360.0`** (обоснование: max per-tool 300 c + 60 c запас → одиночный тяжёлый инструмент не режется; при 300 c тяжёлый вызов оказался бы ровно на границе). `tasks.md` синхронизирован с **авторитетными ADR/spec (360 c)**. Требуется подтверждение @Architect: если верно **300 c** — нужен micro-amend **ADR-1026-15 D3** + `spec.md` §7 (и правка настоящей строки/инварианта 6 @PM); если верно **360 c** — расхождение снимается как неточность парафраза. Единственная точка контакта при реализации — **T-3534** (общий тайм-аут); остальные блоки (B/C/E/F/G) расхождением **не затронуты**.

## Приёмочные инварианты A2 (нарушение = НЕ принято) — привязаны к **SC-A2-01…-13** + **ADR-1026-15 (D)** (сверка T-3527)
> Опираются на §15–§17 + границы §18–§51 и KG-риски Step 0. **13 инвариантов сохранены без потерь**; каждый привязан к SC из `spec.md` §8 и решению ADR-1026-15. Покрытие **SC-A2-01…-13 полное**: **SC-A2-04** — инвариантом 4 (реестр/§16-контракт каждого из канона 11, `spec.md` §6); **SC-A2-09** — инвариантом 1 (общий контекстный резолв); **SC-A2-01** — инвариантом 2.
1. **Нет per-phrase костылей / per-combination обработчиков:** расширяется **общий механизм** цепочек на существующем `tool_loop`; специальных обработчиков под конкретную фразу («фактчекни статью») или под комбинацию инструментов **не создаётся**; контекстный резолв ссылки — **общий** (не per-phrase). **REQ-A2-03/-09; T-3531, T-3532, T-3533.** → **SC-A2-03, SC-A2-09**; **ADR D4, D6**; R3 подтверждён (KG `Risk-a2-phrase-hack-per-combination`).
2. **Зависимые вызовы — последовательно:** если B требует результат A — сначала A, затем результат A в B; зависимые **не параллелятся**. **REQ-A2-01/-02; T-3531.** → **SC-A2-01, SC-A2-02**; **ADR D1, D4**.
3. **Независимые — параллельно только при поддержке архитектуры:** параллелизм вводится **лишь** если архитектура это поддерживает (сейчас `asyncio.gather` нет → **обоснованная последовательность** легитимна); иначе — задокументированное обоснование последовательности, без тяжёлой инфраструктуры. **REQ-A2-08; T-3533.** → **SC-A2-08**; **ADR D4** (**последовательность санкционирована**; `asyncio.gather` не вводить).
4. **Результаты структурированы:** результат инструмента передаётся дальше в **структурированном** виде (гибрид D1: модельно-видимый канал + out-of-band envelope), не исключительно как произвольный неструктурированный текст; **reuse существующих контрактов** (`ToolLoopResult`/`tool_trace`, аддитивно); пример JSON §16 — пример, не обязательная схема. **REQ-A2-05; T-3528, T-3530.** → **SC-A2-05** (+**SC-A2-04** — §16-контракт каждого из канона **11**, `spec.md` §6); **ADR D1**; KG `Risk-a2-unstructured-handoff`.
5. **Ошибки инструментов структурированы:** ошибки возвращаются в понятном структурированном формате; **не превращать каждую ошибку в общий отказ LLM** (сохранить fail-safe/деградацию существующего контура). **REQ-A2-06; T-3529, T-3530.** → **SC-A2-06**; **ADR D2**.
6. **§17-лимиты (макс вызовов / общий тайм-аут / ретраи / дедуп / расходы / частичный результат):** без бесконечных циклов; значения подобраны **после анализа существующих инструментов**; лимиты строятся **поверх** существующих `TOOL_MAX_ROUNDS=4` / ≤2/раунд / per-tool тайм-аутов. **REQ-A2-07; T-3534, T-3535, T-3536.** → **SC-A2-07**; **ADR D3**. Значения (`spec.md` §7): cap **6** (`TOOL_MAX_TOTAL_CALLS`) / тайм-аут **360 c** (`TOOL_CHAIN_TIMEOUT_SECONDS`, мягкий) / дедуп **≤2** (`_TOOL_MAX_SAME_CALL`) / платные **4** (`TOOL_CHAIN_MAX_METERED_CALLS`); R3 подтверждён (KG `Risk-a2-infinite-call-loop`). **⚠️ см. «Расхождение R-1».**
7. **Расширение канона инструментов — ✅ санкционировано (ADR-1026-15 D5):** канон **10 → 11** ровно на **+1 общий `fetch_article`** (URL→Markdown+метаданные; атомарно: схема+роутер+`active_tools`+тесты; «новое — в хвост»), reuse `web_content_extractor.extract`; **дубликаты инструментов/сервисов запрещены**; **фактчек-инструмент и «URL+фактчек» — граница A7** (§36–§37, reuse A2-envelope), `factcheck_tools`=3 без изменений. **REQ-A2-10; T-3537, T-3538.** → **SC-A2-13** (входит в SC-A2-10/-11); **ADR D5**.
8. **Нет 3-го LLM-вызова:** A2 не добавляет LLM-вызовов; 2-вызовность System 2 direct-чата (`await_count==2`) и 2-вызовный Саммари сохраняются (ADR-1022-4/-5); где достаточно программной логики — LLM не вызывается. **REQ-A2-10; T-3536, T-3542.** → **SC-A2-10, SC-A2-12**; **ADR D5, D9, D12**.
9. **§104 / §85-UI вне scope:** `generate_image` (§104) — модель/провайдер/ключи/промпт/параметры/ошибки/порядок/прикрепление **без изменений**; §85-UI (Δ каталога ≠ 0) — **отдельная санкция**, вне A2. **REQ-A2-10; T-3543, T-3544.** → **SC-A2-11**; **ADR D7, D9**.
10. **REUSE ExecutionGraph:** события/наблюдаемость — на существующем `execution_graph_source`/`analytics.py`/`execution_graph.js`; **вторая система аналитики/карты вызовов не создаётся** (владелец карты — A9). **REQ-A2-10; T-3543.** → **SC-A2-10, SC-A2-11**; **ADR D9, D11, D12**.
11. **Δ DDL=0 и Δ каталога=0:** новых таблиц/колонок/индексов нет (SQLite v12); `param_catalog.py` вне diff; новые kill-switch'и — env-only (`ClassVar`); любое отклонение — **только по отдельной санкции**. **REQ-A2-10; T-3525, T-3543.** → **SC-A2-11**; **ADR D7**.
12. **R17/R18:** в логах/событиях — только числа/коды/id/host/HTTP-статус/`error_type`; без ключей/промптов/сырых текстов/сырых ответов LLM и без дампа аргументов/URL; теги/бэкапы не удаляются. **REQ-A2-10; T-3525, T-3542.** → **SC-A2-12**; **ADR D11, D12**.
13. **Обратная совместимость живого direct-чата:** при OFF/legacy-поведении — артефакты/поведение байт-в-байт; живой диалог не регрессирует (2-вызовность, финал `tool_loop`, `lore_compiled`-условия). OFF-путь: `TOOL_CHAIN_LIMITS_ENABLED=false` + `ARTICLE_TOOL_ENABLED=false`. **REQ-A2-10; T-3542.** → **SC-A2-12**; **ADR D1, D8**; KG `Risk-a2-live-chat-regression`.

## Связь с A3/A4 и смежными волнами (границы Эпика 3 — НЕ реализуется в A2)
> A2 — **Wave 2**: последовательные зависимые цепочки + структурированные контракты §16 + ограничение цепочек §17. Ниже — что A2 **НЕ** реализует. **§104 `generate_image` — НЕ ТРОГАТЬ**; **§85 UI — вне scope** (Δ каталога ≠ 0 требует отдельной санкции).
| §ТЗ | Тема | Ориентир |
|---|---|---|
| §18–§25 | Генерация изображений, единый image request | A3/A4 (A2 — граница) |
| §26–§31 | Дневной лимит изображений, Mini App, TZ/сброс | A5 |
| §32–§35 | `get_user_context`, structured memory lookup | A6 |
| §36–§37 | Комбинация URL+фактчек, безопасность tool output | A7 (граница разведена ADR-1026-15 **D9**: механизм цепочки/envelope — A2, фактчек-инструмент — A7) |
| §38–§40/§42–§48 | Новый Decision Making, разведение `action`/`style` | A7 |
| §41/§44 | Реакции Telegram, когда молчать/отвечать/реагировать | A8 |
| §49/§51 | Настройки Decision Making, диагностика, карта вызовов (REUSE ExecutionGraph) | A9 |

## Блок 0 — baseline / точка отката (T-3525)
- [x] **T-3525 [@DevOps]** — ✅ **DONE (24.09.2026).** **Выход:** annotated-тег **`pre-round1026-a2`** → **`e8646af`** (tag-object **`a3ea44d`**), бэкап `var/backups/a2-round1026-20260924-173812/` + `.env.bak.round1026-a2`; baseline-числа зафиксированы. **Критерий:** HEAD == `origin/master` == **`e8646af`** (verified); `APP_VERSION` **2.58.30**; pytest `.venv` **9082/0**, JS **47/47**, каталог **469/426/444/100/98/21**, канон инструментов **10**, **Δ DDL=0** (SQLite v12); тег/бэкапы целы (R18). **SC/ADR:** **SC-A2-11** (Δ DDL=0); **ADR D7, D12**; REQ-A2-10. **Зависимости:** Step 1 @PM ✅. *Deploy для A2 — **EPIC_ONLY** (см. шапку и T-3549); тег — страховка отката.*

## Блок A — spec + ADR (**Step 2 ✅ @Architect**, T-3526) + **сверка @PM (T-3527 ✅)**
> **SC/ADR:** REQ-A2-01…-10 → **SC-A2-01…-13**; **ADR-1026-15 D1–D12** (полное покрытие).
- [x] **T-3526 [@Architect]** — ✅ **DONE (24.09.2026).** **Выход:** `spec.md` (REQ-A2-01…-10, SC-A2-01…-13, трассировка §15/§16/§17 + границы) + **ADR-1026-15** (`adr-1026-15-tool-chain-contract-and-limits.md`, **D1–D12**, Status Proposed → Accepted по merge). **Критерий:** каждый REQ-A2-01…-10 имеет SC; открытые вопросы (a)–(f) **закрыты**; решения не противоречат §15–§17 и не сужают требования; каждая сущность §16 учтена (`spec.md` §6 — канон 11). **SC/ADR:** все **SC-A2-01…-13**; **ADR D1–D12**. **Расхождения:** нет, кроме **R-1** (активационный парафраз 300 c vs ADR/spec **360 c** — см. «Карта решений»). **Зависимости:** Step 1 @PM ✅.
- [x] **T-3527 [@PM]** — ✅ **DONE (24.09.2026).** **Результат:** **PLANNING_CONSISTENT** — заполнены колонки **SC/ADR** трассируемости (REQ-A2-01…-10 → SC-A2-01…-13 → D1–D12); построена **карта решений D1–D12 → задачи/блоки**; **13 инвариантов** привязаны к SC **без потерь** (покрытие SC-A2-01…-13 полное; **SC-A2-04** — через инварианты 4/5); **расширение канона санкционировано** (`fetch_article`, T-3537/T-3538); **границы** A3/A4/A6/A7/A8/A9 + §104/§85-UI закреплены (T-3543/T-3544); **ADR-1013-3 = NOT_APPLICABLE**; **Δ DDL=0 / Δ каталога=0**; env-only рубильники **`TOOL_CHAIN_LIMITS_ENABLED`** / **`ARTICLE_TOOL_ENABLED`**; **deploy = EPIC_ONLY / deferred** (T-3549); **Risk R3 подтверждён**. **Критерий:** каждый REQ-A2-01…-10 привязан к SC/решению ADR; scope/исключения/риск/приёмка/зависимости/deploy/rollback согласованы. **Расхождение R-1** зафиксировано к @Architect (не блокирует Step 3; точка контакта — T-3534). **Зависимости:** T-3526 ✅.

## Блок B — структурированные контракты результатов §16 (**гибрид D1**: модельно-видимый канал + out-of-band envelope; reuse `ToolLoopResult`/`tool_trace`) (T-3528…T-3530) — @Builder
> **SC/ADR:** **SC-A2-04, SC-A2-05, SC-A2-06** (REQ-A2-01/-04/-05/-06); **ADR D1, D2**.
> **Границы:** контракт — **гибрид (ADR D1)**; модельно-видимый канал (`role:"tool"`/`tool_context`) — **байт-в-байт**; канон после A2 = **11** (`fetch_article` — блок E); **первую аналитику не плодить** (§16-контракт ≠ карта вызовов A9).
- [x] **T-3528 [@Builder]** — **Цель:** единый **структурированный контракт результата** инструмента на границе `tool_router.dispatch` / `tool_loop` (§16): результат несёт тип/статус/данные/ошибку/ограничения, а не только plain-строку; **reuse существующих контрактов** (`ToolLoopResult`, `tool_trace`, JSON-статус `_generate_image`); не ломать существующий plain-канал там, где он нужен модели. **Выход:** правки + тесты. **Критерий:** **SC-A2-04, SC-A2-05**; REQ-A2-01/-04/-05; **ADR D1** (гибрид: **out-of-band envelope** + модельно-видимый канал байт-в-байт); существующие контракты переиспользованы (аддитивно); канон после A2 = 11. **Зависимости:** T-3526 ✅, T-3527 ✅.
- [x] **T-3529 [@Builder]** — **Цель:** **структурированные ошибки** инструментов (§16): «ОШИБКА {name}: {Type}» → структурированный формат (код/класс/сообщение/ограничение); **не превращать каждую ошибку в общий отказ LLM**; сохранить fail-safe/деградацию существующего контура. **Выход:** правки + тесты. **Критерий:** **SC-A2-06, SC-A2-04**; REQ-A2-06/-04; **ADR D2**; ошибки структурированы (`status:"error"`+`error_code`/`error_type`); модель получает различающий результат и не «падает целиком»; R17 (без сырых сообщений/URL). **Зависимости:** T-3528.
- [x] **T-3530 [@Builder]** — **Цель:** передача структурированного результата **следующему инструменту/Синтезатору** (не только как неструктурированный текст): сохранить `tool_context` как grounding-канал (совместимость с `direct_chat_service.py:1132–1143`), добавив структурированный **`ctx`-спутник** (`ctx.tool_results`), передаваемый в B. **Выход:** правки + тесты. **Критерий:** **SC-A2-05, SC-A2-06, SC-A2-01**; REQ-A2-01/-05/-06; **ADR D1**; структурированный handoff работает (A→B и → Синтезатор); неструктурированный `tool_context` не теряется; изоляция Вербализатора (A1) не нарушена. **Зависимости:** T-3528, T-3529.

## Блок C — зависимые цепочки §15 (общий механизм на `tool_loop`; результат A→B; без per-combination) (T-3531…T-3533) — @Builder
> **SC/ADR:** **SC-A2-01, SC-A2-02, SC-A2-03, SC-A2-08, SC-A2-09** (REQ-A2-01/-02/-03/-08/-09); **ADR D4** (последовательность санкционирована), **D6** (`ctx.resolved_url`), **D1**.
> **Границы:** расширяется **существующий** общий механизм (`tool_loop.py`), а не создаётся второй роутер/агент; per-combination обработчики и костыли под фразу запрещены; параллелизм **не вводится** — последовательность **санкционирована** (ADR D4).
- [x] **T-3531 [@Builder]** — **Цель:** общий механизм **зависимых вызовов A→B** на существующем многораундовом `tool_loop`: результат A доступен B (через роль `tool` + структурированный `ctx`-спутник); зависимые вызовы **последовательно** (не параллельно); зависимость определяется общим механизмом, а не хардкодом комбинации. **Выход:** правки + тесты. **Критерий:** **SC-A2-01, SC-A2-02, SC-A2-03**; REQ-A2-01/-02/-03; **ADR D1, D4**; A→B работает общим механизмом; зависимые не параллелятся; per-combination/фразовых костылей нет. **Зависимости:** T-3528, T-3530.
- [x] **T-3532 [@Builder]** — **Цель:** **общий** механизм использования **доступного контекста** (§15): ссылка/вложение, однозначно доступное в контексте, не требует повторной присылки пользователем; реализуется как общий резолв **`ctx.resolved_url`** (reuse `smartmodule_urls`; не костыль под конкретную фразу «фактчекни статью»). **Выход:** правки + тесты. **Критерий:** **SC-A2-03, SC-A2-09**; REQ-A2-09/-03; **ADR D6**; контекстный резолв общий, не per-phrase; повтор ссылки не требуется, когда она однозначно доступна; неоднозначность → `no_url`; границы («доступно/неоднозначно») задокументированы. **Зависимости:** T-3531.
- [x] **T-3533 [@Builder]** — **Цель:** **независимые вызовы** — параллельность **не вводится**; зафиксировать **санкционированную последовательность** (ADR D4); не добавлять тяжёлую инфраструктуру ради гипотетических сценариев. **Выход:** правки/заключение + тесты. **Критерий:** **SC-A2-08**; REQ-A2-08/-03/-10; **ADR D4** (`asyncio.gather` **не вводить**); документированное обоснование последовательности; второй роутер/агент не создан. **Зависимости:** T-3531.

## Блок D — лимиты/тайм-ауты/ретраи §17 (поверх существующих 4 раундов/≤2) (T-3534…T-3536) — @Builder
> **SC/ADR:** **SC-A2-07** (REQ-A2-07/-02/-08); **ADR D3**. Конкретные значения — `spec.md` §7: cap **6** (`TOOL_MAX_TOTAL_CALLS`) / тайм-аут **360 c** (`TOOL_CHAIN_TIMEOUT_SECONDS`; **⚠️ см. R-1**) / дедуп **≤2** (`_TOOL_MAX_SAME_CALL`) / платные **4** (`TOOL_CHAIN_MAX_METERED_CALLS`); рубильник `TOOL_CHAIN_LIMITS_ENABLED`.
> **Границы:** строится **поверх** `TOOL_MAX_ROUNDS=4` / `_TOOL_CALLS_PER_ROUND_MAX=2` / per-tool тайм-аутов; без бесконечных циклов и без тяжёлой инфраструктуры; in-flight вызов **не отменяется** (мягкий тайм-аут).
- [x] **T-3534 [@Builder]** — **Цель:** **общий тайм-аут цепочки** (`TOOL_CHAIN_TIMEOUT_SECONDS`, не только per-tool) поверх существующих раундов; **мягкий** (in-flight вызов не отменяется); по истечении — деградация `chain_timeout`/частичный результат, не исключение. **Выход:** правки + тесты. **Критерий:** **SC-A2-07**; REQ-A2-07/-02/-08; **ADR D3** (значение **360 c** — **⚠️ см. R-1**: подтвердить у @Architect); общий тайм-аут действует; бесконечных циклов нет; существующая деградация (`degraded`/`round_limit`) сохранена. **Зависимости:** T-3526 ✅, T-3531.
- [x] **T-3535 [@Builder]** — **Цель:** **дедуп повторного одинакового вызова** (`_TOOL_MAX_SAME_CALL=2`, имя+канон.аргументы `json.dumps(sort_keys=True)`) + **ограничение повторных попыток** + **лимит расходов** (`TOOL_CHAIN_MAX_METERED_CALLS=4`; §17 / **ADR D3**): одинаковый вызов не исполняется повторно сверх лимита (→ `duplicate=true`, последний результат); ретраи ограничены; расходы (metered: search/article/summarize/download/lore/image/transcribe) ограничены (→ `chain_cost_limit`). **Выход:** правки + тесты. **Критерий:** **SC-A2-07**; REQ-A2-07; **ADR D3**; дедуп/ретраи/расходы действуют; сравнение вызовов — общее (имя+аргументы), не per-combination; `worker_budget.image_calls` **не дублируется**. **Зависимости:** T-3534.
- [x] **T-3536 [@Builder]** — **Цель:** **обработка частичного результата** при исчерпании лимитов/ошибке (суммарный cap `TOOL_MAX_TOTAL_CALLS=6` + причины `chain_call_limit`/`chain_timeout`/`chain_cost_limit`); для одного чата — понятный и надёжный координатор (без новой инфраструктуры); §104/§85-UI/3-й LLM-вызов не затрагиваются. **Выход:** правки + тесты. **Критерий:** **SC-A2-07** (входит в SC-A2-10); REQ-A2-07/-10; **ADR D3, D9**; частичный результат отдаётся (не общий отказ LLM); `tool_context` не теряется; тяжёлой инфраструктуры нет; инварианты соблюдены. **Зависимости:** T-3535.

## Блок E — **расширение канона ✅ санкционировано** (ADR-1026-15 **D5**): **+1 `fetch_article`** (T-3537…T-3538) — @Architect/@Builder
> **SC/ADR:** **SC-A2-13, SC-A2-10, SC-A2-11** (REQ-A2-10); **ADR D5**.
> **Решение — Step 2 (вопрос (d)) ✅:** канон **10 → 11** ровно на **+1 общий `fetch_article`** (URL→Markdown+метаданные; reuse `web_content_extractor.extract`; атомарно — схема+роутер+`active_tools`+тесты; «новое — в хвост»); **фактчек-инструмент и «URL+фактчек» (§36–§37) — граница A7**; `factcheck_tools`=3 без изменений; **дубликаты инструментов/сервисов запрещены**; основание — ADR-1026-13 D3 (**санкция использована**). Рубильник **`ARTICLE_TOOL_ENABLED`** (env-only).
- [x] **T-3537 [@Architect/@Builder]** — **Цель:** применить **санкционированное решение ADR-1026-15 D5**: канон расширяется ровно на **+1 `fetch_article`** (URL→Markdown+метаданные через reuse `web_content_extractor.extract`); определить контракт §16 нового инструмента (JSON `{status,url,source_id,chars,truncated,markdown[,title]}` + envelope-спутник; per-tool `_ARTICLE_TOOL_TIMEOUT = 45.0`). **Фактчек-инструмент и «URL+фактчек» — граница A7** (не реализуются). **Выход:** решение/карта + спека инструмента. **Критерий:** **SC-A2-13**; REQ-A2-10; **ADR D5**; дубликатов нет; `factcheck_tools`=3 без изменений. **Зависимости:** T-3526 ✅.
- [x] **T-3538 [@Builder]** — **Цель:** реализовать **`fetch_article`** (reuse `web_content_extractor.extract` + `smartmodule_urls`; `url` optional → при отсутствии `ctx.resolved_url`) **атомарно** — схема + регистрация в `TOOL_CALLING_TOOLS` (**в хвост**, ADR-1020-4) + метод роутера + `active_tools` + тесты; канон **10 → 11**; рубильник env-only **`ARTICLE_TOOL_ENABLED`** (OFF → инструмент не объявляется). **Фактчек-инструмент не реализуется** (граница A7). **Выход:** правки + тесты. **Критерий:** **SC-A2-13** (входит в SC-A2-10/-11); REQ-A2-10; **ADR D5**; атомарность; дубликатов/второго контура нет. **Второй ветки «NOT_APPLICABLE» нет — расширение санкционировано.** **Зависимости:** T-3537.

## Блок F — тесты / R17 / регресс живого direct-чата (OFF/legacy; 2-вызовность) (T-3539…T-3542) — @Builder
> **SC/ADR:** **SC-A2-01…SC-A2-13** (тесты покрывают все SC); **ADR D10** (усиленная R3-приёмка), **D11** (R17-safe), **D1/D3** (границы 6/360/≤2/4).
- [x] **T-3539 [@Builder]** — **Цель:** тесты **§15 (зависимые цепочки)**: A→B последовательны; результат A используется B; контекстный резолв (`ctx.resolved_url`); **отсутствие per-combination/фразовых костылей**; независимые — **обоснованная последовательность** (`asyncio.gather` отсутствует). **Выход:** тесты. **Критерий:** **SC-A2-01, SC-A2-02, SC-A2-03, SC-A2-08, SC-A2-09**; REQ-A2-01/-02/-03/-08/-09; **ADR D4, D6**; §15 покрыт; регрессов нет. **Зависимости:** T-3531, T-3532, T-3533.
- [x] **T-3540 [@Builder]** — **Цель:** тесты **§16 (контракты/структурированные результаты и ошибки)**: envelope на каждый вызов (out-of-band); структурированные ошибки; ошибка инструмента не превращается в общий отказ LLM; reuse `ToolLoopResult`/`tool_trace`; модельно-видимый контент не изменён. **Выход:** тесты. **Критерий:** **SC-A2-04, SC-A2-05, SC-A2-06**; REQ-A2-04/-05/-06; **ADR D1, D2**; §16 покрыт; формат ошибок проверен. **Зависимости:** T-3528, T-3529, T-3530.
- [x] **T-3541 [@Builder]** — **Цель:** тесты **§17 (лимиты)**: суммарный cap 6; общий тайм-аут 360 c (мягкий; in-flight не отменяется); ограничение ретраев/дедуп ≤2; лимит расходов 4; частичный результат; отсутствие бесконечных циклов; границы `chain_timeout`/`chain_call_limit`/`chain_cost_limit`. **Выход:** тесты. **Критерий:** **SC-A2-07**; REQ-A2-07; **ADR D3**; §17 покрыт; граничные значения **6/360/≤2/4** (`spec.md` §7; **⚠️ R-1**) проверены. **Зависимости:** T-3534, T-3535, T-3536.
- [x] **T-3542 [@Builder]** — **Цель:** **R17-тесты** + **регресс живого direct-чата**: в логах/событиях — только числа/коды/id/`error_type`/имена/длины; без ключей/промптов/сырых текстов/аргументов/URL. OFF/legacy-поведение — байт-в-байт; **2-вызовность** (`await_count==2`); `tool_loop`-финал/`lore_compiled`-условия сохранены; `DIRECT_COORDINATOR_ENABLED` (A1) и kill-switch'и **`TOOL_CHAIN_LIMITS_ENABLED=false`** + **`ARTICLE_TOOL_ENABLED=false`** → точный legacy-путь. **Выход:** тесты. **Критерий:** **SC-A2-10, SC-A2-12**; REQ-A2-10/-08; **ADR D10, D11**; живой диалог не регрессирует; R17 соблюдён; 2-вызовность и OFF/legacy подтверждены; **KG `Risk-a2-live-chat-regression` закрыт**. **Зависимости:** T-3531, T-3534, T-3539, T-3540, T-3541.

## Блок G — интеграция/границы (§104 / §85-UI / A1 / A3 / A4 / A7; ExecutionGraph REUSE) (T-3543…T-3544) — @Architect/@Builder
> **SC/ADR:** **SC-A2-10, SC-A2-11** (REQ-A2-10); **ADR D7** (Δ DDL=0 / Δ каталога=0 / §85-UI), **D9** (границы волн + REUSE ExecutionGraph), **D11** (R17-safe наблюдаемость), **D12**. Судьба T-3537/T-3538 — **расширение санкционировано** (`fetch_article`); T-3543/T-3544 — **границы**.
- [x] **T-3543 [@Architect/@Builder]** — **Цель:** подтвердить границы и инварианты: **§104 `generate_image`** не тронут; **§85-UI** — отдельная санкция (Δ каталога ≠ 0), вне A2; **REUSE ExecutionGraph** (вторая аналитика не создана); **Δ DDL=0** и **Δ каталога=0** (ADR D7); канон **11** (`fetch_article` санкционирован ADR D5; при `ARTICLE_TOOL_ENABLED=OFF` — эффективно 10) / дубликаты запрещены; `factcheck_tools`=3; A1 (координатор, изоляция Вербализатора, `DIRECT_COORDINATOR_ENABLED`) не сломан; нет 3-го LLM-вызова. **Выход:** evidence/diff-аудит. **Критерий:** **SC-A2-11**; REQ-A2-10; **ADR D7, D9, D11, D12**; границы не нарушены; Δ DDL / Δ каталога / инструменты / аналитика подтверждены. **Зависимости:** T-3538, T-3542.
- [x] **T-3544 [@Architect/@Builder]** — **Цель:** границы волн: подтвердить, что §18–§25 (**A3/A4**), §32–§35 (**A6**), §36–§37/§38–§48 (**A7**), §41/§44 (**A8**), §49/§51 (**A9**) **не реализованы** в A2; зафиксировать handoff-вход для A3/A4 (единый image request / генерация из досье) и **handoff → A7** (фактчек-инструмент/«URL+фактчек» reuse A2-envelope; `action`/`style` — A7). **Выход:** заключение/границы. **Критерий:** **SC-A2-10, SC-A2-11**; REQ-A2-10; **ADR D9**; ни одна граница не нарушена; вход A3/A4/A7 зафиксирован. **Зависимости:** T-3543.

## Блок H — финализация (единый Reviewer gate БЕЗ Scanner, merge, архивация, **deploy = EPIC_ONLY**, handoff → A3/A4) (T-3545…T-3551)
> **SC/ADR:** все **SC-A2-01…-13**; **ADR D8** (deploy/bump **EPIC_ONLY**), **D9** (handoff A3/A4), **D10** (R3-приёмка), **D12** (AMEND/REUSE). **T-3549 — `DEFERRED_TO_EPIC`** (в агрегированном релизе Эпика 3).
- [x] **T-3545 [@Reviewer]** — **Цель:** **линза 1 единого Reviewer gate** (requirements/correctness): §15 (зависимые A→B последовательны; независимые — по архитектуре; контекстный резолв; нет костыля/per-combination); §16 (структурированные результаты/ошибки; не общий отказ LLM); §17 (макс вызовов/тайм-аут/ретраи/дедуп/расходы/частичный результат; без бесконечных циклов); REQ-A2-01…-10; **SC-A2-01…-13**; нет 3-го LLM-вызова; отсутствие ложных закрытий. **Выход:** `review.md` с вердиктом. **Критерий:** **ADR D10** (усиленная R3-приёмка: threat/failure-анализ + rollback-доказательства OFF→байт-в-байт); значение тайм-аута соответствует подтверждённому @Architect (см. **R-1**); все release-блокеры закрыты или отклонены письменно; Critical/High = 0 → «к приёмке ДА» либо блокеры возвращены. **Зависимости:** T-3542, T-3543, T-3544.
- [x] **T-3546 [@Reviewer]** — **Цель:** **линза 2 единого Reviewer gate** — focused change audit (все содержательные обязанности удалённого @Scanner): канон инструментов (**11**, `fetch_article` санкционирован; дубликатов нет; `factcheck_tools`=3); **Δ DDL / Δ каталога** (ADR D7: **0/0**); R17/R18; **§104 `generate_image`** вне diff; **§85-UI** вне diff; REUSE ExecutionGraph (второй аналитики нет); обратная совместимость живого direct-чата (OFF/legacy, 2-вызовность); отсутствие утечек и скрытых побочных эффектов; пересчитанные `Reviewed-Commit`/`Working-Tree-Hash`/`Spec-Hash`. **Выход:** единый авторитетный результат в **`review.md`** (отдельный Scanner-отчёт **не создаётся** — Scanner удалён). **Критерий:** **ADR D7, D11, D12**; Critical/High = 0 → «к приёмке/эпик-релизу ДА» либо блокеры возвращены; binding соответствует фактическому состоянию. **Зависимости:** T-3545.
- [x] **T-3547 [@Architect]** — **Цель:** merge: раздел `plans/ARCHITECTURE.md` (**номер по факту, ожидается §84**) + финализация **ADR-1026-15** (Accepted) + карта решений; **AMEND-запись ADR-1026-13 D3 в durable-аудит** (санкция использования канона 10→11 — D12); реконсиляция, если Step 2 менял архитектурные решения. **Выход:** раздел + ADR. **Критерий:** **ADR D12**; архитектура сверена; контракты §16 / цепочки §15 / лимиты §17 / границы задокументированы. **Зависимости:** T-3545, T-3546 (при findings — rework @Builder до merge).
- [x] **T-3548 [@PM]** — **Цель:** архивация фичи в `plans/archive/` с сохранением ID/ссылок. **Выход:** папка `plans/archive/tool-chains-round1026/` (+ **`deployment: DEFERRED_TO_EPIC`** в карточке фичи: A2 **принята в pending epic-release candidate Эпика 3**, но **не задеплоена**). **Критерий:** только после подтверждённого единого **`Approved`** + binding + reconcile @Architect; **deploy НЕ требуется** для архивации при `EPIC_ONLY` — фиксируется `DEFERRED_TO_EPIC` (не на основании чекбоксов @Builder); пути/ссылки разрешены, истории не теряются. **SC/ADR:** **ADR D8 (EPIC_ONLY)**. **Зависимости:** T-3547.
- [ ] **T-3549 [@DevOps]** — ⏸ **`DEFERRED_TO_EPIC` — НЕ «done/VERIFIED» (release policy `EPIC_ONLY`; как пер-фича деплой НЕ выполняется; статус подтверждён архивацией T-3548 @PM, 24.09.2026).** **Цель (на границе Эпика 3, под агрегатным Reviewer gate):** включить A2 в **агрегированный релиз Эпика 3** — bump `APP_VERSION` **2.58.30 → 2.58.31** (точную версию подтверждает @DevOps при эпик-релизе) + `README.md` + cache-bust; push без force; прод ff; `/api/health` 200; `database is locked`=0; применимые §114-проверки; манифест/порядок/откат эпика. Активация — автоматическая; рубильники env-only (**`TOOL_CHAIN_LIMITS_ENABLED`** / **`ARTICLE_TOOL_ENABLED`**). **Выход:** `deployment.md` (**VERIFIED** в составе эпик-релиза либо явно **NOT_APPLICABLE**, если агрегатный эпик сочтён read-only). **Критерий:** вердикт с evidence **в контексте эпик-релиза**; откат — kill-switch'и + annotated-тег **`pre-round1026-a2`** → **`e8646af`** + `git revert`; теги/бэкапы не удаляются (R18). **SC/ADR:** **SC-A2-12**; **ADR D8 (EPIC_ONLY), D10**. **Зависимости:** T-3548; **исполнение — на границе эпика**. **⚠️ Согласование @Architect:** `spec.md` §9/§12 формулируют T-3549 как «deploy ДА/VERIFIED»; привести формулировку spec к **EPIC_ONLY/deferred** (правка spec — @Architect, не PM).
- [ ] **T-3550 [@PM/@Orchestrator]** — 📤 **HANDOFF OPEN (исполняется @PM/@Orchestrator при старте A3).** **Цель:** handoff → **A3/A4** (единый image request / генерация изображений, §18–§25): зафиксировать вход A3/A4, обновить backlog-указатель/гейты. **Выход:** handoff-запись. **Критерий:** границы A2/A3/A4 разведены; следующий actionable определён; ни одна открытая граница не потеряна. **Зависимости:** T-3549.
- [ ] **T-3551 [@PM]** — **N/A (резерв не потребовался: трассировка A2 замкнута сверкой T-3527; подтверждено T-3548 @PM).** **Цель:** (резерв) финальная проверка трассируемости A2: полный REQ-A2-01…-10 → §15/§16/§17 → блок → задача → SC → ADR → evidence замкнут; метрики фичи переданы @Orchestrator. **Выход:** подтверждение покрытия. **Критерий:** orphan-REQ нет; каждый REQ имеет задачу/SC; состав задач не сужен. **Зависимости:** T-3550. **Резерв — N/A при замкнутой трассировке на Step 2/T-3527.**

## Открытые вопросы (a)–(f) → ✅ **ЗАКРЫТЫ Step 2 @Architect (ADR-1026-15) и зафиксированы сверкой T-3527**
> Эти вопросы PM **не решал** (design/architecture). Ниже — итоговые ответы ADR-1026-15; полные формулировки — `spec.md` §4/§12 и ADR.
- **(a) Контракт §16 → ✅ ГИБРИД (D1):** модельно-видимый канал (`role:"tool"`/`tool_context`) сохранён + out-of-band envelope; reuse/extension `ToolLoopResult`/`tool_trace`; второй контур не вводится. Задачи T-3528…T-3530.
- **(b) §17-лимиты → ✅ D3:** суммарный cap **6** (`TOOL_MAX_TOTAL_CALLS`); общий тайм-аут **360 c** (`TOOL_CHAIN_TIMEOUT_SECONDS`, мягкий, in-flight не режется); дедуп/ретраи `_TOOL_MAX_SAME_CALL=2` (имя+канон.аргументы); расходы `TOOL_CHAIN_MAX_METERED_CALLS=4` + `worker_budget`; частичный результат `chain_timeout`/`chain_call_limit`/`chain_cost_limit`; рубильник env-only `TOOL_CHAIN_LIMITS_ENABLED`. Задачи T-3534…T-3536. **⚠️ R-1:** активационный парафраз указывал **300 c** — ADR/spec = **360 c**; подтвердить у @Architect.
- **(c) Параллельность → ✅ D4:** санкционирована **последовательность**; `asyncio.gather` **не вводится** (архитектура не concurrency-safe). Задача T-3533.
- **(d) Расширение канона → ✅ D5:** **+1 `fetch_article`** (атомарно, reuse `web_content_extractor.extract`), канон **10→11**; **фактчек-инструмент/§36–§37 — граница A7**; рубильник `ARTICLE_TOOL_ENABLED`. Задачи T-3537, T-3538.
- **(e) Границы A3/A4/A7 → ✅ D9:** A2 = общий механизм цепочек + контракты §16 + лимиты §17 + `fetch_article` + контекстный резолв; A7 = фактчек-инструмент/§36–§37 (reuse A2-envelope); A3/A4/A6/A8/A9 — не реализуются. Задачи T-3543, T-3544.
- **(f) Δ DDL / Δ каталога, deploy/bump → ✅ D7/D8:** **Δ DDL=0**, **Δ каталога=0**; kill-switch'и env-only (`TOOL_CHAIN_LIMITS_ENABLED`, `ARTICLE_TOOL_ENABLED`); deploy — **EPIC_ONLY** (bump 2.58.30 → 2.58.31 в агрегированном релизе Эпика 3; T-3549 **deferred**); Risk **R3**. Задачи T-3525, T-3549.

## Миграционное примечание
- **@Scanner удалён (24.09.2026).** Его содержательные обязанности (focused change audit: инварианты diff, Δ DDL/каталог, R17/R18, §104/§85-UI, REUSE ExecutionGraph, обратная совместимость, утечки/побочные эффекты) переданы **линзе 2 единого Reviewer gate** (T-3546). **Отдельный Scanner-отчёт не создаётся**; @Scanner **нигде не назначается**. Прецедент — A1 (`tool-coordinator-round1026`, T-3518/T-3519).
- **Дедлайн миграции ADR:** ✅ ADR-1026-15 финализирован на **Step 2** (T-3526, D1–D12); `SpecDecision-a2-chain-contract` — ✅ **CLOSED** (ADR D1); **Accepted** — по merge (T-3547).
- **Канон промптов ADR-1013-3:** ✅ **NOT_APPLICABLE** — промпты **не меняются** (ADR-1026-15 D12); `PREV_*`/миграции/эталоны не добавляются; правок промпт-канона нет.

## Итоги выполнения (архивация T-3548 @PM, 24.09.2026)
- **T-3525 ✅** — baseline/точка отката: annotated-тег `pre-round1026-a2` (tag-obj `a3ea44d`) → `e8646af`; `APP_VERSION` 2.58.30; pytest 9082/0; JS 47/47; канон 10; Δ DDL=0.
- **T-3526 ✅** — `spec.md` (REQ-A2-01…-10 / SC-A2-01…-13) + **ADR-1026-15** (D1–D12).
- **T-3527 ✅** — сверка @PM: **PLANNING_CONSISTENT** (SC/ADR-колонки, карта D1–D12 → задачи, 13 инвариантов, Risk R3).
- **T-3528…T-3544 ✅** — реализация/тесты блоков B–G: out-of-band envelope §16 (модельно-видимый канал байт-в-байт), структурированные ошибки, зависимые цепочки §15 + общий `ctx.resolved_url`, §17-лимиты (cap 6 / мягкий 360 c / дедуп ≤2 / платные ≤4 / частичный результат), канон +1 `fetch_article` (10→11), границы A3–A9; pytest **9125/0** (+43), JS **47/47**, Δ DDL=0 / Δ каталога=0.
- **T-3545/T-3546 ✅** — единый Reviewer gate: **Approved C0/H0** (линза 1 requirements/correctness + линза 2 focused change audit; Scanner удалён намеренно); binding Reviewed-Commit `e8646af` / Working-Tree-Hash `f1f1d544…` / Spec-Hash `e87e530f…`; отклонения D-a/D-b/D-c — non-blocking.
- **T-3547 ✅** — merge `plans/ARCHITECTURE.md` **§84** + **ADR-1026-15 Accepted** + AMEND-запись в durable-аудите (`plans/docs/agentic-audit-round1026.md`).
- **T-3548 ✅** — эта архивация: папка перенесена в `plans/archive/tool-chains-round1026/` (**6 файлов**; SHA-256 до/после переноса идентичны 6/6; финализирован **только `tasks.md`** — статусы/шапка); **`deployment.md` отсутствует** — при `deployment: DEFERRED_TO_EPIC` это корректно (пер-фича деплоя нет).
- **T-3549 ⏸ `DEFERRED_TO_EPIC`** — агрегатный релиз Эпика 3 (bump 2.58.30 → 2.58.31, эпик-манифест/порядок/откат) — **НЕ «done/VERIFIED»**.
- **T-3550 📤 handoff → A3** — вход A3/A4 зафиксирован (envelope-контракт, §17-лимиты, канон 11 `fetch_article`, `ctx.resolved_url`, общий механизм цепочек).
- **T-3551 — N/A** (резерв не потребовался; трассировка замкнута T-3527).
