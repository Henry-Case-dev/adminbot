# Задачи: multi-chat-rbac-byok

Раунд 10 (07.09.2026), **часть 1 эпика** «Multi-chat scaling» (research `plans/docs/multi-chat-scaling-research.md`, вариант A, разрешение владельца 07.09: «запланировать 3 части»). Нумерация продолжает T-842 → **T-843…T-869**; база HEAD fac1b9f (раунд 9 заархивирован; дерево на момент планирования: untracked `plans/MEMORY.md` — НЕ трогать, НЕ индексировать). Эпик части = **граничная RBAC + BYOK + параметр-разделение (backend + PG)**: (1.1) роли Global Admin / Local Admin / Moderator / User (архитектура поддерживает Custom — `bot_roles.is_custom` уже есть), (1.2) per-param права min-role view/edit в `param_permissions`, у Global — роль-пикер у каждого инпута TMA, (1.3) слой overrides `chat_profiles.chat_params` + резолв hot.get (chat_params → bot_settings), промпты «as-is» локальному админу + «Использовать мой», BYOK (локальный ключ в своём чате, поля визуально пустые, без своего ключа — глобальный с жёстким бюджетом). spec.md при планировании НЕ создаётся; раздел A зеркалит PM-базу дизайна и протокол Q1–Q9, закрывает @Architect в T-843 правкой (прецедент раундов 7/9). Связанные части — отдельные фичи раунда: `permsoc-module-isolation` (T-878…T-889), `feature-gates-worker-budget` (T-890…T-902), `tma-ia-progressive-disclosure` (T-903…T-913), `global-oversight-dashboard` (T-914…T-924), `tma-ui-fixes` (T-870…T-877).

## Контекст (Step 0; факты проверены @PM при планировании по HEAD fac1b9f)

**PG-схема.** `services/pg_db.py` `DDL_STATEMENTS` (:33-70): `bot_settings` (key TEXT PK, value JSONB, category), `bot_roles` (role_name, permissions JSONB, is_custom), `bot_admins` (telegram_id, role_name); `chat_profiles` (chat_id PK, manual_lore, auto_lore, auto_enabled, auto_period_hours, auto_window_hours, is_active, updated_at, relations JSONB, relations_enabled) — **`chat_params` JSONB НЕ существует** (добавлять аддитивно, прецедент: relations/relations_enabled — дельты D-3 раунда 9); `chat_admins` ((chat_id, telegram_id)), `chat_lore_history`, `chat_links`. Сиды: `SEED_CATEGORIES` (:110-113), moderator-дефолт `sections [limits] + control.*` (НЕ расширять без решения).

**RBAC v2.** `services/permissions.py` — неймспейсы: `section.`/`param.`/`key.`/`action.`, wildcard; `web/api/deps.py`: `get_tma_user` (:80), `requires_permission(required)` (:182), `can_edit_param`/`can_view_key_value` (:198-206); known_sections — `services/param_catalog.py:1072`. Per-chat ролей НЕТ (только глобальные bot_roles/bot_admins + chat_admins-строки для лора).

**Конфиг-резолв.** `services/hot_config.py::hot.get(pg_key, default)` → глобальный ConfigCache (`services/config_cache.py`: init/load_all/get/set/upsert/reload/close) → .env-дефолты; per-chat слоя НЕТ. `services/llm_client.py::_current_api_key()` — `hot.get("keys.llm_api_key", settings.LLM_API_KEY)` + fallback `keys.llm_fallback_api_key` + embed-каскад (Google AI Studio); BYOK-слоя нет.

**TMA.** `web/app.js` TABS (:18-47, типы config|access|status|info; прецедент кастомной вкладки memory_rag :33-37), `activeTab` 'status' (:77), `setTab` (:310), `canViewTab` (:369), `hasPerm`, `api()` (~:264); `web/index.html` — v-else-if по activeTab. Секреты: GET /api/config → {configured, last4}; POST — per-key права deps.py. Аудит `tests/test_frontend_tab_mapping.py`.

**Прецеденты.** Optimistic 409 + транзакция+NOTIFY+история — ChatLoreStore (T-772/T-779); per-chat кэш с NOTIFY/TTL — ChatLoreCache (T-774); ADD COLUMN IF NOT EXISTS (relations) — T-818; каст-хелпер унификации — T-651 (F-1, актуально); SQLite-память НЕ трогать (Epic 86 frozen).

## A. Дизайн (PM-база; открытые вопросы Q1–Q9 закрывает @Architect в T-843 правкой в этом разделе)

- [ ] **T-843 (A1):** @Architect — протокол дизайна части 1: (Q1) **модель ролей**: `bot_roles.role_type TEXT` (аддитивная колонка: 'global_admin'/'local_admin'/'moderator'/'user'/NULL=custom — PM-база) vs permission-пресеты без новой колонки; иерархия `ROLE_RANK` (global_admin > local_admin > moderator > user > none) — где живёт (permissions.py vs новый services/roles.py); что делают role_type-сиды (4 встроенных строчки в bot_roles + их permission-сеты; seeded moderator НЕ расширять); (Q2) **`param_permissions` схема**: отдельная таблица `param_permissions (key TEXT PK, value JSONB)` (PM-база: `{"view_min_role":str,"edit_min_role":str,"hidden_from_local":bool}`, значение из `bot_settings`-паттерна) vs колонка в bot_settings; дефолт-матрица для существующих 372 ключей (PM-база: view=user, edit по категории: keys/секреты → global_admin; prompts → local_admin может видеть ('as-is') но редактировать только глобальный/локальный?; limits/flags/reactions/content/memory → moderator; чёрный список hidden_from_local: keys-поля); (Q3) **per-chat доступы**: семантика `access_for(telegram_id, chat_id)` (global admin → всё; строка chat_admins → local_admin ЭТОГО чата; moderator-глобальная роль → чаты где он в chat_admins?; user → read-only «Статус»/«Как это работает» — какой параметр чата при выборе, если чатов N); (Q4) **chat_params версионирование**: плоский JSONB chat_id→key→value модели (PM-база) vs версия per-key; multi-field-оптимист (409 по `updated_at` chat_profiles — прецедент ChatLoreConflict); что происходит с устаревшим ключом (добавлен новый REGISTRY-параметр в глобал — chat_params без правки остаётся как есть/надо purge); (Q5) **граница per-chat-параметров**: whitelist (research §4.3 «15–30 профильных»: direct-тон/персона, лимиты сна/ностальгии, модули) vs все категории кроме keys; какой путь вызова `hot_chat` в воркерах (dream/nostalgia/lore) и direct_chat; как НЕ трогать ConfigCache-горячий путь; (Q6) **BYOK-хранение**: `chat_keys (chat_id, key_name, key_value, created_at, PK(chat_id,key_name))` (PM-база) vs в chat_params; маска last4, отдача API только {configured, last4}; бюджет-лимиты «без своего ключа → глобальный с жёстким лимитом» (формула: `limits.chat_global_key_budget_tokens`/`_requests` per чат-день; счётчик — новая таблица `chat_usage(chat_id, day, metric, used)` в PG vs реюз memory_dream_log-паттерна; превышение → отказ LLM с понятным ответом бота (песочница/тишина?)); (Q7) **скрытие параметров от локального админа**: `hidden_from_local` в param_permissions + GET/POST /api/config фильтр для local; правила для keys-секции (никогда не показывать {configured,last4} глобального ключа локальному — PM-база; показывать только собственные chat_keys); (Q8) **UI роль-пикера**: per-input кнопка (модалка view/edit min-role) vs per-section; рендер после C2-группировки (basic/advanced — фича tma-ia-progressive-disclosure); (Q9) **inventory**: полные списки точек — вызовы `hot.get` в direct_chat/воркерах, `_current_api_key`-вызовы, известные секции/параметры, TABS-зависимости, существующие POST /api/config пути, moderator-сиды, RUNTIME WARNING (SQLite-схема/инструменты истории не трогаем). Если правки меняют требования задач — пометки `(изменено @Architect T-843)`.

## B. PG-слой: DDL + сиды

- [x] **T-844 (B1):** DDL в `services/pg_db.py` (идемпотентно, порядок/стиль существующих): (1) `param_permissions (key TEXT PRIMARY KEY, value JSONB NOT NULL DEFAULT '{}')` — по Q2; (2) `chat_keys (chat_id BIGINT NOT NULL, key_name TEXT NOT NULL, key_value TEXT NOT NULL, created_at TIMESTAMPTZ NOT NULL DEFAULT now(), PRIMARY KEY (chat_id, key_name))`; (3) `bot_roles` += `role_type TEXT` (`ADD COLUMN IF NOT EXISTS` — прецедент relations T-818). Сиды: 4 встроенные роли-типа (global_admin = существующий 'admin'-сет/wildcard, local_admin, moderator = существующий дефолт, user — read-only) в `SEED_CATEGORIES`/сид-ролях; seed `param_permissions` — по матрице A1 (минимум: дефолты view/edit для всех известных категорий ключей). Верификация: двойной init() — no-op, схема/B категорий и сидов ожидаемые, существующие таблицы/сиды не тронуты. (services/pg_db.py)

## C. Сервисы: доступы + per-chat overrides

- [x] **T-845 (C1):** `services/chat_params.py` — слой override: `get_chat_param(chat_id, key)`, `set_chat_param(chat_id, key, value, *, changed_by, expected_updated_at=None)` (транзакция: UPDATE chat_profiles.chat_params + история (Q4: карточка записи в chat_lore_history field='chat_params' — прецедент T-772) + NOTIFY `chat_params_updated`; optimistic 409 `ChatParamsConflict(chat_id, current_updated_at)`; каст значения через единый хелпер каста (T-651-прецедент)), `get_all_chat_params(chat_id)`; кэш per-chat (паттерн ChatLoreCache T-774: load-on-demand + TTL-фолбэк 120 с + NOTIFY-инвалидация; fail-open пусто). (services/chat_params.py — НОВЫЙ; services/chat_lore_store.py — при размещении методов там (Q4))
- [x] **T-846 (C2):** `services/access.py` — матрица доступа: `access_for(telegram_id, chat_id) -> {role_global, role_chat, is_local_admin, ...}`: global admin (bot_admins/bot_roles admin/wildcard) → все чаты; строка `chat_admins(chat_id, telegram_id)` → local_admin этого чата (Q3); moderator с section-правами → чаты из chat_admins? (Q3); user/прочие → read-only; `can_view_param(access, key)` / `can_edit_param(access, key)` — min-роль из `param_permissions` vs ROLE_RANK + явные переопределения секций (`hidden_from_local`, keys-категория через существующий can_view_key_value — депсы не сломать); иерархия ROLE_RANK по A1. (services/access.py — НОВЫЙ; web/api/deps.py — правки функций, сигнатуры существующих депсов НЕ менять без Q)
- [x] **T-847 (C3):** per-chat-чтение параметров: `hot_chat.get(chat_id, key, default)`-хелпер (в hot_config или services/chat_params.py — Q5): цепочка `chat_params[chat_id]` → hot.get (bot_settings) → settings-дефолт; применить в точках по Q5 (direct_chat: тон/персона/стиль; воркеры: сна/ностальгии/лора; регистрация фильтров-персон — НЕ ломать import-time паттерны, F-5-прецедент; пер-персона ключи reactions.*); воркеры и direct_chat — fail-open (PG-сбой → глобал/дефолт, бот жив). (services/hot_config.py / services/chat_params.py, services/direct_chat_service.py, services/*worker.py, handlers/* — по списку Q9)
- [x] **T-866 (C4):** direct_chat-промпты per-chat (1.3-точка): полный путь построения промпта (chat_prompts/prompt-миграции НЕ трогать) — чтение `prompts.*` через `hot_chat` в `direct_chat_service`: локальный админ получает значение «as-is» (GET /api/config с X-Chat-Id); кнопка/эндпоинт «Использовать мой» (POST /api/config с ключом `prompts.direct_chat_system_prompt`/соответствующими ключами → chat_params) — override действует ТОЛЬКО в его чате; fail-open: у чата нет override → глобальный; канон-миграции (prompt_migrations) применяются только к глобальным ключам (кастом юзера в chat_params НЕ мигрировать — решает @Architect, Q); регресс: существующие байт-тесты канона R9 не тронуты (глобальный путь). (services/direct_chat_service.py, services/chat_prompts.py, services/chat_params.py, web/api/*)
- [x] **T-867 (C5):** по-параметр-разметка whitelist: `param_catalog` REGISTRY-записи += поле `per_chat: bool` (true для «профильных» ключей по Q5: тон/персона/modules/лимиты сна-ностальгии/память; false для системных: DB-paths, TMA_AUTH, TMA-секреты и др.), валидация POST /api/config: ключ без `per_chat` + X-Chat-Id → 422 (не каналится в chat_params); `GET /api/config/params-meta` (отдаёт per_chat-флаги + progressive_level(часть 2-фичи) — консолидированная мета-инфа для TMA-фич; безопасно, без секретов). (services/param_catalog.py, web/api/*)

## D. BYOK

- [x] **T-848 (D1):** `services/chat_keys.py` — store: `get_chat_key(chat_id, key_name)`, `set_chat_key`, `delete_chat_key`, `mask_info(...) -> {configured: bool, last4}`; ОБЯЗАТЕЛЬНО: сырые значения отдаются только сервисам (llm_client), по API — никогда (только mask); R17; удаление/замена — журнал (в chat_lore_history field='chat_keys'? — Q6). (services/chat_keys.py — НОВЫЙ)
- [x] **T-849 (D2):** `services/llm_client.py::_current_api_key()` → резолв per chat: (1) `chat_keys` [chat_id] → «свой ключ чата»; (2) при отсутствии — глобальный `hot.get("keys.llm_api_key")` с бюджетом: `services/chat_usage.py` — суточный счётчик per chatter (chat_id, day, metric: tokens/requests) в PG `chat_usage (chat_id, day, metric, used, PRIMARY KEY (chat_id, day, metric))`, лимиты `limits.chat_global_key_budget_tokens`/`_requests` (REGISTRY, дефолты по A1); превышение → НЕ использовать глобальный ключ: вернуть «нет ключа»-ошибку/песочницу (текст ответа бота — по Q6); (3) fallback-ключ — только глобальный admin-режим (Q6); консистентность: все вызовы `_current_api_key` (chat/embed/vision и др.) проходят через единый `resolve_api_key(chat_id=...)`. (services/llm_client.py, services/chat_usage.py — НОВЫЙ, services/param_catalog.py, config/settings.py)

## E. API

- [x] **T-850 (E1):** модуль `web/api/access.py` (APIRouter, include после api_router в `web/app.py:95-96`): `GET /api/access/me` (глобальная роль, роль в активном чате, список доступных чатов с ролями), `GET /api/access/chats` (список чатов с is_active/локальный-флаг/роль юзера; join chat_admins), `POST /api/access/chats/{chat_id}/admins` {telegram_id, role: 'local_admin'|'moderator'} (только global admin; 409/422/403 по конвенции; запись в chat_admins + role-маппинг), `DELETE .../admins/{telegram_id}`; `GET /api/access/param_permissions` (список key→min-role/скрыт; только global admin), `PUT /api/access/param_permissions/{key}` {view_min_role, edit_min_role, hidden_from_local} (валидация по ROLE_RANK → 422; только global admin). (web/api/access.py — НОВЫЙ, web/app.py — добавка)
- [x] **T-851 (E2):** `GET /api/config` — per-chat слой: при заголовке `X-Chat-Id` (валидация: юзер имеет доступ к чату (Q3) ПРИ ИНАЧЕ 403/404): значения = `chat_params` → глобальный → дефолты; фильтрация по `can_view_param` (локальный админ не видит hidden_from_local и keys-статус глобального ключа — только `{configured:false}` для не-своих ключей); существующие маски секретов НЕ ломать (прецедент маски v3); для local админа промпты отдаются «as-is» (читать может, значение — как есть). (web/api/routes.py или web/api/config.py)
- [x] **T-852 (E3):** `POST /api/config` per-chat: при X-Chat-Id — запись в chat_params (T-845) c `expected_updated_at` (409-хендлер {code: conflict, current_updated_at} — паттерн chat_lore), 422-каст ошибки; keys-категория → `chat_keys` (не chat_params; 409 у chat_keys не нужен — insert-or-replace); минимум per-параметров: КАЖДЫЙ item валидирован (тип/диапазон/право) ДО записи (атомарность-F-1-прецедент T-648), `hidden_from_local` не может быть записан локальным админом (403/422); серверная логика: если ключ глобального параметра — запись LУЧШЕ в chat_params (локальный нейтральз). (web/api/*)
- [x] **T-853 (E4):** BYOK-эндпоинты: `GET /api/config/keys/own` (маски своих chat_keys чата), `PUT /api/config/keys/own` {key_name, value} (запись в chat_keys, last4-маска в ответе), `DELETE /api/config/keys/own/{key_name}` — права: local admin своего чата / global admin; 422-валидация key_name ∈ whitelist (keys.llm_api_key/llm_fallback_api_key/embed_*); `GET /api/config/keys/status` (per-chat: own/global allowed — статусы для TMA). R17-аудит: ни один рут не отдаёт сырое значение. (web/api/access.py или routes.py)

## F. TMA (Vue 3, web/app.js + web/index.html)

- [x] **T-854 (F1):** контекст-пламбинг: `activeChatId` (данные приложения, persist localStorage `adminbot.active_chat_id`), `api()` → все запросы получают заголовок `X-Chat-Id` (префикс-хедер: троит, если нет), `getActiveChat()`/`setActiveChat(id)`, init-таймаер на загрузку: `GET /api/access/me` (если не один глобальный admin — выбрать первый чат). Регресс-правило: поведение при NULL — в точности старое (глобальный конфиг). (web/app.js)
- [x] **T-855 (F2):** роль-пикер (1.2): для global admin рер ветка: UI-кнопка «мини-роль» (badge/gear) рядом с КАЖДЫМ входом-параметром (рендер по generic-конфиг-циклу ~:338-440) → модалка: view_min_role select (5 уровней) / edit_min_role select / «скрыто от локальных» checkbox → `PUT /api/access/param_permissions/{key}`; бейдж-индикация «hidden» у параметров; для non-global — пикер не рендерится. (web/app.js, web/index.html)
- [x] **T-856 (F3):** BYOK-UI: в вкладках с keys-секцией — для local admin: поля ВИЗУАЛЬНО ПУСТЫЕ (не показывать ни значения, ни {configured,last4} глобального — только нейтральный placeholder «Ваш ключ чата…»), кнопка «Использовать мой» (PUT /api/config/keys/own), строка-статус «используется ключ чата»/«используется глобальный ключ (бюджет N/M)» (GET /api/config/keys/status); для global admin — прежний вид (configured/last4) + список статусов чатов. (web/app.js, web/index.html)
- [x] **T-857 (F4):** «Доступы и Роли»-раздел (текущая вкладка access): (1) карточка «Локальные админы чата» — список chat_admins активного чата (telegram_id + роль-чип), добавить/удалить (POST/DELETE /api/access/chats/{id}/admins); (2) карточка «Мой доступ» (GET /api/access/me: глобальная роль, роль в чате, читаю/редактирую — подсвечено); (3) для local admin: карточка «Промпты» инфа «промпты От базовых; нажать "Использовать мой" → сохранить в этого чата» — скрытые (hidden_from_local) записи не рендерятся. (web/app.js, web/index.html)
- [x] **T-868 (F5):** индикатор «переопределено чатом»: в generic-конфиг-рендере (конфиг-вкладки) для параметров с `per_chat=true` (T-867): бейдж-сравнение (значение чата ≠ глобального) + кнопка «Сбросить на глобальное» (эндпоинт сброса chat_params-ключа: `DELETE /api/config/chat/{key}` или POST-вариант — решение @Architect Q4): сброс только global admin/local admin активного чата, 409 optimistic; для hidden-параметров — не показывать; стилизация (тёмная тема, бейдж «чат»). (web/app.js, web/index.html, web/api/*)

## G. Тесты

- [x] **T-858 (G1):** PG+сиды: param_permissions/chat_keys/role_type: повторный init — no-op (прецедент H1 раунда 7), сиды 4 ролей и дефолты param_permissions; chat_params колонка (ADD COLUMN IF NOT EXISTS на чистой БД и «уже есть»); существующие таблицы/SEED_CATEGORIES не тронуты. (tests/test_pg_db.py-стиль)
- [x] **T-859 (G2):** доступы/чат-параметры: access_for-матрица (global admin локально/все чаты; chat_admins-строка → local; moderator → локальный по Q3; user → read-only; не-член → 403-склейка); can_view/is_edit параметров по min-ролям (view=user, edit=moderator, keys-секрет=global-only); hidden_from_local; chat_params: set/get, каст-ошибка 422, optimistic 409 с current_updated_at, NOTIFY-эмит, кэш-инвалидация, fail-open (PG down → глобал). (tests/test_access.py, tests/test_chat_params.py — НОВЫЕ, тесты-стиль test_chat_lore_store.py)
- [x] **T-860 (G3):** BYOK/бюджеты: chat_keys CRUD + маска (никогда raw по API); llm_client-резолв: локальный ключ → приоритет; нет ключа → глобальный + счётчик; бюджет превышен → отказ «нет ключа»/песочница (мок-счётчик), fallback-ключ только глобальному админу; R17-греп (сырые значения не логируются). (tests/test_chat_keys.py, tests/test_llm_client.py — расширение)
- [x] **T-861 (G4):** API-матрица: /api/config с X-Chat-Id — 401 без initData, 403 чужой чат, значения слоёв (чат > глобал > дефолт), фильтрация для local (hidden/keys-статус), 409 optimistic; /api/access/* — права только global admin (403 moderator/local), валидация 422 роли; BYOK-руты: маска, обход невозможен (GET сырья → 403/404); консистентность POST-атомарности (частичный успех исключён). (tests/test_webapp_*.py-стиль)
- [x] **T-862 (G5):** фронт-аудит (база tests/test_frontend_tab_mapping.py) — app.js содержит: activeChatId/X-Chat-Id в api(), роль-пикер-методы и элементы (маркер-строки), BYOK-пустые поля и статус-строки, access-карточки; index.html-ветки присутствуют; обновить существующие ассерты инвентаря вкладок при изменении структуры; полный pytest → 0 failed; `git diff --check` чист. (tests/, web/app.js, web/index.html)

## H. Деплой, README, отчёт

- [ ] **T-863 (H1):** README-доки: раздел «Multi-chat: роли, параметры чатов, BYOK» (раунд 10, часть 1): 4+1 роли (таблица: кто что может), per-param min-role (как поменять из TMA: роль-пикер), chat_params (что хранится, «Использовать мой» для промптов), BYOK (как задать ключ чата, бюджеты глобального ключа limits.chat_global_key_budget_*), таблицы (param_permissions/chat_keys/chat_usage), API-эндпоинты, инвариант «локальный админ НЕ видит глобальный ключ и hidden-параметры»; .env.example — с новыми REGISTRY-дефолтами (по A1). Полный pytest → 0 failed; grep-секреты; git diff --check. (README.md) *(@DevOps финальный шаг — вне архива)*
- [ ] **T-864 (H2):** @DevOps: деплой: pytest → коммит (код+тесты+plans/backlog.md+plans/features/multi-chat-rbac-byok/) → пуш; сервер pull --ff-only; бэкап (SQLite-БД + pg_dump bot_settings-схемы); рестарт → journald: DDL (param_permissions/chat_keys/role_type/chat_params), сиды ролей, ошибок нет; live-верификация: GET /api/config без X-Chat-Id (прежний вид), с X-Chat-Id (слой чата), local-admin-скрытия (настроить тестового local админа в chat_admins), BYOK: PUT own key → 200, маска, статус-строки; 409-сценарий. Результат — в H3. (прод) *(@DevOps финальный шаг — вне архива)*
- [ ] **T-865 (H3):** отчёт юзеру + закрытие: аппрув @Reviewer; архивация: перенос `plans/features/multi-chat-rbac-byok/` → `plans/archive/multi-chat-rbac-byok/` (@PM Archive Phase); правка plans/backlog.md (статус раунда 10: часть 1 выполнено+архив); ARCHITECTURE.md — раздел (решает @Architect); memory-sync docs-коммит. Отчёт: роли, где UI (роль-пикер, доступы), как включить локального админа, «Использовать мой», бюджеты, что НЕ тронуто (SQLite-память; глобальный ConfigCache). (plans/, README.md) *(@DevOps/@Memory финальный шаг — вне архива)*
- [x] **T-869 (H4):** финальная сверка части-1 (перед деплоем): R17-греп (сырые ключи не логируются/не отдаются — новая БД chat_keys + отчёты oversight), `git diff --check` чист, RUNTIME WARNING-аудит (SQLite-схема/инструменты истории/порядок роутеров bot.py не тронуты; `plans/MEMORY.md` untracked — не включить в коммит), подтверждение полного pytest → 0 failed на итоговом дереве (после G1–G5 и правок по ревью). (весь скоуп части-1)
