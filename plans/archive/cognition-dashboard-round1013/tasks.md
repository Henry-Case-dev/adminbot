# Фича F5 — `cognition-dashboard-round1013` (дашборд «Осмысление» + виджет «Интеллект и Память»)

> **Статус: ✅ COMPLETED** (Step 8 @PM Archive, 13.09.2026; реализовано Step 4 @Builder, 13.09.2026). Спека: `spec.md` + `adr-1013-2-graph-library.md`. Реализация завершена; тесты зелёные (pytest 5374 passed, `JS-UNIT-OK`, `node --check` clean).
> **Раунд:** 10.13. **Нумерация:** T-1448…T-1458 (продолжает F4 T-1447).
> **Тип:** frontend (+ аддитивные backend-API чтения). **Приоритет:** P1.
> **Зависимости:** **API F2** (`cognition-belief-decay-round1013`) и **API F3**
> (`cognition-deep-sleep-round1013`) — ленты убеждений/парадигм, статус сна, граф.
> **ТЗ:** `plans/current_task.md`, разделы **5** («UI: Дашборд Cognition и Графы») и **7**
> («Виджет Интеллект и Память в разделе Сводка»).
> **Эпик:** `Epic: Cognition-Sleep-Memory Refactor round1013`.

## 0. Цель

На странице «Статус» — живой дашборд «Осмысление»: две вертикальные бегущие строки (Убеждения/Сон,
Парадигмы/Глубокий сон) с opacity-эффектом, бейджи активных фаз, интерактивный force-directed граф.
Реальная статистика переносится в «Модули». В «Сводку» добавляется компактный виджет «Интеллект и Память»
(пульс процессов, прогресс-бары лимитов/бюджета, метрики БД, Timeline).

## 1. Требования (дословно из ТЗ §5 + §7)

- [x] Блок «Мониторинг Интеллекта» на странице «Статус».
- [x] Две вертикальные бегущие строки: «Убеждения» (Сон) и «Парадигмы» (Глубокий сон); элементы плавно
      проплывают вертикально (верх/низ `opacity-50`, центр `opacity-100`).
- [x] Индикация фаз: идёт сон → бейдж **`[🌙 Сон активен]`**, новые убеждения сразу в ленте; идёт глубокий
      сон → бейдж **`[🌌 Глубокий сон активен]`**, новые парадигмы в ленту; вне фаз — продолжается скролл пула.
- [x] Интерактивный **force-directed граф** (`vis-network` или `d3.js`) во Vue (люди, события, убеждения,
      связи).
- [x] Реальная статистика выносится в раздел **«Модули»**, граф остаётся на дашборде как визуализация.
- [x] **Виджет «Интеллект и Память»** в «Сводке»:
  - Верх: пульс процессов — `[🌙 Сон]` (Спит/Синтезирует/Лимит исчерпан + время следующего пробуждения),
    `[💾 Лор]` (время последнего инжекта), `[📻 Ностальгия]` (таймер тишины / кулдаун).
  - Середина: прогресс-бары — бюджет контекста (напр. 14 500/24 000 токенов, красный = урезание RAG/стиля),
    дневные лимиты LLM воркеров (Сон 45k/60k, Лор 12k/100k).
  - Низ: метрики БД (факты в графе, убеждения, защищённые факты) + Timeline последних действий
    (`04:15 Синтезировано убеждение…`, `12:30 Лор обновлён…`, `18:45 Ностальгия: вброшен факт…`).

## 2. Целевые модули (`file:line` на HEAD `ce25dc7`)

- `web/app.js` — вкладки/секции («Статус» ~`:3524` `renderUptimeChart`, ключевые данные статуса,
  «Модули», «Сводка» через `loadBudgetInfo`/`loadOversight`), компоненты Vue.
- `web/index.html` — страницы «Статус» (`:2632+`, блоки сервер/ключи/аптайм/logs), «Модули», «Сводка».
- `web/api/memory_agi.py` — `GET /memory/dream/beliefs` `:212`, `/memory/dream` `:233`, `/memory/dream/log`
  `:299`, `/memory/nostalgia/log` `:320` (расширение лент/статуса/графа).
- `web/api/routes.py` — `GET /status` `:1093`, `loadBudgetInfo`→`/api/workers/budget`.
- `services/status_service.py` — `build_snapshot()` `:444`, `_server_metrics()` `:354`, uptime-бакеты.
- `services/worker_budget.py` — дневные лимиты/расход воркеров.
- `services/uptime_heartbeat.py` — пульс процессов (см. F6).
- Тесты: `tests/test_webapp_round1013_ui.py` (новый), `tests/test_webapp_*`, `tests/js/routing_test.js`.

## 3. Инварианты (constraints — не нарушать)

- **Ноль новых PG-DDL**; SQLite **v8**; порядок роутеров `bot.py` не трогать.
- **R17:** API отдаёт только маски `{configured, last4}`; никаких ключей/сырых данных в лентах, графе, Timeline.
- **R16:** id — ключ; в графе люди — id/канон-алиас, не только отображаемое имя.
- **Каталог-инварианты 405/90/377/88, TAB_RULES 19** — новые параметры/пункты меню только санкционированным Δ.
- **`/api/status` контракт:** расширение аддитивно; существующие поля (bot/server/llm/uptime/permsoc) не ломать.
  Изменение `/api/status/logs` — в F6 (новый тег) — учесть маркерные тесты.
- Библиотека графа (`vis-network`/`d3.js`) — подключение в TMA-совместимом виде (offline/размер/производительность
  на Android WebView); не ломать CSP и существующий Chart.js.
- **R10.11-5/R10.10-3:** destroy инстансов до return; нет stale-слушателей.
- **Коммиты:** русские conventional commits.

## 4. Зависимости

- **Вверх:** F2 (API убеждений/архива), F3 (API парадигм/статуса глубокого сна), частично F1 (метки/даты).
- **Параллельно:** F6 (EKG/logs) — обе фронт-фичи, пересечение в блоке «Статус» (разные зоны).
- **Порядок:** F5 после F2+F3 (нужны данные). F5 и F6 — не одновременно в одном файле, если возможно.

## 5. Definition of Done

- [x] На «Статус» есть блок «Осмысление» с двумя вертикальными бегущими строками и opacity-эффектом.
- [x] Бейджи `[🌙 Сон активен]` / `[🌌 Глубокий сон активен]` отражают реальный статус; ленты пополняются.
- [x] Интерактивный force-directed граф рендерится и работает (drag/zoom), не роняя TMA на Android.
- [x] Реальная статистика доступна в «Модулях»; граф — только визуализация.
- [x] Виджет «Интеллект и Память» в «Сводке»: пульс процессов, прогресс-бары, метрики БД, Timeline.
- [x] Все данные — из реальных API, без хардкода; R17/R16 соблюдены.
- [x] `node --check web/app.js` clean; `JS-UNIT-OK`; полный `pytest` — 0 failed; `git diff --check` чист.

## 6. Чек-лист задач

- [x] **T-1448 (@Architect, гейт):** spec/ADR: состав API дашборда (ленты/статус/граф/Timeline/бюджет),
  формат ответов и R17-модель, выбор библиотеки графа (`vis-network` vs `d3.js`) и способ подключения,
  расположение блоков (Статус/Модули/Сводка), частота обновления, каталог/меню-Δ.
- [x] **T-1449 (@Builder):** backend-API (аддитивно к `memory_agi.py`/`routes.py`): лента beliefs,
  лента paradigms, статус фаз сна (running/next wake), граф nodes/edges, Timeline активности.
- [x] **T-1450 (@Builder):** блок «Cognition» на «Статус»: две вертикальные бегущие строки с плавным
  opacity-эффектом (верх/низ `opacity-50`, центр `opacity-100`).
- [x] **T-1451 (@Builder):** бейджи активных фаз `[🌙 Сон активен]`/`[🌌 Глубокий сон активен]` в реальном
  времени; новые элементы сразу попадают в свою ленту; вне фаз — скролл текущего пула.
- [x] **T-1452 (@Builder):** интерактивный force-directed граф (люди/события/убеждения/связи) во Vue;
  drag/zoom; корректный destroy; производительность на Android WebView.
- [x] **T-1453 (@Builder):** перенос реальной статистики в «Модули»; на дашборде остаётся граф.
- [x] **T-1454 (@Builder):** виджет «Интеллект и Память» в «Сводке» — верхний ряд: `[🌙 Сон]`
  (Спит/Синтезирует/Лимит + next wake), `[💾 Лор]` (последний инжект), `[📻 Ностальгия]` (тишина/кулдаун).
- [x] **T-1455 (@Builder):** средний ряд — прогресс-бары бюджета контекста и дневных лимитов LLM воркеров
  (красный = RAG/стиль урезаются); данные из `worker_budget`/статуса.
- [x] **T-1456 (@Builder):** нижний ряд — метрики БД (факты/убеждения/защищённые) + компактный Timeline
  последних действий.
- [x] **T-1457 (@Builder):** тесты: JS-юниты (модели лент/бюджета/Timeline, opacity-класс), API-тесты
  (`memory_agi`/`status`), статик-маркеры UI (бейджи, граф, виджет); R17-проверка ответов.
- [x] **T-1458 (@Builder):** `node --check web/app.js`, `node tests/js/routing_test.js` → `JS-UNIT-OK`,
  полный `pytest` (0 failed, дельта), `git diff --check` clean.

## 7. Открытые вопросы (@Architect → владелец)

- **F5-Q1:** `vis-network` (CDN/локально?) или `d3.js`? Влияние на TMA/offline/CSP/размер бандла.
- **F5-Q2:** граф — по какому источнику (SQLite `graph_edges`/`graph_facts`), сколько узлов/рёбер по умолчанию?
- **F5-Q3:** период обновления лент/статуса (polling interval) и поведение при сворачивании TMA.
- **F5-Q4:** бюджет контекста — откуда брать «заполнено/лимит» (последний ответ/оценка токенов)?
- **F5-Q5:** Timeline — единый источник (`memory_dream_log`+`nostalgia_log`+lore) или объединённый API?
- **F5-Q6:** «Модули» — переносим что именно (граф-статистика? лог?) и не дублировать ли «Сводку»?

## 8. Feature flag / progressive delivery

- **Feature flag:** не требуется (UI + аддитивные read-only API; прецедент 10.9–10.12). При рисках
  @Architect может ввести `flags.cognition_dashboard_enabled` (default ON).
- **Rollout stages:** не применимо (внутренний инструмент). При введении флага — `internal → 100%`.
- **Rollback:** атомарный `git revert`.
