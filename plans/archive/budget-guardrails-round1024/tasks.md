# Задачи: budget-guardrails-round1024

> **Раунд 10.24 (UPD5-critical)** · Приоритет **P1 (опционально)** · Шаг 1 @PM · Тип: защитные пин-тесты / freeze-инварианты / R16-R18
> **ТЗ:** `plans/current_task.md`, **строка 404** (сопровождение фикса бюджетов). Секреты не цитировать (R17/R18).
> **Baseline:** HEAD `00eab85`; прод `4314ea4`.
> **Конфликт/AMEND:** без нового ADR; страховка к **F20/F21/F22** (AMEND ADR-1019-3/1019-8). Связано с `tma-menu-freeze` (запрет снят владельцем только для карточки изображений F5 10.24, для «Бюджетов» — не снимать).
> **Границы:** закрывает риски R2/F21, R3/F22 и сквозные R16/R17/R18.

## Цель
Не дать регрессу вернуться: закрепить пин-тестами каталог/TAB_RULES после добавления master-тумблера (F21), зафиксировать freeze-инвариант меню «Модули» и сквозные правила R16 (id — ключ, не имя), R17 (секреты) и R18 (никаких приватных ключей/токенов в артефактах).

## Что уже есть (координаты)
- **Пин-тесты каталога:** `tests/` — `test_param_catalog` (REGISTRY/GROUPS), `test_frontend_tab_mapping` (`_TAB_BY_GROUP`, `TAB_RULES`).
- **Каталог:** `services/param_catalog.py` — `CATEGORY_FLAGS` (`:34`), `GroupSpec` (`:333` — пример `flags_module_images`), регистрация флагов (`:1774-1777`, `:2062-2171`).
- **UI-меню:** `web/app.js:353-396` (`MODULES`, «ровно 11»), `:130-138` (`mod_budgets`, `modules`).
- **Дата целевого чата — только в сиде/тестах:** `config/chat_settings_seed.json:2` (правило «в `services/*` chat_id быть не должно»).

## Задачи
- [x] **T-2376** [@Builder] Пин-тесты: после F21 — точные значения `REGISTRY`/`GROUPS`/`_TAB_BY_GROUP`; проверка, что новый ключ `flags.budgets_enabled` и его `GroupSpec` отображаются в нужную вкладку; `test_frontend_tab_mapping` обновлён в том же коммите (без рассинхрона).
- [x] **T-2377** [@Builder] Freeze-инвариант меню «Модули»: `tma-menu-freeze` для «Бюджетов» **не снимать** (снято только для карточки изображений, UPD3 №1); меню остаётся консистентным (число пунктов/карточек), тест на состав.
- [x] **T-2378** [@Reviewer] Сквозной скан **R16/R17/R18** по артефактам раунда (F20–F22): нет plaintext-секретов/токенов/приватных ключей; `chat_id` — только в сиде/тестах, не в бизнес-логике; секреты в отчётах — маскированы. → выполнен в ревью: `plans/reports/round1024_f23_reviewer.md` (§Инварианты: R16/R17/R18 чисто).
- [ ] **T-2379** [@DevOps] Финальный guard-прогон (полный pytest + `node --check web/app.js` + `git diff --check`) и краткий отчёт; подтвердить kill-switch/OFF-поведение. Без секретов. → локальный артефакт прогона: `plans/reports/round1024_f23_guard_run.md` (7911 passed/0 failed, node OK, diff чист); прод-часть (PG read-only kill-switch) — за @DevOps.

## Критерии приёмки
- Пин-тесты каталога/TAB_RULES зелёные и защищают от случайного Δ каталога сверх санкционированного (F21).
- Freeze меню «Модули» не снят; состав меню консистентен.
- R16/R17/R18 — чисто; секреты не цитируются.
- Полный pytest — 0 failed.

## Риски
- **R1 (Medium):** пин-тесты окажутся «пустыми» (не ловят реальный регресс) → проверка на мутацию (удалить ключ → тест падает).
- **R2 (Low):** избыточный freeze блокирует легитимные будущие правки меню → freeze состава каталога/витрины зафиксирован как **осознанный frozen-contract** (`spec.md` §4): пин-листы правятся одной строкой в коммите фичи, без затрагивания логики.
- **R3 (Low):** секретоподобные строки в фикстурах → R17-редакция.

## Зависимости / ступени
- Зависит от: **F20, F21, F22** (пин-тесты после их реализации).
- Ступень общих файлов: `tests/**` (каталог/фронт), `services/param_catalog.py`, `web/app.js` — после F21.
- Feature flag: не требуется. **Δ DDL = 0, Δ каталога = 0** (только тесты/инварианты).
