# F5 — `module-workspace-tabs-round1025` (Эпик 1, Волна 3)

> **ТЗ:** `plans/current_task.md` §46, §47, §48, §49, §84, §85 (+ критерии §69, §117). **Тип:** UI + IA (web).
> **Приоритет:** P0. **Зависит от:** F4 (`ModuleConfigurationStore` §37–§42, шов `openModuleWorkspace`), F1 (IA/hash-deeplink), F2 (токены/glass), F3 (`scopeEpoch`/`_scopeGuard`). **От F5 зависит:** F9.
> **Статус:** ✅ **COMPLETED + MERGED + ARCHIVED (Шаг 8 @PM, 22.09.2026).** Merge — `plans/ARCHITECTURE.md` **§61**; архив — **`plans/archive/module-workspace-tabs-round1025/`**. Deploy — ⏳ **Шаг 9 @DevOps (T-2741)**: код/тесты/`plans/**` **НЕ закоммичены** (HEAD `12a55bb`); live-гейт владельца **T-2742** — ⏳; метрики (Step 10 @Memory, T-2743) — ⏳.
> **Задачи:** **T-2695…T-2744 (50)**. Нумерация продолжает hotfix7 (максимум по `plans/**` — **T-2694**); дублей ID нет (проверено Шагом 1 @PM по `plans/features/` + `plans/archive/` + `plans/*.md`).
> **Шаг 0 (@Memory) — как данность (не перепроверяется):** карта кода (модалка `openModuleWindow` `web/app.js:3790-3809`, рендер `web/index.html:1286-1508`, `activeModule`/`activeModuleTab`/`activeModuleGroups` `app.js:1497-1517`; шов F4 `openModuleWorkspace(m)` `app.js:4664-4666`; раздел ИИ `HUBS_V2['#/ai']` `app.js:400-421`, `TABS` 42-160, `TAB_ROUTES` 809-822; промпт-редакторы `promptSections` 5524-5555 / `promptStageItems` 5506-5509 / `promptsShowAccordion` 5614-5619, разметка `index.html:626-760`, режимы 1629/5563-5610; `PROVIDER_BLOCKS` `app.js:536+`; `POST /api/llm/test` `web/api/routes.py:1764`, `POST /api/images/test` `:1812`; группы `models_*`/`prompts_*` `services/param_catalog.py:147-186`), конфликт-чек и риски. **Baseline: HEAD `12a55bb`, `APP_VERSION` 2.58.9, pytest 8229/0, каталог 459/98/96/21/418, SQLite v12 (Δ DDL=0).** Источник — `plans/workflow_state.md`, `plans/MEMORY.md`.
> **⚠️ Номера строк карты Step 0 могли сдвинуться после F4 — ориентироваться на символы, а не на строки.**
> **REVISE:** 10.24 F6 `prompts-refactor-accordion-modes` конфликтует с §48/§69 (запрет аккордеонов как навигации) → аккордеоны только для редких технических параметров; «один промпт — один источник данных».

## 0. Жёсткие инварианты и ограничения (для всех задач)

- **Δ DDL = 0** (SQLite `user_version=12`; без миграций/таблиц). **Δ каталога = 0** — `REGISTRY 459 / GROUPS 98 / _TAB_BY_GROUP 96 / TAB_RULES 21 / Settings 418`; `services/param_catalog.py` — только чтение.
- **CSP/zero-build** (ADR-1016-2/ADR-1024-13): новых библиотек/CDN/data-URI/inline-скриптов нет. **Запрет WebGL**.
- **Не дублировать (переиспользовать целиком):** F0 write-path `persistItems`/`saveConfigItem`; F3 `scopeEpoch`/`_scopeGuard`/`configItemNotice`; F4 `ModuleConfigurationStore` §37–§42 (`get/set/refresh/subscribeModuleState`, `moduleOptimistic`/`modulePending`) + шов `openModuleWorkspace`; токены/glass F2 (ADR-1025-9); shell/hotfix6-7 (`--header-h`, `--shell-*`, ADR-1025-12/-13); F9 SaveBar/секреты **не делать за F9**.
- **Сохранить регресс-путь `openModuleWindow(m)`** — F5 меняет только **реализацию** шва `openModuleWorkspace` (§60.2/ADR-1025-14 §D6), точка входа не меняется.
- **§85 backend-пайплайн — Эпик 2** (10.26): F5 делает **только UI-каркас** (табы/маршруты/промпты), серверные этапы/схемы/промпты L1/L2 не создаёт.
- **§49 «не менять сохранённую модель при открытии»**, **§46 «API-ключи не копировать в модуль»**, **§48/§69 «аккордеоны — не основная навигация», «не прятать единственное textarea в аккордеон»** — обязательные приёмочные запреты.
- **§46 генеральный тумблер — из того же store F4**, не отдельный локальный boolean.
- **R16/R17/R18:** API аддитивен (по умолчанию — reuse существующих); логи/отчёты без секретов; `plans/current_task.md` — untracked, **не трогать/не коммитить**.

## Трассируемость: требование ТЗ → задача

| Требование ТЗ | Задачи |
|---|---|
| §46 отдельная страница модуля (Модули → Проверка фактов), вкладки + только применимые | T-2697, T-2699, T-2700 |
| §46 генеральный переключатель вверху из того же канонического store (карточка/панель), без локального boolean | T-2701 |
| §46 все настройки модуля доступны из workspace; API-ключи не копировать в модуль | T-2702, T-2730 |
| §47 раздел ИИ сохранить: Библиотека промптов / Модели и подключения / Личность и стиль / Имена и алиасы / Умный кэш | T-2704, T-2705 |
| §47 ИИ не свалка: память → «Память», функция → «Модули» | T-2706 |
| §48 гибридная библиотека: один промпт — один источник, два маршрута → один объект, без копий | T-2707, T-2709, T-2725, T-2727 |
| §48 desktop дерево/редактор/описание+переменные+параметры; mobile список → полноэкранный редактор | T-2707, T-2708 |
| §48/§69 не прятать единственное textarea в аккордеон; аккордеоны не основная навигация | T-2710, T-2729 |
| §48 сохранять исходный текст промптов без изменений (+ канон/`PREV_*`/идемпотентная миграция ADR-1013-3) | T-2711 |
| §49 группы моделей (текст/речь/видео/эмбеддинги/фон/изображения) | T-2713 |
| §49 карточка (название/назначение/основная/резервная/статус/Проверить/Настроить) | T-2714, T-2715 |
| §49 сохранить провайдеры/base_url/model/ключи/fallback/тайм-ауты/режимы API | T-2716, T-2718 |
| §49 не менять сохранённую модель при открытии формы | T-2717, T-2728 |
| §84 «Модули → Ответы в чате → Модели»; desktop 2 карточки L1/L2, mobile вертикально, без гориз. скролла | T-2719, T-2720, T-2721 |
| §84/§85 границы UI ↔ Эпик 2 (только UI-каркас) | T-2722, T-2724 |
| §85 «Модули → Сводки чатов» + табы Обзор/Подготовка/Кластеризатор/Писатель/Модели/Лимиты/Тестирование | T-2723, T-2726 |
| §85 промпты также через ИИ → Библиотека промптов → Сводки чатов (один объект) | T-2725 |
| §117(1–4) сохранность конфигурации/реестра/виджетов; §79/§116 критерии Эпика 1 | T-2702, T-2731, T-2732 |
| §71 проверка адаптивности/desktop+mobile | T-2703, T-2726 |

## НАПРАВЛЕНИЕ ПРЕДЛОЖЕНО (не согласовано владельцем)

Ниже — декомпозиция PM. Пункты, помеченные **⟨предложено⟩**, выведены из §46–§49/§84/§85 (прямой формулировки в ТЗ нет) и подлежат подтверждению @Architect на Step 2.

---

## Блок 0 — Точка отката и дизайн-контур

- [x] **T-2695** [@DevOps] — **Точка отката и baseline перед кодом.** Тег `pre-round1025-f5`; бэкап `var/backups/f5-round1025-<ts>/` + `.env.bak.round1025-f5` (значения не печатать; удаление только после утверждения владельца — R18); фиксация baseline (HEAD `12a55bb`, `APP_VERSION` **2.58.9**, pytest **8229/0**, JS-раннеры F4, каталог 459/98/96/21/418, SQLite v12). **Готово, когда:** тег и бэкап созданы; baseline зафиксирован; рабочее дерево чистое кроме ожидаемых `plans/**`-правок. **Файлы:** git tag/бэкап (вне репо), запись в `plans/workflow_state.md` (через @Orchestrator).
- [x] **T-2696** [@Architect] — **Step 2: `spec.md` + ADR-1025-15** (`module-workspace-tabs`). Зафиксировать контракт workspace/маршрута, карту «применимых вкладок», модель «один промпт — один источник» (два маршрута → один config-item `prompts_*`), границы §84/§85 ↔ Эпик 2, группировку §49, регресс-путь `openModuleWindow`. Обязательная **AMEND/SUPERSEDE/REUSE-карта**: ADR-1025-14/F4 (шов/store — reuse, заменить реализацию шва), ADR-1025-10/F3 (scope — reuse), ADR-1025-1/F1 (IA/hash-deeplink — не ломать), ADR-1025-9/F2 + ADR-1025-12/-13 (токены/shell — не ломать), ADR-1013-3 (канон промптов/идемпотентная миграция), ADR-1016-2/ADR-1024-13 (CSP/zero-build). **Решить открытые вопросы (a)–(д)** (ниже) либо эскалировать владельцу через @Orchestrator. **Готово, когда:** `spec.md` + `adr-1025-15-*.md` созданы; (a)–(д) зафиксированы; `tasks.md` сверен со spec (расхождения → возврат @PM). **Файлы:** `plans/features/module-workspace-tabs-round1025/spec.md`, `adr-1025-15-*.md`.

## Блок A — Рабочее пространство модуля (§46) — `T-2697…T-2703`

- [x] **T-2697** [@Builder] — **Маршрут рабочего пространства модуля** (по решению @Architect (а)). Реализовать переход «Модули → `<модуль>`» на отдельную страницу (hash-маршрут, совместимый с hash-deeplink F1 и `TAB_ROUTES`). **Готово, когда:** маршрут открывает workspace выбранного модуля; deep-link/навигация «назад» работают; IA F1 не ломается; неизвестный/неприменимый модуль → понятный откат (не белый экран). **Файлы:** `web/app.js` (роутер `TAB_ROUTES`/`HUBS_V2`), `web/index.html`.
- [x] **T-2698** [@Builder] — **🔑 Замена реализации шва `openModuleWorkspace(m)` с сохранением регресс-пути.** `openModuleWorkspace` (`web/app.js:4664-4666`, сейчас `return this.openModuleWindow(m)`) перенаправляется на реальный workspace + маршрут §46. **Точка входа не меняется** (кнопка «Настроить» из F4 продолжает её вызывать), **`openModuleWindow(m)` сохранён как рабочий регресс-путь** (§60.2/§79/§116, ADR-1025-14 §D6). **Готово, когда:** «Настроить» открывает workspace; `openModuleWindow` достижим и не удалён; тест фиксирует сохранение обеих точек входа. **Файлы:** `web/app.js` (`openModuleWorkspace`, `openModuleWindow`), `web/index.html`.
- [x] **T-2699** [@Builder] — **Карта «применимых вкладок»** (по решению @Architect (д)). Метаданные «какие вкладки показывать каждому модулю» — **в JS `MODULES`** (`Δ каталога = 0`), если @Architect не обоснует иное с явным Δ-обоснованием. **Показывать только применимые** вкладки (§46). **Готово, когда:** у каждого модуля корректен набор вкладок; неприменимые не рендерятся; каталог не изменён (459/98/96/21/418); unit/JS-тест карты. **Файлы:** `web/app.js` (`MODULES`), spec-обоснование.
- [x] **T-2700** [@Builder] — **Каркас вкладок workspace:** Обзор / Основные настройки / Синтезатор / Вербализатор / Модели / Лимиты / Тестирование (§46). Рендер существующих настроек модуля внутри соответствующих вкладок; не создавать пустые «декоративные» вкладки. **Готово, когда:** на примере «Проверка фактов» доступны применимые вкладки; содержимое = реальные параметры (не заглушки). **Файлы:** `web/index.html` (шаблон workspace), `web/app.js`, `web/static/app.css`.
- [x] **T-2701** [@Builder] — **Генеральный переключатель вверху из канонического store (§46).** Использовать **тот же** `ModuleConfigurationStore` F4 §37–§42, что карточка каталога и панель избранного. **Не создавать отдельный локальный boolean.** **Готово, когда:** тумблер workspace синхронен с карточкой/панелью; одна серверная мутация на действие; тест синхронности. **Файлы:** `web/app.js` (computed/биндинг на store), `web/index.html`.
- [x] **T-2702** [@Builder] — **«Все настройки модуля доступны из workspace» + §117(1–4).** Реестр покрытия: каждый параметр/виджет модуля, доступный ранее (модалка/старые экраны), достижим из workspace — ничего не потеряно. **API-ключи НЕ копировать в конфигурацию модуля** (§46). **Готово, когда:** чек-лист покрытия заполнен; нет потерянных параметров/виджетов; ключи остаются в разделе секретов (за F9), не в модуле. **Файлы:** `web/app.js`, `web/index.html`, таблица покрытия в spec/`evidence.md`.
- [x] **T-2703** [@Builder] — **Адаптивность workspace (§70/§71).** Desktop/tablet/mobile (320–1440) без горизонтального скролла страницы; вкладки — табы/скролл-в-контейнере, не ломающие статичный layout; container queries F2; тач-цели ≥ 44×44. **Готово, когда:** матрица 320/390/768/1024/1280/1440 без overflow; `failures: 0`. **Файлы:** `web/static/app.css`, `web/index.html`.
  - ✅ Chromium/Playwright в среде доступны (`.venv`, playwright 1.62.0, chromium): `tools/ui_round1025_matrix.py` расширен workspace-маршрутами (`F5_ROUTES`) + пробой `F5_PROBE_JS`; прогон 10 вьюпортов × 17 маршрутов → **`failures: 0`**; на 320/390/768/1024/1280/1440: `overflow=false`, `tabsWrap=0`, тач-цели табов/«Проверить»/«Настроить» = **44px**. Артефакт: `tools/_ui_round1025_raw.json` (evidence.md §8).

## Блок B — Раздел ИИ (§47) — `T-2704…T-2706`

- [x] **T-2704** [@Builder] — **Аудит/карта текущих страниц ИИ → целевые (§47).** Зафиксировать, что сейчас в `HUBS_V2['#/ai']` (`app.js:400-421`) и `TABS`, что относится к Библиотеке промптов / Моделям и подключениям / Личности и стилю / Именам и алиасам / Умному кэшу, что является «свалкой»/дублем. **Готово, когда:** карта «старый экран → новая страница ИИ» составлена; расхождения с §47 перечислены. **Файлы:** `web/app.js` (`HUBS_V2`, `TABS`), отчёт `plans/reports/round1025_f5_ai_map.md` ⟨предложено⟩.
- [x] **T-2705** [@Builder] — **Внутренние страницы ИИ сохранить (§47):** Библиотека промптов, Модели и подключения, Личность и стиль, Имена и алиасы, Умный кэш — доступны из раздела ИИ. **Готово, когда:** все пять страниц открываются; существующие настройки/виджеты не потеряны; маршруты F1 согласованы. **Файлы:** `web/app.js`, `web/index.html`.
- [x] **T-2706** [@Builder] — **ИИ не свалка (§47).** Настройки памяти → в раздел «Память»; настройки конкретной функции → в «Модули». В ИИ остаётся только §47-скоуп. **Готово, когда:** из ИИ не ведётся управление памятью/функциями; страницы модулей/памяти — единственные владельцы этих настроек; нет дублей одного параметра в двух разделах. **Файлы:** `web/app.js`, `web/index.html`.

## Блок C — Гибридная библиотека промптов (§48) — `T-2707…T-2712`

- [x] **T-2707** [@Builder] — **Master-detail desktop (§48):** слева дерево промптов, центр — редактор, справа — описание, переменные и параметры. **Готово, когда:** на desktop три зоны видны; выбор в дереве меняет редактор и правую панель; без горизонтального скролла. **Файлы:** `web/index.html:626-760` (переработка), `web/app.js` (`promptSections`/`promptStageItems`), `web/static/app.css`.
- [x] **T-2708** [@Builder] — **Mobile (§48):** список промптов → **полноэкранный редактор** (отдельный экран), без сжатия в нечитаемое состояние. **Готово, когда:** на 320/390 список → редактор; возврат к списку; нет горизонтального скролла. **Файлы:** `web/index.html`, `web/app.js`, `web/static/app.css`.
- [x] **T-2709** [@Builder] — **🔑 «Один промпт — один источник» (§48).** Два маршрута — «Модули → Фактчек → Синтезатор» и «ИИ → Библиотека промптов → Фактчек → Синтезатор» — открывают **один объект** (один config-item PG `prompts_*`), **без независимых копий**. Редактирование в любом маршруте сохраняется в один источник; второй маршрут видит то же значение. **Готово, когда:** тест «правка через маршрут A видна через маршрут B, серверных копий нет»; один write-path (F0). **Файлы:** `web/app.js` (резолвинг объекта/маршрутов), `web/index.html`.
- [x] **T-2710** [@Builder] — **Снятие аккордеона как основной навигации (§48/§69; REVISE 10.24 F6).** Навигация по промптам — дерево/список/табы (Блок C), **не аккордеоны**. **Аккордеоны допустимы только для редких технических параметров.** **Не прятать единственное textarea в аккордеон** — основной редактор виден сразу. Пересмотреть `promptsShowAccordion` (`app.js:5614-5619`) и uiFlag `PROMPTS_UI_V2_ENABLED`. **Готово, когда:** основной редактор промпта виден без раскрытия; аккордеоны не используются как основная навигация; регресс маркер-тестов проверен. **Файлы:** `web/app.js`, `web/index.html:626-760`.
- [x] **T-2711** [@Builder] — **🔑 Канон промптов без потерь (§48).** **Сохранять исходный текст промптов без изменений** при переносе. Новый канон + `PREV_*`-слепок + **идемпотентная миграция (ADR-1013-3)**. **Готово, когда:** исходные тексты сохранены (byte/содержательно — по spec); миграция идемпотентна (повторный запуск без изменений); тест неизменности канона. **Файлы:** `web/app.js` (тексты промптов), `services/param_catalog.py` (только чтение — Δ=0), тесты.
- [x] **T-2712** [@Builder] — **Промпт-редакторы: reuse существующей механики.** Переиспользовать `promptSections` (`app.js:5524-5555`), `promptStageItems` (`5506-5509`), режимы-табы (`5563-5610`, `index.html:1629`), объект промпта = config-item. **Готово, когда:** редакторы работают в новом master-detail без регресса режимов; регресс-тесты промптов зелёные. **Файлы:** `web/app.js`, `web/index.html`.

## Блок D — Модели и подключения (§49) — `T-2713…T-2718`

- [x] **T-2713** [@Builder] — **Группы моделей (§49):** генерация текста / распознавание речи / видео / эмбеддинги / фоновые задачи / генерация изображений. Источник групп — существующая структура `PROVIDER_BLOCKS` (`app.js:536+`) и группы `models_*`/`prompts_*` (`services/param_catalog.py:147-186`); **не выдумывать новые группы**. **Готово, когда:** все шесть групп отображаются; существующие провайдеры не потеряны; Δ каталога = 0. **Файлы:** `web/app.js` (`PROVIDER_BLOCKS`/группировка), `web/index.html`.
- [x] **T-2714** [@Builder] — **Карточка подключения (§49):** название / назначение / основная модель / резервная модель / статус / **«Проверить»** / **«Настроить»**. **Готово, когда:** карточка содержит все поля; «Настроить» открывает форму настроек; «Проверить» доступна. **Файлы:** `web/index.html`, `web/app.js`, `web/static/app.css`.
  - ✅ `buildConnectionCard`/`workspaceModelCards` (значения из `PROVIDER_BLOCKS`/`models_*`; секреты `keys.*` не читаются), карточка в шаблоне (`data-conn-field` title/purpose/primary-model/fallback-model/status, `data-conn-test`, `data-conn-configure`, `data-connection-settings`); «Настроить» раскрывает существующую форму (reuse `saveBlock`, 0 записей при открытии — §49); «Проверить» — reuse `testBlock` (`/api/llm/test`,`/api/images/test`). Red→green — evidence.md §8.
- [x] **T-2715** [@Builder] — **«Проверить» — reuse существующих эндпоинтов.** Использовать `POST /api/llm/test` (`web/api/routes.py:1764`) и `POST /api/images/test` (`:1812`); **новых API не создавать** (R16). Показать результат проверки понятным статусом (без утечки секретов). **Готово, когда:** «Проверить» вызывает существующий эндпоинт; статус обновляется; секреты не логируются/не возвращаются. **Файлы:** `web/app.js`, при необходимости — reuse `web/api/routes.py` (без изменений).
- [x] **T-2716** [@Builder] — **Сохранить всё существующее (§49):** провайдеры, `base_url`, `model`, API-ключи, fallback-модели, тайм-ауты, специальные режимы API. Ни один параметр не теряется при переработке. **Готово, когда:** чек-лист «параметр → экран» полон; сохранение/загрузка работают через F0 write-path; ключи — в разделе секретов (F9), не в тексте формы как значение. **Файлы:** `web/app.js`, `web/index.html`, spec-таблица.
- [x] **T-2717** [@Builder] — **🔑 «Не менять сохранённую модель при открытии формы» (§49).** При открытии формы/подключения **не подставлять** default/первую модель вместо сохранённой; не сбрасывать выбор. **Готово, когда:** тест «открыть форму → сохранённая модель неизменна»; повторное открытие не мутирует значение. **Файлы:** `web/app.js` (инициализация формы).
- [x] **T-2718** [@Builder] — **Консистентность групп с каталогом (Δ=0).** Ключи групп `models_*`/`prompts_*` используются как есть; `services/param_catalog.py` не изменяется. **Готово, когда:** тест регистра 459/98/96/21/418 зелёный; нет новых ParamSpec/Group. **Файлы:** `web/app.js`, `services/param_catalog.py` (только чтение).

## Блок E — UI прямого чата: L1/L2 (§84) — `T-2719…T-2722`

- [x] **T-2719** [@Builder] — **Маршрут «Модули → Ответы в чате → Модели» (§84).** Desktop: **две карточки рядом** — L1 (Синтезатор) и L2 (Вербализатор). **Готово, когда:** маршрут открывает страницу моделей прямого чата с двумя карточками. **Файлы:** `web/index.html`, `web/app.js`.
- [x] **T-2720** [@Builder] — **Поля карточек L1/L2:** провайдер / модель / **источник значения** / статус подключения / «Проверить». **Готово, когда:** обе карточки показывают все поля; источник значения использует существующие контракты (`configSourceLabel`/F3/F4), не выдуман. **Файлы:** `web/index.html`, `web/app.js`.
- [x] **T-2721** [@Builder] — **Mobile L1/L2 (§84):** карточки **вертикально**, **без горизонтального скролла**. **Готово, когда:** на 320/390 карточки в столбик; overflow = 0; тач-цели «Проверить» ≥ 44×44. **Файлы:** `web/static/app.css`, `web/index.html`.
- [x] **T-2722** [@Builder] — **Границы §84 (решение @Architect (г)).** Сделать **только UI**: не менять backend-двухвызовный пайплайн L1/L2, не создавать серверные этапы/роутинг. **Готово, когда:** диффа по `services/**`/`web/api/**`/`handlers/**` нет; UI показывает существующие значения. **Файлы:** `web/**` только.

## Блок F — UI Саммари: табы (§85) — `T-2723…T-2726`

- [x] **T-2723** [@Builder] — **Маршрут «Модули → Сводки чатов» + табы (§85):** Обзор / Подготовка сообщений / Кластеризатор / Писатель / Модели / Лимиты / Тестирование. **Готово, когда:** табы отображаются и переключаются; существующие настройки Саммари доступны из соответствующих вкладок. **Файлы:** `web/index.html`, `web/app.js`.
- [x] **T-2724** [@Builder] — **🔑 Только UI-каркас; backend §85 — Эпик 2 (решение @Architect (в)).** НЕ создавать серверные этапы/схемы/промпты L1/L2/пайплайн. UI показывает реальные существующие данные, не выдуманные «этапы». **Готово, когда:** нет изменений backend; вкладки не обещают несуществующих функций. **Файлы:** `web/**` только.
- [x] **T-2725** [@Builder] — **Промпты Саммари — тот же объект (§85).** «ИИ → Библиотека промптов → Сводки чатов» открывает **тот же** промпт-объект, что из workspace Саммари (связка с T-2709); **без независимых копий**. **Готово, когда:** тест единого объекта для промптов Саммари. **Файлы:** `web/app.js`, `web/index.html`.
- [x] **T-2726** [@Builder] — **Адаптивность табов Саммари.** Desktop/mobile: табы не создают горизонтальный скролл страницы; на mobile — компактный скролл-в-контейнере либо стек. **Готово, когда:** 320–1440 без overflow; `failures: 0`. **Файлы:** `web/static/app.css`, `web/index.html`.

## Блок G — Инварианты, тесты, маркер-тесты, red→green — `T-2727…T-2734`

- [x] **T-2727** [@Builder] — **Тест «один промпт — один источник» (§48/§85).** Два маршрута → один объект; правка видна в обоих; нет серверных/клиентских копий; один write-path. **Готово, когда:** тест зелёный и **доказательно падает** при появлении второй копии. **Файлы:** `tests/js/round1025_f5_*_test.js`, при необходимости `tests/test_*_round1025.py`.
- [x] **T-2728** [@Builder] — **Тест «не менять сохранённую модель при открытии» (§49).** Открыть форму подключения → сохранённое значение модели неизменно; повторное открытие не мутирует. **Готово, когда:** тест зелёный и падает при подстановке default. **Файлы:** `tests/js/round1025_f5_*_test.js`.
- [x] **T-2729** [@Builder] — **Тест «аккордеон не основная навигация» / «единственное textarea не в аккордеоне» (§48/§69).** Проверка, что основной редактор виден сразу; навигация — не аккордеон. **Готово, когда:** тест фиксирует видимость редактора; падает при сокрытии в аккордеон. **Файлы:** `tests/js/round1025_f5_*_test.js`, `tests/` (маркеры).
- [x] **T-2730** [@Builder] — **Тест «API-ключи не копируются в модуль» (§46).** В конфигурации модуля нет копий API-ключей; ключи — только в разделе секретов. **Готово, когда:** grep/структурный тест зелёный. **Файлы:** `tests/`.
- [x] **T-2731** [@Builder] — **Инварианты окружения.** Δ DDL = 0 (`user_version=12`); Δ каталога = 0 (459/98/96/21/418); CSP/zero-build (нет новых внешних ссылок/data-URI/библиотек); запрет WebGL; новых API нет (только reuse `/api/llm/test`,`/api/images/test`); нет новых state-библиотек. **Готово, когда:** инвариант-тесты зелёные; `services/param_catalog.py` и миграции вне диффа. **Файлы:** `tests/`, `web/**`.
- [x] **T-2732** [@Builder] — **Регресс-путь и §79/§116.** `openModuleWorkspace` — точка входа сохранена; `openModuleWindow` — доступен; настройки модулей/виджеты/разделы/параметры не потеряны; scope-контракт F3/F4 не нарушен. **Готово, когда:** тест фиксирует обе точки входа; чек-лист §79/§116 без пропусков. **Файлы:** `tests/js/`, `tests/`.
- [x] **T-2733** [@Builder] — **Доказательная падаемость (red→green).** Для ключевых проб (T-2727…T-2730) подтвердить, что они падают при откате фикса и зелёные после. **Готово, когда:** red→green подтверждён независимо @Reviewer/@Scanner (описание в `evidence.md`). **Файлы:** `evidence.md`.
  - ✅ Подтверждено **независимо**: `review.md` (итерация 2, @Reviewer Approved) — таблица «Независимый red→green» (5 мутаций: b2/d2/g2/a/e, restore byte-identical); @Scanner-итерация закрыла M-F5S-1 со своим red→green (evidence.md §7). Дополнительно red→green карточки §49/T-2714 — evidence.md §8.
- [x] **T-2734** [@Builder] — **Маркер-тесты — атомарно с кодом.** Обновить пины версий/маршрутов/текстов (`tests/test_*_round1025.py`, `tests/js/round1025_*`), если F5 их затрагивает; один коммит. **Готово, когда:** маркер-тесты синхронны; `git diff --check` OK. **Файлы:** `tests/`.

## Блок H — Версия и README — `T-2735`

- [x] **T-2735** [@Builder] — **Bump `APP_VERSION` 2.58.9 → 2.58.10** (`config/settings.py`) + синхронизация `README.md` (`test_app_version_matches_readme`) + cache-bust `?v=__APP_VERSION__`. **Готово, когда:** bump сделан; тест синхронности зелёный; ассеты используют новый `?v=`. **Файлы:** `config/settings.py`, `README.md`.

## Блок 9 — Регресс → ревью → аудит → merge → архив → деплой → live → метрики — `T-2736…T-2744`

- [x] **T-2736** [@Builder] — **Регресс-прогон.** Полный pytest (база **8229/0**) + JS-набор (F4-раннеры + новые F5) + `node --check web/app.js` / `telegram-init.js` + `git diff --check`. **Готово, когда:** 0 новых падений; целевые F5-тесты зелёные; результаты приложены. **Файлы:** — (прогон).
  - ✅ Итерация 2: `py -3 -m pytest -q` → **8243 passed / 1 skipped / 5 failed** (5 — пред-существующий `aiogram InputRichMessageMedia`, не F5); F5 pytest 17 passed; все `tests/js/*.js` — 0 падений; `node --check web/app.js` + `web/static/telegram-init.js` — OK; `git diff --check` — OK; Δ DDL=0; Δ каталога=0 (evidence.md §2).
  - ✅ Итерация 4 (финал): `py -3 -m pytest -q` → **8245 passed / 1 skipped / 5 failed** (те же env); F5 pytest **19 passed**; все `tests/js/*.js` — 0 падений; Playwright §71 — **failures: 0**; `node --check`/`git diff --check` OK; Δ DDL=0; Δ каталога=0 (evidence.md §8).
- [x] **T-2737** [@Reviewer] — **Ревью F5** (итерации). Контракт шва/маршрута, «один промпт — один источник», запрет аккордеон-навигации, §49 «не менять модель», §46 «ключи не в модуле», границы Эпик 2. **Готово, когда:** Approved или Changes requested с конкретными пунктами. Evidence — `review.md`, `evidence.md`.
- [x] **T-2738** [@Scanner] — **Независимый аудит F5.** R16/R17, отсутствие лишних API/библиотек, Δ каталога/DDL, сохранность регресс-пути, секреты. **Готово, когда:** отчёт `plans/reports/round1025_f5_scanner_audit.md` с вердиктом «к деплою ДА/НЕТ».
- [x] **T-2739** [@Architect] — **Step 7 Merge** → `plans/ARCHITECTURE.md` (**§61+**, т.к. §60 занят F4) + SUPERSEDE/AMEND-карта. **Готово, когда:** раздел добавлен; расхождения spec↔код разрешены.
- [x] **T-2740** [@PM] — **Архивация (Step 8).** Подтвердить @Reviewer Approved + @Scanner C0/H0 + Architect reconciliation + evidence; перенести папку → `plans/archive/module-workspace-tabs-round1025/` (ID/содержимое сохранены, UTF-8); `deployment.md` допишет @DevOps на Шаге 9. **Готово, когда:** архив на месте; `plans/features/` без осиротевших ссылок.
- [ ] **T-2741** [@DevOps] — **Деплой (Step 9).** Коммит(ы) кода+тестов + plans + deploy-doc; push origin/master без force; прод fast-forward; `systemctl restart`; `/api/health` 200; `APP_VERSION` 2.58.10; served `?v=2.58.10`; `database is locked`=0; `deployment.md` VERIFIED. **Готово, когда:** прод отдаёт новый ассет, health 200.
- [ ] **T-2742** [@PM → владелец] — **Live-гейт владельца** (post-deploy): workspace/маршруты/табы, master-detail промптов (desktop+mobile), «Проверить» соединение, §84 L1/L2, §85 Саммари — в **реальном Telegram WebView**. **Готово, когда:** владелец подтвердил (скриншоты/логи).
- [ ] **T-2743** [@Memory] — **KG-синк (Step 3) + метрики (Step 10).** Строка + раздел **10.25-F5** в `plans/metrics.md`; KG: `F5-…` → статус, `ADR-1025-15` → Accepted, `release-round1025-f5` / `metric-snapshot-round1025-f5-final` / `tech-debt-round10.25-f5`; синк `MEMORY.md`/`workflow_state.md`/`round1025-architecture`.
- [x] **T-2744** [@PM] — **Финал: следующий приоритет.** Ретро-аудит остатка ТЗ; следующий actionable — **F6** `memory-analytics-reorg-round1025` (по §2 `plans/round1025-architecture.md`); обновить `plans/backlog.md`. **Готово, когда:** next-F объявлен @Orchestrator.

---

## Зависимости / ступень

- Ступень `param_catalog`: F1 → F8 → F4 → **F5** (только метаданные вкладок/промптов, Δ каталога = 0) → F7.
- Ступень web: F1 → F2 → F3 → F4 → **F5** → F6 → F7 → F11 → F9 → F10 (строго сериализовано).
- **Не начинать раньше:** закрытия F4 (§60) и готовности store §37–§42 / шва `openModuleWorkspace`.
- Преемственность: максимум до F5 — **T-2694** (hotfix7/F4). Дублей ID нет.

## Ожидаемые риски (из Шага 0 @Memory — как данность)

- Потеря/изменение текста промптов при переносе (**Critical**) → T-2711.
- Раздвоение промпта на две независимые копии (**High**) → T-2709, T-2725, T-2727.
- Потеря сохранённой модели при открытии формы (**High**, §49) → T-2717, T-2728.
- Копирование API-ключей в конфигурацию модуля (**High**, §46) → T-2702, T-2730.
- Аккордеоны как основная навигация / скрытие единственного textarea (**High**, §48/§69) → T-2710, T-2729.
- Ломка регресс-пути `openModuleWindow` / точки входа (§60.2/§79) → T-2698, T-2732.
- Заход в backend-пайплайн Эпика 2 под видом UI (**Medium**, §85) → T-2722, T-2724.
- Рост каталога/DDL сверх нуля (**High**) → T-2699, T-2718, T-2731.

## Открытые вопросы для @Architect (Step 2 — решить или эскалировать владельцу через @Orchestrator)

- **(а) Форма workspace-маршрута.** `#/modules/<id>` (или иной hash-формат) и совместимость с IA F1 (`HUBS_V2`/`TAB_ROUTES`/hash-deeplink): где «живёт» маршрут, как соотносятся «Модули → <модуль>» и раздел ИИ, нужны ли алиасы; при этом регресс-путь `openModuleWindow`/§60.2 сохраняется.
- **(б) Структура библиотеки промптов (§48).** Конкретная композиция дерево/редактор/описание+переменные+параметры; как снимается аккордеон (`promptsShowAccordion`/uiFlag `PROMPTS_UI_V2_ENABLED`) **без регресса маркер-тестов** и без потери режимов (`5563-5610`).
- **(в) §85 Саммари ↔ Эпик 2.** Как UI-табы (Обзор/Подготовка/Кластеризатор/Писатель/Модели/Лимиты/Тестирование) соотносятся с будущим backend-пайплайном 10.26: что именно UI-only, какие вкладки показывают существующие данные, а какие — «каркас без обещаний».
- **(г) Границы §84.** Что относится к UI прямого чата (L1/L2 карточки, «Проверить»), а что — к Эпик 2 (независимый роутинг/двухвызовный пайплайн); UI не должен обещать несуществующий backend.
- **(д) Источник «применимых вкладок».** JS `MODULES` (**Δ каталога = 0**) vs `services/param_catalog.py` (риск Δ > 0). Рекомендация PM — JS, если @Architect не обоснует иное.

## Статус

- 🟦 **Step 1 @PM — выполнен (22.09.2026):** `tasks.md` детализирован (блоки 0, A–H, 9; **T-2695…T-2744, 50 задач**); открытые вопросы (а)–(д) (5) зафиксированы; `plans/backlog.md` обновлён (F5 → IN PROGRESS). `spec.md`/ADR/код не создавались; `metrics.md`/`MEMORY.md`/`current_task.md` не трогались.
- ✅ **Step 0 @DevOps (T-2695)** и **Step 2 @Architect (T-2696)** — выполнены (spec + ADR-1025-15).
- ✅ **Step 4 @Builder — выполнен (22.09.2026):** блоки A–H (T-2697…T-2735), кроме открытых пунктов ниже. Изменены `web/app.js`/`web/index.html`/`web/static/app.css`, `config/settings.py` (2.58.9→2.58.10), `README.md`, `plans/reports/round1025_f5_ai_map.md`; новые `tests/js/round1025_f5_*` (3) + `tests/test_webapp_f5_round1025.py` (16 passed); маркеры обновлены атомарно. Прогон: полный pytest **8241 passed / 1 skipped / 5 failed** (5 — пред-существующий `aiogram InputRichMessageMedia` ImportError, не F5); `node --check` OK; `git diff --check` OK; Δ DDL=0; Δ каталога=0.
- ✅ **Ранее открытые пункты закрыты итерацией 4 (см. ниже):** **T-2703** (Playwright/Chromium оказался доступен — прогон `failures: 0`), **T-2726** (закрыт), **T-2714** (поля карточки §49 доделаны: «резервная модель»/«Настроить»), **T-2733** (независимый red→green — `review.md` итер.2 + evidence.md §8).
- 🔧 **Итерация после ревью @Reviewer (22.09.2026, Changes requested, 4 пункта):** FIX-1 (HIGH) — вторая дверь `#/ai/prompts/<slug>[/<stage>]` подключена в `routeToTab`/`applyRoute`/`workspace`; заведены рабочие UI-входы (`data-prompt-library-modules` в разделе ИИ, `data-prompt-doors` в модуле), «один промпт — один источник» и один write-path сохранены. FIX-2 (MEDIUM) — вкладка «Тестирование» теперь по фактическому содержимому (`workspaceTestingBlocks`, reuse `testBlock`), пустая не рендерится; generic empty-state не показывается на страницах workspace. FIX-3 (LOW) — `workspaceCoverage` доказателен: учитывает `m.tabs` и статические `TABS[m.tab].sources`. FIX-4 (LOW) — red→green ключевых проб приложен. Прогон: F5 pytest 17 passed; JS-маркеры OK; полный pytest **8243 passed / 1 skipped / 5 failed** (5 — пред-существующий `aiogram InputRichMessageMedia`); `node --check`/`git diff --check` OK; Δ DDL=0; Δ каталога=0.
- 🔧 **Итерация 3 после @Scanner (22.09.2026, T-2738; M-F5S-1 Medium-блокер + L-F5S-1..3):** M-F5S-1 — `openWorkspacePrompt` учитывает дверь: из библиотеки промптов остаётся в библиотеке (`#/ai/prompts/<slug>[/<stage>]/<key>`), а не строит маршрут на несуществующую промпт-вкладку модуля (`mod_summary`/`mod_sleep`) → редактор реально открывается (был откат на «Обзор»); `parsePromptLibraryRoute` расширен фокусом промпта (`promptKey`), `routeParent`/`workspace()` согласованы; «один объект — один источник» и `m.tab` не тронуты. L-F5S-3 — известный модуль без `MODULE_PROMPT_GROUPS` → `#/ai/prompts`. L-F5S-2 — дерево промптов `role="group"`+`aria-label`, без `listitem` на кнопках. L-F5S-1 — тач-цель табов `[data-workspace-tabs] button` ≥44px на mobile. Прогон: F5 pytest **18 passed** (+1 новая проба); все `tests/js/*.js` 0 падений; целевые регресс-пины **151 passed**; полный pytest **8244 passed / 1 skipped / 5 failed** (те же env); `node --check`/`git diff --check` OK; Δ DDL=0/Δ каталога=0; red→green новой пробы приложен (evidence.md §7).
- ⏳ **Дальше:** повторное @Reviewer (T-2737), расширенный регресс (T-2736) — @Orchestrator.
- ✅ **Итерация 4 (22.09.2026, закрытие остатка):** **T-2714** — карточка подключения §49 доделана (все 7 полей + «Настроить»-раскрытие существующей формы, reuse `saveBlock`/`testBlock`; секреты не читаются; §49 «не менять модель» сохранён). **T-2703** — Chromium/Playwright ДОСТУПНЫ в среде (`.venv` playwright 1.62.0 + chromium-1234): `tools/ui_round1025_matrix.py` расширен workspace-маршрутами/пробой и прогнан → **`failures: 0`** (320/390/768/1024/1280/1440: overflow нет, `tabsWrap=0`, тач-цели 44px). **T-2733** — закрыт: red→green независимо подтверждён в `review.md` (итер.2) + новое red→green карточки (evidence.md §8). Прогон: F5 pytest **19 passed**; все `tests/js/*.js` 0 падений; полный pytest **8245 passed / 1 skipped / 5 failed** (те же env); `node --check`/`git diff --check` OK; Δ DDL=0/Δ каталога=0.
- ✅ **Step 5 @Reviewer (T-2737) — Approved** (итерация 2, 22.09.2026): FIX-1…FIX-4 из Changes requested закрыты, блокер M-F5S-1 закрыт; независимый red→green (5 мутаций, restore byte-identical) — `review.md`.
- ✅ **Step 6 @Scanner (T-2738) — повторный аудит:** **Critical 0 / High 0 / Medium 0 / Low 0 / Info 2 → «к деплою ДА»**; итер.1 `M-F5S-1` + `L-F5S-1..3` закрыты по коду и доказательно — `plans/reports/round1025_f5_scanner_audit.md`.
- ✅ **Step 7 @Architect (T-2739) — Merge:** `plans/ARCHITECTURE.md` **§61** (§61.1–§61.9: workspace §46, ИИ §47, библиотека промптов §48, §49, §84/§85, тесты/маркеры, SUPERSEDE/AMEND-карта §61.7, техдолг §61.8, ссылки §61.9).
- ✅ **Step 8 @PM (T-2740) — архивация (22.09.2026):** папка перенесена в `plans/archive/module-workspace-tabs-round1025/` (ID/содержимое/UTF-8 сохранены); `plans/backlog.md` (строка F5 → ✅, следующий — **F6**) и `plans/MEMORY.md` синхронизированы; T-2744 (следующий приоритет) — ✅. **Открыто:** T-2741 (deploy, ⏳ Шаг 9 @DevOps), T-2742 (live-гейт владельца, ⏳), T-2743 (метрики/KG, ⏳ Шаг 10 @Memory).
