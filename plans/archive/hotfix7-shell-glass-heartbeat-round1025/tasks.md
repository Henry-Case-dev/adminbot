# HOTFIX7 — `hotfix7-shell-glass-heartbeat-round1025` (внеплановый приоритетный пакет, Волна 1.6)

> **ТЗ:** **UPD «Срочный фикс текущего фронта»** в `plans/current_task.md`, **строки 6073–6154** (прочитан дословно на Step 0/1; `current_task.md` **НЕ изменялся**). Пункты UPD 1–6 — обязательный объём; **UPD 7** — аудит остатка ТЗ и автоматическое продолжение.
> **Тип:** UI + shell/glass/heartbeat (web, zero-build). **Приоритет:** P0, **выше F4** (F4 — ⏸ PAUSED).
> **Статус:** ✅ **COMPLETED + MERGED + ARCHIVED + DEPLOYED (Шаг 9 @DevOps, 22.09.2026); Merge `plans/ARCHITECTURE.md` §59; `spec.md` + ADR-1025-13 созданы (@Architect, Step 2). Коммиты `7073e34` (код+тесты) + `a9cec67` (планы) + `2a67829` (deploy-doc), `APP_VERSION` 2.58.8, health 200, `database is locked`=0 → `deployment.md` VERIFIED; T-2690 закрыт фактом. ⏳ live-гейт владельца T-2682 открыт.** @Reviewer **Approved** (итер.2, T-2686); @Scanner **Critical 0 / High 0 / Medium 0** (T-2687).
> **Задачи:** **T-2658…T-2694 (37)**. Нумерация продолжает F4 (максимум по `plans/features/`, `plans/archive/` и репозиторию — **T-2657**); дублей ID нет (проверено `git grep` + поиском по `plans/`).
> **Шаг 0 (@Memory) — как данность (не перепроверяется):** карта кода UPD 1–5, конфликт-анализ с hotfix6/ADR-1025-12, риски, baseline, KG-узлы `HOTFIX7-shell-glass-heartbeat-round1025`. Baseline: HEAD `5a5465c`, `APP_VERSION` **2.58.7**, pytest **8185/0**, JS **26/26**, каталог **459/98/96/21/418** (Δ=0). Источник — `plans/workflow_state.md`, `plans/MEMORY.md`.

## 0. Жёсткие инварианты и ограничения (для всех задач)

- **Δ DDL = 0** (SQLite `user_version=12`, без миграций/новых таблиц/ALTER). **Δ каталога = 0** — `REGISTRY 459 / GROUPS 98 / _TAB_BY_GROUP 96 / TAB_RULES 21 / Settings 418` (`services/param_catalog.py` не трогается).
- **CSP / zero-build:** `script-src 'self'` (ADR-1016-2 / ADR-1024-13), без CDN и новых зависимостей/state-библиотек; Vue 3 global + self-host.
- **Запрет WebGL.** Разрешены **Canvas 2D** и CSS-анимации (runtime-движок heartbeat уже Canvas 2D + rAF — **переиспользовать**, не переписывать стек).
- **Нативные кнопки Telegram (BackButton/MainButton) CSS не двигать** — `--header-h` и их геометрия остаются (ARCHITECTURE §604/§642/§56). Fullscreen-sync — **ADR-1024-24** (сохранить).
- **IA не ломать:** меню/разделы/маршруты (`#/…`) и активный экран сохраняются как есть; правки только геометрии/визуала. **Новый редизайн с нуля запрещён** (UPD, «Важно»).
- **Store-контракт F4 §37–§42 не переписывать** (scope-контракт F3 §37–§42 store, `persistItems` F0 write-path — reuse).
- **СОХРАНИТЬ из hotfix6/ADR-1025-12:** D3 (`computeBottomOffset` = max трёх инсетов), D5 (нативные кнопки не двигать, `--header-h`, fullscreen-sync), D1 (foreground-линза/feature-detect), deny-list tier C, фон §10, контраст **AA §8**, IA F1.
- **AMEND (Step 2 @Architect):** ADR-1025-12 **D2** (стекло панелей) и **D4** (визуал heartbeat); **уточнение** ADR-1025-9 **D2** (значения токенов). Значения — AMEND, не отказ от палитры §8.
- **R17/R18:** без секретов/сырых значений в логах/отчётах; `plans/current_task.md` — untracked, **не коммитить/не изменять**; теги/бэкапы `pre-round1025*` и `stash@{0}` **НЕ удалять**.
- **Откат:** тег `pre-round1025-hotfix7` (T-2658) + `git revert`; soft — env-only `ClassVar` kill-switch (`UI_GLASS_TIER_OVERRIDE`/`UI_HEARTBEAT_CANVAS_ENABLED`/`UI_HEADER_COMPACT_V2`, default ON). Поэтапная раскатка internal→10%→50%→100% **не требуется** (пет-проект, один прод; прецедент §53/§54.1/§55/§56/§58).

## Трассируемость: UPD → задача

| UPD (строки `current_task.md`) | Требование | Задачи |
|---|---|---|
| UPD 1 (6076–6080) | Стабильная геометрия header/sidebar/main/bottom-nav (desktop+fullscreen); в fullscreen ничего не пропадает/режется/наезжает; **heartbeat не исчезает**; container sizing/overflow/z-index/sticky/fixed/safe-area/высоты; Telegram fullscreen/webview | T-2661…T-2665 |
| UPD 2 (6082–6093) | Premium pulse-индикатор вместо «линия + плавающая точка»; цвет/интенсивность от реальных метрик (норма бирюзово-зелёный / warning янтарный / critical красно-розовый); пульс/свечение; **не** бесконечный `translateX`; состояние читается с первого взгляда; normal/fullscreen/mobile | T-2666…T-2670 |
| UPD 3 (6095–6101) | Header и sidebar — серо-графитовые, визуально отдельный слой, не в цвет карточек; shell vs карточки отличаются по тону и глубине | T-2671, T-2672 |
| UPD 4 (6103–6115) | Liquid glass: полупрозрачный серо-графитовый base + backdrop blur + мягкая внутренняя подсветка + тонкая светлая обводка + слабый specular + аккуратная текстура + разделение фон/shell/карточки по глубине; без грубой виньетки/грязного затемнения; премиально и читабельно | T-2673…T-2677 |
| UPD 5 (6117–6122) | Header без наезда на контент; sidebar/topbar цельные shell-элементы; mobile safe-area/Telegram chrome/нижний bar/fullscreen; устранить съезды | T-2678…T-2680 |
| UPD 6 (6124–6136) | **Обязательная проверка:** desktop normal, desktop fullscreen, tablet, mobile regular, **mobile fullscreen inside Telegram WebView**; скриншоты + Playwright (heartbeat виден везде; header не ломается; glass качественный; sidebar/topbar серые и отделены; ничего не съезжает) | T-2681…T-2683 (+ регресс T-2684/T-2685) |
| UPD 7 (6138–6154) | Без human gate: аудит `current_task.md` → невыполненные задачи → продолжить по приоритетам; отчёт: (1) что исправлено по UI, (2) что оставалось, (3) что взято следующим; не зацикливаться на мелких фронт-фиксах | **Блок I** (T-2691…T-2693) |
| «Важно» (6150–6154) | Не делать редизайн с нуля; не ломать IA; не упрощать до «косметического blur» | инварианты §0 + T-2684/T-2685 |

## Открытые вопросы для @Architect (решить в `spec.md`/ADR-1025-13 либо эскалировать владельцу через @Orchestrator)

- **(a) Точный визуальный рецепт glass-shell:** какие токены/значения дают «полупрозрачный серо-графитовый base» и достаточное отличие от карточек — **новые** `--shell-*`-токены vs разведение существующих `--glass-bg`/`--glass-bg-strong` (`app.css:52-65`). Только CSS-токены (Δ каталога = 0). Зафиксировать значения `background`, `border`, `box-shadow`, `backdrop-filter` для header/sidebar/topbar.
- **(b) Глубина shell vs карточки:** как разделить фон → shell → карточки по глубине, не нарушив контраст **AA §8** (`--glass-shadow rgba(3,7,18,.75)`, `app.css:60`) и не создав «грязное затемнение». Кандидаты: снижение непрозрачности тела, мягкая внешняя тень, z-слой без наслоения blur (перф-риск).
- **(c) Метрика → визуал heartbeat:** пороги и маппинг метрик на состояния (норма/предупреждение/критично) — переиспользовать существующую state-machine `_heartbeatTransition` (`app.js:6440-6507`) и телеметрию `heartbeatSample` (`:6512-6561`)? Как кодировать интенсивность (амплитуда/период/насыщенность/свечение) и как анимировать пульс **без «плавающей точки»** и без бесконечного `translateX` (`_hbDraw` `:6658-6674`).
- **(d) Механизм fullscreen-высоты:** `.app-shell` одновременно `min-height:var(--tg-viewport-stable-height,100vh)` (`app.css:805-810`) и `height:100dvh; overflow:hidden` (`:819-828`) — выбрать единый источник (fixed vs `dvh` vs `--tg-*`) и безопасный минимум, при котором ничего не режется; где живёт `--header-h` (`app.js:8564-8586`).
- **(e) Причина «heartbeat исчезает в fullscreen»:** подтвердить Playwright/живым WebView (Step 0 не воспроизводит). Кандидаты: обрезка `overflow:hidden`, конфликт `min-height`/`height`, `--header-h`/`scroll-padding-top`, z-index контейнера heartbeat (`index.html:2786-2815`).
- **(f) Остаток ТЗ (UPD 7):** порядок продолжения после HOTFIX7 — возврат к **F4** (Step 2 @Architect) или иная приоритизация из `plans/reports/round1025_tz_remaining_audit.md`.

---

## Блок 0 — Точка отката, baseline, Step 2 design

- [x] **T-2658** [@DevOps] — **Точка отката и baseline перед кодом.** Тег `pre-round1025-hotfix7`; бэкап `var/backups/hotfix7-round1025-<ts>/` (значения не печатать; удаление — только после утверждения владельца, R18); фиксация baseline (HEAD `5a5465c`, `APP_VERSION` **2.58.7**, pytest **8185/0**, JS **26/26**, каталог 459/98/96/21/418, `database is locked`/health — по последнему подтверждённому состоянию). **Готово, когда:** тег и бэкап созданы; baseline зафиксирован; `git status`/`git diff` чистые кроме ожидаемых plans-правок. **Файлы:** git tag/бэкап (вне репо), запись в `plans/workflow_state.md` (через @Orchestrator).
- [x] **T-2659** [@Architect] — **Step 2: `spec.md` + ADR-1025-13** (`shell-glass-heartbeat`). Зафиксировать визуальный рецепт shell/glass (токены/значения/глубина), метрика→визуал heartbeat (пороги/интенсивность/анимация без «точки»), механизм fullscreen-высоты (fixed vs dvh vs `--tg-*`, безопасный минимум), способ отделения shell от карточек без нарушения AA §8. **Обязательно:** решения по **(a)–(e)** выше либо эскалация владельцу через @Orchestrator (вопрос **(f)** — зона @PM по итогам аудита UPD 7). **Готово, когда:** `spec.md` + ADR-1025-13 созданы; решения (a)–(e) зафиксированы; `tasks.md` сверен со spec (расхождения → возврат @PM). **Файлы:** `plans/features/hotfix7-shell-glass-heartbeat-round1025/spec.md`, `adr-1025-13-*.md`.
- [x] **T-2660** [@Architect] — **AMEND-карта и reconcile `tasks.md`.** Явные AMEND: **ADR-1025-12 D2** (стекло панелей) и **D4** (визуал heartbeat); **ADR-1025-9 D2** (уточнение значений); AMEND ADR-1025-8/hotfix4 — только если меняется геометрия нижнего bar. **СОХРАНИТЬ** без изменений: ADR-1025-12 **D3** (`computeBottomOffset`), **D5** (нативные кнопки, `--header-h`, fullscreen-sync ADR-1024-24), **D1** (foreground-линза/feature-detect), deny-list tier C, фон §10, AA, write-path F0 `persistItems`, IA F1, store-контракт F4 §37–§42. **Готово, когда:** AMEND-карта в spec/ADR; затронутые ADR перечислены с точными пунктами; `tasks.md` не противоречит spec. **Файлы:** `spec.md`, `adr-1025-13-*.md`, `tasks.md`.

## Блок A — UPD 1: layout + fullscreen (стабильная геометрия)

- [x] **T-2661** [@Builder] — **Диагностика fullscreen root cause (воспроизведение).** Воспроизвести съезды в desktop и mobile fullscreen; подтвердить/опровергнуть кандидаты Step 0: `.app-shell` `min-height` vs `height:100dvh; overflow:hidden` (`app.css:805-810`, `:819-828`), `--header-h`/`scroll-padding-top`, z-index/sticky/fixed, safe-area. R17-safe заметки (без секретов). **Готово, когда:** зафиксированы точные root cause и минимальный список правок; есть воспроизведение (скриншот/DevTools). **Файлы:** `web/static/app.css:747-828`, `web/app.js:4717-4806,8564-8586`, `web/index.html:47-230`, `web/static/telegram-init.js:36-89`.
- [x] **T-2662** [@Builder] — **Стабильная геометрия shell desktop.** Header/sidebar/main/bottom-nav — предсказуемые размеры и позиции при 320/390/768/1024/1280/1440 px; нет горизонтального скролла, наездов и «прыжков» высоты. **Готово, когда:** геометрия стабильна на всех контрольных ширинах; IA/маршруты сохранены. **Файлы:** `web/static/app.css:747-828`, `:1555-1674`, `web/index.html:47-230`.
- [x] **T-2663** [@Builder] — **Fullscreen: ничего не исчезает/не режется/не наезжает.** Единый источник высоты по решению @Architect (d); корректные `overflow`, `container sizing`, `z-index`, `sticky/fixed`, safe-area. **Готово, когда:** в desktop fullscreen и mobile fullscreen все зоны (header/sidebar/main/bottom-nav) видимы и не перекрываются; высота не «дёргается». **Файлы:** `web/static/app.css:805-828`, `web/app.js:4717-4806`, `web/static/telegram-init.js:36-89`.
- [x] **T-2664** [@Builder] — **Heartbeat не исчезает в fullscreen (UPD 1, ключевой пункт).** Устранить причину исчезновения виджета в fullscreen (кандидаты (e)); виджет остаётся видимым и корректно расположенным. **Готово, когда:** heartbeat виден в desktop normal/fullscreen и mobile normal/fullscreen (Playwright-подтверждение в T-2681). **Файлы:** `web/index.html:2786-2815`, `web/static/app.css:856-913`, `web/app.js:6589-6678`.
- [x] **T-2665** [@Builder] — **Telegram fullscreen/webview поведение.** Синхронизация с Telegram viewport (stable height, chrome, `viewport-fit=cover`, onboarding/смена ориентации), не ломая fullscreen-sync **ADR-1024-24** и нативные кнопки. **Готово, когда:** вход/выход fullscreen внутри Telegram не двигает нативные кнопки и не ломает safe-area; env-only soft-откат работает. **Файлы:** `web/static/telegram-init.js:36-89`, `web/app.js:4750-4806`.

## Блок B — UPD 2: heartbeat premium (полная переделка визуала)

- [x] **T-2666** [@Builder] — **Убрать «линию + плавающую точку».** Заменить бегущую синусоиду и сдвигающийся вбок импульс `pulseX` (`app.js:6658-6674`) на новый рендер; **без бесконечного `translateX`**, без примитивной SVG-анимации. Движок остаётся **Canvas 2D + rAF** (переиспользовать `startHeartbeatCanvas`/`_hbFrame`, не менять стек). **Готово, когда:** в коде нет горизонтально бегущей точки/линии; новый рендер работает; `UI_HEARTBEAT_CANVAS_ENABLED` OFF → прежнее поведение. **Файлы:** `web/app.js:6589-6678`, `web/static/app.css:856-913`.
- [x] **T-2667** [@Builder] — **Цвет/интенсивность от реальных метрик.** Норма — спокойный бирюзово-зелёный, warning — янтарный/оранжевый, critical — красно-розовый; интенсивность (амплитуда/период/насыщенность/свечение) зависит от метрик. Источник — существующая телеметрия `heartbeatSample` (`app.js:6512-6561`) и поллинг 10–30 с (не новый контур). **Готово, когда:** три состояния визуально различимы и привязаны к реальным данным; `missing ≠ bad` сохранено; палитра согласована с §8/AA. **Файлы:** `web/app.js:6440-6561,6589-6678`.
- [x] **T-2668** [@Builder] — **Пульс, свечение, читаемость «с первого взгляда».** Корректный ECG/пульс-язык (heartbeat-эффект, а не декоративная линия) + свечение состояния; состояние считывается мгновенно (цвет+форма+подпись), без визуальной дешёвости. **Готово, когда:** состояние определяется владельцем с первого взгляда; нет «плавающей точки»; анимация сдержанная и премиальная. **Файлы:** `web/app.js:6589-6678`, `web/static/app.css:856-913`, `web/index.html:2786-2815`.
- [x] **T-2669** [@Builder] — **Heartbeat: normal / fullscreen / mobile.** Корректный рендер и размеры во всех трёх режимах, без расширения за пределы экрана (преемник overflow-фикса C1 hotfix6). **Готово, когда:** виджет не выходит за вьюпорт и не обрезается ни в одном режиме; тач/тултип не сломан. **Файлы:** `web/index.html:2786-2815`, `web/static/app.css:856-913`.
- [x] **T-2670** [@Builder] — **Reduced-motion, перф, feature-detect.** `prefers-reduced-motion` (статичное корректное состояние), пауза rAF вне видимости/на скрытой вкладке, feature-detect Canvas 2D (reuse, преемник `I-H6-1`), cap нагрузки. **Готово, когда:** reduced-motion даёт читаемое статичное состояние; нет лишней нагрузки при скрытой вкладке; нет WebGL. **Файлы:** `web/app.js:6589-6678`, `web/static/app.css:856-913`.

## Блок C — UPD 3: glass shell — отдельный слой

- [x] **T-2671** [@Builder] — **Развести shell и карточки по тону (серо-графитовые).** Устранить общий `--glass-bg=rgba(21,27,42,.5)` у `.app-sidebar` (`app.css:1570`) и карточек `.card/.module-card/.hub-card` (`:169-176`, `:1037`): shell — серо-графитовый и визуально отдельный, не в цвет карточек. Реализация по рецепту @Architect (a). **Готово, когда:** header/sidebar заметно отличаются от карточек по тону; карточки не перекрашены; Δ каталога=0. **Файлы:** `web/static/app.css:52-65,169-180,1028-1043,1564-1674`.
- [x] **T-2672** [@Builder] — **Разделение shell vs карточки по глубине.** Shell выглядит отдельным слоем интерфейса (глубина/тень/z-слой), карточки — тоньше/ближе к фону; без наслоения blur-ов (перф). **Готово, когда:** визуально читается глубина «фон → shell → карточки»; контраст AA сохранён. **Файлы:** `web/static/app.css:52-65,126-180,1564-1674`, `web/app.js:8500-8560`.

## Блок D — UPD 4: liquid glass качество

- [x] **T-2673** [@Builder] — **Серо-графитовый base + backdrop blur.** Полупрозрачный серо-графитовый base layer и корректный `backdrop-filter: blur(...)` для shell-панелей (header/sidebar/topbar); без грубого размытия. **Готово, когда:** панели читаются как стекло, текст читабелен; `@supports`-фолбэк сохранён. **Файлы:** `web/static/app.css:52-65,1053-1173,1564-1674`.
- [x] **T-2674** [@Builder] — **Мягкая внутренняя подсветка + тонкая светлая обводка.** Тонкая полупрозрачная светлая обводка + очень мягкая внутренняя подсветка (inset), без «резинового» блеска. **Готово, когда:** обводка/подсветка различимы, но не грубые; AA сохранён. **Файлы:** `web/static/app.css:52-65,1053-1173`.
- [x] **T-2675** [@Builder] — **Слабый specular highlight + аккуратная текстура.** Сдержанный specular-блик + очень аккуратный шум/texture (при необходимости), без «пластикового» вида. **Готово, когда:** блик/текстура едва заметны и премиальны; перф не деградирует (учесть линзу `#lg-lens`, `index.html:34-46`, cap `UI_LENS_MAX_NODES`). **Файлы:** `web/static/app.css:1053-1173`, `web/index.html:34-46`.
- [x] **T-2676** [@Builder] — **Убрать грубую виньетку и грязное затемнение.** Смягчить/убрать «грязь»: тяжёлая `--glass-shadow rgba(3,7,18,.75)` (`app.css:60`), плотный `body::before` (opacity .42, `:126-138`), подложка `.85`. **Готово, когда:** нет грубой виньетки/грязного затемнения; фон §10 не сломан; текст читабелен. **Файлы:** `web/static/app.css:52-65,120-180`.
- [x] **T-2677** [@Builder] — **Разделение фон/shell/карточки по глубине + читабельность.** Итоговая иерархия глубин и премиальность; сквозная проверка контраста **AA §8** (в т.ч. на `.75rem`). **Готово, когда:** три слоя визуально различимы; AA-контраст подтверждён (отчёт); эффект сдержанный, не «косметический blur». **Файлы:** `web/static/app.css:52-180,1028-1173`.

## Блок E — UPD 5: shell-выравнивание

- [x] **T-2678** [@Builder] — **Header: выравнивание без наезда на контент.** Выровнять элементы шапки (селектор области/бейдж/аватар/роль/⛶), не допускать наложения на контент и на нативные кнопки Telegram; сохранить двухстрочную шапку hotfix6 (`header-compact-v2`, `--header-h`, `scroll-padding-top`). **Готово, когда:** шапка не наезжает на контент и нативные кнопки на desktop/tablet/mobile; `--header-h` остаётся корректным. **Файлы:** `web/static/app.css:747-800`, `web/app.js:8564-8586`.
- [x] **T-2679** [@Builder] — **Sidebar/topbar — цельные shell-элементы.** Визуальная цельность (единый тон/границы/радиусы/глубина) sidebar/drawer/topbar и их отличие от внутренних карточек; не сливаться. **Готово, когда:** shell-элементы выглядят единым слоем и отделены от карточек; drawer на 768–1199 корректен. **Файлы:** `web/static/app.css:1564-1674`.
- [x] **T-2680** [@Builder] — **Mobile: safe-area / Telegram chrome / нижний bar / fullscreen.** Устранить все съезды: `env(safe-area-inset-*)`, Telegram chrome, `.bottom-nav`/`.more-sheet` (преемственность B/hotfix4, D3 `computeBottomOffset`), fullscreen. **Готово, когда:** на mobile/tablet ничего не уползает за экран; нижний bar в пределах вьюпорта; `rect.bottom <= innerHeight` (Playwright-проверка T-2681). **Файлы:** `web/static/app.css:1345,1560-1674`, `web/static/telegram-init.js:36-89`, `tools/ui_round1025_matrix.py`.

## Блок F — UPD 6: обязательная приёмка (5 режимов + live-гейт)

- [x] **T-2681** [@Builder] — **Playwright-матрица 5 режимов + скриншоты.** Прогон: **desktop normal, desktop fullscreen, tablet, mobile regular, mobile fullscreen**. Проверить: heartbeat виден везде; header не ломается; glass shell качественный; sidebar/topbar серые и отделены от карточек; ничего не съезжает; `rect.bottom <= innerHeight`. Артефакты — скриншоты + лог матрицы (`tools/ui_round1025_matrix.py`; при отсутствии `playwright` — зафиксировать как Info, не блокер деплоя). **Готово, когда:** 5 режимов прогнаны, скриншоты приложены, FAIL-ов нет. **Файлы:** `tools/ui_round1025_matrix.py`, `plans/reports/round1025_hotfix7_ui_report.md`.
- [ ] **T-2682** [@DevOps/@владелец] — **Явный live-гейт: реальный Telegram WebView (owner).** Проверка **mobile fullscreen inside Telegram WebView** на реальном устройстве: heartbeat виден, header цел, glass качественный, sidebar/topbar серые и отделены, ничего не съезжает. Автоматизации недоступно — выполняется владельцем. **Готово, когда:** владелец подтвердил (или зафиксирован открытый live-гейт с точной инструкцией). **Файлы:** `plans/reports/round1025_hotfix7_ui_report.md` (запись результата), `plans/workflow_state.md`.
- [x] **T-2683** [@PM] — **Отчёт обязательной приёмки UPD 6.** Свести результаты Playwright-матрицы и live-гейта; проверить, что все 5 пунктов UPD 6 («heartbeat видно везде», «header не ломается», «glass качественный», «sidebar/topbar серые и отделены», «ничего не съезжает») закрыты доказательствами. **Готово, когда:** отчёт создан, каждому пункту UPD 6 сопоставлено доказательство; открытые live-пункты помечены явно. **Файлы:** `plans/reports/round1025_hotfix7_ui_report.md`.

## Блок F2 — Регресс и инварианты

- [x] **T-2684** [@Builder] — **Регресс-прогон + контроль инвариантов.** pytest (целевой), JS-маркеры, `node --check web/app.js`; проверить инварианты: **Δ DDL=0**, **Δ каталога=0** (REGISTRY 459 / GROUPS 98 / `_TAB_BY_GROUP` 96 / TAB_RULES 21 / Settings 418), CSP/zero-build, отсутствие WebGL, нативные кнопки Telegram не сдвинуты (`--header-h`, fullscreen-sync ADR-1024-24), IA/маршруты не сломаны, store-контракт F4 §37–§42 и `persistItems` F0 не переписаны. **Готово, когда:** регресс зелёный; каждый инвариант подтверждён (числа/факты); отклонений нет. **Файлы:** `tests/`, `web/app.js`, `services/param_catalog.py` (только чтение эталона).
- [x] **T-2685** [@Builder] — **Атомарные маркер-тесты (F2/F3/hotfix4/hotfix6/LiquidGlass).** Обновить/добавить маркеры под новое поведение shell/glass/heartbeat **одним коммитом** с кодом (без рассинхронизации эталонов), не задев маркеры F0/F1/F4. **Готово, когда:** маркеры соответствуют коду; ни один существующий гейт не отключён/не ослаблен. **Файлы:** `tests/js/`, маркерные webapp/frontend-тесты.

## Блок G — Ревью и аудит

- [x] **T-2686** [@Reviewer] — **Ревью HOTFIX7.** (`review.md`, итер.2 Approved) Проверить соответствие UPD 1–6, качество (не «косметический blur»), сохранность IA/§8/AA, инварианты §0, полноту приёмки UPD 6. **Готово, когда:** Approved либо Changes Requested со списком; при отказе — возврат @Builder с адресными замечаниями. **Файлы:** `plans/reports/round1025_hotfix7_reviewer.md` (или запись у @Orchestrator).
- [x] **T-2687** [@Scanner] — **Независимый аудит.** Скан shell/glass/heartbeat/fullscreen-правок; регресс-риски, перф, R17-safe логи, Δ DDL/каталога, CSP/no-WebGL; обновить `plans/reports/full_audit_results.md`/`audit_backlog.md` (закрыть процессный хвост hotfix6). **Готово, когда:** Critical 0 / High 0 (или зафиксированы с планом закрытия); отчёт `plans/reports/round1025_hotfix7_scanner_audit.md`. **Файлы:** `plans/reports/round1025_hotfix7_scanner_audit.md`, `plans/reports/full_audit_results.md`, `plans/reports/audit_backlog.md`.

## Блок H — Merge, bump, deploy

- [x] **T-2688** [@Architect] — **Step 7 Merge.** Интегрировать HOTFIX7 в `plans/ARCHITECTURE.md` (следующий свободный параграф — **§59**; проверить фактический максимум) + AMEND-карта (ADR-1025-12 D2/D4, ADR-1025-9 D2; сохранённые D1/D3/D5). **Готово, когда:** сводка §59 создана; AMEND/SUPERSEDE зафиксированы; ссылки на ADR-1025-13 корректны. **Файлы:** `plans/ARCHITECTURE.md`, `plans/features/hotfix7-shell-glass-heartbeat-round1025/spec.md`.
- [x] **T-2689** [@Builder] — **Bump `APP_VERSION`.** `2.58.7 → 2.58.8` + cache-bust `?v=` (в т.ч. vendor-скрипты по возможности), `README.md`. **Готово, когда:** версия обновлена в коде/README/`?v=`; рестарт-требование отмечено. **Файлы:** `config/settings.py` (`APP_VERSION`), `README.md`, `web/index.html`.
- [x] **T-2690** [@DevOps] — **Step 9 Deploy + live-верификация.** Push origin/master; прод `/var/www/admin_bot`; `systemctl restart admin_bot` → active; `/api/health`=200; `database is locked`=0; `APP_VERSION`=2.58.8. Заполнить `deployment.md` (**VERIFIED**) + открыть/закрыть live-гейт T-2682. **Готово, когда:** прод обновлён, health 200, версия подтверждена; результат записан. **Файлы:** `plans/archive/hotfix7-shell-glass-heartbeat-round1025/deployment.md` (пакет уже заархивирован Шагом 8 @PM). — **✅ Выполнено (22.09.2026):** коммиты `7073e34`/`a9cec67`/`2a67829`, прод fast-forward `5a5465c..a9cec67`, `systemctl` active, health 200, `APP_VERSION` 2.58.8, served `?v=2.58.8`, `database is locked`=0; `deployment.md` VERIFIED. ⏳ T-2682 live — за владельцем.

## Блок I — UPD 7: аудит ТЗ и продолжение (без human gate)

- [x] **T-2691** [@PM] — **Аудит остатка `current_task.md`.** Артефакт **`plans/reports/round1025_tz_remaining_audit.md`**: по разделам `current_task.md` — что закрыто (F0, F1, F2, F3, hotfix1–6, §15, §117 и т.д.), что нет (F4 ⏸, F5, F6, F7, F8, F9, F10, F11; Эпик 2 S1–S10; Эпик 3 A0–A10; внеплановые бэклоги), рекомендуемый порядок продолжения по приоритетам. Опираться на Step 0 @Memory и реестр фич. **Готово, когда:** отчёт создан, у каждого пункта статус + ссылка на источник; незакрытые не выданы за закрытые. **Файлы:** `plans/reports/round1025_tz_remaining_audit.md`.
- [x] **T-2692** [@PM] — **Определить следующий приоритет и handoff @Orchestrator.** По итогам аудита: подтвердить ближайшую actionable фичу (базово — возврат к **F4** Step 2 @Architect; скорректировать по приоритетам UPD 7 и открытым live-гейтам) и передать старт без запроса у владельца. **Готово, когда:** назван следующий F с обоснованием; handoff отдан @Orchestrator; `plans/backlog.md` отражает статус (F4 paused / следующий). **Файлы:** `plans/backlog.md`, `plans/reports/round1025_tz_remaining_audit.md` (через @Orchestrator).
- [x] **T-2693** [@PM] — **Итоговый отчёт UPD 7 (3 пункта).** Явно: (1) что исправлено по UI-фиксам HOTFIX7; (2) какие пункты `current_task.md` ещё оставались; (3) что взято следующим в работу. **Готово, когда:** отчёт создан и содержит три обязательных блока без обобщений. **Файлы:** `plans/reports/round1025_tz_remaining_audit.md` (**§9 «Итоговый отчёт UPD-7»** — сводится с аудитом T-2691; отдельный файл не создавался).

## Блок J — Архивация и синк

- [x] **T-2694** [@PM] — **Архивация (Шаг 8) и финальный синк-хендофф.** После верифицированного HOTFIX7 (Reviewer Approved, Scanner C0/H0/**M0**, deploy ⏳ Шаг 9, reconcile @Architect ✅): папка перенесена в `plans/archive/hotfix7-shell-glass-heartbeat-round1025/` (spec.md + ADR-1025-13 + tasks.md + evidence.md + review.md; `deployment.md` допишет @DevOps на Шаге 9), ID и ссылки сохранены, `plans/backlog.md` обновлён (HOTFIX7 закрыт, F4 размораживается после deploy), `plans/MEMORY.md` синхронизирован (@Memory делегировал запись @PM). `metrics.md` — **Шаг 10 @Memory** (PM не трогал). **Файлы:** `plans/archive/hotfix7-shell-glass-heartbeat-round1025/`, `plans/backlog.md`, `plans/MEMORY.md`.
