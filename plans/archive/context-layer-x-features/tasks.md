# Раунд 8 (эпик 06.09.2026): Context-Layer X-Features (24 пункта [х] из CONTEXT_RESEARCH)

Планирование раунда 8 (06.09.2026, продолжает нумерацию T-788 → **T-789…T-814**; база HEAD b198d13; дерево на момент планирования: `M plans/docs/CONTEXT_RESEARCH.md`, `M plans/docs/memory-project-overview.md`, untracked `plans/docs/agi-memory-research.md`). Эпик = реализация 24 отмеченных `[х]` пунктов из research `plans/docs/CONTEXT_RESEARCH.md` (раздел 2, категории «Атрибуция людей» 1–6, «Фокус разговора» 7–12, «Память/история» 13–17, «RAG/поиск» 18–22, «Промпт/структура» 23–26; **НЕ входят**: п.15 «дедуп повторов в окне» и п.19 «порог минимальной релевантности RAG» — `[ ]` в research). Ключевые комментарии владельца: (п.1) uid не должен конфликтовать с именами/никнеймами, приоритеты каскада алиас → никнейм → юзернейм продолжают работать; (п.3) **ВАЖНО** — бот не должен выдавать «хвосты» (технические коды) в чистовых сообщениях: только алиас-никнейм-юзернейм. spec.md при планировании НЕ создаётся (по ТЗ владельца); раздел A настоящего файла зеркалит дизайн-решения (PM-база) и протокол открытых вопросов Q1–Q13, которые @Architect закрывает в T-789 правкой в разделе A.

**RUNTIME WARNING (критично; обязателен @Architect, @Reviewer, @DevOps):** SQLite-схема на проде — user_version **v7**. Локальная машина: graph-этап истории **завершён** (воркер юзера больше не работает), локальная БД с историей (2.2M сообщений) существует и открывается ботом при локальных запусках. **Правило по умолчанию: SQLite-миграции для этих 24 пунктов НЕ нужны** — существующие таблицы/колонки/данные не менять; любые новые таблицы — только идемпотентные `CREATE TABLE IF NOT EXISTS`-добавки в init (как PG-DDL в раунде 7, без bump `PRAGMA user_version`); `PRAGMA user_version = 8` и `ALTER` существующих таблиц — ТОЛЬКО если @Architect явно обоснует необходимость в T-789 (кандидаты: п.10 — parent-линк бот-сообщений в `bot_replies`; п.14 — хранилище уровней конспекта). Запрещено: правки `tools/history_import/`, DDL/rebuild существующих SQLite-таблиц (`database.py`: существующие миграции v2–v7 не трогать), `local_database_*.db*`-файлы (не удалять, не блокировать). Промпт-каноны, меняющие `CHAT_SYSTEM_PROMPT`, требуют: PREV-слепка текущего канона + ступени в `PROMPT_MIGRATIONS` + обновления байт-эталонов (`tests/test_direct_chat_prompts.py`, `tests/test_prompt_migrations.py`) + авто-миграции PG на проде при деплое (пайплайн раунда 5, H2). Изменения порядка блоков user-контента — регресс-тесты порядка/бюджета (`tests/test_direct_chat.py::TestContextPartitioning`, `tests/test_payload_builder.py` и др.).

**Статус:** реализовано, ожидает деплоя (T-813) — заархивировано 06.09.2026 (@PM Archive Phase). Полный pytest **4321 passed / 0 failed**; аппрув @Reviewer; архитектура — `ARCHITECTURE.md` §20; закрыто по ревью: T-789..T-811 [x]. Вне архива (исполняет @DevOps финальным шагом в T-812..T-814): README-раздел «Context-Layer X-Features» (T-812), деплой раунда: коммит (код+тесты+plans/backlog.md+архив plans/archive/context-layer-x-features/) → пуш → сервер git pull --ff-only, бэкап SQLite-БД, авто-миграция канона `prompts.direct_chat_system_prompt` (migrate_prompt_canons) + идемпотентные CREATE IF NOT EXISTS, рестарт, live-верификация (T-813), отчёт юзеру и закрытие (T-814).

## Контекст (Step 0; факты проверены @PM при планировании по HEAD b198d13)

**Сборка контекста direct_chat** (`services/direct_chat_service.py`). `_build_user_content` (:408-456) собирает блоки в порядке `[map, rag, target, protected, (lore), mood, global, thread, anchors]`; `payload_builder.build_messages` (`services/payload_builder.py:12-22`) склеивает user-блоки через `"\n\n"` в один user-блок после system@0. RAG-вызов (:420-422): `get_rag_context(..., sort_by_timestamp=True, include_direct_reply=True)`. `Target_User` (:425) — только имя (`escape_xml_text(target_name)`), без uid. `<Current_Question>` блока НЕТ. `_build_protected_facts` (:640-659) читает `db.get_protected_facts(chat_id, target_name)` — фильтр по канон-имени (не uid). `_build_alias_map` (:886-900) — `UserResolutionMap` по участникам ОКНА (`window`), формат строк `"{name} — {uid}"`, резолв каскадом `aliases.resolve(uid, author_name, None)`. `_build_global_context` (:902-960): валидный бегущий конспект (`db.get_running_summary`) → строки `[summary + «имя: текст» хвоста после window_end_ts]` (:920-931), иначе последние N строк окна (:933-945), рендер `"{name}: {text}"` (:928-930/:940-942 — БЕЗ uid); обрезка `truncate_to_tokens(body)` (:951-956) или `body[:limit]` (chars, :957-959). `_build_conversation_thread` (:962-1008): walk от `message.message_id` по `reply_to_id` через `db.get_smart_message_by_tg_id`; на бот-сообщении (`db.get_bot_reply`, :985-988) — текст добавляется и **break** (:988); рендер `"{name}: {text}"` (:992, БЕЗ uid), потолок thread. `_apply_context_budget` (:460-532): per-block доли от `limits.chat_context_budget_tokens` (default `settings.CHAT_CONTEXT_BUDGET_TOKENS`, 4000) + при превышении общего — порядок урезания `("anchors","global","thread","rag","map")` (:516), шаги `//2`, `_truncate_block` (:534-550) режет тело через `truncate_to_tokens` — тот сохраняет **КОНЕЦ** (`tokens[-max:]`, `services/token_counter.py:62-86`), т.е. в Global_Context под давлением первым выпадает КОНСПЕКТ (начало блока), остаётся свежий verbatim-хвост — слабое место research §1 п.5 = целевой п.9. `target`/`protected` бюджетом не режутся. memorize-хук: `handle` (:230+) → успешный ответ (:358-368) → `fire_and_forget(self.memory.memorize_facts(chat_id, f"{query}\n{answer}", "bot_direct_reply", target_user=target_name))`; `target_name = self._resolve_name(user)` (:236) — канон-имя автора запроса. `remember_bot_reply(chat_id, sent_id, answer)` (:173-178) → `db.upsert_bot_reply`; вызовы: :311 (dedup-replay) и :361 (основной). PG-лор чата (раунд 7): `_chat_lore_state` (:663-713) + блок `<chat_lore>` после `<protected_facts>` (:437-438) — при любых реордерах порядка блоков/бюджета не сломать (флаг `flags.lore_inject_enabled`, fail-open).

**Промпты/миграции.** `services/chat_prompts.py`: `LEGACY_CHAT_SYSTEM_PROMPT` (:18-28, исторический), `PREV_CHAT_SYSTEM_PROMPT` (:32-48, слепок канона раунда 4 — байт-эталон, НЕ трогать), `CHAT_SYSTEM_PROMPT` (:50-66, канон раунда 5: роль/стиль «торопливое письмо», типографика без «»/—, ИНСТРУМЕНТЫ, ограничение 1-2 предложения) — БЕЗ правил приоритета и «как читать блоки». `services/prompt_migrations.py`: `PROMPT_MIGRATIONS` (:52-69), direct_chat — 2 ступени (:53-55: `(LEGACY→CHAT)`, `(PREV→CHAT)`); `migrate_prompt_canons(cache)` (:73-107) — auto-миграция PG `prompts.direct_chat_system_prompt`; новый канон → новый слепок (имя по образцу `PREV_*`) + ступень в списке. Байт-эталоны: `tests/test_direct_chat_prompts.py` (`_CHAT_SYSTEM_PROMPT_REFERENCE` :72+, `test_byte_for_byte` и пр.); миграционные: `tests/test_prompt_migrations.py` (:107-198 проверяют ступени direct_chat). Канон в рантайме: `hot.get("prompts.direct_chat_system_prompt", CHAT_SYSTEM_PROMPT)` (direct_chat_service.py:319-321). REGISTRY/категории: `services/param_catalog.py` (флаги/лимиты-записи с полями settings-дефолтов, пример раунда 7: `LORE_INJECT_ENABLED` :525, `LORE_INJECT_MAX_CHARS` :807); PG-ключи prompts.* и сиды канонов — ConfigCache. Новые флаги/лимиты → запись REGISTRY + дефолт-поле в `config/settings.py` (образцы раунда 7: `flags.lore_inject_enabled`, `limits.lore_inject_max_chars`).

**Память/RAG** (`services/summary_memory.py`). `get_window_messages` (:1039-1073): окно L1 (SUMMARY_WINDOW_HOURS, SUMMARY_MAX_WINDOW_MESSAGES); триггер бегущего конспекта (:1057-1067): `len(rows) >= fill_threshold (chat_context_fill_ratio) И (current is None ИЛИ current.window_end_ts < last_ts)` → fire-and-forget `_build_running_summary` (:1075-1104: head → COMPRESS_PROMPT + verbatim tail → UPSERT в `chat_running_summary` с `expires_at = now + limits.running_summary_ttl_minutes`). `db.get_running_summary` (`services/database.py:1820-1837`): просроченный (expires_at <= now) — **ленивый DELETE и None** → в тихом чате старый конспект не попадает в Global_Context до нового переполнения окна (связь с п.17). `chat_running_summary` (:411): PK chat_id, одна строка на чат — уровней не вмещает (п.14). `db.upsert_running_summary` (:1839-1858). RAG: `get_rag_context` (:1566-1599): `sort_by_timestamp=True` — стабильная сортировка по `created_at/rag_ts` ASC (ТОЛЬКО direct_chat; слабое место §1 п.7 = п.18); рендер `build_rag_context` (:509-534): КАНОН-XML `<context><user_gossip>/<bot_knowledge>` с дата-префиксом `[%Y-%m-%d]` для всех фактов (T-724) и origin-префиксами `_RAG_PREFIXES` (:214+) для части origins; FTS-путь `_search_graph_facts` (:1601-1647) — реранк по `_effective_weight` (w_eff) DESC; KNN-путь `_knn_graph_facts` (:1649+): `rel = cosine × w_eff` + MMR; дедуп RAG↔конспект отсутствует (п.20); LLM-реранка нет (п.22; паттерн — `search_service._rerank_results` (:103-125) с собственным промптом). memorize: `memorize_facts` (:1239-1402) — FACT_EXTRACT → subject/object через `_canon_fact_name` (:1404-1409, канон-алиас, реализовано в 66.9/T-487), `target_user` = канон (66.9), дедуп 64.1, квоты 66.4. compress/purge: `compress_and_purge` (:1912-1970) — L3-сжатие smart_messages (+ extract при graph on) + `_purge_archive` (:2126+) + `db.purge_expired_graph_facts` (граф по expires_at); protected_facts — ОТДЕЛЬНАЯ таблица (`database.py:420`, `get_protected_facts` :1584-1603), в кроне-сжатии не участвует (п.16: проверить ВСЕ purge-пути графа, «высокий w_eff» vs TTL-удаление).

**Thread/бот-сообщения.** `smart_messages` имеет `reply_to_id` (:126), бот-сообщения в smart_messages НЕ пишутся (observer 0a, `handlers/summary.py`; тест `test_observer_does_not_save_bot_messages`). `bot_replies` (:398): только `(chat_id, tg_message_id, text, last_used_at)` — **parent-линка нет** → `_build_conversation_thread` не может продолжить цепочку через бот-ответ (п.10). `db.get_bot_reply` (:1526-1545) TTL 3600с.

**Тесты (затрагиваемые).** `tests/test_direct_chat.py` (TestContextPartitioning :369-461 — порядок секций :373-392, xml-escape, thread-тесты :429-461, бюджет/урезание), `tests/test_direct_chat_prompts.py` (байт-эталоны), `tests/test_prompt_migrations.py`, `tests/test_payload_builder.py`, `tests/test_direct_chat_lore_inject.py`, `tests/test_graphrag_memory.py` (RAG-сортировка/рендер), `tests/test_summary_memory.py`, `tests/test_summary_handlers.py`, `tests/test_summary_xml.py` (экранирование/рендер), `tests/test_summary_aliases.py` (каскад резолва), `tests/test_database.py` (schema/migrations). Полный pytest → 0 failed (правило раундов); `git diff --check`; R17 (секреты в логи/коммиты не попадают).

---

## A. Spec (зеркало дизайна; spec.md отдельным документом НЕ создаётся — ТЗ владельца)

**PM-база дизайна (по умолчанию для B–H; открытые вопросы Q1–Q13 закрывает @Architect в T-789 и фиксирует ПРАВКОЙ в этом разделе — строки-решения; если правки меняют требования задач — пометкой `(изменено @Architect T-789)` в тексте задачи):**

- **Сквозной принцип атрибуции (п.1/п.3/п.5):** uid — служебный дискриминатор ВНУТРИ контекста (`имя [uid]`), display-имя всегда остаётся результатом каскада алиас → никнейм → юзернейм (резолв-функции не меняются). Технические id/суффиксы НЕ должны попадать в чистовые сообщения бота (комментарий владельца п.3) и в user-facing рендеры (persona-карточка, protected_facts, сообщения каналов, RAG-факты как есть?) — правило + промпт-строка (B1) «служебные идентификаторы в квадратных скобках не выводи и не цитируй», тесты на отсутствие «хвостов» в чистых путях.
- **Единый рендер имён:** `<UserResolutionMap>` — `{канон} — {uid}` (как сейчас, :899); строки Global_Context/Conversation_Thread — `{канон} [{uid}]: {текст}`; `<Target_User>` — `{канон} [{uid}]`. XML-эскейп сохраняется. Защита от «выпадения» summary и формат меток свежести (п.9/п.12) — ниже.
- **Порядок блоков user-контента (п.24; PM-база, точную схему фиксирует @Architect в A1):** статика вверх, самое важное к концу: `[map, rag, global, thread, target, protected, (lore после protected), mood?, current_question, anchors?, sandwich-строка]`. Точное размещение Target_User/Protected_Facts/Current_Question «после map/RAG, ближе к Thread/финалу», судьба mood/anchors при реордере — Q1. Порядок урезания в `_apply_context_budget` приводится в соответствие новой важности (никогда не резать target/protected/current_question; конспект в global — только после verbatim-хвоста).
- **UserResolutionMap по активным участникам (п.2):** источник — SQL-агрегат по `smart_messages` за расширенный период (кандидат: `SELECT DISTINCT user_id, MAX(author_name) ... WHERE chat_id=? AND timestamp >= ?` за `limits.chat_map_participants_hours` (PM-база 24–48 ч) + слияние с участниками текущего окна + cap размера карты (PM-база 150); `message_counters`/`user_presence` — альтернатива/дополнение (Q2). Карта продолжает участвовать в map-бюджете.
- **Коллизии имён (п.3):** дискриминатор применяется на ЭТАПЕ РЕНДЕРА (map/global/thread): если два разных uid резолвятся в одинаковое display-имя — второй и последующие получают стабильный суффикс из username или хвоста uid (PM-база: хвост uid в той же скобке: `Саша [345]`), в map строка `{дискриминированное имя} — {uid}`; канон-имена/факты/protected-рендеры/чистовые пути НЕ получают суффиксов (Q3).
- **Порядок урезания конспект-vs-verbatim (п.9):** в `_build_global_context` и `_apply_context_budget` verbatim-хвост режется РАНЬШЕ конспекта: сначала урезание хвоста (с начала блока), конспект — последним и лишь до «конспект-минимума»; chars-ветка (:957-959) приводится к той же семантике; требуется отдельный хелпер «урезать, сохранив голову» рядом с `truncate_to_tokens`.
- **Thread не рвётся на бот-сообщениях (п.10):** цепочка продолжается через бот-ответы (бот-строки помечаются как бот), терминал — только реальный обрыв (`no reply_to_id`/не найдено/глубина/TTL); требует parent-линка бот-реплики → вопрос Q7 (новый линк-столбец/таблица без миграции vs v8).
- **Итог ветки над фоном (п.11, без LLM):** для reply-триггера — компактный блок первых 2–3 последних ходов текущей ветки («кто → что») в верхней части контента (после map, до global), источник — уже собранная цепочка thread; полный Thread остаётся ниже; LLM-резюме — вне скоупа по умолчанию (Q8).
- **Важность при переполнении (п.13):** эвристика без LLM: при урезании verbatim-материала удерживать строки с маркерами (имя участника из карты, «бот», кавычки/цитата, число, вопрос-знак), резать «сырые» строки (AFM/STAE-логика); список маркеров и точное правило — Q в A1.
- **Progressive summarization (п.14):** уровни: новый «широкий» слой поверх уже существующих конспектов; хранилище уровней — новая таблица `CREATE TABLE IF NOT EXISTS` в init SQLite (без bump user_version) либо переиспользование существующих структур — Q10; фоновый шаг на базе существующего fire-and-forget; инжект в Global_Context строкой-уровнем сверху (cap).
- **Compress/purge (п.16):** protected-факты (таблица protected_facts) и факты с высоким w_eff/недавним подтверждением НЕ удаляются purge-путями графа (только устаревшие/слабые); определить критерий «высокий» (Q12); покрыть все purge-точки.
- **Пересборка конспекта по заполнению (п.17):** триггер уже пересобирает по заполнению окна (не по TTL); дельта — семантика expires_at при ЧТЕНИИ: старый валидный конспект не должен «умирать» в тихом чате до нового переполнения (Q11).
- **RAG по релевантности (п.18):** direct_chat вызывает `get_rag_context(..., sort_by_timestamp=False)` (уже ранжировано: KNN rel = cosine×w_eff + MMR, FTS-fallback — w_eff DESC); даты внутри фактов сохраняются. Параметр `sort_by_timestamp` для прочих пайплайнов сохраняется (дефолт False).
- **Дедуп RAG ↔ Global_Context/конспект (п.20):** на сборке direct_chat RAG-факты, нормализованный текст которых пересекается со строками уже включённого конспекта/verbatim-хвоста, исключаются (дешёвый текстовый дедуп; embedding-вариант — по решению @Architect); порядок построения блоков/двухпроходность — Q.
- **Метки origin+давность в RAG (п.21):** у фактов в RAG-блоке direct_chat — origin + давность; дата-префикс уже есть (`[%Y-%m-%d]`, T-724); легаси-рендер `build_rag_context` (канон-структура `<context>`) другими пайплайнами/тестами — не ломать: метки только в direct-рендер или параметром (Q9).
- **Опциональный LLM-реранк (п.22):** флаг `flags.chat_rag_rerank_enabled` (default **false**), канон-промпт rerank (по образцу search_service._rerank_results), прогон top-k фактов перед вставкой в direct_chat, fail-open на исходный список; новые ключи REGISTRY + дефолты (Q13).
- **Канон-дисциплина:** все промпт-правки — с PREV-слепками и ступенями миграций; LEGACY/PREV-слепки прошлых раундов не трогать (байт-эталоны); тон/стиль канона не менять; запрет «»/— сохраняется.
- **Новые конфиг-ключи** (флаги/лимиты, кандидаты): `flags.chat_rag_rerank_enabled`, `limits.chat_map_participants_hours`, `limits.chat_map_participants_cap`, `flags.chat_importance_keep_enabled`, ключи прогрессивного конспекта и др. — точный набор фиксирует @Architect в A1 (REGISTRY + config/settings.py + категории limits/flags, новые категории не заводить; единый hot.get-паттерн с фолбэком settings).
- **Дисциплина исполнения:** тесты по каждому пункту (минимум 1) — в задаче-носителе и сводно в G1 (матрица); полный pytest → 0 failed; `git diff --check` чист; секреты не логировать; порядок исполнения B → C → D → E → F → G (по дереву зависимостей: B1 (промпт) раньше D2 (рендер-форматы ссылаются на правила промпта); реордер (B2) раньше добавления Current_Question (D1) и меток (D5); C1/C2/C3 — общий рендер-слой имён — раньше C5/C6; D2 (урезание) раньше E1 (importance на том же механизме).

- [x] T-789 (A1): @Architect — единый дизайн 24 пунктов в разделе A: для КАЖДОГО пункта — что меняется (файл/функция/формат), порядок реализации и риски; закрыть Q1–Q13 строками-решениями (заменой PM-базы или подтверждением); проверить полноту списков точек кода (рендеры имён: alias-map/global/thread/target; точки урезания: `_truncate_block`, `truncate_to_tokens`, chars-ветка, порядок урезания; purge-точки: `compress_and_purge`/`_purge_archive`/`purge_expired_graph_facts`; вызовы `get_rag_context` по всем пайплайнам; вызовы `remember_bot_reply`; потребители `build_rag_context` и байт-эталонов; REGISTRY-ключи; известные тесты-эталоны) и зафиксировать итоговые имена новых конфиг-ключей и констант-слепков. Решение «миграция v7→v8» для п.10/п.14 — строго здесь (по умолчанию НЕ делать; только новые CREATE IF NOT EXISTS). (plans/features/context-layer-x-features/tasks.md)

## B. Промпт-канон и структура user-блока (п.8, 23, 24, 25, 26)

- [x] T-790 (B1): новый канон `CHAT_SYSTEM_PROMPT` (`services/chat_prompts.py:50-66`): (1) п.23 — первым абзацем правило «как читать блоки» (контекст разбит на теги; `Conversation_Thread` — история твоего диалога, `Global_Context` — фон, `Current_Question` — на что отвечать; теги не цитировать дословно, только использовать; служебные идентификаторы в `[...]` не выводить и не упоминать — связь с п.3/п.1); (2) п.8 — правила приоритета: Thread важнее Global_Context, при конфликте верь более свежему, отвечай только на текущий вопрос, не путай людей из `<UserResolutionMap>`; (3) п.26 — фиксация стабильного префикса для prompt-cache: динамика (Target_User, даты, RAG, mood) строго в user-блок, в system — только статика (зафиксировать докстрингом/структурным комментарием модуля + структурой текста); тон/роль/ИНСТРУМЕНТЫ/ограничение 1-2 предложения/дисциплина «»/— сохраняются без изменений. (4) Слепок: текущий канон раунда 5 до правки → новая константа-слепок (имя — по образцу `PREV_*`, НЕ переименовывая LEGACY/PREV — они байт-эталоны прошлых раундов). (5) `services/prompt_migrations.py`: ступень `(слепок раунда 5, новый канон)` в список `PROMPT_MIGRATIONS["prompts.direct_chat_system_prompt"]` (:53-55) — существующие две ступени сохраняются. (6) Байт-тесты: `tests/test_direct_chat_prompts.py` — новый `_CHAT_SYSTEM_PROMPT_REFERENCE` + тесты новых абзацев (правила чтения блоков/приоритета/служебных id), слепок раунда 5 == текущий канон ДО правки (свежий срез из git), существующие тесты LEGACY/PREV не тронуты; `tests/test_prompt_migrations.py` — новая ступень (значение == слепок раунда 5 → updated; == новый канон → no-op; кастом → не трогаем). Текст канона пишет @Architect. (services/chat_prompts.py, services/prompt_migrations.py, tests/test_direct_chat_prompts.py, tests/test_prompt_migrations.py)
- [x] T-791 (B2): структура user-блока (п.24/п.25) в `_build_user_content` (direct_chat_service.py:408-456): (1) реордер блоков по схеме A1 — важное к концу: Target_User/Protected_Facts (и Current_Question после D1) ПОСЛЕ map/RAG, ближе к Thread/финалу; синхронно поправить `_apply_context_budget` (:460-532): порядок урезания (:516) и per-block доли — в соответствие новой важности (target/protected/current_question не режутся; q-лимиты); (2) п.25 — sandwich-строка-напоминание («коротко, по делу, отвечай на последний вопрос, не путай людей») в КОНЕЦ user-контента (отдельным последним блоком-строкой, в список blocks ДО `_apply_context_budget` с семантикой «не режется» или после сборки — по решению A1); (3) при реордере не сломать PG-лор (`<chat_lore>` после protected :437-438), mood и `_chat_lore_state`-флоу; (4) тесты: `tests/test_direct_chat.py::TestContextPartitioning` (:369-461) — новый ожидаемый порядок блоков, sandwich-строка последней, xml-escape; регресс-тесты бюджета (урезание не трогает target/protected/current/sandwich); `tests/test_direct_chat_lore_inject.py` и `tests/test_payload_builder.py` — актуализировать. (services/direct_chat_service.py, tests/test_direct_chat.py, tests/test_direct_chat_lore_inject.py, tests/test_payload_builder.py)

## C. Атрибуция людей (п.1–6)

- [x] T-792 (C1): uid рядом с именем в рендерах Global_Context/Thread (п.1): (1) `_build_global_context` (:928-930, :940-942) и `_build_conversation_thread` (:980-992) — рендер строк `"{имя} [{uid}]: {текст}"` по схеме A1 (uid из `row["user_id"]`, бот-строки — бот-имя с bot uid или без — Q в A1); map уже содержит `"{имя} — {uid}"` (:899) — формат унифицировать/оставить по решению A1; (2) каскад резолва `aliases.resolve` НЕ меняется (комментарий владельца п.1: алиас → никнейм → юзернейм продолжает работать; uid — только добавка для атрибуции); (3) uid не должен конфликтовать: проверка, что uid-строка не дублирует display-имя (напр., `[123]` vs никнейм-число — допустимо, дискриминатор контекстный; ок); (4) тесты: рендер-эталоны (строки с `[uid]`), VERBATIM-обновления в test_direct_chat, тест «каскад имён не изменён» (test_summary_aliases). (services/direct_chat_service.py, tests/test_direct_chat.py, tests/test_summary_aliases.py)
- [x] T-793 (C2): UserResolutionMap по активным участникам (п.2): (1) новый метод db-слоя (кандидат: `get_active_participants(chat_id, since_ts, cap)` — `SELECT DISTINCT user_id, MAX(author_name) FROM smart_messages WHERE chat_id=? AND timestamp >= ? GROUP BY user_id LIMIT ?`; альтернативы/дополнения `message_counters`/`user_presence` — Q2) в `services/database.py`; (2) `_build_alias_map` (direct_chat_service.py:886-900) — источник = объединение участников ОКНА + активных участников за период (limits A1: `limits.chat_map_participants_hours`, cap); участники без свежих сообщений остаются в карте, их строки в конспекте/RAG остаются привязаны к uid; (3) map-бюджет/пер-блок потолок сохраняются (map урезается последним в общем порядке — но cap-разумный, решить: резать с конца списка (менее активных)); fail-open → текущее поведение; (4) тесты: участник вне окна (в периоде) присутствует в карте; кап работает; период/границы; пустой период → окно (регресс). (services/database.py, services/direct_chat_service.py, тесты — в G1-файлах по конвенции)
- [x] T-794 (C3): дискриминатор коллизий имён (п.3): (1) детект коллизий display-имён среди участников (map и рендеры C1): два разных uid → одно имя → ВТОРОЙ и последующие получают стабильный суффикс (схема A1/Q3: хвост uid в `[...]`); (2) суффиксы — ТОЛЬКО во внутренних рендерах контекста (map/global/thread/target); **комментарий владельца (важно): в чистовых сообщениях бота «хвостов» нет** — суффиксы НЕ попадают в: канон-имена/факты (`_canon_fact_name`), persona-карточку (build_persona_card :837-874), protected-рендер, RAG-факты как есть, `_resolve_name`; дискриминация применяется на этапе рендера, резолв-функции не меняются; (3) промпт-строка «не выводить технические id» — в B1 (уже); (4) тесты: два «Саши» → в контексте два различимых рендера; identity-инвариант: канон/resolve/protected/persona не содержат суффиксов; бот-ответный путь (payload-сборка user-контента) не содержит «хвостов» сверх контекстных строк. (services/direct_chat_service.py, тесты test_direct_chat/test_summary_aliases — по конвенции)
- [x] T-795 (C4): привязка фактов к людям по uid/канон-алиасу (п.4): (1) верификация существующего (66.9/T-487 + user-aliases-admin): `_canon_fact_name` (:1404-1409) приводит subject/object фактов к канон-алиасу; `memorize_facts`/`_memorize_facts_inner` (:1291+) сохраняет `target_user` канон-именем; (2) остаточная дельта по A1/Q4: поведение при смене nickname БЕЗ алиаса (старые факты на старом каноне) — зафиксировать/улучшить по решению @Architect (кандидаты: стабильная привязка «uid → канон» при записи через существующие структуры; НЕ мигрируя существующие данные); (3) пересборка привязки при смене nickname — только если A1 решил (без ALTER); (4) тесты: факт привязывается к канон-алиасу, а не display-имени; смена ника с алиасом — факт на месте (карточка/контекст); смена ника без алиаса — фиксация осознанного поведения. (services/summary_memory.py, services/summary_aliases.py, тесты test_graphrag_memory/test_summary_aliases)
- [x] T-796 (C5): Target_User с uid + фильтры по людям (п.5): (1) рендер `<Target_User>{канон} [{uid}]</Target_User>` (:425; uid автора запроса); (2) protected/RAG-выборки по людям: фильтр остаётся по канон-имени (protected_facts хранят `user_name`, резолв uid → канон — существующий `_resolve_name`-каскад; вариант по A1/Q5); «два Саши» — выборка protected корректна при наличии алиасов (уже), при полной коллизии канонов — поведение по Q3/Q5 (дизамбигуация на рендере + признание на выборке); (3) все места, где `target_name` (канон) рендерится/передаётся для людей (memorize-хук :364-368, persona, protected-сборка :433-434) — единый helper «канон+uid» по A1; (4) тесты: блок содержит uid; protected-факты не смешиваются между «Сашами» с алиасами; регресс persona/lore-inject. (services/direct_chat_service.py, tests/test_direct_chat.py, tests/test_direct_chat_lore_inject.py)
- [x] T-797 (C6): «кто спрашивает vs кому адресован» + memorize-хук (п.6): (1) в Thread-рендере пометить границы «вопрос → ответ» (текущая реплика автора запроса и ответ бота выделены/соседствуют; схема разметки — A1/Q6; минимально: текущее сообщение-вопрос первым ходом ветки); (2) memorize-хук (direct_chat_service.py:364-368): факты бот-диалога атрибутируются автору запроса (target_user = канон автора — уже так); дельта п.6: факты, извлечённые из реплики ПРО ТРЕТЬЕГО лица (subject/object — другой известный участник чата), не должны ложиться в карточку автора запроса как «его» факты (пост-обработка subject'ов/семантика target_user — решение A1; минимально-инвазивно, без изменения формата graph_facts); (3) тесты: memorize диалога «кто спрашивал про Васю» — карточка Васи/спрашивающего не смешиваются; Thread-разметка в контексте. (services/direct_chat_service.py, services/summary_memory.py, тесты test_graphrag_memory/test_direct_chat)

## D. Фокус разговора / удержание нити (п.7, 9, 10, 11, 12)

- [x] T-798 (D1): блок `<Current_Question>` (п.7): (1) в `_build_user_content` — отдельный блок с текстом ТЕКУЩЕГО сообщения (query после вырезания триггер-слова «бот»; источник — сообщение/query хендлера, escape_xml), позиция — по схеме A1/B2 (после map/RAG, у конца, перед/после Thread по решению); (2) блок НЕ режется бюджетом (короткий; семантика как у target); (3) заглушка при пустом тексте — блок не рендерится; (4) тесты: блок присутствует с текстом вопроса; отсутствует при пустом; позиция в порядке блоков (TestContextPartitioning); xml-escape. (services/direct_chat_service.py, tests/test_direct_chat.py)
- [x] T-799 (D2): защита конспекта от обрезки (п.9): (1) `_build_global_context` (:902-960): урезание verbatim-хвоста РАНЬШЕ конспекта (поменять обрезку: сначала хвост-строки после window_end_ts, конспект последним до минимума; chars-ветка :957-959 — та же семантика); (2) `_apply_context_budget` (:460-532)/`_truncate_block` (:534-550): для global-блока обрезка «держать голову» (конспект), а не хвост — новый хелпер в token_counter (типа `truncate_to_tokens_keep_head`) или параметр; порядок урезания (:516) — конспект не выпадает первым (учитывая B2-реордер); (3) WARNING-логи при обрезке сохраняются; (4) тесты: при бюджетном давлении конспект остаётся, verbatim-хвост режется первым; без конспекта — режется хвост окна; регресс существующих truncation-тестов. (services/direct_chat_service.py, services/token_counter.py, tests/test_direct_chat.py)
- [x] T-800 (D3): Conversation_Thread сквозь бот-сообщения (п.10): (1) parent-линк бот-реплик по решению Q7/A1 (PM-база: без миграции v8 — новая таблица `CREATE TABLE IF NOT EXISTS bot_reply_parents (chat_id, tg_message_id, parent_tg_message_id, last_used_at)` в init SQLite + запись в `remember_bot_reply` (direct_chat_service.py:173-178) из точек :311/:361 (родитель = message.message_id) + чтение в walk; ИЛИ v8-колонка, если обосновано в A1); (2) `_build_conversation_thread` (:962-1008): на бот-сообщении — текст добавляется (помечается как бот: рендер C1 `{бот-имя} [{uid?}]: текст` + маркер роли по A1) и цепочка ПРОДОЛЖАЕТСЯ по parent-линку; break — только на реальном обрыве (нет reply_to/parent не найден/TTL/глубина); (3) TTL/LRU-свепы новой структуры — как у bot_replies (3600с/cap); (4) тесты: юзер→бот→юзер→бот — цепочка полная; терминальный обрыв без parent; глубина cap; регресс test_thread_break_on_missing/test_thread_depth_capped и test_observer_does_not_save_bot_messages. (services/database.py, services/direct_chat_service.py, tests/test_direct_chat.py, tests/test_database.py)
- [x] T-801 (D4): итог ветки над фоном (п.11): (1) без LLM: для reply-триггера с глубиной ветки ≥ 2 — компактный блок из 2–3 ПОСЛЕДНИХ ходов текущей ветки («кто спросил → что ответил», рендер C1 с uid), размещение — в верхней части контента (после map, до Global_Context) по схеме A1/Q8; полный Thread остаётся ниже; (2) источник строк — цепочка из `_build_conversation_thread` (переиспользовать сборку, не дублировать walk) или отдельный помощник; имя/формат блока — A1/Q8 (PM-база: `<branch_context>`/первые строки thread); (3) только reply-ветки, для обычных сообщений блока нет (не раздувать); бюджет: малый фикс-кап; (4) тесты: reply-триггер — блок есть с последними ходами; без reply/мелкая ветка — нет; порядок в контенте. (services/direct_chat_service.py, tests/test_direct_chat.py)
- [x] T-802 (D5): метки свежести блоков (п.12): (1) Global_Context: заголовочная строка-метка периода («конспект за последние N минут», окно из `window_start_ts/window_end_ts`/`raw_count` записи running_summary; verbatim-режим — «последние N сообщений»); (2) RAG: дата-префикс уже есть (T-724) — дельта по A1/Q9 (относительная давность/полнота только в direct-рендере, легаси `<context>`-канон не ломать); (3) Current_Question/Thread — без меток (свежие по определению; если A1 решит — временные метки ходов Thread); (4) байт-эталоны/рендер-тесты обновить (test_direct_chat, test_graphrag_memory/test_summary_xml — в зависимости от решения Q9). (services/direct_chat_service.py, services/summary_memory.py, tests по конвенции)

## E. Память / история (п.13, 14, 16, 17)

- [x] T-803 (E1): importance-удержание verbatim при переполнении (п.13): (1) эвристика без LLM (AFM/STAE): при урезании verbatim-материала (механизм D2 + хвост Global_Context) удерживать строки с маркерами важности: содержит имя участника из UserResolutionMap / «бот» / кавычки-цитату / число / вопросительный знак; «сырые» строки (реакции, шум, короткие) режутся первыми; точный список маркеров и пороги — A1 (флаг `flags.chat_importance_keep_enabled` default по решению); (2) интеграция: урезание строк окна/хвоста идёт отбором (важные держатся), а не чистым срезом; (3) тесты: при превышении бюджета важные строки остаются в контексте, шумные выпадают; маркер-матрица (имя/«бот»/число/цитата); флаг off → старое поведение (срез). (services/direct_chat_service.py, services/token_counter.py при необходимости, тесты test_direct_chat)
- [x] T-804 (E2): progressive summarization — summary-of-summary (п.14): (1) хранилище уровней по Q10/A1 (PM-база: новая таблица `CREATE TABLE IF NOT EXISTS chat_summary_levels (chat_id, level, summary, window_start_ts, window_end_ts, raw_count, created_at)` в init database.py — без bump user_version; альтернативы — по решению @Architect); (2) фоновый шаг на базе существующего fire-and-forget (:1064-1067): при пересборке конспекта, если предыдущий конспект покрывал значимое окно (≥ порога сообщений/времени по A1) — он сжимается в «широкий» уровень (summary-of-summary, уровень +1) новым канон-промптом (Q10: отдельная константа в `summary_prompts.py` vs переиспользование COMPRESS_PROMPT; user-facing-канон → PREV/миграции/REGISTRY-дисциплина по решению A1); (3) инжект: строка «широкого» уровня в Global_Context сверху (до свежего конспекта, cap по A1); (4) триггер — от заполнения (см. E4), TTL-семантика уровня — по A1; (5) тесты: уровни пишутся/перезаписываются корректно; при недостижении порога уровня нет; инжект-формат; fail-open (LLM-ошибка → без записи, WARNING); регресс running_summary-тестов. (services/summary_memory.py, services/summary_prompts.py, services/database.py, тесты test_summary_memory/test_summary_handlers)
- [x] T-805 (E3): compress/purge: не трогать protected и высоковесные факты (п.16): (1) аудит всех purge/eviction-путей: `compress_and_purge` (:1912-1970), `_purge_archive` (:2126+), `db.purge_expired_graph_facts`, квотные вытеснения (66.4 `_enforce_user_quota`), graph-fact purge по TTL; (2) защита: protected_facts-таблица кроном не затрагивается (подтвердить тестами); граф: факты с высоким `w_eff`/весом (критерий Q12/A1: вес ≥ порога ИЛИ недавнее `last_confirmed_at` ИЛИ origin protected-семантики) не удаляются TTL/purge-путями — только устаревшие/слабые; (3) поведение «запомни» (origin user_memory, expires_at NULL — вечное) сохраняется; (4) тесты: purge не удаляет protected/high-факты; слабые/старые удаляются; критерий-граница; регресс L3-сжатия. (services/summary_memory.py, services/database.py, тесты test_summary_memory/test_graphrag_database)
- [x] T-806 (E4): пересборка конспекта по заполнению, не по TTL (п.17): (1) триггер (:1057-1067) уже пересобирает по заполнению окна (`fill_threshold` + покрытие последнего сообщения) — зафиксировать тестом; (2) дельта — чтение/TTL: `db.get_running_summary` (:1820-1837) лениво удаляет просроченный конспект → в тихом чате конспект пропадает из Global_Context до нового переполнения; политика по Q11/A1 (PM-база: expires_at не «убивает» конспект при чтении, пока окно не переполнено; пересборка — только по заполнению/новым сообщениям; удаление при реальном устаревании — по решению), реализовать через существующие структуры без миграции (семантика expires_at/чтение); (3) тесты: тихий чат — конспект остаётся в контексте после TTL (новое поведение по решению A1); бурный чат — пересборка по заполнению; регресс TTL-тестов (test_summary_memory/test_direct_chat). (services/summary_memory.py, services/database.py, tests по конвенции)

## F. RAG / поиск (п.18, 20, 21, 22)

- [x] T-807 (F1): RAG-факты по релевантности, не по хронологии (п.18): (1) direct_chat-вызов `get_rag_context` (direct_chat_service.py:420-422): `sort_by_timestamp=False` (порядок уже ранжирован: KNN rel=cosine×w_eff+MMR :1649+, FTS-fallback w_eff DESC :1631-1635); хронология остаётся внутри фактов (дата-префиксы); (2) параметр `sort_by_timestamp` в `get_rag_context` (:1566-1599) сохраняется (другие пайплайны/тесты), дефолт False; (3) тесты: direct-контекст содержит факты в релевантном порядке (KNN-мок с разными rel; FTS-fallback — по w_eff), не по created_at; sort_by_timestamp=True-путь (не-direct) не изменён; даты внутри строк сохранены. (services/summary_memory.py, services/direct_chat_service.py, tests/test_graphrag_memory.py, tests/test_direct_chat.py)
- [x] T-808 (F2): дедуп RAG ↔ Global_Context/конспект (п.20): (1) на сборке direct_chat: RAG-факты, чей нормализованный текст пересекается со строками уже включённого конспекта/verbatim-хвоста Global_Context (порог/метод — A1/Q; PM-база: нормализация + пересечение N-грамм/длинная общая подстрока; embedding-вариант — опция), исключаются из `<RAG_Memory>`; (2) двухпроходность сборки или фильтр с знанием global-строк — по A1 (порядок построения блоков :445-450 rag раньше global — при необходимости переупорядочить ПОСТРОЕНИЕ, не контент-порядок B2); (3) fail-open: ошибка дедупа → факты остаются; (4) тесты: факт, дублирующий строку конспекта/хвоста, в RAG-блок не попадает; не-дубли остаются; точность порога (близкие, но разные — остаются); дедуп не ломает sort/метки. (services/direct_chat_service.py, services/summary_memory.py, тесты по конвенции)
- [x] T-809 (F3): метки origin+давность в RAG (п.21): (1) в RAG-рендере direct_chat у фактов — явные origin + давность (дата-префикс уже есть `[%Y-%m-%d]` (:509-534, T-724); дельта: относительная давность «(N дн. назад)»/полный origin — по Q9/A1); (2) легаси-канон `<context>/<user_gossip>/<bot_knowledge>` и `build_rag_context` для прочих потребителей НЕ меняются (метки — параметром или в direct-обёртке, по решению A1/Q9); (3) байт-эталоны рендера direct-пути обновить (test_graphrag_memory/test_summary_xml/test_direct_chat — по решению); (4) тесты: факты direct-RAG имеют origin+давность; legacy-рендер без изменений (байт-эталон). (services/summary_memory.py, services/direct_chat_service.py, tests по конвенции)
- [x] T-810 (F4): опциональный LLM-реранк top-k (п.22): (1) флаг `flags.chat_rag_rerank_enabled` (default false) + лимиты (top-k кандидатов, бюджет) — REGISTRY + settings-дефолты; (2) реранк-шаг в direct-пути RAG по образцу `search_service._rerank_results` (:103-125): кандидаты (уже отранжированный top-k из F1) → дешёвый rerank-промпт (канон: Q13/A1 — новая константа в summary_prompts/search_prompts-нейминге по решению; user-facing-канон-дисциплина если в PG) → топ остаётся; fail-open: ошибка/тонкий ответ → исходный список (WARNING); (3) НЕ вызывается, когда флаг off (0 LLM-вызовов сверх нормы); (4) тесты: off — rerank не вызывается (мок-история); on — вызывается на top-k, результат используется; LLM-ошибка → исходный список; контекст-кап сохранён. (services/direct_chat_service.py, services/summary_memory.py, services/prompt_migrations.py при новом каноне, param_catalog/config — ключи, тесты по конвенции)

## G. Тесты — сводная матрица покрытия (минимум 1 тест на пункт)

- [x] T-811 (G1): тест-координатор (после B–F): (1) матрица «пункт 1..26 → тест(ы)» — каждый из 24 пунктов [х] покрыт ≥ 1 тестом в задачах B–F (список тест-имён/файлов, в том числе: п.1/2/3/5 — рендеры и отсутствие «хвостов» в чистых путях (комментарий владельца п.3!); п.6 — memorize-атрибуция; п.7/24/25 — порядок/позиции; п.9/13 — урезание и importance; п.10 — thread сквозь бота; п.11 — итог ветки; п.12/21 — метки; п.14/17 — уровни конспекта и TTL-семантика; п.16 — purge-защита; п.18/20/22 — RAG; п.8/23/26 — байт-эталоны канона); (2) регресс-обязательства: байт-тесты промптов (test_direct_chat_prompts), порядок/бюджет блоков (TestContextPartitioning/payload_builder), lore-inject, RAG-рендеры/сортировки (test_graphrag_memory/summary_xml), running-summary/TTL, миграции (test_prompt_migrations) — все актуализированы; (3) полный pytest → 0 failed, `git diff --check` чист; (4) фиксация пропусков/правок — правками в задачах B–F (или в этой задаче при мелких дельтах, с пометкой). (tests/, планы-файлы)

## H. Деплой, README, отчёт, архив

- [x] T-812 (H1): README-доки: раздел «Context-Layer X-Features»: что изменилось в подаче контекста (рендеры с uid, Current_Question, приоритеты блоков, метки свежести, RAG по релевантности/дедуп/реранк-флаг, progressive-конспект, importance-удержание, purge-защита), новые конфиг-ключи (список из A1) и их дефолты, правила канона (стабильный system-префикс для prompt-cache; служебные uid не выводятся в ответах), что НЕ делалось (п.15/п.19, LLM-резюме веток, миграции v8 не вносились — если A1 не решил иначе). .env.example — без изменений (новых env нет). Полный pytest → 0 failed; grep-проверка секретов; `git diff --check` чист. (README.md) *(@DevOps финальный шаг — вне архива)* **Выполнено 06.09.2026 (коммит 6928df9):** раздел «Контекст-слой (раунд 8)» + 12 ключей в справочник (группы DirectChat 8→18 / GraphRAG 7→9, всего 178→190), счётчик тестов 4321; pytest 4321 passed / 0 failed; секрет-скан 0; `git diff --check` чист.
- [x] T-813 (H2): @DevOps: деплой раунда: pytest 0 failed локально → коммит (код+тесты+plans/backlog.md+plans/features/context-layer-x-features/) → пуш; сервер `git pull --ff-only`; БЭКАП SQLite-БД (прецедент раундов) — при деплое авто-миграция канона `prompts.direct_chat_system_prompt` (migrate_prompt_canons, ступень B1) и идемпотентные CREATE IF NOT EXISTS (если A1 добавил таблицы); мягкий рестарт бота → journald: миграция промпта в PG (если прод был на каноне раунда 5), ошибок нет; live-верификация: (1) канон в `prompts.direct_chat_system_prompt` == новый (или кастом не тронут), (2) реплика бота в runtime-чате: контекст-инструменты (debug-лог/админ-средства при наличии) показывают uid-рендеры/Current_Question/новый порядок блоков, (3) ответы бота БЕЗ технических «хвостов», (4) флаг реранка off по умолчанию. Результат — в H3. (прод) *(@DevOps финальный шаг — вне архива)* **Выполнено 06.09.2026 (коммиты a859819/2e8480d/739234d; HEAD aafb769):** сервер 198.46.175.136: `git pull --ff-only` b198d13..aafb769; рестарт `admin_bot` → active (21:17 UTC); журнал: миграций user_version НЕТ (SQLite остался v7 — PRAGMA user_version=7), CREATE IF NOT EXISTS применились — live-SQL: таблицы `bot_reply_parents` и `chat_summary_levels` (2 строки); `[prompt_migration] канон обновлён | key=prompts.direct_chat_system_prompt` (канон R8, 3-я ступень PREV_R8); malformed/error/traceback = 0; health /api/health = 200; uptime-heartbeat тикает (60s, executed successfully).
- [x] T-814 (H3): отчёт юзеру + закрытие: аппрув @Reviewer (полный прогон/регресс-обзор); архивация: перенос `plans/features/context-layer-x-features/` → `plans/archive/context-layer-x-features/`; правка `plans/backlog.md` (статус раунда 8: выполнено+архив, прецеденты раундов 5–7); `ARCHITECTURE.md` — раздел по эпику (принятая практика раундов 5–7 — решает @Architect); финальный memory-sync docs-коммит (правило T-725). Отчёт юзеру: что сделано по 24 пунктам (сводка по категориям), новые флаги/лимиты и как менять, что НЕ тронуто (п.15/п.19, SQLite-схема v7 — миграций не вносилось (если не решили иначе в A1), история-БД нетронута — RUNTIME WARNING). (plans/, README.md, ARCHITECTURE.md) *(@DevOps/@Memory финальный шаг — вне архива)* **Выполнено 06.09.2026:** архив перенесён @PM; backlog — полный ✅ раунда 8 с прод-фактами; ARCHITECTURE.md §20 — @Architect; финальный memory-sync docs-коммит (T-725) — @DevOps/@Memory; отчёт юзеру сформирован (24/24 [х] реализованы, п.15/п.19 — вне скоупа).
