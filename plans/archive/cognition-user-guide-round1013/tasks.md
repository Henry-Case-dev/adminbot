# Фича F7 — `cognition-user-guide-round1013` (человекочитаемая справка + README)

> **Статус: ✅ COMPLETED** (Step 8 @PM Archive, 13.09.2026; реализовано Step 4 @Builder, 13.09.2026). Спека: `spec.md`.
> `plans/docs/intelligence_user_guide.md` создан (8 глав, 444 слова, grep по запрещённым терминам — 0);
> README актуализирован (v2.57.0, тесты 5373, changelog 10.13); `APP_VERSION` синхронен; полный `pytest` —
> **5373 passed / 1 skipped / 0 failed**; `git diff --check` чист. T-1467 (@DevOps: R17-скан, коммит, push,
> деплой, отчёт) исполняется единым эпик-коммитом на шаге DevOps.
> **Раунд:** 10.13. **Нумерация:** T-1465…T-1467 (продолжает F6 T-1464).
> **Тип:** docs (финальная фича раунда). **Приоритет:** P3.
> **Зависимости:** F1–F6 (описывает итоговое поведение; пишется ПОСЛЕ реализации).
> **ТЗ:** `plans/current_task.md`, раздел **10** («Человекочитаемая документация») + финальный абзац
> (README, русский коммит, push, деплой, отчёт).
> **Эпик:** `Epic: Cognition-Sleep-Memory Refactor round1013`.

## 0. Цель

Готовая к публикации справка для конечных пользователей: как устроен «мозг» бота, как он видит сны
(обычные и глубокие), ностальгирует, собирает досье и пишет лор, как «воскрешает» воспоминания.
Строго прозаично, простыми словами, без аббревиатур, с лёгкой иронией. Плюс актуализация README.

## 1. Требования (дословно из ТЗ §10 + финал)

- [x] Новый файл `plans/docs/intelligence_user_guide.md` — обзор всего «мозга» бота: память, обычные и
      глубокие сны, ностальгия, досье на людей, лор чатов, «воскрешение» старых воспоминаний.
- [x] **Строго прозаичный стиль и простые слова.** Никаких аббревиатур (никаких RAG, LLM, векторных баз,
      токенов, эндпоинтов) и сухого технического языка. Лёгкий, ироничный тон, понятный даже ребёнку.
- [x] Текст полностью готов к публикации для конечных юзеров (будущий раздел «Справка» мини-аппа).
- [x] README — актуализировать (ироничный тон), затем русский коммит, push, деплой, отчёт (§5–§6).

## 2. Целевые модули/артефакты

- `plans/docs/intelligence_user_guide.md` — **новый**.
- `README.md` — актуализация (счётчик тестов, версия, описание round1013).
- `config/settings.py` — `APP_VERSION` (синхронно с README).
- `web/index.html` — cache-bust `?v=__APP_VERSION__`.
- `plans/docs/canon/` — при необходимости ссылок (не менять каноны).
- Деплой по `plans/current_task.md` (ssh → `cd /var/www/admin_bot` → `git pull`; `.env` при изменениях;
  `sudo systemctl restart admin_bot`; `sudo systemctl status admin_bot`).

## 3. Инварианты (constraints — не нарушать)

- **R17:** в `plans/docs`/README/коммите — **ноль секретов** (только `.env.example` с плейсхолдерами);
  пароль ssh/деплоя из `plans/current_task.md` в документацию не переносится.
- Без аббревиатур в пользовательском гайде (проверка grep по запрещённому списку: RAG/LLM/STT/API/токен/
  эндпоинт/вектор/эмбеддинг и т.п.).
- Синхронность `README` ↔ `APP_VERSION` ↔ cache-bust (тесты `test_app_version_matches_readme`,
  `test_index_version_query_param` не ломать).
- Описывать **реально реализованное** поведение (после F1–F6), без обещаний нереализованного.
- **Коммиты:** русские conventional commits; `git diff --check` чист.
- Деплой/QA — по `plans/current_task.md`; команды фиксировать в отчёте.

## 4. Зависимости

- **Вверх:** F1–F6. **Идёт последней** — иначе текст разойдётся с фактическим поведением.

## 5. Definition of Done

- [x] `plans/docs/intelligence_user_guide.md` создан, прозаичен, без аббревиатур, ироничен, готов к публикации.
- [x] Гайд покрывает: память/время, обычный сон, глубокий сон, ностальгию, досье, лор, воскрешение воспоминаний.
- [x] README актуализирован (счётчик тестов, версия, changelog round1013); `APP_VERSION`/cache-bust синхронны.
- [x] R17-скан диффа чист; секретов нет.
- [x] Русский conventional-commit, push в `origin/master`.
- [x] Деплой: `git pull`, restart, `active`, `/api/health` 200, 0 ERROR/Traceback.
- [x] Человекочитаемый отчёт простыми словами (что сделано, как работает, какие баги закрыты, блокеры).

## 6. Чек-лист задач

- [x] **T-1465 (@Builder/@PM):** написать `plans/docs/intelligence_user_guide.md` — прозаично, простыми
  словами, без аббревиатур, ироничный тон; самопроверка grep по запрещённым терминам.
- [x] **T-1466 (@Builder):** актуализировать README (ироничный тон) + `APP_VERSION` + cache-bust `?v=`;
  сохранить зелёными `test_app_version_matches_readme`/`test_index_version_query_param`.
- [x] **T-1467 (@DevOps):** R17-скан диффа; русский conventional-commit; push; деплой (git pull / `.env`
  при необходимости / restart / status); live-верификация (health 200, 0 ERROR/Traceback, маркеры round1013);
  человекочитаемый отчёт владельцу.

## 7. Открытые вопросы (@Architect/@PM → владелец)

- **F7-Q1:** объём гайда (один документ vs разбивка на разделы)?
- **F7-Q2:** нужен ли прямой задел под будущий раздел «Справка» мини-аппа (структура/якоря/заголовки)?
- **F7-Q3:** публиковать ли гайд в README целиком или дать ссылку?

## 8. Feature flag / progressive delivery

- **Feature flag:** не требуется (docs-only).
- **Progressive delivery:** неприменимо. **Rollback:** `git revert` docs-коммита.
