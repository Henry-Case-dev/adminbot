# Spec F7 — `cognition-user-guide-round1013` (человекочитаемая справка + README)

> Статус: **✅ COMPLETED** (Step 8 @PM Archive, 13.09.2026; спека Step 2 @Architect, 13.09.2026). База: HEAD `ce25dc7`.
> ТЗ: `plans/current_task.md` §10 + финальный абзац. Tasks: T-1465…T-1467. Docs. P3.
> Зависимости: F1–F6 (описывает итоговое поведение; идёт ПОСЛЕ реализации).

## 0. Цель

Готовая к публикации пользовательская справка о «мозге» бота: память и время,
обычные и глубокие сны, ностальгия, досье, лор, «воскрешение» воспоминаний.
Строго прозаично, простыми словами, без аббревиатур, с лёгкой иронией. + README.

## 1. Объём

### In scope
- Новый `plans/docs/intelligence_user_guide.md`.
- Актуализация `README.md` (счётчик тестов, версия, changelog round1013).
- Синхронность `APP_VERSION` ↔ README ↔ cache-bust.
- Деплой/QA-чеклист (по ТЗ), отчёт.

### Out of scope
- Любой код-функционал; изменение канонов; публикация в TMA (задел на будущее).

## 2. Структура гайда (F7-Q1/Q2 — РЕШЕНО)

**Один документ**, главы с H2-заголовками (задел под будущий раздел «Справка»):

1. «Как бот всё помнит» — память, время, давность («внимание, возможно устарело»).
2. «Как бот видит сны» — обычный сон (убеждения), что такое убеждение, как оно тускнеет.
3. «Глубокий сон» — мета-прозрения, «мост времени», фоновый исторический слой.
4. «Ностальгия» — почему бот иногда вспоминает старое, таймеры тишины.
5. «Досье на людей» — факты vs шутки/мемы (иронический фильтр), «локальные ярлыки».
6. «Лор чата» — летопись; кто пишет и как.
7. «Воскрешение воспоминаний» — резонанс, реаниматор, всплеск упоминаний.
8. «Словарик» — простые объяснения (без аббревиатур).

**F7-Q3 — РЕШЕНО:** в README — краткий абзац round1013 + **ссылка** на гайд
(не публиковать целиком; README остаётся техническим).

## 3. Требования к тексту (проверяемые)

- **Запрещённые термины** (grep-проверка, регистронезависимо, в гайде):
  `RAG`, `LLM`, `STT`, `API`, `эндпоинт`, `токен`, `вектор`/`векторный`,
  `эмбеддинг`, `промпт`, `JSON`, `SQLite`, `PostgreSQL`, `PG`, `cron`, `воркер`,
  `пайплайн`, `контекст` (в техсмысле), `база данных`.
  Допустимые бытовые синонимы: «память», «записи», «мозг», «дневник», «сон»,
  «шёпот», «подсказки».
- Тон: лёгкий, ироничный, добрый, понятный ребёнку; без сухого техноязыка.
- Без кавычек-ёлочек/длинных тире в стиле проекта (ТЗ: типографика).
- Описывать **реально реализованное** поведение (после F1–F6); никаких обещаний.

## 4. Обновление README/версии

- `config/settings.py::APP_VERSION` — синхронно с README; cache-bust
  `web/index.html` `?v=__APP_VERSION__`.
- Не ломать `test_app_version_matches_readme`, `test_index_version_query_param`.
- README: счётчик тестов (фактический после полного pytest), краткое описание round1013.

## 5. Файлы

| Файл | Что |
|---|---|
| `plans/docs/intelligence_user_guide.md` | **новый** гайд |
| `README.md` | changelog round1013 + ссылка; счётчик тестов; ироничный тон |
| `config/settings.py` | `APP_VERSION` (при необходимости) |
| `web/index.html` | `?v=__APP_VERSION__` (проверить) |

## 6. Каталог-Δ

**+0**. Feature flags — не требуется (docs-only).

## 7. Тест-план

1. Grep-самопроверка гайда по запрещённому списку — 0 совпадений.
2. `test_app_version_matches_readme` и `test_index_version_query_param` — зелёные.
3. R17-скан диффа: нет секретов/паролей/`ssh`-реквизитов из `plans/current_task.md`.
4. Полный `pytest` 0 failed; `git diff --check` чист.
5. Деплой-верификация: `git pull`, restart, `active`, `/api/health` 200, 0 Traceback.

## 8. Риски

| Риск | Митигация |
|---|---|
| Технический жаргон просочится | Grep-чеклист + ревью владельца |
| Расхождение текста с кодом | Пишется ПОСЛЕ F1–F6 |
| Секреты из ТЗ в доки | R17-скан; только `.env.example`-плейсхолдеры |
| Рассинхрон версии/README/cache-bust | Пин-тесты |

## 9. Критерии приёмки

- [ ] `plans/docs/intelligence_user_guide.md` создан: прозаичен, без аббревиатур, ироничен, готов к публикации.
- [ ] Покрыты: память/время, обычный сон, глубокий сон, ностальгия, досье, лор, воскрешение.
- [ ] README актуализирован (тесты/версия/changelog); `APP_VERSION`/cache-bust синхронны.
- [ ] R17-скан чист; русский conventional-commit; push/deploy/отчёт по ТЗ.

## 10. Разрешение open questions (F7)

- **F7-Q1** — один документ с главами.
- **F7-Q2** — да, структура/якоря/H2 — задел под будущий раздел «Справка».
- **F7-Q3** — в README краткий абзац + ссылка, не целиком.
