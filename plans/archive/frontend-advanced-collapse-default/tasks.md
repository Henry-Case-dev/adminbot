# Задачи: frontend-advanced-collapse-default (ТЗ 5 раунда 10.4)

Раунд 10.4, фича D (T-1018…T-1023). Стадия старта — PLANNED; spec.md @Architect.
ТЗ владельца (п.5): «Расширенные настройки» ДОЛЖНЫ быть свёрнуты по умолчанию;
сейчас при группе без базовых карточек аккордеон принудительно раскрыт
(`:open="basicItems(grp).length === 0 || expandOpen(activeTab)"`).

Отправная точка (HEAD 1410a68):
- web/index.html:673-675 — `<details class="advanced" v-... :open="basicItems(grp).length === 0 || expandOpen(activeTab)" @toggle="toggleExpand(activeTab)">`.
- Персист: app.js:1293-1303 `expandOpen`/`toggleExpand` (localStorage
  `adminbot.expand:<tab>`) — НЕ трогать (это осознанное «раскрыть/свернуть»).
- Прецедент BUG-5 (10.2): details рендерится при advancedItems>0; группа —
  при basic|advanced > 0; F-13 AC-3: `<template v-for>` + v-if на дочернем узле.
- Маркер-тест: test_webapp_nav_disclosure_ui.py:130-131
  `':open="basicItems(grp).length === 0 || expandOpen(activeTab)"' in html`
  — СЛОМАЕТСЯ, обновить.
- Зависимости: фичи A/C/E реструктурируют группы-конфиг вкладки (появляются
  группы с 0 basic — например, после разметки 'advanced'): реализовать D
  ПЕРВОЙ (d → c → e → a → b → f → h → g), иначе реструктурированные вкладки
  на время раунда будут с самопринудительно раскрытыми аккордеонами.

## A. Рендер [@Builder]

- [ ] T-1018 — проанализировать семантику `:open`: (1) `expandOpen(activeTab)`
  — пользовательский персист; (2) `basicItems(grp).length === 0` — эвристика
  «чтобы вкладка не выглядела пустой». **AC-A1:** в задачах-фиксе зафиксировать
  итог анализа в комментарии HTML (почему эвристику убираем: ТЗ-5).
- [ ] T-1019 — фикс: `:open="expandOpen(activeTab)"` (один источник истины).
  **AC-A2:** маркер-строка в index.html обновлена; `(basicItems(grp).length === 0
  ||` отсутствует; аккордеон при 0 базовых и без персиста — закрыт.
- [ ] T-1020 — UX-защита для групп целиком advanced (posледствие A/C/E):
  заголовок «Расширенные настройки (N)» остаётся видимым; добавить вариант
  бейджа/подсказки (напр. суммарный бейдж-счётчик advanced в шапке вкладки или
  `title`-подсказка) — решение @Architect; минимальный вариант: без изменений.
  **AC-A3:** независимо от варианта: группа с 0 basic и N advanced — свернута,
  но пользователь ВИДИТ `(N)` в summary и может раскрыть (клик по summary).

## B. Маркеры и регресс [@Builder]

- [ ] T-1021 — test_webapp_nav_disclosure_ui.py:130-131: обновить под
  `:open="expandOpen(activeTab)"` + негатив (старой строки нет); тест
  `test_details_advanced_marker`/`test_expand_localstorage` остаются.
- [ ] T-1022 — маркер-стабильность с фичами A/C/E: после реструктуризации —
  проверить, что вкладки не выглядят пустыми (см. requirement C-фичи: базовые
  рубильники остаются basic). **AC-B1:** сквозная проверка «для каждой
  конфиг-вкладки существует ≥1 группа с basicItems>0» — новый тест
  (виртуальное состояние: каталог→разметка; без браузера).
- [ ] T-1023 — регресс: полный pytest 0 failed; `node --check web/app.js` clean;
  `git diff --check` чист; live-маркер (после деплоя): на вкладке с 0 базовых
  аккордеон закрыт, клик раскрывает, персист работает (перезагрузка → состояние
  сохранилось). **AC:** live-отчёт в задаче.

**Критерии приёмки ТЗ 5 (сводные):**
- По умолчанию «Расширенные» свёрнуты ВСЕГДА (в т.ч. группы без базовых);
  единственный способ раскрыть — клик пользователя (персист localStorage).
- Группы без карточек не рендерятся (BUG-5-поведение) — не сломано;
  «(0)» не появляется.
- test_webapp_nav_disclosure_ui.py — обновлён и зелёный; полный pytest — 0 failed;
  node --check — clean.
