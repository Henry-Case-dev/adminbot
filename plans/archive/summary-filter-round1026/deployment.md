# deployment.md — S1 `summary-filter-round1026` (T-3129 @DevOps: baseline/rollback)

## Вердикт: **NOT_APPLICABLE** (для задачи T-3129)

**Обоснование.** T-3129 — подготовительный блок 0 (baseline + точка отката), поставки рантайма нет:
изменялись только `plans/**` и артефакты вне git (`var/backups/**`, `.env.bak.round1026-s1`).
`services/**`, `web/**`, `config/**`, миграции и каталог **не менялись** → рестарт/health/миграции/cache-bust неприменимы,
`APP_VERSION` **2.58.17** без bump.

- Среда деплоя: — (деплой не выполняется)
- Commit/версия: HEAD `01f3c57`, `APP_VERSION` 2.58.17
- Точка отката: annotated-тег **`pre-round1026-s1`** (`10a5c74`→`01f3c57`), запушен в origin
- Инварианты: Δ DDL=0, Δ каталога=0 (459/98/96/21/418), R17/R18
- **Реальный деплой S1 — задача T-3157** (bump `APP_VERSION` 2.58.17→2.58.18, cache-bust, минимальные тесты §107/§114).

Статус: **NOT_APPLICABLE** (обосновано; поставка отсутствует).
