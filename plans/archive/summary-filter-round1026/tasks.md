# S1 — `summary-filter-round1026` (Эпик 2 «Summary Hybrid Pipeline», Раунд 10.26)

> **ТЗ:** `plans/current_task.md` §80, §87–§89, §93 (+ §82/§85/§107/§108–§109/§83 — уточняющие).
> **Тип:** backend/алгоритм (детерминированный префильтр без LLM). `spec.md`/ADR — **@Architect (Step 2)**, PM их не создаёт.
> **Приоритет:** P0. **Тип фичи:** этап 0 пайплайна — вход L1 (подготовка сообщений).
> **Зависит от:** Эпик 1 (авто-приёмка ✅ F10 §70; live ⏳ PENDING OWNER VERIFICATION).
> **Статус:** ✅ **COMPLETED + MERGED + ARCHIVED + DEPLOYED (Шаг 8 @PM + Шаг 9 @DevOps, 23.09.2026)** — блоки A–F (T-3132…T-3150) + rework H (T-3160/T-3161) закрыты; Merge — `plans/ARCHITECTURE.md` **§71** (Шаг 7 @Architect, T-3155); архив — **`plans/archive/summary-filter-round1026/`**; @Reviewer **Approved** (итер.2, T-3152), @Scanner **C0/H0/M0/L1** (T-3153, `plans/reports/round1026_s1_scanner_audit.md`); каталог 467/426/442/100/98/21 (санкция ADR-1026-1 D1), pytest 8501/0, JS 42/42, Δ DDL=0; `APP_VERSION` 2.58.18. **deploy — ✅ Шаг 9 @DevOps (T-3157, VERIFIED, 23.09.2026; коммиты `4cd4ae6`/`89bda3d`/`31763ab`, `APP_VERSION` 2.58.18, health 200, `database is locked`=0, `deployment.md` VERIFIED); метрики — ✅ Шаг 10 @Memory (T-3158).** **live-приёмка (браузерный UI/E2E, per-chat override, публикация) — [ ] PENDING OWNER VERIFICATION (D4; T-3151).** Техдолг — §71.5. Код закоммичен (`4cd4ae6`/`89bda3d`/`31763ab`).
> **Baseline:** HEAD `01f3c57` (== `origin/master`), `APP_VERSION` 2.58.17, pytest `.venv` **8463/0**, JS **42/42**, каталог **459/418/434/98/96/21**, **Δ DDL=0**.
> **Не трогать:** `plans/current_task.md` (source of truth, только чтение), `plans/metrics.md`/`plans/MEMORY.md` (ведут @Memory).
> **Рекомендация @Memory (принята):** дробление S1 на (a) ядро фильтра+вес §87–§88 → блок A, (b) тонкая настройка §89 → блок B, (c) чанки/лимит §93 → блок C.

## Трассируемость (REQ → verbatim §ТЗ → блок)
| REQ | Источник (verbatim, `current_task.md`) | Блок | Задачи |
|---|---|---|---|
| REQ-S1-01 | §80 «Заменить текущую обработку сырого шестичасового лога новым пайплайном… Шестичасовой лог → Алгоритмический фильтр → … Не переписывать работающий механизм публикации» | D | T-3144, T-3146 |
| REQ-S1-02 | §87 «Главный переключатель: «Алгоритмическая предфильтрация»… Значение по умолчанию: **ВКЛЮЧЕНО**» | B | T-3137, T-3139 |
| REQ-S1-03 | §87 «Основные параметры: Минимальный вес сообщения. Начальное значение: 1. Минимальная длина для бонуса. Начальное значение: 5 слов. Окно временного всплеска. Начальное значение: 120 секунд» | A, B | T-3134, T-3135, T-3137 |
| REQ-S1-04 | §87 «Предлагаемые ключи: `summary.filter.min_weight` / `summary.filter.min_words_for_bonus` / `summary.filter.burst_window_seconds`… Проверить существующую конфигурационную схему. **Не создавать параллельное хранилище**» | B | T-3130, T-3137 |
| REQ-S1-05 | §88 «Каждое сообщение начинает с веса 0. Добавить +1 за каждое условие: 1. Содержит адресное упоминание или reply_to. 2. На сообщение кто-то ответил. 3. Находится во временном всплеске. 4. Длиннее установленного лимита слов» | A | T-3132, T-3133, T-3134 |
| REQ-S1-06 | §88 «Один критерий даёт максимум один балл. Не начислять несколько баллов за множество упоминаний» | A | T-3133, T-3136 |
| REQ-S1-07 | §88 «Не считать любое соседство двух сообщений временным всплеском. Учитывать плотность сообщений» | A, B | T-3134, T-3137 |
| REQ-S1-08 | §89 «Отдельная группа: «Тонкая настройка». Сюда: Минимальная плотность всплеска. Сохранение контекста ответов. Количество ближайших контекстных сообщений. Лимит восстановленного контекста. Порог плотности подобрать по реальным логам. Не считать любой короткий тред бесполезным» | B, C | T-3137, T-3138, T-3143 |
| REQ-S1-09 | §93 «Не считать, что любой шестичасовой лог поместится в контекст модели L1. Оценивать токенный объём. Если данных слишком много: Разделить вход на перекрывающиеся фрагменты… Устранить дубли. Сохранить связи. **Не отрезать последние сообщения молча**» | C | T-3141, T-3142, T-3143 |
| REQ-S1-10 | §108/§109 «FILTER_START / FILTER_COMPLETE / FILTER_ERROR… FILTER_COMPLETE: Исходные сообщения. Сохранённые… Процент отсева. Длительность» + «Каждый запуск Саммари получает run_id… Не выводить API-ключи в логи» | E | T-3147, T-3148 |
| REQ-S1-11 | §107 «Алгоритмический фильтр включён по умолчанию… Не оставлять фичу выключенной в ожидании ручной активации. При этом перед деплоем выполнить минимальные функциональные тесты» | B, F, G | T-3139, T-3149, T-3153, T-3157 |
| REQ-S1-12 | §83 «Не сбрасывать существующие модели… Новые конфигурационные поля должны иметь корректные значения по умолчанию» | B | T-3137, T-3140 |
| REQ-S1-13 | §85/§87 «Маршрут: Модули → Сводки чатов → Подготовка сообщений» | B | T-3138 |

## Приёмочные инварианты S1 (нарушение = НЕ принято)
1. **S1 НЕ меняет публикацию** — `generate_image`/`generate_image_verbose`, `sendRichMessage`/`build_cover_media`/`send_rich_message`, fallback `sendMessage` не трогаются (**D4, ADR-1025-24**). Любой дифф публикационного пути = блокер → СТОП.
2. **System-2 остаётся ровно 2 LLM-вызова** (Редактор → Рассказчик, **ADR-1022-3/4**). Фильтр — алгоритмический, **0 новых LLM-вызовов**; правится только участок входа L1.
3. **Δ DDL = 0** жёстко. **Δ каталога** — только по письменной санкции @Architect (Step 2, T-3130); без санкции — env-only (`ClassVar`, default ON), Δ каталога = 0.
4. **Не ломать:** save-path F0, store F4, UI workspace F5, ExecutionGraph-адаптер F6, permsoc F7, секреты/SaveBar F9, Статус F11; ADR-1023-1/-3/-6/-8/-9, ADR-1024-4/-10, ADR-1013-3 (канон: `PREV_*` + миграция + эталон).
5. **Детерминизм:** одинаковый вход (+инъекция `now`) → одинаковый выход; порядок = хронология; «фильтр не теряет важное» (адресное/короткий важный тред/reply-тред не отбрасываются необратимо).
6. **CSP/zero-build**, **R17/R18** (в логах — только числа/коды/id, без ключей и без содержимого сообщений).
7. **S6 (`summary-publish-integration`) и S10 (`summary-deploy`) — отдельный гейт:** смена существующего публикационного пайплайна только после **live-приёмки Эпика 1** + независимого решения @Architect; S1 их не открывает.

## Блок 0 — baseline, точка отката, Step 2 @Architect
- [x] **T-3129 [@DevOps]** — зафиксировать baseline и точку отката S1: подтвердить HEAD `01f3c57`, `APP_VERSION` 2.58.17, pytest 8463/0, JS 42/42, каталог 459/418/434/98/96/21; создать annotated-тег **`pre-round1026-s1`** и записать точную команду отката. **Готово, когда:** тег создан и запушен, в evidence — `git rev-parse pre-round1026-s1`, baseline-числа и команда rollback. **Файлы:** git tag + `plans/archive/summary-filter-round1026/evidence.md`.
- [x] **T-3130 [@Architect, Step 2]** — `spec.md` + ADR по S1 и **санкция** по открытым вопросам: (а) `summary.filter.*` — новые параметры каталога (какая группа/namespace, Δ каталога?) или env-only (Δ каталога=0); (б) подтвердить **Δ DDL=0**; (в) затрагиваются ли промпт-каноны (если да — `PREV_*` + ступень `PROMPT_MIGRATIONS` + эталон, ADR-1013-3); (г) **форма алгоритма веса** (точная функция баллов и порога, семантика «всплеска по плотности»); (д) граница S1↔S3 по §93 («объединить темы/устранить дубли» — фильтр или L1 Кластеризатор). **Готово, когда:** в `spec.md` зафиксированы контракты/сценарии, в ADR — решения; без санкции реализация каталога/DDL не начинается. **Файлы:** `plans/archive/summary-filter-round1026/spec.md` (+ ADR при необходимости).
- [x] **T-3131 [@PM]** — после Step 2 сверить `tasks.md` ↔ `spec.md` (контракты/сценарии/REQ) и при расхождении запросить правку @Architect, не позволяя @Builder изобретать решение. **Готово, когда:** каждый REQ-S1-01…-13 привязан к сценарию спеки, расхождений нет. **Файлы:** этот `tasks.md`.

## Блок A — ядро алгоритмического фильтра §87–§88 (T-3132…T-3136)
- [x] **T-3132 [@Builder]** — новый модуль `services/summary_filter.py`: чистые функции `score_message(...) -> int` (4 критерия, максимум 1 балл за критерий) и `filter_window(messages, params) -> FilterResult` (`kept`/`dropped`/`reasons`/`counts`/`stats`; вход — уже прочитанный 6-часовой лог из `get_window_messages`). Без LLM, без обращения к БД, без сети. **Готово, когда:** модуль импортируется без DDL/сайд-эффектов, контракт FilterResult задокументирован, покрыт unit-тестами. **Файлы:** `services/summary_filter.py`, `tests/test_summary_filter.py`.
- [x] **T-3133 [@Builder]** — критерии 1–2: (1) адресное упоминание (`mentions`) **или** `reply_to_id`; (2) «на сообщение кто-то ответил» (индекс `reply_to_id` по окну); **ровно максимум 1 балл за критерий**, множество упоминаний ≠ больше баллов; бот-сообщения/предыдущие Саммари не начисляют баллы. **Готово, когда:** кейсы «много упоминаний → 1 балл» и «бота не считаем событием» зафиксированы тестами. **Файлы:** `services/summary_filter.py`, `tests/test_summary_filter.py`.
- [x] **T-3134 [@Builder]** — критерии 3–4: (3) временной всплеск **по плотности** сообщений в окне `burst_window_seconds` (не «любое соседство двух сообщений»); (4) длина > `min_words_for_bonus`. **Готово, когда:** тесты различают «всплеск» и «просто два сообщения рядом»; граница длины проверена (ровно порог / порог+1). **Файлы:** `services/summary_filter.py`, `tests/test_summary_filter.py`.
- [x] **T-3135 [@Builder]** — порог отбора `min_weight` (начальное 1) и гарантия «фильтр не теряет важное»: адресные/короткие важные треды и reply-родители сохраняются как кандидаты; для S2 сохраняются `message_id`/`reply_to_id` (необратимо ничего не выбрасывается — `dropped` доступен для восстановления контекста). **Готово, когда:** кейсы «короткий важный тред», «параллельные разговоры», «почти пустой лог» дают ожидаемый состав. **Файлы:** `services/summary_filter.py`, `tests/test_summary_filter.py`.
- [x] **T-3136 [@Builder]** — детерминизм и устойчивость: стабильные тай-брейки, хронологический порядок `kept`, инъекция `now` (никакого чтения системных часов внутри алгоритма), линейная сложность (один проход по окну) на ~10k сообщений. **Готово, когда:** двойной прогон даёт байт-идентичный результат; замер времени зафиксирован. **Файлы:** `services/summary_filter.py`, `tests/test_summary_filter.py`.

## Блок B — тонкая настройка §89: параметры, тумблер, UI (T-3137…T-3140)
- [x] **T-3137 [@Builder]** — параметры `summary.filter.*` по санкции T-3130: главный тумблер «Алгоритмическая предфильтрация» (**default ON**), `min_weight` (1), `min_words_for_bonus` (5 слов), `burst_window_seconds` (120 с); §89 «Тонкая настройка»: минимальная плотность всплеска, сохранение контекста ответов, количество ближайших контекстных сообщений, лимит восстановленного контекста. Чтение — через `hot.get(key, settings.FIELD)`; корректные дефолты; без параллельного хранилища. **Готово, когда:** значения резолвятся global→chat→код-дефолт, Δ каталога/DDL соответствуют санкции (или env-only), покрыто тестами. **Файлы:** `config/settings.py`, `services/param_catalog.py` (только по санкции), `services/summary_filter.py`, `tests/`.
- [x] **T-3138 [@Builder]** — UI: группа «Тонкая настройка» и тумблер на маршруте §87 «Модули → Сводки чатов → Подготовка сообщений» (вкладка workspace `mod_summary`, каркас F5) — только через существующий механизм config-items/`persistItems` F0 (одна серверная мутация на операцию), без новых API; метаданные в JS `MODULES` (Δ каталога=0), mobile — вертикально, без h-scroll. **Готово, когда:** сохранение/перечитывание значений подтверждено E2E, изоляция чатов не нарушена (§73-паттерн), CSP/zero-build сохранены. **Файлы:** `web/index.html`, `web/app.js` (+ `web/static/app.css` при необходимости).
- [x] **T-3139 [@Builder]** — поведение OFF: при выключенном тумблере сообщения не фильтруются — вход L1 **байт-в-байт** прежний (baseline-поток), никакой деградации/побочных записей; ON — режим по умолчанию (§107). **Готово, когда:** OFF-прогон даёт идентичный вход, ON/OFF переключаются без рестарта (hot-параметр). **Файлы:** `services/summary_generator.py`, `services/summary_filter.py`, `tests/`.
- [x] **T-3140 [@Builder]** — наследование и сохранность (§83): новые поля не сбрасывают и не подменяют существующие значения/настройки; global-дефолт vs per-chat override не смешиваются (чат A ≠ чат B); ручное заполнение не требуется для первого запуска. **Готово, когда:** есть тест «значения соседних ключей/чатов не изменились». **Файлы:** `services/summary_filter.py`, `tests/`.

## Блок C — чанки/лимит контекста §93 (T-3141…T-3143)
- [x] **T-3141 [@Builder]** — оценка токенного объёма отфильтрованного окна (reuse существующих потолков `limits.summary_max_context_tokens`/`summary_max_context_chars`, без новых ключей) → детерминированное решение «влезает / не влезает» с логируемым статусом. **Готово, когда:** оценка не зависит от сети/LLM, границы проверены тестами. **Файлы:** `services/summary_filter.py`, `tests/`.
- [x] **T-3142 [@Builder]** — «не влезает»: разбиение входа на **перекрывающиеся** фрагменты (детерминированная граница, overlap по сообщениям/границам тредов) + явный результат (`fragments`, `overlap`, `reason`); **последние сообщения не отрезаются молча** (любое усечение — статус + лог); дедуп и сохранение связей через `message_id`/`reply_to_id`. **Готово, когда:** фрагменты собираются без потери последних сообщений, связи reply-тредов не рвутся. **Файлы:** `services/summary_filter.py`, `tests/`.
- [x] **T-3143 [@Builder]** — лимит восстановленного контекста (§89) согласован с фрагментами; контракт передачи фрагментов/пакета во вход L1 зафиксирован для S3 (без реализации L1). **Готово, когда:** контракт не конфликтует со `spec.md`; «объединить темы/устранить дубли» отнесено по решению T-3130(д) к S1 или S3 явно. **Файлы:** `services/summary_filter.py`, `tasks.md` S2/S3-перекрёстная ссылка.

## Блок D — интеграция в `SummaryGenerator._run` (вход L1) без ломки System-2 (T-3144…T-3146)
- [x] **T-3144 [@Builder]** — врезка фильтра в `services/summary_generator.py::SummaryGenerator._run` после `compress_and_purge`/`get_window_messages`, до сборки XML/`_compose_user_content`; фильтр отдаёт подготовленный вход L1 (Stage-1 «Редактор»). Изменять только участок входа, не порядок этапов. **Готово, когда:** вход L1 = отфильтрованный состав; дифф локален и покрыт тестом. **Файлы:** `services/summary_generator.py`, `tests/`.
- [x] **T-3145 [@Builder]** — сохранить двухвызовность: Stage-1 Редактор → Stage-2 Рассказчик, **ровно 2 физических LLM-вызова** (ADR-1022-3/4); `truncate_to_tokens`, `services/summary_xml.py`, RAG/graph-подмес, `_compose_user_content`, стриминг/чанки и `_ensure_shiz_postfix` не меняют поведение. **Готово, когда:** тест фиксирует ровно 2 вызова и отсутствие регресса XML/RAG-путей. **Файлы:** `services/summary_generator.py`, `tests/`.
- [x] **T-3146 [@Builder]** — fail-open и граница публикации: любая ошибка фильтра не роняет Саммари (WARNING + нефильтрованное окно, без тихой потери), публикационный путь (`_deliver_rich`→`generate_image`→`build_cover_media`/`send_rich_message`, fallback `sendMessage`) **не изменён** (D4). **Готово, когда:** инъекция ошибки фильтра даёт рабочее Саммари; `git diff` по публикационным модулям пуст. **Файлы:** `services/summary_generator.py`, `tests/`.

## Блок E — логирование и `run_id`-совместимость с S7 (аддитивно) (T-3147…T-3148)
- [x] **T-3147 [@Builder]** — аддитивные события `FILTER_START/FILTER_COMPLETE/FILTER_ERROR` (§108) с полями §109: исходных сообщений, сохранённых, восстановленных из контекста, процент отсева, длительность; поддержка/приём `run_id` так, чтобы S7 (`summary-logging-runid`) добавил его без переделки контура. Без ключей/содержимого сообщений (R17/R18). **Готово, когда:** события видны в существующем логе, контракт `run_id` не конфликтует с S7. **Файлы:** `services/summary_filter.py`, `services/summary_generator.py`, `tests/`.
- [x] **T-3148 [@Builder]** — выставить метрики фильтра (`source/saved/restored/drop_percent/duration`) во внутреннюю структуру, которую сможет читать §111/§112 (S8) — **без** второй системы визуализации и без создания узлов ExecutionGraph (kind `algorithm` зарезервирован, эмитит S8). **Готово, когда:** данные доступны аддитивно, UI/адаптер не тронуты. **Файлы:** `services/summary_generator.py`, `tests/`.

## Блок F — тесты (фильтр + регресс Summary-пайплайна) (T-3149…T-3151)
- [x] **T-3149 [@Builder]** — unit-матрица фильтра: пропуск мусора vs сохранение важного (адресное, reply-тред, длинное, всплеск), «максимум 1 балл за критерий», §89-кейсы («короткий тред не считается бесполезным»), граничные (пустой лог, одно сообщение, ровно порог), детерминизм (двойной прогон), перф-замер. **Готово, когда:** каждый REQ-S1-03/05/06/07/08 имеет ≥1 тест, зелёные. **Файлы:** `tests/test_summary_filter.py`.
- [x] **T-3150 [@Builder]** — регресс пайплайна: существующие тесты Саммари зелёные; ровно 2 LLM-вызова; OFF байт-в-байт; fail-open; окружение/каталог не изменились сверх санкции (**Δ DDL=0**); публикация не в диффе. **Готово, когда:** полный pytest не хуже baseline 8463/0, JS 42/42. **Файлы:** `tests/`, отчёт прогона.
- [x] **T-3151 [@Builder]** — UI/E2E (если UI в скоупе T-3138): тумблер default ON, тонкая настройка сохраняется одной серверной мутацией, per-chat изоляция, reload сохраняет значения. **Готово, когда:** evidence — перехваты сети + прогон, без дублей F0/F5-тестов. **Закрыто (23.09.2026):** статические JS-тесты (42/42) + питон-инварианты (per-chat A/B, OFF байт-в-байт); браузерный стенд/Playwright в окружении отсутствовал → **live/браузерный E2E — [ ] PENDING OWNER VERIFICATION (§71.5)**. **Файлы:** `tests/`, `plans/reports/round1026_s1_ui.md`.

## Блок G — ревью / аудит / merge / архив / deploy / live / метрики + продолжение (T-3152…T-3159)
- [x] **T-3152 [@Reviewer]** — ревью S1: соответствие §87–§89/§93 и `spec.md`, отсутствие ложных закрытий, «фильтр не теряет важное». **Готово, когда:** `review.md` с вердиктом (итерации по необходимости). **Итог (23.09.2026):** **Approved** (итерация 2) — B-1/M-1 и L-1/L-R1026S1-1/-2 закрыты; pytest 8501/0, JS 42/42, публикация/промпты вне diff. **Файлы:** `plans/archive/summary-filter-round1026/review.md`.
- [x] **T-3153 [@Scanner]** — независимый аудит: **Δ DDL=0**, **Δ каталога** = санкция либо 0, R17/R18, CSP/zero-build, **публикация и двухвызовность не тронуты (D4)**, отсутствие новых внешних зависимостей. **Готово, когда:** отчёт с C/H/M/L/Info. **Итог (23.09.2026):** **C0/H0/M0/L1 → «к деплою ДА»** (L-R1026S1-3, non-blocking). **Файлы:** `plans/reports/round1026_s1_scanner_audit.md`.
- [x] **T-3154 [@Builder]** — rework по findings @Reviewer/@Scanner. **Готово, когда:** все блокеры закрыты, повторный прогон зелёный. **Закрыто (23.09.2026):** блок H (T-3160) + санкция ADR-1026-2 (T-3161); повторный прогон pytest 8501/0, JS 42/42 — зелёный.
- [x] **T-3155 [@Architect]** — merge/сверка архитектуры: раздел в `plans/ARCHITECTURE.md` + финализация ADR S1. **Готово, когда:** раздел обновлён, ADR Accepted. **Итог (23.09.2026):** **Merge §71** зафиксирован; ADR-1026-1/-2 **Accepted** фактом мержа. **Файлы:** `plans/ARCHITECTURE.md`.
- [x] **T-3156 [@PM]** — архивация фичи в `plans/archive/` с сохранением ID (после подтверждения Reviewer/Scanner/deploy/Architect). **Готово, когда:** папка перенесена, относительные ссылки разрешены. **Выполнено (Шаг 8 @PM, 23.09.2026):** папка перенесена в **`plans/archive/summary-filter-round1026/`** (UTF-8, содержимое/чекбоксы сохранены), ссылки `plans/features/…` → `plans/archive/…` разведены. **Файлы:** `plans/archive/summary-filter-round1026/`.
- [x] **T-3157 [@DevOps]** — deploy: **VERIFIED** (bump `APP_VERSION`, cache-bust) или **NOT_APPLICABLE** с обоснованием; перед деплоем — минимальные функциональные тесты §114/§107. **Готово, когда:** `deployment.md` с вердиктом и evidence. → **✅ Шаг 9 @DevOps (23.09.2026): VERIFIED** — коммиты `4cd4ae6`/`89bda3d`/`31763ab`, push `origin/master` без force, прод ff `e7170b7..89bda3d`, active, `/api/health` **200**, `APP_VERSION` 2.58.18, served `?v=2.58.18`, `database is locked`=0, `deployment.md` VERIFIED (`T-3129 deployment.md` — NOT_APPLICABLE, подготовительный блок). **Файлы:** `plans/archive/summary-filter-round1026/deployment.md`.
- [x] **T-3158 [@Memory]** — метрики S1 в `plans/metrics.md` + KG-обновление. **Готово, когда:** строка метрик заполнена фактическими данными (старт/финиш, раунды ревью, rework, evidence). → **✅ Шаг 10 @Memory (23.09.2026): `metrics.md` раздел 10.26-S1 + техдолг §71.5; KG `release-round1026-s1`/`metric-snapshot-round1026-s1-final`/`tech-debt-round10.26-s1`.** **Файлы:** `plans/metrics.md`, KG.
- [x] **T-3159 [@PM]** — продолжение: handoff @Orchestrator на **S2 `summary-context-restore`** (§90–§92; блок 0 S2 — @DevOps тег, Step 2 @Architect) сразу после приёмки S1. **Готово, когда:** следующий actionable F назван, зависимости S2 (S1) отражены. **Выполнено (23.09.2026):** `plans/backlog.md` — следующая **S2 `summary-context-restore`**; зависимость S1 → S2 отражена; **гейт S6/S10** отмечен (§71.5, ADR-1025-24 D4). **Файлы:** `plans/backlog.md` (строка S2).

## Блок H — rework по ревью Step 2b (@Architect, 23.09.2026) (T-3160…T-3161)
- [x] **T-3160 [@Builder]** — **M-1 (Medium):** главный тумблер `flags.summary_filter_enabled` — **per-chat** (спека §6.1). В `services/summary_generator.py::_run` заменить `hot.get("flags.summary_filter_enabled", …)` (`:336-337`) на `bool(await _chat_limit(chat_id, "flags.summary_filter_enabled", hot.get("flags.summary_filter_enabled", settings.SUMMARY_FILTER_ENABLED)))` (симметрично `reply_context_enabled`, `:495-498`). **Готово, когда:** per-chat override применяется (чат A ≠ чат B), OFF — байт-в-байт, есть регресс-тест; spec/каталог не меняются. **Файлы:** `services/summary_generator.py`, `tests/`, `evidence.md`. **Сделано (rework, 23.09.2026):** per-chat через `_chat_limit`; интеграционный регресс `tests/test_summary_filter_integration.py` (chat A ON ≠ chat B OFF; OFF → исходный вход); spec/каталог не тронуты. Также закрыт **L-R1026S1-1** (sentinel-нормализация потолка токенов) и **L-1/L-R1026S1-2** (интеграционные тесты). Ревью — повторно.
- [x] **T-3161 [@Architect/@PM]** — **B-1 (High, governance): закрыт** санкцией **ADR-1026-2** (AMEND ADR-1025-21 D6); из кода F8-суперсессия уже подтверждена @Reviewer/@Scanner — дополнительных правок не требуется. **Готово, когда:** AMEND-запись внесена в `adr-1026-1`/`spec.md`/`ARCHITECTURE.md §67.6`; повторное ревью T-3152 подтверждает снятие блокера. **Подтверждено (23.09.2026):** AMEND внесён; снятие блокера подтверждено @Reviewer (T-3152) и @Scanner (T-3153). **Файлы:** `adr-1026-2-*.md`, `adr-1026-1-*.md`, `spec.md`, `plans/ARCHITECTURE.md`.

## Риски
- **Потеря важного фильтром** (High) — закрывается блоком A (гарантии) + F-тесты + §89 «не считать короткий тред бесполезным».
- **Δ каталога без санкции** (Medium) — блок B стартует только после T-3130; иначе env-only.
- **Случайный заход в публикационный путь** (High, D4) — инвариант 1 + аудит T-3153 (`git diff` по публикационным модулям пуст).
- **Граница S1↔S3 по §93 размыта** (Medium) — фиксируется решением T-3130(д).
- **Ложное закрытие по «зелёной сборке»** (High, прецедент 10.20/10.22) — приёмка только по живым артефактам и evidence.
- **Live-гейт Эпика 1 (PENDING)** (Medium) — S1 безопасен (вход/алгоритм), но S6/S10 остаются за отдельным гейтом.

## Открытые вопросы к @Architect (Step 2, T-3130)
1. **`summary.filter.*`** — новые параметры каталога (группа/namespace, Δ каталога) или **env-only** (`ClassVar`, default ON, Δ каталога=0)? Параллельное хранилище §87 запрещено.
2. Подтвердить **Δ DDL = 0** (хранение — существующий каталог/PG `chat_params`, без новых таблиц/колонок).
3. **Промпт-каноны:** затрагиваются ли (§87–§89 — алгоритм, промпты не меняются); если да — `PREV_*` + ступень `PROMPT_MIGRATIONS` + эталон (ADR-1013-3) — атомарно.
4. **Форма алгоритма веса:** точная функция (сумма 4 бинарных критериев), семантика «всплеска по плотности» и «минимальной плотности всплеска», поведение при равенстве порогу.
5. **Граница S1↔S3 (§93):** «объединить темы / устранить дубли» — зона S1 (препроцессинг) или L1 Кластеризатора (S3)? Влияет на контракт фрагментов.
6. **UI:** новая группа параметров в `mod_summary` — подтвердить маршрут §85/§87 и то, что Δ каталога не требуется (метаданные в JS `MODULES`).
