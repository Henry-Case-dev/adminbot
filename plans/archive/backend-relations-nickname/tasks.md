# Задачи: backend-relations-nickname (ТЗ 9 раунда 10.4)

Раунд 10.4, фича H (T-1057…T-1065). Стадия старта — PLANNED; spec.md @Architect.
ТЗ владельца (п.9): в «Участниках и отношениях» у людей есть username, но нет
nickname, а у некоторых — ни того ни другого (только id). Разобраться в каскаде
(chat_lore.py:585-613 → nickname `_participant_names` 30 дней/top-200
smart_messages → username Bot API top-50; app.js:2335-2346 resolveRelationName)
и найти/починить причину. **НЕ ломать relations/R16** («id никогда не имя»;
имена as-is; каскад alias→nickname→username).

Отправная точка (HEAD 1410a68; рекон tma-structure-10.4):
- web/api/chat_lore.py:534-618 — GET relations (каскад имён :585-613):
  alias (AliasResolver, hot.get `limits.summary_aliases` :555-556) → nickname
  (`_participant_names` :621-637 — author_name последних 30 дней, top-200
  get_active_participants; имена — КАК ЕСТЬ, пустые/пробельные пропущены) →
  username (Bot API-обогащение `user_display_info` — ТОЛЬКО топ-50 списка,
  `_RELATIONS_ENRICH_TOP=50`, :56-61, кэш 1ч; остальным username=None,
  фото=None) → '' (key name удалён; id как имя — никогда, :607-613).
- services/user_relations.py:331-369 get_relations_snapshot: name = names[uid]
  или `_display_name(uid)` (:213) — иначе; :349-352.
- app.js:2335-2346 resolveRelationName: alias (front-shema из configItems) →
  u.name → u.username → ''; подпись @username — только если имя не из username
  (10.2-инвариант). u.name — что отдаёт сервер.
- Не-идящая гипотеза (подтверждена частично): (1) nickname-карта строится по
  окну 30д/200 строк — активные участники без сообщений в окне остаются без
  nickname; (2) username уходит всем НЕ в топ-50 = None (частичное обогащение);
  (3) `_display_name` может отдать str(uid) (снапшот-остаток) → в каскад
  попадает уже после (случай «только id»); (4) alias-путь при отсутствии
  алиасов resolve() возвращает uid → пропуск (правильный guard :597-598),
  но для «алиасов нет → nickname нет → username None» итог: '' или пустая
  карточка (с «id» мелким) — вот и «только id» у части.
- Уже сделанное (10.1/10.2): топ-50/кэш/ленивые аватары/каскад names as-is;
  «ID никогда не имя» инвариант тестируется (+1 тест 10.2).

## A. Диагностика/гипотезы [@Builder/@Architect]

- [ ] T-1057 — воспроизвести: (1) участник в users_meta, без сообщений за 30д
  (ниже top-200) НО с username в Bot API — имя пустое; (2) участник в топ-50
  activity, но без 30-д сообщений — имя = username (ок — есть);
  (3) участник вне топ-50 — username=None → имя '' — «только id».
  **AC-N1:** рекон-документ фичи: таблица-«кейс → причина → фикс» (по каждой
  из гипотез 1-4; подтверждение/опровержение - тест/логика).
- [ ] T-1058 — проверить `_display_name` (user_relations.py:213): источник
  (users_meta-имя?) и не может ли вернуть str(uid)/пробел; зафиксировать
  поведение (не ломаем «как есть» — только дефект uid-as-name устранён).
  **AC-N2:** отчёт по `_display_name` (что возвращает; случаев uid — нет;
  если есть — фикс).
- [ ] T-1059 — выборочный SQL-рекон: get_active_participants
  (services/database.py:2360+) — окно 30д/200: селекты по учаcтникам чата
  (в тестах — фикс-данные; на проде — ручной SQL @DevOps).
  **AC-N3:** чёткий ответ: «nickname-карта не покрывает X из Y участников».

## B. Фикс каскада [@Builder/@Architect]

- [ ] T-1060 — имя-источник расширить: nickname-карта = объединение
  (1) «до-30д»-окна (get_active_participants — как есть) + (2) **все**
  участники из users_meta-снапшота (RelationsService.get_relations_snapshot —
  поля name из _display_name/источников; имена as-is; пустые → пропуск) +
  (3) username-обогащение для ВСЕХ строк списка (не top-50) через существующий
  user_display_info-кэш (1ч; обосновать NFR: ≤100 вызовов при первом рендере —
  кэш и ленивая загрузка аватаров уже есть — выбрать и зафиксировать режим:
  а) обогащение всех (кэш+stagger), б) ленивое дозаполнение фронтом — решение
  @Architect с обоснованием).
  **AC-N4:** участник с username и БЕЗ сообщений в окне 30д → в списке
  показывается username (маркер в тесте); участник без того и того → '' (
  id мелким — как сейчас); «id никогда имя» — тест сохранён (R16).
- [ ] T-1061 — каскад в get_relations_snapshot: `names`-параметр готовит
  вызывающий; в API-каскаде (web/api/chat_lore.py:585-613) — порядок
  alias → nickname → username → '' без срывов (не сбрасывать пользовательский
  stage/stage_manual/note; не менять сортировку activity_score/last_seen).
  **AC-N5:** существующие тесты re-relations (test_relations_service,
  test_webapp_avatars_ui, +1 тест R16) — зелёные; diff-ограничен каскадом
  имён (stage/score/стадии не диффятся).
- [ ] T-1062 — фронт resolveRelationName: синхронизировать с новым каскадом
  (при oтсутствии u.name — username; подпись/инвариант 10.2 сохранить);
  **AC-N6:** зависимость: фича B (per-chat алиасы, каскад в web/api/chat_lore.py)
  — исполнить B ДО этой задачи/фичи (иначе двойные диффы каскада);
  маркеры app.js обновлены.

## C. Тесты/регресс [@Builder]

- [ ] T-1063 — новые юнит/интеграционные: username-only участник (вне 30д-окна),
  nickname+username участник (приоритет nickname), пустой участник (id — не
  имя), алиас-приоритет (блокирует nickname/username — R16/PWN-инвариант).
- [ ] T-1064 — прочее: полный pytest 0 failed; node --check; git diff --check;
  проверка отсутствия регресса топ-k/скорости (тесты сравнительно быстрые).
- [ ] T-1065 — live-проверка (после деплоя, @DevOps + владелец): для
  -1002661910336 и (если задан) ЛС: список участников — у каждого имя
  (username-фолбэк для неактивных), у никнейм-ов — в комментарии; ID как
  видимое имя — НИГДЕ.

**Критерии приёмки ТЗ 9 (сводные):**
- У всех участников списка есть имя (последний фолбэк-username; если нет ни
  того ни другого — пустое, но «id» НЕ заменяет имя — R16);
- Каскад: alias → nickname → username → '' (id никогда);
- не сломаны: отношения/стадии/notes/409; F-14-гейт summary_enabled-каскада;
  «Имена как есть» (без чистки) — 10.2-инвариант;
- REGISTRY 383/71/359; SQLite v8 — без дифов; до-прод → полный pytest.
