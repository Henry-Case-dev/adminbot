# S2 — `summary-context-restore-round1026` (Эпик 2 «Summary Hybrid Pipeline», Раунд 10.26)

> **ТЗ:** `plans/current_task.md` §80 (цель/пайплайн), **§90–§92** (ядро), §89 (тонкая настройка: «Количество ближайших контекстных сообщений», «Лимит восстановленного контекста»), §93 (лимит/фрагменты, «не отрезать молча»), §96 (пакет фактов — только граница, не реализация), §107/§114 (запуск после деплоя). `spec.md`/ADR — **@Architect (Step 2)**, PM их не создаёт.
> **Тип:** backend/алгоритм (детерминированное достраивание контекста, **0 LLM-вызовов**). P0.
> **Зависит от:** **S1 `summary-filter-round1026`** (✅ COMPLETED + MERGED §71 + DEPLOYED 2.58.18). Потребляет `FilterResult.kept`/`dropped`.
> **Статус:** ✅ **COMPLETED + MERGED (§73) + ARCHIVED** (Шаг 8 @PM, T-3249, 23.09.2026). Блоки 0/A–H ✅ (блок H/T-3244 — **NOT_APPLICABLE**, Δ каталога=0); @Reviewer **Approved** (T-3245); @Scanner **C0/H0/M0/L2/I2 → «к деплою ДА»** (T-3246); Merge §73 @Architect (T-3248). Числа: pytest `.venv` **8569/0**, JS **43/43**; **Δ DDL=0**, **Δ каталога=0**; публикация/промпты/XML вне diff (D4); ровно 2 LLM-вызова. **✅ deploy — Шаг 9 @DevOps (T-3250) VERIFIED; метрики — Шаг 10 @Memory (T-3251) ✅; live — `[ ]` PENDING OWNER VERIFICATION.** `APP_VERSION` **2.58.21** (bump @Builder; закоммичен/задеплоен — `eda7325`/`6fa456c`/`989ff8d`).
> **Baseline (Step 0 @Memory, HEAD `7895e77`):** `APP_VERSION` **2.58.20**, pytest `.venv` **8525/0**, JS **43/43**, каталог **467/426/442/100/98/21**, **Δ DDL=0**, CSP/zero-build, R17/R18.
> **ID:** **T-3224…T-3252 (29)** — дублей нет (максимум занятого — **T-3223**, правка владельца визуального эпика; визуальный эпик — T-3162…T-3223).
> **Не трогать:** `plans/current_task.md` (source of truth, только чтение), `plans/metrics.md`/`plans/MEMORY.md` (ведут @Memory).

## Трассируемость (REQ → verbatim §ТЗ → блок)
| REQ | Источник (verbatim, `current_task.md`) | Блок | Задачи |
|---|---|---|---|
| REQ-S2-01 | §80 «Шестичасовой лог → Алгоритмический фильтр → **Восстановление контекста** → L1 — Кластеризатор → … Не переписывать работающий механизм публикации» | A, C, G | T-3227, T-3233, T-3243 |
| REQ-S2-02 | §90 «Найти сохранённые сообщения. **Определить reply_to.** Добавить необходимые родительские сообщения» | A | T-3227, T-3228 |
| REQ-S2-03 | §90 «**Добавить ограниченный ближайший контекст**» | B | T-3230, T-3231 |
| REQ-S2-04 | §90 «**Сохранить хронологию. Удалить дубли. Сохранить исходные message_id**» (+ §90 пример 100→101→102) | C | T-3232, T-3233 |
| REQ-S2-05 | §90 «Нельзя оставлять только сообщение 102. Но и нельзя возвращать весь шестичасовой лог. **Контекст должен быть ограничен**» | A, B, E | T-3227, T-3230, T-3231, T-3238 |
| REQ-S2-06 | §91 «Короткие важные треды. Длинные бессодержательные сообщения. Параллельные разговоры. Упоминания. reply_to. Медиа. Подписи. Транскрипты. Сообщения самого бота» | D | T-3234…T-3237 |
| REQ-S2-07 | §91 «**Не включать предыдущие Саммари как новые события** следующего Саммари» | D | T-3237 |
| REQ-S2-08 | §92 «Передавать структурированные сообщения: message_id, chat_id, timestamp, author_id, display_name, text, reply_to_id, mentions, message_type… **Использовать только реальные доступные поля. Не выдумывать отсутствующие метаданные. Сохранять корректную идентификацию авторов**» | F | T-3240, T-3241 |
| REQ-S2-09 | §89 «Сохранение контекста ответов. Количество ближайших контекстных сообщений. **Лимит восстановленного контекста**» | A, B, E | T-3229, T-3230, T-3238 |
| REQ-S2-10 | §93 «Оценивать токенный объём… **Не отрезать последние сообщения молча**» | E | T-3238, T-3239 |
| REQ-S2-11 | §107 «Алгоритмический фильтр включён по умолчанию… Не оставлять фичу выключенной… перед деплоем выполнить минимальные функциональные тесты» | G, I | T-3242, T-3243, T-3250 |
| REQ-S2-12 | §108/§109 «FILTER_COMPLETE: … **Сохранённые** … **Восстановленных из контекста** …» (`restored_count` заполняет S2) | A, I | T-3228, T-3251 |
| REQ-S2-13 | §85/§87 (маршрут) «Модули → Сводки чатов → **Подготовка сообщений**» | H | T-3244 |

## Приёмочные инварианты S2 (нарушение = НЕ принято)
1. **S2 НЕ меняет публикацию, промпты и XML** — `generate_image`/`build_cover_media`/`send_rich_message`/fallback `sendMessage`, `services/summary_prompts.py`, `services/summary_xml.py` **вне diff** (**D4, ADR-1025-24**). Любой дифф этих модулей = блокер → СТОП.
2. **Ровно 2 физических LLM-вызова** (ADR-1022-3/4/5). S2 — детерминированный pre-LLM этап, **0 новых LLM-вызовов**.
3. **Переиспользовать `services/thread_chain.py`** (`collect_thread_chain`/`ChainItem`/`format_chain_line`/`render_reply_chains`, F6/ADR-1023-2) — **вторую цепочку реплаев не создавать**.
4. **Δ DDL = 0** жёстко. **Δ каталога = 0** — S2 **не требует новых параметров** (ключи §89 заведены S1: `limits.summary_filter_context_neighbors`, `limits.summary_filter_context_max_messages`, `flags.summary_filter_reply_context_enabled`). Новый параметр — только по письменной санкции @Architect (Step 2).
5. **Детерминизм:** один и тот же вход (+инъекция `now`) → одинаковый восстановленный набор; порядок — единая хронология; повторный прогон байт-идентичен.
6. **Сохранение идентичности:** исходные Telegram-`message_id`/`tg_message_id` и DB-`id` не подменяются; `reply_to_id` сохраняется для L1 (§92).
7. **Fail-open:** ошибка восстановления не роняет Саммари (WARNING + контекст из фильтра, без тихой потери).
8. **CSP/zero-build, R17/R18** (в логах — только числа/коды/id, без ключей и содержимого сообщений).
9. **Гейт S6/S10 закрыт** (ADR-1025-24 D4): публикация/hybrid не открываются; live-приёмка Эпика 1 — PENDING OWNER VERIFICATION.

## Блок 0 — baseline, точка отката, Step 2 @Architect (T-3224…T-3226)
- [x] **T-3224 [@DevOps]** — зафиксировать baseline и точку отката S2: подтвердить HEAD `7895e77`, `APP_VERSION` 2.58.20, pytest 8525/0, JS 43/43, каталог 467/426/442/100/98/21; создать annotated-тег **`pre-round1026-s2`** и бэкап; записать точную команду откатa. **Готово, когда:** тег создан и запушен, в evidence — `git rev-parse pre-round1026-s2`, baseline-числа, команда rollback и путь бэкапа. **Файлы:** git tag + backup + `plans/archive/summary-context-restore-round1026/evidence.md`.
- [x] **T-3225 [@Architect, Step 2]** — `spec.md` + ADR по S2 и **санкции** по открытым вопросам (ниже): контракт «восстановленного» набора S1→XML; граница §93 S2↔S3; семантика `restored_count`; тип/группа параметров (Δ каталога); задействован ли промпт-канон (если да — `PREV_*` + ступень `PROMPT_MIGRATIONS` + эталон, ADR-1013-3); нужен ли UI-контроль; deploy+bump 2.58.20→2.58.21. **Готово, когда:** контракты/сценарии зафиксированы, решения — в ADR; без санкции новые параметры/DDL/каталог не вводятся. **Файлы:** `plans/archive/summary-context-restore-round1026/spec.md` (+ ADR). ✅ `spec.md` + **ADR-1026-4** (D1–D7) — Accepted фактом Merge §73; Δ каталога=0/D1–D7.
- [x] **T-3226 [@PM]** — после Step 2 сверить `tasks.md` ↔ `spec.md` (контракты/сценарии/REQ) и при расхождении запросить правку @Architect, не позволяя @Builder изобретать решение. **Готово, когда:** каждый REQ-S2-01…-13 привязан к сценарию спеки, расхождений нет. **Файлы:** этот `tasks.md`. ✅ REQ-S2-01…-13 ↔ SC-01…SC-17 в `spec.md` §3/§12 — расхождений нет.

## Блок A — ядро восстановления §90: reply-родители (T-3227…T-3229)
- [x] **T-3227 [@Builder]** — новый модуль восстановления контекста (напр. `services/summary_context_restore.py`): чистые функции `restore_context(kept, dropped, window, params) -> RestoreResult` (без LLM/БД/сети). Для каждого сохранённого сообщения определить `reply_to_id` и подтянуть **необходимые родительские** сообщения, включая **транзитивную цепочку** (пример §90: 100→101→102 — родители по всей цепочке, не весь шестичасовой лог). **Переиспользовать `services/thread_chain.py`** (`collect_thread_chain`) — вторую не плодить. **Готово, когда:** кейс 100/101/102 восстанавливает 100 и 101 к 102; модуль импортируется без сайд-эффектов; покрыт unit-тестами. **Файлы:** `services/summary_context_restore.py`, `tests/`.
- [x] **T-3228 [@Builder]** — заполнить `restored_count` (в S1 = 0) фактическим числом добавленных сообщений; вернуть аддитивную структуру, читаемую §108/§109 (S7) и `_apply_filter`-контуром, **без** второй системы визуализации и без узлов ExecutionGraph (kind `algorithm` эмитит S8). **Готово, когда:** счётчики `source/saved/restored` согласованы, доступны аддитивно, UI/адаптер не тронуты. **Файлы:** `services/summary_context_restore.py`, `services/summary_generator.py`, `tests/`.
- [x] **T-3229 [@Builder]** — «сохранение контекста ответов» §89 через существующий ключ `limits.summary_filter_context_neighbors` (=1, per-chat): число ближайших контекстных сообщений вокруг reply-цепочки; поведение при OFF (`flags.summary_filter_reply_context_enabled=false`) — reply-родители не добавляются. **Готово, когда:** резолв global→chat→код-дефолт работает, счётчик соседей соблюдается, покрыто тестами. **Файлы:** `services/summary_context_restore.py`, `services/summary_generator.py`, `tests/`.

## Блок B — ограниченный соседний контекст (T-3230…T-3231)
- [x] **T-3230 [@Builder]** — жёсткий cap `limits.summary_filter_context_max_messages` (=50) на общий восстановленный набор; детерминированный приоритет отбора (сначала необходимые reply-родители, затем ближайший контекст) — при превышении лишнее отбрасывается **по явному правилу**, а не случайно. **Готово, когда:** набор никогда не превышает cap; тест «cap=50 при 200 кандидатах». **Файлы:** `services/summary_context_restore.py`, `tests/`.
- [x] **T-3231 [@Builder]** — «ограниченный ближайший контекст» §90: доборы хвоста временного всплеска (соседи по хронологии вокруг ключевых сообщений) в пределах `context_neighbors`/cap; **не** разворачивать весь шестичасовой лог. **Готово, когда:** тест «всплеск → добавлены ближайшие, не весь лог»; границы проверены. **Файлы:** `services/summary_context_restore.py`, `tests/`.

## Блок C — хронология, дубли, идентичность ID (T-3232…T-3233)
- [x] **T-3232 [@Builder]** — единая ASC-сортировка восстановленного набора по `(timestamp, id)` (детерминированный тай-брейк) с сохранением исходной хронологии §90; дедупликация пересечений `kept` ∩ `restored` ∩ `dropped` (в т.ч. `overlap_message_ids` из `Fragment`). **Готово, когда:** двойной прогон байт-идентичен, дублей нет, порядок хронологический. **Файлы:** `services/summary_context_restore.py`, `tests/`.
- [x] **T-3233 [@Builder]** — сохранить **исходные** `message_id`/`tg_message_id` и DB-`id` без подмены; `reply_to_id` (Telegram-id родителя) проставить для L1 (§92); поля строки окна (`id`, `tg_message_id`, `reply_to_id`, `user_id`, `text`, `timestamp`, `media_type`, `author_name`; поля `mentions` в окне нет) — не выдумывать. **Готово, когда:** тест подтверждает неизменность ID и корректную привязку reply. **Файлы:** `services/summary_context_restore.py`, `tests/`.

## Блок D — спец-случаи §91 (T-3234…T-3237)
- [x] **T-3234 [@Builder]** — короткие важные треды (не терять) и длинные бессодержательные (не раздувать контекст) — приоритеты восстановления по правилу, согласованному со `spec.md`. **Готово, когда:** оба кейса различаются тестом. **Файлы:** `services/summary_context_restore.py`, `tests/`.
- [x] **T-3235 [@Builder]** — параллельные разговоры (не склеивать несвязанные ветки) и упоминания (учёт доступных метаданных; `mentions` в окне отсутствует — не выдумывать). **Готово, когда:** тест «два параллельных треда не перемешиваются». **Файлы:** `services/summary_context_restore.py`, `tests/`.
- [x] **T-3236 [@Builder]** — медиа/подписи/транскрипты: сохранять сообщения с медиа (`media_type`) и текст подписей/транскриптов как часть контекста; пустой текст при медиа — не повод дропнуть родителя. **Готово, когда:** кейсы «медиа без текста», «подпись», «транскрипт» покрыты тестами. **Файлы:** `services/summary_context_restore.py`, `tests/`.
- [x] **T-3237 [@Builder]** — сообщения бота и **предыдущие Саммари не считать событиями** нового Саммари (§91): не восстанавливать как родительский контекст, согласованно с семантикой S1. **Готово, когда:** тест «прошлое Саммари/бот-ответ не попадает восстановленным событием». **Файлы:** `services/summary_context_restore.py`, `tests/`.

## Блок E — бюджет §89/§93: cap'ы, токенный лимит, «не резать молча» (T-3238…T-3239)
- [x] **T-3238 [@Builder]** — оценка токенного объёма восстановленного набора через существующие потолки `limits.summary_max_context_tokens`/`_chars` (`resolve_context_tokens`/`resolve_chat_limit`) — **без новых ключей**; детерминированное решение «влезает / не влезает». **Готово, когда:** оценка не зависит от сети/LLM, границы (ровно лимит / лимит+1) проверены. **Файлы:** `services/summary_context_restore.py`, `tests/`.
- [x] **T-3239 [@Builder]** — «не отрезать последние сообщения молча» §93: любое усечение — явный статус + лог (не более чем числовые/id-поля, R17); при переполнении сохраняются последние сообщения/связи. **Готово, когда:** тест «усечение всегда логируется со статусом», последние сообщения не теряются молча. **Файлы:** `services/summary_context_restore.py`, `tests/`.

## Блок F — подготовка данных для L1 §92 (T-3240…T-3241)
- [x] **T-3240 [@Builder]** — сформировать структурированный вход для L1 с полями §92 (`message_id`, `chat_id`, `timestamp`, `author_id`, `display_name`, `text`, `reply_to_id`, `mentions`, `message_type`) — **только фактически доступные** (переиспользовать имеющиеся поля строки окна), отсутствующие **не выдумывать**; `chat_id` брать из контекста запуска. **Готово, когда:** сериализация детерминирована, отсутствующие поля не заполняются фиктивными значениями, покрыто тестами. **Файлы:** `services/summary_context_restore.py`, `services/summary_generator.py` (только подготовка входа), `tests/`.
- [x] **T-3241 [@Builder]** — корректная идентификация авторов §92: `author_id`/`display_name` из существующих источников (`user_id`/`author_name`), без подмены и без выдуманных имён. **Готово, когда:** тест на разных авторов в одном треде. **Файлы:** `services/summary_context_restore.py`, `tests/`.

## Блок G — тесты / интеграция (T-3242…T-3243)
- [x] **T-3242 [@Builder]** — unit-матрица: детерминизм (двойной прогон), fail-open (инъекция ошибки восстановления → рабочее Саммари), OFF байт-в-байт (`flags.summary_filter_reply_context_enabled=false` → вход без восстановления), граничные (пустой `dropped`, нет `reply_to_id»). **Готово, когда:** каждый REQ-S2-02/03/04/07/10 имеет ≥1 тест, зелёные. **Файлы:** `tests/test_summary_context_restore.py`.
- [x] **T-3243 [@Builder]** — интеграция в `services/summary_generator.py` (врезка после `_apply_filter`, до `summary_xml.build`): XML получает восстановленный набор; RAG/граф/память — **исходные rows** (как в S1); ровно **2 LLM-вызова**; A/B per-chat (`flags.summary_filter_enabled`); публикация/промпты/XML вне diff. **Готово, когда:** интеграционные тесты зелёные, полный pytest не хуже baseline 8525/0, JS 43/43. **Файлы:** `services/summary_generator.py`, `tests/`.

## Блок H — UI «Подготовка сообщений» (T-3244)
- [x] **T-3244 [@Builder]** — **NOT_APPLICABLE** (ADR-1026-4 D6/D7, spec §10): S2 не вводит новых параметров — §89-ключи `SUMMARY_FILTER_CONTEXT_NEIGHBORS`/`SUMMARY_FILTER_CONTEXT_MAX_MESSAGES` уже рендерятся в существующей группе «Тонкая настройка» вкладки «Подготовка сообщений» (`mod_summary`); Δ каталога=0, Δ JS `MODULES`=0, новых API нет. UI-контроль на маршруте §85/§87 — **только если нужно** по решению @Architect (T-3225) и **только через существующий механизм config-items/`persistItems` F0** (одна серверная мутация на операцию), без новых API и **без Δ каталога** (метаданные в JS `MODULES`); иначе — блок закрывается как NOT_APPLICABLE с обоснованием. **Готово, когда:** сохранение/перечитывание подтверждено, per-chat изоляция не нарушена, CSP/zero-build сохранены, Δ каталога=0. **Файлы:** `web/index.html`, `web/app.js` (+ `web/static/app.css` при необходимости).

## Блок I — ревью / аудит / merge / архив / deploy / метрики / продолжение (S3) (T-3245…T-3252)
- [x] **T-3245 [@Reviewer]** — ревью S2: соответствие §90–§92, отсутствие ложных закрытий, «контекст ограничен и не теряет важное». **Готово, когда:** `review.md` с вердиктом (итерации по необходимости). **Файлы:** `plans/archive/summary-context-restore-round1026/review.md`. ✅ **Approved** (блокеров нет; live — PENDING OWNER VERIFICATION).
- [x] **T-3246 [@Scanner]** — независимый аудит: **Δ DDL=0**, **Δ каталога=0** (или санкция), R17/R18, CSP/zero-build, **публикация/промпты/XML вне diff (D4)**, ровно 2 LLM-вызова, отсутствие новых внешних зависимостей. **Готово, когда:** отчёт с C/H/M/L/Info. **Файлы:** `plans/reports/round1026_s2_scanner_audit.md`. ✅ **C0/H0/M0/L2/I2 → «к деплою ДА»** (L-R1026S2-1/-2 — owned follow-up).
- [x] **T-3247 [@Builder]** — rework по findings @Reviewer/@Scanner. **Готово, когда:** все блокеры закрыты, повторный прогон зелёный. **Файлы:** по findings. ✅ **rework не требовался** — блокеров 0 (только Low L-R1026S2-1/-2, non-blocking, техдолг §73.5).
- [x] **T-3248 [@Architect]** — merge/сверка архитектуры: раздел в `plans/ARCHITECTURE.md` + финализация ADR S2. **Готово, когда:** раздел обновлён, ADR Accepted. **Файлы:** `plans/ARCHITECTURE.md`. ✅ **Merge §73** выполнен (ADR-1026-4 Accepted фактом мержа).
- [x] **T-3249 [@PM]** — архивация фичи в `plans/archive/` с сохранением ID (после подтверждения Reviewer/Scanner/deploy/Architect). **Готово, когда:** папка перенесена, относительные ссылки разрешены. **Файлы:** `plans/archive/summary-context-restore-round1026/`.
- [x] **T-3250 [@DevOps]** — deploy: bump `APP_VERSION` **2.58.20 → 2.58.21** (cache-bust) или **NOT_APPLICABLE** с обоснованием; перед деплоем — минимальные функциональные тесты §114/§107; push без force, прод ff, `/api/health` 200, served `?v=`, `database is locked`=0. **Готово, когда:** `deployment.md` с вердиктом и evidence. **Файлы:** `config/settings.py`/`README.md`/`web/index.html` (bump), `plans/archive/summary-context-restore-round1026/deployment.md`. ✅ **Шаг 9 — VERIFIED** (`eda7325`/`6fa456c`/`989ff8d`, 2.58.21, health 200, served `?v=2.58.21`, `database is locked`=0; `deployment.md` VERIFIED).
- [x] **T-3251 [@Memory]** — метрики S2 в `plans/metrics.md` (раздел 10.26-S2) + KG-обновление. **Готово, когда:** строка метрик заполнена фактическими данными (старт/финиш, раунды ревью, rework, evidence, `restored_count`). **Файлы:** `plans/metrics.md`, KG. ✅ **Шаг 10 — выполнен @Memory** (метрики 10.26-S2 + KG `release-round1026-s2`/`metric-snapshot-round1026-s2-final`/`tech-debt-round10.26-s2`).
- [x] **T-3252 [@PM/@Orchestrator]** — продолжение `current_task.md`: handoff @Orchestrator на **S3 `summary-l1-clusterizer`** (§94–§95). **Готово, когда:** следующий actionable F назван, зависимости S3 (S1, S2) отражены, гейт S6/S10 отмечен. **Файлы:** `plans/backlog.md`, `plans/workflow_state.md`. ✅ Следующий actionable F — **S3 `summary-l1-clusterizer`** (§94–§95); зависимости S1/S2 отражены; гейт S6/S10 закрыт (D4).

## Риски
- **Потеря reply-контекста** (§90; родители вне окна, сквозь бот-ответы) (Critical) — закрывается блоком A (reuse `thread_chain`) + тесты G.
- **Дубли/порядок** (High) — блок C (единая ASC-сортировка + дедуп) + тест двойного прогона.
- **Выход за лимиты** (`context_neighbors`/`context_max_messages`/токены) (High) — блоки B/E + детерминированный приоритет отбора.
- **Случайный заход в публикацию/промпты/XML** (High, D4) — инвариант 1 + аудит T-3246 (`git diff` пуст).
- **Ложное закрытие по «зелёной сборке»** (High, прецедент 10.20/10.22) — приёмка только по живым артефактам и evidence.
- **New параметр без санкции** (Medium) — блок 0 T-3225; иначе env-only/существующие ключи S1.

## Открытые вопросы к @Architect (Step 2, T-3225) — ✅ ЗАКРЫТЫ
> **Решения зафиксированы в `spec.md` (Step 2, 23.09.2026) и ADR-1026-4 (D1–D7), Accepted фактом Merge §73.** Ниже — исходная постановка вопросов (историческое, для трассируемости).
1. **Точный контракт «восстановленного» набора между S1 и XML:** что именно передаётся дальше (`RestoreResult` → `summary_xml.build`): только строки-сообщения, или сообщения + карта reply-связей? Меняется ли подпись/вход `summary_xml.build` (инвариант: XML-модуль не менять внутри)?
2. **Границы §93 (S2 vs S3):** «оценивать токенный объём / перекрывающиеся фрагменты / объединить темы» — что из этого S2 (детерминированное достраивание) и что S3 (Кластеризатор)? S2 не должен заниматься семантикой тем.
3. **Как считать `restored_count`:** число добавленных сообщений (только reply-родители + ближайший контекст) или включая дедуплицированные? Единая семантика с §108/§109.
4. **Нужен ли UI-контроль** (блок H) для S2, или ключи §89, уже заведённые S1, достаточны и UI не требуется (NOT_APPLICABLE)?
5. **Новые параметры:** требуется ли что-то сверх `context_neighbors`/`context_max_messages`/`reply_context_enabled` (тогда — санкция Δ каталога), или S2 обходится **Δ каталога=0**?
6. **Промпт-канон:** затрагивается ли (ожидаемо нет — вход/алгоритм); при «да» — `PREV_*` + ступень `PROMPT_MIGRATIONS` + эталон (ADR-1013-3) атомарно.
7. **Deploy + bump:** подтвердить `2.58.20 → 2.58.21` и то, что S2 деплоится без открытия гейта S6/S10 (публикация не меняется).
