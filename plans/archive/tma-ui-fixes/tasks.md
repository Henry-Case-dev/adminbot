# Задачи: tma-ui-fixes

Раунд 10 (07.09.2026), **часть 3.2 эпика** «Multi-chat scaling» (research `plans/docs/multi-chat-scaling-research.md`, вариант A). Нумерация продолжает T-869 → **T-870…T-877**; база HEAD fac1b9f. Скоуп — **только UI/UX-фиксы TMA (web/app.js + web/index.html + тесты-аудит)**, без изменений схемы/API-контрактов (кроме переезда поля в другую вкладку — данные в БД не трогаем); дизайн-протокол @Architect НЕ требуется (список фиксов от владельца закрыт, нет открытых вопросов — при необходимости уточнения в T-870). Связанные части раунда: `multi-chat-rbac-byok` (T-843…T-869), `permsoc-module-isolation` (T-878…T-889), `feature-gates-worker-budget` (T-890…T-902), `tma-ia-progressive-disclosure` (T-903…T-913), `global-oversight-dashboard` (T-914…T-924).

## A. Шапка и логи

- [x] **T-870 (A1):** выравнивание шапки (flexbox): header/панель навигации — `display: flex; align-items: center; gap` вместо float/absolute/inline-костылей (проверить текущ. CSS web/index.html); пункты: заголовок/лого, активная вкладка, чат-бейдж; проверка на узком экране (380 px) — перенос/скролл; без изменения разметки данных. (web/index.html, web/app.js при правках класса)
- [x] **T-871 (A2):** логи (вкладка «Статус»/debug-зона): контейнер логирования — `<pre><code>` моно-шрифт 0.75rem, `white-space: pre-wrap; word-break: break-all;` компактный скролл (фикс-высота, thin-scrollbar, автоскролл к низу), строки-записи меньше вертикальных отступов; сохранить ring-синглтон/фолбэк-аптайм (исправления 01.09 — НЕ ломать). (web/index.html, web/app.js)

## B. «Лор чатов» — отношения и титулы

- [x] **T-872 (B1):** relations-список: резолв имени и аватара участника: имя — приоритет Alias (`summary_aliases`) → nickname → username → (фолбэк) user_id; аватар — существующий прокси `/api/avatar` (bdd5e89) с фолбэком-инициалом; заменить «голые» user_id в строках (если есть); длина строки — компакт. (web/app.js методы loadRelations, web/index.html, прецедент bdd5e89)
- [x] **T-873 (B2):** титулы вместо chat_id: в списке/карточках «Лор чатов» (и других местах с «-100…» в заголовках) — имя чата (из локального кэша чатов бота/названия) + `chat_id` мелким шрифтом или сноской; фолбэк — старый вид id. (web/app.js, web/index.html)
- [x] **T-874 (B3):** убрать длинное тире перед «авто»: заменить текстовые «— авто»/«— автоматически» на компактный чип-бейдж «авто» (цветовой отличительный стиль, с прежним цветом); затронутое: авто-лор лента, истории, relations (без изменения смысла данных). (web/index.html, web/app.js)
- [x] **T-875 (B4):** relations-дропдаун стадии — компактный (уменьшить высоту/ширину, короткие метки: stranger…veteran RU, placeholder), поле «заметка»/note — шире (textarea с большей шириной/авто-ростом, `min-width`, стилизация тёмной темы). (web/index.html, web/app.js)

## C. Доступы

- [x] **T-876 (C1):** перенос «Telegram ID админа» из «Реакции и Триггеры» → «Доступы»: (1) rg/аудит по app.js/index.html: найти поле и его привязку (какой ключ/метод — ID админа для реакций или роль-доступа?); (2) перенести блок в вкладку access (структура и рендер), данные/ключ БД НЕ меняются (перенос только украшательный; значения в bot_settings остаются как есть); обновить тест-аудит вкладок/маркеров при необходимости. (web/index.html, web/app.js)

## D. Регресс

- [x] **T-877 (D1):** фронт-аудит (test_frontend_tab_mapping.py: маркеры — flex-класс шапки, `pre code`, чип «авто», relations-резолв/аватар, поле в Доступах; отсутствие «Telegram ID админа» в reactions-ветке); полный pytest → 0 failed; `git diff --check` чист; ручная сверка страниц (de)сктоп/мобайл — в отчёте H? (без H-секции: фиксы без деплоя-плана — финальный шаг общий: деплой раунда 10 выполняет @DevOps в последней фиче раунда, здесь только регресс). (tests/, web/)
