# Раунд 4: betterstack-own-handler-video-memory-cmds

Планирование раунда 4 (04.09.2026, продолжает нумерацию T-705 → T-706…T-726; база 3088816, дерево: `M plans/docs/memory-project-overview.md` + `?? migrate_history/` — см. T-723). ПЯТЬ блоков.

ПРОБЛЕМА 1 — BetterStack: hotfix 30.08 уже стоит (bot.py:626-634 `log_config=None`; uvicorn 0.52.4 при None не трогает root-логгер — проверено по исходникам). Реальной причины «логи не идут» в КОДЕ не видно; logtail-python 0.4.0 — чёрный ящик (handler.py: `raise_exceptions=False`, `drop_extra_events=True`, `pipe = Queue(maxsize=1000)` — при Full молчаливый `dropcount += 1` и БЕЗ логирования, uploader-ошибки только в Fake500, emit-исключения глотаются); формат payload-фрейма logtail (frame.py): `{dt: ISO UTC, level: нижний, severity: levelno/10, message, context: {runtime: {function, file, line, thread_id, thread_name, logger_name}, system: {pid, process_name}}, +extra}`; POST `https://in.logs.betterstack.com` (или host) с `Authorization: Bearer <token>`, Content-Type msgpack (JSON-фрейм BetterStack тоже принимает — решение в spec). Плюс: aiogram.event логирует КАЖДЫЙ апдейт на INFO («Update id=… is/not handled», aiogram 3.31 dispatcher.py:174-185) → спам в панели; уровня aiogram.event НИКТО не задаёт.

ПРОБЛЕМА 2 — видео (research готов): кандидаты-роуты (а) minimax-m3:free (GMICloud) и (б) google/gemma-4-31b-it:free (AI Studio — прямые .mp4 URL НЕ поддерживает, только YouTube) — video_url заявлен в каталоге, но free-роуты не гарантируют доставку; ОБЕ модели НЕ имеют аудио-модальности (видео = кадры; gemma-31b ≤60с@1fps). Из free-моделей с audio+video на OR — только nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free (нужна live-проверка). Free-лимиты OR: 20 rpm, 50/1000 req/день. Симптомы: (а) отказные ответы моделей («не вижу видео», «can't see…» и т.п.) проходят как успех L1/L2 и уходят юзеру; (б) если модели реально не получают видео (free-роуты) — пустой вход → галлюцинации/пусто; (в) даже успешный визуал НЕ слышит звук (нет аудио-модальности). Текущий пайплайн: publish → L1/L2 video_url → при пустоте STT → <100 симв → пул 5.13 «разборчивой речи нет».

ПРОБЛЕМА 3 — команды памяти «запомни/забудь» в direct_chat. Существующее: /forget (FTS-prefix по bot_direct_reply своего target_user), /clear, protected_facts (таблица есть, в проде пуста; в контекст direct_chat подмешиваются как <protected_facts>), memorize_facts — автоматический. RBAC: чат сейчас использует ТОЛЬКО settings.ADMIN_USER_ID; PG-роли bot_admins (5885953495 admin; 1313107079, 134812796 moderator) используются только в TMA. ConfigCache API: `get_role(telegram_id) -> str|None`, `roles()`, `admins()`, `admins_full()` (services/config_cache.py:234-256); bot_admins: telegram_id/role_name/added_by (pg_db.py:51-59). Тумблеры «Реакции и Триггеры»/флаги живут в hot-конфиге (param_catalog.py).

ПРОБЛЕМА 4 — промпт-баг TMA: фронт web/app.js saveConfigItem (536-566) — `if (value === null || value === '' || isNaN(value))` (:550) применяет isNaN ко ВСЕМ полям, а `isNaN('текст') === true` → ложный toast «Некорректное значение для …» (:551) для str-полей (промпты/контент); сервер (web/api/routes.py) для str принимает всегда. Пустая строка для prompts/content — сервер отклоняет (422 «промпт не может быть пустым», _coerce/post_config) — тост/422: решение @Architect (T-719).

БЛОК 5 — research/процесс: импорт истории (3 JSON Telegram Desktop export, ~602МБ суммарно, ~1.27M сообщений, ТРИ РАЗНЫХ чата: 2417005237 «10.2024.json» окт24-мар25; 2661910336 «10.08.2025.json» апр25-авг25 — юзер просил именно его; 1691371902 «желтая до 10.2024.json» дек22-окт24 — юзер, вероятно, считает их одним — отметить в отчёте), формат `{id,type,date,date_unixtime,from,from_id,text,text_entities}`; `.gitignore` migrate_history ОТСУТСТВУЕТ (сейчас `?? migrate_history/` в дереве — риск коммита 600МБ!); дат/имён в RAG-фактах НЕТ (голый текст; в БД created_at есть; даты с именами — только в L1-инструменте query_chat_memory «[Имя 2024-05-15]: текст») → «что было N-числа» через RAG сейчас НЕ работает.

**Статус:** archived (выполнено 2026-09-04, 3794 passed, 0 failed). Аппрув @Reviewer; архитектура — `ARCHITECTURE.md` §16. Вне архива (исполняет @DevOps финальным шагом в T-726): правило процесса G1 → plans/project.md, README-правки, коммит всего, пуш, деплой и live-верификация.

## A. Дизайн

- [x] T-705 (A1): @Architect spec.md (plans/features/betterstack-own-handler-video-memory-cmds/spec.md) — дизайн ВСЕГО ДО реализации, по пунктам B-G: (B) BetterStackHandler — формат POST (JSON-фрейм-совместимость с logtail frame.py: dt/level/severity/message/context.runtime{function,file,line,thread_id,thread_name,logger_name}/context.system{pid,process_name}; Bearer-заголовок; JSON vs msgpack), размер буфера/интервал флашера, анти-рекурсия с log_ring-фильтром, журнал ошибок ≤1/60с, счётчики, тест-событие, flush на shutdown; (C) видео — список маркеров отказных ответов (RU/EN, «не вижу», «не могу просмотреть», «no video», «can't see», «don't have access», «недоступно» + порог мин-длины N), стратегия «пустой уровень → каскад L2/субтитры/STT», РЕШЕНИЕ: промпт-маркер VIDEO_UNAVAILABLE (правка канона YOUTUBE_VIDEO_SYSTEM_PROMPT + PG hot-миграция как с CHAT_SYSTEM_PROMPT) ИЛИ только маркерный детект без правки промпта; (D) память-команды — хранилище «запомни» (graph_facts origin 'user_memory' + CHECK-миграция v5 по D201-паттерну vs protected_facts), TTL, семантика «забудь» (scope чат/свои, min-length, защита protected_facts), фразы-подтверждения, RBAC-матрица (админ/модер/юзер × тумблер flags.memory_commands_user_enabled); (E) промпт-баг — серверный 422 пустых prompts/content в routes.py _coerce/post_config vs фронт-тост; (F) даты в RAG-фактах — какие origin получают [дата]-префикс; (G) состав финального коммита. (plans/features/betterstack-own-handler-video-memory-cmds/) *(закрыта при архивации 04.09.2026 — spec создан и одобрен @Reviewer, лежит рядом в plans/archive/betterstack-own-handler-video-memory-cmds/spec.md)*

## B. BetterStack (проблема 1)

- [x] T-706 (B1): собственный BetterStackHandler (новый модуль services/, замена LogtailHandler в bot.py:104-116 basicConfig): logging.Handler; HTTP POST на in.logs.betterstack.com (https://in.logs.betterstack.com/{token}, Authorization: Bearer) с payload-совместимостью logtail frame.py (поля dt/level/severity/message/context — см. А/контекст выше; эталон — .venv/Lib/site-packages/logtail/{handler,frame,uploader}.py); буфер + фоновый флашер (переиспользовать механику logtail: thread + flush()), НО с наблюдаемостью вместо тихих потерь: счётчики (sent/dropped/errors/bytes), ЯВНЫЙ лог ошибок отправки в journald на WARNING не чаще 1/60с (анти-спам) и лог дропов при полном буфере; стартовое тест-событие «[betterstack] attached | token_len=N | handler=own-v1» (без токена — «[betterstack] skipped»); flush при shutdown (путь SIGTERM); без рекурсии через log_ring (фильтр log_ring.py:88-91). (bot.py, services/, services/log_ring.py) *(реализовано: services/betterstack_handler.py, spec 3.1)*

- [x] T-707 (B2): уровень aiogram.event = WARNING (после basicConfig в bot.py; гасит INFO-спам «Update id=… is/not handled» aiogram 3.31 dispatcher.py:174-185 — событий на уровне WARNING в нём нет); .env.example — комментарий к BETTERSTACK-токену/вкладке + поведение нового хендлера; отвязка от logtail-python (requirements) — по решению spec (если нигде больше не используется). (bot.py, .env.example, requirements) *(requirements НЕ тронут — решение spec 3.1.7: logtail-python остаётся)*

- [x] T-708 (B3): тесты Части B: фрейм-совместимость с logtail (поля/типы: dt ISO UTC, level, severity=levelno/10, message, context.runtime/system); HTTP-отправка (мок POST-сессии): успех, 4xx/5xx/network-error → WARNING-журнал и НЕ чаще 1/60с (таймстемп-гейт), счётчик ошибок; дроп при полном буфере логируется (не тихо); стартовое событие при наличии/отсутствии токена; flush на shutdown отдаёт буфер; рекурсии нет (лог-событие хендлера не порождает новую отправку). (tests/) *(tests/test_betterstack_handler.py, 15 тестов)*

## C. Видео: отказные ответы и дефолты (проблема 2)

- [x] T-709 (C1): детект отказных/пустых ответов мультимодалок в video_cascade_client.py и youtube_summarizer_service.py (обе точки L1/L2, youtube_summarizer_service.py:98-99/243-244 и video_cascade_client.py): список маркеров (RU+EN: «не вижу», «не могу просмотреть», «не могу получить», «no video», «can't see», «don't have access», «недоступно» и т.п. — финальный список в spec T-705) + ответ короче N симв → трактуется как ПУСТОЙ уровень (VideoLevelError/эквивалент) → каскад продолжается (L2 → fallback субтитры/STT); при отказе — НЕ подмешивать STT-попытку при «успешном» коротком тексте: только при отказе/пустоте (решение юзера); константы/пулы в services/smartmodule_phrases.py стиль-секции VIDEO_* (202-230); тесты: отказной текст L1 → уходит в L2; пустой ответ → fallback; обычный текст не задетекчен. (services/video_cascade_client.py, services/youtube_summarizer_service.py, services/smartmodule_phrases.py, tests)

- [x] T-710 (C2): сменить дефолты моделей: models.video_primary_model → nvidia/nemotron-3-nano-omni-30b-a3b-reasoning:free (единственная free с audio+video по research; строка в каталоге OR есть — проверено; live-проверку поддержки делает DevOps/юзер при деплое), models.video_fallback_model → minimax/minimax-m3:free (видит кадры; остаётся горячим); источники: config/settings.py:752-753 VIDEO_PRIMARY_MODEL/VIDEO_FALLBACK_MODEL дефолты + .env.example комментарий + param_catalog.py дефолты ключей models.video_primary_model/models.video_fallback_model (youtube_summarizer_service.py:98-99 читает их через hot.get); gemma-4-31b-it:free выпадает из дефолтов (AI Studio не берёт прямые .mp4) — но НЕ удалять роуты/код; тест: дефолтные значения строк = ожидаемым. (config/settings.py, param_catalog.py, .env.example, tests)

- [x] T-711 (C3): (@Architect, опционально после T-705) промпт-маркер: если решение T-705 = менять канон — добавить в YOUTUBE_VIDEO_SYSTEM_PROMPT строку «если видео недоступно — ответь ровно „VIDEO_UNAVAILABLE“» + обновление канона (plans/docs/canon/architecture.md §35.3/72.1, байт-в-байт дисциплина, один атомарный коммит) + PG hot-миграция значения (паттерн CHAT_SYSTEM_PROMPT) + детект маркера в C1; если решение = НЕ менять промпт — закрыть задачу с пометкой «marker-детект без правки промпта» и покрыть маркер-фразы в списке T-709. (канон, PG-миграция, C1-код) *(закрыто по решению spec 3.2.3: маркерный детект БЕЗ правки промпта — маркеры RU/EN покрыты в T-709)*

## D. Память-команды «запомни/забудь» (проблема 3)

- [x] T-712 (D1): роутинг/распознавание в direct_chat: «запомни …» и «забудь …» (после обращения — reply/mention/«бот»; direct_chat ловит reply/mention/«бот») — prefix-распознавание без триггера-модели; аргумент «забудь» — слово/фраза (min-length 3), не-матчи → обычный direct_chat (без ложных срабатываний, стиль /forget-путей); фразы-ответы — из пула D5, включая подтверждение с ЧИСЛОМ удалённых (удаление — D3); точка входа — handlers (direct_chat-обработчики) + services/direct_chat_service.py, не ломать reply/mention-логику и /forget /clear. (handlers/, services/direct_chat_service.py) *(handlers/direct_chat.py — _parse_memory_command/_handle_memory_command; consumed до handle(); fix-раунд 4: regex-эталоны spec 3.4.2 байт-в-байт — forget-норма ТОЛЬКО «забудь», синонимы «забыть/удали из памяти/выкинь из памяти» — обычный LLM-диалог (НЕ consumed), remember-разделитель требует пробел после `[,:]` («запомни:бензин» — не команда); «забудь всё» — noop-help)*

- [x] T-713 (D2): хранение «запомни X» (решение @Architect в T-705): вариант А — graph_facts origin 'user_memory' + CHECK-миграция v5 (D201-паттерн: database.py:13-17 _SCHEMA_VERSION_* + список origin в ОДНОМ месте :18-20 + _migrate_* по образцу _migrate_video_origins_v4 :215/:289+, пересоздание с сохранением id, PRAGMA user_version=5; факт попадает в RAG-контекст чата, TTL — решение spec); вариант Б — protected_facts (личные/вечные; для target_user в <protected_facts>); для админа «запомни» = глобальный факт чата; при варианте А — проверить, что migrate-версия и CREATE TABLE синхронны, и INSERT 'user_memory' не падает (аналог T-693). (services/database.py, services/summary_memory.py, tests) *(v5 миграция; MemoryManager.remember_user_fact; вес 1.0/TTL по limits.memory_commands_remember_ttl_days)*

- [x] T-714 (D3): удаление «забудь <слово/фраза>»: из graph_facts по scope: для админа/модера — весь чат, для обычного юзера — только СВОИ факты (target_user=себе) И только при тумблере D4; механика — LIKE/FTS-prefix (по аналогии /forget, но не только bot_direct_reply — по тексту факта); защита protected_facts от удаления (кроме админа — решение spec); min-length 3; журнал удалений — INSERT в graph_fact_compressions (существующая таблица); подтверждение с числом удалённых (пул D5); no-match → фраза «пусто». (services/summary_memory.py / services/database.py, tests) *(database.forget_memory_facts + _memory_forget_words; reason='user_forget'; fix-раунд 4: forget-аргумент «всё» (casefold) — НЕ удаление, help-фраза/consumed (гард в _parse_memory_command, spec 3.4.8 «забудь всё» НЕ реализуем))*

- [x] T-715 (D4): RBAC + тумблер: хелпер «админ/модер чата» по ConfigCache (get_role(telegram_id)/admins()/roles() — services/config_cache.py:234-256, bot_admins telegram_id/role_name pg_db.py:51-59) с фолбэком settings.ADMIN_USER_ID (PG недоступен/роль не найдена); новый тумблер flags.memory_commands_user_enabled (default false; param_catalog.py + TMA-категория флагов «Реакции и Триггеры» — механику повторить с reactions.vasya_enabled-образца); тумблер false + не-админ/не-модер → команда не работает (фраза из пула); тесты RBAC-матрицы. (services/config_cache.py, config/settings.py, param_catalog.py, handlers/, tests) *(services/chat_access.py — is_admin/is_moderator/privilege; флаг/лимит в _FLAGS flags_memory/_LIMITS limits_memory → вкладка «Память и RAG»)*

- [x] T-716 (D5): пул фраз CHAT_MEMORY_CMD_* в services/smartmodule_phrases.py (стиль CHAT_FORGET_*:149-162/CHAT_CLEAR_DONE_PHRASE): запомнено/удалено N/нет совпадений/нет прав/слишком коротко/тумблер-выключен; тест непересечения фраз (стиль test_direct_chat_prompts.py:79-99). (services/smartmodule_phrases.py, tests) *(spec 3.4.7 — 7 констант; тест TestChatMemoryPhrasePools)*

- [x] T-717 (D6): тесты Части D: распознавание префиксов (reply/mention/«бот», слово vs фраза, не-триггеры уходят в обычный direct_chat); матрица прав (админ/модер/юзер × тумблер on/off); «запомни» → факт в БД и появляется в get_rag_context (вариант А) / в protected_facts-контексте (вариант Б); «забудь» → удаление по LIKE с числом в ответе + запись в graph_fact_compressions; protected_facts не удаляются юзером; min-length 3; /forget и /clear не сломаны (регресс). (tests/) *(tests/test_memory_commands.py + tests/test_chat_access.py; регресс test_direct_chat*/test_graphrag* зелёный)*

## E. Промпт-баг TMA (проблема 4)

- [x] T-718 (E1): фронт web/app.js saveConfigItem (536-566): `isNaN(value)` (:550) применять ТОЛЬКО к числовым полям (int/float); для str — проверку убрать; для json — JSON.parse-проверка; :551 toast остаётся только для реально невалидных значений; node --check. (web/app.js)

- [x] T-719 (E2): пустые промпты/контент: решение @Architect (T-705): (а) серверный 422 в web/api/routes.py (_coerce или post_config: для категорий prompts/content если strip()=='' → 422 «промпт не может быть пустым») ИЛИ (б) фронт-тост в saveConfigItem; предпочтение — серверная 422 (единая точка валидации, защищает и другие клиенты); реализация + согласование с E1. (web/api/routes.py, web/app.js)

- [x] T-720 (E3): тесты Части E: серверные POST prompts.* (категории prompts/content) с кавычками/переносами/спецсимволами → 200 и значение сохранено без изменений; пустая строка/пробелы → 422 (если решение E2 = серверная 422); числовые поля с некорректным значением → 422 как раньше (не сломать); node --check web/app.js; регресс test_webapp_api. (tests/, web/)

## F. Research-отчёты и даты в RAG (блок 5)

- [x] T-721 (F1): отчёт плана импорта истории → plans/docs/memory-import-research.md: дата-сет (3 JSON, ~602МБ, ~1.27M сообщений, ТРИ разных чата — с id/периодами выше; ОТМЕТИТЬ: юзер просил «историю чата 2661910336», но файлы покрывают 3 чата — вероятно, юзер считает их одним, вопрос в отчёт юзеру); стоимость (FTS5 — бесплатно; эмбеддинги ~$0.3-5; LLM-факт-экстракция ~$3-15 на 100k сообщений DeepSeek-класса; время 1-3ч на 100k с параллелизмом; free-роуты OR 50 req/день — непригодны); подходы (FTS5 сразу; чанки+эмбеддинги; факты с датами в тексте И колонкой; референсы nano-graphrag/sqlite-graphrag); дата-фильтрация (metadata + pre-filter); цель: импорт в smart_messages с timestamp (L1 query_chat_memory заработает сразу) + факт-экстракция с датами; рекомендации — план на будущий раунд, НЕ реализация. (plans/docs/) *(документ создан docs-агентом — plans/docs/memory-import-research.md)*

- [x] T-722 (F2): отчёт sqlite→PG → plans/docs/sqlite-to-pg-research.md (Epic 86, статус backlog planned): риски (типы/CHECK-констрейнты, row-объекты, FTS5/sqlite-vec virtual tables → pgvector/tsvector, user_version → schema_version, драйверы aiosqlite → asyncpg, конкурентность/блокировки, pgloader), границы миграции GraphRAG vs PG-админки, рекомендации для DESIGN @Architect. (plans/docs/) *(документ создан docs-агентом — plans/docs/sqlite-to-pg-research.md)*

- [x] T-723 (F3): .gitignore += `migrate_history/` (сейчас в дереве `?? migrate_history/`, ~600МБ — риск случайного коммита; в .gitignore ОТСУТСТВУЕТ); verify: git status чист от migrate_history; если папка когда-либо попадала в историю — решить (git rm --cached не нужен: файлы untracked). (.gitignore)

- [x] T-724 (F4): даты в RAG-фактах: лёгкий фикс — при рендере chat_history-фактов в RAG-контексте (get_rag_context) добавлять префикс даты из created_at («%Y-%m-%d», чат-локально): формат «[2024-05-15] текст» (согласовать с L1-форматом query_chat_memory «[Имя 2024-05-15]: текст» — решение spec: только chat_history или все origin, где created_at есть); ЦЕЛЬ: «что было N-числа» через RAG-факты начинает работать; query_chat_memory (уже с датами) не трогать; тесты: факт chat_history с created_at рендерится с датой; граничные (created_at NULL/будущее). (services/summary_memory.py, tests) *(по решению spec 3.6 — ВСЕ origin, где created_at есть; дата ПЕРЕД origin-префиксом для знаниевых; 3-кортежи в build_rag_context/get_rag_context)*

## G. Процесс и финал

- [ ] T-725 (G1): зафиксировать правило процесса: авто-коммит memory-sync (plans/docs/memory-project-overview.md + синки @Memory) в конце КАЖДОГО раунда БЕЗ запроса + grep-проверка на секреты (токены/ключи) перед коммитом; внести в plans/project.md («Git и процессы»), стиль существующих правил; применить по факту в этом раунде (финальный коммит G2 включает memory-project-overview.md). (plans/project.md) *(не выполнено — исполняет @DevOps в T-726: правило процесса G1 ещё не внесено в plans/project.md)*

- [ ] T-726 (G2): @DevOps финал: полный pytest → 0 failed; grep-проверка на секреты перед коммитом (правило G1); коммит ВСЕГО незакоммиченного (plans/backlog.md, plans/features/betterstack-own-handler-video-memory-cmds/, код/тесты B-G, plans/docs/memory-import-research.md, sqlite-to-pg-research.md, memory-project-overview.md; .env/migrate_history НЕ коммитить); пуш в origin/master; деплой на 198.46.175.136 (pull, мягкий SIGTERM-рестарт без SIGKILL); live-верификация: (1) BetterStack — тест-событие «[betterstack] attached | handler=own-v1» в journald и события во вкладке панели; (2) видео — «че за видос» где L1 отвечает отказным текстом → каскад продолжается (не уходит юзеру); nemotron-omni:free live-проверка; (3) «запомни/забудь» в чате админом и юзером (тумблер off/on); (4) сохранение промпта с кавычками/переносами в TMA без ложного тоста; результат + отчёт юзеру (в т.ч. про 3 разных чата в migrate_history и что RAG-даты заработали) — в отчёт PM. *(исполняет @DevOps финальным шагом: правило в plans/project.md, README, коммит всего, пуш, деплой)*
