# A4 — `image-context-memory-round1026` (Эпик 3 «Agentic Intelligence», Wave 4, Раунд 10.26)

> **Статус (ФИНАЛ, 25.09.2026): A4 ЗАКРЫТА ✅** — планирование ✅ `PLANNING_CONSISTENT` (T-3628) + Build блоков B–G ✅ (T-3629…T-3639 + T-3644/T-3645 + cycle-3 **T-3646**) + **единый Reviewer gate T-3640 = `Approved` на 4-м цикле** (25.09.2026; Scanner отсутствует; обе линзы; R3 сохранён) + **`DEFERRED_TO_EPIC`** (T-3642) + **архивация** (T-3641 → `plans/archive/image-context-memory-round1026/`) + **handoff T-3643 → A7**. Промежуточно: cycle-2 = `Needs Fixes` (F1 High — класс-незакрытая alias-коллизия; F2/N1/N3 закрыты) → **root-cause amendment @Architect: ADR-1026-19 D13** (корроборационный шлюз spec §3.1, SC-A4-09 amended, spec §9 тест-лист) → дельта-сверка @PM D13 ✅ + cycle-3 build **T-3646**. **Следующая волна Эпика 3 — A7 `decision-making`** (§38–§40/§42–§48; канонический ID/разделы — backlog, строка A7).
> **Готово:** T-3626 ✅ (Step 0/baseline зафиксирован @Memory; см. «Fact-pack»); **T-3627 ✅** (`spec.md` + ADR-1026-19, 25.09.2026, @Architect); **T-3628 ✅** (сверка @PM, 25.09.2026, `PLANNING_CONSISTENT`). **U-1…U-8 — ✅ закрыты** решениями ADR-1026-19 D1–D8 (см. «Открытые вопросы»).
> **Эпик:** Эпик 3 «Agentic Intelligence», **Wave 4 (фактическая очередь после A6)**. Изначально в плане A4 стояла в Wave 2 `[A3 → A5] ∥ A6 → A4`; фактический порядок после закрытия A5/A6 — **`A0 ✅ → A1 ✅ → A2 ✅ → A3 ✅ → A5 ✅ → A6 ✅ → A4 → A7 → A8 → A9 → A10`** (backlog `:306`, `:314`, A6 handoff `:328`). **Тип** (backlog `:306`): **backend/memory+LLM** — генерация изображения из досье/RAG (разрешение имён/алиасов, визуальный срез, сборка промпта), потребитель контракта A6. **P0**. **Epic-ID:** Эпик 3 «Agentic Intelligence» (раунд 10.26); **release policy — EPIC_ONLY**: эта фича — `included in pending epic release` (пер-фича деплоя/тега/bump нет).
> **ТЗ-источник (IMMUTABLE, `plans/current_task.md`, только чтение — R17/R18):** **§22 «ИЗОБРАЖЕНИЯ НА ОСНОВЕ ПАМЯТИ»** (`:4765–4796`), **§23 «КАКИЕ ДАННЫЕ ИСПОЛЬЗОВАТЬ»** (`:4799–4829`), **§24 «РАЗРЕШЕНИЕ ИМЁН»** (`:4832–4858`), **§25 «СБОРКА IMAGE PROMPT»** (`:4861–4902`), **§37 «БЕЗОПАСНОСТЬ TOOL OUTPUT»** (`:5252–5279`). **Контекст-потребление (§32–§34 `:5066–5177` — реализованы A6, A4 только CONSUMES):** §32 «ДОСЬЕ КАК ИНСТРУМЕНТ», §33 «СТРУКТУРИРОВАННЫЙ MEMORY LOOKUP», §34 «ДОСЬЕ + RAG». **Приёмочные ориентиры:** **§52** п.3 «Генерацию с использованием досье.» (`:5758–5759`), п.4 «Генерацию без доступных сведений о внешности человека.» (`:5760–5761`), п.5 «Неоднозначное имя пользователя.» (`:5763`), п.11 «Досье → RAG → стилизация ответа.» (`:5775`); **§53** «бот не умеет получить нужные сведения из досье» (`:5818–5819`), «бот путает пользователей с одинаковыми именами» (`:5821`), «генератор получает полный сырой лог вместо подготовленного промпта» (`:5823–5824`); **§54** п.6 «Механизм использования досье и RAG.» (`:5882`).
> **Зависимости:** **A0** ✅ (durable-аудит `plans/docs/agentic-audit-round1026.md`: EV-21, `#duplicates`, `#epic3-reuse`; санкция расширения канона D3 **исчерпана A2** — повторно НЕ использовать), **A1** ✅ (Merge §83, ADR-1026-14), **A2** ✅ (Merge §84, ADR-1026-15 — envelope `ToolLoopResult.tool_results`/`ToolContext.result_for`, §17-лимиты), **A3** ✅ (Merge §85, ADR-1026-16 — единый `ImageRequest` + поля-заглушки памяти `resolved_subjects`/`context_required`/`context_sources`, хук `build_final_prompt`; §104 AST-гейт), **A5** ✅ (Merge §86, ADR-1026-17 — image-лимиты `worker_budget`; **A4 не трогает**), **A6** ✅ (Merge §87, ADR-1026-18 — **контракт `get_user_context`** канон **12**). **A3 owner-gate (ADR-1026-16) — `PENDING OWNER VERIFICATION` (внешний, A4 его НЕ закрывает).**
> **Baseline (Step 0 @Memory, 25.09.2026; в Step 1 НЕ перемеряется):** HEAD **`e8646af`** == `origin/master` + **UNCOMMITTED epic-release рабочее дерево A2/A3/A5/A6 (≙88 файлов dirty)** — A4 строится **поверх**, ничего из этого не откатывать/не коммитить. `APP_VERSION` **2.58.30**; канон инструментов **12** (после A6); **Δ DDL = 0** (SQLite v12); **Δ каталога (ожидание) = 0** (env-only). **Deploy — `DEFERRED_TO_EPIC` (EPIC_ONLY)**: bump — только в агрегатном релизе Эпика 3; **release-узел и пер-фича тег НЕ создаются**; baseline-анкер = коммит **`e8646af`**; hot-откат = env-only kill-switch OFF; cold = `git revert`/анкер `e8646af`; теги/бэкапы не удаляются (R18).
> **Risk (ФИНАЛ, сверка @PM T-3628, 25.09.2026): `Risk-Level: R3`** (spec §13, ADR D10; было preliminary — подтверждено). Обоснование: A4 впервые выносит приватные сведения из досье/RAG во **внешний** генератор изображений (egress персональных данных) и создаёт поверхность prompt injection, меняя **разделяемые** image-модули живого direct-чата — blast radius шире read-only A6. **R3-усиление обязательно:** `threat-failure-analysis.md` (`threat→mechanism→code→test`, задача **T-3644**) + adversarial-приёмка + rollback-доказательства OFF-паритета + diff-аудит границ (**T-3645**). Reviewer может понизить до R2 по фактическому diff при доказанной байт-в-байт аддитивности OFF и невозможности инъекции/утечки.
> **Ожидаемые числа (Step 0 → ФИНАЛ T-3628):** канон **12 без изменений** (новых инструментов НЕТ; ADR-санкция расширения **не выдана**, D9); **Δ каталога = 0** (env-only kill-switch); **Δ DDL = 0** (SQLite v12; новая БД/второй резолвер/второй RAG-контур запрещены); `APP_VERSION` остаётся **2.58.30**; deploy **DEFERRED_TO_EPIC**.
> **Карта кода (Step 0 @Memory, факт; в Step 1 НЕ перепроверяется):** A3-заглушки `ImageRequest` `resolved_subjects`/`context_required`/`context_sources` — `services/image_generation.py:252–254`, конструктор `build_image_request` `:259–275`, **хук сборки промпта `build_final_prompt` `:278–285`** (ADR-1026-16 D5 прямо помечает точку реализации A4), раннер `run_image_request` `:288–301`; входы-потребители: `maybe_handle_keyword` (`services/image_generation.py:1241-я обл.`) и `tool_router._generate_image` (`services/tool_router.py:2317`). **Резолв имён:** `services/summary_aliases.py` — `AliasResolver.resolve` `:50–75`, `build_alias_resolver` `:78–102` (stable, не dirty) — **переиспользовать, второй резолвер не создавать**. **Потребление A6:** helper `tool_router._memory_lookup_resolve` `:1303–1338` (в т.ч. ветка `ambiguous`), `database.get_user_context_facts` `:4653`; лексикон внешности `tool_router:151–157`/`:1761` — **фильтр только**; **извлечение внешности — пробел A4**. §104-гейт: `tests/test_unified_image_request_round1026.py::TestBoundsA3` (AST). §37/R17-прецедент логирования — `tool_router` (R17-safe поля).

## Трассируемость (REQ → verbatim §ТЗ → блок → задачи → SC/ADR)
> **REQ-A4-01…-17** выделены из §22–§25 + §37 (+ приёмочные ориентиры §52/§53/§54 п.6; §32–§34 — только CONSUMPTION). Состав **не сужен**, REQ не добавляется «для удобства»; все привязаны к блокам/задачам; **орфан-REQ нет**. Колонки **SC/ADR — заполнены на сверке T-3628** из `spec.md` §2/§2.1 и ADR-1026-19 (D1–D12 + D13 amendment); ни один REQ не остаётся без SC; каждый SC восходит к ≥1 REQ. **Delta D13 (cycle-2 F1, amendment @Architect) реконсилирован:** SC-A4-09 усилен независимой корроборацией (G1/G2/G3), добавлена cycle-3 задача **T-3646** (см. раздел «A4 delta reconcile D13»).

| REQ | Источник (verbatim, `current_task.md`) | Блок | Задачи | SC | ADR |
|---|---|---|---|---|---|
| REQ-A4-01 | §22 желаемое поведение 1–7: «Определить, о каком Лёхе идёт речь… Найти соответствующего пользователя… Получить релевантные сведения из досье… Сформировать промпт изображения… Вызвать существующий генератор.» (`:4774–4789`) | B, C, D, E | T-3629, T-3631, T-3633, T-3635 | SC-A4-01 | D1, D2, D11, D12 |
| REQ-A4-02 | §22 «Не передавать генератору всё досье целиком.» «Не отправлять весь чат в промпт изображения.» «Извлекать только сведения, которые нужны для конкретной задачи.» (`:4791–4796`) | C, D, F | T-3631, T-3632, T-3633, T-3638 | SC-A4-02, SC-A4-18 | D3, D4, **D13** |
| REQ-A4-03 | §22 «При необходимости получить ограниченный срез сообщений или RAG-памяти.» (`:4782–4783`) | C | T-3632 | SC-A4-03 | D4 |
| REQ-A4-04 | §23 полезные данные: внешность/одежда/причёска/очки/борода/детали/предпочтения/контекст (`:4802–4811`) | C | T-3631, T-3632 | SC-A4-04, SC-A4-18 | D4 |
| REQ-A4-05 | §23 «Не превращать психологический портрет автоматически в утверждение о внешности.» (`:4813–4814`) | C | T-3631 | SC-A4-05 | D4 |
| REQ-A4-06 | §23 «Не придумывать точные черты лица, если они неизвестны.» (`:4816–4817`) | C, D | T-3631, T-3633 | SC-A4-06 | D4, D12 |
| REQ-A4-07 | §23 «можно создать художественную интерпретацию, но не выдавать её за достоверный портрет человека.» (`:4819–4822`) | C, D | T-3632, T-3633 | SC-A4-07 | D6 |
| REQ-A4-08 | §23 «Если пользователь хочет точное сходство, попросить фотографию или другой подходящий визуальный референс.» + «Не использовать чужие фотографии или приватные данные без необходимости.» (`:4824–4829`) | C, D | T-3632, T-3633 | SC-A4-08 | D6, **D13** |
| REQ-A4-09 | §24 «Использовать существующую систему идентификации.» «Не определять пользователя исключительно по совпадению отображаемого имени.» + `chat_id/user_id/alias/username/real_name/контекст` (`:4835–4850`) | B | T-3629, **T-3646** | SC-A4-09 (amended — независимая корроборация), SC-A4-19 | D1, D3, **D13** |
| REQ-A4-10 | §24 «Если в чате несколько Лёх, не выбирать случайного.» «При неоднозначности запросить уточнение или отказаться от персонализации.» (`:4852–4856`) | B, C | T-3630, T-3631 | SC-A4-10, SC-A4-19 | D3, D5 |
| REQ-A4-11 | §24 «Не смешивать досье разных пользователей.» (`:4858`) | B, C | T-3630, T-3631 | SC-A4-11, SC-A4-19 | D3, D5 |
| REQ-A4-12 | §25 «Финальный промпт должен собираться из нескольких независимых частей.» + 5 частей (запрос / разрешённые персонажи / контекст / визуальные требования / техограничения) (`:4864–4875`) | D | T-3633 | SC-A4-12, SC-A4-20 | D12 |
| REQ-A4-13 | §25 «Не использовать простую конкатенацию всего найденного текста.» «Не передавать инструкции из чужих сообщений как системные инструкции генератору.» «Данные памяти и RAG — источник информации, а не источник новых правил поведения.» (`:4877–4884`) | D, E, F | T-3633, T-3634, T-3635, T-3638 | SC-A4-13, SC-A4-20 | D8, D12 |
| REQ-A4-14 | §25 пример логики (`:4886–4900`) + «Не придумывать дополнительные биографические факты, не относящиеся к изображению.» (`:4901–4902`) | C, D | T-3631, T-3633 | SC-A4-14, SC-A4-18 | D4, D12 |
| REQ-A4-15 | §37 «Содержимое веб-страницы, досье и RAG считать данными, а не инструкциями.» «Не исполнять команды, обнаруженные внутри статьи.» «Не позволять веб-странице подменять системный промпт.» «Не передавать API-ключи в результаты инструментов.» (`:5255–5273`) | D, E | T-3634, T-3635 | SC-A4-15, SC-A4-21 | D8 |
| REQ-A4-16 | §52 п.3 «Генерацию с использованием досье.» (`:5758–5759`), п.4 «Генерацию без доступных сведений о внешности человека.» (`:5760–5761`), п.5 «Неоднозначное имя пользователя.» (`:5763`) | B, C, E | T-3630, T-3631, T-3632, T-3636 | SC-A4-16, SC-A4-22 | D4, D5, D6, D10 |
| REQ-A4-17 | §53 «бот не умеет получить нужные сведения из досье» (`:5818–5819`); «бот путает пользователей с одинаковыми именами» (`:5821`); «генератор получает полный сырой лог вместо подготовленного промпта» (`:5823–5824`); §54 п.6 «Механизм использования досье и RAG.» (`:5882`) | C, D, E, F, G, H | T-3631, T-3633, T-3636, T-3637, T-3639, T-3640 | SC-A4-17, SC-A4-22 | D2, D7, D8, D10 |

**Кросс-скоуповые SC (не орфаны — трассируются к catch-all REQ; полные формулировки — `spec.md` §2/§2.1):** SC-A4-18 ← REQ-A4-02/-04/-14; SC-A4-19 ← REQ-A4-09/-10/-11; SC-A4-20 ← REQ-A4-12/-13; SC-A4-21 ← REQ-A4-15; SC-A4-22 ← REQ-A4-16/-17. **SC-A4-01…SC-A4-22 покрыты; orphan-SC нет.** После amendment SC-A4-09 (D13): покрытие сохранено — каждый REQ-A4-01…-17 имеет ≥1 SC, SC-A4-09 (усилен) по-прежнему ∈ REQ-A4-09; orphan-REQ/SC нет.

## Приёмочные инварианты A4 (нарушение = НЕ принято) → привязка к REQ/SC/задачам
> **14 инвариантов**, каждый привязан к REQ из §22–§25/§37 (+ §52/§53/§54-ориентиры) и к SC (`spec.md` §5); **losses нет** — все 14 сохранены и покрыты SC-A4-01…SC-A4-22. **Amendment D13** усиливает инвариант 6 (alias-матч → кандидат; `resolved` — через корроборацию); класс-инвариант D13 приведён ниже (счёт 14 сохраняется).
1. **Никакого полного дампа:** ни всё досье, ни весь чат не уходят генератору; в промпт попадает только выборочный, ограниченный срез. **REQ-A4-02; T-3631, T-3632, T-3633, T-3638; SC-A4-02, SC-A4-18.**
2. **Только task-relevant извлечение:** извлекаются лишь сведения, нужные для конкретной задачи изображения; «на всякий случай» не собирается. **REQ-A4-02, REQ-A4-04; T-3631, T-3632; SC-A4-04, SC-A4-18.**
3. **Психология ≠ внешность:** психологический портрет автоматически не превращается в утверждение о внешности. **REQ-A4-05; T-3631; SC-A4-05.**
4. **Нет выдуманных черт лица:** точные черты лица не придумываются, если они неизвестны. **REQ-A4-06; T-3631, T-3633; SC-A4-06.**
5. **Арт ≠ достоверный портрет:** художественная интерпретация не выдаётся за достоверный портрет; при точном сходстве — запрос фото/референса; чужие фото/приватные данные без необходимости не используются. **REQ-A4-07, REQ-A4-08; T-3632, T-3633; SC-A4-07, SC-A4-08.**
6. **Только существующая система идентификации:** `AliasResolver` (`summary_aliases.py`) — второй резолвер запрещён; alias-совпадение — **только кандидат**, `resolved` требует независимой корроборации (G1 person-intent + G2 persona-рекорд via `db.get_persona_names`; G3 image-object cross-check), определение только по отображаемому имени недопустимо (D13). **REQ-A4-09; T-3629, T-3646; SC-A4-09 (amended), SC-A4-19.**
7. **Нет случайного выбора одноимённого:** при нескольких Лёхах случайный не выбирается. **REQ-A4-10; T-3630; SC-A4-10, SC-A4-19.**
8. **Неоднозначность → уточнение/отказ от персонализации:** при неоднозначности либо запрос уточнения, либо отказ от персонализации (нейтральный арт), без «угадывания». **REQ-A4-10; T-3630, T-3631; SC-A4-10.**
9. **Досье разных пользователей не смешиваются:** субъекты/факты не сливаются; при `ambiguous` — кандидаты только по id/ref. **REQ-A4-11; T-3630, T-3631; SC-A4-11, SC-A4-19.**
10. **5-частная сборка без наивной конкатенации:** промпт собирается из независимых частей (запрос / разрешённые персонажи / контекст / визуальные требования / техограничения); простое склеивание всего найденного текста запрещено. **REQ-A4-12, REQ-A4-13; T-3633; SC-A4-12, SC-A4-20.**
11. **Данные ≠ инструкции (§37):** память/RAG/веб — источник информации, а не правил; инструкции из чужих сообщений не становятся системными; API-ключи не попадают в результаты. **REQ-A4-13, REQ-A4-15; T-3634, T-3635; SC-A4-13, SC-A4-15, SC-A4-21.**
12. **Consume A6 без реимплементации:** lookup/purpose-роутинг/envelope/резолв не переписываются; второго резолвера, второго RAG-контура, второй аналитики и второго image-pipeline нет; A6 — источник generic-контракта (`get_user_context`), A4 — потребитель. **REQ-A4-01, REQ-A4-04; T-3629, T-3631, T-3635, T-3638; SC-A4-01, SC-A4-19.**
13. **§104 no-go + A5 no-go + канон 12 без новых инструментов:** `generate_image`/модель/провайдер/ключи/параметры/ошибки/публикация не трогаются; `worker_budget`/`limits.*` (A5) вне diff; канон инструментов остаётся **12** (13-й инструмент не вводится; ADR-санкция расширения **не выдана**, D9). **REQ-A4-13; T-3635, T-3636, T-3638; SC-A4-01.**
14. **R17/R18 + EPIC_ONLY:** логи — только R17-safe поля (никогда текст досье/сообщений/имён/промптов/ключей); `current_task.md`, теги, бэкапы, `stash` целы; deploy **`DEFERRED_TO_EPIC`**, @DevOps не вызывается; owner-gate A3 (`PENDING OWNER VERIFICATION`) — внешний и A4 его не закрывает. **REQ-A4-15, REQ-A4-17; T-3634, T-3637, T-3641; SC-A4-17.**
> **Класс-инвариант (D13; spec §3.1, ADR D13) — дополнение к инварианту 6 (счёт 14 сохраняется):** рядовой generic image-запрос («нарисуй <существительное>», «сделай картинку <существительное>») **не может** разблокировать персонализацию **независимо от того, входит ли существительное в какой-либо лексикон**, т.к. G1 требует явной ссылки на человека; новая image-лексема утечку не открывает. **REQ-A4-09; T-3646; SC-A4-09; D13.**

## Карта решений ADR-1026-19 → задачи (сверка T-3628 + delta D13)
> Каждое решение **D1–D13** ADR-1026-19 отображается на задачи/блоки; каждая задача блоков B–H (**T-3629…T-3639 + T-3644/T-3645**, а также cycle-3 **T-3646**) фигурирует хотя бы в одной строке — **орфанов нет**. Перенос из `adr-1026-19-image-context-memory.md` §«AMEND / REUSE-карта» (таблица «Решение → Задачи»), адаптирован под фактический состав задач после расширения T-3628 и delta D13.
| Решение | Суть | Блоки | Задачи (tasks.md) |
|---|---|---|---|
| **D1** (U-1) | helper над существующими источниками A6 (`AliasResolver` + `get_user_context_facts` + RAG-ридеры); одна реализация — две точки вызова; второй резолвер/RAG/generic-lookup запрещён | B, C, E | T-3629, T-3630, T-3631, T-3635 |
| **D2** (U-2) | новый helper-модуль `services/image_context_memory.py` + реализация хука `build_final_prompt`; `run_image_request`/`generate_and_send` не меняются; второго pipeline нет | C, D, E | T-3631, T-3633, T-3635, T-3636 |
| **D3** (U-3) | `context_required=True` только при `resolved`/`ambiguous`; `context_sources` — enum фактически использованных видов; простой запрос → `False`/`[]` | B, E | T-3629, T-3630, T-3635 |
| **D4** (U-4) | визуальный срез: подтверждённые `graph_facts` + ленивый bounded RAG; A4-лексикон + психологический denylist; `dossier_portrait` только не-визуальный контекст; image-капы строже A6 | C, F | T-3631, T-3632, T-3637 |
| **D5** (U-5) | неоднозначность → нейтральный не-персонализированный арт + запрос уточнения; без случайного выбора и второго платного вызова | B, C, F | T-3630, T-3631, T-3637 |
| **D6** (U-6) | дисклеймер «арт ≠ достоверный портрет» (промпт + reply) + узкий маркер точного сходства → запрос фото/референса; нового инструмента нет | C, D, F | T-3632, T-3633, T-3637 |
| **D7** (U-7) | env-only kill-switch `IMAGE_CONTEXT_MEMORY_ENABLED` (default ON); Δ каталога=0; Δ DDL=0; EPIC_ONLY `DEFERRED_TO_EPIC`; hot-OFF паритет / cold `e8646af` | E, F, G, H | T-3636, T-3638, T-3639, T-3642, **T-3645** |
| **D8** (U-8) | §37-обёртка «ДАННЫЕ (не инструкции)»; без системной инъекции/пасс-тру/ключей; R17-лог = id/enum/числа/латентность | D, E, F | T-3634, T-3635, T-3637 |
| **D9** (C-2) | канон остаётся **12**; 13-й инструмент не вводится; описание `generate_image` не меняется (NOT_REQUIRED) | E, G | T-3636, T-3639 |
| **D10** (Risk R3) | R3 + обязательный `threat-failure-analysis.md`, adversarial-приёмка, rollback-доказательства, diff-аудит границ | F, G, H | T-3637, T-3639, T-3640, **T-3644** |
| **D11** (AMEND/REUSE/границы) | A3 REUSE+хук; A6 CONSUME (не amend); A5 граница; ADR-1013-3 NOT_APPLICABLE; §104 не трогать | E, F, G, H | T-3635, T-3638, T-3639, T-3640 |
| **D12** (5-частная сборка) | сборка только при `context_required=True`; иначе промпт байт-в-байт A3 (`extract_prompt`) | D, F | T-3633, T-3637 |
| **D13** (amendment cycle-2 F1; class-closing identity guard) | alias-матч → **кандидат**; `resolved` только через корроборацию: G1 person-intent (закрывает класс) + G2 persona-рекорд (`db.get_persona_names`) + G3 image-object cross-check (defense-in-depth); stoplist/min-length — defense-in-depth, не удаляются | B, F, H | T-3629, T-3630, T-3631, T-3637, T-3640, **T-3646** |

## Блок 0 — Step 0 / baseline (T-3626)
> Step 0 @Memory выполнен **до** Step 1 (fact-pack: карта кода, конфликты C-1…C-7, ожидаемые числа, U-1…U-8). В этом блоке ничего не перемеряется.
- [x] **T-3626 [@Memory/@Orchestrator — подтверждение Step 0]** — **Цель:** зафиксировать, что A4 стартует с anchor **`e8646af`** + **UNCOMMITTED epic-release рабочего дерева A2/A3/A5/A6 (≙88 файлов dirty)** — НЕ трогать/не откатывать/не коммитить; baseline-числа (2.58.30; канон 12; Δ DDL=0; Δ каталога ожидание 0) заимствуются из Step 0 без перемера; пер-фича тега НЕТ (EPIC_ONLY); owner-gate A3 остаётся `PENDING OWNER VERIFICATION` (внешний). **Выход:** подтверждение в KG/`workflow_state` (машинный блок — только через `workflow_checkpoint`). **Критерий:** baseline-анкер и точка отката согласованы; незакоммиченный epic-release кандидат не затрагивается задачей A4. **Зависимости:** Шаг 0 @Memory ✅. **Статус:** ✅ **Step 0 факт-пак получен входом (25.09.2026)** — отметка-подтверждение за @Memory/@Orchestrator.

## Блок A — spec + НОВЫЙ ADR (Шаг 2 @Architect) + сверка @PM
- [x] **T-3627 [@Architect]** — **Статус:** ✅ **Выполнено (25.09.2026):** созданы `spec.md` + `adr-1026-19-image-context-memory.md` (D1–D12; U-1…U-8 закрыты). **Цель:** создать `spec.md` (REQ-A4-01…-17 → SC; трассировка §22–§25/§37 + приёмка §52 п.3/4/5, §53, §54 п.6; границы с A3/A5/A6/A7/§104) + **НОВЫЙ ADR-1026-19** (следующий свободный номер — **ADR-1026-19**), решения которого: (i) политика разрешения имён/алиасов и неоднозначности в image-потоке (**U-5**); (ii) источники и капы визуального среза, включая **извлечение внешности** — пробел, оставленный A6 (**U-3, U-4**); (iii) точка входа и место кода для memory-доступа image-пути — детерминированный helper vs вызов vs tool-only, без второго pipeline (**U-1, U-2**); (iv) схема 5-частной сборки промпта на хуке `build_final_prompt`; (v) ветка «арт ≠ достоверный портрет» + запрос фото-референса (**U-6**); (vi) §37-обёртка «данные ≠ инструкции» + R17-формат логов (**U-8**); (vii) env-only kill-switch, дельта каталога, EPIC_ONLY deploy/rollback (**U-7**); (viii) финальный `Risk-Level` (предварительно R3 — обосновать/понизить); (ix) явное подтверждение Δ DDL=0 / канон 12 / Δ каталога (ожидание 0). **Выход:** `spec.md` + `adr-1026-19-*.md` (Status Proposed; Accepted — по merge). **Критерий:** каждый REQ-a4 имеет SC; решения не сужают §22–§25/§37; U-1…U-8 закрыты решениями либо явно перенесены с обоснованием; границы A4 (потребитель A6) соблюдены. **Зависимости:** T-3626. **Ответы на U-1…U-8 — здесь (PM не отвечает).**
- [x] **T-3628 [@PM — сверка]** — **Статус:** ✅ **Выполнено (25.09.2026): `PLANNING_CONSISTENT`** (tasks.md ↔ spec.md ↔ ADR-1026-19 согласованы; см. «Карта решений ADR-1026-19 → задачи»). **Дельта-сверка D13 ✅ (25.09.2026):** tasks.md реконсилирован с amendment @Architect (ADR-1026-19 D13, spec §3.1/SC-A4-09/§9) — добавлена cycle-3 build-задача **T-3646**; см. раздел «A4 delta reconcile D13». **Цель:** сверка `tasks.md` ↔ `spec.md` ↔ ADR-1026-19: заполнить SC/ADR-колонки трассируемости и инвариантов, построить карту «решения → задачи/блоки», привести deploy/baseline/risk к ADR. **Выход:** **`PLANNING_CONSISTENT`** (или точный список расхождений → реконсиляция @Architect через @Orchestrator; **Build не стартует**). **Критерий:** scope/исключения/risk/приёмка/зависимости/deploy/rollback согласованы; orphan-REQ/SC нет; канон 12 и Δ DDL/Δ каталога зафиксированы. **Зависимости:** T-3627.

## A4 delta reconcile D13 (cycle-2 F1 → cycle-3 build, re-pin)
> **Статус:** ✅ **дельта-сверка D13 (25.09.2026)** — `tasks.md` реконсилирован с amendment @Architect после Reviewer T-3640 cycle 2 (`Needs Fixes`, F1 High: класс-незакрытая alias-коллизия → egress чужого досье; F2/N1/N3 закрыты). Root-cause + amendment: **ADR-1026-19 D13** (класс-закрывающий корроборационный шлюз), **spec §3.1** переписана (G0/G1/G2/G3 + класс-инвариант), **SC-A4-09** усилен независимой корроборацией, **spec §9** дополнена 9-пунктовым регресс-листом.
> **Новый T-ID:** **T-3646** — следующий свободный после T-3645 (grep: заняты T-3626…T-3645; коллизий нет); cycle-3 build-задача в блоке B (реализация шлюза + 9-item регресс).
> **Re-pin (обязательно до cycle-3 review):** Spec-Hash был `d98aab75d9f08af57d71393cf367f87fb2f7725ed958ee42419c7d52fdc9eb6a` → **новый `63064ccbd5891bde4082d365e7c52def0e0640529a06f3eaa0fe632e33f840c7`** (spec amended: §1/§2.1/§3.1/§5/§7.1/§9/§12; ADR-1026-19 D13 — ADR-hash `8e97e495…`). **Binding cycle-1** (`working_tree 6a452e93…`, spec `d98aab75…`) и **binding cycle-2** (`working_tree 85e909e6…`, spec `d98aab75…`) — **STALE** (spec изменён; код подлежит правке T-3646). **Review cycle 3 — pending:** единый Reviewer gate (T-3640) выполнит свежий binding после фиксов @Builder (T-3646 → T-3640 cycle 3).
> **Подтверждение неизменного (delta D13):** release policy **EPIC_ONLY**; deploy **`DEFERRED_TO_EPIC`**; **Δ DDL = 0** (только существующий read `db.get_persona_names`); **Δ каталога = 0** (env-only); канон **12** без изменений; **Risk-Level R3** без изменений (egress реален до закрытия F1).
> **Орфан-чек после amendment SC-A4-09:** каждый REQ-A4-01…-17 имеет ≥1 SC; SC-A4-09 (усилен) по-прежнему ∈ REQ-A4-09; orphan-REQ/SC нет.
> **Консистентность:** scope/исключения/risk/приёмка/зависимости/deploy/rollback согласованы; допуск к Build cycle 3 — **`PLANNING_CONSISTENT`-delta** для T-3646 (в отличие от полного `PLANNING_CONSISTENT` T-3628).

## Блок B — разрешение имён / идентификация (§24)
> REQ-A4-09, REQ-A4-10, REQ-A4-11. Reuse: `summary_aliases.AliasResolver` — **второй резолвер запрещён**.
- [x] **T-3629 [@Builder — идентификация субъекта]** — **Цель:** определение «кто такой Лёха» для image-запроса через **существующий** `AliasResolver` (`build_alias_resolver`/`resolve`, `services/summary_aliases.py`), приоритет алиасов по реальной серверной логике; учитывать `chat_id/user_id/alias/username/real_name/контекст`; **не** определять пользователя только по совпадению отображаемого имени. **Выход:** правки + тесты. **Критерий:** REQ-A4-09 → SC; инвариант 6. **Зависимости:** T-3628.
- [x] **T-3630 [@Builder — неоднозначность/смешение]** — **Цель:** при нескольких одноимённых — **не** выбирать случайного; неоднозначность → запрос уточнения или отказ от персонализации; досье разных пользователей не сливаются (reuse семантики `ambiguous` из A6 `tool_router._memory_lookup_resolve:1303–1338`; кандидаты — только id/ref). **Выход:** правки + тесты (коллизия одинаковых имён). **Критерий:** REQ-A4-10/-11 → SC; инварианты 7, 8, 9. **Зависимости:** T-3629.
- [x] **T-3646 [@Builder — cycle 3: класс-закрывающий корроборационный шлюз D13]** — **Статус:** ✅ **Выполнено** (реализован шлюз G0–G3 в `services/image_context_memory.py`; тесты spec §9 a–i; A4 67/0, полный pytest 9280/0, §104 7/0, JS 47/0, каталог 470/427/445/101/99/21, канон 12; evidence.md «Cycle 3 (T-3646, D13)»). — **Цель:** реализовать шлюз корроборации spec §3.1 (ADR D13) поверх T-3629/T-3630: (1) `_resolve_subject(...)` возвращает **кандидата** (не `resolved`), `_MIN_ALIAS_TOKEN_LEN`/`_stem`/`_ALIAS_STOPLIST` сохранены (G0 — defense-in-depth; склонения `Лёха→Лёху` не ломаются); (2) **G1 person-intent** по закрытому набору маркеров (`как выглядит/выглядел/выглядела`, `портрет`, `внешность/внешне`, `лицо/лица`, `фото/фотограф/снимок`, `досье`, `в образе/образ`, `про/о/об <токен>`, `парень/мужчина/человек/девушка/женщина/друг/подруга/знакомый/знакомая`, `похож/похожа`; заглавная буква — слабый сигнал, G1 не удовлетворяет) → провал = `none`, `empty_reason="no_person_intent"`, память **не читается**; (3) **G2 независимая корроборация** — кандидат подтверждён `db.get_persona_names(chat_id, now)` (только имя+счётчик, факты не читаются); провал/ридер недоступен = `none`, `empty_reason="unknown_person"`; (4) **G3 image-object cross-check** — новый `_IMAGE_OBJECT_LEXICON` (производный от `_ALIAS_STOPLIST` + объектные стемы) + cross-check `_classify_visual`; срабатывание = `none`, `empty_reason="no_person_intent"`; (5) **класс-инвариант:** голый generic-запрос («нарисуй <существительное>») не разблокирует персонализацию независимо от лексиконов; (6) stoplist/min-length понижены до defense-in-depth (не удаляются). **Границы:** Δ DDL=0 (только существующий read `get_persona_names`), Δ каталога=0, канон 12, §104/A5/A6 не затронуты. **Тесты (9-item регресс spec §9 a–i):** (a) 6 cycle-2 repro (alias `Тигр/Роза/Панда/Зайка/Ромашка/Лисичка` + generic-запрос) → `context_required is False`, `resolved_subjects == []`, spy `fact_calls == []`; (b) 3 cycle-1 repro (`Кот/Лис/Малыш`) остаются зелёными; (c) позитив `{"7":"Лёха"}` + «нарисуй как выглядит Лёха»/«Нарисуй Лёху в образе самурая» → `resolved` (склонение сохранено); (d) `{"7":"Лёв"}` + «нарисуй Льва» → `none` (false-negative, принято §24); (e) G1-neg `{"7":"Сергей"}` + голое «нарисуй Сергея» → `none`, память не читается; (f) G2-neg (person-маркер, кандидата нет в `get_persona_names`) → `none`; (g) G3 (совпавший объектный токен `тигр` не персонализирует даже с person-маркером); (h) стоплист активен (stems/`_ALIAS_STOPLIST` не удалены); (i) новая 4+ объектная лексема вне стоплиста в голом generic-запросе → `none` (класс закрыт); плюс полный pytest, JS, `TestBoundsA3` 7 зелёных, каталог без изменений, `git diff --check` 0. **Выход:** правки + тесты + evidence. **Критерий:** REQ-A4-09/-02/-08 (egress закрыт класс-закрывающим способом) → SC-A4-09 (amended), SC-A4-19, SC-A4-02; инвариант 6 + класс-инвариант D13; F1 cycle-2 закрыт материально. **Зависимости:** T-3629, T-3630, T-3631, T-3637 (D13); перед T-3640 cycle 3.

## Блок C — визуальный срез (§22–§23; CONSUME A6)
> REQ-A4-02/-03/-04/-05/-06/-07/-08/-14. Потребление `get_user_context` (A6, канон 12) + envelope A2 — **без реимплементации lookup**; извлечение внешности — пробел A4.
- [x] **T-3631 [@Builder — извлечение визуальных сведений]** — **Цель:** извлечь task-relevant визуальные сведения (внешность/одежда/причёска/очки/борода/характерные детали/предпочтения) из существующих досье/фактов через потребление A6-контракта (`get_user_context` purpose `appearance`/`biography`/`general`; envelope `ToolContext.result_for`); **не** превращать психологический портрет в утверждение о внешности; **не** придумывать точные черты лица; не добавлять биографические факты, не относящиеся к изображению; отсутствие данных — honest `no_data`, не домысел. **Выход:** правки + тесты. **Критерий:** REQ-A4-04/-05/-06/-14 → SC; инварианты 2, 3, 4, 12. **Зависимости:** T-3630, T-3627.
- [x] **T-3632 [@Builder — ограниченный срез + арт/портрет]** — **Цель:** при необходимости — **ограниченный** срез сообщений/RAG (лениво, по требованию, капы; «сотни сообщений» и полный чат запрещены); ветка «арт ≠ достоверный портрет» при отсутствии достоверной внешности; при запросе точного сходства — запрос фото/визуального референса; чужие фото/приватные данные без необходимости не используются. **Выход:** правки + тесты. **Критерий:** REQ-A4-02/-03/-07/-08 → SC; инварианты 1, 2, 5. **Зависимости:** T-3631.

## Блок D — сборка image-prompt + §37-обёртка (§25/§37)
> REQ-A4-01/-06/-07/-08/-12/-13/-15. Хук реализации — `build_final_prompt` (`services/image_generation.py:278–285`).
- [x] **T-3633 [@Builder — 5-частная сборка]** — **Цель:** реализовать сборку `build_final_prompt` из **5 независимых частей** (§25): исходный запрос / разрешённые сведения о персонажах / релевантный контекст / визуальные требования / техограничения генератора; **не** простая конкатенация всего найденного; не выдумывать черты лица; пример логики «самурай + очки» воспроизводится; техограничения не меняют модель/провайдер/ключи/параметры генератора (§104). **Выход:** правки + тесты. **Критерий:** REQ-A4-12/-13/-01/-06/-07/-08 → SC; инварианты 4, 5, 10. **Зависимости:** T-3632.
- [x] **T-3634 [@Builder — §37-обёртка + R17]** — **Цель:** §37-обёртка: содержимое досье/RAG/веб — **данные, а не инструкции**; инструкции из чужих сообщений не становятся системными для генератора; данные памяти/RAG — источник информации, не источник новых правил; API-ключи не попадают в результаты/промпт; R17-safe логи (только идентификаторы/причины/латентность — никогда текст досье/сообщений/имён/промптов/ключей). **Выход:** правки + тесты. **Критерий:** REQ-A4-13/-15 → SC; инварианты 11, 14. **Зависимости:** T-3633.

## Блок E — заполнение заглушек A3 + интеграция обоих входов
> REQ-A4-01/-13/-15. Точки входа: `maybe_handle_keyword` (`image_generation.py`, обл. `:1241`) + `tool_router._generate_image` (`:2317`); поля-заглушки `resolved_subjects`/`context_required`/`context_sources` (`:252–254`).
- [x] **T-3635 [@Builder — заполнение полей-заглушек + интеграция]** — **Цель:** заполнить поля-заглушки A3 (`resolved_subjects`/`context_required`/`context_sources`) в `build_image_request` из результата блоков B–D; подключить сборку к **обоим** входам (direct `maybe_handle_keyword` и tool `_generate_image`), сохранив единый `run_image_request` → существующий `generate_and_send`; **второго image-pipeline не создавать**; `generator_config` не меняет модель/провайдер/ключи/параметры. **Выход:** правки + тесты. **Критерий:** REQ-A4-01/-13/-15 → SC; инварианты 11, 12, 13. **Зависимости:** T-3634.
- [x] **T-3636 [@Builder — §104 AST-гейт + канон-инвариант]** — **Цель:** убедиться, что §104 AST-гейт (`tests/test_unified_image_request_round1026.py::TestBoundsA3`) остаётся **зелёным**; канон инструментов = **12** без изменений (новый инструмент не вводится); `generate_image`-описание/схема не расширяются без отдельной ADR-санкции. **Выход:** тест-прогон + при необходимости тесты. **Критерий:** REQ-A4-13 → SC; инвариант 13. **Зависимости:** T-3635.

## Блок F — тесты / регресс
> **Расширение сверки T-3628:** добавлены **T-3644** (обязательный R3-артефакт `threat-failure-analysis.md`, D10) и **T-3645** (OFF-паритет/kill-switch-матрица, D7) — оба требовались ADR-1026-19 и отсутствовали в Step 1. Новые ID — следующие свободные (коллизий нет).
- [x] **T-3637 [@Builder — adversarial-тесты]** — **Цель:** тесты: генерация с использованием досье; генерация **без** сведений о внешности (арт, не портрет); неоднозначное имя (уточнение/отказ, нет случайного выбора, досье не смешаны); отсутствие полного дампа/полного чата в промпте; отсутствие выдуманных черт лица; 5-частная сборка (не наивная конкатенация); «данные ≠ инструкции» (инъекция из чужого сообщения не становится системной); §37 (нет API-ключей в результатах); R17-логи (нет текста досье); запрос фото при точном сходстве; §104-гейт зелёный; канон 12. **Выход:** новый тест-файл + прогон. **Критерий:** REQ-A4-16/-17/-13/-15/-07/-08 → SC-A4-16, SC-A4-17, SC-A4-13, SC-A4-20, SC-A4-21, SC-A4-14, SC-A4-22; инварианты 1–14. **Зависимости:** T-3635.
- [x] **T-3644 [@Builder — R3 `threat-failure-analysis.md`]** — **Цель:** создать `threat-failure-analysis.md` в формате «threat → mechanism → code → test» (обязательный R3-артефакт, ADR D10): prompt injection (§37), изоляция субъектов (досье не смешиваются), egress приватных данных во внешний генератор, соблюдение image-капов, R17-логи без текста/имён/ключей, OFF-паритет, §104-неизменность; каждый threat привязан к конкретному коду/тесту. **Выход:** `plans/features/image-context-memory-round1026/threat-failure-analysis.md` + ссылки на тесты. **Критерий:** все R3-триггеры (spec §13/ADR D10) разобраны; артефакт — приёмочное доказательство T-3640. **Зависимости:** T-3637.
- [x] **T-3645 [@Builder — OFF-паритет/kill-switch-матрица]** — **Цель:** тесты матрицы флагов (spec §7.4): `UNIFIED_IMAGE_REQUEST_ENABLED=ON` + `IMAGE_CONTEXT_MEMORY_ENABLED=OFF` → A3-паритет (промпт **байт-в-байт** `extract_prompt`, память не читается, `context_required=False`); `UNIFIED_IMAGE_REQUEST_ENABLED=OFF` → legacy, helper не вызывается; ON/ON → обогащение/5-частная сборка. **Выход:** тесты + прогон. **Критерий:** D7; rollback-доказательство hot-отката; SC-A4-01. **Зависимости:** T-3635, T-3636.
- [x] **T-3638 [@Builder — регресс]** — **Цель:** полный pytest (baseline Step 0 → +N/0), JS-тесты; канон **ровно 12** без регрессов; **Δ каталога = 0** (ожидаемо **470/427/445/101/99/21** без изменений); **Δ DDL = 0** (SQLite v12); `APP_VERSION` **2.58.30** (без bump); `git diff --check` = 0; дифф-аудит: ничего из A5-областей (`worker_budget`/`limits.*`/image-limit) и §104 не попало в diff. **Выход:** отчёт прогона. **Критерий:** 0 регрессий; инварианты 12, 13. **Зависимости:** T-3637, T-3644, T-3645.

## Блок G — границы волн
- [x] **T-3639 [@Builder — diff-аудит границ]** — **Цель:** явная проверка границ: §104 `generate_image`/публикация не тронуты; **A5** (`worker_budget`/`limits.*`) вне diff; **A6** lookup/purpose/envelope **не переписан** (только потребление); **A7** (§36 URL/§38+ Decision Making) вне diff; «данные ≠ инструкции» соблюдены; второго резолвера/RAG-контура/аналитики/pipeline нет; канон 12; `APP_VERSION` 2.58.30. **Выход:** diff-аудит-отчёт. **Критерий:** инварианты 12, 13; REQ-A4-02/-13. **Зависимости:** T-3638.

## Блок H — финализация
- [x] **T-3640 [@Reviewer — единый gate]** — **Статус:** ✅ **`Approved` на 4-м цикле (25.09.2026)** — единый Reviewer gate (обе линзы; Scanner отсутствует; risk **R3** сохранён после D13-амендмента); binding **Reviewed-Commit `e8646af`** / Working-Tree-Hash `5f9ac819…` / Spec-Hash `63064ccb…`; артефакт `review-T-3640.md`; findings **C4-N1 (MEDIUM)/C4-N2 (Low)/N2 (Low)** — non-blocking watch-items агрегатного gate эпика. **Цель:** приёмка A4 единым Reviewer gate (Scanner **НЕ назначается** — удалён): C0/H0, обе линзы (requirements/correctness + focused change audit), binding Reviewed-Commit/Working-Tree-Hash/Spec-Hash; проверить: REQ-A4-01…-17 покрыты, §24-идентификация/неоднозначность, визуальный срез без дампа, 5-частная сборка, §37-обёртка, §52 п.3/4/5, §53 (досье получено; одинаковые имена не путаются; генератор получает подготовленный промпт), §54 п.6; канон 12; OFF-паритет `IMAGE_CONTEXT_MEMORY_ENABLED=OFF`; R3-артефакт `threat-failure-analysis.md` (T-3644); risk **R3** (финал, ADR D10/§13; Reviewer может понизить по diff). **Выход:** вердикт (`review-T-3640.md`). **Критерий:** Approved или документированные release-блокеры. **Зависимости:** T-3636, T-3637, T-3638, T-3639, T-3644, T-3645.
- [x] **T-3641 [@PM — merge/archivation]** — **Статус:** ✅ **Выполнено (25.09.2026):** merge `plans/ARCHITECTURE.md` **§88** @Architect (ADR-1026-19 → Accepted); feature-папка **перенесена без потерь** → `plans/archive/image-context-memory-round1026/` (6 файлов: spec, ADR, tasks, evidence, threat-failure-analysis, review-T-3640; SHA-256 до/после идентичны — spec `63064ccb…` байт-в-байт, `tasks.md` изменён только статусной правкой); исходная папка `plans/features/image-context-memory-round1026/` удалена; входящая ссылка §88.10 обновлена на архив. **Цель:** merge в `plans/ARCHITECTURE.md` (новый раздел §88) @Architect (ADR-1026-19 → Accepted); архивация feature-папки `plans/archive/image-context-memory-round1026/` @PM — **полный перенос без потерь** (spec, ADR, tasks, evidence, review). durable-аудит A0 и архивы A0–A6 — только чтение. **Критерий:** архив полон; ссылки проверены; **backlog/metrics правятся только своими задачами на своих шагах** (на сверке — НЕ правились). **@Architect merge ✅ (25.09.2026):** `plans/ARCHITECTURE.md` **§88** добавлен; ADR-1026-19 → **Accepted**; **@PM-архивация ✅ (25.09.2026)** — папка перенесена в `plans/archive/image-context-memory-round1026/`, исходная отсутствует; входящая ссылка §88.10 обновлена. **Зависимости:** T-3640.
- [x] **T-3642 = `DEFERRED_TO_EPIC`** — **Статус:** ✅ **Отмечено (25.09.2026)** — A4 = **accepted into pending epic release** (НЕ «done/VERIFIED»); агрегатный bump `APP_VERSION` **2.58.31** и @DevOps — на границе эпика (здесь НЕ выполняются). **Цель:** пометить A4 как **accepted into pending epic release** (deployment `DEFERRED_TO_EPIC`; НЕ «done/VERIFIED»), вклад в агрегатный релиз Эпика 3. НЕ per-фича деплой; **@DevOps не вызывается**. **Откат:** hot = env-only kill-switch **`IMAGE_CONTEXT_MEMORY_ENABLED=OFF`** (режим A3, байт-в-байт; и/или `UNIFIED_IMAGE_REQUEST_ENABLED=OFF` → legacy); cold = `git revert` к анкеру **`e8646af`**; DDL-откат не нужен (Δ DDL=0). **Зависимости:** T-3640.
- [x] **T-3643 [@PM — handoff → A7]** — **Статус:** ✅ **Выполнено (25.09.2026):** backlog обновлён — A4 ✅ COMPLETED + MERGED (§88) + ARCHIVED + ACCEPTED INTO PENDING EPIC 3 RELEASE; **▶️ следующая — A7 `decision-making`** (§38–§40/§42–§48; канонический ID/разделы — строка таблицы A7), затем A8 → A9 → A10; добавлен компактный блок **«A4 handoff»** для A7 (контракты `get_user_context`/`image_context_memory`, хук `build_final_prompt`, kill-switches, архив/§88, watch-items C4-N1…/F1–F7/L-A6-01…-06, owner-gate A3). **Цель:** вердикт порядка (следующая — **A7 `decision-making`** §38–§40/§42–§48); перенос вход-фактов A4 в постановку A7: image-поток теперь строит единый `ImageRequest` с заполненными полями памяти; контракт `get_user_context`/envelope A2 — вход; границы «A4 = потребитель image-памяти, A7 = Decision Making». **Критерий:** backlog/KG-ссылки согласованы с фактическим порядком (обновление backlog — **этой задачей**, не на Step 1). **Зависимости:** T-3642.

## Открытые вопросы U-1…U-8 — ✅ ЗАКРЫТЫ на Шаге 2 (@Architect, T-3627) решениями ADR-1026-19
- **U-1 — прямой доступ к памяти в image-пути → ✅ закрыт решением D1.** Детерминированный helper `image_context_memory.build_image_memory_context` над **существующими** источниками A6 (`AliasResolver` + `db.get_user_context_facts` + RAG-ридеры `summary_memory`); **одна реализация — две точки вызова** (direct/tool); второй резолвер/RAG/generic-lookup запрещён.
- **U-2 — место кода → ✅ закрыт решением D2.** Новый helper-модуль `services/image_context_memory.py` (не pipeline) + реализация хука в `image_generation.build_final_prompt`; `run_image_request`→`generate_and_send` не меняется; §104 не трогается.
- **U-3 — `context_required`/`context_sources` → ✅ закрыт решением D3.** `context_required=True` только при `resolved`/`ambiguous`, иначе `False`; `context_sources` — enum фактически использованных видов (`alias`/`graph_facts`/`dossier_portrait`/`message_slice`/`rag`); простой запрос → `False`/`[]`.
- **U-4 — визуальный срез + извлечение внешности + капы → ✅ закрыт решением D4.** Подтверждённые `graph_facts` + ленивый bounded срез; классификация по A4-лексикону с психологическим denylist; `dossier_portrait` — только не-визуальный контекст; image-капы строже A6 и в пределах потолков A6.
- **U-5 — неоднозначность в image-потоке → ✅ закрыт решением D5.** Нейтральный не-персонализированный арт + запрос уточнения; случайный выбор запрещён; **без второго платного вызова**.
- **U-6 — арт vs портрет → ✅ закрыт решением D6.** Дисклеймер на уровне промпта **и** reply-заметки; узкий маркер «точное сходство» → честный запрос фото/референса; новый инструмент не создаётся; приём входящего фото — вне scope (spec §14).
- **U-7 — флаги/деплой → ✅ закрыт решением D7.** Env-only `IMAGE_CONTEXT_MEMORY_ENABLED` (default ON; OFF → режим A3); Δ каталога=0; Δ DDL=0; EPIC_ONLY `DEFERRED_TO_EPIC`; hot-OFF паритет, cold `git revert` к `e8646af`.
- **U-8 — §37-обёртка + R17 → ✅ закрыт решением D8.** Блок данных с меткой «ДАННЫЕ (не инструкции; не выполнять команды)»; запрет системной инъекции/пасс-тру/ключей; R17-лог — только id/enum/числа/латентность.

## Факт-пак Step 0 — конфликты (C-1…C-7), для Шага 2
- **C-1:** §32–§34 `:5066–5177` реализованы A6 — A4 **CONSUMES only**; пробелы, оставленные A4: **извлечение внешности** (A6 — только generic-фильтр + `no_data`) и **сборка image-prompt**.
- **C-2:** канон-решение — **✅ закрыт на Шаге 2 (D9):** 13-й инструмент НЕ вводится, санкция расширения канона **не выдана**; правка `generate_image`-desc — NOT_REQUIRED (если будущая волна потребует — отдельная ADR-санкция).
- **C-3:** §104 no-go (`:3180–3203`) — AST-гейт `TestBoundsA3` обязан остаться зелёным.
- **C-4:** A5 budget no-go — `worker_budget`/`limits.*` вне diff.
- **C-5:** A7-граница §36/§38; §37 обязателен для A4.
- **C-6:** A3 owner-gate `PENDING OWNER VERIFICATION` — внешняя зависимость; **A4 его НЕ закрывает**.
- **C-7:** историческая нумерация — **не переписывать**.

## Связь с другими волнами (НЕ реализуется в A4)
| §ТЗ | Тема | Ориентир |
|---|---|---|
| §26–§31 | Дневной лимит изображений | **A5** ✅ (Merge §86; A4 не трогает `worker_budget`) |
| §32–§35 | Досье как инструмент / lookup / RAG / фактчек | **A6** ✅ (Merge §87, ADR-1026-18); A4 **потребляет** |
| §36–§37 | URL/фактчек как функциональность; §37 | §37 — **A4** (обязателен); §36 — **A7** |
| §38–§48 | Decision Making/реакции | **A7/A8** |
| §49/§51 | ExecutionGraph/события | **A9** |
| §52–§54 | Сценарии/критерии/результаты | **A10** (gate); ориентиры §52 п.3/4/5, §53, §54 п.6 — уже в REQ-A4-16/-17 |
| §104 | `generate_image` — не трогать | **no-go** для A4 (AST-гейт) |

## Санкции/подтверждения сверки T-3628 (ADR-1026-19, финал)
- **Risk-Level: `R3`** — финал (spec §13, ADR D10); было preliminary — подтверждено. Обязателен `threat-failure-analysis.md` (T-3644).
- **Deploy: `DEFERRED_TO_EPIC`** (EPIC_ONLY); пер-фича деплой/тег/bump нет; `APP_VERSION` **2.58.30**; @DevOps не вызывается.
- **Канон инструментов: 12** — без 13-го инструмента; ADR-санкция расширения **не выдана** (D9); изменение `TOOL_GENERATE_IMAGE` (даже только `description`) **NOT_REQUIRED**; JSON-схема/параметры не меняются.
- **Δ каталога = 0** (env-only; счётчики **470/427/445/101/99/21** без изменений); F8/ADR-1026-2 не переиздаётся.
- **Δ DDL = 0** (SQLite v12; новых таблиц/колонок/индексов/PG нет).
- **Kill-switch: `IMAGE_CONTEXT_MEMORY_ENABLED`** (env-only, default ON) → OFF = режим A3 (память не читается, промпт = `extract_prompt`); hot-откат; cold = `git revert` к `e8646af`; DDL-откат не нужен.
- **§104 AST-гейт `TestBoundsA3`** (`tests/test_unified_image_request_round1026.py`) — обязан остаться **зелёным** (T-3636).
- **Owner-gate A3 (ADR-1026-16) — `PENDING OWNER VERIFICATION`** (внешний): A4 его **не** закрывает.
- **Промпт-канон ADR-1013-3 = `NOT_APPLICABLE`** (D11): новая сборка image-prompt — не системный/воркерный LLM-промпт.
- **Spec §14 open (зафиксировано, не блокер):** обработка **входящего фото/визуального референса** — **вне scope A4**; §23 реализован как **запрос** референса (текст), не как приём/обработка файла. Если PM/владелец потребует приём фото — отдельная постановка/ADR (потенциально R3+). Финальные имена `IMAGE_CONTEXT_*` — рабочие (меняются только правкой spec/ADR).
- **Release-order:** `A0 → A1 → A2 → A3 → A5 → A6 → A4 → A7`; A4 reuse A3 (`ImageRequest`/хук) и A6 (источники/RAG) без изменения их контрактов.
- **Delta D13 (25.09.2026) — без изменений:** release policy **EPIC_ONLY**; deploy **`DEFERRED_TO_EPIC`**; **Δ DDL = 0**; **Δ каталога = 0**; канон **12**; **Risk-Level R3**; owner-gate A3 `PENDING OWNER VERIFICATION` — внешний.

## Миграционное примечание / запреты
- **@Scanner удалён (24.09.2026)** — обязанности в **едином Reviewer gate**; нигде не назначать.
- **@DevOps на per-фича деплой НЕ вызывается** (release policy **EPIC_ONLY**); release-узел и пер-фича тег не создаются.
- **Без коммитов из A4**; незакоммиченный epic-release кандидат A2/A3/A5/A6 (≙88 файлов dirty) не трогать.
- **`plans/current_task.md` — immutable** (не изменять/не аннотировать/не сокращать); машинный блок `workflow_state.md` — только через `workflow_checkpoint`.
- **`PLANNING_CONSISTENT` получен (T-3628)** — Build (Step 3 @Builder) разрешён; `spec.md`/ADR-1026-19 написал только @Architect (T-3627); PM spec/ADR не пишет.
- **Канон 12 — без новых инструментов** (ADR-санкция расширения не выдана, D9); **A6 lookup не реимплементировать**; **второй image-pipeline / второй резолвер / второй RAG-контур / вторая аналитика — запрещены**.
- **§104 `generate_image` не трогать**; **A5 `worker_budget`/`limits.*` не трогать**.
- **Owner-gate A3 `PENDING OWNER VERIFICATION` — внешний**, A4 его не закрывает.
- **R17/R18:** логи — только R17-safe поля (никогда текст досье/сообщений/имён/промптов/ключей); `current_task.md`, теги, бэкапы, `stash` — целы.
- **`plans/metrics.md`, `plans/ARCHITECTURE.md`, `plans/MEMORY.md`, `plans/backlog.md`, durable-аудит A0** — в планировании/сверке НЕ редактировались (backlog обновляется задачей handoff T-3643; metrics — строкой по факту завершения; ARCHITECTURE — merge T-3641).
