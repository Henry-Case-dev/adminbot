# A3 — `unified-image-request-round1026` (Эпик 3 «Agentic Intelligence», Wave 2, Раунд 10.26)

> **Статус (архивация T-3583 @PM, 25.09.2026):** **COMPLETED + MERGED (§85) + ARCHIVED + deployment DEFERRED_TO_EPIC** — release policy **EPIC_ONLY** (пер-фича деплоя/бампа нет; `APP_VERSION` **2.58.30** без bump; bump 2.58.31 — в агрегатном релизе Эпика 3). Единый Reviewer gate **Approved C0/H0 (R3)** — remediation блокеров B-1/B-2 документальным rework T-3580 (product-код не менялся); binding Reviewed-Commit `e8646af` / Working-Tree-Hash `29e8da0a…` / Spec-Hash `a98d20d1…`; **ADR-1026-16 (D1–D11) Accepted**; merge — `plans/ARCHITECTURE.md` **§85**; архив — `plans/archive/unified-image-request-round1026/` (7 файлов; **`deployment.md` отсутствует** — при `DEFERRED_TO_EPIC` это корректно: пер-фича деплоя нет). **Открыто:** **T-3584 = `DEFERRED_TO_EPIC`** (агрегатный релиз Эпика 3, **НЕ «done/VERIFIED»**), **T-3585 handoff → A5 `image-context-memory`** (§22–§25/§32–§34/§37), owner-гейт прод-проб изображений — **PENDING OWNER VERIFICATION** (живые пробы HY-01/02/04/05 + per-chat из PG + живая A/B описания), non-blocking `L-R1026A3-1…-8` (**-6 реконсилирован** при архивации). **▶️ Следующая — A5 `image-context-memory`** (§22–§25/§32–§34/§37).
> **Статус (Step 2 + сверка T-3554 @PM, 24.09.2026):** **🚦 PLANNING_CONSISTENT.** `spec.md` (**REQ-A3-01…-10 → SC-A3-01…-14**) + **ADR-1026-16 (D1–D11)** созданы @Architect (T-3553 ✅); сверка `tasks.md` ↔ `spec.md` ↔ `ADR-1026-16` выполнена @PM (T-3554 ✅) — заполнены колонки **SC/ADR**, построена **карта «D1…D11 → задачи/блоки»**, **14 инвариантов** привязаны к **SC-A3-01…-14**, deploy/baseline приведены к решению **D8 (`EPIC_ONLY`, без пер-фича тега)**. Product code НЕ менялся; деплой НЕ выполнялся. **Deploy — `DEFERRED` (`EPIC_ONLY`)**: пер-фича деплоя/тега НЕТ. **▶️ Следующий — Step 3 @Builder (T-3555…, блок B — начинать с диагностики §21).**
> **Историческая запись (Step 1 @PM, 24.09.2026):** открытые вопросы (a)–(f) **не решались PM** — переданы Step 2 @Architect; трассируемость **REQ-A3-01…-10 → §18/§19/§20/§21** заполнена; **инварианты — 14**; **Risk — предварительно R3** (подтверждён @Architect: `Risk-Level: R3`). Ответы (a)–(f) закрыты **ADR-1026-16** (см. раздел «Закрытие открытых вопросов»).
>
> **ТЗ (verbatim, `plans/current_task.md`; файл только читается — R17/R18):**
> - **§18 «ГЕНЕРАЦИЯ ИЗОБРАЖЕНИЙ»** (`:4634–4657`): «Сейчас генерация доступна двумя способами.» — «ПЕРВЫЙ: Прямая ключевая фраза: «Бот, нарисуй ...»»; «ВТОРОЙ: Вызов инструмента из LLM, когда пользователь просит изображение в свободной форме.»; «Оба способа должны вести в единый механизм подготовки и генерации изображения.»; «Не создавать два независимых image pipeline.»; «Существующий рабочий прямой вызов сохранить без регрессий.»
> - **§19 «ПРЯМОЙ ВЫЗОВ И TOOL CALLING»** (`:4660–4690`): «Прямой вызов сейчас передаёт генератору только текст после: «Бот, нарисуй». Это поведение необходимо расширить. Но не ломать простые запросы, которые уже работают.»; пример «Бот, нарисуй красного кота» — «Не нужно обязательно запускать RAG или извлекать досье. Достаточно обычного промпта изображения.»; пример «Бот, нарисуй Лёху в образе самурая» — «Здесь может потребоваться дополнительный контекст о конкретном человеке.»; «Не использовать память в каждом запросе ради самого факта её существования.»
> - **§20 «ЕДИНЫЙ IMAGE REQUEST»** (`:4693–4729`): «Создать единое внутреннее представление запроса генерации изображения.»; пример логических полей — `source: direct | tool`, `chat_id`, `requester_id`, `original_message_id`, `user_request`, `resolved_subjects`, `context_required`, `context_sources`, `final_prompt`, `generator_config`; «Не обязательно использовать именно эти имена. Главное — оба способа вызова должны передавать запрос в один и тот же pipeline.»; «Не допускать двойной генерации, когда один запрос одновременно сработал по ключевой фразе и через tool calling.»
> - **§21 «TOOL CALLING ГЕНЕРАЦИИ ИЗОБРАЖЕНИЙ»** (`:4732–4762`): «Исследовать, почему текущие вызовы не работают. Проверить описание инструмента для LLM.»; модель должна понимать «когда вызывать инструмент; какие аргументы обязательны; как формировать описание изображения; что делать при отсутствии контекста; как интерпретировать результат; как сообщать пользователю об ошибке»; «Не заставлять модель самостоятельно формировать внутренние HTTP-запросы.»; «Модель должна передавать структурированные аргументы инструмента.»; «Бэкенд должен валидировать их и вызывать существующий генератор.»; «После выполнения инструмента результат должен корректно возвращаться в цикл обработки tool calls.»; «Не заменять реальную ошибку генератора вымышленным отказом модели.»
> - **Сопутствующий ориентир (не отдельный REQ):** **§25 «СБОРКА IMAGE PROMPT»** (`:4861–4903`) — «Финальный промпт должен собираться из нескольких независимых частей…» — используется только как ориентир для поля `final_prompt` (§20); новых требований не вводит.
> **Границы (verbatim-ориентиры; НЕ реализуются в A3):** **§22 «ИЗОБРАЖЕНИЯ НА ОСНОВЕ ПАМЯТИ»** (`:4765+`) — **A4** (в ImageRequest допускаются только поля-заглушки `context_required`/`context_sources`); **§36–§37** (URL+фактчек, безопасность tool output) — **A7**; **§104 «GENERATE_IMAGE — НЕ ТРОГАТЬ»** (`:3180–3203`) — модель/провайдер/ключи/промпт обложки/параметры/обработка ошибок/порядок публикации/механизм прикрепления **не меняются**; **§85-UI** (`# 85. UI СЛОЙ`, `:2646+`) — **отдельная санкция** (Δ каталога ≠ 0), вне A3.
> **Тип:** backend/image + tool calling (единый image request + диагностика/починка tool-path). **P0** — Wave 2 Эпика 3.
> **Зависит от:** **A0** ✅ (`plans/docs/agentic-audit-round1026.md`, APPROVED, durable-вход по анкорам `#s12-6-7`, `#root-cause`, `#labels`; **HY-01…HY-06**), **A1** ✅ (`tool-coordinator-round1026`, Merge §83, ADR-1026-14 Accepted, deploy 2.58.30 VERIFIED), **A2** ✅ (`tool-chains-round1026`, Merge §84, ADR-1026-15 Accepted, deployment `DEFERRED_TO_EPIC`; вход — envelope §16 `ToolLoopResult.tool_results`/`ToolContext.result_for`, §17-лимиты, канон **11**), **Эпик 1** ✅ (§70) и **Эпик 2** ✅ (S1–S10, deploy 2.58.29→2.58.30). Расширение/правка канона под A3 — **только по санкции Step 2** (базовый канон **11**; санкция D4 — только текст описания).
> **Baseline (Step 0 @Memory для A3; в Step 1 НЕ перемеряется):** HEAD **`e8646af`** == `origin/master` (рабочее дерево содержит **незакоммиченный A2** — ожидаемый pending Epic 3 release candidate, release policy EPIC_ONLY); `APP_VERSION` **2.58.30** (`config/settings.py`, без bump); pytest `.venv` **9125/0** (verified A2); JS **47/47** (verified A2); каталог **REGISTRY 469 / Settings 426 / categorized 444 / GROUPS 100 / `_TAB_BY_GROUP` 98 / TAB_RULES 21**; канон инструментов **11** (`services/tool_schemas.py`, `generate_image` — 9-й, `active_tools:417`); **Δ DDL=0** (SQLite v12); прод активен 2.58.30 (MainPID 652183, verified A1/A2). **Точка отката — коммит `e8646af`** (пер-фича annotated-тег **НЕ создаётся** — `EPIC_ONLY`, решение **ADR-1026-16 D8**; агрегатный анкер/бэкапы — на границе Эпика 3). Теги/бэкапы **не удаляются** (R18).
> **Карта кода (Step 0 @Memory, факт; baseline `e8646af` + незакоммиченный A2 — строки пересняты по факту):**
> **Прямой (keyword) путь:** `services/image_generation.py` — `IMAGE_KEYWORD_RE:93`, `extract_prompt:163`, `resolve_module_enabled:170` (env `IMAGE_GENERATION_ENABLED` AND каталожный `flags.image_generation_module_enabled` per-chat), `generate_and_send:936`, `maybe_handle_keyword:968`; `_consume_budget:491–510` (global → per-chat, PG down → fail-open); `<image_result status="ok">` «повторно не вызывай» `:988–991`. Врезка/пре-гейт в `services/direct_chat_service.py` — вызов пре-гейта `:874–883`, `image_pre_gate_fired` `:881` → принудительное `image_enabled=False` `:934–939` → `generate_image` исключается из `active_tools`; `_image_pre_gate_block:1574`.
> **Tool-путь:** `services/tool_schemas.py` — `TOOL_GENERATE_IMAGE:323` (description EN `:327–330`, `prompt` required `:334–339`, `additionalProperties=false`), `TOOL_CALLING_TOOLS:375` (канон **11**; `generate_image` — 9-й `:384`), `active_tools:417` (`generate_image` исключается при `image_generation_enabled=False` `:433–434`); `services/tool_router.py` — `dispatch:491` (registry `generate_image:502`), `_generate_image:1553` (`resolve_module_enabled` → `_require_str("prompt")` → `generate_and_send`; success/failure JSON, `IMAGE_GENERATION_FALLBACK_PHRASE`); `services/tool_loop.py` — роль `tool` `:438`, возврат результата в цикл (§17-лимиты A2).
> **Сходимость:** оба входа уже сходятся в **один** генератор `generate_and_send`; расхождение — на этапе **подготовки запроса** (§18/§20 его и требуют устранить).
> **Бюджет/наблюдаемость:** `services/worker_budget.py::METRIC_IMAGE_CALLS:35` (env-лимиты global 200 / per-chat 60); ExecutionGraph `services/execution_graph_source.py::STEP_KIND:73` (`image→llm`, `tool→tool`); отдельных `IMAGE_*`-событий для пути §18–§21 нет (обычные `[image] …` + `step='image'`).
> **ID:** **T-3552…T-3586 (35)** — дублей нет, гэпов нет (максимум фактически занятого ID — **T-3551**, последняя задача A2; `T-3552` встречается в `plans/workflow_state.md`/`plans/metrics.md` только как порог «T-IDs ≥ T-3552» в `next_action`, как ID задачи **не использовался**). Сквозная нумерация продолжена от A2 (`T-3525…T-3551`).
> **ADR:** ✅ **ADR-1026-16** (`adr-1026-16-unified-image-request.md`, **D1–D11**, Status **Proposed → Accepted по merge**; финализация — T-3582 @Architect, ожидаемый раздел `plans/ARCHITECTURE.md` **§85**) — создан Step 2 @Architect (T-3553). Открытые вопросы (a)–(f) — **закрыты** (см. раздел ниже). `SpecDecision-a3-image-request` — **CLOSED** (Step 2).
> **Deploy — ⏸ `DEFERRED` (EPIC_ONLY; ADR-1026-16 D8):** release policy **EPIC_ONLY**; A3 **не деплоится отдельно**; bump `APP_VERSION` **2.58.30 → 2.58.31** (точную версию подтверждает @DevOps при эпик-релизе) входит в **агрегатный релиз Эпика 3** (манифест/порядок/откат — на границе эпика). Задача **T-3584 = `DEFERRED_TO_EPIC`** (НЕ «done/VERIFIED»). Новые рубильники — **env-only** `ClassVar` **`UNIFIED_IMAGE_REQUEST_ENABLED`** (default **ON**, Δ каталога=0; прецедент ADR-1026-14 D6 / ADR-1026-15 D5). Откат: hot — env-only kill-switch OFF (OFF → прежнее поведение); cold — `git revert` к коммиту **`e8646af`** (пер-фича тега нет; агрегатный анкер — на границе эпика). Теги/бэкапы не удаляются (R18).
> **Risk (`Risk-Level` — финал @Architect в `spec.md` §10):** ⚠️ **R3** (подтверждён ADR-1026-16 D9) — A3 меняет рантайм-семантику **разделяемых** модулей (`image_generation.py`, `tool_router.py`, `direct_chat_service.py` пре-гейт), затрагивает живой direct-чат и tool-путь, при двух **Critical** KG-рисках. **KG-риски (5, Step 0):** **Critical** — `Risk-a3-unproven-root-cause` (фикс по недоказанной гипотезе); **Critical** — `Risk-a3-direct-path-regression` (регресс прямого вызова); **High** — `Risk-a3-double-generation`; **High** — `Risk-a3-second-image-pipeline` (второй pipeline); **High** — `Risk-a3-fake-model-refusal` (замена реальной ошибки генератора вымышленным отказом модели); **Medium** — `Risk-a3-stale-code-map`. **Усиленные требования R3 (D9):** threat/failure-анализ с тестом на угрозу; rollback-эквивалентность (OFF/legacy); adversarial-приёмка (двойной триггер, tool при пре-гейте, ошибка генератора, OFF-киль-свитч, 2-вызовность) + diff-аудит. Reviewer может повысить при скрытом изменении поведения/утечке.
> **⛔ Границы:** product code — только **Step 3 @Builder** (после Step 1/2 и сверки T-3554); **`plans/current_task.md` (immutable user-input) — НЕ трогать**; машинный блок `OPENCODE_WORKFLOW_STATE_V1` — не редактировать вручную; `spec.md`/ADR — **Step 2 @Architect** ✅; `plans/backlog.md`, `plans/metrics.md`, `plans/ARCHITECTURE.md` — на своих шагах (handoff/merge); durable-аудит A0 `plans/docs/agentic-audit-round1026.md` — **только чтение**; архивы A0–A2 — **только чтение**. **Без коммитов из этого шага.** **@Scanner удалён** — единый Reviewer gate; **нигде не назначать**. **@DevOps на деплой не вызывается** (EPIC_ONLY; baseline-тег тоже не создаётся).

## Трассируемость (REQ → verbatim §ТЗ → блок → задачи → **SC/ADR**)
> REQ-A3-01…-10 выделены из §18/§19/§20/§21 (+ инварианты §104/§22/§36–§37/§85-UI и KG-рисков Step 0). REQ-состав **не сужен**; новых REQ не добавлено; **orphan-REQ нет** (каждый REQ привязан к блоку/задаче/SC). Колонки **SC/ADR** заполнены сверкой **T-3554** по `spec.md` (§2, §8) и **ADR-1026-16** (D1–D11). Каждый SC-A3-01…-14 восходит к REQ; orphan-SC нет.
| REQ | Источник (verbatim, `current_task.md`) | Блок | Задачи | SC / ADR-1026-16 |
|---|---|---|---|---|
| REQ-A3-01 | §18 «Оба способа должны вести в единый механизм подготовки и генерации изображения.» + «Не создавать два независимых image pipeline.» (`:4651–4654`); §20 «Создать единое внутреннее представление запроса генерации изображения.» + «оба способа вызова должны передавать запрос в один и тот же pipeline.» (`:4696–4725`) | A, C | T-3553, T-3559, T-3560, T-3561, T-3562 | **SC-A3-01**; **D2** (единый `ImageRequest`/раннер, reuse `generate_and_send`) |
| REQ-A3-02 | §18 «Существующий рабочий прямой вызов сохранить без регрессий.» (`:4656–4657`); §19 «Но не ломать простые запросы, которые уже работают.» (`:4670–4671`) | D, F | T-3564, T-3572 | **SC-A3-02**; **D2** (`build_final_prompt` reuse `extract_prompt` → прежний промпт) |
| REQ-A3-03 | §19 «Это поведение необходимо расширить.» + «Бот, нарисуй красного кота». «Не нужно обязательно запускать RAG или извлекать досье. Достаточно обычного промпта изображения.» (`:4668–4680`) | C, D | T-3559, T-3565 | **SC-A3-03**; **D5** (политика контекста, RAG/досье не обязательны) |
| REQ-A3-04 | §19 «Бот, нарисуй Лёху в образе самурая». «Здесь может потребоваться дополнительный контекст о конкретном человеке.» + «Не использовать память в каждом запросе ради самого факта её существования.» (`:4682–4690`) | C, D | T-3559, T-3566 | **SC-A3-04**; **D5** (поля-заглушки; память не читается); граница §22/A4 (**D7**) |
| REQ-A3-05 | §21 «Исследовать, почему текущие вызовы не работают. Проверить описание инструмента для LLM.» + список «когда вызывать / какие аргументы обязательны / как формировать описание / что при отсутствии контекста / как интерпретировать результат / как сообщать об ошибке» (`:4735–4746`) | B | T-3555, T-3556, T-3557, T-3558 | **SC-A3-05**; **D1** (протокол HY-01…HY-06) + **D11** (вердикт AMEND-записью); вход HY-01…HY-06 |
| REQ-A3-06 | §21 «Не заставлять модель самостоятельно формировать внутренние HTTP-запросы.» + «Модель должна передавать структурированные аргументы инструмента.» + «Бэкенд должен валидировать их и вызывать существующий генератор.» (`:4748–4755`) | E | T-3567, T-3568 | **SC-A3-06**; **D4** (описание под 6 пунктов §21; валидация бэкендом) |
| REQ-A3-07 | §21 «После выполнения инструмента результат должен корректно возвращаться в цикл обработки tool calls.» (`:4757–4759`) | E | T-3569, T-3571 | **SC-A3-07**; **D7** (REUSE A2-envelope §16) |
| REQ-A3-08 | §21 «Не заменять реальную ошибку генератора вымышленным отказом модели.» (`:4761–4762`) | E, F | T-3570, T-3575 | **SC-A3-08**; **D4** (реальный `reason`/`reason_class` доводится) |
| REQ-A3-09 | §20 «Не допускать двойной генерации, когда один запрос одновременно сработал по ключевой фразе и через tool calling.» (`:4727–4729`) | D, F | T-3563, T-3574 | **SC-A3-09**; **D3** (forced OFF + авторитетный маркер прогона) |
| REQ-A3-10 | Границы/invariants: §104 «GENERATE_IMAGE — НЕ ТРОГАТЬ» (`:3180–3203`); §22 (A4, `:4765+`); §36–§37 (A7); §85-UI (`:2646+`, отдельная санкция); канон **11** (правки — только по санкции D4); Δ DDL/Δ каталога; R17/R18; без 3-го LLM-вызова; REUSE ExecutionGraph | 0, G, H | T-3552, T-3577, T-3578, T-3579 | **SC-A3-10…SC-A3-14**; **D6** (Δ DDL=0/Δ каталога=0, env-only `UNIFIED_IMAGE_REQUEST_ENABLED`), **D7** (границы/reuse), **D8** (deploy EPIC_ONLY), **D9** (R3), **D10** (R17) |

## Карта решений ADR-1026-16 (D1–D11) → задачи/блоки
> Источник — ADR-1026-16, §«AMEND / REUSE-карта». Каждое решение имеет ≥1 задачу; каждая задача блоков B–H опирается на решение ADR (или на границу/инвариант). Осиротевших решений/задач без обоснования нет.
| ADR | Решение (кратко) | Блок | Задачи |
|---|---|---|---|
| **D1** | Диагностика §21 — фикс **не на гипотезе**: протокол HY-01…HY-06 + таблица «причина → минимальный фикс»; вердикт — **AMEND-записью** в ADR после блока B | B | T-3555, T-3556, T-3557, T-3558 (+ T-3553) |
| **D2** | Единый `ImageRequest` + `build_image_request`/`build_final_prompt`/`run_image_request` в `image_generation.py`; reuse существующего `generate_and_send`; **второй pipeline запрещён** | C, E | T-3559, T-3560, T-3561, T-3562, T-3568 |
| **D3** | Двойная генерация: сохранён forced `image_enabled=False` + **авторитетный маркер прогона в `ToolContext`** (`already_handled` → `skipped` без повторной генерации/списания); `<image_result>`-guard — мягкая подсказка | D | T-3563, T-3574 |
| **D4** | `TOOL_GENERATE_IMAGE`: **только текст** `description`/`prompt`-описания под 6 пунктов §21; **канон 11** (состав/имена/порядок/`required`/`factcheck_tools`=3 не меняются); §104 не затронут; **ADR-1013-3 = NOT_APPLICABLE** | E | T-3567, T-3568, T-3570, T-3571 |
| **D5** | Память/досье §19 — **условные поля-заглушки**; в A3 память/RAG/досье **не читаются**; реализация — **A4** | C, D | T-3559, T-3566 |
| **D6** | **Δ DDL=0**; **Δ каталога=0**; env-only `UNIFIED_IMAGE_REQUEST_ENABLED` (default ON); §85-UI — отдельная санкция | G | T-3577, T-3578 |
| **D7** | Границы волн: §104 не трогать; A4/A5/A7 вне diff; **REUSE** ExecutionGraph + **A2-envelope** §16 | E, G | T-3569, T-3576, T-3577, T-3579 |
| **D8** | Deploy = **`DEFERRED` (EPIC_ONLY)**: пер-фича деплоя/тега **нет**; точка отката `e8646af` + `git revert`; агрегатный анкер — граница эпика; @DevOps на деплой не вызывается | 0, H | T-3552, T-3582, T-3584 |
| **D9** | **Risk R3** + усиленные доказательства: threat/failure с тестом на угрозу; rollback-эквивалентность; adversarial-приёмка + diff-аудит | F, H | T-3580, T-3581 (+ T-3572…T-3576 как evidence) |
| **D10** | Наблюдаемость **R17-safe**: reuse существующих `[image]`/`[tools]`-логов + ExecutionGraph; второй аналитики нет | F, G | T-3575, T-3576 (+ T-3579) |
| **D11** | Порядок блоков **B ↔ A** + **AMEND-протокол**: ADR фиксирует протокол, а не результат → сверка T-3554 сразу после T-3553 | A | T-3553, T-3554 |

## Приёмочные инварианты A3 (нарушение = НЕ принято) → привязка к SC-A3-01…-14
> Опираются на §18–§21 + границы §104/§22/§36–§37/§85-UI и KG-риски Step 0. **14 инвариантов**, каждый привязан к своему **SC** и решению ADR (соответствие `spec.md` §5 — 1:1, без потерь).
1. **Один image pipeline:** оба входа строят один `ImageRequest` и вызывают один раннер → существующий `generate_and_send`; второго pipeline/генератора нет. **SC-A3-01; REQ-A3-01; D2; T-3559, T-3560, T-3561, T-3562.**
2. **Прямой канонический запрос без регрессий:** тот же `final_prompt`, та же отправка, тот же бюджет. **SC-A3-02; REQ-A3-02; D2; T-3564, T-3572.**
3. **Простой запрос не требует RAG/досье;** обычного промпта достаточно. **SC-A3-03; REQ-A3-03; D5; T-3565.**
4. **Память условная и в A3 не читается;** поля-заглушки `context_required=False` / `context_sources=[]` / `resolved_subjects=[]`; **§22/A4 не нарушена**. **SC-A3-04; REQ-A3-04; D5; T-3566.**
5. **Диагностика HY-01…HY-06 документирована по каждой HY** (вердикт: `подтверждено` / `опровергнуто` / **`INCONCLUSIVE`** — с блокером и планом проб); фикс не на гипотезе; внешняя причина фиксируется честно — только по живой пробе. **SC-A3-05 (после amend T-3580); REQ-A3-05; D1, D11; T-3555, T-3556, T-3557, T-3558.** *(Реконсилировано [L-R1026A3-6] @PM при архивации T-3583, 25.09.2026: формулировка до amend («доказательна») приведена к пост-amend SC-A3-05 `spec.md`, допускающему документированную ветку `INCONCLUSIVE`; авторитет — `spec.md`.)*
6. **Модель → только структурированные аргументы;** бэкенд валидирует и вызывает существующий генератор; HTTP модель не формирует. **SC-A3-06; REQ-A3-06; D4; T-3568.**
7. **Результат инструмента возвращается в цикл** через reuse A2-envelope; контракт не дублируется. **SC-A3-07; REQ-A3-07; D7; T-3569, T-3571.**
8. **Реальная ошибка генератора доводится;** вымышленный «отказ модели» не подставляется. **SC-A3-08; REQ-A3-08; D4; T-3570, T-3575.**
9. **Двойная генерация исключена** (ровно одна на запрос). **SC-A3-09; REQ-A3-09; D3; T-3563, T-3574.**
10. **§104 `generate_image` вне diff; §85-UI вне scope.** **SC-A3-10, SC-A3-13; REQ-A3-10; D7; T-3577.**
11. **Δ DDL=0; Δ каталога=0;** новые рубильники env-only `ClassVar` (**`UNIFIED_IMAGE_REQUEST_ENABLED`**, default ON); **R17/R18**. **SC-A3-11; REQ-A3-10; D6, D10; T-3576, T-3578.**
12. **2-вызовность System 2 (`await_count==2`) сохранена;** 0 новых LLM-вызовов; OFF/legacy эквивалентен baseline. **SC-A3-12; REQ-A3-10; D7; T-3576, T-3579.**
13. **REUSE ExecutionGraph** (второй аналитики нет); **канон 11** — меняется **только текст** описания `TOOL_GENERATE_IMAGE` (санкция D4; состав/имена/порядок/`required`/`factcheck_tools`=3 не меняются). **SC-A3-13; REQ-A3-10; D4, D7; T-3567, T-3579.**
14. **Deploy = `DEFERRED` (EPIC_ONLY);** вклад в pending epic-release; hot/cold-откат определён; теги/бэкапы целы. **SC-A3-14; REQ-A3-10; D8; T-3552, T-3584.**
> **Закреплённые факты (сверка T-3554):** канон инструментов = **11** (правится **только текст** описания `TOOL_GENERATE_IMAGE` — D4); **§104 / §85-UI / A4 / A7 — вне diff**; **ADR-1013-3 = NOT_APPLICABLE** (промпт-канон не мигрирует); **Δ DDL=0 / Δ каталога=0**; env-only **`UNIFIED_IMAGE_REQUEST_ENABLED`** (default ON); **Risk R3**.

## Связь с другими волнами (границы Эпика 3 — НЕ реализуется в A3)
> A3 — **Wave 2**: единый ImageRequest + диагностика/починка tool-path изображений. Ниже — что A3 **НЕ** реализует. **§104 `generate_image` — НЕ ТРОГАТЬ**; **§85-UI — вне scope**.
| §ТЗ | Тема | Ориентир |
|---|---|---|
| §22–§25 | Изображения на основе памяти, досье/RAG, разрешение имён, сборка prompt | **A4** (в A3 — только поля-заглушки `context_required`/`context_sources`; §25 — сопутствующий ориентир `final_prompt`) |
| §26–§31 | Дневной лимит изображений, Mini App, TZ/сброс | **A5** (handoff T-3585) |
| §32–§35 | `get_user_context`, structured memory lookup | **A6** |
| §36–§37 | Комбинация URL+фактчек, безопасность tool output | **A7** |
| §38–§48 | Новый Decision Making, `action`/`style`, реакции | **A7/A8** |
| §49/§51 | Настройки Decision Making, карта вызовов (REUSE ExecutionGraph) | **A9** |

## Блок 0 — baseline / точка отката (T-3552) — ✅ ЗАКРЫТ
- [x] **T-3552 [@Memory baseline / @DevOps — без пер-фича тега]** — ✅ **ЗАКРЫТ (Step 0 @Memory + сверка T-3554).** **Цель:** зафиксировать baseline и точку отката до любых изменений. **Выход:** baseline-числа зафиксированы; **точка отката = коммит `e8646af`**; пер-фича annotated-тег **НЕ создаётся** (`EPIC_ONLY` — ADR-1026-16 **D8**); бэкапы/агрегатный анкер — на границе Эпика 3. **Критерий:** HEAD == `origin/master` == **`e8646af`** (с ожидаемым **незакоммиченным A2** — pending Epic 3 release candidate; release policy EPIC_ONLY); `APP_VERSION` **2.58.30**; pytest `.venv` **9125/0**, JS **47/47**, каталог **469/426/444/100/98/21**, канон инструментов **11**, **Δ DDL=0** (SQLite v12); теги/бэкапы целы (R18). **SC/ADR:** **SC-A3-14** (+ **SC-A3-11**); **ADR-1026-16 D8**; REQ-A3-10. **Зависимости:** Step 1 @PM ✅. *@DevOps на деплой НЕ вызывается (EPIC_ONLY); пер-фича tag-обвязка и бэкапы перенесены на границу эпика.*
  > **⚠️ Замечание (не решается PM):** в рабочем дереве на момент baseline присутствует **незакоммиченный A2**. Порядок (закоммитить A2-кандидат / снять baseline поверх) — согласовать на границе эпика (@Orchestrator/@DevOps); A3 не теряет незакоммиченный кандидат.

## Блок A — spec + ADR (**Step 2 @Architect**, T-3553 ✅) + **сверка @PM (T-3554 ✅)**
> **SC/ADR:** REQ-A3-01…-10 → **SC-A3-01…-14**; **ADR-1026-16** (D1–D11). Product code не менялся.
- [x] **T-3553 [@Architect]** — ✅ **ЗАКРЫТ (Step 2).** **Цель:** создать `spec.md` (REQ-A3-01…-10, SC-A3-01…-14, трассировка §18/§19/§20/§21 + границы §104/§22/§36–§37/§85-UI) + **ADR-1026-16** (`adr-1026-16-*.md`). **Критерий:** каждый REQ-A3-01…-10 имеет SC; открытые вопросы (a)–(f) закрыты; решения **не строятся на недоказанной гипотезе** (результат блока B учитывается AMEND-записью до фикса — D11); решения не противоречат §18–§21 и не сужают требования; `SpecDecision-a3-image-request` → **CLOSED**. **SC/ADR:** все **SC-A3-01…-14**; **ADR-1026-16 D1…D11** (Status Proposed → Accepted по merge). **Зависимости:** Step 1 @PM ✅.
- [x] **T-3554 [@PM]** — ✅ **ЗАКРЫТ (настоящая сверка, 24.09.2026).** **Цель:** сверка `tasks.md` ↔ `spec.md`/ADR-1026-16. **Выход:** заполнены колонки **SC/ADR** трассируемости; построена карта «**D1…D11 → задачи/блоки**»; 14 инвариантов привязаны к **SC-A3-01…-14**; deploy/baseline приведены к **D8** (без пер-фича тега, анкер `e8646af`); решение **PLANNING_CONSISTENT**. **Критерий:** scope/исключения/риск/приёмка/зависимости/deploy/rollback согласованы; каждый REQ-A3-01…-10 привязан к SC/решению ADR; orphan-REQ/SC нет. **Расхождения:** констатировано одно — пер-фича **annotated-тег `pre-round1026-a3`** из Step 1 (T-3552/T-3584) **противоречил** ADR-1026-16 D8/spec §9 («пер-фича тег не создаётся — EPIC_ONLY»); приведено к ADR **в самом `tasks.md`** (авторитет — `spec.md`/ADR; архитектурное решение не менялось). Прочих расхождений по scope/риску/приёмке/зависимостям нет; новых вопросов к @Architect не возникло. **Зависимости:** T-3553 ✅.
  > **Порядок блоков B ↔ A (ADR-1026-16 D11):** ADR фиксирует **протокол** диагностики, а не её результат → сверка T-3554 выполнена **сразу после T-3553** ✅. Фактический **вердикт HY-01…HY-06 и минимальный фикс** вносятся **AMEND-записью** в ADR-1026-16 **после блока B (T-3558)** — без переписывания контрактов §18/§20; при изменении scope фикса — реконсиляция @Architect через @Orchestrator.

## Блок B — диагностика §21: доказать/опровергнуть HY-01…HY-06 (T-3555…T-3558) — @Builder
> **SC/ADR:** **SC-A3-05**; REQ-A3-05; **ADR-1026-16 D1** (протокол) + **D11** (AMEND). **Критично:** фикс **не строится на гипотезе** (KG `Risk-a3-unproven-root-cause`); каждая гипотеза либо подтверждается пробой/логом, либо опровергается **fact-based**.
> **Границы:** только диагностика/пробы/логи; product-фикс — блоки C–E (после вывода). §104/§85-UI не трогаются. **▶️ Начинать Step 3 с этого блока.**
- [x] **T-3555 [@Builder]** — **Цель:** проба **tool-совместимости провайдера/модели** (HY-01) + проверка **plain-fallback** (HY-04, `tool_loop.py:290–292`): реальный пробный `chat/completions` c `tools`, фиксация `finish_reason`/наличия `tool_calls`; проверить, срабатывает ли plain-fallback при отклонении `tools` провайдером. **Выход:** проба/лог + вывод «подтверждено/опровергнуто». **Критерий:** **REQ-A3-05**; гипотезы HY-01/HY-04 имеют **доказательный** вердикт; без изменений product-поведения. **SC/ADR:** SC-A3-05; D1. **Зависимости:** T-3552, T-3553.
- [x] **T-3556 [@Builder]** — **Цель:** проверить **реальную ошибку генератора** (HY-02): собрать прод/тест-логи `[image] generation failed | reason=…`/`reason_class=…` (`image_generation.py:810–813`/`:914–916`), классифицировать (`timeout/network/unauthorized/bad_request/rate_limited/budget/bad_json/too_large`). **Выход:** сводка по `reason_class`. **Критерий:** **REQ-A3-05**; вердикт по HY-02 доказателен; R17 (без сырых текстов/ключей). **SC/ADR:** SC-A3-05; D1. **Зависимости:** T-3555.
- [x] **T-3557 [@Builder]** — **Цель:** проверить **состояние рубильников** (HY-03: env `IMAGE_GENERATION_ENABLED` + per-chat `flags.image_generation_module_enabled` через `resolve_module_enabled()`), **влияние описания/схемы инструмента** (HY-05: A/B описания `TOOL_GENERATE_IMAGE`) и **разграничение фраз** (HY-06: свободная форма «Бот, нарисуй …» забирается пре-гейтом → tool-путь выключен; симптом относится к свободной форме). **Выход:** матрица фактов + вывод. **Критерий:** **REQ-A3-05**; HY-03/HY-05/HY-06 имеют вердикт; **канон 11** не меняется в диагностике. **SC/ADR:** SC-A3-05; D1. **Зависимости:** T-3556.
- [x] **T-3558 [@Builder]** — **Цель:** **сводный вывод диагностики**: какие из HY-01…HY-06 — реальная причина, какие опровергнуты; минимальный достаточный фикс и **необходимые пробы/логи** для приёмки. **Выход:** `evidence.md` (раздел диагностики) + рекомендация для **AMEND-записи** в ADR-1026-16. **Критерий:** **REQ-A3-05**; фикс (блоки C–E) опирается на **доказанную** причину; KG `Risk-a3-unproven-root-cause` **снят/подтверждён письменно**; вердикт + минимальный фикс передаются в ADR **AMEND-записью (@Architect)**, без переписывания контрактов §18/§20. **SC/ADR:** SC-A3-05; D1, D11. **Зависимости:** T-3555, T-3556, T-3557.

## Блок C — единый ImageRequest-контракт §20 (поля/точка входа; оба источника сходятся до подготовки запроса) (T-3559…T-3562) — @Architect/@Builder
> **SC/ADR:** **SC-A3-01, SC-A3-03, SC-A3-04**; REQ-A3-01/-03/-04; **ADR-1026-16 D2** (контракт/раннер), **D5** (поля памяти — заглушки, реализация — A4). Поля — по §20 (имена рабочих полей — §6 `spec.md`); второй pipeline запрещён.
- [x] **T-3559 [@Architect/@Builder]** — **Цель:** зафиксировать **контракт единого ImageRequest** (§20, §6 `spec.md`): `source: direct|tool`, `chat_id`, `requester_id`, `original_message_id`, `user_request`, `resolved_subjects` (заглушка), `context_required`/`context_sources` (политика/заглушки), `final_prompt`, `generator_config` (резерв) + **точка входа** `build_image_request(...)`/`build_final_prompt(...)`/`run_image_request(...)` в `services/image_generation.py` и место сходимости обоих источников **до** этапа подготовки запроса. **Выход:** контракт в `spec.md`/ADR + тип/карта. **Критерий:** **SC-A3-01/-03/-04**; **REQ-A3-01/-03/-04**; §104 не затронут; поля памяти — только заглушки (§22 → A4). **SC/ADR:** SC-A3-01, SC-A3-03, SC-A3-04; D2, D5. **Зависимости:** T-3553, T-3558.
- [x] **T-3560 [@Builder]** — **Цель:** привести **прямой (keyword) путь** к построению `ImageRequest` (`source=direct`) и передаче в общий pipeline подготовки (пре-гейт → `build_image_request` → `run_image_request`). **Выход:** правки + тесты. **Критерий:** **SC-A3-01, SC-A3-02**; **REQ-A3-01/-02**; прямой путь строит ImageRequest и **не регрессирует** (`build_final_prompt` reuse `extract_prompt` — прежний промпт). **SC/ADR:** SC-A3-01, SC-A3-02; D2. **Зависимости:** T-3559.
- [x] **T-3561 [@Builder]** — **Цель:** привести **tool-путь** к построению `ImageRequest` (`source=tool`) и передаче в **тот же** pipeline подготовки. **Выход:** правки + тесты. **Критерий:** **SC-A3-01**; **REQ-A3-01**; tool-путь строит ImageRequest; **второго pipeline нет**. **SC/ADR:** SC-A3-01; D2. **Зависимости:** T-3559.
- [x] **T-3562 [@Builder]** — **Цель:** гарантировать, что оба источника **сходятся до подготовки запроса** (единая подготовка/сборка `final_prompt`), устранено расхождение §18; §25 — ориентир сборки, без новых правил. **Выход:** правки + тесты. **Критерий:** **SC-A3-01**; **REQ-A3-01**; единственная точка подготовки; дубликатов контрактов нет (KG `Risk-a3-stale-code-map` учтён). **SC/ADR:** SC-A3-01; D2. **Зависимости:** T-3560, T-3561.

## Блок D — устранение двойной генерации §20 + сохранение/расширение прямого пути §18/§19 (T-3563…T-3566) — @Builder
> **SC/ADR:** **SC-A3-02, SC-A3-03, SC-A3-04, SC-A3-09**; REQ-A3-02/-03/-04/-09; **ADR-1026-16 D3** (forced OFF + маркер), **D2** (нет регресса), **D5** (память условно). Открытый вопрос (c) закрыт ADR (D3).
- [x] **T-3563 [@Builder]** — **Цель:** **исключить двойную генерацию** (§20): один запрос исполняется **ровно один раз**; **механизм по ADR-1026-16 D3** — сохранить forced `image_enabled=False` при срабатывании пре-гейта + прокинуть существующий сигнал `image_pre_gate_fired` в аддитивное поле `ToolContext` (напр. `image_request_handled`); `_generate_image` первым делом проверяет маркер и при `True` возвращает `{"status":"skipped","reason":"already_handled",…}` **без вызова генератора/списания**; **не per-phrase костыль**. **Выход:** правки + тесты. **Критерий:** **SC-A3-09**; **REQ-A3-09**; двойная генерация невозможна; KG `Risk-a3-double-generation` закрыт. **SC/ADR:** SC-A3-09; D3. **Зависимости:** T-3562.
- [x] **T-3564 [@Builder]** — **Цель:** **сохранить прямой путь без регрессий** (§18/§19): простой запрос («Бот, нарисуй красного кота») обрабатывается как раньше; существующий рабочий прямой вызов сохранён. **Выход:** правки + тесты. **Критерий:** **SC-A3-02**; **REQ-A3-02**; регрессий нет (тот же `final_prompt`/отправка/бюджет); KG `Risk-a3-direct-path-regression` закрыт. **SC/ADR:** SC-A3-02; D2. **Зависимости:** T-3560, T-3563.
- [x] **T-3565 [@Builder]** — **Цель:** **расширить прямой запрос** (§19) без обязательных RAG/досье: простой запрос не триггерит обязательный RAG/досье; достаточно обычного промпта. **Выход:** правки + тесты. **Критерий:** **SC-A3-03**; **REQ-A3-03**; RAG/досье не обязательны для простых запросов. **SC/ADR:** SC-A3-03; D5. **Зависимости:** T-3564.
- [x] **T-3566 [@Builder]** — **Цель:** **условное** использование контекста/памяти (§19): контекст подключается **только когда нужен**; в ImageRequest — поля-заглушки `context_required=False`/`context_sources=[]`/`resolved_subjects=[]`; **сама память (досье/RAG) — реализация A4**, в A3 **не читается**. **Выход:** правки + тесты. **Критерий:** **SC-A3-04**; **REQ-A3-04**; память не используется «ради факта»; граница §22/A4 не нарушена. **SC/ADR:** SC-A3-04; D5, D7. **Зависимости:** T-3565.

## Блок E — tool calling §21 (описание/структурированные аргументы/валидация/возврат в цикл; без выдуманного отказа) (T-3567…T-3571) — @Architect/@Builder
> **SC/ADR:** **SC-A3-05, SC-A3-06, SC-A3-07, SC-A3-08**; REQ-A3-05/-06/-07/-08; **ADR-1026-16 D4** (описание/ошибки, канон 11), **D2/D7** (reuse генератора/A2-envelope). Правки описания/аргументов `TOOL_GENERATE_IMAGE` — **санкционированы D4** (только текст; канон **11**).
- [x] **T-3567 [@Architect/@Builder]** — **Цель:** по санкции **D4** привести **текст** `description` и поле-описание `prompt` у `TOOL_GENERATE_IMAGE` в соответствие §21 (когда вызывать / обязательные аргументы / как формировать описание / что при отсутствии контекста / как интерпретировать результат / как сообщать об ошибке). **Выход:** правки описания/схемы (**только текст**) + тесты. **Критерий:** **SC-A3-06**; **REQ-A3-05/-06**; **состав/имена/порядок/`required`/`factcheck_tools`=3 не меняются**, **канон = 11**; «новое — в хвост» (ADR-1020-4) **не применяется** (инструмент не добавляется); §104 не затронут. **SC/ADR:** SC-A3-06; D4. **Зависимости:** T-3553, T-3558.
- [x] **T-3568 [@Builder]** — **Цель:** модель передаёт **структурированные аргументы**; **бэкенд валидирует** (`_require_str("prompt")`) и вызывает **существующий** генератор (`_generate_image` → `run_image_request` → `generate_and_send`); модель не формирует HTTP-запросы. **Выход:** правки + тесты. **Критерий:** **SC-A3-06**; **REQ-A3-06**; валидация на бэкенде; reuse существующего генератора (§104). **SC/ADR:** SC-A3-06; D2, D4. **Зависимости:** T-3567.
- [x] **T-3569 [@Builder]** — **Цель:** результат инструмента **корректно возвращается в цикл** обработки tool calls (§21): reuse **A2-envelope §16** (`ToolLoopResult.tool_results`/`ToolContext.result_for`), `_classify_output` → `tool_results`, без второго контракта. **Выход:** правки + тесты. **Критерий:** **SC-A3-07**; **REQ-A3-07**; возврат в цикл работает; контракт A2 переиспользован (не дублирован). **SC/ADR:** SC-A3-07; D7. **Зависимости:** T-3568.
- [x] **T-3570 [@Builder]** — **Цель:** **реальная ошибка генератора не подменяется вымышленным отказом модели** (§21): ошибка/`reason_class` генератора доводится в результат инструмента (`status:"error", reason:…`); никакого синтетического «отказа модели»; текст нейтрально-пользовательский. **Выход:** правки + тесты. **Критерий:** **SC-A3-08**; **REQ-A3-08**; KG `Risk-a3-fake-model-refusal` закрыт; R17 (без сырых текстов). **SC/ADR:** SC-A3-08; D4. **Зависимости:** T-3568.
- [x] **T-3571 [@Builder]** — **Цель:** **интерпретация результата / сообщение об ошибке** пользователю по описанию инструмента (§21) — корректное информирование без подмены причины. **Выход:** правки + тесты. **Критерий:** **SC-A3-07, SC-A3-08**; **REQ-A3-07/-08**; пользователь получает различающее сообщение. **SC/ADR:** SC-A3-07, SC-A3-08; D4, D7. **Зависимости:** T-3569, T-3570.

## Блок F — тесты / R17 / регресс (прямой путь, tool-путь, отсутствие двойной генерации, ошибки) (T-3572…T-3576) — @Builder
> **SC/ADR:** **SC-A3-01…-14** (тесты покрывают все SC); REQ-A3-01…-10; **ADR-1026-16 D9** (усиленная приёмка R3), **D10** (R17). R17-safe; регресс живого direct-чата (2-вызовность, OFF/legacy).
- [x] **T-3572 [@Builder]** — **Цель:** тесты **прямого пути без регрессий**: простой запрос работает как раньше; существующий рабочий прямой вызов сохранён. **Выход:** тесты. **Критерий:** **SC-A3-02**; **REQ-A3-02**; регрессий нет. **SC/ADR:** SC-A3-02; D2. **Зависимости:** T-3564, T-3565.
- [x] **T-3573 [@Builder]** — **Цель:** тесты **tool-пути**: структурированные аргументы; валидация на бэкенде; вызов существующего генератора; возврат результата в цикл (reuse A2-envelope). **Выход:** тесты. **Критерий:** **SC-A3-06, SC-A3-07**; **REQ-A3-06/-07**. **SC/ADR:** SC-A3-06, SC-A3-07; D4, D7. **Зависимости:** T-3568, T-3569.
- [x] **T-3574 [@Builder]** — **Цель:** тест **отсутствия двойной генерации** (ключевая фраза + tool одновременно): ровно одна генерация на запрос; tool возвращает `skipped`/`already_handled`; бюджет списан один раз. **Выход:** тесты. **Критерий:** **SC-A3-09**; **REQ-A3-09**; KG `Risk-a3-double-generation` подтверждён закрытым. **SC/ADR:** SC-A3-09; D3. **Зависимости:** T-3563.
- [x] **T-3575 [@Builder]** — **Цель:** тесты **ошибок**: реальная ошибка генератора доводится, не подменяется вымышленным отказом; ошибка инструмента даёт различающий результат. **Выход:** тесты. **Критерий:** **SC-A3-08**; **REQ-A3-08**. **SC/ADR:** SC-A3-08; D4, D10. **Зависимости:** T-3570, T-3571.
- [x] **T-3576 [@Builder]** — **Цель:** **R17-тесты** + **регресс живого direct-чата**: в логах — только числа/коды/id/`reason_class`/`source`/длины; **2-вызовность** (`await_count==2`) сохранена; OFF/legacy-путь байт-в-байт; kill-switch **`UNIFIED_IMAGE_REQUEST_ENABLED`** OFF → прежнее поведение. **Выход:** тесты. **Критерий:** **SC-A3-11, SC-A3-12**; **REQ-A3-10**; живой диалог не регрессирует; R17 соблюдён. **SC/ADR:** SC-A3-11, SC-A3-12; D6, D7, D10. **Зависимости:** T-3572, T-3573, T-3574, T-3575.

## Блок G — интеграция/границы (§104, A4/A7/§85-UI, ExecutionGraph reuse, бюджет `image_calls`) (T-3577…T-3579) — @Architect/@Builder
> **SC/ADR:** **SC-A3-10…-14**; REQ-A3-10; **ADR-1026-16 D6** (Δ DDL/каталога, env-only), **D7** (границы/reuse). **Границы и инварианты** — не реализация.
- [x] **T-3577 [@Architect/@Builder]** — **Цель:** подтвердить границы: **§104 `generate_image`** не тронут (вне diff); **A4 (§22–§25)** — вне A3; **§36–§37 (A7)** — вне A3; **§85-UI — отдельная санкция** (Δ каталога ≠ 0), вне A3; **Δ DDL=0 / Δ каталога=0**. **Выход:** evidence/diff-аудит. **Критерий:** **SC-A3-10, SC-A3-13**; **REQ-A3-10**; границы не нарушены. **SC/ADR:** SC-A3-10, SC-A3-13; D6, D7. **Зависимости:** T-3576.
- [x] **T-3578 [@Architect/@Builder]** — **Цель:** интеграция с **A2-envelope/рубильниками**: reuse контрактов A2 (§16) без дублирования; новый рубильник — **env-only `ClassVar` `UNIFIED_IMAGE_REQUEST_ENABLED`** (default ON; Δ каталога=0); учесть вход `resolve_module_enabled` (env AND per-chat). **Выход:** правки/заключение + тесты. **Критерий:** **SC-A3-11**; **REQ-A3-10**; дублей контрактов нет (KG `Risk-a3-stale-code-map`); Δ DDL/Δ каталога зафиксированы (**0/0**). **SC/ADR:** SC-A3-11; D6, D7. **Зависимости:** T-3577.
- [x] **T-3579 [@Architect/@Builder]** — **Цель:** подтвердить **отсутствие 3-го LLM-вызова** / сохранение 2-вызовности System 2 direct-чата; **REUSE ExecutionGraph** (вторая аналитика/карта вызовов не создаётся; `STEP_KIND` `image→llm`/`tool→tool` — существующий); бюджет `worker_budget.image_calls` (global/per-chat) **не дублируется**. **Выход:** заключение + evidence. **Критерий:** **SC-A3-12, SC-A3-13**; **REQ-A3-10**; 2-вызовность; второй аналитики нет; бюджет единый. **SC/ADR:** SC-A3-12, SC-A3-13; D7, D10. **Зависимости:** T-3578.

## Блок H — финализация (единый Reviewer gate БЕЗ Scanner, merge, архивация, **deploy = `DEFERRED_TO_EPIC`**, handoff → A5/A4) (T-3580…T-3586)
> **SC/ADR:** все **SC-A3-01…-14**; **ADR-1026-16** (D8 deploy/bump EPIC_ONLY, D9 R3-приёмка, D11 AMEND). **T-3584 — `DEFERRED_TO_EPIC`** (агрегатный релиз Эпика 3).
- [x] **T-3580 [@Reviewer]** — **Цель:** **линза 1 единого Reviewer gate** (requirements/correctness): §18 (единый механизм/нет второго pipeline; прямой вызов без регрессий); §19 (расширение без RAG по умолчанию; память условно); §20 (единый ImageRequest; нет двойной генерации); §21 (структурированные аргументы/валидация/возврат в цикл; реальная ошибка не подменена); REQ-A3-01…-10; **SC-A3-01…-14**; нет 3-го LLM-вызова; отсутствие ложных закрытий. **Выход:** `review.md` с вердиктом. **Критерий:** доказанность причины (блок B), отсутствие регрессий прямого пути; все release-блокеры закрыты или отклонены письменно; Critical/High = 0 → «к приёмке ДА» либо блокеры возвращены. **SC/ADR:** SC-A3-01…-14; D1, D9. **Зависимости:** T-3576, T-3577, T-3578, T-3579.
- [x] **T-3581 [@Reviewer]** — **Цель:** **линза 2 единого Reviewer gate** — focused change audit (содержательные обязанности удалённого @Scanner): канон инструментов (**11**; правки описания — **только текст** по санкции D4; дубликатов нет); **Δ DDL / Δ каталога**; R17/R18; **§104 `generate_image`** вне diff; **§22/A4** и **§36–§37/A7** вне diff; **§85-UI** вне diff; REUSE ExecutionGraph (второй аналитики нет); обратная совместимость живого direct-чата (2-вызовность, OFF/legacy); отсутствие утечек и скрытых побочных эффектов; пересчитанные `Reviewed-Commit`/`Working-Tree-Hash`/`Spec-Hash`. **Выход:** единый авторитетный результат в **`review.md`** (отдельный Scanner-отчёт **не создаётся** — Scanner удалён). **Критерий:** Critical/High = 0 → «к приёмке/эпик-релизу ДА» либо блокеры возвращены; binding соответствует фактическому состоянию. **SC/ADR:** SC-A3-10…-14; D6, D7, D9, D10. **Зависимости:** T-3580.
- [x] **T-3582 [@Architect]** — **Цель:** merge: раздел `plans/ARCHITECTURE.md` (**номер по факту; ожидается §85**) + финализация **ADR-1026-16** (Accepted) + **AMEND-запись** вердикта HY-01…HY-06 и минимального фикса (из T-3558, D11) + карта решений; реконсиляция, если Step 2 менял архитектурные решения. **Выход:** раздел + ADR. **Критерий:** контракт ImageRequest §20 / диагностика §21 / границы задокументированы; архитектура сверена; AMEND не переписывает контракты §18/§20. **SC/ADR:** все SC; D1, D8, D11. **Зависимости:** T-3580, T-3581 (при findings — rework @Builder до merge).
- [x] **T-3583 [@PM]** — **Цель:** архивация фичи в `plans/archive/` с сохранением ID/ссылок. **Выход:** папка `plans/archive/unified-image-request-round1026/` (+ **`deployment: DEFERRED_TO_EPIC`** в карточке фичи: A3 **принята в pending epic-release candidate Эпика 3**, но **не задеплоена**). **Критерий:** только после подтверждённого единого **`Approved`** + binding + reconcile @Architect; **deploy НЕ требуется** для архивации при `EPIC_ONLY` — фиксируется `DEFERRED_TO_EPIC` (не на основании чекбоксов @Builder); пути/ссылки разрешены, истории не теряются. **SC/ADR:** SC-A3-14; D8. **Зависимости:** T-3582.
- [ ] **T-3584 [@DevOps]** — ⏸ **`DEFERRED_TO_EPIC` — НЕ «done/VERIFIED» (release policy `EPIC_ONLY`; как пер-фича деплой НЕ выполняется).** **Цель (на границе Эпика 3, под агрегатным Reviewer gate):** включить A3 в **агрегированный релиз Эпика 3** — bump `APP_VERSION` **2.58.30 → 2.58.31** (точную версию подтверждает @DevOps при эпик-релизе) + `README.md` + cache-bust; push без force; прод ff; `/api/health` 200; `database is locked`=0; применимые §114-проверки; манифест/порядок/откат эпика. **Выход:** `deployment.md` (**VERIFIED** в составе эпик-релиза либо явно **NOT_APPLICABLE**). **Критерий:** вердикт с evidence **в контексте эпик-релиза**; **откат — hot: env kill-switch `UNIFIED_IMAGE_REQUEST_ENABLED=false`; cold: `git revert` к коммиту `e8646af` + агрегатный анкер Эпика 3** (пер-фича annotated-тега `pre-round1026-a3` **НЕТ** — снят по ADR-1026-16 D8); теги/бэкапы не удаляются (R18). **SC/ADR:** SC-A3-14; D8. **Зависимости:** T-3583; **исполнение — на границе эпика**. **Статус подтверждён архивацией T-3583 @PM, 25.09.2026 — остаётся `DEFERRED_TO_EPIC` (агрегатный релиз Эпика 3); НЕ «done/VERIFIED».**
- [ ] **T-3585 [@PM/@Orchestrator]** — 📤 **HANDOFF OPEN (исполняется @PM/@Orchestrator при старте следующей фичи).** **Цель:** handoff → **A5** `unified-image-request` смежной волны (дневной лимит изображений, §26–§31) и → **A4** (изображения на основе памяти, §22–§25): зафиксировать вход A5/A4 (единый ImageRequest, поля-заглушки `context_required`/`context_sources`/`resolved_subjects`, envelope §16, канон 11, вход `resolve_module_enabled`, env-only `UNIFIED_IMAGE_REQUEST_ENABLED`), обновить backlog-указатель/гейты. **Выход:** handoff-запись. **Критерий:** границы A3/A4/A5 разведены; следующий actionable определён; ни одна открытая граница не потеряна. → **Handoff-запись закреплена: handoff-запись ✅ A3 в `plans/backlog.md` (T-3583 @PM, 25.09.2026; вход A5 — см. карточку выше / «Итоги выполнения»); исполнение — при старте A5.** **SC/ADR:** SC-A3-13, SC-A3-14; D7, D8. **Зависимости:** T-3584.
- [ ] **T-3586 [@PM]** — **N/A (резерв не потребовался: трассировка A3 замкнута сверкой T-3554; подтверждено T-3583 @PM, 25.09.2026).** Финальная проверка трассируемости A3: полный REQ-A3-01…-10 → §18/§19/§20/§21 → блок → задача → SC → ADR → evidence замкнут; метрики фичи переданы @Orchestrator. **Выход:** подтверждение покрытия. **Критерий:** orphan-REQ нет; каждый REQ имеет задачу/SC; состав задач не сужен. **Зависимости:** T-3585. **Резерв — N/A при замкнутой трассировке на Step 2/T-3554.**

## Закрытие открытых вопросов (a)–(f) → **ADR-1026-16** (Step 2 @Architect)
> Ответы — `spec.md` §4/§12 + **ADR-1026-16** (D1–D11). PM не решал; ниже — привязка.
- **(a) Причина tool-отказа → ✅ D1.** Протокол HY-01…HY-06 (§7 `spec.md`); **фикс не на гипотезе**; вердикт + минимальный фикс — **AMEND-записью** после блока B (T-3558); внешняя причина фиксируется честно. Задачи — T-3555…T-3558.
- **(b) Форма ImageRequest / точка входа → ✅ D2.** Единый `ImageRequest` + `build_image_request`/`build_final_prompt`/`run_image_request` в `services/image_generation.py`; §6 `spec.md`. Задачи — T-3559…T-3562.
- **(c) Отсутствие двойной генерации → ✅ D3.** Сохраняется forced `image_enabled=False` + авторитетный маркер прогона в `ToolContext` (`already_handled` → `skipped`); `<image_result>`-guard сохранён. Задачи — T-3563, T-3574.
- **(d) Канон/описание инструмента → ✅ D4.** Санкционировано **текстовое** уточнение описания под 6 пунктов §21; состав/имена/порядок/`required`/`factcheck_tools`=3 не меняются; **канон 11**; §104 не затронут; **ADR-1013-3 N/A**. Задачи — T-3567, T-3568.
- **(e) Политика памяти/досье §19 → ✅ D5.** Условная; в A3 только поля-заглушки + политика; реализация — A4; «память ради факта» не используется. Задачи — T-3559, T-3566.
- **(f) Δ DDL / Δ каталога / границы / deploy / риск → ✅ D6–D9.** **Δ DDL=0**, **Δ каталога=0**, env-only `UNIFIED_IMAGE_REQUEST_ENABLED`; §85-UI — отдельная санкция; deploy = **DEFERRED (EPIC_ONLY)**; **Risk-Level R3**. Задачи — T-3552, T-3577, T-3578, T-3584.

## Миграционное примечание
- **@Scanner удалён (24.09.2026).** Его содержательные обязанности (focused change audit: инварианты diff, Δ DDL/каталог, R17/R18, §104/§22/§36–§37/§85-UI, REUSE ExecutionGraph, обратная совместимость, утечки/побочные эффекты) переданы **линзе 2 единого Reviewer gate** (T-3581). **Отдельный Scanner-отчёт не создаётся**; @Scanner **нигде не назначается**. Прецеденты — A1 (`T-3518/T-3519`), A2 (`T-3546`).
- **@DevOps — только `DEFERRED_TO_EPIC` (T-3584) на границе эпика.** Release policy **EPIC_ONLY**: пер-фича деплоя **и пер-фича annotated-тега** нет; **@DevOps на деплой/baseline-тег НЕ вызывается** до границы эпика.
- **ADR-1026-16 ✅ создан на Step 2 (T-3553); Status Proposed → Accepted по merge (T-3582).** Открытые вопросы (a)–(f) — **закрыты**. `SpecDecision-a3-image-request` — **CLOSED**. Диагностический вердикт — **AMEND-запись** после блока B (T-3558), не переписывание контрактов.
- **Канон промптов ADR-1013-3 = NOT_APPLICABLE** (промпты изображений не меняются; правки промпт-канона не выполняются; меняются лишь описания инструментов — D4).

## Контроль качества сверки T-3554 (PM)
- **T-ID:** **T-3552…T-3586 (35)**; дублей нет; гэпов нет; максимум занятого до A3 — **T-3551**.
- **REQ:** **10** (REQ-A3-01…-10); orphan-REQ нет; каждый REQ привязан к SC/блоку/задаче/решению ADR.
- **SC:** **14** (SC-A3-01…-14); orphan-SC нет; каждый SC восходит к REQ (§8 `spec.md`).
- **ADR-1026-16:** **D1–D11**; каждое решение имеет ≥1 задачу (карта выше); ADR-1013-3 = **NOT_APPLICABLE**.
- **Инварианты:** **14** — привязаны к SC-A3-01…-14 (1:1, без потерь).
- **Блоки:** **0, A, B, C, D, E, F, G, H** (9 блоков).
- **Deploy:** `DEFERRED` (`EPIC_ONLY`, D8); откат — env OFF / `git revert` к **`e8646af`**; пер-фича тега нет; агрегатный анкер — граница эпика.
- **Расхождения:** одно (annotated-тег `pre-round1026-a3`) — приведено к ADR-1026-16 D8 в `tasks.md`; новых вопросов к @Architect не возникло; scope/исключения/риск/приёмка/зависимости/rollback согласованы.
- **Scanner:** не назначался (удалён); его обязанности — линза 2 Reviewer (T-3581).
- **Product code не менялся; коммитов нет; `plans/current_task.md` и машинный блок не трогались; R17/R18 соблюдены.**
- **▶️ Следующий шаг:** **Step 3 @Builder — блок B (T-3555…T-3558, начинать с диагностики §21).** *(Историческая запись на момент T-3554; актуальные итоги — ниже.)*

## Итоги выполнения (архивация T-3583 @PM, 25.09.2026)
- **T-3552 ✅** — baseline/точка отката: HEAD **`e8646af`** (== `origin/master`; рабочее дерево — pending epic-release candidate с незакоммиченным A2); **пер-фича annotated-тег НЕ создавался** (EPIC_ONLY — ADR-1026-16 D8); `APP_VERSION` 2.58.30; pytest `.venv` 9125/0; JS 47/47; канон 11; Δ DDL=0.
- **T-3553 ✅** — `spec.md` (**REQ-A3-01…-10 / SC-A3-01…-14**) + **ADR-1026-16** (D1–D11); после remediation B-1 — amend SC-A3-05/§3/§7 (ветка `INCONCLUSIVE` + owner-гейт прод-проб), §10 п.1 (B-2). Final Spec-Hash — **`a98d20d1…`** (совпадает с binding re-review).
- **T-3554 ✅** — сверка @PM: **PLANNING_CONSISTENT** (SC/ADR-колонки, карта D1–D11 → задачи, 14 инвариантов, Risk R3; расхождение с пер-фича тегом приведено к D8).
- **T-3555…T-3558 ✅** — диагностика §21 (блок B): **HY-06 механика подтверждена** (пре-гейт ловит только «бот+нарисуй/сгенерируй/создай» формы), **HY-03 опровергнута** (env-слой — все ON), «виноват промпт» — опровергнуто A0; **HY-01/02/04/05 — `INCONCLUSIVE`** (билд-окружение недоступно) + owner-гейт прод-проб; протокол — `image-diagnostics.md`; фикс не на гипотезе.
- **T-3559…T-3579 ✅** — реализация блоков C–G: единый **ImageRequest** + `build_final_prompt`/`run_image_request` в `services/image_generation.py` (оба входа direct/tool → существующий `generate_and_send`; второй pipeline запрещён); защита от двойной генерации (пре-гейт + авторитетный маркер `ToolContext.image_request_handled` → `skipped/already_handled`); `TOOL_GENERATE_IMAGE` — текст описания (6 пунктов §21), канон **11**; env-only `UNIFIED_IMAGE_REQUEST_ENABLED`; память/досье — поля-заглушки; §104 — AST-гейт генератора; границы A4/A7/§85-UI подтверждены. pytest `.venv` 9125 → **9156 passed / 0 failed** (+31), JS **47/47**, `git diff --check`=0; **Δ DDL=0 / Δ каталога=0**; 2-вызовность; R17.
- **T-3580/T-3581 ✅** — единый Reviewer gate (Scanner удалён; отдельного Scanner-approval нет): итер.1 **Needs Fixes** — блокеры **B-1** (High: документированные вердикты HY-01…HY-06 с веткой `INCONCLUSIVE`) и **B-2** (Medium: R3-обязательный threat/failure-анализ) → документальный rework **T-3580** (amend spec/ADR @Architect + `threat-failure-analysis.md`; **product-код не менялся** — байт-идентичность подтверждена @Reviewer) → целевой re-review **Approved C0/H0 (R3)**; binding `e8646af` / `29e8da0a…` / `a98d20d1…`; non-blocking `L-R1026A3-1…-8`.
- **T-3582 ✅** — merge `plans/ARCHITECTURE.md` **§85** + **ADR-1026-16 Accepted** (D1–D11) + AMEND-вердикт HY.
- **T-3583 ✅** — эта архивация: папка перенесена в `plans/archive/unified-image-request-round1026/` (**7 файлов**; **SHA-256 до/после переноса идентичны 7/7**; финализирован **только `tasks.md`** — статус-карточка, чекбоксы T-3555…T-3582, T-3586, инвариант 5 = реконсил [L-R1026A3-6], этот раздел; остальные 6 файлов байт-в-байт без изменений). **`deployment.md` отсутствует** — при `deployment: DEFERRED_TO_EPIC` это корректно (пер-фича деплоя нет). Чекбоксы T-3555…T-3582 проставлены @PM при архивации по `review.md`/`evidence.md` (**Approved C0/H0**, merge §85); тексты задач не переписывались.
- **T-3584 ⏸ `DEFERRED_TO_EPIC`** — агрегатный релиз Эпика 3 (bump `APP_VERSION` 2.58.30 → 2.58.31, эпик-манифест/порядок/откат; живая приёмка §18–§21 + прод-пробы HY — owner-гейт/эпик-гейт) — **НЕ «done/VERIFIED»; @DevOps не вызывался.**
- **T-3585 📤 HANDOFF OPEN** — вход следующей фичи зафиксирован (см. карточку выше и handoff-запись ✅ A3 в `plans/backlog.md`); исполнение — при старте следующей фичи (Step 1 @PM).
- **T-3586 — N/A** (резерв не потребовался; трассировка замкнута T-3554; метрики фичи — строка **10.26-A3** в `plans/metrics.md`, переданы @Memory/@Orchestrator на Step 10).

**SHA-256 (фиксируются до и после переноса; после «Итоги» актуален финальный хеш `tasks.md`):**
- `adr-1026-16-unified-image-request.md` — `eee21b961617f2c646c4e3a2ac653de5adf2bb75514c87e71744034ccefb176d`
- `evidence.md` — `659e9a238998cffeb383856f24cee3d293e043e213e09a9fc2a99ac30f585e27`
- `image-diagnostics.md` — `edb0e282d6a33df3e8a697907dc40417cdad90ffb9d734eba2ef40977440614b`
- `review.md` — `fc2eb1278eb3d6bb0a1c20e45b84171be1da2ac1842df62cbbb09b7fd3b1546b`
- `spec.md` — `a98d20d15a382c172eb07a762cb8eea3273eb757f7f6fc125615d137f057e9e2` (binding Spec-Hash)
- `threat-failure-analysis.md` — `e4ffbc0c936546685f317f3c8fae57f8bcdcb4fd9af0411af26efa8095faf7de`
- `tasks.md` — до финализации `7268f22f3fa80ed807794d3753462ab317d0d6ba753279d9ed21be0ef6cf99a7`; после финализации (правка — только документальная: карточка/чекбоксы/инвариант 5/этот раздел) хеш изменён; целостность остальных 6 файлов подтверждена байт-в-байт (SHA-256 до/после переноса идентичны).
