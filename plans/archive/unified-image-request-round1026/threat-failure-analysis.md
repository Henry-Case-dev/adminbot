# A3 `unified-image-request-round1026` — Threat / Failure analysis (R3, ADR-1026-16 D9, remediation B-2)

- **Epic-ID:** Эпик 3 «Agentic Intelligence» (Wave 2). **Feature-ID:** `unified-image-request-round1026`.
- **Risk-Level:** **R3** (не понижен; обоснование — ADR-1026-16 D9 / spec §10). Два Critical KG-риска
  (`Risk-a3-unproven-root-cause`, `Risk-a3-direct-path-regression`) + три High (`Risk-a3-double-generation`,
  `Risk-a3-second-image-pipeline`, `Risk-a3-fake-model-refusal`) + blast radius разделяемых модулей живого direct-чата.
- **Baseline:** HEAD **`e8646af`** == `origin/master`; рабочее дерево содержит **незакоммиченный A2**
  (pending Epic 3 release candidate — сохранён) + незакоммиченный A3. Все `file:line` ниже проверены по
  **фактическому состоянию рабочего дерева** 25.09.2026 (анкер отката — `e8646af`; `EPIC_ONLY`, пер-фича тега нет).
- **Источники фактов:** `evidence.md` §1–§5, `image-diagnostics.md` §0–§2 (факты, не измышления; прод-пробы — честные
  `INCONCLUSIVE`, owner-гейт `PENDING OWNER VERIFICATION`).
- **Статус:** дополнение к реализованной A3 (T-3555…T-3579); ожидает независимого @Reviewer.
  **Deploy — `DEFERRED_TO_EPIC` (EPIC_ONLY).**

## 1. Угрозы → механизм защиты → код → тест/доказательство

| # | Угроза | Механизм защиты | Реализация (файл:строка, факт) | Тест / доказательство |
|---|---|---|---|---|
| **T1** | **Двойная генерация** (High `Risk-a3-double-generation`): один запрос исполняется и по ключевой фразе, и через tool call — двойное списание бюджета + дубликат в чат | **3 слоя defense-in-depth.** Слой 1 (сохранён из baseline): срабатывание пре-гейта → принудительное `image_enabled=False` → `generate_image` не объявляется модели (`active_tools` исключает инструмент). Слой 2 (**новый, авторитетный, серверный**): сигнал `image_pre_gate_fired` прокидывается в аддитивное поле `ToolContext.image_request_handled`; `_generate_image` **первым делом** проверяет маркер и при `True` возвращает структурный `{"status":"skipped","reason":"already_handled"}` **без вызова генератора** — без второго платного вызова и без списания `image_calls`. Слой 3 (сохранён): `<image_result>`-guard — мягкая модельная подсказка, авторитетом является Слой 2. Маркер привязан к **прогону сообщения** (не per-phrase) | `services/direct_chat_service.py:938–939` (`if image_pre_gate_fired: image_enabled=False`); прокид маркера `:967–968` (`image_request_handled=image_pre_gate_fired`); `services/tool_router.py:425,446` (поле ToolContext), `:1559` (`_generate_image`), `:1579–1590` (проверка маркера первым делом → `skipped/already_handled`) | `tests/test_unified_image_request_round1026.py`: `test_skipped_without_regeneration` (:236 — маркер → `skipped/already_handled`, `generate_and_send`/`run_image_request`/бюджет не вызываются), `test_loop_double_trigger_is_one_generation` (:270 — adversarial: полный `chat_with_tools`, модель вызывает `generate_image` при маркере → **ровно одна** генерация, tool-роль содержит skipped, A2-envelope видит `data.status=="skipped"`); `test_marker_off_legacy` (:256 — при OFF-киль-свитче маркер не проверяется, поведение baseline). PASS (evidence §5) |
| **T2** | **Регресс рабочего прямого вызова** (Critical `Risk-a3-direct-path-regression`): канонический запрос «Бот, нарисуй красного кота» начинает работать иначе (другой промпт/отправка/бюджет) | Прямой путь ON: `maybe_handle_keyword` строит `ImageRequest(source="direct")`, но `build_final_prompt` **переиспользует** существующий `extract_prompt` → промпт **байт-в-байт** прежний («Бот, нарисуй красного кота» → «красного кота»); раннер вызывает тот же `generate_and_send` с теми же параметрами и тем же бюджетом `image_calls`. OFF-киль-свитч: legacy-вызов **байт-в-байт прежний**, без `ImageRequest` вообще | `services/image_generation.py:1047` (`maybe_handle_keyword`), ON-ветка `:1063–1068` (`unified_image_request_enabled()`), OFF-ветка `:1073+`; `:227` (`build_image_request`), `:246` (`build_final_prompt` → reuse `extract_prompt:163`), `:256` (`run_image_request`) | `test_direct_on_same_prompt_as_legacy` (:142 — ON даёт **байт-в-байт тот же вызов генератора**, что и OFF/legacy), `test_direct_off_no_image_request` (:170 — OFF без ImageRequest, прямой вызов baseline), `test_build_final_prompt_reuses_extract_prompt` (:85 — байт-в-байт прежний промпт), `test_run_image_request_calls_existing_generator` (:92). PASS |
| **T3** | **Второй image pipeline / второй генератор** (High `Risk-a3-second-image-pipeline`): tool-путь получает собственную подготовку/генерацию, расхождение §18 воспроизводится в новом месте | **Единый раннер `run_image_request`**: обе ветки (direct ON и tool ON) строят один `ImageRequest` и идут в одну точку → **существующий** `generate_and_send` (генератор не переписан, §104 — AST-эквивалентность, T10). Нового модуля-контракта нет (дом — `services/image_generation.py`, общий для обоих входов); `generator_config` не может менять модель/провайдер/ключи/параметры §104 | `services/image_generation.py:256` (`run_image_request` — единственный раннер), `:202` (dataclass `ImageRequest`), `:227` (`build_image_request`); tool-ветка: `services/tool_router.py:1596–1607` (ImageRequest-ветка), legacy `:1608+`; обе сходятся в `generate_and_send:1015` (не тронут) | `test_run_image_request_calls_existing_generator` (:92 — раннер вызывает **существующий** `generate_and_send` с прежними параметрами), `TestToolPath` (tool ON — единый раннер; OFF — `test_tool_off_direct_call:215`), AST-гейт `test_104_generator_functions_ast_identical` (:560, сравнение с AST `git show e8646af`). PASS |
| **T4** | **Подмена реальной ошибки генератора вымышленным «отказом модели»** (High `Risk-a3-fake-model-refusal`): вместо честного `reason` модели/пользователю сообщается выдуманный синтетический отказ | Реальный `reason`/`reason_class` генератора **доводится** по всей цепочке: логи классифицируют реальную причину; при провале tool-путь возвращает `status:"error", reason:…`, текст-сообщение нейтрально-пользовательское (`IMAGE_GENERATION_FALLBACK_PHRASE`) и **не** содержит утверждений об «отказе модели»; ошибка входит в цикл как `role:"tool"` JSON через reuse A2-envelope (`ctx.tool_results`, `metered=True`, `error_code=reason`). Для недоступных из билд-окружения прод-проб — **честный `INCONCLUSIVE`** (HY-01/02/04/05) с блокером + планом проб на **owner-гейте**; внешняя причина не объявляется без живой пробы | Логи генератора: `services/image_generation.py:890` (`[image] generation failed … reason=%s`), `:993` (`[image] attempt failed … reason_class=%s`); fallback-фраза `:86–87` (`IMAGE_GENERATION_FALLBACK_PHRASE`); tool-путь `_generate_image:1559` (возврат реального reason без подмены); вердикты/HY — `image-diagnostics.md` §1/§2, `evidence.md` §1/§7, ADR-1026-16 D1 AMEND (owner-гейт `PENDING OWNER VERIFICATION`) | `test_generator_reason_is_surfaced` (:343 — реальный `reason` (`timeout`/`network`) доводится, сообщение равно fallback-фразе и **не содержит «модель отказалась»**, ошибка в цикле `role:"tool"`, envelope reuse). Документационное доказательство: `image-diagnostics.md` §1.1–§1.3 (проба HY-01: 10/10 `LLMServerError`, контроль без tools **тоже 500** ⇒ «внешний отказ прод-провайдера» НЕ доказан и не выдавался за установленный факт). PASS |
| **T5** | **Фикс/решение на недоказанной причине** (Critical `Risk-a3-unproven-root-cause`): механизм «лечит» нераскрытую гипотезу (HY-01/02/04/05 = `INCONCLUSIVE`, прод-пробы физически недоступны) | Дисциплина D1: **фикс только по доказанной причине**. Реализованное = (a) унификация §18/§20 — **безусловное требование ТЗ** (не следствие причины); (b) **страховки, не зависящие от причины**: маркер идемпотентности D3 (T1), fail-closed-валидация аргументов + доведение реального `reason` + возврат в цикл (T4, T8), текст описания D4 как **требование §21 (6 пунктов)**, а не «доказанный фикс». Ветка «внешняя причина → остановка/смена провайдера» **не активирована**: ни один факт не указывает на отказ прод-провайдера (известный сбой — env-эндпоинт билдера, и с tools, и БЕЗ). KG-риск закрыт письменно | Вердикт-факты: `image-diagnostics.md` §1.1 (HY-01: env-эндпоинт `apinet.cloud` HTTP 500 `get_channel_failed` на `deepseek-v4-flash` — и с tools, и БЕЗ; fallback-ключ 401), §1.2 (HY-04: механизм `tool_loop.py:291` — код-факт, прод-логов нет), §1.3 (HY-02), §1.4 (HY-03: env-слой все ON; per-chat — PG недоступен), §1.6 (HY-05: A/B не дала результат); план 4 прод-проб владельцу — `image-diagnostics.md` §1.1/§2 + ADR-1026-16 D1 AMEND | Документационное: `image-diagnostics.md` §2 (сводная таблица вердиктов: HY-06 — механика подтверждена кодом `IMAGE_KEYWORD_RE` `image_generation.py:93–101`; HY-03 env-слой опровергнут чтением `.env`+settings 24.09.2026; HY-01/02/04/05 — `INCONCLUSIVE` с блокером и планом); `evidence.md` §1/§7 (AMEND-черновик D1). Кодовое подтверждение «фикс не опирается на гипотезу»: все страховки покрыты тестами независимо от причины — T1 (маркер), T4/T8 (reason/валидация/цикл), T7 (описание). PASS |
| **T6** | **R17-утечка** (секреты/сырьё в логах/пробах): сырые промпты, URL, ключи, тела ответов попадают в логи/эвиденс | В логах отказа — **только числа/коды**: `mode/model/reason`, `attempt=%d/%d`, `reason_class`, id; промпт/текст/URL/ключи/аргументы не логируются; пробы — скрипт вне рабочего дерева, **ключ не печатается** (`image-diagnostics.md` §0/§1.1); в tool-результате только структурированные `status/message/reason` | `services/image_generation.py:890` (формат лога — только поля-коды), `:993` (`reason_class=%s` — класс, не содержимое); `services/tool_router.py` `_generate_image:1559` (JSON без сырья); гейт `unified_image_request_enabled:190` — без логирования; тест-класс `TestR17Logs` (`tests/test_unified_image_request_round1026.py:474–476`) | `test_failure_logs_are_codes_only` (:476 — в логе отказа нет промпта, `http`, `Bearer`, только `reason=…`). Проба HY-01 задокументирована без секрета («ключ присутствует, не печатается» — `image-diagnostics.md` §1.4). PASS |
| **T7** | **Дублирование контрактов/инструментов** (второй роутер / новый канон / дубль `TOOL_GENERATE_IMAGE` / добавление `PREV_*`-листов промптов) | Канон инструментов **11 без изменений** (`TOOL_CALLING_TOOLS` — состав/имена/порядок не менялись; `generate_image` — 9-й, `prompt` required, `additionalProperties:false`); новых аргументов/инструментов нет; `factcheck_tools` = 3 без изменений; заменены **только тексты описаний** (санкция D4) — два варианта: `*_LEGACY` байт-в-байт baseline-текст, `*_V21` — 6 пунктов §21; активный — по env-only киль-свитчу; A2-envelope **reuse** (второй контракт возврата не создаётся); ADR-1013-3 NOT_APPLICABLE (нет `PREV_*`/`PROMPT_MIGRATIONS`) | `services/tool_schemas.py:329` (`TOOL_GENERATE_IMAGE_LEGACY` — байт-в-байт baseline), `:350` (`TOOL_GENERATE_IMAGE_V21`), `:389–393` (выбор по киль-свитчу, default ON), `:427` (`TOOL_CALLING_TOOLS` — канон 11); envelope — `services/tool_router.py:445/456` (A2, reuse); киль-свитч `config/settings.py:899–904` (env-only `ClassVar`, вне каталога) | `test_canon_unchanged` (:393 — канон 11, имена/порядок/`required`/factcheck неизменны), `test_schema_structure_identical_in_variants` (:399 — структурная идентичность вариантов), `test_v21_covers_six_s21_points` (:409), `test_legacy_text_is_baseline` (:424), `test_default_is_refined` (:432), `test_kill_switch_off_restores_legacy_via_env` (:439), `test_active_tools_with_kill_switch_off_effective` (:460), `test_kill_switch_env_only_not_in_catalog` (:625 — Δ каталога=0). PASS |
| **T8** | **Ошибка/битые аргументы → исключение в цикле tool-calls** (пустой/невалидный `prompt` / модуль OFF): исключение убивает `chat_with_tools` | **Fail-closed**: `_generate_image` валидирует (`_require_str("prompt")` → при отсутствии `status:"error"` без генерации); module OFF → `status:"error"`; результат классифицируется A2-механизмом в envelope `error` — цикл продолжается, исключений нет; прежние исходы (пустой блок / `status:error`) сохранены | `services/tool_router.py:1559+` (`_generate_image`: гейт модуля → валидация → fail-closed), A2-классификация `_classify_output` → `tool_results:445`/`result_for:456` (reuse, не дубль) | `test_missing_prompt_fail_closed` (:310 — `{}` → `status:"error"` **без генерации**; envelope `status=error`; цикл не падает); `test_module_off_error_and_no_generation`; `TestDirectPath` OFF-варианты. PASS |
| **T9** | **Регресс 2-вызовности System 2** (`await_count==2`): добавление image-механизма ломает координаторный контур (3-й LLM-вызов / сбитое условие `tool_trace`) | A3 не добавляет ни одного LLM-вызова: раннер — обвязка **существующего** генератора (image-модель ≠ `generate_chat`); в `image_generation.py` нет `generate_chat`-вызовов (статический гейт), `tool_router` не добавлял вызовов; условия System 2 (`tool_trace`, `lore_compiled`) не менялись; полный набор A2/A1-тестов жив | `services/image_generation.py` (нет вызовов генерации LLM — статический контроль), `services/direct_chat_service.py` (исполнимая логика — та же, лишь прокид маркера `:967–968`) | `test_two_llm_calls_intact` (`tests/test_unified_image_request_round1026.py:641` — статическая проверка запрета новых LLM-вызовов); полная suite **9156/0**, включая живые `await_count==2`-тесты координатора (`tests/test_tool_coordinator_round1026.py`) и `tests/test_direct_two_call_round1022.py`; `evidence.md` §4 (`await_count==2` сохранён). PASS |
| **T10** | **Инвалидация внешних bound-гейтов**: 4 исторических гейта (запрет `image_generation.py` как §104-файла) под A3 сняли запрет → без компенсации остаётся окно для мутации генератора вне волны | Компенсация: §104-защита перенесена в **жёсткий AST-гейт A3** — AST-эквивалентность против baseline `git show e8646af` для функций `generate` / `generate_and_send` / `generate_image` / `generate_image_verbose` / `extract_prompt` / `is_image_keyword` + литерал fallback-фразы; NOTE-исключение в 4 bound-гейтах прошлых волн — прецедент A2 (`bot.py`); смысл запретов сохранён, защитный гейт стал жёстче (уровень AST, не только список путей) | `tests/test_unified_image_request_round1026.py::TestBoundsA3` — AST-эквивалентность; 4 bound-теста с NOTE: `tests/test_tool_coordinator_round1026.py:617–638`, `tests/test_summary_deploy_round1026.py:493–512`, `tests/test_summary_execution_graph_round1026.py:435–452`, `tests/test_summary_publish_integration_round1026.py:1237–1250` | `test_104_generator_functions_ast_identical` (:560), `test_104_fallback_phrase_unchanged` (:575); 4 обновлённых bound-гейта — PASS (исключения только по NOTE-санкции A3; запреты остальных волн не нарушены). PASS |
| **T11** | **Недоступность живых прод-проб** (гейт §21 без вердиктов): `INCONCLUSIVE` превращается в «без последующих действий», или наоборот — в «внешнюю причину» без доказательства | Честная документация: каждая недоступная проба фиксирует **блокер** (что недоступно) + **план проб на owner-гейте** (точная команда + критерий + владелец); **«отказ модели»/«внешняя причина» не выдумываются** — активируются только по живой прод-пробе; приёмка не блокируется (пока реализованный механизм не опирается на недоказанную причину — T5) | `image-diagnostics.md` §0 (доступность) / §1.1 (конкретный сбой: `get_channel_failed`, и с tools и без; критерий `finish_reason=="tool_calls"`), §1.4 (per-chat: PG-каталог), §2 (план 4 проб владельцу); `evidence.md` §6 (честный список «что не проверено отсюда»); ADR-1026-16 D1 AMEND (owner-гейт `PENDING OWNER VERIFICATION`, HY-03 per-chat) | Документационный артефакт (присутствует, не пуст): вердикты по всем 6 HY — `image-diagnostics.md` §2 сводная таблица; `evidence.md` §1 таблица вердиктов. Единственный оставшийся пробел — **исполнение прод-проб владельцем** (за пределами полномочий @Builder; честно заявлено). PASS |

## 2. Adversarial-приёмка (результаты — матрица негативных сценариев)

Набор выполнен в `tests/test_unified_image_request_round1026.py` (31 тест) + regressed 4 bound-гейта
прошлых волн + живые координаторные тесты (2-вызовность). Полный прогон: **pytest .venv 9156 passed / 0
failed** (baseline 9125 verified A2 + 31 A3); **JS 47/47**; `git diff --check` exit=0. Детали —
`evidence.md` §4/§5.

| Сценарий | Ожидание | Тест | Результат |
|---|---|---|---|
| Двойной триггер через полный `chat_with_tools` (адверсариал: модель зовёт `generate_image` при маркере) | ровно одна генерация; tool-роль = skipped; envelope `data.status=="skipped"` | `test_loop_double_trigger_is_one_generation` | PASS |
| Повторный tool-вызов при `image_request_handled=True` | `skipped/already_handled`, генератор/бюджет не вызываются | `test_skipped_without_regeneration` | PASS |
| Канонический прямой запрос («Бот, нарисуй красного кота») | байт-в-байт прежний вызов генератора | `test_direct_on_same_prompt_as_legacy` | PASS |
| Киль-свитч OFF (subprocess env) | legacy-вызов без ImageRequest; LEGACY-описание байт-в-байт baseline | `test_direct_off_no_image_request`, `test_tool_off_direct_call`, `test_kill_switch_off_restores_legacy_via_env` | PASS |
| Пустой/битый prompt | fail-closed: `status:"error"`, генерации нет, цикл жив | `test_missing_prompt_fail_closed` | PASS |
| Реальная ошибка генератора | `reason` доводится, текст нейтральный, «модель отказалась» отсутствует | `test_generator_reason_is_surfaced` | PASS |
| Модуль OFF (env/per-chat) | ошибка без генерации; активный набор без `generate_image` | `test_module_off_error_and_no_generation`, `test_active_tools_with_kill_switch_off_effective` | PASS |
| Память ради факта (политика контекста) | поля-заглушки `context_required=False`/`context_sources=[]`; чтения памяти нет | `test_build_image_request_defaults` | PASS |
| R17-утечка | только коды в логах | `test_failure_logs_are_codes_only` | PASS |
| §104 drift | AST-эквивалентность vs `e8646af` | `test_104_generator_functions_ast_identical` | PASS |
| DDL/каталог/версия drift | запрещено | `test_no_ddl_in_touched_sources`, `test_catalog_counts_unchanged`, `test_kill_switch_env_only_not_in_catalog` | PASS |
| 3-й LLM-вызов | нет | `test_two_llm_calls_intact` + полная suite (координатор `await_count==2` жив) | PASS |

## 3. Rollback-эквивалентность (точка отката / hot-off)

- **Hot:** env `UNIFIED_IMAGE_REQUEST_ENABLED=false` (config/settings.py:897–904; env-only `ClassVar`) →
  (i) прямой путь — legacy-вызов `generate_and_send(extract_prompt(query))` байт-в-байт;
  (ii) tool-путь — прежний прямой вызов без `ImageRequest`; (iii) маркер прогона не проверяется;
  (iv) описание инструмента — LEGACY байт-в-байт baseline. Доказательство: `test_direct_on_same_prompt_as_legacy`,
  `test_direct_off_no_image_request`, `test_tool_off_direct_call`, `test_marker_off_legacy`,
  `test_legacy_text_is_baseline`.
- **Cold:** `git revert` к точке **`e8646af`** (пер-фича тег **не создавался** — `EPIC_ONLY`; агрегатный
  анкер/бэкапы — на границе эпика); R18: теги/бэкапы не тронуты. A3-изменения — 5 product-файлов с
  аддитивными ветками по киль-свитчу.

## 4. Diff-аудит (границы)

```
git diff --name-only e8646af
```

- **A3-only:** `services/image_generation.py`, `tests/test_unified_image_request_round1026.py` (новый),
  4 bound-гейта (NOTE), `evidence.md`/`image-diagnostics.md` (новые).
- **A3 поверх A2:** `services/tool_router.py`, `services/direct_chat_service.py` (прокид маркера),
  `services/tool_schemas.py` (только тексты описаний), `config/settings.py` (один env-only ClassVar).
- **Вне diff подтверждены:** `services/summary_prompts.py`, `services/prompt_migrations.py`,
  `services/param_catalog.py` (Δ каталога=0), `db/**` (Δ DDL=0), `web/**`, `web/api/routes.py`,
  `services/telegram_send.py`, `services/execution_graph_source.py` (REUSE, второй аналитики нет),
  публикационные модули. Незакоммиченный A2-кандидат сохранён (release-order — ADR D8).

## 5. Честные ограничения анализа

- Прод-пробы HY-01/HY-02/HY-04/HY-05 (и per-chat HY-03) **не выполнены** — физически недоступны из
  билд-окружения; **никакой** вывод о причине §21 отсюда не делается; исполнение — owner-гейт
  (`PENDING OWNER VERIFICATION`). Пока реализованный механизм не опирается на недоказанную причину (T5),
  R3-приёмка допустима; при появлении живых прод-логов — повторная сверка T4/T5.
- «Байт-в-байт» OFF-эквивалентность доказана между ветвями одного кода + AST-сравнением против baseline
  `e8646af`; отдельный рантайм-прогон baseline-кода в этой сессии не выполнялся.
