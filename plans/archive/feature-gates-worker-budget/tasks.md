# Задачи: feature-gates-worker-budget

Раунд 10 (07.09.2026), **часть 2.3 + 2.4 эпика** «Multi-chat scaling» (research `plans/docs/multi-chat-scaling-research.md`, вариант A). Нумерация продолжает T-889 → **T-890…T-902**; база HEAD fac1b9f. Скоуп = **Feature Gates + Глобальный Worker-бюджет**: (2.3) новые чаты при добавлении бота (my_chat_member) получают «твёрдый» профиль с жёстким Opt-In — тяжёлые фичи (Сон/Dream, Ностальгия, авто-лор) по умолчанию OFF, включение — ЯВНОЕ (локальный админ/глобальный), первые действия фичи → автоматический opt-in-следок; (2.4) суточный ГЛОБАЛЬНЫЙ бюджет фоновых воркеров на уровне БД (расширение концепции `memory_dream_log.tokens` — уже есть kind/tokens-бюджет) в единый ledger: лимиты LLM-вызовов/токенов день, приоритеты-деградация, счётчики per chat/global, stop при превышении. Spec.md при планировании НЕ создаётся; A1 (Q1–Q6) закрывает @Architect правкой в разделе A. Связанные части: `multi-chat-rbac-byok` (T-843…T-869) — доступы и chat_params-слой, `permsoc-module-isolation` (T-878…T-889) — master-флаг PERMsoc ТУТ не «по умолчанию» включать (задача-конфликт: эта фича отвечает за gates всех тяжёлых фич, PERMsoc — только свои 5 триггеров), `tma-ia-progressive-disclosure` (T-903…T-913), `global-oversight-dashboard` (T-914…T-924).

## Контекст (Step 0; факты проверены @PM при планировании по HEAD fac1b9f)

**Текущее состояние флагов (MEMORY.md «Тумблеры персоналий»):** сон/ностальгия/авто-лор — «включены по умолчанию» (глобальные флаги `flags.dream_enabled`, `flags.nostalgia_enabled`, `flags.lore_auto_enabled`; part-2.3 эпоса переводит на жёсткий Opt-In: дефолты флагов — OFF+per-chat gates; dig/relations — уже OFF по наличию флаг-настроек). `memory_dream_log` — SQLite: kind/tokens-бюджет (суточный по local-часам), `dream_state` (PK chat_id, watermark), `nostalgia_log`; бюджет-день производится внутри воркеров, общего суточного БД-ледежера нет. Воркеры: LoreWorker (PG, автолор-merge), DreamWorker (SQLite, дистилляция-кластер окно 4–6, `dream_max_chats_per_run=10`), NostalgiaWorker (SQLite, тишина≥45 мин/hours 23–8/cooldown 12ч/max_per_day=3). Lifecycle: `handlers/chat_lifecycle.py` (my_chat_member → ensure_profile/is_active; migrate_to_chat_id → remap) — точка добавления новых фич-гейтов. SQLite-схема — только аддитивные CREATE IF NOT EXISTS (прецедент users_meta/memory_dream_log раунда 9); ALTER/rebuild существующих табу.

## A. Дизайн (PM-база; Q1–Q6 закрывает @Architect в T-890)

- [ ] **T-890 (A1):** @Architect — протокол дизайна: (Q1) **модель Opt-In**: `chat_profiles.gates_opt_in BOOLEAN NOT NULL DEFAULT FALSE` (PM-база, аддитивная колонка) vs per-feature gates JSONB (`feature_gates JSONB NOT NULL DEFAULT '{}'` — research §4.2: ключи direct_chat/memory/dream/nostalgia/lore_auto/relations); какая модель проще и стройнее с part-1 (chat_params-слой: gates-ключи тоже в chat_params? единообразие: part-1 chat_params это «параметры», а gates-«тяжёлые» — флаги-чипы, отдельные столбцы + собственные приоритеты в chat_params), как согласовать с permsoc master (отдельный вопрос: PERMsoc-фича = не тяжёлая — 2.1 без Opt-In?); (Q2) **«тяжёлые фичи» — точный список**: dream (Сон), nostalgia (Ностальгия), lore_auto (авто-лор), relations (инжект отношений)?, dig_into_lore? — (PM-база: 3: dream/nostalgia/lore_auto; relations/dig не тяжёлые (имя-не гейтится) по опыту раунда 9: сenos; (Q3) **включение**: что делает «явное включение»: локальный админ (часть-1: local_admin роль) ИЛИ глобальный admin; первый включённый тяжёлый feature → `gates_opt_in=true` (флаг-участие); поведение при Opt-In OFF: воркеры-скипы всех 3-х НО бот продолжает отвечать (direct) — ровно по ТЗ «не отвечает пока не активирован» не нужно? (research: бот молчит, пока чат не активирован, кроме /start-подсказки — PM-база: малый вариант: direct/ответы работают (больше UX: юзер видит бота), тяжёлые — нет; решение Q3); (Q4) **бюджет-ледеджер**: новая PG-таблица `worker_budget (day DATE, scope TEXT ('global'|'chat:<id>'), metric TEXT ('llm_calls'|'llm_tokens'), used BIGINT, PRIMARY KEY (day, scope, metric))` + SQLite? — единственный источник правды: PG (PM-база) — SQLite-воркеры (сон/ностальгия) пишут через PgDatabase (уже есть pg-instance в runtime: прецедент chat_lore-запись) — риск: PG down при тике → fail-open скp (не останавливаться) — решение; лимиты REGISTRY `limits.worker_daily_*` token/вызовы global и per-chat; деградация по приоритетам (ностальгия → лор → сон; P2-лейн research), jitter-тики (размазка); (Q5) **интеграция**: где именно консюм (DreamWorker дистилляция, NostalgiaWorker генерация, LoreWorker merge-вызов; крон/саммари — вне?), наследование `memory_dream_log.tokens` (старый аудит оставить как есть — не ломать; ледежер — надстройка, миграция-старые-строки НЕ требуются), счётчики edge (день-в-торset?); (Q6) **инвентарь**: точки: ensure_profile/chat_lifecycle, существующие флаги 3-х воркеров в REGISTRY/они уже в `flags.*`, `bot.py on_startup` DI, TMA-вкладки (memory_rag — сон/ностальгия; лор-вкладка — авто-лор), тесты (dream/nostalgia/lore — моки), README. Изменения задач — пометками `(изменено @Architect T-890)`.

## B. Gates: база + сервис

- [x] **T-891 (B1):** DDL/PG: `chat_profiles` += `gates_opt_in BOOLEAN NOT NULL DEFAULT FALSE` (ADD COLUMN IF NOT EXISTS — прецедент relations T-818); (по Q1 при модели JSONB — `feature_gates JSONB NOT NULL DEFAULT '{}'`); `worker_budget` — CREATE TABLE IF NOT EXISTS в `services/pg_db.py` DDL_STATEMENTS (PK(day, scope, metric), дефолты-лимиты в REGISTRY не в таблице); идемпотентность двойного init(); существующие таблицы/сиды не тронуты. (services/pg_db.py)
- [x] **T-892 (B2):** `services/feature_gates.py` (НОВЫЙ): `gates_enabled(chat_id, feature) -> bool` (приоритет: chat-гейт-чипы → глобальные флаги (hot.get) → дефолт (по Q2/3: тяжёлые OFF по умолчанию)); `set_feature_gate(chat_id, feature, enabled, changed_by)` (запись в chat_params/колонку по Q1; 409-паттерн — часть-1), `has_opt_in(chat_id)`, `auto_opt_in(chat_id)` (первое включение тяжёлой → gates_opt_in=true + история field='gates' — по Q3), `allowed_features(chat_id) -> set`; кэш per-chat 60-120 с/TTL (прецедент ChatLoreCache) + fail-open (PG down → глобальный дефолт=OFF — безопасно). (services/feature_gates.py — НОВЫЙ; services/chat_params.py — слой overrides)

## C. Lifecycle + воркеры

- [x] **T-893 (C1):** `handlers/chat_lifecycle.py`: при `ensure_profile` (my_chat_member — прецедент T-777): новый чат → `gates_opt_in=false` + записи-гейт-дефолты (тяжёлые OFF), существующие → не менять (миграция-ожидание: правило Q3/бэкфил-скрипт T-890-e); пул-опций: для активных чатов: backfill-скрипт `scripts/backfill_feature_gates.py` — по Q3 (яри-чаты с включёнными флагами → opt-in=true; ровная детерминированная логика; идемпотентно; stdout; при деплое @DevOps). (handlers/chat_lifecycle.py, scripts/backfill_feature_gates.py — НОВЫЙ)
- [x] **T-894 (C2):** гейт в воркерах (после part-1/до — независимо): LoreWorker (`generate_for_chat`: skip если `gates_enabled(chat_id,'lore_auto')` false — WARNING-лог «skip: gate lore_auto»), DreamWorker (тик/дистилляция: gate 'dream'), NostalgiaWorker (тик: gate 'nostalgia'); при Opt-IN false у чата — не проходить; существующие флаги-гейты (`flags.dream_enabled` и др.) — остаются как глобальные дефолты (по Q2); согласование с частью-1 (gates-контроль per-chat-админом — по части-1 RBAC). (services/lore_worker.py, services/dream_worker.py, services/nostalgia_worker.py)
- [x] **T-895 (C3):** `services/worker_budget.py` (НОВЫЙ): `consume(scope, metric, amount=1) -> bool` (атомарно: участок день (UTC/local — Q4: local-день UTC+5), INSERT/UPDATE ... pg_database, при превышении лимита → False+лог), `get_usage(scope=None)`, интерфейс лимитов REGISTRY (см. T-897): `worker_daily_llm_calls_global`, `worker_daily_llm_tokens_global`, per-chat (`worker_daily_*_per_chat`), порог-«серьёзность» через приоритеты: `budget_budget_exhausted(priority)` для деградации (PM-база: ностальгия 0, лор 1, сон 2 — останавливаются после отката); день-менеджмент (GET/INSERT — начало дня: первый вызов создаёт строку с 0; noop-выделение пустых строк). (services/worker_budget.py — НОВЫЙ, services/pg_db.py — repository-методы)

## D. REGISTRY + интеграция + API

- [x] **T-896 (D1):** REGISTRY-записи (services/param_catalog.py, config/settings.py, .env.example — порядок/группы по образцу T-755): `limits.worker_daily_llm_calls_global` (int, по Q4 — PM-база 200), `limits.worker_daily_llm_tokens_global` (int, 500000), `limits.worker_daily_llm_calls_per_chat` (35), `limits.worker_daily_llm_tokens_per_chat` (100000), `limits.worker_priority_order` (строка-имям — по Q4), `limits.worker_budget_jitter` (int мин. — размазка тиков, 5), группы limits_worker; обновление `known_sections`/валidaций не сломать. (services/param_catalog.py, config/settings.py, .env.example)
- [x] **T-897 (D2):** интеграция consuma в трёх воркерах: DreamWorker (до LLM-дистилляции: consume(global, 'llm_calls', 1) + tokens — из estimate; при False → skip с аудитом (memory_dream_log status='budget_skip') — прецедент); NostalgiaWorker (вызов слоя B: consume; при False → nostalgia_log status='budget_skip'); LoreWorker (merge-вызов: consume; скип — история не пишется, WARNING); приоритетный порядок — по Q4 (деградация: сначала падает сон, потом лор, последняя ностальгия — если общий бюджет улетает); jitter-тики (интервал + random [0, jitter_min]) в тик-планировке (не ломать интерактив); DI: создание в bot.py on_startup (зона memory_maintenance/lore_worker; порядок НЕ менять) + передача в воркеры. (services/dream_worker.py, services/nostalgia_worker.py, services/lore_worker.py, bot.py)
- [x] **T-898 (D3):** API: `GET /api/chat/{chat_id}/gates` (состояние: opt_in, 3 фичи, who_can_toggle — по RBAC части-1), `PUT /api/chat/{chat_id}/gates` {feature, enabled} (global admin/local admin — по Q5; optimistic 409 — паттерн chat_lore; hidden = чужой чат 403), `GET /api/workers/budget` (день, global/per-chat usage/лимиты, приоритеты; права — 403 для не-admin, 200 global+mylocal? по Q4); модуль web/api (NOВЫЙ) include в web/app.py (прецедент chat_lore API). (web/api/*, web/app.py)

## E. TMA

- [x] **T-899 (E1):** блок «Модули и Фичи» (по временной структуре: до tma-ia — вкладка «Реакции и Триггеры»/«Лор чатов» (авто-лор-кнопка — на месте)): карточка «Тяжёлые фичи»: три чипа (Сон/Ностальгия/Авто-лор): статусы (ON/OFF) — бейджи, кнопки переключения (по RBAC: глобальный admin — toggle, local admin — вопрос разрешения из part-1; по умолчанию — отображать read-only таблицу) + строка «Opt-In: включено/выключено» (associate для юзера — explanation); `GET/PUT gates`-методы (тосты, 401/403-редирект как соседи). (web/app.js, web/index.html)
- [x] **T-900 (E2):** блок «Бюджет фона» (там же): прогресс-бары день/глобал + per-chat (usage/limit), список приоритетов запуска/ожидании — данные GET /api/workers/budget; для владельца — кнопка-обновление (интервал ничего) + рейт-лимиты? — нет, только view (без изменения лимитов из TMA — лимиты через config-раздел limits). (web/app.js, web/index.html)

## F. Тесты

- [x] **T-901 (F1):** gates/budget-тесты: gates: новых чат opt-in=false (lifecycle-фикстура), тяжёлые OFF, воркер-скип (3-х воркеров: мок — gate off → не вызывается), `auto_opt_in` при включении; budget: консюм-мал (лимит 10 → 11-й False), новые день — авто-сброс (эмуляция), per-chat/global независимы, деградация по приоритетам (порог: global скор → сон skip-первым), jitter-функция (диапазон), fail-open (PG down → не падает — по Q4); API: 401/403/404/422/409 + матрица прав (global/local/mod/user; и какой-либо local-нa-чужой чат 403); существующие тесты dream/nostalgia/lore — обновить моки под gates (по инвентарю Q6); полный pytest → 0 failed; `git diff --check`; RUNTIME WARNING (SQLite-схема — только аддитивные CREATE IF NOT EXISTS worker_budget-НЕ относится (PG); memory_dream_log/memory-таблицы не трогаем). (tests/test_feature_gates.py, tests/test_worker_budget.py — НОВЫЕ; tests-обновления существующих)

## G. Деплой/README/отчёт

- [ ] **T-902 (G1):** README-раздел «Масштабирование: фичи-гейты и бюджет фона» (Opt-In: что тяжёлое, как включить; глобальный/пер-чат бюджеты, приоритеты, деградация; новые ключи limits.worker_* + флаги; backfill-скрипт, деплой-шаги: бэкап, рестарт, live-гейт-верификация: новый чат — бот приветствует, но не «включает» сон (проверить по логам: skip-строки); для существующих чатов — гейт-значение соответствия правил); memory-sync коммит. (README.md, plans/) *(@DevOps финальный шаг — вне архива)*
